<templates xml:space="preserve">
    <t t-name="apex_dashboard.Dashboard" owl="1">
        <div class="vh-100 overflow-auto bg-muted p-4">
            <div class="row mb-4 align-items-center justify-content-between bg-white shadow-sm p-4 mx-0 rounded">
                <div class="col-md-4">
                    <h1 class="text-primary fw-bold mb-0">APEX Dashboard</h1>
                </div>
                <div class="col-md-4">
                    <select class="form-select" t-model="state.period" t-on-change="onChangePeriod">
                        <option value="0">Select Period</option>
                        <option value="7">Last 7 Days</option>
                        <option value="30">Last 30 Days</option>
                        <option value="90">Last 90 Days</option>
                        <option value="365">Last Year</option>
                    </select>
                </div>
                <div class="col-md-4">
                    <select class="form-select" t-model="state.selectedStudent" t-on-change="onChangeStudent">
                        <option value="false">All Students</option>
                        <t t-foreach="state.students" t-as="student" t-key="student.id">
                            <option t-att-value="student.id" t-esc="student.name"/>
                        </t>
                    </select>
                </div>
            </div>

            <div class="row mb-4">
                <KpiCard name="'Total Tasks'" value="state.submissions.value" 
                         percentage="state.submissions.percentage" icon="'fa-file-text'" 
                         onClick="() => this.viewSubmissions()"/>
                <KpiCard name="'Active Tasks'" value="state.tasks.value" 
                         percentage="state.tasks.percentage" icon="'fa-tasks'" />
                <KpiCard name="'Overdue Items'" value="state.overdue.value" 
                         percentage="state.overdue.percentage" icon="'fa-clock-o'" />
                <KpiCard name="'New Resources'" value="state.resources_new.value" 
                         percentage="state.resources_new.percentage" icon="'fa-cubes'" />
            </div>

            <div class="row">
                <div class="col-lg-6 mb-4">
                    <div class="bg-white shadow-sm p-4 rounded h-100">
                        <h3 class="fw-bold mb-3">Submission Trends</h3>
                        <ChartRenderer type="'bar'" title="'Submissions by Status'" data="state.chartData" />
                    </div>
                </div>
                <div class="col-lg-6 mb-4">
                    <div class="bg-white shadow-sm p-4 rounded h-100">
                        <h3 class="fw-bold mb-3">Tasks Assigned by Subject</h3>
                        <ChartRenderer type="'doughnut'" title="'Tasks Assigned by Subject'" data="state.doughnutData" />
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-lg-6 mb-4">
                    <div class="bg-white shadow-sm p-4 rounded h-100">
                        <h3 class="fw-bold mb-3">Submission Trends</h3>
                        <ChartRenderer type="'bar'" title="" data="state.chartData" />
                    </div>
                </div>
                <div class="col-lg-6 mb-4">
                    <div class="bg-white shadow-sm p-4 rounded h-100">
                        <h3 class="fw-bold mb-3">Tasks Assigned by Subject + Due Status</h3>
                        <ChartRenderer type="'pie'" title="" data="state.doughnutData2" />
                    </div>
                </div>
            </div>
        </div>
    </t>
</templates>