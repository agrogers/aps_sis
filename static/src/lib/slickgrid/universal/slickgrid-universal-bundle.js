var Slicker = (() => {
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __export = (target, all) => {
    for (var name in all)
      __defProp(target, name, { get: all[name], enumerable: true });
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

  // node_modules/@slickgrid-universal/vanilla-bundle/dist/index.js
  var index_exports = {};
  __export(index_exports, {
    Aggregators: () => Aggregators,
    BindingService: () => BindingService,
    Editors: () => Editors,
    Enums: () => enums_exports,
    EventPubSubService: () => EventPubSubService,
    Filters: () => Filters,
    Formatters: () => Formatters,
    GroupTotalFormatters: () => GroupTotalFormatters,
    SlickEmptyWarningComponent: () => SlickEmptyWarningComponent,
    SlickPaginationComponent: () => SlickPaginationComponent,
    SlickVanillaGridBundle: () => SlickVanillaGridBundle,
    Slicker: () => Slicker,
    SortComparers: () => SortComparers,
    UniversalContainerService: () => UniversalContainerService,
    Utilities: () => Utilities
  });

  // node_modules/@slickgrid-universal/binding/dist/binding.service.js
  var BindingService = class {
    constructor(binding) {
      this._value = null;
      this._boundedEventWithListeners = [];
      this._elementBindings = [];
      this._binding = binding;
      this._property = binding.property || "";
      this._elementBindings = [];
      if (binding.property && binding.variable && (binding.variable.hasOwnProperty(binding.property) || binding.property in binding.variable)) {
        this._value = binding.variable[binding.property];
      } else {
        this._value = binding.variable;
      }
      if (typeof binding.variable === "object") {
        Object.defineProperty(binding.variable, binding.property, {
          get: this.valueGetter.bind(this),
          set: this.valueSetter.bind(this)
        });
      }
    }
    get boundedEventWithListeners() {
      return this._boundedEventWithListeners;
    }
    get elementBindings() {
      return this._elementBindings;
    }
    get property() {
      return this._property;
    }
    dispose() {
      this.unbindAll();
      this._boundedEventWithListeners = [];
      this._elementBindings = [];
    }
    valueGetter() {
      return this._value;
    }
    valueSetter(val) {
      this._value = val;
      if (Array.isArray(this._elementBindings)) {
        for (const binding of this._elementBindings) {
          if (binding?.element && binding?.attribute) {
            binding.element[binding.attribute] = val;
          }
        }
      }
    }
    /**
     * Add binding to 1 or more DOM Element by an object attribute and optionally on an event, we can do it in couple ways
     * 1- if there's no event provided, it will simply replace the DOM elemnt (by an attribute), for example an innerHTML
     * 2- when an event is provided, we will replace the DOM element (by an attribute) every time an event is triggered
     *    2.1- we could also provide an extra callback method to execute when the event gets triggered
     */
    bind(elements, attribute, eventName, eventCallback) {
      if (elements && elements.forEach) {
        elements.forEach((elm) => this.bindSingleElement(elm, attribute, eventName, eventCallback));
      } else if (elements) {
        this.bindSingleElement(elements, attribute, eventName, eventCallback);
      }
      return this;
    }
    /** Unbind (remove) an element event listener */
    unbind(element, eventName, listener, options, eventUid) {
      if (element) {
        element.removeEventListener(eventName, listener, options);
        const eventIdx = this._boundedEventWithListeners.findIndex((be) => be.uid === eventUid);
        if (eventIdx >= 0) {
          this._boundedEventWithListeners.splice(eventIdx, 1);
        }
      }
    }
    /** Unbind All (remove) bounded elements with listeners */
    unbindAll() {
      let boundedEvent = this._boundedEventWithListeners.pop();
      while (boundedEvent) {
        const { element, eventName, listener, uid } = boundedEvent;
        this.unbind(element, eventName, listener, void 0, uid);
        boundedEvent = this._boundedEventWithListeners.pop();
      }
    }
    /**
     * Add binding to a single element by an object attribute and optionally on an event, we can do it in couple ways
     * 1- if there's no event provided, it will simply replace the DOM element (by an attribute), for example an innerHTML
     * 2- when an event is provided, we will replace the DOM element (by an attribute) every time an event is triggered
     *    2.1- we could also provide an extra callback method to execute when the event gets triggered
     */
    bindSingleElement(element, attribute, eventName, eventCallback) {
      const binding = { element, attribute };
      if (element) {
        if (eventName) {
          const listener = () => {
            let elmValue = element[attribute];
            if (this.hasData(elmValue) && element?.type === "number") {
              elmValue = +elmValue;
            }
            this.valueSetter(elmValue);
            if (this._binding.variable.hasOwnProperty(this._binding.property) || this._binding.property in this._binding.variable) {
              this._binding.variable[this._binding.property] = this.valueGetter();
            }
            if (typeof eventCallback === "function") {
              return eventCallback(this.valueGetter());
            }
          };
          binding.event = eventName;
          binding.listener = listener;
          element.addEventListener(eventName, listener);
          this._boundedEventWithListeners.push({ element, eventName, listener, uid: this.generateUuidV4() });
        }
        this._elementBindings.push(binding);
        element[attribute] = this._value;
      }
    }
    /** Generate a UUID version 4 RFC compliant */
    generateUuidV4() {
      return "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, (c) => {
        const r2 = Math.random() * 16 | 0;
        const v2 = c === "x" ? r2 : r2 & 3 | 8;
        return v2.toString(16);
      });
    }
    hasData(value) {
      return value !== void 0 && value !== null && value !== "";
    }
  };

  // node_modules/@slickgrid-universal/binding/dist/binding.helper.js
  var BindingHelper = class {
    constructor() {
      this._observers = [];
      this._querySelectorPrefix = "";
    }
    get querySelectorPrefix() {
      return this._querySelectorPrefix || "";
    }
    set querySelectorPrefix(prefix) {
      this._querySelectorPrefix = prefix;
    }
    get observers() {
      return this._observers;
    }
    dispose() {
      let observer = this._observers.pop();
      while (observer) {
        observer.dispose();
        observer = this._observers.pop();
      }
    }
    addElementBinding(variable, property, selector, attribute, events, callback) {
      const elements = document.querySelectorAll(`${this.querySelectorPrefix}${selector}`);
      const observer = this._observers.find((bind) => bind.property === variable) || new BindingService({ variable, property });
      if (Array.isArray(events)) {
        events.forEach((eventName) => observer?.bind(elements, attribute, eventName, callback));
      } else {
        observer?.bind(elements, attribute, events, callback);
      }
      this._observers.push(observer);
    }
    /** From a DOM element selector, which could be zero or multiple elements, add an event listener   */
    bindEventHandler(selector, eventName, callback, options) {
      document.querySelectorAll(`${this.querySelectorPrefix}${selector}`).forEach((elm) => {
        elm?.addEventListener(eventName, callback, options);
      });
    }
    /**
     * From a DOM element selector, which could be zero or multiple elements, set the value on a given attribute name
     * For example ('div.hello', 'textContent', 'world') => would set the textContent equal to 'world' on a div element having the class 'hello'
     */
    setElementAttributeValue(selector, attribute, value) {
      const elements = document.querySelectorAll(`${this.querySelectorPrefix}${selector}`);
      elements.forEach((elm) => {
        elm.textContent = "";
        if (elm && attribute in elm) {
          elm[attribute] = value;
        }
      });
    }
  };

  // node_modules/@slickgrid-universal/binding/dist/bindingEvent.service.js
  var BindingEventService = class {
    constructor() {
      this._boundedEvents = [];
    }
    get boundedEvents() {
      return this._boundedEvents;
    }
    dispose() {
      this.unbindAll();
      this._boundedEvents = [];
    }
    /** Bind an event listener to any element */
    bind(elementOrElements, eventNameOrNames, listener, listenerOptions, groupName = "") {
      const eventNames = Array.isArray(eventNameOrNames) ? eventNameOrNames : [eventNameOrNames];
      if (!(elementOrElements instanceof HTMLElement || elementOrElements instanceof DocumentFragment) && elementOrElements?.forEach) {
        elementOrElements.forEach((element) => this.bindElementEvents(element, eventNames, listener, listenerOptions, groupName));
      } else if (elementOrElements) {
        this.bindElementEvents(elementOrElements, eventNames, listener, listenerOptions, groupName);
      }
    }
    /** Unbind a specific listener that was bounded earlier */
    unbind(elementOrElements, eventNameOrNames, listener) {
      const elements = Array.isArray(elementOrElements) ? elementOrElements : [elementOrElements];
      const eventNames = Array.isArray(eventNameOrNames) ? eventNameOrNames : [eventNameOrNames];
      for (const eventName of eventNames) {
        for (const element of elements) {
          if (typeof element?.removeEventListener === "function") {
            element.removeEventListener(eventName, listener);
          }
        }
      }
    }
    unbindByEventName(element, eventName) {
      const boundedEvent = this._boundedEvents.find((e2) => e2.element === element && e2.eventName === eventName);
      if (boundedEvent) {
        this.unbind(boundedEvent.element, boundedEvent.eventName, boundedEvent.listener);
      }
    }
    /**
     * Unbind all event listeners that were bounded, optionally provide a group name to unbind all listeners assigned to that specific group only.
     */
    unbindAll(groupName) {
      if (groupName) {
        const groupNames = Array.isArray(groupName) ? groupName : [groupName];
        for (let i = this._boundedEvents.length - 1; i >= 0; --i) {
          const boundedEvent = this._boundedEvents[i];
          if (groupNames.some((g2) => g2 === boundedEvent.groupName)) {
            const { element, eventName, listener } = boundedEvent;
            this.unbind(element, eventName, listener);
            this._boundedEvents.splice(i, 1);
          }
        }
      } else {
        while (this._boundedEvents.length > 0) {
          const boundedEvent = this._boundedEvents.pop();
          const { element, eventName, listener } = boundedEvent;
          this.unbind(element, eventName, listener);
        }
      }
    }
    // --
    // private functions
    /** bind all event(s) to the element */
    bindElementEvents(element, eventNames, listener, listenerOptions, groupName = "") {
      for (const eventName of eventNames) {
        element.addEventListener(eventName, listener, listenerOptions);
        this._boundedEvents.push({ element, eventName, listener, groupName });
      }
    }
  };

  // node_modules/@slickgrid-universal/utils/dist/index.js
  var dist_exports = {};
  __export(dist_exports, {
    addToArrayWhenNotExists: () => addToArrayWhenNotExists,
    addWhiteSpaces: () => addWhiteSpaces,
    arrayRemoveItemByIndex: () => arrayRemoveItemByIndex,
    calculateAvailableSpace: () => calculateAvailableSpace,
    classNameToList: () => classNameToList,
    createDomElement: () => createDomElement,
    deepCopy: () => deepCopy,
    deepMerge: () => deepMerge,
    destroyAllElementProps: () => destroyAllElementProps,
    emptyElement: () => emptyElement,
    emptyObject: () => emptyObject,
    extend: () => extend,
    findFirstAttribute: () => findFirstAttribute,
    findWidthOrDefault: () => findWidthOrDefault,
    getFunctionDetails: () => getFunctionDetails,
    getHtmlStringOutput: () => getHtmlStringOutput,
    getInnerSize: () => getInnerSize,
    getOffset: () => getOffset,
    getOffsetRelativeToParent: () => getOffsetRelativeToParent,
    getStyleProp: () => getStyleProp,
    htmlDecode: () => htmlDecode,
    htmlEncode: () => htmlEncode,
    htmlEncodeWithPadding: () => htmlEncodeWithPadding,
    insertAfterElement: () => insertAfterElement,
    isDefined: () => isDefined,
    isDefinedNumber: () => isDefinedNumber,
    isEmptyObject: () => isEmptyObject,
    isHtml: () => isHtml,
    isNumber: () => isNumber,
    isObject: () => isObject,
    isObjectEmpty: () => isObjectEmpty,
    isPrimitiveOrHTML: () => isPrimitiveOrHTML,
    isPrimitiveValue: () => isPrimitiveValue,
    parseBoolean: () => parseBoolean,
    queueMicrotaskPolyfill: () => queueMicrotaskPolyfill,
    removeAccentFromText: () => removeAccentFromText,
    setDeepValue: () => setDeepValue,
    setStyles: () => setStyles,
    stripTags: () => stripTags,
    titleCase: () => titleCase,
    toCamelCase: () => toCamelCase,
    toKebabCase: () => toKebabCase,
    toSentenceCase: () => toSentenceCase,
    toSnakeCase: () => toSnakeCase,
    uniqueArray: () => uniqueArray,
    uniqueObjectArray: () => uniqueObjectArray,
    windowScrollPosition: () => windowScrollPosition
  });

  // node_modules/@slickgrid-universal/utils/dist/utils.js
  function addToArrayWhenNotExists(inputArray, inputItem, itemIdPropName = "id") {
    let arrayRowIndex = -1;
    if (inputItem && typeof inputItem === "object" && itemIdPropName in inputItem) {
      arrayRowIndex = inputArray.findIndex((item) => item[itemIdPropName] === inputItem[itemIdPropName]);
    } else {
      arrayRowIndex = inputArray.findIndex((item) => item === inputItem);
    }
    if (arrayRowIndex < 0) {
      inputArray.push(inputItem);
    }
  }
  function addWhiteSpaces(nbSpaces, spaceChar = " ") {
    let result = "";
    for (let i = 0; i < nbSpaces; i++) {
      result += spaceChar;
    }
    return result;
  }
  function arrayRemoveItemByIndex(array, index2) {
    return array.filter((_el, i) => index2 !== i);
  }
  function deepCopy(objectOrArray) {
    const cloneObj = () => {
      const clone2 = {};
      Object.keys(objectOrArray).forEach((key) => {
        if (Object.prototype.hasOwnProperty.call(objectOrArray, key)) {
          clone2[key] = deepCopy(objectOrArray[key]);
        }
      });
      return clone2;
    };
    const cloneArr = () => objectOrArray.map((item) => deepCopy(item));
    const type = Object.prototype.toString.call(objectOrArray).slice(8, -1).toLowerCase();
    if (type === "object") {
      return cloneObj();
    }
    if (type === "array") {
      return cloneArr();
    }
    return objectOrArray;
  }
  function deepMerge(target, ...sources) {
    if (!sources.length) {
      return target;
    }
    const source = sources.shift();
    target = !isObject(target) && isObject(source) ? {} : target;
    if (isObject(target) && isObject(source)) {
      Object.keys(source).forEach((prop) => {
        if (source.hasOwnProperty(prop)) {
          if (prop in target) {
            if (typeof target[prop] !== "object") {
              target[prop] = source[prop];
            } else {
              if (typeof source[prop] !== "object") {
                target[prop] = source[prop];
              } else {
                if (target[prop].concat && source[prop].concat) {
                  target[prop] = target[prop].concat(source[prop]);
                } else {
                  target[prop] = deepMerge(target[prop], source[prop]);
                }
              }
            }
          } else {
            target[prop] = source[prop];
          }
        }
      });
    }
    return deepMerge(target, ...sources);
  }
  function emptyObject(obj) {
    if (isObject(obj)) {
      Object.keys(obj).forEach((key) => {
        if (obj.hasOwnProperty(key)) {
          delete obj[key];
        }
      });
    }
    obj = null;
    obj = {};
    return obj;
  }
  function getFunctionDetails(fn, addReturn = true) {
    let isAsyncFn = false;
    const getFunctionBody = (func) => {
      const fnStr = func.toString();
      isAsyncFn = fnStr.includes("async ");
      if (fnStr.replaceAll(" ", "").includes("=>({")) {
        const matches2 = fnStr.match(/(({.*}))/g) || [];
        return matches2.length >= 1 ? `return ${matches2[0].trimStart()}` : fnStr;
      }
      const isOneLinerArrowFn = !fnStr.includes("{") && fnStr.includes("=>");
      const body = fnStr.substring(fnStr.indexOf("{") + 1 || fnStr.indexOf("=>") + 2, fnStr.includes("}") ? fnStr.lastIndexOf("}") : fnStr.length);
      if (addReturn && isOneLinerArrowFn && !body.startsWith("return")) {
        return "return " + body.trimStart();
      }
      return body;
    };
    const getFunctionParams = (func) => {
      const STRIP_COMMENTS = /(\/\/.*$)|(\/\*[\s\S]*?\*\/)|(\s*=[^,)]*(('(?:\\'|[^'\r\n])*')|("(?:\\"|[^"\r\n])*"))|(\s*=[^,)]*))/gm;
      const ARG_NAMES = /([^\s,]+)/g;
      const fnStr = func.toString().replace(STRIP_COMMENTS, "");
      return fnStr.slice(fnStr.indexOf("(") + 1, fnStr.indexOf(")")).match(ARG_NAMES) ?? [];
    };
    return {
      params: getFunctionParams(fn),
      body: getFunctionBody(fn),
      isAsync: isAsyncFn
    };
  }
  function isEmptyObject(obj) {
    if (obj === null || obj === void 0) {
      return true;
    }
    return Object.entries(obj).length === 0;
  }
  function isDefined(value) {
    return value !== void 0 && value !== null && value !== "";
  }
  function isDefinedNumber(value) {
    return value !== null && !isNaN(value) && value !== "";
  }
  function isObject(item) {
    return item !== null && typeof item === "object" && !Array.isArray(item) && !(item instanceof Date);
  }
  function isPrimitiveValue(val) {
    return typeof val === "boolean" || typeof val === "number" || typeof val === "string" || val === null || val === void 0;
  }
  function isPrimitiveOrHTML(val) {
    return isHtml(val) || isPrimitiveValue(val);
  }
  function isHtml(val) {
    return typeof HTMLElement !== "undefined" && val instanceof HTMLElement || typeof DocumentFragment !== "undefined" && val instanceof DocumentFragment;
  }
  function isNumber(value, strict = false) {
    if (strict) {
      return value === null || value === void 0 || typeof value === "string" ? false : !isNaN(value);
    }
    return value === null || value === void 0 || value === "" ? false : !isNaN(+value);
  }
  function isObjectEmpty(obj) {
    return !obj || obj && typeof obj === "object" && Object.keys(obj).length === 0;
  }
  function parseBoolean(input) {
    return /(true|1)/i.test(input + "");
  }
  function queueMicrotaskPolyfill(callback) {
    if (typeof queueMicrotask === "function") {
      queueMicrotask(callback);
    } else {
      Promise.resolve().then(callback);
    }
  }
  function removeAccentFromText(text, shouldLowerCase = false) {
    const normalizedText = typeof text.normalize === "function" ? text.normalize("NFD").replace(/[\u0300-\u036f]/g, "") : text;
    return shouldLowerCase ? normalizedText.toLowerCase() : normalizedText;
  }
  function setDeepValue(obj, path, value) {
    if (typeof path === "string") {
      path = path.split(".");
    }
    if (path.length > 1) {
      const e2 = path.shift();
      if (obj && e2 !== void 0) {
        setDeepValue(obj[e2] = isDefined(obj[e2]) && (Array.isArray(obj[e2]) || Object.prototype.toString.call(obj[e2]) === "[object Object]") ? obj[e2] : {}, path, value);
      }
    } else if (obj && path[0]) {
      obj[path[0]] = value;
    }
  }
  function titleCase(inputStr, shouldTitleCaseEveryWords = false) {
    if (typeof inputStr === "string") {
      if (shouldTitleCaseEveryWords) {
        return inputStr.replace(/\w\S*/g, (outputStr) => {
          return outputStr.charAt(0).toUpperCase() + outputStr.substring(1).toLowerCase();
        });
      }
      return inputStr.charAt(0).toUpperCase() + inputStr.slice(1);
    }
    return inputStr;
  }
  function toCamelCase(inputStr) {
    if (typeof inputStr === "string") {
      return inputStr.replace(/(?:^\w|[A-Z]|\b\w|[\s+\-_/])/g, (match, offset2) => {
        if (/[\s+\-_/]/.test(match)) {
          return "";
        }
        return offset2 === 0 ? match.toLowerCase() : match.toUpperCase();
      });
    }
    return inputStr;
  }
  function toKebabCase(inputStr) {
    if (typeof inputStr === "string") {
      return toCamelCase(inputStr).replace(/([A-Z])|([-_])/g, "-$1").toLowerCase();
    }
    return inputStr;
  }
  function toSentenceCase(inputStr) {
    if (typeof inputStr === "string") {
      const result = inputStr.replace(/([A-Z])|([-_])/g, " $1").replace(/\s+/g, " ").trim();
      return result.charAt(0).toUpperCase() + result.slice(1);
    }
    return inputStr;
  }
  function toSnakeCase(inputStr) {
    if (typeof inputStr === "string") {
      return toCamelCase(inputStr).replace(/([A-Z])/g, "_$1").toLowerCase();
    }
    return inputStr;
  }
  function uniqueArray(arr) {
    if (Array.isArray(arr) && arr.length > 0) {
      return arr.filter((item, index2) => {
        return arr.indexOf(item) >= index2;
      });
    }
    return arr;
  }
  function uniqueObjectArray(arr, propertyName = "id") {
    if (Array.isArray(arr) && arr.length > 0) {
      const result = [];
      const map2 = /* @__PURE__ */ new Map();
      for (const item of arr) {
        if (item && !map2.has(item[propertyName])) {
          map2.set(item[propertyName], true);
          result.push({
            id: item[propertyName],
            name: item.name
          });
        }
      }
      return result;
    }
    return arr;
  }

  // node_modules/@slickgrid-universal/utils/dist/domUtils.js
  function calculateAvailableSpace(element) {
    const vh = window.innerHeight || 0;
    const vw = window.innerWidth || 0;
    const { top: pageScrollTop, left: pageScrollLeft } = windowScrollPosition();
    const { top: elementOffsetTop = 0, left: elementOffsetLeft = 0 } = getOffset(element) || {};
    const top = elementOffsetTop - pageScrollTop;
    const left = elementOffsetLeft - pageScrollLeft;
    const bottom = vh - (elementOffsetTop - pageScrollTop + element.clientHeight);
    const right = vw - (elementOffsetLeft - pageScrollLeft + element.clientWidth);
    return { top, bottom, left, right };
  }
  function createDomElement(tagName, elementOptions, appendToParent) {
    const elm = document.createElement(tagName);
    if (elementOptions) {
      Object.keys(elementOptions).forEach((elmOptionKey) => {
        if (elmOptionKey === "innerHTML") {
          console.warn(`[Slickgrid-Universal] For better CSP (Content Security Policy) support, do not use "innerHTML" directly in "createDomElement('${tagName}', { innerHTML: 'some html'})", it is better as separate assignment: "const elm = createDomElement('span'); elm.innerHTML = 'some html';"`);
        }
        const elmValue = elementOptions[elmOptionKey];
        if (typeof elmValue === "object") {
          Object.assign(elm[elmOptionKey], elmValue);
        } else {
          elm[elmOptionKey] = elementOptions[elmOptionKey];
        }
      });
    }
    appendToParent?.appendChild(elm);
    return elm;
  }
  function setStyles(element, styles3) {
    Object.keys(styles3).forEach((key) => {
      const camelStyleKey = key;
      const value = styles3[camelStyleKey];
      if (value !== void 0 && value !== null) {
        element.style[camelStyleKey] = value;
      }
    });
  }
  function classNameToList(className = "") {
    return className.split(" ").filter((cls) => cls);
  }
  function destroyAllElementProps(obj) {
    if (!obj || typeof obj !== "object")
      return;
    Object.keys(obj).forEach((key) => {
      if (Array.isArray(obj[key])) {
        destroyAllElementProps(obj[key]);
      } else if (obj[key] instanceof HTMLElement) {
        obj[key] = null;
      }
    });
  }
  function emptyElement(element, shouldRemove = false) {
    while (element?.firstChild) {
      element.removeChild(element.firstChild);
    }
    if (shouldRemove) {
      element?.remove();
    }
  }
  function getHtmlStringOutput(input, type = "innerHTML") {
    if (input instanceof DocumentFragment) {
      return [].map.call(input.childNodes, (x2) => x2[type]).join("") || input.textContent || "";
    } else if (input instanceof HTMLElement) {
      return input[type];
    }
    return String(input ?? "");
  }
  function getOffsetRelativeToParent(parentElm, childElm) {
    if (!parentElm || !childElm) {
      return void 0;
    }
    const parentPos = parentElm.getBoundingClientRect();
    const childPos = childElm.getBoundingClientRect();
    return {
      top: childPos.top - parentPos.top,
      right: childPos.right - parentPos.right,
      bottom: childPos.bottom - parentPos.bottom,
      left: childPos.left - parentPos.left
    };
  }
  function getOffset(elm) {
    if (!elm?.getBoundingClientRect) {
      return { top: 0, bottom: 0, left: 0, right: 0 };
    }
    const { top, left, bottom, right } = elm.getBoundingClientRect();
    return {
      top: top + window.pageYOffset,
      left: left + window.pageXOffset,
      bottom,
      right
    };
  }
  function getInnerSize(elm, type) {
    let size = 0;
    if (elm) {
      const clientSize = type === "height" ? "clientHeight" : "clientWidth";
      const sides = type === "height" ? ["top", "bottom"] : ["left", "right"];
      size = elm[clientSize];
      for (const side of sides) {
        const sideSize = parseFloat(getStyleProp(elm, `padding-${side}`) || "") || 0;
        size -= sideSize;
      }
    }
    return size;
  }
  function getStyleProp(elm, property) {
    if (elm) {
      return getComputedStyle(elm).getPropertyValue(property);
    }
    return null;
  }
  function findFirstAttribute(inputElm, attributes) {
    if (inputElm) {
      for (const attribute of attributes) {
        const attrData = inputElm.getAttribute(attribute);
        if (attrData) {
          return attrData;
        }
      }
    }
    return null;
  }
  function findWidthOrDefault(inputWidth, defaultValue = "auto") {
    return (/^[0-9]+$/i.test(`${inputWidth}`) ? `${+inputWidth}px` : inputWidth) || defaultValue;
  }
  function htmlEncode(inputValue) {
    const val = typeof inputValue === "string" ? inputValue : String(inputValue);
    const entityMap = {
      "&": "&amp;",
      "<": "&lt;",
      ">": "&gt;",
      '"': "&quot;",
      "'": "&#39;"
    };
    return (val || "").toString().replace(/[&<>"']/g, (s) => entityMap[s]);
  }
  function htmlDecode(input) {
    if (isDefined(input)) {
      let str = input.toString();
      str = str.replace(/&#(\d+);&#(\d+);/g, (_2, high, low) => {
        high = parseInt(high, 10);
        low = parseInt(low, 10);
        if (high >= 55296 && high <= 56319 && low >= 56320 && low <= 57343) {
          return String.fromCodePoint((high - 55296) * 1024 + (low - 56320) + 65536);
        }
        return String.fromCharCode(high) + String.fromCharCode(low);
      });
      str = str.replace(/&#(\d+);/g, (_2, dec) => String.fromCharCode(dec));
      let prev;
      do {
        prev = str;
        const txt = document.createElement("textarea");
        txt.innerHTML = str;
        str = txt.value;
      } while (str !== prev);
      return str;
    }
    return "";
  }
  function htmlEncodeWithPadding(inputStr, paddingLength) {
    const inputStrLn = inputStr.length;
    let outputStr = htmlEncode(inputStr);
    if (inputStrLn < paddingLength) {
      for (let i = inputStrLn; i < paddingLength; i++) {
        outputStr += `&nbsp;`;
      }
    }
    return outputStr;
  }
  function insertAfterElement(referenceNode, newNode) {
    referenceNode.parentNode?.insertBefore(newNode, referenceNode.nextSibling);
  }
  function windowScrollPosition() {
    return {
      left: window.pageXOffset || document.documentElement.scrollLeft || 0,
      top: window.pageYOffset || document.documentElement.scrollTop || 0
    };
  }

  // node_modules/@slickgrid-universal/utils/dist/nodeExtend.js
  var hasOwn = Object.prototype.hasOwnProperty;
  var toStr = Object.prototype.toString;
  var defineProperty = Object.defineProperty;
  var gOPD = Object.getOwnPropertyDescriptor;
  var isArray = function isArray2(arr) {
    if (typeof Array.isArray === "function") {
      return Array.isArray(arr);
    }
    return toStr.call(arr) === "[object Array]";
  };
  var isPlainObject = function isPlainObject2(obj) {
    if (!obj || toStr.call(obj) !== "[object Object]") {
      return false;
    }
    const hasOwnConstructor = hasOwn.call(obj, "constructor");
    const hasIsPrototypeOf = obj.constructor && obj.constructor.prototype && hasOwn.call(obj.constructor.prototype, "isPrototypeOf");
    if (obj.constructor && !hasOwnConstructor && !hasIsPrototypeOf) {
      return false;
    }
    let key;
    for (key in obj) {
    }
    return typeof key === "undefined" || hasOwn.call(obj, key);
  };
  var setProperty = function setProperty2(target, options) {
    if (defineProperty && options.name === "__proto__") {
      defineProperty(target, options.name, {
        enumerable: true,
        configurable: true,
        value: options.newValue,
        writable: true
      });
    } else {
      target[options.name] = options.newValue;
    }
  };
  var getProperty = function getProperty2(obj, name) {
    if (name === "__proto__") {
      if (!hasOwn.call(obj, name)) {
        return void 0;
      } else if (gOPD) {
        return gOPD(obj, name).value;
      }
    }
    return obj[name];
  };
  function extend(...args) {
    let options;
    let name;
    let src;
    let copy;
    let copyIsArray;
    let clone2;
    let target = args[0];
    let i = 1;
    const length = args.length;
    let deep = false;
    if (typeof target === "boolean") {
      deep = target;
      target = args[1] || {};
      i = 2;
    }
    if (target === null || target === void 0 || typeof target !== "object" && typeof target !== "function") {
      target = {};
    }
    for (; i < length; ++i) {
      options = args[i];
      if (options !== null && options !== void 0) {
        for (name in options) {
          src = getProperty(target, name);
          copy = getProperty(options, name);
          if (target !== copy) {
            if (deep && copy && (isPlainObject(copy) || (copyIsArray = isArray(copy)))) {
              if (copyIsArray) {
                copyIsArray = false;
                clone2 = src && isArray(src) ? src : [];
              } else {
                clone2 = src && isPlainObject(src) ? src : {};
              }
              setProperty(target, { name, newValue: extend(deep, clone2, copy) });
            } else if (typeof copy !== "undefined") {
              setProperty(target, { name, newValue: copy });
            }
          }
        }
      }
    }
    return target;
  }

  // node_modules/@slickgrid-universal/utils/dist/stripTagsUtil.js
  var STATE_PLAINTEXT = /* @__PURE__ */ Symbol("plaintext");
  var STATE_HTML = /* @__PURE__ */ Symbol("html");
  var STATE_COMMENT = /* @__PURE__ */ Symbol("comment");
  var ALLOWED_TAGS_REGEX = /<(\w*)>/g;
  var NORMALIZE_TAG_REGEX = /<\/?([^\s/>]+)/;
  function stripTags(htmlText, allowableTags, tagReplacement) {
    function init2(html, allowable_tags, tag_replacement) {
      if (typeof html !== "string" && (isNumber(html) || typeof html === "boolean")) {
        return String(html);
      }
      if (html instanceof HTMLElement) {
        html = html.innerHTML;
      }
      if (typeof html !== "string" && html !== void 0 && html !== null) {
        throw new TypeError(`'html' parameter must be a string`);
      }
      return striptags_internal(html || "", init_context(allowable_tags || "", tag_replacement || ""));
    }
    function init_context(allowable_tags, tag_replacement) {
      return {
        allowable_tags: parse_allowable_tags(allowable_tags),
        tag_replacement,
        state: STATE_PLAINTEXT,
        tag_buffer: "",
        depth: 0,
        in_quote_char: ""
      };
    }
    function striptags_internal(html, context) {
      const allowable_tags = context.allowable_tags;
      const tag_replacement = context.tag_replacement;
      let state2 = context.state;
      let tag_buffer = context.tag_buffer;
      let depth = context.depth;
      let in_quote_char = context.in_quote_char;
      let output = "";
      for (let idx = 0, length = html.length; idx < length; idx++) {
        const char = html[idx];
        if (state2 === STATE_PLAINTEXT) {
          switch (char) {
            case "<":
              state2 = STATE_HTML;
              tag_buffer += char;
              break;
            default:
              output += char;
              break;
          }
        } else if (state2 === STATE_HTML) {
          switch (char) {
            case "<":
              if (in_quote_char) {
                break;
              }
              depth++;
              break;
            case ">":
              if (in_quote_char) {
                break;
              }
              if (depth) {
                depth--;
                break;
              }
              in_quote_char = "";
              state2 = STATE_PLAINTEXT;
              tag_buffer += ">";
              if (allowable_tags.has(normalize_tag(tag_buffer))) {
                output += tag_buffer;
              } else {
                output += tag_replacement;
              }
              tag_buffer = "";
              break;
            case '"':
            case "'":
              if (char === in_quote_char) {
                in_quote_char = "";
              } else {
                in_quote_char = in_quote_char || char;
              }
              tag_buffer += char;
              break;
            case "-":
              if (tag_buffer === "<!-") {
                state2 = STATE_COMMENT;
              }
              tag_buffer += char;
              break;
            case " ":
            case "\n":
              if (tag_buffer === "<") {
                state2 = STATE_PLAINTEXT;
                output += "< ";
                tag_buffer = "";
                break;
              }
              tag_buffer += char;
              break;
            default:
              tag_buffer += char;
              break;
          }
        } else if (state2 === STATE_COMMENT) {
          switch (char) {
            case ">":
              if (tag_buffer.slice(-2) === "--") {
                state2 = STATE_PLAINTEXT;
              }
              tag_buffer = "";
              break;
            default:
              tag_buffer += char;
              break;
          }
        }
      }
      context.state = state2;
      context.tag_buffer = tag_buffer;
      context.depth = depth;
      context.in_quote_char = in_quote_char;
      return output;
    }
    function parse_allowable_tags(allowable_tags) {
      let tag_set = /* @__PURE__ */ new Set();
      if (typeof allowable_tags === "string") {
        let match;
        while (match = ALLOWED_TAGS_REGEX.exec(allowable_tags)) {
          tag_set.add(match[1]);
        }
      } else if (Array.isArray(allowable_tags)) {
        tag_set = new Set(allowable_tags);
      }
      return tag_set;
    }
    function normalize_tag(tag_buffer) {
      const match = NORMALIZE_TAG_REGEX.exec(tag_buffer);
      return match ? match[1].toLowerCase() : null;
    }
    return init2(htmlText, allowableTags, tagReplacement);
  }

  // node_modules/@slickgrid-universal/common/dist/services/backendUtility.service.js
  var backendUtility_service_exports = {};
  __export(backendUtility_service_exports, {
    BackendUtilityService: () => BackendUtilityService
  });
  var BackendUtilityService = class {
    constructor(rxjs) {
      this.rxjs = rxjs;
      this._infiniteScrollBottomHit = false;
      this._currentRequestId = 0;
    }
    addRxJsResource(rxjs) {
      this.rxjs = rxjs;
    }
    /** Execute the Backend Processes Callback, that could come from an Observable or a Promise callback */
    executeBackendProcessesCallback(startTime, processResult, backendApi, totalItems) {
      const endTime = /* @__PURE__ */ new Date();
      if (processResult) {
        if (backendApi.service.postProcess) {
          backendApi.service.postProcess(processResult);
        }
        if (backendApi?.internalPostProcess) {
          backendApi.internalPostProcess(processResult);
        }
      }
      if (backendApi.postProcess !== void 0) {
        if (processResult instanceof Object) {
          processResult.metrics = {
            startTime,
            endTime,
            executionTime: endTime.valueOf() - startTime.valueOf(),
            itemCount: totalItems,
            totalItemCount: totalItems
          };
          if (backendApi.service.options?.infiniteScroll) {
            processResult.infiniteScrollBottomHit = this._infiniteScrollBottomHit;
            delete processResult.itemCount;
          }
        }
        backendApi.postProcess(processResult);
        this.setInfiniteScrollBottomHit(false);
      }
    }
    /** On a backend service api error, we will run the "onError" if there is 1 provided or just throw back the error when nothing is provided */
    onBackendError(e2, backendApi) {
      if (typeof backendApi?.onError === "function") {
        backendApi.onError(e2);
      } else {
        throw e2;
      }
    }
    /**
     * Execute the backend callback, which are mainly the "process" & "postProcess" methods.
     * Also note that "preProcess" was executed prior to this callback
     */
    executeBackendCallback(backendServiceApi, query, args, startTime, totalItems, extraCallbacks) {
      if (backendServiceApi) {
        if (args && !args.clearFilterTriggered && !args.clearSortTriggered && extraCallbacks?.emitActionChangedCallback) {
          extraCallbacks.emitActionChangedCallback.call(this, "remote");
        }
        this._abortController?.abort();
        this._abortController = new AbortController();
        const process = backendServiceApi.process(query, { signal: this._abortController.signal });
        if (process instanceof Promise && process.then) {
          const requestId = ++this._currentRequestId;
          process.then((processResult) => {
            if (this._currentRequestId === requestId) {
              this.executeBackendProcessesCallback(startTime, processResult, backendServiceApi, totalItems);
              extraCallbacks?.successCallback?.call(this, args);
            }
          }).catch((error) => {
            if (error.name !== "AbortError") {
              extraCallbacks?.errorCallback?.call(this, args);
              this.onBackendError(error, backendServiceApi);
            }
          });
        } else if (this.rxjs?.isObservable(process)) {
          const rxjs = this.rxjs;
          if (rxjs.isObservable(extraCallbacks?.httpCancelRequestSubject)) {
            extraCallbacks?.httpCancelRequestSubject.next();
          }
          process.pipe(rxjs.takeUntil(rxjs.iif(() => rxjs.isObservable(extraCallbacks?.httpCancelRequestSubject), extraCallbacks?.httpCancelRequestSubject, rxjs.EMPTY))).subscribe((processResult) => {
            this.executeBackendProcessesCallback(startTime, processResult, backendServiceApi, totalItems);
            extraCallbacks?.successCallback?.call(this, args);
          }, (error) => {
            extraCallbacks?.errorCallback?.call(this, args);
            this.onBackendError(error, backendServiceApi);
          });
        }
      }
    }
    /** Refresh the dataset through the Backend Service */
    refreshBackendDataset(gridOptions) {
      let query = "";
      const backendApi = gridOptions?.backendServiceApi;
      if (!backendApi || !backendApi.service || !backendApi.process) {
        throw new Error(`BackendServiceApi requires at least a "process" function and a "service" defined`);
      }
      if (backendApi.service) {
        query = backendApi.service.buildQuery();
      }
      if (query && query !== "") {
        const startTime = /* @__PURE__ */ new Date();
        backendApi.preProcess?.();
        const totalItems = gridOptions?.pagination?.totalItems ?? 0;
        this.executeBackendCallback(backendApi, query, null, startTime, totalItems);
      }
    }
    setInfiniteScrollBottomHit(scrollBottomHit) {
      this._infiniteScrollBottomHit = scrollBottomHit;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/observers.js
  var observers_exports = {};
  __export(observers_exports, {
    collectionObserver: () => collectionObserver,
    propertyObserver: () => propertyObserver
  });
  function collectionObserver(arr, callback) {
    if (Array.isArray(arr)) {
      const mutationMethods = ["pop", "push", "reverse", "shift", "unshift", "splice", "sort"];
      const originalActions = [];
      mutationMethods.forEach((changeMethod) => {
        arr[changeMethod] = (...args) => {
          const res = Array.prototype[changeMethod].apply(arr, args);
          originalActions.push({ method: changeMethod, action: res });
          callback.apply(arr, [arr, args]);
          return res;
        };
      });
      const disconnect = () => mutationMethods.forEach((changeMethod) => {
        arr[changeMethod] = () => originalActions[changeMethod].action;
      });
      return { disconnect };
    }
    return null;
  }
  function propertyObserver(obj, prop, callback) {
    let innerValue = obj[prop];
    Object.defineProperty(obj, prop, {
      configurable: true,
      get() {
        return innerValue;
      },
      set(newValue) {
        innerValue = newValue;
        callback.apply(obj, [newValue, obj[prop]]);
      }
    });
  }

  // node_modules/@slickgrid-universal/common/dist/services/utilities.js
  var utilities_exports = {};
  __export(utilities_exports, {
    CancelledException: () => CancelledException,
    addTreeLevelAndAggregatorsByMutation: () => addTreeLevelAndAggregatorsByMutation,
    addTreeLevelByMutation: () => addTreeLevelByMutation,
    cancellablePromise: () => cancellablePromise,
    castObservableToPromise: () => castObservableToPromise,
    createDocumentFragmentOrElement: () => createDocumentFragmentOrElement,
    decimalFormatted: () => decimalFormatted,
    fetchAsPromise: () => fetchAsPromise,
    findItemInTreeStructure: () => findItemInTreeStructure,
    findOrDefault: () => findOrDefault,
    flattenToParentChildArray: () => flattenToParentChildArray,
    formatNumber: () => formatNumber,
    getAllDateFieldTypes: () => getAllDateFieldTypes,
    getCellValueFromQueryFieldGetter: () => getCellValueFromQueryFieldGetter,
    getColumnFieldType: () => getColumnFieldType,
    getDescendantProperty: () => getDescendantProperty,
    getTranslationPrefix: () => getTranslationPrefix,
    getTreeDataOptionPropName: () => getTreeDataOptionPropName,
    isColumnDateType: () => isColumnDateType,
    mapOperatorByFieldType: () => mapOperatorByFieldType,
    mapOperatorToShorthandDesignation: () => mapOperatorToShorthandDesignation,
    mapOperatorType: () => mapOperatorType,
    objectWithoutKey: () => objectWithoutKey,
    sortPresetColumns: () => sortPresetColumns,
    thousandSeparatorFormatted: () => thousandSeparatorFormatted,
    unflattenParentChildArrayToTree: () => unflattenParentChildArrayToTree,
    unsubscribeAll: () => unsubscribeAll
  });

  // node_modules/un-flatten-tree/dist/un-flatten-tree.es.js
  var map = Array.prototype.map;
  var reduce = Array.prototype.reduce;
  var identity = (x2) => x2;
  function flatten(tree, getChildNodes, convertNode = identity, generateId = () => void 0) {
    const stack = tree && tree.length ? [{ pointer: tree, offset: 0 }] : [];
    const flat = [];
    let current;
    while (stack.length) {
      current = stack.pop();
      while (current.offset < current.pointer.length) {
        const node = current.pointer[current.offset];
        const nodeId = generateId(node);
        const children = getChildNodes(node);
        flat.push(convertNode(node, current.node, nodeId, current.nodeId));
        current.offset += 1;
        if (children) {
          stack.push(current);
          current = {
            pointer: children,
            offset: 0,
            node,
            nodeId
          };
        }
      }
    }
    return flat;
  }

  // node_modules/@slickgrid-universal/common/dist/constants.js
  var Constants = class {
  };
  Constants.locales = {
    TEXT_ALL_SELECTED: "All Selected",
    TEXT_ALL_X_RECORDS_SELECTED: "All {{x}} records selected",
    TEXT_APPLY_MASS_UPDATE: "Apply Mass Update",
    TEXT_APPLY_TO_SELECTION: "Update Selection",
    TEXT_CANCEL: "Cancel",
    TEXT_CLEAR_ALL_FILTERS: "Clear all Filters",
    TEXT_CLEAR_ALL_GROUPING: "Clear all Grouping",
    TEXT_CLEAR_ALL_SORTING: "Clear all Sorting",
    TEXT_CLEAR_PINNING: "Unfreeze Columns/Rows",
    TEXT_CLONE: "Clone",
    TEXT_COLLAPSE_ALL_GROUPS: "Collapse all Groups",
    TEXT_CONTAINS: "Contains",
    TEXT_COLUMNS: "Columns",
    TEXT_COLUMN_RESIZE_BY_CONTENT: "Resize by Content",
    TEXT_COMMANDS: "Commands",
    TEXT_COPY: "Copy",
    TEXT_DROP_COLUMN_HEADER_TO_GROUP_BY: "Drop a column header here to group by the column",
    TEXT_EQUALS: "Equals",
    TEXT_EQUAL_TO: "Equal to",
    TEXT_ENDS_WITH: "Ends With",
    TEXT_ERROR_EDITABLE_GRID_REQUIRED: "Your grid must be editable in order to use the Composite Editor Modal.",
    TEXT_ERROR_ENABLE_CELL_NAVIGATION_REQUIRED: 'Composite Editor requires the flag "enableCellNavigation" to be set to True in your Grid Options.',
    TEXT_ERROR_NO_CHANGES_DETECTED: "Sorry we could not detect any changes.",
    TEXT_ERROR_NO_EDITOR_FOUND: "We could not find any Editor in your Column Definition.",
    TEXT_ERROR_NO_RECORD_FOUND: "No records selected for edit or clone operation.",
    TEXT_ERROR_ROW_NOT_EDITABLE: "Current row is not editable.",
    TEXT_ERROR_ROW_SELECTION_REQUIRED: "You must select some rows before trying to apply new value(s).",
    TEXT_EXPAND_ALL_GROUPS: "Expand all Groups",
    TEXT_EXPORT_TO_CSV: "Export in CSV format",
    TEXT_EXPORT_TO_TEXT_FORMAT: "Export in Text format (Tab delimited)",
    TEXT_EXPORT_TO_EXCEL: "Export to Excel",
    TEXT_EXPORT_TO_PDF: "Export to PDF",
    TEXT_EXPORT_TO_TAB_DELIMITED: "Export in Text format (Tab delimited)",
    TEXT_FORCE_FIT_COLUMNS: "Force fit columns",
    TEXT_FILTER_SHORTCUTS: "Filter Shortcuts",
    TEXT_FREEZE_COLUMNS: "Freeze Columns",
    TEXT_GREATER_THAN: "Greater than",
    TEXT_GREATER_THAN_OR_EQUAL_TO: "Greater than or equal to",
    TEXT_GROUP_BY: "Group By",
    TEXT_HIDE_COLUMN: "Hide Column",
    TEXT_ITEMS: "items",
    TEXT_ITEMS_PER_PAGE: "items per page",
    TEXT_ITEMS_SELECTED: "items selected",
    TEXT_OF: "of",
    TEXT_OK: "OK",
    TEXT_OPTIONS: "Options",
    TEXT_LAST_UPDATE: "Last Update",
    TEXT_LESS_THAN: "Less than",
    TEXT_LESS_THAN_OR_EQUAL_TO: "Less than or equal to",
    TEXT_LOADING: "Loading...",
    TEXT_NOT_CONTAINS: "Not contains",
    TEXT_NOT_EQUAL_TO: "Not equal to",
    TEXT_PAGE: "Page",
    TEXT_RECORDS_SELECTED: "records selected",
    TEXT_REFRESH_DATASET: "Refresh Dataset",
    TEXT_REMOVE_FILTER: "Remove Filter",
    TEXT_REMOVE_SORT: "Remove Sort",
    TEXT_RESET_INPUT_VALUE: "Reset Input Value",
    TEXT_RESET_FORM: "Reset Form",
    TEXT_SAVE: "Save",
    TEXT_SELECT_ALL: "Select All",
    TEXT_SYNCHRONOUS_RESIZE: "Synchronous resize",
    TEXT_SORT_ASCENDING: "Sort Ascending",
    TEXT_SORT_DESCENDING: "Sort Descending",
    TEXT_STARTS_WITH: "Starts With",
    TEXT_TOGGLE_ALL_GROUPS: "Toggle all Groups",
    TEXT_TOGGLE_DARK_MODE: "Toggle Dark Mode",
    TEXT_TOGGLE_FILTER_ROW: "Toggle Filter Row",
    TEXT_TOGGLE_PRE_HEADER_ROW: "Toggle Pre-Header Row",
    TEXT_UNFREEZE_COLUMNS: "Unfreeze Columns",
    TEXT_X_OF_Y_SELECTED: "# of % selected",
    TEXT_X_OF_Y_MASS_SELECTED: "{{x}} of {{y}} selected"
  };
  Constants.treeDataProperties = {
    CHILDREN_PROP: "children",
    COLLAPSED_PROP: "__collapsed",
    HAS_CHILDREN_PROP: "__hasChildren",
    LAZY_LOADING_PROP: "__lazyLoading",
    TREE_LEVEL_PROP: "__treeLevel",
    PARENT_PROP: "__parentId"
  };
  Constants.DEFAULT_FORMATTER_NUMBER_MIN_DECIMAL = 2;
  Constants.DEFAULT_FORMATTER_NUMBER_MAX_DECIMAL = 2;
  Constants.DEFAULT_FORMATTER_CURRENCY_MIN_DECIMAL = 2;
  Constants.DEFAULT_FORMATTER_CURRENCY_MAX_DECIMAL = 4;
  Constants.DEFAULT_FORMATTER_PERCENT_MIN_DECIMAL = void 0;
  Constants.DEFAULT_FORMATTER_PERCENT_MAX_DECIMAL = void 0;
  Constants.DEFAULT_NUMBER_DECIMAL_SEPARATOR = ".";
  Constants.DEFAULT_NUMBER_THOUSAND_SEPARATOR = "";
  Constants.DEFAULT_NEGATIVE_NUMBER_WRAPPED_IN_BRAQUET = false;
  Constants.SLIDER_DEFAULT_MIN_VALUE = 0;
  Constants.SLIDER_DEFAULT_MAX_VALUE = 100;
  Constants.SLIDER_DEFAULT_STEP = 1;
  Constants.VALIDATION_REQUIRED_FIELD = "Field is required";
  Constants.VALIDATION_EDITOR_VALID_NUMBER = "Please enter a valid number";
  Constants.VALIDATION_EDITOR_VALID_INTEGER = "Please enter a valid integer number";
  Constants.VALIDATION_EDITOR_INTEGER_BETWEEN = "Please enter a valid integer number between {{minValue}} and {{maxValue}}";
  Constants.VALIDATION_EDITOR_INTEGER_MAX = "Please enter a valid integer number that is lower than {{maxValue}}";
  Constants.VALIDATION_EDITOR_INTEGER_MAX_INCLUSIVE = "Please enter a valid integer number that is lower than or equal to {{maxValue}}";
  Constants.VALIDATION_EDITOR_INTEGER_MIN = "Please enter a valid integer number that is greater than {{minValue}}";
  Constants.VALIDATION_EDITOR_INTEGER_MIN_INCLUSIVE = "Please enter a valid integer number that is greater than or equal to {{minValue}}";
  Constants.VALIDATION_EDITOR_NUMBER_BETWEEN = "Please enter a valid number between {{minValue}} and {{maxValue}}";
  Constants.VALIDATION_EDITOR_NUMBER_MAX = "Please enter a valid number that is lower than {{maxValue}}";
  Constants.VALIDATION_EDITOR_NUMBER_MAX_INCLUSIVE = "Please enter a valid number that is lower than or equal to {{maxValue}}";
  Constants.VALIDATION_EDITOR_NUMBER_MIN = "Please enter a valid number that is greater than {{minValue}}";
  Constants.VALIDATION_EDITOR_NUMBER_MIN_INCLUSIVE = "Please enter a valid number that is greater than or equal to {{minValue}}";
  Constants.VALIDATION_EDITOR_DECIMAL_BETWEEN = "Please enter a valid number with a maximum of {{maxDecimal}} decimals";
  Constants.VALIDATION_EDITOR_TEXT_LENGTH_BETWEEN = "Please make sure your text length is between {{minLength}} and {{maxLength}} characters";
  Constants.VALIDATION_EDITOR_TEXT_MAX_LENGTH = "Please make sure your text is less than {{maxLength}} characters";
  Constants.VALIDATION_EDITOR_TEXT_MAX_LENGTH_INCLUSIVE = "Please make sure your text is less than or equal to {{maxLength}} characters";
  Constants.VALIDATION_EDITOR_TEXT_MIN_LENGTH = "Please make sure your text is more than {{minLength}} character(s)";
  Constants.VALIDATION_EDITOR_TEXT_MIN_LENGTH_INCLUSIVE = "Please make sure your text is at least {{minLength}} character(s)";

  // node_modules/@slickgrid-universal/common/dist/enums/index.js
  var enums_exports = {};
  __export(enums_exports, {
    ExtensionName: () => ExtensionName,
    SortDirectionNumber: () => SortDirectionNumber
  });

  // node_modules/@slickgrid-universal/common/dist/enums/extensionName.enum.js
  var ExtensionName;
  (function(ExtensionName2) {
    ExtensionName2["autoTooltip"] = "autoTooltip";
    ExtensionName2["cellExternalCopyManager"] = "cellExternalCopyManager";
    ExtensionName2["cellMenu"] = "cellMenu";
    ExtensionName2["checkboxSelector"] = "checkboxSelector";
    ExtensionName2["columnPicker"] = "columnPicker";
    ExtensionName2["contextMenu"] = "contextMenu";
    ExtensionName2["customTooltip"] = "customTooltip";
    ExtensionName2["draggableGrouping"] = "draggableGrouping";
    ExtensionName2["groupItemMetaProvider"] = "groupItemMetaProvider";
    ExtensionName2["gridMenu"] = "gridMenu";
    ExtensionName2["headerButton"] = "headerButton";
    ExtensionName2["headerMenu"] = "headerMenu";
    ExtensionName2["hybridSelection"] = "hybridSelection";
    ExtensionName2["rowBasedEdit"] = "rowBasedEdit";
    ExtensionName2["rowDetailView"] = "rowDetailView";
    ExtensionName2["rowMoveManager"] = "rowMoveManager";
  })(ExtensionName || (ExtensionName = {}));

  // node_modules/@slickgrid-universal/common/dist/enums/sortDirectionNumber.enum.js
  var SortDirectionNumber;
  (function(SortDirectionNumber2) {
    SortDirectionNumber2[SortDirectionNumber2["asc"] = 1] = "asc";
    SortDirectionNumber2[SortDirectionNumber2["desc"] = -1] = "desc";
    SortDirectionNumber2[SortDirectionNumber2["neutral"] = 0] = "neutral";
  })(SortDirectionNumber || (SortDirectionNumber = {}));

  // node_modules/@slickgrid-universal/common/dist/services/utilities.js
  var CancelledException = class _CancelledException extends Error {
    constructor(message) {
      super(message);
      Object.setPrototypeOf(this, _CancelledException.prototype);
    }
  };
  function cancellablePromise(inputPromise) {
    let hasCancelled = false;
    if (inputPromise instanceof Promise) {
      return {
        promise: inputPromise.then((result) => {
          if (hasCancelled) {
            throw new CancelledException("Cancelled Promise");
          }
          return result;
        }),
        cancel: () => hasCancelled = true
      };
    }
    return inputPromise;
  }
  function castObservableToPromise(rxjs, input, fromServiceName = "") {
    let promise = input;
    if (input instanceof Promise) {
      return input;
    } else if (rxjs.isObservable(input)) {
      promise = rxjs.firstValueFrom(input);
    }
    if (!(promise instanceof Promise)) {
      throw new Error(`Something went wrong, Slickgrid-Universal ${fromServiceName} is not able to convert the Observable into a Promise.`);
    }
    return promise;
  }
  function createDocumentFragmentOrElement(gridOptions, defaultElmType = "span") {
    return gridOptions?.preventDocumentFragmentUsage ? document.createElement(defaultElmType) : new DocumentFragment();
  }
  function addTreeLevelByMutation(treeArray, options, treeLevel = 0) {
    const childrenPropName = getTreeDataOptionPropName(options, "childrenPropName");
    if (Array.isArray(treeArray)) {
      treeArray.forEach((item) => {
        if (item) {
          if (Array.isArray(item[childrenPropName]) && item[childrenPropName].length > 0) {
            treeLevel++;
            addTreeLevelByMutation(item[childrenPropName], options, treeLevel);
            treeLevel--;
          }
          item[options.levelPropName] = treeLevel;
        }
      });
    }
  }
  function addTreeLevelAndAggregatorsByMutation(treeArray, options, treeLevel = 0, parent = null) {
    const childrenPropName = getTreeDataOptionPropName(options, "childrenPropName");
    const { aggregator } = options;
    if (Array.isArray(treeArray)) {
      treeArray.forEach((item) => {
        if (item) {
          const isParent = Array.isArray(item[childrenPropName]);
          if (Array.isArray(item[childrenPropName]) && item[childrenPropName].length > 0) {
            aggregator.init(item, true);
            treeLevel++;
            addTreeLevelAndAggregatorsByMutation(item[childrenPropName], options, treeLevel, item);
            treeLevel--;
          }
          if (parent && aggregator.isInitialized && typeof aggregator.accumulate === "function" && !item?.__filteredOut) {
            aggregator.accumulate(item, isParent);
            aggregator.storeResult(parent.__treeTotals);
          }
          item[options.levelPropName] = treeLevel;
        }
      });
    }
  }
  function flattenToParentChildArray(treeArray, options) {
    const identifierPropName = getTreeDataOptionPropName(options, "identifierPropName");
    const childrenPropName = getTreeDataOptionPropName(options, "childrenPropName");
    const hasChildrenPropName = getTreeDataOptionPropName(options, "hasChildrenPropName");
    const parentPropName = getTreeDataOptionPropName(options, "parentPropName");
    const levelPropName = getTreeDataOptionPropName(options, "levelPropName");
    if (options?.shouldAddTreeLevelNumber) {
      if (Array.isArray(options?.aggregators)) {
        options.aggregators.forEach((aggregator) => {
          addTreeLevelAndAggregatorsByMutation(treeArray, { childrenPropName, levelPropName, aggregator });
        });
      } else {
        addTreeLevelByMutation(treeArray, { childrenPropName, levelPropName });
      }
    }
    const flat = flatten(treeArray, (node) => node[childrenPropName], (node, parentNode) => {
      return {
        [identifierPropName]: node[identifierPropName],
        [parentPropName]: parentNode !== void 0 ? parentNode[identifierPropName] : null,
        [hasChildrenPropName]: !!node[childrenPropName],
        ...objectWithoutKey(node, childrenPropName)
        // reuse the entire object except the children array property
      };
    });
    return flat;
  }
  function getTreeDataOptionPropName(treeDataOptions, optionName, defaultDataIdPropName = "id") {
    let propName = "";
    switch (optionName) {
      case "childrenPropName":
        propName = treeDataOptions?.[optionName] ?? Constants.treeDataProperties.CHILDREN_PROP;
        break;
      case "collapsedPropName":
        propName = treeDataOptions?.[optionName] ?? Constants.treeDataProperties.COLLAPSED_PROP;
        break;
      case "hasChildrenPropName":
        propName = treeDataOptions?.[optionName] ?? Constants.treeDataProperties.HAS_CHILDREN_PROP;
        break;
      case "identifierPropName":
        propName = treeDataOptions?.[optionName] ?? defaultDataIdPropName;
        break;
      case "lazyLoadingPropName":
        propName = treeDataOptions?.[optionName] ?? Constants.treeDataProperties.LAZY_LOADING_PROP;
        break;
      case "levelPropName":
        propName = treeDataOptions?.[optionName] ?? Constants.treeDataProperties.TREE_LEVEL_PROP;
        break;
      case "parentPropName":
        propName = treeDataOptions?.[optionName] ?? Constants.treeDataProperties.PARENT_PROP;
        break;
    }
    return propName;
  }
  function unflattenParentChildArrayToTree(flatArray, options) {
    const identifierPropName = getTreeDataOptionPropName(options, "identifierPropName");
    const childrenPropName = getTreeDataOptionPropName(options, "childrenPropName");
    const parentPropName = getTreeDataOptionPropName(options, "parentPropName");
    const levelPropName = getTreeDataOptionPropName(options, "levelPropName");
    const collapsedPropName = getTreeDataOptionPropName(options, "collapsedPropName");
    const inputArray = flatArray || [];
    const roots = [];
    const all = {};
    inputArray.forEach((item) => {
      all[item[identifierPropName]] = item;
      delete item[childrenPropName];
    });
    Object.keys(all).forEach((id) => {
      const item = all[id];
      if (!(parentPropName in item) || item[parentPropName] === null || item[parentPropName] === void 0 || item[parentPropName] === "") {
        roots.push(item);
      } else if (item[parentPropName] in all) {
        const p = all[item[parentPropName]];
        if (!(childrenPropName in p)) {
          p[childrenPropName] = [];
        }
        p[childrenPropName].push(item);
        if (p[collapsedPropName] === void 0) {
          p[collapsedPropName] = options?.initiallyCollapsed ?? false;
        }
      }
    });
    if (Array.isArray(options?.aggregators)) {
      options.aggregators.forEach((aggregator) => {
        addTreeLevelAndAggregatorsByMutation(roots, { childrenPropName, levelPropName, aggregator }, 0);
      });
    } else {
      addTreeLevelByMutation(roots, { childrenPropName, levelPropName }, 0);
    }
    return roots;
  }
  function findItemInTreeStructure(treeArray, predicate, childrenPropertyName) {
    if (!childrenPropertyName) {
      throw new Error('findItemInTreeStructure requires parameter "childrenPropertyName"');
    }
    const initialFind = treeArray.find(predicate);
    const elementsWithChildren = treeArray.filter((x2) => childrenPropertyName in x2 && x2[childrenPropertyName]);
    if (initialFind) {
      return initialFind;
    } else if (elementsWithChildren.length) {
      const childElements = [];
      for (const item of elementsWithChildren) {
        if (childrenPropertyName in item) {
          item[childrenPropertyName].forEach((el) => childElements.push(el));
        }
      }
      return findItemInTreeStructure(childElements, predicate, childrenPropertyName);
    }
    return void 0;
  }
  function decimalFormatted(input, minDecimal, maxDecimal, decimalSeparator = ".", thousandSeparator = "") {
    if (isNaN(+input)) {
      return input;
    }
    const minDec = minDecimal === void 0 ? 2 : minDecimal;
    const maxDec = maxDecimal === void 0 ? 2 : maxDecimal;
    let amount = String(Math.round(+input * Math.pow(10, maxDec)) / Math.pow(10, maxDec));
    if (amount.indexOf(".") < 0 && minDec > 0) {
      amount += ".";
    }
    while (amount.length - amount.indexOf(".") <= minDec) {
      amount += "0";
    }
    const decimalSplit = amount.split(".");
    let integerNumber;
    let decimalNumber;
    if (thousandSeparator) {
      integerNumber = decimalSplit.length >= 1 ? thousandSeparatorFormatted(decimalSplit[0], thousandSeparator) : void 0;
    } else {
      integerNumber = decimalSplit.length >= 1 ? decimalSplit[0] : amount;
    }
    if (decimalSplit.length > 1) {
      decimalNumber = decimalSplit[1];
    }
    let output = "";
    if (integerNumber !== void 0 && decimalNumber !== void 0) {
      output = `${integerNumber}${decimalSeparator}${decimalNumber}`;
    } else if (integerNumber !== void 0 && integerNumber !== null) {
      output = integerNumber;
    }
    return output;
  }
  function formatNumber(input, minDecimal, maxDecimal, wrapNegativeNumberInBraquets, symbolPrefix = "", symbolSuffix = "", decimalSeparator = ".", thousandSeparator = "") {
    if (isNaN(+input)) {
      return input;
    }
    const calculatedValue = Math.round(parseFloat(input) * 1e6) / 1e6;
    if (calculatedValue < 0) {
      const absValue = Math.abs(calculatedValue);
      if (wrapNegativeNumberInBraquets) {
        if (!isNaN(minDecimal) || !isNaN(maxDecimal)) {
          return `(${symbolPrefix}${decimalFormatted(absValue, minDecimal, maxDecimal, decimalSeparator, thousandSeparator)}${symbolSuffix})`;
        }
        const formattedValue = thousandSeparatorFormatted(`${absValue}`, thousandSeparator);
        return `(${symbolPrefix}${formattedValue}${symbolSuffix})`;
      } else {
        if (!isNaN(minDecimal) || !isNaN(maxDecimal)) {
          return `-${symbolPrefix}${decimalFormatted(absValue, minDecimal, maxDecimal, decimalSeparator, thousandSeparator)}${symbolSuffix}`;
        }
        const formattedValue = thousandSeparatorFormatted(`${absValue}`, thousandSeparator);
        return `-${symbolPrefix}${formattedValue}${symbolSuffix}`;
      }
    } else {
      if (!isNaN(minDecimal) || !isNaN(maxDecimal)) {
        return `${symbolPrefix}${decimalFormatted(input, minDecimal, maxDecimal, decimalSeparator, thousandSeparator)}${symbolSuffix}`;
      }
      const formattedValue = thousandSeparatorFormatted(`${input}`, thousandSeparator);
      return `${symbolPrefix}${formattedValue}${symbolSuffix}`;
    }
  }
  function getCellValueFromQueryFieldGetter(columnDef, dataContext, defaultValue) {
    if (typeof columnDef.queryFieldNameGetterFn === "function") {
      const queryFieldName = columnDef.queryFieldNameGetterFn(dataContext);
      if (queryFieldName?.indexOf(".") >= 0) {
        defaultValue = getDescendantProperty(dataContext, queryFieldName);
      } else {
        defaultValue = dataContext.hasOwnProperty(queryFieldName) ? dataContext[queryFieldName] : defaultValue;
      }
    }
    return defaultValue;
  }
  function getDescendantProperty(object, path) {
    if (!object || !path) {
      return object;
    }
    return path.split(".").reduce((obj, prop) => obj && obj[prop], object);
  }
  function getTranslationPrefix(gridOptions) {
    if (gridOptions?.translationNamespace) {
      return gridOptions.translationNamespace + (gridOptions.translationNamespaceSeparator || "");
    }
    return "";
  }
  function getColumnFieldType(columnDef) {
    return columnDef.outputType || columnDef.type || "string";
  }
  function getAllDateFieldTypes() {
    return [
      "date",
      "dateTime",
      "dateIso",
      "dateTimeIso",
      "dateTimeShortIso",
      "dateTimeIsoAmPm",
      "dateTimeIsoAM_PM",
      "dateEuro",
      "dateEuroShort",
      "dateTimeEuro",
      "dateTimeShortEuro",
      "dateTimeEuroAmPm",
      "dateTimeEuroAM_PM",
      "dateTimeEuroShort",
      "dateTimeEuroShortAmPm",
      "dateTimeEuroShortAM_PM",
      "dateUs",
      "dateUsShort",
      "dateTimeUs",
      "dateTimeShortUs",
      "dateTimeUsAmPm",
      "dateTimeUsAM_PM",
      "dateTimeUsShort",
      "dateTimeUsShortAmPm",
      "dateTimeUsShortAM_PM",
      "dateUtc"
    ];
  }
  function isColumnDateType(fieldType) {
    if (getAllDateFieldTypes().includes(fieldType)) {
      return true;
    }
    return false;
  }
  function mapOperatorType(operator) {
    let map2;
    switch (operator) {
      case "<":
      case "LT":
        map2 = "LT";
        break;
      case "<=":
      case "LE":
        map2 = "LE";
        break;
      case ">":
      case "GT":
        map2 = "GT";
        break;
      case ">=":
      case "GE":
        map2 = "GE";
        break;
      case "!=":
      case "NE":
        map2 = "NE";
        break;
      case "*":
      case "a*":
      case "StartsWith":
        map2 = "StartsWith";
        break;
      case "*z":
      case "EndsWith":
        map2 = "EndsWith";
        break;
      case "=":
      case "==":
      case "EQ":
        map2 = "EQ";
        break;
      case "IN":
        map2 = "IN";
        break;
      case "NIN":
      case "NOT_IN":
        map2 = "NOT_IN";
        break;
      case "<>":
      case "Not_Contains":
      case "NOT_CONTAINS":
        map2 = "Not_Contains";
        break;
      case "Contains":
      case "CONTAINS":
      default:
        map2 = "Contains";
        break;
    }
    return map2;
  }
  function mapOperatorToShorthandDesignation(operator) {
    let shortOperator = "";
    switch (operator) {
      case "GT":
      case ">":
        shortOperator = ">";
        break;
      case "GE":
      case ">=":
        shortOperator = ">=";
        break;
      case "LT":
      case "<":
        shortOperator = "<";
        break;
      case "LE":
      case "<=":
        shortOperator = "<=";
        break;
      case "NE":
      case "<>":
        shortOperator = "<>";
        break;
      case "=":
      case "==":
      case "EQ":
        shortOperator = "=";
        break;
      case "StartsWith":
      case "a*":
      case "*":
        shortOperator = "a*";
        break;
      case "EndsWith":
      case "*z":
        shortOperator = "*z";
        break;
      default:
        shortOperator = operator;
        break;
    }
    return shortOperator;
  }
  function mapOperatorByFieldType(fieldType) {
    let map2;
    if (isColumnDateType(fieldType)) {
      map2 = "EQ";
    } else {
      switch (fieldType) {
        case "unknown":
        case "string":
        case "text":
        case "password":
        case "readonly":
          map2 = "Contains";
          break;
        case "float":
        case "number":
        default:
          map2 = "EQ";
          break;
      }
    }
    return map2;
  }
  function objectWithoutKey(obj, omitKey) {
    return Object.keys(obj).reduce((result, objKey) => {
      if (objKey !== omitKey) {
        result[objKey] = obj[objKey];
      }
      return result;
    }, {});
  }
  function thousandSeparatorFormatted(inputValue, separator = ",") {
    if (inputValue !== null && inputValue !== void 0) {
      const stringValue = `${inputValue}`;
      const decimalSplit = stringValue.split(".");
      if (decimalSplit.length === 2) {
        return `${decimalSplit[0].replace(/\B(?=(\d{3})+(?!\d))/g, separator)}.${decimalSplit[1]}`;
      }
      return stringValue.replace(/\B(?=(\d{3})+(?!\d))/g, separator);
    }
    return inputValue;
  }
  function findOrDefault(array, logic, defaultVal = {}) {
    if (Array.isArray(array)) {
      return array.find(logic) || defaultVal;
    }
    return array;
  }
  function fetchAsPromise(input, rxjs) {
    return new Promise((resolve, reject) => {
      if (Array.isArray(input)) {
        resolve(input);
      } else if (input instanceof Promise) {
        input.then((response) => {
          if (Array.isArray(response)) {
            resolve(response);
          } else if (response?.status >= 200 && response.status < 300 && typeof response.json === "function") {
            if (response.bodyUsed) {
              const errorMsg = "[SlickGrid-Universal] The response body passed to Fetch was already read. Either pass the dataset from the Response or clone the response first using response.clone()";
              console.warn(errorMsg);
              resolve(null);
            } else {
              resolve(response.json());
            }
          } else if (response?.content) {
            resolve(response["content"]);
          } else {
            resolve(response);
          }
        }).catch((error) => reject(error));
      } else if (input && rxjs?.isObservable(input)) {
        resolve(castObservableToPromise(rxjs, input));
      } else {
        resolve(null);
      }
    });
  }
  function sortPresetColumns(allColumns, presetColumns) {
    const presetColumnMap = new Map(presetColumns.map((col, index2) => [col.id, { index: index2, column: col }]));
    return allColumns.map((column, originalIndex) => {
      const presetColumnData = presetColumnMap.get(column.id);
      const presetColumn = presetColumnData?.column;
      return {
        ...column,
        ...presetColumn && {
          cssClass: presetColumn.cssClass ?? column.cssClass,
          headerCssClass: presetColumn.headerCssClass ?? column.headerCssClass,
          width: presetColumn.width ?? column.width
        },
        hidden: !presetColumnMap.has(column.id) || (presetColumns.find((c) => c.id === column.id)?.hidden ?? false),
        _originalIndex: originalIndex,
        _presetIndex: presetColumnData?.index
      };
    }).sort((a, b) => {
      if (a._presetIndex !== void 0 && b._presetIndex !== void 0) {
        return a._presetIndex - b._presetIndex;
      }
      return a._originalIndex - b._originalIndex;
    }).map(({ _originalIndex, _presetIndex, ...column }) => column);
  }
  function unsubscribeAll(subscriptions) {
    if (Array.isArray(subscriptions)) {
      while (subscriptions.length > 0) {
        const subscription = subscriptions.pop();
        if (subscription?.unsubscribe) {
          subscription.unsubscribe();
        }
      }
    }
    return subscriptions;
  }

  // node_modules/@slickgrid-universal/common/dist/sortComparers/sortUtilities.js
  var sortUtilities_exports = {};
  __export(sortUtilities_exports, {
    sortByFieldType: () => sortByFieldType
  });

  // node_modules/@formkit/tempo/dist/iso8601.mjs
  var iso8601Match = /^([0-9]{4})-([0-1][0-9])(?:-([0-3][0-9]))?(?:[T ]?([0-2][0-9])(?::([0-5][0-9]))?(?::([0-5][0-9]))?)?(?:\.[0-9]+)?(Z|(?:\+|\-)[0-9]{2}:?[0-9]{2}(?::?[0-9]{2})?)?$/;
  function iso8601(date2) {
    const matches2 = date2.match(iso8601Match);
    if (matches2) {
      const month = Number(matches2[2]);
      if (month < 1 || month > 12) return false;
      if (typeof matches2[3] !== void 0) {
        const date22 = Number(matches2[3]);
        if (date22 < 1 || date22 > 31) return false;
      }
      if (typeof matches2[4] !== void 0) {
        const hours = Number(matches2[4]);
        if (hours < 0 || hours > 23) return false;
      }
      return true;
    }
    return false;
  }

  // node_modules/@formkit/tempo/dist/date.mjs
  function normalize(date2) {
    const matches2 = date2.match(iso8601Match);
    if (matches2 && typeof matches2[4] === "undefined") {
      return date2 += "T00:00:00";
    }
    return date2;
  }
  function date(date2) {
    if (!date2) {
      date2 = /* @__PURE__ */ new Date();
    }
    if (date2 instanceof Date) {
      return new Date(date2);
    }
    date2 = date2.trim();
    if (iso8601(date2)) {
      return new Date(normalize(date2));
    }
    throw new Error(`Non ISO 8601 compliant date (${date2}).`);
  }

  // node_modules/@formkit/tempo/dist/monthEnd.mjs
  function monthEnd(inputDate) {
    const d2 = date(inputDate);
    d2.setDate(1);
    d2.setMonth(d2.getMonth() + 1);
    d2.setDate(0);
    return d2;
  }

  // node_modules/@formkit/tempo/dist/monthDays.mjs
  function monthDays(inputDate) {
    const d2 = monthEnd(inputDate);
    return d2.getDate();
  }

  // node_modules/@formkit/tempo/dist/common.mjs
  var specDate = "1999-03-04T02:05:01.000Z";
  var memoParts = /* @__PURE__ */ new Map();
  var clockAgnostic = [
    ["YYYY", { year: "numeric" }],
    ["YY", { year: "2-digit" }],
    ["MMMM", { month: "long" }],
    ["MMM", { month: "short" }],
    ["MM", { month: "2-digit" }],
    ["M", { month: "numeric" }],
    ["DD", { day: "2-digit" }],
    ["D", { day: "numeric" }],
    ["dddd", { weekday: "long" }],
    ["ddd", { weekday: "short" }],
    ["d", { weekday: "narrow" }],
    ["mm", { minute: "2-digit" }],
    ["m", { minute: "numeric" }],
    ["ss", { second: "2-digit" }],
    ["s", { second: "numeric" }],
    ["ZZ", { timeZoneName: "long" }],
    ["Z", { timeZoneName: "short" }]
  ];
  var clock24 = [
    ["HH", { hour: "2-digit" }],
    ["H", { hour: "numeric" }]
  ];
  var clock12 = [
    ["hh", { hour: "2-digit" }],
    ["h", { hour: "numeric" }],
    ["a", { dayPeriod: "narrow" }],
    ["A", { dayPeriod: "narrow" }]
  ];
  var fractionalSeconds = [
    ["SSS", { fractionalSecond: "3-digit" }]
  ];
  var fixedLength = {
    DD: 2,
    HH: 2,
    MM: 2,
    YY: 2,
    YYYY: 4,
    hh: 2,
    mm: 2,
    ss: 2
  };
  function fixedLengthByOffset(offsetString) {
    if (/^[+-]\d{2}:\d{2}:\d{2}/.test(offsetString)) {
      return 9;
    }
    if (/^[+-]\d{6}/.test(offsetString)) {
      return 8;
    }
    if (/^[+-]\d{2}:\d{2}/.test(offsetString)) {
      return 6;
    }
    if (/^[+-]\d{4}/.test(offsetString)) {
      return 5;
    }
    throw new Error("Invalid offset format");
  }
  var genitiveTokens = ["MMMM", "MMM", "dddd", "ddd"];
  var tokens = /* @__PURE__ */ new Map(
    /* @__PURE__ */ [...clockAgnostic, ...clock24, ...clock12, ...fractionalSeconds].map((format2) => {
      return [format2[0], format2];
    })
  );
  var dayPeriodMap = /* @__PURE__ */ new Map();
  var styles = ["full", "long", "medium", "short"];
  var two = (n) => String(n).padStart(2, "0");
  var four = (n) => String(n).padStart(2, "0");
  function normStr(part) {
    if (part.type === "literal") {
      part.value = part.value.normalize("NFKC");
    }
    return part;
  }
  function fill(inputDate, parts2, locale, genitive = false, offset2 = null) {
    const partMap = createPartMap(inputDate, parts2, locale, genitive);
    const d2 = date(inputDate);
    function value({ partName, partValue, token }) {
      if (partName === "literal") return partValue;
      const value2 = partMap[partName];
      if (partName === "hour" && token === "H") {
        return value2.replace(/^0/, "") || "0";
      }
      if (["mm", "ss", "MM"].includes(token) && value2.length === 1) {
        return `0${value2}`;
      }
      if (partName === "dayPeriod") {
        const p = ap(d2.getUTCHours() < 12 ? "am" : "pm", locale);
        return token === "A" ? p.toUpperCase() : p.toLowerCase();
      }
      if (partName === "fractionalSecond") {
        return String(d2.getUTCMilliseconds()).padStart(3, "0");
      }
      if (partName === "timeZoneName") {
        return offset2 != null ? offset2 : minsToOffset(-1 * d2.getTimezoneOffset(), token);
      }
      return value2;
    }
    return parts2.map((part) => {
      return {
        ...part,
        value: value(part)
      };
    });
  }
  function createPartMap(inputDate, parts2, locale, genitive = false) {
    const d2 = date(inputDate);
    const hour12 = parts2.filter((part) => part.hour12);
    const hour24 = parts2.filter((part) => !part.hour12);
    const valueParts = [];
    const genitiveParts = [];
    function addValues(requestedParts, hour122 = false) {
      const preciseLocale = `${locale}-u-hc-${hour122 ? "h12" : "h23"}`;
      valueParts.push(
        ...new Intl.DateTimeFormat(
          preciseLocale,
          requestedParts.reduce(
            (options, part) => {
              if (part.partName === "literal") return options;
              if (genitive && genitiveTokens.includes(part.token)) {
                genitiveParts.push(part);
              }
              return Object.assign(options, part.option);
            },
            { timeZone: "UTC" }
          )
        ).formatToParts(d2).map(normStr)
      );
      if (genitive && genitiveParts.length) {
        for (const part of genitiveParts) {
          let formattedParts = [];
          switch (part.token) {
            case "MMMM":
              formattedParts = new Intl.DateTimeFormat(preciseLocale, {
                dateStyle: "long",
                timeZone: "UTC"
              }).formatToParts(d2).map(normStr);
              break;
            case "MMM":
              formattedParts = new Intl.DateTimeFormat(preciseLocale, {
                dateStyle: "medium",
                timeZone: "UTC"
              }).formatToParts(d2).map(normStr);
              break;
          }
          const genitiveFormattedPart = formattedParts.find((p) => p.type === part.partName);
          const index2 = valueParts.findIndex((p) => p.type === part.partName);
          if (genitiveFormattedPart && index2 > -1) {
            valueParts[index2] = genitiveFormattedPart;
          }
        }
      }
    }
    if (hour12.length) addValues(hour12, true);
    if (hour24.length) addValues(hour24);
    return valueParts.reduce((map2, part) => {
      map2[part.type] = part.value;
      return map2;
    }, {});
  }
  function secsToOffset(totalSecs, token = "Z") {
    const sign = totalSecs < 0 ? "-" : "+";
    const absSecs = Math.abs(totalSecs);
    const hours = String(Math.floor(absSecs / 3600)).padStart(2, "0");
    const mins = String(Math.floor(absSecs % 3600 / 60)).padStart(2, "0");
    const secs = Math.round(absSecs % 60);
    if (token === "ZZ") {
      return secs === 0 ? `${sign}${hours}${mins}` : `${sign}${hours}${mins}${String(secs).padStart(2, "0")}`;
    }
    return secs === 0 ? `${sign}${hours}:${mins}` : `${sign}${hours}:${mins}:${String(secs).padStart(2, "0")}`;
  }
  function minsToOffset(timeDiffInMins, token = "Z") {
    return secsToOffset(timeDiffInMins * 60, token);
  }
  function offsetToSecs(offset2, token) {
    validOffset(offset2, token);
    const match = offset2.match(/([+-])([0-3][0-9]):?([0-5][0-9])(?::?([0-5][0-9]))?/);
    const [_2, sign, hours, mins, secs = "0"] = match;
    const totalSecs = Number(hours) * 3600 + Number(mins) * 60 + Number(secs);
    return sign === "+" ? totalSecs : -totalSecs;
  }
  function validOffset(offset2, token = "Z") {
    const valid = ((token2) => {
      switch (token2) {
        case "Z":
          return /^([+-])[0-3][0-9]:[0-5][0-9](?::[0-5][0-9])?$/.test(offset2);
        case "ZZ":
          return /^([+-])[0-3][0-9][0-5][0-9](?:[0-5][0-9])?$/.test(offset2);
      }
    })(token);
    if (!valid) throw new Error(`Invalid offset: ${offset2}`);
    return offset2;
  }
  function escapeTokens(str) {
    return clockAgnostic.concat(clock24).concat(clock12).concat(fractionalSeconds).sort((a, b) => a[0].length > b[0].length ? 1 : -1).reduce((target, part) => {
      return target.replace(part[0], `\\${part[0]}`);
    }, str);
  }
  function isNumeric(part) {
    return ["numeric", "2-digit"].includes(part.partValue);
  }
  function validate(parts2) {
    let lastPart = void 0;
    for (const part of parts2) {
      if (part.partName === "literal" && !isNaN(parseFloat(part.partValue))) {
        throw new Error(`Numbers in format (${part.partValue}).`);
      }
      if (lastPart && lastPart.partName !== "literal" && part.partName !== "literal") {
        if (!(lastPart.token in fixedLength) && !(part.token in fixedLength) && !(isNumeric(lastPart) && part.token.toLowerCase() === "a") && lastPart.token !== "SSS") {
          throw new Error(`Illegal adjacent tokens (${lastPart.token}, ${part.token})`);
        }
      }
      lastPart = part;
    }
    return parts2;
  }
  function getOffsetFormat(format2) {
    if (typeof format2 === "string") {
      return format2.includes("ZZ") ? "ZZ" : "Z";
    }
    return "time" in format2 && format2.time === "full" ? "Z" : "ZZ";
  }

  // node_modules/@formkit/tempo/dist/ap.mjs
  function ap(ampm, locale) {
    const l = dayPeriodMap.get(locale);
    if (l && l[ampm]) return l[ampm];
    const specimen = new Date(specDate);
    specimen.setUTCHours(ampm === "am" ? 5 : 20);
    const subparts = new Intl.DateTimeFormat(locale, {
      timeStyle: "full",
      timeZone: "UTC",
      hour12: true
    }).formatToParts(specimen).map(normStr);
    const period = subparts.find((part) => part.type === "dayPeriod");
    if (period) {
      const localePeriods = l || {};
      dayPeriodMap.set(
        locale,
        Object.assign(localePeriods, { [ampm]: period.value })
      );
      return period.value;
    }
    return ampm;
  }

  // node_modules/@formkit/tempo/dist/applyOffset.mjs
  function applyOffset(dateInput, offset2 = "+00:00") {
    const d2 = date(dateInput);
    const len = fixedLengthByOffset(offset2);
    const token = len === 5 || len === 8 ? "ZZ" : "Z";
    const timeDiffInSecs = offsetToSecs(offset2, token);
    return new Date(d2.getTime() + timeDiffInSecs * 1e3);
  }

  // node_modules/@formkit/tempo/dist/deviceTZ.mjs
  function deviceTZ() {
    return Intl.DateTimeFormat().resolvedOptions().timeZone;
  }

  // node_modules/@formkit/tempo/dist/offset.mjs
  function relativeTime(d2, timeZone) {
    const utcParts = new Intl.DateTimeFormat("en-US", {
      era: "short",
      year: "numeric",
      month: "numeric",
      day: "numeric",
      hour: "numeric",
      minute: "numeric",
      second: "numeric",
      timeZone,
      hourCycle: "h23"
    }).formatToParts(d2);
    const p = {};
    utcParts.forEach((part) => {
      if (part.type !== "literal") p[part.type] = part.value;
    });
    const year = p.era === "BC" ? 1 - Number(p.year) : Number(p.year);
    const result = new Date(Date.UTC(0, 0, 1, Number(p.hour), Number(p.minute), Number(p.second)));
    result.setUTCFullYear(year, Number(p.month) - 1, Number(p.day));
    return result;
  }
  function offset(utcTime, tzA = "UTC", tzB = "device", timeZoneToken = "Z") {
    var _a;
    tzB = tzB === "device" ? (_a = deviceTZ()) != null ? _a : "utc" : tzB;
    const d2 = date(utcTime);
    const timeA = relativeTime(d2, tzA);
    const timeB = relativeTime(d2, tzB);
    const timeDiffInSecs = Math.round((timeB.getTime() - timeA.getTime()) / 1e3);
    return secsToOffset(timeDiffInSecs, timeZoneToken);
  }

  // node_modules/@formkit/tempo/dist/dayStart.mjs
  function dayStart(inputDate) {
    const d2 = date(inputDate);
    d2.setHours(0, 0, 0, 0);
    return d2;
  }

  // node_modules/@formkit/tempo/dist/parts.mjs
  function parts(format2, locale) {
    if (styles.includes(format2) || typeof format2 === "object") {
      return styleParts(format2, locale);
    }
    let f2 = format2;
    let match = 0;
    const testPattern = (pattern) => {
      if (!pattern[2]) pattern[2] = new RegExp(`(.)?(${pattern[0]})`, "g");
      if (pattern[2].test(f2)) {
        let didAdd = 0;
        f2 = f2.replace(pattern[2], (_2, prefix, actualMatch) => {
          if (prefix === "\\") return actualMatch;
          return `${typeof prefix === "string" ? prefix : ""}{!${didAdd++ ? match : match++}!}`;
        });
        return !!didAdd;
      }
      return false;
    };
    function validate2(patterns) {
      const parts3 = patterns.map((part) => part.partName);
      const deduped = new Set(parts3);
      if (parts3.length > deduped.size) {
        throw new Error(`Cannot reuse format tokens.`);
      }
      return patterns;
    }
    function createPart(hour12, [token, option2, exp]) {
      const partName = Object.keys(option2)[0];
      const partValue = option2[partName];
      return {
        option: option2,
        partName,
        partValue,
        token,
        pattern: exp,
        hour12
      };
    }
    const found24Patterns = clockAgnostic.filter(testPattern).concat(clock24.filter(testPattern)).concat(fractionalSeconds.filter(testPattern)).map(createPart.bind(null, false));
    const parts2 = validate2(
      found24Patterns.concat(
        clock12.filter(testPattern).map(createPart.bind(null, true))
      )
    );
    const extractIndex = /^\{!(\d+)!\}$/;
    return f2.split(/(\{!\d+!\})/).map((match2) => {
      const hasIndex = match2.match(extractIndex);
      if (hasIndex) {
        return parts2[Number(hasIndex[1])];
      }
      return {
        option: { literal: match2 },
        partName: "literal",
        partValue: match2,
        token: match2,
        pattern: new RegExp(""),
        hour12: false
      };
    }).filter((part) => !(part.partName === "literal" && part.partValue === ""));
  }
  function styleParts(format2, locale) {
    const options = {
      timeZone: "UTC"
    };
    if (typeof format2 === "string") {
      options.dateStyle = format2;
    } else {
      if ("date" in format2) options.dateStyle = format2.date;
      if ("time" in format2) options.timeStyle = format2.time;
    }
    const formatter = new Intl.DateTimeFormat(locale, options);
    const segments = formatter.formatToParts(new Date(specDate)).map(normStr);
    const hourTypeSegments = formatter.formatToParts(/* @__PURE__ */ new Date("1999-04-05T23:05:01.000Z")).map(normStr);
    const hourPart = hourTypeSegments.find((segment) => segment.type === "hour");
    const hourType = hourPart && hourPart.value === "23" ? 24 : 12;
    return segments.map((part) => {
      const partName = part.type;
      const formatPattern = guessPattern(
        part.type,
        part.value,
        locale,
        part.type === "hour" ? hourType : void 0,
        options
      );
      if (formatPattern === void 0) return;
      const partValue = formatPattern[1][partName];
      if (!partValue) return;
      if (!formatPattern[2])
        formatPattern[2] = new RegExp(`${formatPattern[0]}`, "g");
      return {
        option: { [partName]: partValue },
        partName,
        partValue,
        token: formatPattern[0],
        pattern: formatPattern[2],
        hour12: hourType === 12
      };
    }).filter((part) => !!part);
  }
  function guessPattern(partName, partValue, locale, hour, options) {
    const l = partValue.length;
    const n = !isNaN(Number(partValue));
    let style;
    switch (partName) {
      case "year":
        return l === 2 ? tokens.get("YY") : tokens.get("YYYY");
      case "month":
        if (n) return l === 1 ? tokens.get("M") : tokens.get("MM");
        style = partStyle(locale, partName, partValue);
        switch (style) {
          case "long":
            return tokens.get("MMMM");
          default:
            return tokens.get("MMM");
        }
      case "day":
        return l === 1 ? tokens.get("D") : tokens.get("DD");
      case "weekday":
        style = partStyle(locale, partName, partValue);
        switch (style) {
          case "narrow":
            return tokens.get("d");
          case "short":
            return tokens.get("ddd");
          default:
            return tokens.get("dddd");
        }
      case "hour":
        if (hour === 12) return l === 1 ? tokens.get("h") : tokens.get("hh");
        return l === 1 ? tokens.get("H") : tokens.get("HH");
      case "minute":
        return l === 1 ? tokens.get("m") : tokens.get("mm");
      case "second":
        return l === 1 ? tokens.get("s") : tokens.get("ss");
      case "dayPeriod":
        return /^[A-Z]+$/u.test(partValue) ? tokens.get("A") : tokens.get("a");
      case "literal":
        return [partValue, { literal: partValue }, new RegExp("")];
      case "timeZoneName":
        return options.timeStyle === "full" ? tokens.get("Z") : tokens.get("ZZ");
      default:
        return void 0;
    }
  }
  function partStyle(locale, part, value) {
    if (!memoParts.has(locale)) {
      const date2 = new Date(specDate);
      const weekdays = [3, 8, 9, 7, 6, 4, 3];
      const parts2 = ["weekday", "month", "dayPeriod"];
      const partStyles = ["long", "short", "narrow"];
      const formats2 = {};
      for (let i = 0; i < 12; i++) {
        date2.setMonth(0 + i);
        if (i in weekdays) date2.setDate(weekdays[i]);
        date2.setUTCHours(8 + i);
        for (const style of partStyles) {
          const segments = new Intl.DateTimeFormat(
            locale,
            parts2.reduce(
              (options, part2) => Object.assign(options, { [part2]: style }),
              { hour12: true, timeZone: "UTC" }
            )
          ).formatToParts(date2).map(normStr);
          if (style === "long" || style === "short") {
            const genitiveFormattedParts = new Intl.DateTimeFormat(locale, {
              dateStyle: style === "short" ? "medium" : "long",
              timeZone: "UTC"
            }).formatToParts(date2).map(normStr);
            const genitiveMonth = genitiveFormattedParts.find(
              (part2) => part2.type === "month"
            );
            const index2 = segments.findIndex((part2) => part2.type === "month");
            if (index2 > -1 && genitiveMonth) segments[index2] = genitiveMonth;
          }
          segments.forEach((part2) => {
            if (part2.type === "literal") return;
            const type = part2.type;
            formats2[type] = Object.assign(formats2[type] || {}, {
              [part2.value]: style
            });
          });
        }
      }
      memoParts.set(locale, formats2);
    }
    const formats = memoParts.get(locale);
    return formats ? formats[part][value] : void 0;
  }

  // node_modules/@formkit/tempo/dist/removeOffset.mjs
  function removeOffset(dateInput, offset2 = "+00:00") {
    const positive = offset2.slice(0, 1) === "+";
    return applyOffset(
      dateInput,
      offset2.replace(positive ? "+" : "-", positive ? "-" : "+")
    );
  }

  // node_modules/@formkit/tempo/dist/deviceLocale.mjs
  function deviceLocale() {
    return Intl.DateTimeFormat().resolvedOptions().locale;
  }

  // node_modules/@formkit/tempo/dist/format.mjs
  function format(inputDateOrOptions, format2 = "long", locale = "device", genitive = false, partFilter) {
    let tz, forceOffset;
    if (typeof inputDateOrOptions === "object" && !(inputDateOrOptions instanceof Date)) {
      ;
      ({
        date: inputDateOrOptions,
        format: format2,
        locale,
        genitive,
        partFilter,
        tz
      } = inputDateOrOptions);
    }
    if (format2 === "ISO8601") return date(inputDateOrOptions).toISOString();
    if (tz) {
      forceOffset = offset(inputDateOrOptions, "utc", tz, getOffsetFormat(format2));
    }
    tz != null ? tz : tz = deviceTZ();
    if ((tz == null ? void 0 : tz.toLowerCase()) !== "utc") {
      inputDateOrOptions = removeOffset(
        inputDateOrOptions,
        offset(inputDateOrOptions, tz, "utc")
      );
    }
    if (!locale || locale === "device") {
      locale = deviceLocale();
    }
    return fill(
      inputDateOrOptions,
      parts(format2, locale).filter(partFilter != null ? partFilter : (() => true)),
      locale,
      genitive,
      forceOffset
    ).map((p) => p.value).join("");
  }

  // node_modules/@formkit/tempo/dist/formatStr.mjs
  function formatStr(format2, locale = "en", escapeLiterals = false, filterParts = () => true) {
    return parts(format2, locale).filter(filterParts).reduce(
      (f2, p) => f2 += escapeLiterals && p.partName === "literal" ? escapeTokens(p.token) : p.token,
      ""
    ).normalize("NFKC");
  }

  // node_modules/@formkit/tempo/dist/fourDigitYear.mjs
  function fourDigitYear(value) {
    const y2 = (/* @__PURE__ */ new Date()).getFullYear();
    const currentYear = y2 % 100;
    const century = Math.floor(y2 / 100);
    const parsedYear = Number(value);
    return (century + (parsedYear > currentYear + 20 ? -1 : 0)) * 100 + parsedYear;
  }

  // node_modules/@formkit/tempo/dist/range.mjs
  function range(token, locale = "en", genitive = false) {
    const r2 = (n, c) => Array(n).fill("").map((_2, i) => `${c(i)}`);
    if (token === "M") return r2(12, (i) => i + 1);
    if (token === "MM")
      return r2(12, (i) => {
        const m = i + 1;
        return m < 10 ? `0${m}` : m;
      });
    if (token.startsWith("M"))
      return range("MM").map(
        (m) => format(`2000-${m}-05`, token, locale, genitive)
      );
    if (token.startsWith("d"))
      return r2(7, (i) => `0${i + 2}`).map(
        (d2) => format(`2022-10-${d2}`, token, locale)
      );
    if (token === "a")
      return [ap("am", locale).toLowerCase(), ap("pm", locale).toLowerCase()];
    if (token === "A")
      return [ap("am", locale).toUpperCase(), ap("pm", locale).toUpperCase()];
    if (token.startsWith("Y")) {
      const year = (/* @__PURE__ */ new Date()).getFullYear();
      return r2(120, (i) => i + 1).reduce(
        (ranges, i) => {
          if (i !== "120")
            ranges.push(format(`${year + Number(i)}-06-06`, token, locale));
          ranges.unshift(format(`${year - Number(i)}-06-06`, token, locale));
          return ranges;
        },
        [format(`${year}-06-06`, token, locale)]
      );
    }
    if (token.startsWith("D"))
      return r2(31, (i) => `${token === "DD" && i < 9 ? "0" : ""}${i + 1}`);
    if (token.startsWith("H"))
      return r2(24, (i) => `${token === "HH" && i < 10 ? "0" : ""}${i}`);
    if (token.startsWith("h"))
      return r2(12, (i) => `${token === "hh" && i < 9 ? "0" : ""}${i + 1}`);
    if (token.startsWith("m") || token.startsWith("s"))
      return r2(60, (i) => `${token.length > 1 && i < 10 ? "0" : ""}${i}`);
    return [];
  }

  // node_modules/@formkit/tempo/dist/parse.mjs
  function parse(dateStrOrOptions, format2 = "ISO8601", locale = "device") {
    let partFilter = () => true;
    let dateStr;
    let dateOverflow = "backward";
    if (typeof dateStrOrOptions === "object") {
      ;
      ({
        date: dateStr,
        format: format2 = "ISO8601",
        locale = "device",
        dateOverflow = "backward",
        partFilter = () => true
      } = dateStrOrOptions);
    } else {
      dateStr = dateStrOrOptions;
    }
    if (!dateStr) throw new Error("parse() requires a date string.");
    const invalid = () => {
      throw new Error(
        `Date (${dateStr}) does not match format (${formatStr(format2, locale)})`
      );
    };
    if (format2 === "ISO8601") return date(dateStr);
    const genitive = styles.includes(format2) || typeof format2 === "object";
    const formatParts = validate(parts(format2, locale).filter(partFilter));
    if (!formatParts.length) throw new Error("parse() requires a pattern.");
    let parsedParts;
    try {
      parsedParts = parseParts(dateStr, formatParts);
    } catch {
      return invalid();
    }
    const now = /* @__PURE__ */ new Date();
    const parsed = /* @__PURE__ */ new Map([
      ["YYYY", now.getFullYear()],
      ["MM", now.getMonth() + 1],
      ["DD", now.getDate()],
      ["HH", 0],
      ["mm", 0],
      ["ss", 0],
      ["SSS", 0]
    ]);
    let a = null;
    let offset2 = "";
    parsedParts.forEach((part) => {
      if (part.partName === "literal") return;
      if (part.token === part.value) return invalid();
      const v2 = Number(part.value);
      if (part.token === "SSS") {
        const digits = part.value.padEnd(3, "0").slice(0, 3);
        parsed.set("SSS", Number(digits));
      } else if (parsed.has(part.token)) {
        parsed.set(part.token, v2);
      } else if (part.token === "YY") {
        parsed.set("YYYY", fourDigitYear(part.value));
      } else {
        const t = part.token;
        if (t.startsWith("d")) {
          return;
        } else if (t === "D") {
          parsed.set("DD", v2);
        } else if (t === "H" || t.startsWith("h")) {
          parsed.set("HH", v2);
        } else if (t === "M") {
          parsed.set("MM", v2);
        } else if (t === "a" || t === "A") {
          a = part.value.toLowerCase() === ap("am", locale).toLowerCase();
        } else if (t === "Z" || t === "ZZ") {
          offset2 = validOffset(part.value, t);
        } else {
          const values = range(t, locale, genitive);
          const index2 = values.indexOf(part.value);
          if (index2 !== -1) {
            switch (t) {
              case "MMM":
              case "MMMM":
                parsed.set("MM", index2 + 1);
                break;
            }
          }
        }
      }
    });
    let hours = parsed.get("HH") || 0;
    if (a === false) {
      hours += hours === 12 ? 0 : 12;
      parsed.set("HH", hours === 24 ? 0 : hours);
    } else if (a === true && hours === 12) {
      parsed.set("HH", 0);
    }
    parsed.set("MM", (parsed.get("MM") || 1) - 1);
    let [Y, M2, D2, h2, m, s, ms] = Array.from(parsed.values());
    const maxDaysInMonth = monthDays(/* @__PURE__ */ new Date(`${four(Y)}-${two(M2 + 1)}-10`));
    if (maxDaysInMonth < D2 && dateOverflow === "throw")
      throw new Error(`Invalid date ${four(Y)}-${two(M2 + 1)}-${two(D2)}`);
    D2 = dateOverflow === "backward" ? Math.min(D2, maxDaysInMonth) : D2;
    const msStr = String(ms).padStart(3, "0");
    if (offset2) {
      const isoStringUtc = `${four(Y)}-${two(M2 + 1)}-${two(D2)}T${two(h2)}:${two(m)}:${two(s)}.${msStr}Z`;
      const d22 = new Date(isoStringUtc);
      if (!isFinite(+d22)) return invalid();
      const len = fixedLengthByOffset(offset2);
      const token = len === 5 || len === 8 ? "ZZ" : "Z";
      const offsetSecs = offsetToSecs(offset2, token);
      return new Date(d22.getTime() - offsetSecs * 1e3);
    }
    const isoString = `${four(Y)}-${two(M2 + 1)}-${two(D2)}T${two(h2)}:${two(m)}:${two(s)}.${msStr}`;
    const d2 = new Date(isoString);
    if (isFinite(+d2)) return d2;
    return invalid();
  }
  function parseParts(dateStr, formatParts) {
    let i = 0;
    const advance = (parts2) => [
      parts2[i++],
      parts2[i]
    ];
    let pos = 0;
    const parsed = [];
    let n = void 0;
    do {
      const [current, next] = advance(formatParts);
      n = next;
      let len = 1;
      if (current.partName === "literal") {
        len = current.partValue.length;
      } else if (current.partName === "timeZoneName") {
        len = fixedLengthByOffset(dateStr.substring(pos));
      } else if (current.token === "SSS") {
        let end = pos;
        while (end < dateStr.length && /\d/.test(dateStr.charAt(end))) {
          end++;
        }
        len = end - pos;
      } else if (current.token in fixedLength) {
        len = fixedLength[current.token];
      } else if (next) {
        if (next.partName === "literal") {
          len = dateStr.indexOf(next.partValue, pos) - pos;
          if (len < 0) throw new Error();
        } else if (next.partName === "dayPeriod") {
          for (let i2 = 1; i2 <= 4; i2++) {
            if (isNaN(Number(dateStr.charAt(pos + i2)))) {
              len = i2;
              break;
            }
          }
        } else {
          const nextChar = dateStr.substring(pos).search(/\d/);
          if (nextChar !== -1) len = pos + nextChar;
        }
      } else {
        len = dateStr.length;
      }
      parsed.push({ ...current, value: dateStr.substring(pos, pos + len) });
      pos += len;
    } while (n);
    return parsed;
  }

  // node_modules/@slickgrid-universal/common/dist/services/dateUtils.js
  function mapTempoDateFormatWithFieldType(fieldType, options) {
    let map2;
    const withZeroPadding = options?.withZeroPadding ?? false;
    switch (fieldType) {
      case "dateTime":
      case "dateTimeIso":
        map2 = "YYYY-MM-DD HH:mm:ss";
        break;
      case "dateTimeIsoAmPm":
        map2 = "YYYY-MM-DD hh:mm:ss a";
        break;
      case "dateTimeIsoAM_PM":
        map2 = "YYYY-MM-DD hh:mm:ss A";
        break;
      case "dateTimeShortIso":
        map2 = "YYYY-MM-DD HH:mm";
        break;
      // all Euro Formats (date/month/year)
      case "dateEuro":
        map2 = "DD/MM/YYYY";
        break;
      case "dateEuroShort":
        map2 = withZeroPadding ? "DD/MM/YY" : "D/M/YY";
        break;
      case "dateTimeEuro":
        map2 = "DD/MM/YYYY HH:mm:ss";
        break;
      case "dateTimeShortEuro":
        map2 = withZeroPadding ? "DD/MM/YYYY HH:mm" : "D/M/YYYY H:m";
        break;
      case "dateTimeEuroAmPm":
        map2 = "DD/MM/YYYY hh:mm:ss a";
        break;
      case "dateTimeEuroAM_PM":
        map2 = "DD/MM/YYYY hh:mm:ss A";
        break;
      case "dateTimeEuroShort":
        map2 = withZeroPadding ? "DD/MM/YY HH:mm:ss" : "D/M/YY H:m:s";
        break;
      case "dateTimeEuroShortAmPm":
        map2 = withZeroPadding ? "DD/MM/YY hh:mm:ss a" : "D/M/YY h:m:s a";
        break;
      case "dateTimeEuroShortAM_PM":
        map2 = withZeroPadding ? "DD/MM/YY hh:mm:ss A" : "D/M/YY h:m:s A";
        break;
      // all US Formats (month/date/year)
      case "dateUs":
        map2 = "MM/DD/YYYY";
        break;
      case "dateUsShort":
        map2 = withZeroPadding ? "MM/DD/YY" : "M/D/YY";
        break;
      case "dateTimeUs":
        map2 = "MM/DD/YYYY HH:mm:ss";
        break;
      case "dateTimeUsAmPm":
        map2 = "MM/DD/YYYY hh:mm:ss a";
        break;
      case "dateTimeUsAM_PM":
        map2 = "MM/DD/YYYY hh:mm:ss A";
        break;
      case "dateTimeUsShort":
        map2 = withZeroPadding ? "MM/DD/YY HH:mm:ss" : "M/D/YY H:m:s";
        break;
      case "dateTimeUsShortAmPm":
        map2 = withZeroPadding ? "MM/DD/YY hh:mm:ss a" : "M/D/YY h:m:s a";
        break;
      case "dateTimeUsShortAM_PM":
        map2 = withZeroPadding ? "MM/DD/YY hh:mm:ss A" : "M/D/YY h:m:s A";
        break;
      case "dateTimeShortUs":
        map2 = withZeroPadding ? "MM/DD/YYYY HH:mm" : "M/D/YYYY H:m";
        break;
      case "dateUtc":
        map2 = "ISO8601";
        break;
      case "date":
      case "dateIso":
      default:
        map2 = options?.withDefaultIso8601 ? "ISO8601" : "YYYY-MM-DD";
        break;
    }
    return map2;
  }
  function formatDateByFieldType(inputDate, inputFieldType, outputFieldType) {
    const inputFormat = inputFieldType ? mapTempoDateFormatWithFieldType(inputFieldType) : void 0;
    const outputFormat = mapTempoDateFormatWithFieldType(outputFieldType);
    const date2 = inputDate instanceof Date ? inputDate : tryParseDate(inputDate, inputFormat);
    if (date2 && inputDate !== void 0) {
      if (outputFieldType === "dateUtc") {
        return date2.toISOString();
      }
      return format(date2, outputFormat, "en-US");
    }
    return "";
  }
  function tryParseDate(inputDate, inputFormat, strict = false) {
    try {
      if (!inputDate) {
        return false;
      }
      const d2 = inputDate instanceof Date ? inputDate : parse({
        date: inputDate,
        format: inputFormat,
        dateOverflow: strict ? "throw" : "backward",
        locale: "en-US"
      });
      return d2.getFullYear() > 1e3 ? d2 : false;
    } catch (_e) {
      return false;
    }
  }
  function toUtcDate(inputDate) {
    return removeOffset(inputDate, offset(inputDate, "utc"));
  }

  // node_modules/@slickgrid-universal/common/dist/sortComparers/dateUtilities.js
  function compareDates(value1, value2, sortDirection, format2, strict) {
    let diff2 = 0;
    if (value1 === value2) {
      diff2 = 0;
    } else {
      const date1 = tryParseDate(value1, format2, strict) || new Date(1001, 1, 1);
      const date2 = tryParseDate(value2, format2, strict) || new Date(1001, 1, 1);
      diff2 = date1.valueOf() - date2.valueOf();
    }
    return sortDirection * diff2;
  }
  function getAssociatedDateSortComparer(fieldType) {
    const FORMAT = fieldType === "date" ? void 0 : mapTempoDateFormatWithFieldType(fieldType);
    return ((value1, value2, sortDirection) => {
      if (FORMAT === void 0) {
        return compareDates(value1, value2, sortDirection, FORMAT, false);
      }
      return compareDates(value1, value2, sortDirection, FORMAT, true);
    });
  }

  // node_modules/@slickgrid-universal/common/dist/sortComparers/booleanSortComparer.js
  var booleanSortComparer = (value1, value2, sortDirection = SortDirectionNumber.neutral) => {
    let position = 0;
    if (value1 === value2) {
      position = 0;
    } else if (value1 === null) {
      position = -1;
    } else if (value2 === null) {
      position = 1;
    } else {
      if (sortDirection) {
        position = value1 < value2 ? -1 : 1;
      } else {
        position = value1 < value2 ? 1 : -1;
      }
    }
    return sortDirection * position;
  };

  // node_modules/@slickgrid-universal/common/dist/sortComparers/objectStringSortComparer.js
  var objectStringSortComparer = (value1, value2, sortDirection = SortDirectionNumber.neutral, sortColumn, gridOptions) => {
    if (!sortColumn || !sortColumn.dataKey) {
      throw new Error('Sorting a "FieldType.object" requires you to provide the "dataKey" (object property name) of the object so that we can use it to sort correctly');
    }
    const stringValue1 = value1?.hasOwnProperty(sortColumn.dataKey) ? value1[sortColumn.dataKey] : value1;
    const stringValue2 = value2?.hasOwnProperty(sortColumn.dataKey) ? value2[sortColumn.dataKey] : value2;
    const checkForUndefinedValues = sortColumn?.valueCouldBeUndefined ?? gridOptions?.cellValueCouldBeUndefined ?? false;
    let position = 0;
    if (typeof value1 !== "object") {
      position = -Infinity;
    } else if (typeof value2 !== "object") {
      position = Infinity;
    } else if (stringValue1 === null || checkForUndefinedValues && stringValue1 === void 0) {
      position = -1;
    } else if (stringValue2 === null || checkForUndefinedValues && stringValue2 === void 0) {
      position = 1;
    } else if (stringValue1 === stringValue2) {
      position = 0;
    } else if (sortDirection) {
      position = stringValue1 < stringValue2 ? -1 : 1;
    } else {
      position = stringValue1 < stringValue2 ? 1 : -1;
    }
    return sortDirection * position;
  };

  // node_modules/@slickgrid-universal/common/dist/sortComparers/stringSortComparer.js
  var stringSortComparer = (value1, value2, sortDirection = SortDirectionNumber.neutral, sortColumn, gridOptions) => {
    let position = 0;
    const checkForUndefinedValues = sortColumn?.valueCouldBeUndefined ?? gridOptions?.cellValueCouldBeUndefined ?? false;
    if (value1 === value2) {
      position = 0;
    } else if (value1 === null || checkForUndefinedValues && value1 === void 0) {
      position = -1;
    } else if (value2 === null || checkForUndefinedValues && value2 === void 0) {
      position = 1;
    } else {
      if (gridOptions?.ignoreAccentOnStringFilterAndSort) {
        value1 = removeAccentFromText(value1, false);
        value2 = removeAccentFromText(value2, false);
      }
      if (sortDirection) {
        position = value1 < value2 ? -1 : 1;
      } else {
        position = value1 < value2 ? 1 : -1;
      }
    }
    return sortDirection * position;
  };

  // node_modules/@slickgrid-universal/common/dist/sortComparers/numericSortComparer.js
  var numericSortComparer = (value1, value2, sortDirection = SortDirectionNumber.neutral, sortColumn, gridOptions) => {
    const checkForUndefinedValues = sortColumn?.valueCouldBeUndefined ?? gridOptions?.cellValueCouldBeUndefined ?? false;
    const x2 = getSortVal(value1, checkForUndefinedValues);
    const y2 = getSortVal(value2, checkForUndefinedValues);
    return sortDirection * (x2 === y2 ? 0 : x2 > y2 ? 1 : -1);
  };
  function getSortVal(val, checkForUndefinedValues) {
    return isNaN(val) || val === "" || val === null || checkForUndefinedValues && val === void 0 ? -Infinity : parseFloat(val);
  }

  // node_modules/@slickgrid-universal/common/dist/sortComparers/sortComparers.index.js
  function buildAllSortComparers() {
    const comparers = {
      boolean: booleanSortComparer,
      numeric: numericSortComparer,
      objectString: objectStringSortComparer,
      string: stringSortComparer
    };
    getAllDateFieldTypes().forEach((dateType) => {
      comparers[dateType] = getAssociatedDateSortComparer(dateType);
    });
    return comparers;
  }
  var SortComparers = buildAllSortComparers();

  // node_modules/@slickgrid-universal/common/dist/sortComparers/sortUtilities.js
  function sortByFieldType(fieldType, value1, value2, sortDirection, sortColumn, gridOptions) {
    let sortResult = 0;
    if (isColumnDateType(fieldType)) {
      const dateSortComparer = getAssociatedDateSortComparer(fieldType);
      sortResult = dateSortComparer(value1, value2, sortDirection, sortColumn, gridOptions);
    } else {
      switch (fieldType) {
        case "boolean":
          sortResult = SortComparers.boolean(value1, value2, sortDirection, sortColumn, gridOptions);
          break;
        case "float":
        case "integer":
        case "number":
          sortResult = SortComparers.numeric(value1, value2, sortDirection, sortColumn, gridOptions);
          break;
        case "object":
          sortResult = SortComparers.objectString(value1, value2, sortDirection, sortColumn, gridOptions);
          break;
        case "string":
        case "text":
        case "password":
        case "readonly":
        default:
          sortResult = SortComparers.string(value1, value2, sortDirection, sortColumn, gridOptions);
          break;
      }
    }
    return sortResult;
  }

  // node_modules/@slickgrid-universal/event-pub-sub/dist/eventPubSub.service.js
  var EventPubSubService = class {
    get elementSource() {
      return this._elementSource;
    }
    set elementSource(element) {
      this._elementSource = element;
    }
    get subscribedEvents() {
      return this._subscribedEvents;
    }
    get subscribedEventNames() {
      return this._subscribedEvents.map((pubSubEvent) => pubSubEvent.name);
    }
    constructor(elementSource) {
      this._subscribedEvents = [];
      this.eventNamingStyle = "camelCase";
      this._elementSource = elementSource || document.createElement("div");
    }
    dispose() {
      clearTimeout(this._timer);
      this.unsubscribeAll();
      this._subscribedEvents = [];
      this._elementSource?.remove();
      this._elementSource = null;
    }
    /**
     * Dispatch of Custom Event, which by default will bubble up & is cancelable
     * @param {String} eventName - event name to dispatch
     * @param {*} data - optional data to include in the dispatching
     * @param {Boolean} isBubbling - is the event bubbling up?
     * @param {Boolean} isCancelable - is the event cancellable?
     * @param {Function} externalizeEventCallback - user can optionally retrieve the CustomEvent used in the PubSub for its own usage via a callback (called just before the event dispatch)
     * @returns {Boolean} returns true if either event's cancelable attribute value is false or its preventDefault() method was not invoked, and false otherwise.
     */
    dispatchCustomEvent(eventName, data, isBubbling = true, isCancelable = true, externalizeEventCallback) {
      const eventInit = { bubbles: isBubbling, cancelable: isCancelable };
      if (data) {
        eventInit.detail = data;
      }
      const custEvent = new CustomEvent(eventName, eventInit);
      if (typeof externalizeEventCallback === "function") {
        externalizeEventCallback(custEvent);
      }
      return this._elementSource?.dispatchEvent(custEvent);
    }
    /**
     * Get the event name by the convention defined, it could be: all lower case, camelCase, PascalCase or kebab-case
     * @param {String} inputEventName - name of the event
     * @param {String} eventNamePrefix - prefix to use in the event name
     * @returns {String} - output event name
     */
    getEventNameByNamingConvention(inputEventName, eventNamePrefix) {
      let outputEventName = inputEventName;
      switch (this.eventNamingStyle) {
        case "camelCase":
        case "camelCaseWithExtraOnPrefix":
          if (this.eventNamingStyle === "camelCaseWithExtraOnPrefix") {
            outputEventName = `${eventNamePrefix}${inputEventName.replace(/^on/, "onOn")}`;
          }
          outputEventName = eventNamePrefix !== "" ? `${eventNamePrefix}${titleCase(outputEventName)}` : outputEventName;
          break;
        case "kebabCase":
          outputEventName = eventNamePrefix !== "" ? `${eventNamePrefix}-${toKebabCase(outputEventName)}` : toKebabCase(outputEventName);
          break;
        case "lowerCase":
        case "lowerCaseWithoutOnPrefix":
          if (this.eventNamingStyle === "lowerCaseWithoutOnPrefix") {
            outputEventName = `${eventNamePrefix}${inputEventName.replace(/^on/, "")}`;
          }
          outputEventName = `${eventNamePrefix}${outputEventName}`.toLowerCase();
          break;
      }
      return outputEventName;
    }
    /**
     * Method to publish a message via a dispatchEvent.
     * Return is a Boolean (from the event dispatch) unless a delay is provided if so we'll return the dispatched event in a Promise with a delayed cycle
     * The delay is rarely use and is only used when we want to make sure that certain events have the time to execute
     * and we do this because most framework require a cycle before the binding is processed and binding a spinner end up showing too late
     * for example this is used for the following events: onBeforeFilterClear, onBeforeFilterChange, onBeforeSortChange
     * @param {String} event - The event or channel to publish to.
     * @param {*} data - The data to publish on the channel.
     * @param {Number} delay - optional argument to delay the publish event
     * @param {Function} externalizeEventCallback - user can optionally retrieve the CustomEvent used in the PubSub for its own usage via a callback (called just before the event dispatch)
     * @returns {Boolean | Promise} - return type will be a Boolean unless a `delay` is provided then a `Promise<Boolean>` will be returned
     */
    publish(eventName, data, delay, externalizeEventCallback) {
      const eventNameByConvention = this.getEventNameByNamingConvention(eventName, "");
      if (delay) {
        return new Promise((resolve) => {
          clearTimeout(this._timer);
          this._timer = setTimeout(() => resolve(this.dispatchCustomEvent(eventNameByConvention, data, false, true, externalizeEventCallback)), delay);
        });
      } else {
        return this.dispatchCustomEvent(eventNameByConvention, data, false, true, externalizeEventCallback);
      }
    }
    /**
     * Subscribes to a message channel or message type.
     * @param {String|String[]} event The event channel or event data type.
     * @param {Function} callback The callback to be invoked when the specified message is published.
     * @return possibly a Subscription
     */
    subscribe(eventNames, callback) {
      eventNames = Array.isArray(eventNames) ? eventNames : [eventNames];
      const subscriptions = [];
      eventNames.forEach((eventName) => {
        const eventNameByConvention = this.getEventNameByNamingConvention(eventName, "");
        this._elementSource.addEventListener(eventNameByConvention, (event) => callback.call(null, event.detail));
        this._subscribedEvents.push({ name: eventNameByConvention, listener: callback });
        subscriptions.push(() => this.unsubscribe(eventNameByConvention, callback));
      });
      return {
        unsubscribe: () => subscriptions.forEach((unsub) => unsub())
      };
    }
    /**
     * Subscribes to a message channel or message type.
     * This is similar to the "subscribe" except that the callback receives an event typed as CustomEventInit and the data will be inside its "event.detail"
     * @param {String} event - the event name/message
     * @param {Function} callback - The callback to be invoked when the specified message is published.
     * @return {Subscription} possibly a Subscription
     */
    subscribeEvent(eventName, listener) {
      const eventNameByConvention = this.getEventNameByNamingConvention(eventName, "");
      this._elementSource.addEventListener(eventNameByConvention, listener);
      this._subscribedEvents.push({ name: eventNameByConvention, listener });
      return {
        unsubscribe: () => this.unsubscribe(eventNameByConvention, listener)
      };
    }
    /**
     * Unsubscribes a message or event name
     * @param {String} event - the event name/message
     * @param {Function} listener - event listener callback
     * @param {Boolean} [shouldRemoveFromEventList] - should we also remove the event from the subscriptions array?
     * @return possibly a Subscription
     */
    unsubscribe(eventName, listener, shouldRemoveFromEventList = true) {
      const eventNameByConvention = this.getEventNameByNamingConvention(eventName, "");
      this._elementSource.removeEventListener(eventNameByConvention, listener);
      if (shouldRemoveFromEventList) {
        this.removeSubscribedEventWhenFound(eventName, listener);
      }
    }
    /** Unsubscribes all subscriptions/events that currently exists */
    unsubscribeAll(subscriptions) {
      if (Array.isArray(subscriptions)) {
        let subscription;
        do {
          subscription = subscriptions.pop();
          if (subscription?.dispose) {
            subscription.dispose();
          } else if (subscription?.unsubscribe) {
            subscription.unsubscribe();
          }
        } while (subscription);
      } else {
        let pubSubEvent = this._subscribedEvents.pop();
        while (pubSubEvent) {
          this.unsubscribe(pubSubEvent.name, pubSubEvent.listener, false);
          pubSubEvent = this._subscribedEvents.pop();
        }
      }
    }
    // --
    // protected functions
    // --------------------
    removeSubscribedEventWhenFound(eventName, listener) {
      const eventIdx = this._subscribedEvents.findIndex((evt) => evt.name === eventName && evt.listener === listener);
      if (eventIdx >= 0) {
        this._subscribedEvents.splice(eventIdx, 1);
      }
    }
  };

  // node_modules/autocompleter/autocomplete.es.js
  function autocomplete(settings) {
    var doc = document;
    var container = settings.container || doc.createElement("div");
    var preventSubmit = settings.preventSubmit || 0;
    container.id = container.id || "autocomplete-" + uid();
    var containerStyle = container.style;
    var debounceWaitMs = settings.debounceWaitMs || 0;
    var disableAutoSelect = settings.disableAutoSelect || false;
    var customContainerParent = container.parentElement;
    var items = [];
    var inputValue = "";
    var minLen = 2;
    var showOnFocus = settings.showOnFocus;
    var selected;
    var fetchCounter = 0;
    var debounceTimer;
    var destroyed = false;
    var suppressAutocomplete = false;
    if (settings.minLength !== void 0) {
      minLen = settings.minLength;
    }
    if (!settings.input) {
      throw new Error("input undefined");
    }
    var input = settings.input;
    container.className = [container.className, "autocomplete", settings.className || ""].join(" ").trim();
    container.setAttribute("role", "listbox");
    input.setAttribute("role", "combobox");
    input.setAttribute("aria-expanded", "false");
    input.setAttribute("aria-autocomplete", "list");
    input.setAttribute("aria-controls", container.id);
    input.setAttribute("aria-owns", container.id);
    input.setAttribute("aria-activedescendant", "");
    input.setAttribute("aria-haspopup", "listbox");
    containerStyle.position = "absolute";
    function uid() {
      return Date.now().toString(36) + Math.random().toString(36).substring(2);
    }
    function detach() {
      var parent = container.parentNode;
      if (parent) {
        parent.removeChild(container);
      }
    }
    function clearDebounceTimer() {
      if (debounceTimer) {
        window.clearTimeout(debounceTimer);
      }
    }
    function attach() {
      if (!container.parentNode) {
        (customContainerParent || doc.body).appendChild(container);
      }
    }
    function containerDisplayed() {
      return !!container.parentNode;
    }
    function clear() {
      fetchCounter++;
      items = [];
      inputValue = "";
      selected = void 0;
      input.setAttribute("aria-activedescendant", "");
      input.setAttribute("aria-expanded", "false");
      detach();
    }
    function updatePosition() {
      if (!containerDisplayed()) {
        return;
      }
      input.setAttribute("aria-expanded", "true");
      containerStyle.height = "auto";
      containerStyle.width = input.offsetWidth + "px";
      var maxHeight = 0;
      var inputRect;
      function calc() {
        var docEl = doc.documentElement;
        var clientTop = docEl.clientTop || doc.body.clientTop || 0;
        var clientLeft = docEl.clientLeft || doc.body.clientLeft || 0;
        var scrollTop = window.pageYOffset || docEl.scrollTop;
        var scrollLeft = window.pageXOffset || docEl.scrollLeft;
        inputRect = input.getBoundingClientRect();
        var top = inputRect.top + input.offsetHeight + scrollTop - clientTop;
        var left = inputRect.left + scrollLeft - clientLeft;
        containerStyle.top = top + "px";
        containerStyle.left = left + "px";
        maxHeight = window.innerHeight - (inputRect.top + input.offsetHeight);
        if (maxHeight < 0) {
          maxHeight = 0;
        }
        containerStyle.top = top + "px";
        containerStyle.bottom = "";
        containerStyle.left = left + "px";
        containerStyle.maxHeight = maxHeight + "px";
      }
      calc();
      calc();
      if (settings.customize && inputRect) {
        settings.customize(input, inputRect, container, maxHeight);
      }
    }
    function update2() {
      container.textContent = "";
      input.setAttribute("aria-activedescendant", "");
      var render = function(item, _2, __) {
        var itemElement = doc.createElement("div");
        itemElement.textContent = item.label || "";
        return itemElement;
      };
      if (settings.render) {
        render = settings.render;
      }
      var renderGroup = function(groupName, _2) {
        var groupDiv = doc.createElement("div");
        groupDiv.textContent = groupName;
        return groupDiv;
      };
      if (settings.renderGroup) {
        renderGroup = settings.renderGroup;
      }
      var fragment = doc.createDocumentFragment();
      var prevGroup = uid();
      items.forEach(function(item, index2) {
        if (item.group && item.group !== prevGroup) {
          prevGroup = item.group;
          var groupDiv = renderGroup(item.group, inputValue);
          if (groupDiv) {
            groupDiv.className += " group";
            fragment.appendChild(groupDiv);
          }
        }
        var div = render(item, inputValue, index2);
        if (div) {
          div.id = container.id + "_" + index2;
          div.setAttribute("role", "option");
          div.addEventListener("click", function(ev) {
            suppressAutocomplete = true;
            try {
              settings.onSelect(item, input);
            } finally {
              suppressAutocomplete = false;
            }
            clear();
            ev.preventDefault();
            ev.stopPropagation();
          });
          if (item === selected) {
            div.className += " selected";
            div.setAttribute("aria-selected", "true");
            input.setAttribute("aria-activedescendant", div.id);
          }
          fragment.appendChild(div);
        }
      });
      container.appendChild(fragment);
      if (items.length < 1) {
        if (settings.emptyMsg) {
          var empty = doc.createElement("div");
          empty.id = container.id + "_" + uid();
          empty.className = "empty";
          empty.textContent = settings.emptyMsg;
          container.appendChild(empty);
          input.setAttribute("aria-activedescendant", empty.id);
        } else {
          clear();
          return;
        }
      }
      attach();
      updatePosition();
      updateScroll();
    }
    function updateIfDisplayed() {
      if (containerDisplayed()) {
        update2();
      }
    }
    function resizeEventHandler() {
      updateIfDisplayed();
    }
    function scrollEventHandler(e2) {
      if (e2.target !== container) {
        updateIfDisplayed();
      } else {
        e2.preventDefault();
      }
    }
    function inputEventHandler() {
      if (!suppressAutocomplete) {
        fetch(
          0
          /* Keyboard */
        );
      }
    }
    function updateScroll() {
      var elements = container.getElementsByClassName("selected");
      if (elements.length > 0) {
        var element = elements[0];
        var previous = element.previousElementSibling;
        if (previous && previous.className.indexOf("group") !== -1 && !previous.previousElementSibling) {
          element = previous;
        }
        if (element.offsetTop < container.scrollTop) {
          container.scrollTop = element.offsetTop;
        } else {
          var selectBottom = element.offsetTop + element.offsetHeight;
          var containerBottom = container.scrollTop + container.offsetHeight;
          if (selectBottom > containerBottom) {
            container.scrollTop += selectBottom - containerBottom;
          }
        }
      }
    }
    function selectPreviousSuggestion() {
      var index2 = items.indexOf(selected);
      selected = index2 === -1 ? void 0 : items[(index2 + items.length - 1) % items.length];
      updateSelectedSuggestion(index2);
    }
    function selectNextSuggestion() {
      var index2 = items.indexOf(selected);
      selected = items.length < 1 ? void 0 : index2 === -1 ? items[0] : items[(index2 + 1) % items.length];
      updateSelectedSuggestion(index2);
    }
    function updateSelectedSuggestion(index2) {
      if (items.length > 0) {
        unselectSuggestion(index2);
        selectSuggestion(items.indexOf(selected));
        updateScroll();
      }
    }
    function selectSuggestion(index2) {
      var element = doc.getElementById(container.id + "_" + index2);
      if (element) {
        element.classList.add("selected");
        element.setAttribute("aria-selected", "true");
        input.setAttribute("aria-activedescendant", element.id);
      }
    }
    function unselectSuggestion(index2) {
      var element = doc.getElementById(container.id + "_" + index2);
      if (element) {
        element.classList.remove("selected");
        element.removeAttribute("aria-selected");
        input.removeAttribute("aria-activedescendant");
      }
    }
    function handleArrowAndEscapeKeys(ev, key) {
      var containerIsDisplayed = containerDisplayed();
      if (key === "Escape") {
        clear();
      } else {
        if (!containerIsDisplayed || items.length < 1) {
          return;
        }
        key === "ArrowUp" ? selectPreviousSuggestion() : selectNextSuggestion();
      }
      ev.preventDefault();
      if (containerIsDisplayed) {
        ev.stopPropagation();
      }
    }
    function handleEnterKey(ev) {
      if (selected) {
        if (preventSubmit === 2) {
          ev.preventDefault();
        }
        suppressAutocomplete = true;
        try {
          settings.onSelect(selected, input);
        } finally {
          suppressAutocomplete = false;
        }
        clear();
      }
      if (preventSubmit === 1) {
        ev.preventDefault();
      }
    }
    function keydownEventHandler(ev) {
      var key = ev.key;
      switch (key) {
        case "ArrowUp":
        case "ArrowDown":
        case "Escape":
          handleArrowAndEscapeKeys(ev, key);
          break;
        case "Enter":
          handleEnterKey(ev);
          break;
      }
    }
    function focusEventHandler() {
      if (showOnFocus) {
        fetch(
          1
          /* Focus */
        );
      }
    }
    function fetch(trigger) {
      if (input.value.length >= minLen || trigger === 1) {
        clearDebounceTimer();
        debounceTimer = window.setTimeout(function() {
          return startFetch(input.value, trigger, input.selectionStart || 0);
        }, trigger === 0 || trigger === 2 ? debounceWaitMs : 0);
      } else {
        clear();
      }
    }
    function startFetch(inputText, trigger, cursorPos) {
      if (destroyed)
        return;
      var savedFetchCounter = ++fetchCounter;
      settings.fetch(inputText, function(elements) {
        if (fetchCounter === savedFetchCounter && elements) {
          items = elements;
          inputValue = inputText;
          selected = items.length < 1 || disableAutoSelect ? void 0 : items[0];
          update2();
        }
      }, trigger, cursorPos);
    }
    function keyupEventHandler(e2) {
      if (settings.keyup) {
        settings.keyup({
          event: e2,
          fetch: function() {
            return fetch(
              0
              /* Keyboard */
            );
          }
        });
        return;
      }
      if (!containerDisplayed() && e2.key === "ArrowDown") {
        fetch(
          0
          /* Keyboard */
        );
      }
    }
    function clickEventHandler(e2) {
      settings.click && settings.click({
        event: e2,
        fetch: function() {
          return fetch(
            2
            /* Mouse */
          );
        }
      });
    }
    function blurEventHandler() {
      setTimeout(function() {
        if (doc.activeElement !== input) {
          clear();
        }
      }, 200);
    }
    function manualFetch() {
      startFetch(input.value, 3, input.selectionStart || 0);
    }
    container.addEventListener("mousedown", function(evt) {
      evt.stopPropagation();
      evt.preventDefault();
    });
    container.addEventListener("focus", function() {
      return input.focus();
    });
    detach();
    function destroy3() {
      input.removeEventListener("focus", focusEventHandler);
      input.removeEventListener("keyup", keyupEventHandler);
      input.removeEventListener("click", clickEventHandler);
      input.removeEventListener("keydown", keydownEventHandler);
      input.removeEventListener("input", inputEventHandler);
      input.removeEventListener("blur", blurEventHandler);
      window.removeEventListener("resize", resizeEventHandler);
      doc.removeEventListener("scroll", scrollEventHandler, true);
      input.removeAttribute("role");
      input.removeAttribute("aria-expanded");
      input.removeAttribute("aria-autocomplete");
      input.removeAttribute("aria-controls");
      input.removeAttribute("aria-activedescendant");
      input.removeAttribute("aria-owns");
      input.removeAttribute("aria-haspopup");
      clearDebounceTimer();
      clear();
      destroyed = true;
    }
    input.addEventListener("keyup", keyupEventHandler);
    input.addEventListener("click", clickEventHandler);
    input.addEventListener("keydown", keydownEventHandler);
    input.addEventListener("input", inputEventHandler);
    input.addEventListener("blur", blurEventHandler);
    input.addEventListener("focus", focusEventHandler);
    window.addEventListener("resize", resizeEventHandler);
    doc.addEventListener("scroll", scrollEventHandler, true);
    return {
      destroy: destroy3,
      fetch: manualFetch
    };
  }
  var autocomplete_es_default = autocomplete;

  // node_modules/vanilla-calendar-pro/index.mjs
  var __defProp2 = Object.defineProperty;
  var __defProps = Object.defineProperties;
  var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
  var __getOwnPropSymbols = Object.getOwnPropertySymbols;
  var __hasOwnProp2 = Object.prototype.hasOwnProperty;
  var __propIsEnum = Object.prototype.propertyIsEnumerable;
  var __defNormalProp = (e2, t, n) => t in e2 ? __defProp2(e2, t, { enumerable: true, configurable: true, writable: true, value: n }) : e2[t] = n;
  var __spreadValues = (e2, t) => {
    for (var n in t || (t = {})) __hasOwnProp2.call(t, n) && __defNormalProp(e2, n, t[n]);
    if (__getOwnPropSymbols) for (var n of __getOwnPropSymbols(t)) __propIsEnum.call(t, n) && __defNormalProp(e2, n, t[n]);
    return e2;
  };
  var __spreadProps = (e2, t) => __defProps(e2, __getOwnPropDescs(t));
  var __publicField = (e2, t, n) => (__defNormalProp(e2, "symbol" != typeof t ? t + "" : t, n), n);
  var errorMessages = { notFoundSelector: (e2) => `${e2} is not found, check the first argument passed to new Calendar.`, notInit: 'The calendar has not been initialized, please initialize it using the "init()" method first.', notLocale: "You specified an incorrect language label or did not specify the required number of values \u200B\u200Bfor \xABlocale.weekdays\xBB or \xABlocale.months\xBB.", incorrectTime: "The value of the time property can be: false, 12 or 24.", incorrectMonthsCount: "For the \xABmultiple\xBB calendar type, the \xABdisplayMonthsCount\xBB parameter can have a value from 2 to 12, and for all others it cannot be greater than 1." };
  var setContext = (e2, t, n) => {
    e2.context[t] = n;
  };
  var destroy = (e2) => {
    var t, n, a, o, l;
    if (!e2.context.isInit) throw new Error(errorMessages.notInit);
    e2.inputMode ? (null == (t = e2.context.mainElement.parentElement) || t.removeChild(e2.context.mainElement), null == (a = null == (n = e2.context.inputElement) ? void 0 : n.replaceWith) || a.call(n, e2.context.originalElement), setContext(e2, "inputElement", void 0)) : null == (l = (o = e2.context.mainElement).replaceWith) || l.call(o, e2.context.originalElement), setContext(e2, "mainElement", e2.context.originalElement), e2.onDestroy && e2.onDestroy(e2);
  };
  var skipOpenOnFocus = /* @__PURE__ */ new WeakSet();
  var shouldSkipOpenOnFocus = (e2) => skipOpenOnFocus.has(e2);
  var setSkipOpenOnFocus = (e2) => {
    skipOpenOnFocus.add(e2);
  };
  var clearSkipOpenOnFocus = (e2) => {
    skipOpenOnFocus.delete(e2);
  };
  var PREV_TABINDEX_ATTR = "data-vc-prev-tabindex";
  var isFocusable = (e2) => e2.tabIndex >= 0 && !e2.hasAttribute("disabled") && "true" !== e2.getAttribute("aria-disabled");
  var storePrevTabIndex = (e2) => {
    if (e2.hasAttribute(PREV_TABINDEX_ATTR)) return;
    const t = e2.getAttribute("tabindex");
    e2.setAttribute(PREV_TABINDEX_ATTR, null != t ? t : "");
  };
  var restorePrevTabIndex = (e2) => {
    if (!e2.hasAttribute(PREV_TABINDEX_ATTR)) return;
    const t = e2.getAttribute(PREV_TABINDEX_ATTR);
    "" === t || null === t ? e2.removeAttribute("tabindex") : e2.setAttribute("tabindex", t), e2.removeAttribute(PREV_TABINDEX_ATTR);
  };
  var disableTabbing = (e2) => {
    isFocusable(e2) && (storePrevTabIndex(e2), e2.tabIndex = -1);
    const t = document.createTreeWalker(e2, NodeFilter.SHOW_ELEMENT, { acceptNode: (e3) => isFocusable(e3) ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP });
    for (; t.nextNode(); ) {
      const e3 = t.currentNode;
      storePrevTabIndex(e3), e3.tabIndex = -1;
    }
  };
  var restoreTabbing = (e2) => {
    restorePrevTabIndex(e2), e2.querySelectorAll(`[${PREV_TABINDEX_ATTR}]`).forEach(restorePrevTabIndex);
  };
  var hide = (e2) => {
    if (e2.context.isShowInInputMode && e2.context.currentType) {
      if (e2.context.mainElement.dataset.vcCalendarHidden = "", setContext(e2, "isShowInInputMode", false), e2.inputMode && disableTabbing(e2.context.mainElement), e2.context.cleanupHandlers[0] && (e2.context.cleanupHandlers.forEach(((e3) => e3())), setContext(e2, "cleanupHandlers", [])), e2.inputMode && e2.context.inputElement && e2.context.mainElement.contains(document.activeElement)) {
        ("function" == typeof e2.openOnFocus || true === e2.openOnFocus) && setSkipOpenOnFocus(e2), e2.context.inputElement.focus();
      }
      e2.onHide && e2.onHide(e2);
    }
  };
  function getOffset2(e2) {
    if (!e2 || !e2.getBoundingClientRect) return { top: 0, bottom: 0, left: 0, right: 0 };
    const t = e2.getBoundingClientRect(), n = document.documentElement;
    return { bottom: t.bottom, right: t.right, top: t.top + window.scrollY - n.clientTop, left: t.left + window.scrollX - n.clientLeft };
  }
  function getViewportDimensions() {
    return { vw: Math.max(document.documentElement.clientWidth || 0, window.innerWidth || 0), vh: Math.max(document.documentElement.clientHeight || 0, window.innerHeight || 0) };
  }
  function getWindowScrollPosition() {
    return { left: window.scrollX || document.documentElement.scrollLeft || 0, top: window.scrollY || document.documentElement.scrollTop || 0 };
  }
  function calculateAvailableSpace2(e2) {
    const { top: t, left: n } = getWindowScrollPosition(), { top: a, left: o } = getOffset2(e2), { vh: l, vw: s } = getViewportDimensions(), i = a - t, r2 = o - n;
    return { top: i, bottom: l - (i + e2.clientHeight), left: r2, right: s - (r2 + e2.clientWidth) };
  }
  function getAvailablePosition(e2, t, n = 5) {
    const a = { top: true, bottom: true, left: true, right: true }, o = [];
    if (!t || !e2) return { canShow: a, parentPositions: o };
    const { bottom: l, top: s } = calculateAvailableSpace2(e2), { top: i, left: r2 } = getOffset2(e2), { height: c, width: d2 } = t.getBoundingClientRect(), { vh: u, vw: m } = getViewportDimensions(), p = m / 2, h2 = u / 2;
    return [{ condition: i < h2, position: "top" }, { condition: i > h2, position: "bottom" }, { condition: r2 < p, position: "left" }, { condition: r2 > p, position: "right" }].forEach((({ condition: e3, position: t2 }) => {
      e3 && o.push(t2);
    })), Object.assign(a, { top: c <= s - n, bottom: c <= l - n, left: d2 <= r2, right: d2 <= m - r2 }), { canShow: a, parentPositions: o };
  }
  var handleDay = (e2, t, n, a) => {
    var o;
    const l = a.querySelector(`[data-vc-date="${t}"]`), s = null == l ? void 0 : l.querySelector("[data-vc-date-btn]");
    if (!l || !s) return;
    if ((null == n ? void 0 : n.modifier) && s.classList.add(...n.modifier.trim().split(" ")), !(null == n ? void 0 : n.html)) return;
    const i = document.createElement("div");
    i.className = e2.styles.datePopup, i.dataset.vcDatePopup = "", i.innerHTML = e2.sanitizerHTML(n.html), s.ariaExpanded = "true", s.ariaLabel = `${s.ariaLabel}, ${null == (o = null == i ? void 0 : i.textContent) ? void 0 : o.replace(/^\s+|\s+(?=\s)|\s+$/g, "").replace(/&nbsp;/g, " ")}`, l.appendChild(i), requestAnimationFrame((() => {
      if (!i) return;
      const { canShow: e3 } = getAvailablePosition(l, i), t2 = e3.bottom ? l.offsetHeight : -i.offsetHeight, n2 = e3.left && !e3.right ? l.offsetWidth - i.offsetWidth / 2 : !e3.left && e3.right ? i.offsetWidth / 2 : 0;
      Object.assign(i.style, { left: `${n2}px`, top: `${t2}px` });
    }));
  };
  var createDatePopup = (e2, t) => {
    var n;
    e2.popups && (null == (n = Object.entries(e2.popups)) || n.forEach((([n2, a]) => handleDay(e2, n2, a, t))));
  };
  var getDate = (e2) => /* @__PURE__ */ new Date(`${e2}T00:00:00`);
  var getDateString = (e2) => `${e2.getFullYear()}-${String(e2.getMonth() + 1).padStart(2, "0")}-${String(e2.getDate()).padStart(2, "0")}`;
  var parseDates = (e2) => e2.reduce(((e3, t) => {
    if (t instanceof Date || "number" == typeof t) {
      const n = t instanceof Date ? t : new Date(t);
      e3.push(n.toISOString().substring(0, 10));
    } else t.match(/^(\d{4}-\d{2}-\d{2})$/g) ? e3.push(t) : t.replace(/(\d{4}-\d{2}-\d{2}).*?(\d{4}-\d{2}-\d{2})/g, ((t2, n, a) => {
      const o = getDate(n), l = getDate(a), s = new Date(o.getTime());
      for (; s <= l; s.setDate(s.getDate() + 1)) e3.push(getDateString(s));
      return t2;
    }));
    return e3;
  }), []);
  var updateAttribute = (e2, t, n, a = "") => {
    t ? e2.setAttribute(n, a) : e2.getAttribute(n) === a && e2.removeAttribute(n);
  };
  var setDateModifier = (e2, t, n, a, o, l, s) => {
    var i, r2, c, d2;
    const u = getDate(e2.context.displayDateMin) > getDate(l) || getDate(e2.context.displayDateMax) < getDate(l) || (null == (i = e2.context.disableDates) ? void 0 : i.includes(l)) || !e2.selectionMonthsMode && "current" !== s || !e2.selectionYearsMode && getDate(l).getFullYear() !== t;
    updateAttribute(n, u, "data-vc-date-disabled"), a && updateAttribute(a, u, "aria-disabled", "true"), a && updateAttribute(a, u, "tabindex", "-1"), updateAttribute(n, !e2.disableToday && e2.context.dateToday === l, "data-vc-date-today"), updateAttribute(n, !e2.disableToday && e2.context.dateToday === l, "aria-current", "date"), updateAttribute(n, null == (r2 = e2.selectedWeekends) ? void 0 : r2.includes(o), "data-vc-date-weekend");
    const m = (null == (c = e2.selectedHolidays) ? void 0 : c[0]) ? parseDates(e2.selectedHolidays) : [];
    if (updateAttribute(n, m.includes(l), "data-vc-date-holiday"), (null == (d2 = e2.context.selectedDates) ? void 0 : d2.includes(l)) ? (n.setAttribute("data-vc-date-selected", ""), a && a.setAttribute("aria-selected", "true"), e2.context.selectedDates.length > 1 && "multiple-ranged" === e2.selectionDatesMode && (e2.context.selectedDates[0] === l && e2.context.selectedDates[e2.context.selectedDates.length - 1] === l ? n.setAttribute("data-vc-date-selected", "first-and-last") : e2.context.selectedDates[0] === l ? n.setAttribute("data-vc-date-selected", "first") : e2.context.selectedDates[e2.context.selectedDates.length - 1] === l && n.setAttribute("data-vc-date-selected", "last"), e2.context.selectedDates[0] !== l && e2.context.selectedDates[e2.context.selectedDates.length - 1] !== l && n.setAttribute("data-vc-date-selected", "middle"))) : n.hasAttribute("data-vc-date-selected") && (n.removeAttribute("data-vc-date-selected"), a && a.removeAttribute("aria-selected")), !e2.context.disableDates.includes(l) && e2.enableEdgeDatesOnly && e2.context.selectedDates.length > 1 && "multiple-ranged" === e2.selectionDatesMode) {
      const t2 = getDate(e2.context.selectedDates[0]), a2 = getDate(e2.context.selectedDates[e2.context.selectedDates.length - 1]), o2 = getDate(l);
      updateAttribute(n, o2 > t2 && o2 < a2, "data-vc-date-selected", "middle");
    }
  };
  var getLocaleString = (e2, t, n) => (/* @__PURE__ */ new Date(`${e2}T00:00:00.000Z`)).toLocaleString(t, n);
  var getWeekNumber = (e2, t) => {
    const n = getDate(e2), a = (n.getDay() - t + 7) % 7;
    n.setDate(n.getDate() + 4 - a);
    const o = new Date(n.getFullYear(), 0, 1), l = Math.ceil(((+n - +o) / 864e5 + 1) / 7);
    return { year: n.getFullYear(), week: l };
  };
  var addWeekNumberForDate = (e2, t, n) => {
    const a = getWeekNumber(n, e2.firstWeekday);
    a && (t.dataset.vcDateWeekNumber = String(a.week));
  };
  var setDaysAsDisabled = (e2, t, n) => {
    var a, o, l, s, i;
    const r2 = null == (a = e2.disableWeekdays) ? void 0 : a.includes(n), c = e2.disableAllDates && !!(null == (o = e2.context.enableDates) ? void 0 : o[0]);
    !r2 && !c || (null == (l = e2.context.enableDates) ? void 0 : l.includes(t)) || (null == (s = e2.context.disableDates) ? void 0 : s.includes(t)) || (e2.context.disableDates.push(t), null == (i = e2.context.disableDates) || i.sort(((e3, t2) => +new Date(e3) - +new Date(t2))));
  };
  var createDate = (e2, t, n, a, o, l) => {
    const s = getDate(o).getDay(), i = "string" == typeof e2.locale && e2.locale.length ? e2.locale : "en", r2 = document.createElement("div");
    let c;
    r2.className = e2.styles.date, r2.dataset.vcDate = o, r2.dataset.vcDateMonth = l, r2.dataset.vcDateWeekDay = String(s), r2.role = "gridcell", ("current" === l || e2.displayDatesOutside) && (c = document.createElement("button"), c.className = e2.styles.dateBtn, c.type = "button", c.ariaLabel = getLocaleString(o, i, { dateStyle: "long", timeZone: "UTC" }), c.dataset.vcDateBtn = "", c.innerText = String(a), r2.appendChild(c)), e2.enableWeekNumbers && addWeekNumberForDate(e2, r2, o), setDaysAsDisabled(e2, o, s), setDateModifier(e2, t, r2, c, s, o, l), n.addDate(r2), e2.onCreateDateEls && e2.onCreateDateEls(e2, r2);
  };
  var createDatesFromCurrentMonth = (e2, t, n, a, o) => {
    for (let l = 1; l <= n; l++) {
      const n2 = new Date(a, o, l);
      createDate(e2, a, t, l, getDateString(n2), "current");
    }
  };
  var createDatesFromNextMonth = (e2, t, n, a, o) => {
    const l = o + 1 === 12 ? a + 1 : a, s = o + 1 === 12 ? "01" : o + 2 < 10 ? `0${o + 2}` : o + 2;
    for (let o2 = 1; o2 <= n; o2++) {
      const n2 = o2 < 10 ? `0${o2}` : String(o2);
      createDate(e2, a, t, o2, `${l}-${s}-${n2}`, "next");
    }
  };
  var createDatesFromPrevMonth = (e2, t, n, a, o) => {
    let l = new Date(n, a, 0).getDate() - (o - 1);
    const s = 0 === a ? n - 1 : n, i = 0 === a ? 12 : a < 10 ? `0${a}` : a;
    for (let a2 = o; a2 > 0; a2--, l++) {
      createDate(e2, n, t, l, `${s}-${i}-${l}`, "prev");
    }
  };
  var createWeekNumbers = (e2, t, n, a, o) => {
    if (!e2.enableWeekNumbers) return;
    a.textContent = "";
    const l = document.createElement("b");
    l.className = e2.styles.weekNumbersTitle, l.innerText = "#", l.dataset.vcWeekNumbers = "title", a.appendChild(l);
    const s = document.createElement("div");
    s.className = e2.styles.weekNumbersContent, s.dataset.vcWeekNumbers = "content", a.appendChild(s);
    const i = document.createElement("button");
    i.type = "button", i.className = e2.styles.weekNumber;
    const r2 = o.querySelectorAll("[data-vc-date]"), c = Math.ceil((t + n) / 7);
    for (let t2 = 0; t2 < c; t2++) {
      const n2 = r2[0 === t2 ? 6 : 7 * t2].dataset.vcDate, a2 = getWeekNumber(n2, e2.firstWeekday);
      if (!a2) return;
      const o2 = i.cloneNode(true);
      o2.innerText = String(a2.week), o2.dataset.vcWeekNumber = String(a2.week), o2.dataset.vcWeekYear = String(a2.year), o2.role = "rowheader", o2.ariaLabel = `${a2.week}`, s.appendChild(o2);
    }
  };
  var createDates = (e2) => {
    const t = new Date(e2.context.selectedYear, e2.context.selectedMonth, 1), n = e2.context.mainElement.querySelectorAll('[data-vc="dates"]'), a = e2.context.mainElement.querySelectorAll('[data-vc-week="numbers"]');
    n.forEach(((n2, o) => {
      e2.selectionDatesMode || (n2.dataset.vcDatesDisabled = ""), n2.textContent = "";
      const l = new Date(t);
      l.setMonth(l.getMonth() + o);
      const s = l.getMonth(), i = l.getFullYear(), r2 = (new Date(i, s, 1).getDay() - e2.firstWeekday + 7) % 7, c = new Date(i, s + 1, 0).getDate(), d2 = r2 + c, u = Math.ceil(d2 / 7), m = 7 * u - d2, p = [];
      for (let t2 = 0; t2 < u; t2++) {
        const t3 = document.createElement("div");
        t3.className = e2.styles.datesRow, t3.setAttribute("data-vc-dates", "row"), t3.setAttribute("role", "row"), p.push(t3);
      }
      let h2 = 0, v2 = 0;
      const g2 = { addDate: (e3) => {
        p[h2].appendChild(e3), v2++, v2 >= 7 && (h2++, v2 = 0);
      } };
      createDatesFromPrevMonth(e2, g2, i, s, r2), createDatesFromCurrentMonth(e2, g2, c, i, s), createDatesFromNextMonth(e2, g2, m, i, s);
      for (const e3 of p) n2.appendChild(e3);
      createDatePopup(e2, n2), createWeekNumbers(e2, r2, c, a[o], n2);
    }));
  };
  var layoutDefault = (e2) => `
  <div class="${e2.styles.header}" data-vc="header" role="toolbar" aria-label="${e2.labels.navigation}">
    <#ArrowPrev [month] />
    <div class="${e2.styles.headerContent}" data-vc-header="content">
      <#Month />
      <#Year />
    </div>
    <#ArrowNext [month] />
  </div>
  <div class="${e2.styles.wrapper}" data-vc="wrapper">
    <#WeekNumbers />
    <div class="${e2.styles.content}" data-vc="content" role="grid">
      <#Week />
      <#Dates />
      <#DateRangeTooltip />
    </div>
  </div>
  <#ControlTime />
`;
  var layoutMonths = (e2) => `
  <div class="${e2.styles.header}" data-vc="header" role="toolbar" aria-label="${e2.labels.navigation}">
    <div class="${e2.styles.headerContent}" data-vc-header="content">
      <#Month />
      <#Year />
    </div>
  </div>
  <div class="${e2.styles.wrapper}" data-vc="wrapper">
    <div class="${e2.styles.content}" data-vc="content">
      <#Months />
    </div>
  </div>
`;
  var layoutMultiple = (e2) => `
  <div class="${e2.styles.controls}" data-vc="controls" role="toolbar" aria-label="${e2.labels.navigation}">
    <#ArrowPrev [month] />
    <#ArrowNext [month] />
  </div>
  <div class="${e2.styles.grid}" data-vc="grid">
    <#Multiple>
      <div class="${e2.styles.column}" data-vc="column" role="region">
        <div class="${e2.styles.header}" data-vc="header">
          <div class="${e2.styles.headerContent}" data-vc-header="content">
            <#Month />
            <#Year />
          </div>
        </div>
        <div class="${e2.styles.wrapper}" data-vc="wrapper">
          <#WeekNumbers />
          <div class="${e2.styles.content}" data-vc="content" role="grid">
            <#Week />
            <#Dates />
          </div>
        </div>
      </div>
    <#/Multiple>
    <#DateRangeTooltip />
  </div>
  <#ControlTime />
`;
  var layoutYears = (e2) => `
  <div class="${e2.styles.header}" data-vc="header" role="toolbar" aria-label="${e2.labels.navigation}">
    <#ArrowPrev [year] />
    <div class="${e2.styles.headerContent}" data-vc-header="content">
      <#Month />
      <#Year />
    </div>
    <#ArrowNext [year] />
  </div>
  <div class="${e2.styles.wrapper}" data-vc="wrapper">
    <div class="${e2.styles.content}" data-vc="content">
      <#Years />
    </div>
  </div>
`;
  var ArrowNext = (e2, t) => `<button type="button" class="${e2.styles.arrowNext}" data-vc-arrow="next" aria-label="${e2.labels.arrowNext[t]}"></button>`;
  var ArrowPrev = (e2, t) => `<button type="button" class="${e2.styles.arrowPrev}" data-vc-arrow="prev" aria-label="${e2.labels.arrowPrev[t]}"></button>`;
  var ControlTime = (e2) => e2.selectionTimeMode ? `<div class="${e2.styles.time}" data-vc="time" role="group" aria-label="${e2.labels.selectingTime}"></div>` : "";
  var DateRangeTooltip = (e2) => e2.onCreateDateRangeTooltip ? `<div class="${e2.styles.dateRangeTooltip}" data-vc-date-range-tooltip="hidden"></div>` : "";
  var Dates = (e2) => `<div class="${e2.styles.dates}" data-vc="dates" aria-live="assertive" aria-label="${e2.labels.dates}" ${"multiple" === e2.type ? "aria-multiselectable" : ""}></div>`;
  var Month = (e2) => `<button type="button" class="${e2.styles.month}" data-vc="month"></button>`;
  var Months = (e2) => `<div class="${e2.styles.months}" data-vc="months" role="grid" aria-live="assertive" aria-label="${e2.labels.months}"></div>`;
  var Week = (e2) => `<div class="${e2.styles.week}" data-vc="week" role="row" aria-label="${e2.labels.week}"></div>`;
  var WeekNumbers = (e2) => e2.enableWeekNumbers ? `<div class="${e2.styles.weekNumbers}" data-vc-week="numbers" role="row" aria-label="${e2.labels.weekNumber}"></div>` : "";
  var Year = (e2) => `<button type="button" class="${e2.styles.year}" data-vc="year"></button>`;
  var Years = (e2) => `<div class="${e2.styles.years}" data-vc="years" role="grid" aria-live="assertive" aria-label="${e2.labels.years}"></div>`;
  var components = { ArrowNext, ArrowPrev, ControlTime, Dates, DateRangeTooltip, Month, Months, Week, WeekNumbers, Year, Years };
  var getComponent = (e2) => components[e2];
  var parseLayout = (e2, t) => t.replace(/[\n\t]/g, "").replace(/<#(?!\/?Multiple)(.*?)>/g, ((t2, n) => {
    const a = (n.match(/\[(.*?)\]/) || [])[1], o = n.replace(/[/\s\n\t]|\[(.*?)\]/g, ""), l = getComponent(o), s = l ? l(e2, null != a ? a : null) : "";
    return e2.sanitizerHTML(s);
  })).replace(/[\n\t]/g, "");
  var parseMultipleLayout = (e2, t) => t.replace(new RegExp("<#Multiple>(.*?)<#\\/Multiple>", "gs"), ((t2, n) => {
    const a = Array(e2.context.displayMonthsCount).fill(n).join("");
    return e2.sanitizerHTML(a);
  })).replace(/[\n\t]/g, "");
  var createLayouts = (e2, t) => {
    const n = { default: layoutDefault, month: layoutMonths, year: layoutYears, multiple: layoutMultiple };
    if (Object.keys(n).forEach(((t2) => {
      const a = t2;
      e2.layouts[a].length || (e2.layouts[a] = n[a](e2));
    })), e2.context.mainElement.className = e2.styles.calendar, e2.context.mainElement.dataset.vc = "calendar", e2.context.mainElement.dataset.vcType = e2.context.currentType, e2.context.mainElement.role = "application", e2.context.mainElement.tabIndex = 0, e2.context.mainElement.ariaLabel = e2.labels.application, "multiple" !== e2.context.currentType) {
      if ("multiple" === e2.type && t) {
        const n2 = e2.context.mainElement.querySelector('[data-vc="controls"]'), a = e2.context.mainElement.querySelector('[data-vc="grid"]'), o = t.closest('[data-vc="column"]');
        return n2 && n2.remove(), a && (a.dataset.vcGrid = "hidden"), o && (o.dataset.vcColumn = e2.context.currentType), void (o && (o.innerHTML = e2.sanitizerHTML(parseLayout(e2, e2.layouts[e2.context.currentType]))));
      }
      e2.context.mainElement.innerHTML = e2.sanitizerHTML(parseLayout(e2, e2.layouts[e2.context.currentType]));
    } else e2.context.mainElement.innerHTML = e2.sanitizerHTML(parseMultipleLayout(e2, parseLayout(e2, e2.layouts[e2.context.currentType])));
  };
  var setVisibilityArrows = (e2, t, n, a) => {
    e2.style.visibility = n ? "hidden" : "", t.style.visibility = a ? "hidden" : "";
  };
  var handleDefaultType = (e2, t, n) => {
    const a = getDate(getDateString(new Date(e2.context.selectedYear, e2.context.selectedMonth, 1))), o = new Date(a.getTime()), l = new Date(a.getTime());
    o.setMonth(o.getMonth() - e2.monthsToSwitch), l.setMonth(l.getMonth() + e2.monthsToSwitch);
    const s = getDate(e2.context.dateMin), i = getDate(e2.context.dateMax);
    e2.selectionYearsMode || (s.setFullYear(a.getFullYear()), i.setFullYear(a.getFullYear()));
    const r2 = !e2.selectionMonthsMode || o.getFullYear() < s.getFullYear() || o.getFullYear() === s.getFullYear() && o.getMonth() < s.getMonth(), c = !e2.selectionMonthsMode || l.getFullYear() > i.getFullYear() || l.getFullYear() === i.getFullYear() && l.getMonth() > i.getMonth() - (e2.context.displayMonthsCount - 1);
    setVisibilityArrows(t, n, r2, c);
  };
  var handleYearType = (e2, t, n) => {
    const a = getDate(e2.context.dateMin), o = getDate(e2.context.dateMax), l = !!(a.getFullYear() && e2.context.displayYear - 7 <= a.getFullYear()), s = !!(o.getFullYear() && e2.context.displayYear + 7 >= o.getFullYear());
    setVisibilityArrows(t, n, l, s);
  };
  var visibilityArrows = (e2) => {
    if ("month" === e2.context.currentType) return;
    const t = e2.context.mainElement.querySelector('[data-vc-arrow="prev"]'), n = e2.context.mainElement.querySelector('[data-vc-arrow="next"]');
    if (!t || !n) return;
    ({ default: () => handleDefaultType(e2, t, n), year: () => handleYearType(e2, t, n) })["multiple" === e2.context.currentType ? "default" : e2.context.currentType]();
  };
  var visibilityHandler = (e2, t, n, a, o) => {
    const l = new Date(a.setFullYear(e2.context.selectedYear, e2.context.selectedMonth + n)).getFullYear(), s = new Date(a.setMonth(e2.context.selectedMonth + n)).getMonth(), i = e2.context.locale.months.long[s], r2 = t.closest('[data-vc="column"]');
    r2 && (r2.ariaLabel = `${i} ${l}`);
    const c = { month: { id: s, label: i }, year: { id: l, label: l } };
    t.innerText = String(c[o].label), t.dataset[`vc${o.charAt(0).toUpperCase() + o.slice(1)}`] = String(c[o].id), t.ariaLabel = `${e2.labels[o]} ${c[o].label}`;
    const d2 = { month: e2.selectionMonthsMode, year: e2.selectionYearsMode }, u = false === d2[o] || "only-arrows" === d2[o];
    u && (t.tabIndex = -1), t.disabled = u;
  };
  var visibilityTitle = (e2) => {
    const t = e2.context.mainElement.querySelectorAll('[data-vc="month"]'), n = e2.context.mainElement.querySelectorAll('[data-vc="year"]'), a = new Date(e2.context.selectedYear, e2.context.selectedMonth, 1);
    [t, n].forEach(((t2) => null == t2 ? void 0 : t2.forEach(((t3, n2) => visibilityHandler(e2, t3, n2, a, t3.dataset.vc)))));
  };
  var setYearModifier = (e2, t, n, a, o) => {
    var l;
    const s = { month: "[data-vc-months-month]", year: "[data-vc-years-year]" }, i = { month: { selected: "data-vc-months-month-selected", aria: "aria-selected", value: "vcMonthsMonth", selectedProperty: "selectedMonth" }, year: { selected: "data-vc-years-year-selected", aria: "aria-selected", value: "vcYearsYear", selectedProperty: "selectedYear" } };
    o && (null == (l = e2.context.mainElement.querySelectorAll(s[n])) || l.forEach(((e3) => {
      e3.removeAttribute(i[n].selected), e3.removeAttribute(i[n].aria);
    })), setContext(e2, i[n].selectedProperty, Number(t.dataset[i[n].value])), visibilityTitle(e2), "year" === n && visibilityArrows(e2)), a && (t.setAttribute(i[n].selected, ""), t.setAttribute(i[n].aria, "true"));
  };
  var getColumnID = (e2, t) => {
    var n;
    if ("multiple" !== e2.type) return { currentValue: null, columnID: 0 };
    const a = e2.context.mainElement.querySelectorAll('[data-vc="column"]'), o = Array.from(a).findIndex(((e3) => e3.closest(`[data-vc-column="${t}"]`)));
    return { currentValue: o >= 0 ? Number(null == (n = a[o].querySelector(`[data-vc="${t}"]`)) ? void 0 : n.getAttribute(`data-vc-${t}`)) : null, columnID: Math.max(o, 0) };
  };
  var createMonthEl = (e2, t, n, a, o, l, s) => {
    const i = t.cloneNode(false);
    return i.className = e2.styles.monthsMonth, i.innerText = a, i.ariaLabel = o, i.role = "gridcell", i.dataset.vcMonthsMonth = `${s}`, l && (i.ariaDisabled = "true"), l && (i.tabIndex = -1), i.disabled = l, setYearModifier(e2, i, "month", n === s, false), i;
  };
  var createMonths = (e2, t) => {
    var n, a;
    const o = null == (n = null == t ? void 0 : t.closest('[data-vc="header"]')) ? void 0 : n.querySelector('[data-vc="year"]'), l = o ? Number(o.dataset.vcYear) : e2.context.selectedYear, s = (null == t ? void 0 : t.dataset.vcMonth) ? Number(t.dataset.vcMonth) : e2.context.selectedMonth;
    setContext(e2, "currentType", "month"), createLayouts(e2, t), visibilityTitle(e2);
    const i = e2.context.mainElement.querySelector('[data-vc="months"]');
    if (!e2.selectionMonthsMode || !i) return;
    const r2 = e2.monthsToSwitch > 1 ? e2.context.locale.months.long.map(((t2, n2) => s - e2.monthsToSwitch * n2)).concat(e2.context.locale.months.long.map(((t2, n2) => s + e2.monthsToSwitch * n2))).filter(((e3) => e3 >= 0 && e3 <= 12)) : Array.from(Array(12).keys()), c = document.createElement("button");
    c.type = "button";
    for (let t2 = 0; t2 < 12; t2++) {
      const n2 = getDate(e2.context.dateMin), a2 = getDate(e2.context.dateMax), o2 = e2.context.displayMonthsCount - 1, { columnID: d2 } = getColumnID(e2, "month"), u = l <= n2.getFullYear() && t2 < n2.getMonth() + d2 || l >= a2.getFullYear() && t2 > a2.getMonth() - o2 + d2 || l > a2.getFullYear() || t2 !== s && !r2.includes(t2), m = createMonthEl(e2, c, s, e2.context.locale.months.short[t2], e2.context.locale.months.long[t2], u, t2);
      i.appendChild(m), e2.onCreateMonthEls && e2.onCreateMonthEls(e2, m);
    }
    null == (a = e2.context.mainElement.querySelector("[data-vc-months-month]:not([disabled])")) || a.focus();
  };
  var TimeInput = (e2, t, n, a, o) => `
  <label class="${t}" data-vc-time-input="${e2}">
    <input type="text" name="${e2}" maxlength="2" aria-label="${n[`input${e2.charAt(0).toUpperCase() + e2.slice(1)}`]}" value="${a}" ${o ? "disabled" : ""}>
  </label>
`;
  var TimeRange = (e2, t, n, a, o, l, s) => `
  <label class="${t}" data-vc-time-range="${e2}">
    <input type="range" name="${e2}" min="${a}" max="${o}" step="${l}" aria-label="${n[`range${e2.charAt(0).toUpperCase() + e2.slice(1)}`]}" value="${s}">
  </label>
`;
  var handleActions = (e2, t, n, a) => {
    ({ hour: () => setContext(e2, "selectedHours", n), minute: () => setContext(e2, "selectedMinutes", n) })[a](), setContext(e2, "selectedTime", `${e2.context.selectedHours}:${e2.context.selectedMinutes}${e2.context.selectedKeeping ? ` ${e2.context.selectedKeeping}` : ""}`), e2.onChangeTime && e2.onChangeTime(e2, t, false), e2.inputMode && e2.context.inputElement && e2.context.mainElement && e2.onChangeToInput && e2.onChangeToInput(e2, t);
  };
  var transformTime24 = (e2, t) => {
    var n;
    return (null == (n = { 0: { AM: "00", PM: "12" }, 1: { AM: "01", PM: "13" }, 2: { AM: "02", PM: "14" }, 3: { AM: "03", PM: "15" }, 4: { AM: "04", PM: "16" }, 5: { AM: "05", PM: "17" }, 6: { AM: "06", PM: "18" }, 7: { AM: "07", PM: "19" }, 8: { AM: "08", PM: "20" }, 9: { AM: "09", PM: "21" }, 10: { AM: "10", PM: "22" }, 11: { AM: "11", PM: "23" }, 12: { AM: "00", PM: "12" } }[Number(e2)]) ? void 0 : n[t]) || String(e2);
  };
  var handleClickKeepingTime = (e2, t, n, a, o) => {
    const l = (l2) => {
      const s = "AM" === e2.context.selectedKeeping ? "PM" : "AM", i = transformTime24(e2.context.selectedHours, s);
      Number(i) <= a && Number(i) >= o ? (setContext(e2, "selectedKeeping", s), n.value = i, handleActions(e2, l2, e2.context.selectedHours, "hour"), t.ariaLabel = `${e2.labels.btnKeeping} ${e2.context.selectedKeeping}`, t.innerText = e2.context.selectedKeeping) : e2.onChangeTime && e2.onChangeTime(e2, l2, true);
    };
    return t.addEventListener("click", l), () => {
      t.removeEventListener("click", l);
    };
  };
  var transformTime12 = (e2) => ({ 0: "12", 13: "01", 14: "02", 15: "03", 16: "04", 17: "05", 18: "06", 19: "07", 20: "08", 21: "09", 22: "10", 23: "11" })[Number(e2)] || String(e2);
  var updateInputAndRange = (e2, t, n, a) => {
    e2.value = n, t.value = a;
  };
  var updateKeepingTime$1 = (e2, t, n) => {
    t && n && (setContext(e2, "selectedKeeping", n), t.innerText = n);
  };
  var handleInput$1 = (e2, t, n, a, o, l, s) => {
    const i = { hour: (i2, r3, c) => {
      if (!e2.selectionTimeMode) return;
      ({ 12: () => {
        if (!e2.context.selectedKeeping) return;
        const d2 = Number(transformTime24(r3, e2.context.selectedKeeping));
        if (!(d2 <= l && d2 >= s)) return updateInputAndRange(n, t, e2.context.selectedHours, e2.context.selectedHours), void (e2.onChangeTime && e2.onChangeTime(e2, c, true));
        updateInputAndRange(n, t, transformTime12(r3), transformTime24(r3, e2.context.selectedKeeping)), i2 > 12 && updateKeepingTime$1(e2, a, "PM"), handleActions(e2, c, transformTime12(r3), o);
      }, 24: () => {
        if (!(i2 <= l && i2 >= s)) return updateInputAndRange(n, t, e2.context.selectedHours, e2.context.selectedHours), void (e2.onChangeTime && e2.onChangeTime(e2, c, true));
        updateInputAndRange(n, t, r3, r3), handleActions(e2, c, r3, o);
      } })[e2.selectionTimeMode]();
    }, minute: (a2, i2, r3) => {
      if (!(a2 <= l && a2 >= s)) return n.value = e2.context.selectedMinutes, void (e2.onChangeTime && e2.onChangeTime(e2, r3, true));
      n.value = i2, t.value = i2, handleActions(e2, r3, i2, o);
    } }, r2 = (e3) => {
      const t2 = Number(n.value), a2 = n.value.padStart(2, "0");
      i[o] && i[o](t2, a2, e3);
    };
    return n.addEventListener("change", r2), () => {
      n.removeEventListener("change", r2);
    };
  };
  var updateInputAndTime = (e2, t, n, a, o) => {
    t.value = o, handleActions(e2, n, o, a);
  };
  var updateKeepingTime = (e2, t, n) => {
    t && (setContext(e2, "selectedKeeping", n), t.innerText = n);
  };
  var handleRange = (e2, t, n, a, o) => {
    const l = (l2) => {
      const s = Number(t.value), i = t.value.padStart(2, "0"), r2 = "hour" === o, c = 24 === e2.selectionTimeMode, d2 = s > 0 && s < 12;
      r2 && !c && updateKeepingTime(e2, a, 0 === s || d2 ? "AM" : "PM"), updateInputAndTime(e2, n, l2, o, !r2 || c || d2 ? i : transformTime12(t.value));
    };
    return t.addEventListener("input", l), () => {
      t.removeEventListener("input", l);
    };
  };
  var handleMouseOver = (e2) => e2.setAttribute("data-vc-input-focus", "");
  var handleMouseOut = (e2) => e2.removeAttribute("data-vc-input-focus");
  var handleTime = (e2, t) => {
    const n = t.querySelector('[data-vc-time-range="hour"] input[name="hour"]'), a = t.querySelector('[data-vc-time-range="minute"] input[name="minute"]'), o = t.querySelector('[data-vc-time-input="hour"] input[name="hour"]'), l = t.querySelector('[data-vc-time-input="minute"] input[name="minute"]'), s = t.querySelector('[data-vc-time="keeping"]');
    if (!(n && a && o && l)) return;
    const i = (e3) => {
      e3.target === n && handleMouseOver(o), e3.target === a && handleMouseOver(l);
    }, r2 = (e3) => {
      e3.target === n && handleMouseOut(o), e3.target === a && handleMouseOut(l);
    };
    return t.addEventListener("mouseover", i), t.addEventListener("mouseout", r2), handleInput$1(e2, n, o, s, "hour", e2.timeMaxHour, e2.timeMinHour), handleInput$1(e2, a, l, s, "minute", e2.timeMaxMinute, e2.timeMinMinute), handleRange(e2, n, o, s, "hour"), handleRange(e2, a, l, s, "minute"), s && handleClickKeepingTime(e2, s, n, e2.timeMaxHour, e2.timeMinHour), () => {
      t.removeEventListener("mouseover", i), t.removeEventListener("mouseout", r2);
    };
  };
  var createTime = (e2) => {
    const t = e2.context.mainElement.querySelector('[data-vc="time"]');
    if (!e2.selectionTimeMode || !t) return;
    const [n, a] = [e2.timeMinHour, e2.timeMaxHour], [o, l] = [e2.timeMinMinute, e2.timeMaxMinute], s = e2.context.selectedKeeping ? transformTime24(e2.context.selectedHours, e2.context.selectedKeeping) : e2.context.selectedHours, i = "range" === e2.timeControls;
    var r2;
    t.innerHTML = e2.sanitizerHTML(`
    <div class="${e2.styles.timeContent}" data-vc-time="content">
      ${TimeInput("hour", e2.styles.timeHour, e2.labels, e2.context.selectedHours, i)}
      ${TimeInput("minute", e2.styles.timeMinute, e2.labels, e2.context.selectedMinutes, i)}
      ${12 === e2.selectionTimeMode ? (r2 = e2.context.selectedKeeping, `<button type="button" class="${e2.styles.timeKeeping}" aria-label="${e2.labels.btnKeeping} ${r2}" data-vc-time="keeping" ${i ? "disabled" : ""}>${r2}</button>`) : ""}
    </div>
    <div class="${e2.styles.timeRanges}" data-vc-time="ranges">
      ${TimeRange("hour", e2.styles.timeRange, e2.labels, n, a, e2.timeStepHour, s)}
      ${TimeRange("minute", e2.styles.timeRange, e2.labels, o, l, e2.timeStepMinute, e2.context.selectedMinutes)}
    </div>
  `), handleTime(e2, t);
  };
  var createWeek = (e2) => {
    const t = e2.selectedWeekends ? [...e2.selectedWeekends] : [], n = [...e2.context.locale.weekdays.long].reduce(((n2, a2, o) => [...n2, { id: o, titleShort: e2.context.locale.weekdays.short[o], titleLong: a2, isWeekend: t.includes(o) }]), []), a = [...n.slice(e2.firstWeekday), ...n.slice(0, e2.firstWeekday)];
    e2.context.mainElement.querySelectorAll('[data-vc="week"]').forEach(((t2) => {
      const n2 = e2.onClickWeekDay ? document.createElement("button") : document.createElement("b");
      e2.onClickWeekDay && (n2.type = "button"), a.forEach(((a2) => {
        const o = n2.cloneNode(true);
        o.innerText = a2.titleShort, o.className = e2.styles.weekDay, o.role = "columnheader", o.ariaLabel = a2.titleLong, o.dataset.vcWeekDay = String(a2.id), a2.isWeekend && (o.dataset.vcWeekDayOff = ""), t2.appendChild(o);
      }));
    }));
  };
  var createYearEl = (e2, t, n, a, o) => {
    const l = t.cloneNode(false);
    return l.className = e2.styles.yearsYear, l.innerText = String(o), l.ariaLabel = String(o), l.role = "gridcell", l.dataset.vcYearsYear = `${o}`, a && (l.ariaDisabled = "true"), a && (l.tabIndex = -1), l.disabled = a, setYearModifier(e2, l, "year", n === o, false), l;
  };
  var createYears = (e2, t) => {
    var n;
    const a = (null == t ? void 0 : t.dataset.vcYear) ? Number(t.dataset.vcYear) : e2.context.selectedYear;
    setContext(e2, "currentType", "year"), createLayouts(e2, t), visibilityTitle(e2), visibilityArrows(e2);
    const o = e2.context.mainElement.querySelector('[data-vc="years"]');
    if (!e2.selectionYearsMode || !o) return;
    const l = "multiple" !== e2.type || e2.context.selectedYear === a ? 0 : 1, s = document.createElement("button");
    s.type = "button";
    for (let t2 = e2.context.displayYear - 7; t2 < e2.context.displayYear + 8; t2++) {
      const n2 = t2 < getDate(e2.context.dateMin).getFullYear() + l || t2 > getDate(e2.context.dateMax).getFullYear(), i = createYearEl(e2, s, a, n2, t2);
      o.appendChild(i), e2.onCreateYearEls && e2.onCreateYearEls(e2, i);
    }
    null == (n = e2.context.mainElement.querySelector("[data-vc-years-year]:not([disabled])")) || n.focus();
  };
  var trackChangesHTMLElement = (e2, t, n) => {
    new MutationObserver(((e3) => {
      for (let a = 0; a < e3.length; a++) {
        if (e3[a].attributeName === t) {
          n();
          break;
        }
      }
    })).observe(e2, { attributes: true });
  };
  var haveListener = { value: false, set: () => haveListener.value = true, check: () => haveListener.value };
  var setTheme = (e2, t) => e2.dataset.vcTheme = t;
  var trackChangesThemeInSystemSettings = (e2, t) => {
    if (setTheme(e2.context.mainElement, t.matches ? "dark" : "light"), "system" !== e2.selectedTheme || haveListener.check()) return;
    const n = (e3) => {
      const t2 = document.querySelectorAll('[data-vc="calendar"]');
      null == t2 || t2.forEach(((t3) => setTheme(t3, e3.matches ? "dark" : "light")));
    };
    t.addEventListener ? t.addEventListener("change", n) : t.addListener(n), haveListener.set();
  };
  var detectTheme = (e2, t) => {
    const n = e2.themeAttrDetect.length ? document.querySelector(e2.themeAttrDetect) : null, a = e2.themeAttrDetect.replace(/^.*\[(.+)\]/g, ((e3, t2) => t2));
    if (!n || "system" === n.getAttribute(a)) return void trackChangesThemeInSystemSettings(e2, t);
    const o = n.getAttribute(a);
    o ? (setTheme(e2.context.mainElement, o), trackChangesHTMLElement(n, a, (() => {
      const t2 = n.getAttribute(a);
      t2 && setTheme(e2.context.mainElement, t2);
    }))) : trackChangesThemeInSystemSettings(e2, t);
  };
  var handleTheme = (e2) => {
    "not all" !== window.matchMedia("(prefers-color-scheme)").media ? "system" === e2.selectedTheme ? detectTheme(e2, window.matchMedia("(prefers-color-scheme: dark)")) : setTheme(e2.context.mainElement, e2.selectedTheme) : setTheme(e2.context.mainElement, "light");
  };
  var capitalizeFirstLetter = (e2) => e2.charAt(0).toUpperCase() + e2.slice(1).replace(/\./, "");
  var getLocaleWeekday = (e2, t, n) => {
    const a = /* @__PURE__ */ new Date(`1978-01-0${t + 1}T00:00:00.000Z`), o = a.toLocaleString(n, { weekday: "short", timeZone: "UTC" }), l = a.toLocaleString(n, { weekday: "long", timeZone: "UTC" });
    e2.context.locale.weekdays.short.push(capitalizeFirstLetter(o)), e2.context.locale.weekdays.long.push(capitalizeFirstLetter(l));
  };
  var getLocaleMonth = (e2, t, n) => {
    const a = /* @__PURE__ */ new Date(`1978-${String(t + 1).padStart(2, "0")}-01T00:00:00.000Z`), o = a.toLocaleString(n, { month: "short", timeZone: "UTC" }), l = a.toLocaleString(n, { month: "long", timeZone: "UTC" });
    e2.context.locale.months.short.push(capitalizeFirstLetter(o)), e2.context.locale.months.long.push(capitalizeFirstLetter(l));
  };
  var getLocale = (e2) => {
    var t, n, a, o, l, s, i, r2;
    if (!(e2.context.locale.weekdays.short[6] && e2.context.locale.weekdays.long[6] && e2.context.locale.months.short[11] && e2.context.locale.months.long[11])) if ("string" == typeof e2.locale) {
      if ("string" == typeof e2.locale && !e2.locale.length) throw new Error(errorMessages.notLocale);
      Array.from({ length: 7 }, ((t2, n2) => getLocaleWeekday(e2, n2, e2.locale))), Array.from({ length: 12 }, ((t2, n2) => getLocaleMonth(e2, n2, e2.locale)));
    } else {
      if (!((null == (n = null == (t = e2.locale) ? void 0 : t.weekdays) ? void 0 : n.short[6]) && (null == (o = null == (a = e2.locale) ? void 0 : a.weekdays) ? void 0 : o.long[6]) && (null == (s = null == (l = e2.locale) ? void 0 : l.months) ? void 0 : s.short[11]) && (null == (r2 = null == (i = e2.locale) ? void 0 : i.months) ? void 0 : r2.long[11]))) throw new Error(errorMessages.notLocale);
      setContext(e2, "locale", __spreadValues({}, e2.locale));
    }
  };
  var create = (e2) => {
    const t = { default: () => {
      createWeek(e2), createDates(e2);
    }, multiple: () => {
      createWeek(e2), createDates(e2);
    }, month: () => createMonths(e2), year: () => createYears(e2) };
    handleTheme(e2), getLocale(e2), createLayouts(e2), visibilityTitle(e2), visibilityArrows(e2), createTime(e2), t[e2.context.currentType]();
  };
  var handleArrowKeys = (e2) => {
    const t = (t2) => {
      var n;
      const a = t2.target;
      if (!["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight"].includes(t2.key) || "button" !== a.localName) return;
      const o = Array.from(e2.context.mainElement.querySelectorAll('[data-vc="calendar"] button')), l = o.indexOf(a);
      if (-1 === l) return;
      const s = (i = o[l]).hasAttribute("data-vc-date-btn") ? 7 : i.hasAttribute("data-vc-months-month") ? 4 : i.hasAttribute("data-vc-years-year") ? 5 : 1;
      var i;
      const r2 = (0, { ArrowUp: () => Math.max(0, l - s), ArrowDown: () => Math.min(o.length - 1, l + s), ArrowLeft: () => Math.max(0, l - 1), ArrowRight: () => Math.min(o.length - 1, l + 1) }[t2.key])();
      null == (n = o[r2]) || n.focus();
    };
    return e2.context.mainElement.addEventListener("keydown", t), () => e2.context.mainElement.removeEventListener("keydown", t);
  };
  var handleMonth = (e2, t) => {
    const n = getDate(getDateString(new Date(e2.context.selectedYear, e2.context.selectedMonth, 1)));
    ({ prev: () => n.setMonth(n.getMonth() - e2.monthsToSwitch), next: () => n.setMonth(n.getMonth() + e2.monthsToSwitch) })[t](), setContext(e2, "selectedMonth", n.getMonth()), setContext(e2, "selectedYear", n.getFullYear()), visibilityTitle(e2), visibilityArrows(e2), createDates(e2);
  };
  var handleClickArrow = (e2, t) => {
    const n = t.target.closest("[data-vc-arrow]");
    if (n) {
      if (["default", "multiple"].includes(e2.context.currentType)) handleMonth(e2, n.dataset.vcArrow);
      else if ("year" === e2.context.currentType && void 0 !== e2.context.displayYear) {
        const a = { prev: -15, next: 15 }[n.dataset.vcArrow];
        setContext(e2, "displayYear", e2.context.displayYear + a), createYears(e2, t.target);
      }
      e2.onClickArrow && e2.onClickArrow(e2, t);
    }
  };
  var resolveToggle = (e2, t) => void 0 === t || ("function" == typeof t ? t(e2) : t);
  var canToggleSelection = (e2) => resolveToggle(e2, e2.enableDateToggle);
  var handleSelectDate = (e2, t, n) => {
    const a = t.dataset.vcDate, o = t.closest("[data-vc-date][data-vc-date-selected]"), l = canToggleSelection(e2);
    if (o && !l) return;
    const s = o ? e2.context.selectedDates.filter(((e3) => e3 !== a)) : n ? [...e2.context.selectedDates, a] : [a];
    setContext(e2, "selectedDates", s);
  };
  var createDateRangeTooltip = (e2, t, n) => {
    if (!t) return;
    if (!n) return t.dataset.vcDateRangeTooltip = "hidden", void (t.textContent = "");
    const a = e2.context.mainElement.getBoundingClientRect(), o = n.getBoundingClientRect();
    t.style.left = o.left - a.left + o.width / 2 + "px", t.style.top = o.bottom - a.top - o.height + "px", t.dataset.vcDateRangeTooltip = "visible", t.innerHTML = e2.sanitizerHTML(e2.onCreateDateRangeTooltip(e2, n, t, o, a));
  };
  var state = { self: null, lastDateEl: null, isHovering: false, rangeMin: void 0, rangeMax: void 0, tooltipEl: null, timeoutId: null };
  var addHoverEffect = (e2, t, n) => {
    var a, o, l;
    if (!(null == (o = null == (a = state.self) ? void 0 : a.context) ? void 0 : o.selectedDates[0])) return;
    const s = getDateString(e2);
    (null == (l = state.self.context.disableDates) ? void 0 : l.includes(s)) || (state.self.context.mainElement.querySelectorAll(`[data-vc-date="${s}"]`).forEach(((e3) => e3.dataset.vcDateHover = "")), t.forEach(((e3) => e3.dataset.vcDateHover = "first")), n.forEach(((e3) => {
      "first" === e3.dataset.vcDateHover ? e3.dataset.vcDateHover = "first-and-last" : e3.dataset.vcDateHover = "last";
    })));
  };
  var removeHoverEffect = () => {
    var e2, t;
    if (!(null == (t = null == (e2 = state.self) ? void 0 : e2.context) ? void 0 : t.mainElement)) return;
    state.self.context.mainElement.querySelectorAll("[data-vc-date-hover]").forEach(((e3) => e3.removeAttribute("data-vc-date-hover")));
  };
  var handleHoverDatesEvent = (e2) => {
    var t, n;
    if (!e2 || !(null == (n = null == (t = state.self) ? void 0 : t.context) ? void 0 : n.selectedDates[0])) return;
    if (!e2.closest('[data-vc="dates"]')) return state.lastDateEl = null, createDateRangeTooltip(state.self, state.tooltipEl, null), void removeHoverEffect();
    const a = e2.closest("[data-vc-date]");
    if (!a || state.lastDateEl === a) return;
    state.lastDateEl = a, createDateRangeTooltip(state.self, state.tooltipEl, a), removeHoverEffect();
    const o = a.dataset.vcDate, l = getDate(state.self.context.selectedDates[0]), s = getDate(o), i = state.self.context.mainElement.querySelectorAll(`[data-vc-date="${state.self.context.selectedDates[0]}"]`), r2 = state.self.context.mainElement.querySelectorAll(`[data-vc-date="${o}"]`), [c, d2] = l < s ? [i, r2] : [r2, i], [u, m] = l < s ? [l, s] : [s, l];
    for (let e3 = new Date(u); e3 <= m; e3.setDate(e3.getDate() + 1)) addHoverEffect(e3, c, d2);
  };
  var handleHoverSelectedDatesRangeEvent = (e2) => {
    const t = null == e2 ? void 0 : e2.closest("[data-vc-date-selected]");
    if (!t && state.lastDateEl) return state.lastDateEl = null, void createDateRangeTooltip(state.self, state.tooltipEl, null);
    t && state.lastDateEl !== t && (state.lastDateEl = t, createDateRangeTooltip(state.self, state.tooltipEl, t));
  };
  var optimizedHoverHandler = (e2) => (t) => {
    const n = t.target;
    state.isHovering || (state.isHovering = true, requestAnimationFrame((() => {
      e2(n), state.isHovering = false;
    })));
  };
  var optimizedHandleHoverDatesEvent = optimizedHoverHandler(handleHoverDatesEvent);
  var optimizedHandleHoverSelectedDatesRangeEvent = optimizedHoverHandler(handleHoverSelectedDatesRangeEvent);
  var handleCancelSelectionDates = (e2) => {
    state.self && "Escape" === e2.key && (state.lastDateEl = null, setContext(state.self, "selectedDates", []), state.self.context.mainElement.removeEventListener("mousemove", optimizedHandleHoverDatesEvent), state.self.context.mainElement.removeEventListener("keydown", handleCancelSelectionDates), createDateRangeTooltip(state.self, state.tooltipEl, null), removeHoverEffect());
  };
  var handleMouseLeave = () => {
    null !== state.timeoutId && clearTimeout(state.timeoutId), state.timeoutId = setTimeout((() => {
      state.lastDateEl = null, createDateRangeTooltip(state.self, state.tooltipEl, null), removeHoverEffect();
    }), 50);
  };
  var updateDisabledDates = () => {
    var e2, t, n, a;
    if (!(null == (n = null == (t = null == (e2 = state.self) ? void 0 : e2.context) ? void 0 : t.selectedDates) ? void 0 : n[0]) || !(null == (a = state.self.context.disableDates) ? void 0 : a[0])) return;
    const o = getDate(state.self.context.selectedDates[0]), [l, s] = state.self.context.disableDates.map(((e3) => getDate(e3))).reduce((([e3, t2], n2) => [o >= n2 ? n2 : e3, o < n2 && null === t2 ? n2 : t2]), [null, null]);
    l && setContext(state.self, "displayDateMin", getDateString(new Date(l.setDate(l.getDate() + 1)))), s && setContext(state.self, "displayDateMax", getDateString(new Date(s.setDate(s.getDate() - 1))));
    state.self.disableDatesPast && !state.self.disableAllDates && getDate(state.self.context.displayDateMin) < getDate(state.self.context.dateToday) && setContext(state.self, "displayDateMin", state.self.context.dateToday);
  };
  var handleSelectDateRange = (e2, t) => {
    state.self = e2, state.lastDateEl = t, removeHoverEffect(), e2.disableDatesGaps && (state.rangeMin = state.rangeMin ? state.rangeMin : e2.context.displayDateMin, state.rangeMax = state.rangeMax ? state.rangeMax : e2.context.displayDateMax), e2.onCreateDateRangeTooltip && (state.tooltipEl = e2.context.mainElement.querySelector("[data-vc-date-range-tooltip]"));
    const n = null == t ? void 0 : t.dataset.vcDate;
    if (n) {
      const t2 = 1 === e2.context.selectedDates.length && e2.context.selectedDates[0].includes(n), a = t2 && !canToggleSelection(e2) ? [n, n] : t2 && canToggleSelection(e2) ? [] : e2.context.selectedDates.length > 1 ? [n] : [...e2.context.selectedDates, n];
      setContext(e2, "selectedDates", a), e2.context.selectedDates.length > 1 && e2.context.selectedDates.sort(((e3, t3) => +new Date(e3) - +new Date(t3)));
    }
    ({ set: () => (e2.disableDatesGaps && updateDisabledDates(), createDateRangeTooltip(state.self, state.tooltipEl, t), state.self.context.mainElement.removeEventListener("mousemove", optimizedHandleHoverSelectedDatesRangeEvent), state.self.context.mainElement.removeEventListener("mouseleave", handleMouseLeave), state.self.context.mainElement.removeEventListener("keydown", handleCancelSelectionDates), state.self.context.mainElement.addEventListener("mousemove", optimizedHandleHoverDatesEvent), state.self.context.mainElement.addEventListener("mouseleave", handleMouseLeave), state.self.context.mainElement.addEventListener("keydown", handleCancelSelectionDates), () => {
      state.self.context.mainElement.removeEventListener("mousemove", optimizedHandleHoverDatesEvent), state.self.context.mainElement.removeEventListener("mouseleave", handleMouseLeave), state.self.context.mainElement.removeEventListener("keydown", handleCancelSelectionDates);
    }), reset: () => {
      const [n2, a] = [e2.context.selectedDates[0], e2.context.selectedDates[e2.context.selectedDates.length - 1]], o = e2.context.selectedDates[0] !== e2.context.selectedDates[e2.context.selectedDates.length - 1], l = parseDates([`${n2}:${a}`]).filter(((t2) => !e2.context.disableDates.includes(t2))), s = o ? e2.enableEdgeDatesOnly ? [n2, a] : l : [e2.context.selectedDates[0], e2.context.selectedDates[0]];
      if (setContext(e2, "selectedDates", s), e2.disableDatesGaps && (setContext(e2, "displayDateMin", state.rangeMin), setContext(e2, "displayDateMax", state.rangeMax)), state.self.context.mainElement.removeEventListener("mousemove", optimizedHandleHoverDatesEvent), state.self.context.mainElement.removeEventListener("mouseleave", handleMouseLeave), state.self.context.mainElement.removeEventListener("keydown", handleCancelSelectionDates), e2.onCreateDateRangeTooltip) return e2.context.selectedDates[0] || (state.self.context.mainElement.removeEventListener("mousemove", optimizedHandleHoverSelectedDatesRangeEvent), state.self.context.mainElement.removeEventListener("mouseleave", handleMouseLeave), createDateRangeTooltip(state.self, state.tooltipEl, null)), e2.context.selectedDates[0] && (state.self.context.mainElement.addEventListener("mousemove", optimizedHandleHoverSelectedDatesRangeEvent), state.self.context.mainElement.addEventListener("mouseleave", handleMouseLeave), createDateRangeTooltip(state.self, state.tooltipEl, t)), () => {
        state.self.context.mainElement.removeEventListener("mousemove", optimizedHandleHoverSelectedDatesRangeEvent), state.self.context.mainElement.removeEventListener("mouseleave", handleMouseLeave);
      };
    } })[1 === e2.context.selectedDates.length ? "set" : "reset"]();
  };
  var updateDateModifier = (e2) => {
    e2.context.mainElement.querySelectorAll("[data-vc-date]").forEach(((t) => {
      const n = t.querySelector("[data-vc-date-btn]"), a = t.dataset.vcDate, o = getDate(a).getDay();
      setDateModifier(e2, e2.context.selectedYear, t, n, o, a, "current");
    }));
  };
  var handleClickDate = (e2, t) => {
    var n;
    const a = t.target, o = a.closest("[data-vc-date-btn]");
    if (!e2.selectionDatesMode || !["single", "multiple", "multiple-ranged"].includes(e2.selectionDatesMode) || !o) return;
    const l = o.closest("[data-vc-date]");
    ({ single: () => handleSelectDate(e2, l, false), multiple: () => handleSelectDate(e2, l, true), "multiple-ranged": () => handleSelectDateRange(e2, l) })[e2.selectionDatesMode](), null == (n = e2.context.selectedDates) || n.sort(((e3, t2) => +new Date(e3) - +new Date(t2))), e2.onClickDate && e2.onClickDate(e2, t), e2.inputMode && e2.context.inputElement && e2.context.mainElement && e2.onChangeToInput && e2.onChangeToInput(e2, t);
    const s = a.closest('[data-vc-date-month="prev"]'), i = a.closest('[data-vc-date-month="next"]');
    ({ prev: () => e2.enableMonthChangeOnDayClick ? handleMonth(e2, "prev") : updateDateModifier(e2), next: () => e2.enableMonthChangeOnDayClick ? handleMonth(e2, "next") : updateDateModifier(e2), current: () => updateDateModifier(e2) })[s ? "prev" : i ? "next" : "current"]();
  };
  var typeClick = ["month", "year"];
  var getValue = (e2, t, n) => {
    const { currentValue: a, columnID: o } = getColumnID(e2, t);
    return "month" === e2.context.currentType && o >= 0 ? n - o : "year" === e2.context.currentType && e2.context.selectedYear !== a ? n - 1 : n;
  };
  var handleMultipleYearSelection = (e2, t) => {
    const n = getValue(e2, "year", Number(t.dataset.vcYearsYear)), a = getDate(e2.context.dateMin), o = getDate(e2.context.dateMax), l = e2.context.displayMonthsCount - 1, { columnID: s } = getColumnID(e2, "year"), i = e2.context.selectedMonth < a.getMonth() && n <= a.getFullYear(), r2 = e2.context.selectedMonth > o.getMonth() - l + s && n >= o.getFullYear(), c = n < a.getFullYear(), d2 = n > o.getFullYear(), u = i || c ? a.getFullYear() : r2 || d2 ? o.getFullYear() : n, m = i || c ? a.getMonth() : r2 || d2 ? o.getMonth() - l + s : e2.context.selectedMonth;
    setContext(e2, "selectedYear", u), setContext(e2, "selectedMonth", m);
  };
  var handleMultipleMonthSelection = (e2, t) => {
    const n = t.closest('[data-vc-column="month"]').querySelector('[data-vc="year"]'), a = getValue(e2, "month", Number(t.dataset.vcMonthsMonth)), o = Number(n.dataset.vcYear), l = getDate(e2.context.dateMin), s = getDate(e2.context.dateMax), i = a < l.getMonth() && o <= l.getFullYear(), r2 = a > s.getMonth() && o >= s.getFullYear();
    setContext(e2, "selectedYear", o), setContext(e2, "selectedMonth", i ? l.getMonth() : r2 ? s.getMonth() : a);
  };
  var handleItemClick = (e2, t, n, a) => {
    var o;
    ({ year: () => {
      if ("multiple" === e2.type) return handleMultipleYearSelection(e2, a);
      setContext(e2, "selectedYear", Number(a.dataset.vcYearsYear));
    }, month: () => {
      if ("multiple" === e2.type) return handleMultipleMonthSelection(e2, a);
      setContext(e2, "selectedMonth", Number(a.dataset.vcMonthsMonth));
    } })[n]();
    ({ year: () => {
      var n2;
      return null == (n2 = e2.onClickYear) ? void 0 : n2.call(e2, e2, t);
    }, month: () => {
      var n2;
      return null == (n2 = e2.onClickMonth) ? void 0 : n2.call(e2, e2, t);
    } })[n](), e2.context.currentType !== e2.type ? (setContext(e2, "currentType", e2.type), create(e2), null == (o = e2.context.mainElement.querySelector(`[data-vc="${n}"]`)) || o.focus()) : setYearModifier(e2, a, n, true, true);
  };
  var handleClickType = (e2, t, n) => {
    var a;
    const o = t.target, l = o.closest(`[data-vc="${n}"]`), s = { year: () => createYears(e2, o), month: () => createMonths(e2, o) };
    if (l && e2.onClickTitle && e2.onClickTitle(e2, t), l && e2.context.currentType !== n) return s[n]();
    const i = o.closest(`[data-vc-${n}s-${n}]`);
    if (i) return handleItemClick(e2, t, n, i);
    const r2 = o.closest('[data-vc="grid"]'), c = o.closest('[data-vc="column"]');
    (e2.context.currentType === n && l || "multiple" === e2.type && e2.context.currentType === n && r2 && !c) && (setContext(e2, "currentType", e2.type), create(e2), null == (a = e2.context.mainElement.querySelector(`[data-vc="${n}"]`)) || a.focus());
  };
  var handleClickMonthOrYear = (e2, t) => {
    const n = { month: e2.selectionMonthsMode, year: e2.selectionYearsMode };
    typeClick.forEach(((a) => {
      n[a] && t.target && handleClickType(e2, t, a);
    }));
  };
  var handleClickWeekNumber = (e2, t) => {
    if (!e2.enableWeekNumbers || !e2.onClickWeekNumber) return;
    const n = t.target.closest("[data-vc-week-number]"), a = e2.context.mainElement.querySelectorAll("[data-vc-date-week-number]");
    if (!n || !a[0]) return;
    const o = Number(n.innerText), l = Number(n.dataset.vcWeekYear), s = Array.from(a).filter(((e3) => Number(e3.dataset.vcDateWeekNumber) === o));
    e2.onClickWeekNumber(e2, o, l, s, t);
  };
  var handleClickWeekDay = (e2, t) => {
    if (!e2.onClickWeekDay) return;
    const n = t.target.closest("[data-vc-week-day]"), a = t.target.closest('[data-vc="column"]'), o = a ? a.querySelectorAll("[data-vc-date-week-day]") : e2.context.mainElement.querySelectorAll("[data-vc-date-week-day]");
    if (!n || !o[0]) return;
    const l = Number(n.dataset.vcWeekDay), s = Array.from(o).filter(((e3) => Number(e3.dataset.vcDateWeekDay) === l));
    e2.onClickWeekDay(e2, l, s, t);
  };
  var handleClick = (e2) => {
    const t = (t2) => {
      handleClickArrow(e2, t2), handleClickWeekDay(e2, t2), handleClickWeekNumber(e2, t2), handleClickDate(e2, t2), handleClickMonthOrYear(e2, t2);
    };
    return e2.context.mainElement.addEventListener("click", t), () => e2.context.mainElement.removeEventListener("click", t);
  };
  var initMonthsCount = (e2) => {
    if ("multiple" === e2.type && (e2.displayMonthsCount <= 1 || e2.displayMonthsCount > 12)) throw new Error(errorMessages.incorrectMonthsCount);
    if ("multiple" !== e2.type && e2.displayMonthsCount > 1) throw new Error(errorMessages.incorrectMonthsCount);
    setContext(e2, "displayMonthsCount", e2.displayMonthsCount ? e2.displayMonthsCount : "multiple" === e2.type ? 2 : 1);
  };
  var getLocalDate = () => {
    const e2 = /* @__PURE__ */ new Date();
    return new Date(e2.getTime() - 6e4 * e2.getTimezoneOffset()).toISOString().substring(0, 10);
  };
  var resolveDate = (e2, t) => "today" === e2 ? getLocalDate() : e2 instanceof Date || "number" == typeof e2 || "string" == typeof e2 ? parseDates([e2])[0] : t;
  var initRange = (e2) => {
    var t, n, a;
    const o = resolveDate(e2.dateMin, e2.dateMin), l = resolveDate(e2.dateMax, e2.dateMax), s = resolveDate(e2.displayDateMin, o), i = resolveDate(e2.displayDateMax, l);
    setContext(e2, "dateToday", resolveDate(e2.dateToday, e2.dateToday)), setContext(e2, "displayDateMin", s ? getDate(o) >= getDate(s) ? o : s : o), setContext(e2, "displayDateMax", i ? getDate(l) <= getDate(i) ? l : i : l);
    const r2 = e2.disableDatesPast && !e2.disableAllDates && getDate(s) < getDate(e2.context.dateToday);
    setContext(e2, "displayDateMin", r2 || e2.disableAllDates ? e2.context.dateToday : s), setContext(e2, "displayDateMax", e2.disableAllDates ? e2.context.dateToday : i), setContext(e2, "disableDates", e2.disableDates[0] && !e2.disableAllDates ? parseDates(e2.disableDates) : e2.disableAllDates ? [e2.context.displayDateMin] : []), e2.context.disableDates.length > 1 && e2.context.disableDates.sort(((e3, t2) => +new Date(e3) - +new Date(t2))), setContext(e2, "enableDates", e2.enableDates[0] ? parseDates(e2.enableDates) : []), (null == (t = e2.context.enableDates) ? void 0 : t[0]) && (null == (n = e2.context.disableDates) ? void 0 : n[0]) && setContext(e2, "disableDates", e2.context.disableDates.filter(((t2) => !e2.context.enableDates.includes(t2)))), e2.context.enableDates.length > 1 && e2.context.enableDates.sort(((e3, t2) => +new Date(e3) - +new Date(t2))), (null == (a = e2.context.enableDates) ? void 0 : a[0]) && e2.disableAllDates && (setContext(e2, "displayDateMin", e2.context.enableDates[0]), setContext(e2, "displayDateMax", e2.context.enableDates[e2.context.enableDates.length - 1])), setContext(e2, "dateMin", e2.displayDisabledDates ? o : e2.context.displayDateMin), setContext(e2, "dateMax", e2.displayDisabledDates ? l : e2.context.displayDateMax);
  };
  var initSelectedDates = (e2) => {
    var t;
    setContext(e2, "selectedDates", (null == (t = e2.selectedDates) ? void 0 : t[0]) ? parseDates(e2.selectedDates) : []);
  };
  var displayClosestValidDate = (e2) => {
    const t = (t2) => {
      const n2 = new Date(t2);
      setInitialContext(e2, n2.getMonth(), n2.getFullYear());
    };
    if (e2.displayDateMin && "today" !== e2.displayDateMin && (n = e2.displayDateMin, a = /* @__PURE__ */ new Date(), new Date(n).getTime() > a.getTime())) {
      const n2 = e2.selectedDates.length && e2.selectedDates[0] ? parseDates(e2.selectedDates)[0] : e2.displayDateMin;
      return t(getDate(resolveDate(n2, e2.displayDateMin))), true;
    }
    var n, a;
    if (e2.displayDateMax && "today" !== e2.displayDateMax && ((e3, t2) => new Date(e3).getTime() < t2.getTime())(e2.displayDateMax, /* @__PURE__ */ new Date())) {
      const n2 = e2.selectedDates.length && e2.selectedDates[0] ? parseDates(e2.selectedDates)[0] : e2.displayDateMax;
      return t(getDate(resolveDate(n2, e2.displayDateMax))), true;
    }
    return false;
  };
  var setInitialContext = (e2, t, n) => {
    setContext(e2, "selectedMonth", t), setContext(e2, "selectedYear", n), setContext(e2, "displayYear", n);
  };
  var initSelectedMonthYear = (e2) => {
    var t;
    if (e2.enableJumpToSelectedDate && (null == (t = e2.selectedDates) ? void 0 : t[0]) && void 0 === e2.selectedMonth && void 0 === e2.selectedYear) {
      const t2 = getDate(parseDates(e2.selectedDates)[0]);
      return void setInitialContext(e2, t2.getMonth(), t2.getFullYear());
    }
    if (displayClosestValidDate(e2)) return;
    const n = void 0 !== e2.selectedMonth && Number(e2.selectedMonth) >= 0 && Number(e2.selectedMonth) < 12, a = void 0 !== e2.selectedYear && Number(e2.selectedYear) >= 0 && Number(e2.selectedYear) <= 9999;
    setInitialContext(e2, n ? Number(e2.selectedMonth) : getDate(e2.context.dateToday).getMonth(), a ? Number(e2.selectedYear) : getDate(e2.context.dateToday).getFullYear());
  };
  var initTime = (e2) => {
    var t, n, a;
    if (!e2.selectionTimeMode) return;
    if (![12, 24].includes(e2.selectionTimeMode)) throw new Error(errorMessages.incorrectTime);
    const o = 12 === e2.selectionTimeMode, l = o ? /^(0[1-9]|1[0-2]):([0-5][0-9]) ?(AM|PM)?$/i : /^([0-1]?[0-9]|2[0-3]):([0-5][0-9])$/;
    let [s, i, r2] = null != (a = null == (n = null == (t = e2.selectedTime) ? void 0 : t.match(l)) ? void 0 : n.slice(1)) ? a : [];
    s ? o && !r2 && (r2 = "AM") : (s = o ? transformTime12(String(e2.timeMinHour)) : String(e2.timeMinHour), i = String(e2.timeMinMinute), r2 = o ? Number(transformTime12(String(e2.timeMinHour))) >= 12 ? "PM" : "AM" : null), setContext(e2, "selectedHours", s.padStart(2, "0")), setContext(e2, "selectedMinutes", i.padStart(2, "0")), setContext(e2, "selectedKeeping", r2), setContext(e2, "selectedTime", `${e2.context.selectedHours}:${e2.context.selectedMinutes}${r2 ? ` ${r2}` : ""}`);
  };
  var initAllVariables = (e2) => {
    setContext(e2, "currentType", e2.type), initMonthsCount(e2), initRange(e2), initSelectedMonthYear(e2), initSelectedDates(e2), initTime(e2);
  };
  var reset = (e2, { year: t, month: n, dates: a, time: o, locale: l }, s = true) => {
    var i;
    const r2 = { year: e2.selectedYear, month: e2.selectedMonth, dates: e2.selectedDates, time: e2.selectedTime };
    if (e2.selectedYear = t ? r2.year : e2.context.selectedYear, e2.selectedMonth = n ? r2.month : e2.context.selectedMonth, e2.selectedTime = o ? r2.time : e2.context.selectedTime, e2.selectedDates = "only-first" === a && (null == (i = e2.context.selectedDates) ? void 0 : i[0]) ? [e2.context.selectedDates[0]] : true === a ? r2.dates : e2.context.selectedDates, l) {
      setContext(e2, "locale", { months: { short: [], long: [] }, weekdays: { short: [], long: [] } });
    }
    initAllVariables(e2), s && create(e2), e2.selectedYear = r2.year, e2.selectedMonth = r2.month, e2.selectedDates = r2.dates, e2.selectedTime = r2.time, "multiple-ranged" === e2.selectionDatesMode && a && handleSelectDateRange(e2, null);
  };
  var createToInput = (e2) => {
    const t = document.createElement("div");
    return t.className = e2.styles.calendar, t.dataset.vc = "calendar", t.dataset.vcInput = "", t.dataset.vcCalendarHidden = "", setContext(e2, "inputModeInit", true), setContext(e2, "isShowInInputMode", false), setContext(e2, "mainElement", t), document.body.appendChild(e2.context.mainElement), reset(e2, { year: true, month: true, dates: true, time: true, locale: true }), setTimeout((() => show(e2))), e2.onInit && e2.onInit(e2), handleArrowKeys(e2), handleClick(e2);
  };
  var canOpenOnFocus = (e2) => resolveToggle(e2, e2.openOnFocus);
  var handleInput = (e2) => {
    setContext(e2, "inputElement", e2.context.mainElement);
    const t = () => {
      e2.context.inputModeInit ? setTimeout((() => show(e2))) : createToInput(e2);
    };
    e2.context.inputElement.addEventListener("click", t);
    const n = "function" == typeof e2.openOnFocus || true === e2.openOnFocus, a = () => {
      shouldSkipOpenOnFocus(e2) ? clearSkipOpenOnFocus(e2) : canOpenOnFocus(e2) && t();
    };
    n && e2.context.inputElement.addEventListener("focus", a);
    const o = (t2) => {
      const n2 = "Tab" === t2.key && !t2.shiftKey, a2 = ["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight"].includes(t2.key);
      (n2 || a2) && ((t3) => {
        var n3;
        if (!e2.context.isShowInInputMode) return false;
        if (document.activeElement !== e2.context.inputElement) return false;
        const a3 = (e3) => e3.tabIndex >= 0 && !e3.hasAttribute("disabled") && "true" !== e3.getAttribute("aria-disabled"), o2 = null != (n3 = document.createTreeWalker(e2.context.mainElement, NodeFilter.SHOW_ELEMENT, { acceptNode: (e3) => a3(e3) ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP }).nextNode()) ? n3 : a3(e2.context.mainElement) ? e2.context.mainElement : null;
        !o2 || o2.tabIndex < 0 || (t3.preventDefault(), o2.focus());
      })(t2);
    };
    return e2.context.inputElement.addEventListener("keydown", o), () => {
      e2.context.inputElement.removeEventListener("click", t), n && e2.context.inputElement.removeEventListener("focus", a), e2.context.inputElement.removeEventListener("keydown", o);
    };
  };
  var init = (e2) => (setContext(e2, "originalElement", e2.context.mainElement.cloneNode(true)), setContext(e2, "isInit", true), e2.inputMode ? handleInput(e2) : (initAllVariables(e2), create(e2), e2.onInit && e2.onInit(e2), handleArrowKeys(e2), handleClick(e2)));
  var update = (e2, t) => {
    if (!e2.context.isInit) throw new Error(errorMessages.notInit);
    reset(e2, __spreadValues(__spreadValues({}, { year: true, month: true, dates: true, time: true, locale: true }), t), !(e2.inputMode && !e2.context.inputModeInit)), e2.onUpdate && e2.onUpdate(e2);
  };
  var replaceProperties = (e2, t) => {
    const n = Object.keys(t);
    for (let a = 0; a < n.length; a++) {
      const o = n[a];
      "object" != typeof e2[o] || "object" != typeof t[o] || t[o] instanceof Date || Array.isArray(t[o]) ? void 0 !== t[o] && (e2[o] = t[o]) : replaceProperties(e2[o], t[o]);
    }
  };
  var set = (e2, t, n) => {
    replaceProperties(e2, t), e2.context.isInit && update(e2, n);
  };
  function findBestPickerPosition(e2, t) {
    const n = "left";
    if (!t || !e2) return n;
    const { canShow: a, parentPositions: o } = getAvailablePosition(e2, t), l = a.left && a.right;
    return (l && a.bottom ? "center" : l && a.top ? ["top", "center"] : Array.isArray(o) ? ["bottom" === o[0] ? "top" : "bottom", ...o.slice(1)] : o) || n;
  }
  var setPosition = (e2, t, n) => {
    if (!e2) return;
    const a = "auto" === n ? findBestPickerPosition(e2, t) : n, o = { top: -t.offsetHeight, bottom: e2.offsetHeight, left: 0, center: e2.offsetWidth / 2 - t.offsetWidth / 2, right: e2.offsetWidth - t.offsetWidth }, l = Array.isArray(a) ? a[0] : "bottom", s = Array.isArray(a) ? a[1] : a;
    t.dataset.vcPosition = l;
    const { top: i, left: r2 } = getOffset2(e2), c = i + o[l];
    let d2 = r2 + o[s];
    const { vw: u } = getViewportDimensions();
    if (d2 + t.clientWidth > u) {
      const e3 = window.innerWidth - document.body.clientWidth;
      d2 = u - t.clientWidth - e3;
    } else d2 < 0 && (d2 = 0);
    Object.assign(t.style, { left: `${d2}px`, top: `${c}px` });
  };
  var show = (e2) => {
    if (e2.context.isShowInInputMode) return;
    if (!e2.context.currentType) return void e2.context.mainElement.click();
    setContext(e2, "cleanupHandlers", []), setContext(e2, "isShowInInputMode", true), e2.inputMode && restoreTabbing(e2.context.mainElement), setPosition(e2.context.inputElement, e2.context.mainElement, e2.positionToInput), e2.context.mainElement.removeAttribute("data-vc-calendar-hidden");
    const t = () => {
      setPosition(e2.context.inputElement, e2.context.mainElement, e2.positionToInput);
    };
    window.addEventListener("resize", t), e2.context.cleanupHandlers.push((() => window.removeEventListener("resize", t)));
    const n = (t2) => {
      "Escape" === t2.key && hide(e2);
    };
    document.addEventListener("keydown", n), e2.context.cleanupHandlers.push((() => document.removeEventListener("keydown", n)));
    const a = (t2) => {
      t2.target === e2.context.inputElement || e2.context.mainElement.contains(t2.target) || hide(e2);
    };
    document.addEventListener("click", a, { capture: true }), e2.context.cleanupHandlers.push((() => document.removeEventListener("click", a, { capture: true }))), e2.onShow && e2.onShow(e2);
  };
  var labels = { application: "Calendar", navigation: "Calendar Navigation", arrowNext: { month: "Next month", year: "Next list of years" }, arrowPrev: { month: "Previous month", year: "Previous list of years" }, month: "Select month, current selected month:", months: "List of months", year: "Select year, current selected year:", years: "List of years", week: "Days of the week", weekNumber: "Numbers of weeks in a year", dates: "Dates in the current month", selectingTime: "Selecting a time ", inputHour: "Hours", inputMinute: "Minutes", rangeHour: "Slider for selecting hours", rangeMinute: "Slider for selecting minutes", btnKeeping: "Switch AM/PM, current position:" };
  var styles2 = { calendar: "vc", controls: "vc-controls", grid: "vc-grid", column: "vc-column", header: "vc-header", headerContent: "vc-header__content", month: "vc-month", year: "vc-year", arrowPrev: "vc-arrow vc-arrow_prev", arrowNext: "vc-arrow vc-arrow_next", wrapper: "vc-wrapper", content: "vc-content", months: "vc-months", monthsMonth: "vc-months__month", years: "vc-years", yearsYear: "vc-years__year", week: "vc-week", weekDay: "vc-week__day", weekNumbers: "vc-week-numbers", weekNumbersTitle: "vc-week-numbers__title", weekNumbersContent: "vc-week-numbers__content", weekNumber: "vc-week-number", dates: "vc-dates", datesRow: "vc-dates__row", date: "vc-date", dateBtn: "vc-date__btn", datePopup: "vc-date__popup", dateRangeTooltip: "vc-date-range-tooltip", time: "vc-time", timeContent: "vc-time__content", timeHour: "vc-time__hour", timeMinute: "vc-time__minute", timeKeeping: "vc-time__keeping", timeRanges: "vc-time__ranges", timeRange: "vc-time__range" };
  var OptionsCalendar = class {
    constructor() {
      __publicField(this, "type", "default"), __publicField(this, "inputMode", false), __publicField(this, "openOnFocus", true), __publicField(this, "positionToInput", "left"), __publicField(this, "firstWeekday", 1), __publicField(this, "monthsToSwitch", 1), __publicField(this, "themeAttrDetect", "html[data-theme]"), __publicField(this, "locale", "en"), __publicField(this, "dateToday", "today"), __publicField(this, "dateMin", "1970-01-01"), __publicField(this, "dateMax", "2470-12-31"), __publicField(this, "displayDateMin"), __publicField(this, "displayDateMax"), __publicField(this, "displayDatesOutside", true), __publicField(this, "displayDisabledDates", false), __publicField(this, "displayMonthsCount"), __publicField(this, "disableDates", []), __publicField(this, "disableAllDates", false), __publicField(this, "disableDatesPast", false), __publicField(this, "disableDatesGaps", false), __publicField(this, "disableWeekdays", []), __publicField(this, "disableToday", false), __publicField(this, "enableDates", []), __publicField(this, "enableEdgeDatesOnly", true), __publicField(this, "enableDateToggle", true), __publicField(this, "enableWeekNumbers", false), __publicField(this, "enableMonthChangeOnDayClick", true), __publicField(this, "enableJumpToSelectedDate", false), __publicField(this, "selectionDatesMode", "single"), __publicField(this, "selectionMonthsMode", true), __publicField(this, "selectionYearsMode", true), __publicField(this, "selectionTimeMode", false), __publicField(this, "selectedDates", []), __publicField(this, "selectedMonth"), __publicField(this, "selectedYear"), __publicField(this, "selectedHolidays", []), __publicField(this, "selectedWeekends", [0, 6]), __publicField(this, "selectedTime"), __publicField(this, "selectedTheme", "system"), __publicField(this, "timeMinHour", 0), __publicField(this, "timeMaxHour", 23), __publicField(this, "timeMinMinute", 0), __publicField(this, "timeMaxMinute", 59), __publicField(this, "timeControls", "all"), __publicField(this, "timeStepHour", 1), __publicField(this, "timeStepMinute", 1), __publicField(this, "sanitizerHTML", ((e2) => e2)), __publicField(this, "onClickDate"), __publicField(this, "onClickWeekDay"), __publicField(this, "onClickWeekNumber"), __publicField(this, "onClickTitle"), __publicField(this, "onClickMonth"), __publicField(this, "onClickYear"), __publicField(this, "onClickArrow"), __publicField(this, "onChangeTime"), __publicField(this, "onChangeToInput"), __publicField(this, "onCreateDateRangeTooltip"), __publicField(this, "onCreateDateEls"), __publicField(this, "onCreateMonthEls"), __publicField(this, "onCreateYearEls"), __publicField(this, "onInit"), __publicField(this, "onUpdate"), __publicField(this, "onDestroy"), __publicField(this, "onShow"), __publicField(this, "onHide"), __publicField(this, "popups", {}), __publicField(this, "labels", __spreadValues({}, labels)), __publicField(this, "layouts", { default: "", multiple: "", month: "", year: "" }), __publicField(this, "styles", __spreadValues({}, styles2));
    }
  };
  var _Calendar = class e extends OptionsCalendar {
    constructor(t, n) {
      var a;
      super(), __publicField(this, "init", (() => init(this))), __publicField(this, "update", ((e2) => update(this, e2))), __publicField(this, "destroy", (() => destroy(this))), __publicField(this, "show", (() => show(this))), __publicField(this, "hide", (() => hide(this))), __publicField(this, "set", ((e2, t2) => set(this, e2, t2))), __publicField(this, "context"), this.context = __spreadProps(__spreadValues({}, this.context), { locale: { months: { short: [], long: [] }, weekdays: { short: [], long: [] } } }), setContext(this, "mainElement", "string" == typeof t ? null != (a = e.memoizedElements.get(t)) ? a : this.queryAndMemoize(t) : t), n && replaceProperties(this, n);
    }
    queryAndMemoize(t) {
      const n = document.querySelector(t);
      if (!n) throw new Error(errorMessages.notFoundSelector(t));
      return e.memoizedElements.set(t, n), n;
    }
  };
  __publicField(_Calendar, "memoizedElements", /* @__PURE__ */ new Map());
  var Calendar = _Calendar;

  // node_modules/@slickgrid-universal/common/dist/commonEditorFilter/commonEditorFilterUtils.js
  function addAutocompleteLoadingByOverridingFetch(inputElm, autocompleterOptions) {
    const previousFetch = autocompleterOptions.fetch;
    if (previousFetch) {
      autocompleterOptions.fetch = (searchTerm, updateCallback, trigger, cursorPos) => {
        inputElm.classList.add("slick-autocomplete-loading");
        const previousCallback = updateCallback;
        const newUpdateCallback = (items) => {
          previousCallback(items);
          inputElm.classList.remove("slick-autocomplete-loading");
        };
        previousFetch(searchTerm, newUpdateCallback, trigger, cursorPos);
      };
    }
  }
  function getCollectionFromObjectWhenEnabled(collection, columnFilterOrEditor) {
    const collectionOptions = columnFilterOrEditor?.collectionOptions ?? {};
    if (!Array.isArray(collection) && collectionOptions?.collectionInsideObjectProperty && isObject(collection)) {
      const collectionInsideObjectProperty = collectionOptions.collectionInsideObjectProperty;
      collection = getDescendantProperty(collection, collectionInsideObjectProperty || "");
    }
    return collection;
  }
  function resetDatePicker(pickerInstance) {
    const today = /* @__PURE__ */ new Date();
    pickerInstance.selectedDates = [];
    pickerInstance.selectedMonth = today.getMonth();
    pickerInstance.selectedYear = today.getFullYear();
    const dateInputElm = pickerInstance.context.inputElement;
    if (dateInputElm) {
      dateInputElm.value = "";
    }
    pickerInstance.update();
  }
  function createBlankSelectEntry(labelName, valueName, labelPrefixName, labelSuffixName) {
    const blankEntry = {
      [labelName]: "",
      [valueName]: ""
    };
    if (labelPrefixName) {
      blankEntry[labelPrefixName] = "";
    }
    if (labelSuffixName) {
      blankEntry[labelSuffixName] = "";
    }
    return blankEntry;
  }
  function setPickerFocus(pickerElm) {
    setTimeout(() => {
      const buttonElm = pickerElm.querySelector("[data-vc-date-selected] button");
      const outlineElm = buttonElm ?? pickerElm.querySelector('[data-vc="month"]');
      if (outlineElm) {
        outlineElm.focus();
        outlineElm.tabIndex = 0;
      }
    });
  }
  function setPickerDates(colEditorOrFilter, dateInputElm, pickerInstance, options) {
    const { oldVal, newVal, columnDef, selectedSettings, updatePickerUI } = options;
    if (oldVal !== newVal) {
      const inputFieldType = colEditorOrFilter.type || columnDef.type;
      const outputFieldType = columnDef.outputType || colEditorOrFilter.type || columnDef.type || "dateUtc";
      const newDates = Array.isArray(newVal) ? newVal : [newVal || ""];
      const pickerDates = [];
      const isoFormat = mapTempoDateFormatWithFieldType("dateIso");
      const inputFormat = inputFieldType ? mapTempoDateFormatWithFieldType(inputFieldType) : void 0;
      for (const initialDate of newDates) {
        const date2 = initialDate instanceof Date ? initialDate : tryParseDate(initialDate, inputFormat);
        if (date2) {
          pickerDates.push(date2);
        }
      }
      const newSettingSelected = selectedSettings ?? {
        selectedDates: [pickerDates.map((p) => format(p, isoFormat)).join(":")],
        selectedMonth: pickerDates[0]?.getMonth(),
        selectedYear: pickerDates[0]?.getFullYear(),
        selectedTime: inputFormat === "ISO8601" || (inputFormat || "").toLowerCase().includes("h") ? format(pickerDates[0], "HH:mm") : void 0
      };
      if (updatePickerUI !== false && hasCalendarChanges(pickerInstance, newSettingSelected) && pickerInstance instanceof Calendar) {
        pickerInstance.selectedDates = newSettingSelected.selectedDates;
        pickerInstance.selectedMonth = newSettingSelected.selectedMonth;
        pickerInstance.selectedYear = newSettingSelected.selectedYear;
        pickerInstance.selectedTime = newSettingSelected.selectedTime;
        pickerInstance.update();
      }
      dateInputElm.value = newDates.length ? pickerDates.map((p) => formatDateByFieldType(p, void 0, outputFieldType)).join(" \u2014 ") : "";
    }
    function hasCalendarChanges(sourceObj, targetObj) {
      let isChanged = false;
      for (const selectType of ["selectedDates", "selectedMonth", "selectedYear", "selectedTime"]) {
        if (sourceObj[selectType] !== targetObj[selectType]) {
          isChanged = true;
        }
      }
      return isChanged;
    }
  }
  function filterCollectionWithOptions(inputCollection, collectionService, collectionFilterBy, collectionOptions) {
    if (collectionFilterBy) {
      const filterBy = collectionFilterBy;
      const filterCollectionBy = collectionOptions?.filterResultAfterEachPass || null;
      return collectionService?.filterCollection(inputCollection, filterBy, filterCollectionBy) || [];
    }
    return inputCollection;
  }
  function sortCollectionWithOptions(inputCollection, columnDef, collectionService, collectionSortBy, enableTranslateLabel) {
    if (collectionSortBy) {
      const sortBy = collectionSortBy;
      return collectionService?.sortCollection(columnDef, inputCollection, sortBy, enableTranslateLabel) || [];
    }
    return inputCollection;
  }

  // node_modules/@slickgrid-universal/common/dist/core/slickCore.js
  var SlickEventData = class {
    get defaultPrevented() {
      return this._isDefaultPrevented;
    }
    constructor(event, args) {
      this.event = event;
      this.args = args;
      this._isPropagationStopped = false;
      this._isImmediatePropagationStopped = false;
      this._isDefaultPrevented = false;
      this.returnValue = void 0;
      this.nativeEvent = event;
      this._arguments = args;
      if (event) {
        [
          "altKey",
          "ctrlKey",
          "metaKey",
          "shiftKey",
          "key",
          "keyCode",
          "clientX",
          "clientY",
          "offsetX",
          "offsetY",
          "pageX",
          "pageY",
          "bubbles",
          "target",
          "type",
          "which",
          "x",
          "y"
        ].forEach((key) => this[key] = event[key]);
      }
      this._eventTarget = this.nativeEvent ? this.nativeEvent.target : void 0;
    }
    /**
     * Stops event from propagating up the DOM tree.
     * @method stopPropagation
     */
    stopPropagation() {
      this._isPropagationStopped = true;
      this.nativeEvent?.stopPropagation();
    }
    /**
     * Returns whether stopPropagation was called on this event object.
     * @method isPropagationStopped
     * @return {Boolean}
     */
    isPropagationStopped() {
      return this._isPropagationStopped;
    }
    /**
     * Prevents the rest of the handlers from being executed.
     * @method stopImmediatePropagation
     */
    stopImmediatePropagation() {
      this._isImmediatePropagationStopped = true;
      if (this.nativeEvent) {
        this.nativeEvent.stopImmediatePropagation();
      }
    }
    /**
     * Returns whether stopImmediatePropagation was called on this event object.\
     * @method isImmediatePropagationStopped
     * @return {Boolean}
     */
    isImmediatePropagationStopped() {
      return this._isImmediatePropagationStopped;
    }
    getNativeEvent() {
      return this.nativeEvent;
    }
    preventDefault() {
      if (this.nativeEvent) {
        this.nativeEvent.preventDefault();
      }
      this._isDefaultPrevented = true;
    }
    isDefaultPrevented() {
      if (this.nativeEvent) {
        return this.nativeEvent.defaultPrevented;
      }
      return this._isDefaultPrevented;
    }
    addReturnValue(value) {
      if (this.returnValue === void 0 && value !== void 0) {
        this.returnValue = value;
      }
    }
    getReturnValue() {
      return this.returnValue;
    }
    getArguments() {
      return this._arguments;
    }
    resetReturnValue() {
      this.returnValue = void 0;
    }
  };
  var SlickEvent = class {
    get subscriberCount() {
      return this._handlers.length;
    }
    /**
     * Constructor
     * @param {String} [eventName] - event name that could be used for dispatching CustomEvent (when enabled)
     * @param {BasePubSub} [pubSubService] - event name that could be used for dispatching CustomEvent (when enabled)
     */
    constructor(eventName, pubSub) {
      this.eventName = eventName;
      this.pubSub = pubSub;
      this._handlers = [];
      this._pubSubService = pubSub;
    }
    /**
     * Adds an event handler to be called when the event is fired.
     * <p>Event handler will receive two arguments - an <code>EventData</code> and the <code>data</code>
     * object the event was fired with.<p>
     * @method subscribe
     * @param {Function} fn - Event handler.
     */
    subscribe(fn) {
      this._handlers.push(fn);
    }
    /**
     * Removes an event handler added with <code>subscribe(fn)</code>.
     * @method unsubscribe
     * @param {Function} [fn] - Event handler to be removed.
     */
    unsubscribe(fn) {
      for (let i = this._handlers.length - 1; i >= 0; i--) {
        if (this._handlers[i] === fn) {
          this._handlers.splice(i, 1);
        }
      }
    }
    /**
     * Fires an event notifying all subscribers.
     * @method notify
     * @param {Object} args Additional data object to be passed to all handlers.
     * @param {EventData} [event] - An <code>EventData</code> object to be passed to all handlers.
     *      For DOM events, an existing W3C event object can be passed in.
     * @param {Object} [scope] - The scope ("this") within which the handler will be executed.
     *      If not specified, the scope will be set to the <code>Event</code> instance.
     */
    notify(args, evt, scope, ignorePrevEventDataValue = false) {
      const sed = evt instanceof SlickEventData ? evt : new SlickEventData(evt, args);
      if (ignorePrevEventDataValue) {
        sed.resetReturnValue();
      }
      scope = scope || this;
      for (let i = 0; i < this._handlers.length && !(sed.isPropagationStopped() || sed.isImmediatePropagationStopped()); i++) {
        const returnValue = this._handlers[i].call(scope, sed, args);
        sed.addReturnValue(returnValue);
      }
      if (typeof this._pubSubService?.publish === "function" && this.eventName) {
        const ret = this._pubSubService.publish(
          this.eventName,
          { args, eventData: sed },
          void 0,
          // assign the PubSub internal event to our SlickEventData.nativeEvent
          // so that we can call preventDefault() which would return a `returnValue = false`
          (evt2) => sed.nativeEvent ??= evt2
        );
        sed.addReturnValue(ret);
      }
      return sed;
    }
    setPubSubService(pubSub) {
      this._pubSubService = pubSub;
    }
  };
  var SlickEventHandler = class {
    constructor() {
      this.handlers = [];
    }
    get subscriberCount() {
      return this.handlers.length;
    }
    subscribe(event, handler) {
      this.handlers.push({ event, handler });
      event.subscribe(handler);
      return this;
    }
    unsubscribe(event, handler) {
      let i = this.handlers.length;
      while (i--) {
        if (this.handlers[i].event === event && this.handlers[i].handler === handler) {
          this.handlers.splice(i, 1);
          event.unsubscribe(handler);
          return;
        }
      }
      return this;
    }
    unsubscribeAll() {
      let i = this.handlers.length;
      while (i--) {
        this.handlers[i].event.unsubscribe(this.handlers[i].handler);
      }
      this.handlers = [];
      return this;
    }
  };
  var SlickRange = class {
    constructor(fromRow, fromCell, toRow, toCell) {
      if (toRow === void 0 && toCell === void 0) {
        toRow = fromRow;
        toCell = fromCell;
      }
      this.fromRow = Math.min(fromRow, toRow);
      this.fromCell = Math.min(fromCell, toCell);
      this.toRow = Math.max(fromRow, toRow);
      this.toCell = Math.max(fromCell, toCell);
    }
    /**
     * Returns whether a range represents a single cell.
     * @method isSingleCell
     * @return {Boolean}
     */
    isSingleCell() {
      return this.fromRow === this.toRow && this.fromCell === this.toCell;
    }
    /**
     * Returns whether a range represents a single row.
     * @method isSingleRow
     * @return {Boolean}
     */
    isSingleRow() {
      return this.fromRow === this.toRow;
    }
    /**
     * Returns whether a range contains a given cell.
     * @method contains
     * @param row {Integer}
     * @param cell {Integer}
     * @return {Boolean}
     */
    contains(row, cell) {
      return row >= this.fromRow && row <= this.toRow && cell >= this.fromCell && cell <= this.toCell;
    }
    /**
     * Row Count.
     * @method rowCount
     * @return {Number}
     */
    rowCount() {
      return this.toRow - this.fromRow + 1;
    }
    /**
     * Cell Count.
     * @method cellCount
     * @return {Number}
     */
    cellCount() {
      return this.toCell - this.fromCell + 1;
    }
    /**
     * Returns a readable representation of a range.
     * @method toString
     * @return {String}
     */
    toString() {
      if (this.isSingleCell()) {
        return `(${this.fromRow}:${this.fromCell})`;
      } else {
        return `(${this.fromRow}:${this.fromCell} - ${this.toRow}:${this.toCell})`;
      }
    }
  };
  var SlickDragExtendHandle = class {
    constructor(gridUid) {
      this.cssClass = "slick-drag-replace-handle";
      this.id = `${gridUid}_drag_replace_handle`;
    }
    removeEl() {
      document.getElementById(this.id)?.remove();
    }
    createEl(activeCellNode) {
      if (activeCellNode) {
        const dragReplaceEl = document.createElement("div");
        dragReplaceEl.classList.add("slick-drag-replace-handle");
        dragReplaceEl.id = this.id;
        activeCellNode.appendChild(dragReplaceEl);
      }
    }
  };
  var SlickNonDataItem = class {
    constructor() {
      this.__nonDataRow = true;
    }
  };
  var SlickGroup = class extends SlickNonDataItem {
    constructor() {
      super();
      this.__group = true;
      this.level = 0;
      this.count = 0;
      this.value = null;
      this.title = null;
      this.collapsed = false;
      this.selectChecked = false;
      this.totals = null;
      this.rows = [];
      this.groups = null;
      this.groupingKey = "";
    }
    /**
     * Compares two Group instances.
     * @method equals
     * @return {Boolean}
     * @param group {Group} Group instance to compare to.
     */
    equals(group) {
      return this.value === group.value && this.count === group.count && this.collapsed === group.collapsed && this.title === group.title;
    }
  };
  var SlickGroupTotals = class extends SlickNonDataItem {
    constructor() {
      super();
      this.__groupTotals = true;
      this.group = null;
      this.initialized = false;
    }
  };
  var SlickEditorLock = class {
    constructor() {
      this.activeEditController = null;
    }
    /**
     * Returns true if a specified edit controller is active (has the edit lock).
     * If the parameter is not specified, returns true if any edit controller is active.
     * @method isActive
     * @param editController {EditController}
     * @return {Boolean}
     */
    isActive(editController) {
      return editController ? this.activeEditController === editController : this.activeEditController !== null;
    }
    /**
     * Sets the specified edit controller as the active edit controller (acquire edit lock).
     * If another edit controller is already active, and exception will be throw new Error(.
     * @method activate
     * @param editController {EditController} edit controller acquiring the lock
     */
    activate(editController) {
      if (editController === this.activeEditController) {
        return;
      }
      if (this.activeEditController !== null) {
        throw new Error(`SlickEditorLock.activate: an editController is still active, can't activate another editController`);
      }
      if (!editController.commitCurrentEdit) {
        throw new Error("SlickEditorLock.activate: editController must implement .commitCurrentEdit()");
      }
      if (!editController.cancelCurrentEdit) {
        throw new Error("SlickEditorLock.activate: editController must implement .cancelCurrentEdit()");
      }
      this.activeEditController = editController;
    }
    /**
     * Unsets the specified edit controller as the active edit controller (release edit lock).
     * If the specified edit controller is not the active one, an exception will be throw new Error(.
     * @method deactivate
     * @param editController {EditController} edit controller releasing the lock
     */
    deactivate(editController) {
      if (!this.activeEditController) {
        return;
      }
      if (this.activeEditController !== editController) {
        throw new Error("SlickEditorLock.deactivate: specified editController is not the currently active one");
      }
      this.activeEditController = null;
    }
    /**
     * Attempts to commit the current edit by calling "commitCurrentEdit" method on the active edit
     * controller and returns whether the commit attempt was successful (commit may fail due to validation
     * errors, etc.).  Edit controller's "commitCurrentEdit" must return true if the commit has succeeded
     * and false otherwise.  If no edit controller is active, returns true.
     * @method commitCurrentEdit
     * @return {Boolean}
     */
    commitCurrentEdit() {
      return this.activeEditController ? this.activeEditController.commitCurrentEdit() : true;
    }
    /**
     * Attempts to cancel the current edit by calling "cancelCurrentEdit" method on the active edit
     * controller and returns whether the edit was successfully cancelled.  If no edit controller is
     * active, returns true.
     * @method cancelCurrentEdit
     * @return {Boolean}
     */
    cancelCurrentEdit() {
      return this.activeEditController ? this.activeEditController.cancelCurrentEdit() : true;
    }
  };
  var Utils = class _Utils {
    static height(el, value) {
      if (!el) {
        return;
      }
      if (value === void 0) {
        return el.getBoundingClientRect().height;
      }
      _Utils.setStyleSize(el, "height", value);
    }
    static width(el, value) {
      if (!el || !el.getBoundingClientRect) {
        return;
      }
      if (value === void 0) {
        return el.getBoundingClientRect().width;
      }
      _Utils.setStyleSize(el, "width", value);
    }
    static setStyleSize(el, style, val) {
      if (typeof val === "function") {
        val = val();
      }
      setStyles(el, { [style]: typeof val === "string" ? val : `${val}px` });
    }
    static isHidden(el) {
      return el.offsetWidth === 0 && el.offsetHeight === 0;
    }
    static parents(el, selector) {
      const parents = [];
      const visible = selector === ":visible";
      const hidden = selector === ":hidden";
      while ((el = el.parentNode) && el !== document) {
        if (!el || !el.parentNode) {
          break;
        }
        if (hidden) {
          if (_Utils.isHidden(el)) {
            parents.push(el);
          }
        } else if (visible) {
          if (!_Utils.isHidden(el)) {
            parents.push(el);
          }
        } else if (!selector || el.matches(selector)) {
          parents.push(el);
        }
      }
      return parents;
    }
    static toFloat(value) {
      const x2 = parseFloat(value);
      if (isNaN(x2)) {
        return 0;
      }
      return x2;
    }
    static show(el, type = "") {
      if (Array.isArray(el)) {
        el.forEach((e2) => e2.style.display = type);
      } else {
        el.style.display = type;
      }
    }
    static hide(el) {
      if (Array.isArray(el)) {
        el.forEach((e2) => e2.style.display = "none");
      } else {
        el.style.display = "none";
      }
    }
    static applyDefaults(targetObj, srcObj) {
      if (typeof srcObj === "object") {
        Object.keys(srcObj).forEach((key) => {
          if (srcObj.hasOwnProperty(key) && !targetObj.hasOwnProperty(key)) {
            targetObj[key] = srcObj[key];
          }
        });
      }
    }
    /**
     * User could optionally add PubSub Service to SlickEvent
     * When it is defined then a SlickEvent `notify()` call will also dispatch it by using the PubSub publish() method
     * @param {BasePubSub} [pubSubService]
     * @param {*} scope
     */
    static addSlickEventPubSubWhenDefined(pubSub, scope) {
      if (pubSub) {
        for (const prop in scope) {
          if (scope[prop] instanceof SlickEvent && typeof scope[prop].setPubSubService === "function") {
            scope[prop].setPubSubService(pubSub);
          }
        }
      }
    }
  };
  Utils.storage = {
    // https://stackoverflow.com/questions/29222027/vanilla-alternative-to-jquery-data-function-any-native-javascript-alternati
    _storage: /* @__PURE__ */ new WeakMap(),
    put: function(element, key, obj) {
      if (!this._storage.has(element)) {
        this._storage.set(element, /* @__PURE__ */ new Map());
      }
      this._storage.get(element).set(key, obj);
    },
    get: function(element, key) {
      const el = this._storage.get(element);
      if (el) {
        return el.get(key);
      }
      return null;
    },
    remove: function(element, key) {
      const ret = this._storage.get(element).delete(key);
      if (!(this._storage.get(element).size === 0)) {
        this._storage.delete(element);
      }
      return ret;
    }
  };
  var SlickSelectionUtils = class {
    //   |---0----|---1----|---2----|---3----|---4----|---5----|
    // 0 |        |        |        |     ^  |        |        |
    //   |--------|--------|--------|--------|--------|--------|
    // 1 |        |        |        |        |        |        |
    //   |--------|--------|--------|--------|--------|--------|
    // 2 |        |        |   1    |   2    |    > h |        |
    //   |--------|--------|--------|--------|--------|--------|
    // 3 |   <    |        |   4    |   5   x|    > h |    >   |
    //   |--------|--------|--------|--------|--------|--------|
    // 4 |        |        |    > v |    > v |    > v |        |
    //   |--------|--------|--------|--------|--------|--------|
    // 5 |        |        |        |    v   |        |        |
    //   |--------|--------|--------|--------|--------|--------|
    //
    // original range (1,2,4,5) expanded one cell to right and down
    //  '> h' indicates horizontal target copy area
    //  '> v' indicates vertical target copy area
    // note bottom right (corner) cell is considered part of vertical copy area
    static normaliseDragRange(rawRange) {
      const rtn = {
        start: {
          row: (rawRange.end.row ?? 0) > (rawRange.start.row ?? 0) ? rawRange.start.row : rawRange.end.row,
          cell: (rawRange.end.cell ?? 0) > (rawRange.start.cell ?? 0) ? rawRange.start.cell : rawRange.end.cell
        },
        end: {
          row: (rawRange.end.row ?? 0) > (rawRange.start.row ?? 0) ? rawRange.end.row : rawRange.start.row,
          cell: (rawRange.end.cell ?? 0) > (rawRange.start.cell ?? 0) ? rawRange.end.cell : rawRange.start.cell
        }
      };
      rtn.rowCount = (rtn.end.row ?? 0) - (rtn.start.row ?? 0) + 1;
      rtn.cellCount = (rtn.end.cell ?? 0) - (rtn.start.cell ?? 0) + 1;
      rtn.wasDraggedUp = (rawRange.end.row ?? 0) < (rawRange.start.row ?? 0);
      rtn.wasDraggedLeft = (rawRange.end.row ?? 0) < (rawRange.start.row ?? 0);
      return rtn;
    }
    static copyRangeIsLarger(baseRange, copyToRange) {
      return copyToRange.fromRow < baseRange.fromRow || copyToRange.fromCell < baseRange.fromCell || copyToRange.toRow > baseRange.toRow || copyToRange.toCell > baseRange.toCell;
    }
    static normalRangeOppositeCellFromCopy(normalisedDragRange, targetCell) {
      return {
        row: targetCell.row < (normalisedDragRange.end.row || 0) ? normalisedDragRange.end.row || 0 : normalisedDragRange.start.row || 0,
        cell: targetCell.cell < (normalisedDragRange.end.cell || 0) ? normalisedDragRange.end.cell || 0 : normalisedDragRange.start.cell || 0
      };
    }
    /** copy to range above or below - includes corner space target range */
    static verticalTargetRange(baseRange, copyToRange) {
      const copyUp = copyToRange.fromRow < baseRange.fromRow;
      const copyDown = copyToRange.toRow > baseRange.toRow;
      if (!copyUp && !copyDown) {
        return null;
      }
      return copyUp ? new SlickRange(copyToRange.fromRow, copyToRange.fromCell, baseRange.fromRow - 1, baseRange.toCell) : new SlickRange(baseRange.toRow + 1, copyToRange.fromCell, copyToRange.toRow, baseRange.toCell);
    }
    /** copy to range left or right - excludes corner space target range */
    static horizontalTargetRange(baseRange, copyToRange) {
      const copyLeft = copyToRange.fromCell < baseRange.fromCell;
      const copyRight = copyToRange.toCell > baseRange.toCell;
      if (!copyLeft && !copyRight) {
        return null;
      }
      return copyLeft ? new SlickRange(baseRange.fromRow, copyToRange.fromCell, baseRange.toRow, baseRange.fromCell - 1) : new SlickRange(baseRange.fromRow, baseRange.toCell + 1, baseRange.toRow, copyToRange.toCell);
    }
    /** copy to corner space target range */
    static cornerTargetRange(baseRange, copyToRange) {
      const copyUp = copyToRange.fromRow < baseRange.fromRow;
      const copyDown = copyToRange.toRow > baseRange.toRow;
      const copyLeft = copyToRange.fromCell < baseRange.fromCell;
      const copyRight = copyToRange.toCell > baseRange.toCell;
      if (!copyLeft && !copyRight || !copyUp && !copyDown) {
        return null;
      }
      return copyLeft ? copyUp ? new SlickRange(copyToRange.fromRow, copyToRange.fromCell, baseRange.fromRow - 1, baseRange.fromCell - 1) : new SlickRange(baseRange.toRow + 1, copyToRange.fromCell, copyToRange.toRow, baseRange.fromCell - 1) : copyUp ? new SlickRange(copyToRange.fromRow, baseRange.toCell + 1, baseRange.fromRow - 1, copyToRange.toCell) : new SlickRange(baseRange.toRow + 1, baseRange.toCell + 1, copyToRange.toRow, copyToRange.toCell);
    }
    static copyCellsToTargetRange(baseRange, targetRange, grid) {
      const columns = grid.getVisibleColumns();
      const options = grid.getOptions();
      let fromRowOffset = 0;
      let fromCellOffset = 0;
      for (let i = 0; i < targetRange.rowCount(); i++) {
        const toRow = grid.getDataItem(targetRange.fromRow + i);
        const fromRow = grid.getDataItem(baseRange.fromRow + fromRowOffset);
        fromCellOffset = 0;
        for (let j2 = 0; j2 < targetRange.cellCount(); j2++) {
          const toColDef = columns[targetRange.fromCell + j2];
          const fromColDef = columns[baseRange.fromCell + fromCellOffset];
          if (toColDef && fromColDef && !toColDef.hidden && !fromColDef.hidden) {
            let val = fromRow[fromColDef.field];
            if (options.dataItemColumnValueExtractor) {
              val = options.dataItemColumnValueExtractor(fromRow, fromColDef);
            }
            toRow[toColDef.field] = val;
          }
          fromCellOffset++;
          if (fromCellOffset >= baseRange.cellCount()) {
            fromCellOffset = 0;
          }
        }
        fromRowOffset++;
        if (fromRowOffset >= baseRange.rowCount()) {
          fromRowOffset = 0;
        }
      }
    }
  };
  var SlickGlobalEditorLock = new SlickEditorLock();
  var preClickClassName = "slick-edit-preclick";

  // node_modules/@slickgrid-universal/common/dist/extensions/slickGroupItemMetadataProvider.js
  var SlickGroupItemMetadataProvider = class {
    constructor(inputOptions) {
      this.pluginName = "GroupItemMetadataProvider";
      this._defaults = {
        groupCssClass: "slick-group",
        groupTitleCssClass: "slick-group-title",
        totalsCssClass: "slick-group-totals",
        groupFocusable: true,
        indentation: 15,
        totalsFocusable: false,
        toggleCssClass: "slick-group-toggle",
        toggleExpandedCssClass: "expanded",
        toggleCollapsedCssClass: "collapsed",
        toggleOnNodeTitle: false,
        enableExpandCollapse: true,
        groupFormatter: this.defaultGroupCellFormatter.bind(this),
        totalsFormatter: this.defaultTotalsCellFormatter.bind(this),
        includeHeaderTotals: false
      };
      this._eventHandler = new SlickEventHandler();
      this._options = extend(true, {}, this._defaults, inputOptions);
    }
    /** Getter of the SlickGrid Event Handler */
    get eventHandler() {
      return this._eventHandler;
    }
    /** Getter of SlickGrid DataView object */
    get dataView() {
      return this._grid?.getData() ?? {};
    }
    get gridOptions() {
      return this._grid?.getOptions() || {};
    }
    init(grid) {
      this._grid = grid;
      this._eventHandler.subscribe(grid.onClick, this.handleGridClick.bind(this));
      this._eventHandler.subscribe(grid.onKeyDown, this.handleGridKeyDown.bind(this));
    }
    destroy() {
      this.dispose();
    }
    dispose() {
      this._eventHandler?.unsubscribeAll();
    }
    getOptions() {
      return this._options;
    }
    setOptions(inputOptions) {
      this._options = { ...this._options, ...inputOptions };
    }
    getGroupRowMetadata(item, _row) {
      return {
        selectable: false,
        focusable: this._options.groupFocusable,
        cssClasses: `${this._options.groupCssClass} slick-group-level-${item?.level || 0}`,
        formatter: this._options.includeHeaderTotals && this._options.totalsFormatter || void 0,
        isGroup: true,
        columns: {
          0: {
            colspan: this._options.includeHeaderTotals ? "1" : "*",
            formatter: this._options.groupFormatter,
            editorClass: null
          }
        }
      };
    }
    // prettier-ignore
    getTotalsRowMetadata(item, _row) {
      return {
        selectable: false,
        focusable: this._options.totalsFocusable,
        cssClasses: `${this._options.totalsCssClass} slick-group-level-${item?.group?.level || 0}`,
        formatter: this._options.totalsFormatter,
        editorClass: null,
        isGroup: true
      };
    }
    //
    // protected functions
    // -------------------
    defaultGroupCellFormatter(_row, _cell, _value, _columnDef, item) {
      if (!this._options.enableExpandCollapse) {
        return item.title;
      }
      const groupLevel = item.level || 0;
      const indentation = this._options?.indentation ?? 15;
      const marginLeft = `${groupLevel * indentation}px`;
      const toggleClass2 = item.collapsed ? this._options.toggleCollapsedCssClass : this._options.toggleExpandedCssClass;
      const containerElm = createDocumentFragmentOrElement(this.gridOptions);
      containerElm.appendChild(createDomElement("span", {
        className: `${this._options.toggleCssClass} ${toggleClass2}`,
        ariaExpanded: String(!item.collapsed),
        style: { marginLeft }
      }));
      const groupTitleElm = createDomElement("span", { className: this._options.groupTitleCssClass || "" });
      groupTitleElm.setAttribute("level", groupLevel);
      if (this._options?.toggleOnNodeTitle) {
        groupTitleElm.classList.add("pointer");
      }
      isHtml(item.title) ? groupTitleElm.appendChild(item.title) : applyHtmlToElement(groupTitleElm, item.title ?? "", this.gridOptions);
      containerElm.appendChild(groupTitleElm);
      return containerElm;
    }
    defaultTotalsCellFormatter(_row, _cell, _value, columnDef, item, grid) {
      return columnDef?.groupTotalsFormatter?.(item, columnDef, grid) ?? "";
    }
    /** Handle a grid cell clicked, it could be a Group that is being collapsed/expanded or do nothing when it's not */
    handleGridClick(e2, args) {
      const target = e2.target;
      const item = this._grid?.getDataItem(args.row);
      const toggleCssClass = this._options.toggleCssClass || "";
      const groupTitleCssClass = this._options.groupTitleCssClass || "";
      const isIconClicked = target?.classList.contains(toggleCssClass);
      const isElmToggled = this._options?.toggleOnNodeTitle ? isIconClicked || target?.closest(`.${toggleCssClass},.${groupTitleCssClass}`) : isIconClicked;
      if (item instanceof SlickGroup && isElmToggled) {
        this.handleDataViewExpandOrCollapse(item);
        e2.stopImmediatePropagation();
        e2.preventDefault();
      }
    }
    /**
     * Handle a keyboard down event on a grouping cell.
     * Supports Space, ArrowRight (expand), and ArrowLeft (collapse) for a11y consistency.
     */
    handleGridKeyDown(e2) {
      if (this._options.enableExpandCollapse && !this._grid.getEditorLock()?.isActive() && // do not intercept keyboard actions while inline editor is active
      (e2.key === "ArrowLeft" || e2.key === "ArrowRight" || e2.key === " ")) {
        const activeCell = this._grid?.getActiveCell();
        if (activeCell) {
          const item = this._grid.getDataItem(activeCell.row);
          const shouldToggle = item instanceof SlickGroup && (e2.key === " " || e2.key === "ArrowRight" && item.collapsed || e2.key === "ArrowLeft" && !item.collapsed);
          if (shouldToggle) {
            this.handleDataViewExpandOrCollapse(item);
            e2.stopImmediatePropagation();
            e2.preventDefault();
          }
        }
      }
    }
    handleDataViewExpandOrCollapse(item) {
      const range2 = this._grid?.getRenderedRange();
      this.dataView.setRefreshHints({
        ignoreDiffsBefore: range2.top,
        ignoreDiffsAfter: range2.bottom + 1
      });
      if (item.collapsed) {
        this.dataView.expandGroup(item.groupingKey);
      } else {
        this.dataView.collapseGroup(item.groupingKey);
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/multipleFormatter.js
  var multipleFormatter = (row, cell, value, columnDef, dataContext, grid) => {
    const params = columnDef.params || {};
    if (!params.formatters || !Array.isArray(params.formatters)) {
      throw new Error(`[Slickgrid-Universal] The multiple formatter requires the "formatters" to be provided as a column params.
    For example: this.columns = [{ id: title, field: title, formatter: Formatters.multiple, params: { formatters: [Formatters.lowercase, Formatters.uppercase] }`);
    }
    const formatters = params.formatters;
    let currentValue = value;
    formatters.forEach((formatter, idx) => {
      if (typeof formatter !== "function") {
        throw new Error(`[Slickgrid-Universal] the "params.formatters" at index(${idx}) to be used by "Formatters.multiple" is invalid, please verify all formatter functions.`);
      }
      currentValue = formatter.call(void 0, row, cell, currentValue, columnDef, dataContext, grid);
    });
    return currentValue;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/formatterUtilities.js
  function autoAddEditorFormatterToColumnsWithEditor(columns, customEditableFormatter) {
    if (Array.isArray(columns)) {
      for (const columnDef of columns) {
        if (columnDef.editor) {
          if (columnDef.formatter && columnDef.formatter !== multipleFormatter && columnDef.formatter !== customEditableFormatter) {
            const prevFormatter = columnDef.formatter;
            columnDef.formatter = multipleFormatter;
            columnDef.params = { ...columnDef.params, formatters: [prevFormatter, customEditableFormatter] };
          } else if (columnDef.formatter && columnDef.formatter === multipleFormatter && columnDef.params) {
            if (columnDef.params.formatters.findIndex((formatter) => formatter === customEditableFormatter) === -1) {
              columnDef.params.formatters = [...columnDef.params.formatters, customEditableFormatter];
            }
          } else {
            columnDef.formatter = customEditableFormatter;
          }
        }
      }
    }
  }
  function getCopyCellValue(args) {
    const grid = args?.grid || {};
    const gridOptions = grid.getOptions();
    const cell = args?.cell ?? 0;
    const row = args?.row ?? 0;
    const columnDef = args?.column;
    const dataContext = args?.dataContext;
    const exportOptions = gridOptions && (gridOptions.excelExportOptions || gridOptions.textExportOptions);
    let textToCopy = exportWithFormatterWhenDefined(row, cell, columnDef, dataContext, grid, exportOptions);
    if (typeof columnDef.queryFieldNameGetterFn === "function") {
      textToCopy = getCellValueFromQueryFieldGetter(columnDef, dataContext, "");
    }
    if (typeof textToCopy === "string") {
      textToCopy = textToCopy.replace(/^([·⮞⮟]\s*)|([·⮞⮟])\s*/gi, "").replace(/[\u00b7\u034f]/gi, "").trim();
    }
    return textToCopy;
  }
  async function copyCellToClipboard(args) {
    let textToCopy = "";
    try {
      const grid = args?.grid || {};
      const gridOptions = grid.getOptions();
      textToCopy = getCopyCellValue(args);
      const clipboardOverrideFn = gridOptions.clipboardWriteOverride;
      clipboardOverrideFn ? clipboardOverrideFn(textToCopy) : await navigator.clipboard.writeText(textToCopy);
    } catch (err) {
      console.error(`Unable to read/write to clipboard. Please check your browser settings or permissions. Error: ${err}`);
    }
    return textToCopy;
  }
  function retrieveFormatterOptions(columnDef, grid, numberType, formatterType) {
    let defaultMinDecimal;
    let defaultMaxDecimal;
    let numberPrefix = "";
    let numberSuffix = "";
    switch (numberType) {
      case "currency":
        defaultMinDecimal = Constants.DEFAULT_FORMATTER_CURRENCY_MIN_DECIMAL;
        defaultMaxDecimal = Constants.DEFAULT_FORMATTER_CURRENCY_MAX_DECIMAL;
        break;
      case "decimal":
        defaultMinDecimal = Constants.DEFAULT_FORMATTER_NUMBER_MIN_DECIMAL;
        defaultMaxDecimal = Constants.DEFAULT_FORMATTER_NUMBER_MAX_DECIMAL;
        break;
      case "percent":
        defaultMinDecimal = Constants.DEFAULT_FORMATTER_PERCENT_MIN_DECIMAL;
        defaultMaxDecimal = Constants.DEFAULT_FORMATTER_PERCENT_MAX_DECIMAL;
        break;
      default:
        break;
    }
    const gridOptions = grid.getOptions?.() ?? {};
    const minDecimal = getValueFromParamsOrFormatterOptions("minDecimal", columnDef, gridOptions, defaultMinDecimal);
    const maxDecimal = getValueFromParamsOrFormatterOptions("maxDecimal", columnDef, gridOptions, defaultMaxDecimal);
    const decimalSeparator = getValueFromParamsOrFormatterOptions("decimalSeparator", columnDef, gridOptions, Constants.DEFAULT_NUMBER_DECIMAL_SEPARATOR);
    const thousandSeparator = getValueFromParamsOrFormatterOptions("thousandSeparator", columnDef, gridOptions, Constants.DEFAULT_NUMBER_THOUSAND_SEPARATOR);
    const wrapNegativeNumber = getValueFromParamsOrFormatterOptions("displayNegativeNumberWithParentheses", columnDef, gridOptions, Constants.DEFAULT_NEGATIVE_NUMBER_WRAPPED_IN_BRAQUET);
    const currencyPrefix = getValueFromParamsOrFormatterOptions("currencyPrefix", columnDef, gridOptions, "");
    const currencySuffix = getValueFromParamsOrFormatterOptions("currencySuffix", columnDef, gridOptions, "");
    if (formatterType === "cell") {
      numberPrefix = getValueFromParamsOrFormatterOptions("numberPrefix", columnDef, gridOptions, "");
      numberSuffix = getValueFromParamsOrFormatterOptions("numberSuffix", columnDef, gridOptions, "");
    }
    return {
      minDecimal,
      maxDecimal,
      decimalSeparator,
      thousandSeparator,
      wrapNegativeNumber,
      currencyPrefix,
      currencySuffix,
      numberPrefix,
      numberSuffix
    };
  }
  function getValueFromParamsOrFormatterOptions(optionName, columnDef, gridOptions, defaultValue) {
    const params = columnDef.params;
    if (params?.hasOwnProperty(optionName)) {
      return params[optionName];
    } else if (gridOptions?.formatterOptions?.hasOwnProperty(optionName)) {
      return gridOptions.formatterOptions[optionName];
    }
    return defaultValue;
  }
  function getAssociatedDateFormatter(fieldType, defaultSeparator) {
    const defaultDateFormat = mapTempoDateFormatWithFieldType(fieldType, { withZeroPadding: true });
    return (_row, _cell, value, columnDef, _dataContext, grid) => {
      const gridOptions = grid.getOptions?.() ?? {};
      const customSeparator = gridOptions?.formatterOptions?.dateSeparator ?? defaultSeparator;
      const inputType = columnDef?.type ?? "date";
      const inputDateFormat = mapTempoDateFormatWithFieldType(inputType, { withDefaultIso8601: true });
      let outputDate = parseDateByIOFormats(columnDef, value, inputDateFormat, defaultDateFormat);
      if (outputDate && customSeparator !== defaultSeparator) {
        const regex = new RegExp(defaultSeparator, "ig");
        outputDate = outputDate.replace(regex, customSeparator);
      }
      return outputDate;
    };
  }
  function getBaseDateFormatter() {
    return (_row, _cell, value, columnDef) => {
      const inputType = columnDef?.type ?? "date";
      const inputDateFormat = mapTempoDateFormatWithFieldType(inputType, { withDefaultIso8601: true });
      const outpuDateFormat = columnDef.params?.dateFormat;
      if (!outpuDateFormat) {
        throw new Error(`[Slickgrid-Universal] Using the base "Formatter.date" requires "params.outputFormat" defined and was not found in your column "${columnDef.id}".`);
      }
      return parseDateByIOFormats(columnDef, value, inputDateFormat, outpuDateFormat);
    };
  }
  function exportWithFormatterWhenDefined(row, col, columnDef, dataContext, grid, exportOptions, skipSanitization = false) {
    let isEvaluatingFormatter = false;
    if (columnDef?.hasOwnProperty("exportWithFormatter")) {
      isEvaluatingFormatter = !!columnDef.exportWithFormatter;
    } else if (exportOptions?.hasOwnProperty("exportWithFormatter")) {
      isEvaluatingFormatter = !!exportOptions.exportWithFormatter;
    }
    let formatter;
    if (dataContext && columnDef.exportCustomFormatter) {
      formatter = columnDef.exportCustomFormatter;
    } else if (isEvaluatingFormatter && columnDef.formatter) {
      formatter = columnDef.formatter;
    }
    const output = parseFormatterWhenExist(formatter, row, col, columnDef, dataContext, grid);
    if (skipSanitization) {
      return output;
    }
    return exportOptions?.sanitizeDataExport && typeof output === "string" ? stripTags(output) : output;
  }
  function parseFormatterWhenExist(formatter, row, col, columnDef, dataContext, grid) {
    const fieldId = columnDef.field || columnDef.id || "";
    const fieldProperty = typeof fieldId === "string" && fieldId.indexOf(".") > 0 ? fieldId.split(".")[0] : fieldId;
    const hasOwnProp = !!dataContext && Object.prototype.hasOwnProperty.call(dataContext, fieldProperty);
    const cellValue = hasOwnProp ? dataContext[fieldProperty] : null;
    let output;
    if (typeof formatter === "function") {
      const formattedData = formatter(row, col, cellValue, columnDef, dataContext, grid);
      const cellResult = isPrimitiveOrHTML(formattedData) ? formattedData : formattedData.html || formattedData.text;
      output = getHtmlStringOutput(cellResult);
    } else {
      output = cellValue;
    }
    if (output == null) {
      return "";
    }
    if (typeof output === "object" && !(output instanceof Date) && Object.keys(output).length === 0) {
      return "";
    }
    return output;
  }
  function parseDateByIOFormats(columnDef, value, inputDateFormat, outputDateFormat) {
    const isParsingAsUtc = columnDef.params?.parseDateAsUtc ?? false;
    const d1 = tryParseDate(value, inputDateFormat);
    let outputDate = value;
    if (d1) {
      const d2 = isParsingAsUtc ? toUtcDate(d1) : d1;
      outputDate = format(d2, outputDateFormat, "en-US");
    }
    return outputDate;
  }

  // node_modules/@slickgrid-universal/common/dist/core/slickDataview.js
  function isLiveDomFormatterResult(result) {
    if (result) {
      if (isHtml(result)) {
        return true;
      }
      if (typeof result === "object") {
        const htmlResult = result.html;
        if (isHtml(htmlResult)) {
          return true;
        }
      }
    }
    return false;
  }
  var SlickDataView = class {
    constructor(options, externalPubSub) {
      this.externalPubSub = externalPubSub;
      this.defaults = {
        globalItemMetadataProvider: null,
        groupItemMetadataProvider: null,
        inlineFilters: false,
        useCSPSafeFilter: false
      };
      this.idProperty = "id";
      this.items = [];
      this.rows = [];
      this.idxById = /* @__PURE__ */ new Map();
      this.rowsById = void 0;
      this.filter = null;
      this.filterCSPSafe = null;
      this.updated = null;
      this.suspend = false;
      this.isBulkSuspend = false;
      this.bulkDeleteIds = /* @__PURE__ */ new Map();
      this.sortAsc = true;
      this.refreshHints = {};
      this.prevRefreshHints = {};
      this.filteredItems = [];
      this.filterCache = [];
      this.formattedDataCache = {};
      this.formattedCellCache = {};
      this.formattedCacheMetadata = {
        isPopulating: false,
        lastProcessedRow: -1,
        totalFormattedCells: 0
      };
      this._populateCacheRafId = void 0;
      this._cachePopulationGeneration = 0;
      this.groupingInfoDefaults = {
        getter: void 0,
        formatter: void 0,
        comparer: (a, b) => a.value === b.value ? 0 : a.value > b.value ? 1 : -1,
        predefinedValues: [],
        aggregators: [],
        aggregateEmpty: false,
        aggregateCollapsed: false,
        aggregateChildGroups: false,
        collapsed: false,
        displayTotalsRow: true,
        lazyTotalsCalculation: false
      };
      this.groupingInfos = [];
      this.groups = [];
      this.toggledGroupsByLevel = [];
      this.groupingDelimiter = ":|:";
      this.selectedRowIds = [];
      this.pagesize = 0;
      this.pagenum = 0;
      this.totalRows = 0;
      this.onBeforePagingInfoChanged = new SlickEvent("onBeforePagingInfoChanged", externalPubSub);
      this.onGroupExpanded = new SlickEvent("onGroupExpanded", externalPubSub);
      this.onGroupCollapsed = new SlickEvent("onGroupCollapsed", externalPubSub);
      this.onPagingInfoChanged = new SlickEvent("onPagingInfoChanged", externalPubSub);
      this.onRowCountChanged = new SlickEvent("onRowCountChanged", externalPubSub);
      this.onRowsChanged = new SlickEvent("onRowsChanged", externalPubSub);
      this.onRowsOrCountChanged = new SlickEvent("onRowsOrCountChanged", externalPubSub);
      this.onSelectedRowIdsChanged = new SlickEvent("onSelectedRowIdsChanged", externalPubSub);
      this.onSetItemsCalled = new SlickEvent("onSetItemsCalled", externalPubSub);
      this.onFormattedDataCacheProgress = new SlickEvent("onFormattedDataCacheProgress", externalPubSub);
      this.onFormattedDataCacheCompleted = new SlickEvent("onFormattedDataCacheCompleted", externalPubSub);
      this._options = extend(true, {}, this.defaults, options);
    }
    /**
     * Begins a bached update of the items in the data view.
     * including deletes and the related events are postponed to the endUpdate call.
     * As certain operations are postponed during this update, some methods might not
     * deliver fully consistent information.
     * @param {Boolean} [bulkUpdate] - if set to true, most data view modifications
     */
    beginUpdate(bulkUpdate) {
      this.suspend = true;
      this.isBulkSuspend = bulkUpdate === true;
    }
    endUpdate() {
      const wasBulkSuspend = this.isBulkSuspend;
      this.isBulkSuspend = false;
      this.suspend = false;
      if (wasBulkSuspend) {
        this.processBulkDelete();
        this.ensureIdUniqueness();
      }
      this.refresh();
    }
    destroy() {
      this.items = [];
      this.idProperty = "id";
      this.idxById = null;
      this.rowsById = null;
      this.filter = null;
      this.filterCSPSafe = null;
      this.updated = null;
      this.sortComparer = null;
      this.filterCache = [];
      this.filteredItems = [];
      this.compiledFilter = null;
      this.compiledFilterCSPSafe = null;
      this.compiledFilterWithCaching = null;
      this.compiledFilterWithCachingCSPSafe = null;
      this.clearFormattedDataCache();
      if (this._grid) {
        this._grid.onSelectedRowsChanged?.unsubscribe();
        this._grid.onCellCssStylesChanged?.unsubscribe();
      }
      this.onRowsOrCountChanged?.unsubscribe();
    }
    /** provide some refresh hints as to what to rows needs refresh */
    setRefreshHints(hints) {
      this.refreshHints = hints;
    }
    /** get extra filter arguments of the filter method */
    getFilterArgs() {
      return this.filterArgs;
    }
    /** add extra filter arguments to the filter method */
    setFilterArgs(args) {
      this.filterArgs = args;
    }
    /**
     * Processes all delete requests placed during bulk update
     * by recomputing the items and idxById members.
     */
    processBulkDelete() {
      if (!this.idxById) {
        return;
      }
      let id;
      let item;
      let newIdx = 0;
      for (let i = 0, l = this.items.length; i < l; i++) {
        item = this.items[i];
        id = item[this.idProperty];
        if (id === void 0) {
          throw new Error(`[SlickGrid DataView] Each data element must implement a unique 'id' property`);
        }
        if (this.bulkDeleteIds.has(id)) {
          this.idxById.delete(id);
        } else {
          this.items[newIdx] = item;
          this.idxById.set(id, newIdx);
          ++newIdx;
        }
      }
      this.items.length = newIdx;
      this.bulkDeleteIds = /* @__PURE__ */ new Map();
    }
    updateIdxById(startingIndex) {
      if (this.isBulkSuspend || !this.idxById) {
        return;
      }
      startingIndex = startingIndex || 0;
      let id;
      for (let i = startingIndex, l = this.items.length; i < l; i++) {
        id = this.items[i][this.idProperty];
        if (id === void 0) {
          throw new Error(`[SlickGrid DataView] Each data element must implement a unique 'id' property`);
        }
        this.idxById.set(id, i);
      }
    }
    ensureIdUniqueness() {
      if (this.isBulkSuspend || !this.idxById) {
        return;
      }
      let id;
      for (let i = 0, l = this.items.length; i < l; i++) {
        id = this.items[i][this.idProperty];
        if (id === void 0 || this.idxById.get(id) !== i) {
          throw new Error(`[SlickGrid DataView] Each data element must implement a unique 'id' property`);
        }
      }
    }
    /** Get all DataView Items */
    getItems() {
      return this.items;
    }
    /** Get the DataView Id property name to use (defaults to "Id" but could be customized to something else when instantiating the DataView) */
    getIdPropertyName() {
      return this.idProperty;
    }
    /**
     * Set the Items with a new Dataset and optionally pass a different Id property name
     * @param {Array<*>} data - array of data
     * @param {String} [objectIdProperty] - optional id property to use as primary id
     */
    setItems(data, objectIdProperty) {
      if (objectIdProperty !== void 0) {
        this.idProperty = objectIdProperty;
      }
      this.items = this.filteredItems = data;
      this.onSetItemsCalled.notify({ idProperty: this.idProperty, itemCount: this.items.length }, null, this);
      this.idxById = /* @__PURE__ */ new Map();
      this.updateIdxById();
      this.ensureIdUniqueness();
      this.refresh();
      if (this._gridOptions?.enableFormattedDataCache) {
        this.clearFormattedDataCache();
        this.populateFormattedDataCacheAsync();
      }
    }
    /** Set Paging Options */
    setPagingOptions(args) {
      if (this.onBeforePagingInfoChanged.notify(this.getPagingInfo(), null, this).getReturnValue() !== false) {
        if (isDefined(args.pageSize)) {
          this.pagesize = args.pageSize;
          this.pagenum = this.pagesize ? Math.min(this.pagenum, Math.max(0, Math.ceil(this.totalRows / this.pagesize) - 1)) : 0;
        }
        if (isDefined(args.pageNum)) {
          this.pagenum = Math.min(args.pageNum, Math.max(0, Math.ceil(this.totalRows / this.pagesize) - 1));
        }
        this.onPagingInfoChanged.notify(this.getPagingInfo(), null, this);
        this.refresh();
      }
    }
    /** Get Paging Options */
    getPagingInfo() {
      const totalPages = this.pagesize ? Math.max(1, Math.ceil(this.totalRows / this.pagesize)) : 1;
      return {
        pageSize: this.pagesize,
        pageNum: this.pagenum,
        totalRows: this.totalRows,
        totalPages,
        dataView: this
      };
    }
    /** Sort Method to use by the DataView */
    sort(comparer, ascending) {
      this.sortAsc = ascending;
      this.sortComparer = comparer;
      if (ascending === false) {
        this.items.reverse();
      }
      this.items.sort(comparer);
      if (ascending === false) {
        this.items.reverse();
      }
      this.idxById = /* @__PURE__ */ new Map();
      this.updateIdxById();
      this.refresh();
    }
    /** Re-Sort the dataset */
    reSort() {
      if (this.sortComparer) {
        this.sort(this.sortComparer, this.sortAsc);
      }
    }
    /** Get only the DataView filtered items */
    getFilteredItems() {
      return this.filteredItems;
    }
    /** Get the array length (count) of only the DataView filtered items */
    getFilteredItemCount() {
      return this.filteredItems.length;
    }
    /** Get current Filter used by the DataView */
    getFilter() {
      return this._options.useCSPSafeFilter ? this.filterCSPSafe : this.filter;
    }
    /**
     * Set a Filter that will be used by the DataView
     * @param {Function} fn - filter callback function
     */
    setFilter(filterFn) {
      this.filterCSPSafe = filterFn;
      this.filter = filterFn;
      if (this._options.inlineFilters) {
        this.compiledFilterCSPSafe = this.compileFilterCSPSafe;
        this.compiledFilterWithCachingCSPSafe = this.compileFilterWithCachingCSPSafe;
        this.compiledFilter = this.compileFilter(this._options.useCSPSafeFilter);
        this.compiledFilterWithCaching = this.compileFilterWithCaching(this._options.useCSPSafeFilter);
      }
      this.refresh();
    }
    /** Get current Grouping info */
    getGrouping() {
      return this.groupingInfos;
    }
    /** Set some Grouping */
    setGrouping(groupingInfo) {
      if (!this._options.groupItemMetadataProvider) {
        this._options.groupItemMetadataProvider = new SlickGroupItemMetadataProvider(this._grid?.getOptions().groupItemMetadataOption);
      }
      this.groups = [];
      this.toggledGroupsByLevel = [];
      groupingInfo = groupingInfo || [];
      this.groupingInfos = groupingInfo instanceof Array ? groupingInfo : [groupingInfo];
      for (let i = 0; i < this.groupingInfos.length; i++) {
        const gi = this.groupingInfos[i] = extend(true, {}, this.groupingInfoDefaults, this.groupingInfos[i]);
        gi.getterIsAFn = typeof gi.getter === "function";
        gi.compiledAccumulators = [];
        let idx = gi.aggregators.length;
        while (idx--) {
          gi.compiledAccumulators[idx] = this.compileAccumulatorLoopCSPSafe(gi.aggregators[idx]);
        }
        this.toggledGroupsByLevel[i] = {};
      }
      this.refresh();
    }
    /** Get an item in the DataView by its row index */
    getItemByIdx(i) {
      return this.items[i];
    }
    /** Get row index in the DataView by its Id */
    getIdxById(id) {
      return this.idxById?.get(id);
    }
    ensureRowsByIdCache() {
      if (!this.rowsById) {
        this.rowsById = {};
        for (let i = 0, l = this.rows.length; i < l; i++) {
          this.rowsById[this.rows[i][this.idProperty]] = i;
        }
      }
    }
    /** Get row number in the grid by its item object */
    getRowByItem(item) {
      this.ensureRowsByIdCache();
      return this.rowsById?.[item[this.idProperty]];
    }
    /** Get row number in the grid by its Id */
    getRowById(id) {
      this.ensureRowsByIdCache();
      return this.rowsById?.[id];
    }
    /** Get an item in the DataView by its Id */
    getItemById(id) {
      return this.items[this.idxById.get(id)];
    }
    /** From the items array provided, return the mapped rows */
    mapItemsToRows(itemArray) {
      const rows = [];
      this.ensureRowsByIdCache();
      for (let i = 0, l = itemArray.length; i < l; i++) {
        const row = this.rowsById?.[itemArray[i][this.idProperty]];
        if (isDefined(row)) {
          rows[rows.length] = row;
        }
      }
      return rows;
    }
    /** From the Ids array provided, return the mapped rows */
    mapIdsToRows(idArray) {
      const rows = [];
      this.ensureRowsByIdCache();
      for (let i = 0, l = idArray.length; i < l; i++) {
        const row = this.rowsById?.[idArray[i]];
        if (isDefined(row)) {
          rows[rows.length] = row;
        }
      }
      return rows;
    }
    /** From the rows array provided, return the mapped Ids */
    mapRowsToIds(rowArray) {
      const ids = [];
      for (let i = 0, l = rowArray.length; i < l; i++) {
        if (rowArray[i] < this.rows.length) {
          const rowItem = this.rows[rowArray[i]];
          ids[ids.length] = rowItem[this.idProperty];
        }
      }
      return ids;
    }
    /**
     * Performs the update operations of a single item by id without
     * triggering any events or refresh operations.
     * @param id The new id of the item.
     * @param item The item which should be the new value for the given id.
     */
    updateSingleItem(id, item) {
      if (!this.idxById) {
        return;
      }
      if (!this.idxById.has(id)) {
        throw new Error("[SlickGrid DataView] Invalid id");
      }
      if (id !== item[this.idProperty]) {
        const newId = item[this.idProperty];
        if (!isDefined(newId)) {
          throw new Error("[SlickGrid DataView] Cannot update item to associate with a null id");
        }
        if (this.idxById.has(newId)) {
          throw new Error("[SlickGrid DataView] Cannot update item to associate with a non-unique id");
        }
        this.idxById.set(newId, this.idxById.get(id));
        this.idxById.delete(id);
        if (this.updated?.[id]) {
          delete this.updated[id];
        }
        id = newId;
      }
      this.items[this.idxById.get(id)] = item;
      if (!this.updated) {
        this.updated = {};
      }
      this.updated[id] = true;
    }
    /**
     * Updates a single item in the data view given the id and new value.
     * @param id The new id of the item.
     * @param item The item which should be the new value for the given id.
     */
    updateItem(id, item) {
      this.updateSingleItem(id, item);
      this.refresh();
      this.invalidateFormattedCacheForUpdatedItem(id, item);
    }
    /**
     * Updates multiple items in the data view given the new ids and new values.
     * @param id {Array} The array of new ids which is in the same order as the items.
     * @param newItems {Array} The new items that should be set in the data view for the given ids.
     */
    updateItems(ids, newItems) {
      if (ids.length !== newItems.length) {
        throw new Error("[SlickGrid DataView] Mismatch on the length of ids and items provided to update");
      }
      for (let i = 0, l = newItems.length; i < l; i++) {
        this.updateSingleItem(ids[i], newItems[i]);
      }
      this.refresh();
      for (let i = 0, l = newItems.length; i < l; i++) {
        this.invalidateFormattedCacheForUpdatedItem(ids[i], newItems[i]);
      }
    }
    /**
     * Inserts a single item into the data view at the given position.
     * @param insertBefore {Number} The 0-based index before which the item should be inserted.
     * @param item The item to insert.
     */
    insertItem(insertBefore, item) {
      this.items.splice(insertBefore, 0, item);
      this.updateIdxById(insertBefore);
      this.refresh();
    }
    /**
     * Inserts multiple items into the data view at the given position.
     * @param insertBefore {Number} The 0-based index before which the items should be inserted.
     * @param newItems {Array}  The items to insert.
     */
    insertItems(insertBefore, newItems) {
      Array.prototype.splice.apply(this.items, [insertBefore, 0].concat(newItems));
      this.updateIdxById(insertBefore);
      this.refresh();
    }
    /**
     * Adds a single item at the end of the data view.
     * @param item The item to add at the end.
     */
    addItem(item) {
      this.items.push(item);
      this.updateIdxById(this.items.length - 1);
      this.refresh();
    }
    /**
     * Adds multiple items at the end of the data view.
     * @param {Array} newItems The items to add at the end.
     */
    addItems(newItems) {
      this.items = this.items.concat(newItems);
      this.updateIdxById(this.items.length - newItems.length);
      this.refresh();
    }
    /**
     * Deletes a single item identified by the given id from the data view.
     * @param {String|Number} id The id identifying the object to delete.
     */
    deleteItem(id) {
      if (!this.idxById) {
        return;
      }
      if (this.isBulkSuspend) {
        this.bulkDeleteIds.set(id, true);
      } else {
        const idx = this.idxById.get(id);
        if (idx === void 0) {
          throw new Error("[SlickGrid DataView] Invalid id");
        }
        this.idxById.delete(id);
        this.items.splice(idx, 1);
        this.updateIdxById(idx);
        this.refresh();
        delete this.formattedDataCache[id];
        delete this.formattedCellCache[id];
      }
    }
    /**
     * Deletes multiple item identified by the given ids from the data view.
     * @param {Array} ids The ids of the items to delete.
     */
    deleteItems(ids) {
      if (ids.length === 0 || !this.idxById) {
        return;
      }
      if (this.isBulkSuspend) {
        for (let i = 0, l = ids.length; i < l; i++) {
          const id = ids[i];
          const idx = this.idxById.get(id);
          if (idx === void 0) {
            throw new Error("[SlickGrid DataView] Invalid id");
          }
          this.bulkDeleteIds.set(id, true);
        }
      } else {
        const indexesToDelete = [];
        for (let i = 0, l = ids.length; i < l; i++) {
          const id = ids[i];
          const idx = this.idxById.get(id);
          if (idx === void 0) {
            throw new Error("[SlickGrid DataView] Invalid id");
          }
          this.idxById.delete(id);
          indexesToDelete.push(idx);
        }
        indexesToDelete.sort((a, b) => a - b);
        for (let i = indexesToDelete.length - 1; i >= 0; --i) {
          this.items.splice(indexesToDelete[i], 1);
        }
        this.updateIdxById(indexesToDelete[0]);
        this.refresh();
        for (let i = 0, l = ids.length; i < l; i++) {
          delete this.formattedDataCache[ids[i]];
          delete this.formattedCellCache[ids[i]];
        }
      }
    }
    /** Add an item in a sorted dataset (a Sort function must be defined) */
    sortedAddItem(item) {
      if (!this.sortComparer) {
        throw new Error("[SlickGrid DataView] sortedAddItem() requires a sort comparer, use sort()");
      }
      this.insertItem(this.sortedIndex(item), item);
    }
    /** Update an item in a sorted dataset (a Sort function must be defined) */
    sortedUpdateItem(id, item) {
      if (!this.idxById) {
        return;
      }
      if (!this.idxById.has(id) || id !== item[this.idProperty]) {
        throw new Error(`[SlickGrid DataView] Invalid or non-matching id ${id}`);
      }
      if (!this.sortComparer) {
        throw new Error("[SlickGrid DataView] sortedUpdateItem() requires a sort comparer, use sort()");
      }
      const oldItem = this.getItemById(id);
      if (this.sortComparer(oldItem, item) !== 0) {
        this.deleteItem(id);
        this.sortedAddItem(item);
      } else {
        this.updateItem(id, item);
      }
    }
    sortedIndex(searchItem) {
      let low = 0;
      let high = this.items.length;
      while (low < high) {
        const mid = low + high >>> 1;
        if (this.sortComparer(this.items[mid], searchItem) === -1) {
          low = mid + 1;
        } else {
          high = mid;
        }
      }
      return low;
    }
    /** Get item count, that is the full dataset lenght of the DataView */
    getItemCount() {
      return this.items.length;
    }
    /** Get row count (rows displayed in current page which also exclude any filtered items) */
    getLength() {
      return this.rows.length;
    }
    /** Retrieve an item from the DataView at specific index */
    getItem(i) {
      const item = this.rows[i];
      if (item?.__group && item.totals && !item.totals?.initialized) {
        const gi = this.groupingInfos[item.level];
        if (!gi.displayTotalsRow) {
          this.calculateTotals(item.totals);
          item.title = gi.formatter ? gi.formatter(item) : item.value;
        }
      } else if (item?.__groupTotals && !item.initialized) {
        this.calculateTotals(item);
      }
      return item;
    }
    getItemMetadata(row) {
      const item = this.rows[row];
      if (item === void 0) {
        return null;
      }
      if (this._options.globalItemMetadataProvider?.getRowMetadata) {
        return this._options.globalItemMetadataProvider.getRowMetadata(item, row);
      }
      if (item.__group && this._options.groupItemMetadataProvider?.getGroupRowMetadata) {
        return this._options.groupItemMetadataProvider.getGroupRowMetadata(item, row);
      }
      if (item.__groupTotals && this._options.groupItemMetadataProvider?.getTotalsRowMetadata) {
        return this._options.groupItemMetadataProvider.getTotalsRowMetadata(item, row);
      }
      return null;
    }
    expandCollapseAllGroups(level, collapse) {
      if (!isDefined(level)) {
        for (let i = 0; i < this.groupingInfos.length; i++) {
          this.toggledGroupsByLevel[i] = {};
          this.groupingInfos[i].collapsed = collapse;
          if (collapse === true) {
            this.onGroupCollapsed.notify({ level: i, groupingKey: null });
          } else {
            this.onGroupExpanded.notify({ level: i, groupingKey: null });
          }
        }
      } else {
        this.toggledGroupsByLevel[level] = {};
        this.groupingInfos[level].collapsed = collapse;
        if (collapse === true) {
          this.onGroupCollapsed.notify({ level, groupingKey: null });
        } else {
          this.onGroupExpanded.notify({ level, groupingKey: null });
        }
      }
      this.refresh();
    }
    /**
     * @param {Number} [level] Optional level to collapse.  If not specified, applies to all levels.
     */
    collapseAllGroups(level) {
      this.expandCollapseAllGroups(level, true);
    }
    /**
     * @param {Number} [level] Optional level to expand.  If not specified, applies to all levels.
     */
    expandAllGroups(level) {
      this.expandCollapseAllGroups(level, false);
    }
    expandCollapseGroup(level, groupingKey, collapse) {
      if (this.toggledGroupsByLevel[level]) {
        this.toggledGroupsByLevel[level][groupingKey] = this.groupingInfos[level].collapsed ^ collapse;
      }
      this.refresh();
    }
    /**
     * @param varArgs Either a SlickGroup's "groupingKey" property, or a
     *     variable argument list of grouping values denoting a unique path to the row.  For
     *     example, calling collapseGroup('high', '10%') will collapse the '10%' subgroup of
     *     the 'high' group.
     */
    collapseGroup(...args) {
      const calledArgs = Array.prototype.slice.call(args);
      const arg0 = calledArgs[0];
      let groupingKey;
      let level;
      if (args.length === 1 && arg0.indexOf(this.groupingDelimiter) !== -1) {
        groupingKey = arg0;
        level = arg0.split(this.groupingDelimiter).length - 1;
      } else {
        groupingKey = args.join(this.groupingDelimiter);
        level = args.length - 1;
      }
      this.expandCollapseGroup(level, groupingKey, true);
      this.onGroupCollapsed.notify({ level, groupingKey });
    }
    /** Get all data items for a specific grouping key */
    getItemsByGroupingKey(groupingKey) {
      return this.groups.find((g2) => g2.groupingKey === groupingKey)?.rows || [];
    }
    /**
     * @param varArgs Either a SlickGroup's "groupingKey" property, or a
     *     variable argument list of grouping values denoting a unique path to the row.  For
     *     example, calling expandGroup('high', '10%') will expand the '10%' subgroup of
     *     the 'high' group.
     */
    expandGroup(...args) {
      const calledArgs = Array.prototype.slice.call(args);
      const arg0 = calledArgs[0];
      let groupingKey;
      let level;
      if (args.length === 1 && arg0.indexOf(this.groupingDelimiter) !== -1) {
        level = arg0.split(this.groupingDelimiter).length - 1;
        groupingKey = arg0;
      } else {
        level = args.length - 1;
        groupingKey = args.join(this.groupingDelimiter);
      }
      this.expandCollapseGroup(level, groupingKey, false);
      this.onGroupExpanded.notify({ level, groupingKey });
    }
    getGroups() {
      return this.groups;
    }
    /**
     * Get the grouping value for an item, respecting dataItemColumnValueExtractor if available.
     * This allows grouping to work correctly with custom extractors for nested object properties.
     */
    getGroupingValue(item, fieldName) {
      if (this._grid) {
        const extractor = this._grid.getOptions?.().dataItemColumnValueExtractor;
        if (extractor) {
          const columns = this._grid.getColumns?.();
          if (columns) {
            const column = columns.find((col) => col.field === fieldName || col.id === fieldName);
            if (column) {
              const extractedValue = extractor(item, column);
              if (extractedValue !== void 0 && extractedValue !== null) {
                return extractedValue;
              }
            }
          }
        }
      }
      return item[fieldName];
    }
    extractGroups(rows, parentGroup) {
      let group;
      let val;
      const groups = [];
      const groupsByVal = {};
      let r2;
      const level = parentGroup ? parentGroup.level + 1 : 0;
      const gi = this.groupingInfos[level];
      for (let i = 0, l = gi.predefinedValues?.length ?? 0; i < l; i++) {
        val = gi.predefinedValues?.[i];
        group = groupsByVal[val];
        if (!group) {
          group = new SlickGroup();
          group.value = val;
          group.level = level;
          group.groupingKey = (parentGroup ? parentGroup.groupingKey + this.groupingDelimiter : "") + val;
          groups[groups.length] = group;
          groupsByVal[val] = group;
        }
      }
      for (let i = 0, l = rows.length; i < l; i++) {
        r2 = rows[i];
        val = gi.getterIsAFn ? gi.getter(r2) : this.getGroupingValue(r2, gi.getter);
        group = groupsByVal[val];
        if (!group) {
          group = new SlickGroup();
          group.value = val;
          group.level = level;
          group.groupingKey = (parentGroup ? parentGroup.groupingKey + this.groupingDelimiter : "") + val;
          groups[groups.length] = group;
          groupsByVal[val] = group;
        }
        group.rows[group.count++] = r2;
      }
      if (level < this.groupingInfos.length - 1) {
        for (let i = 0; i < groups.length; i++) {
          group = groups[i];
          group.groups = this.extractGroups(group.rows, group);
        }
      }
      if (groups.length) {
        this.addTotals(groups, level);
      }
      groups.sort(this.groupingInfos[level].comparer);
      return groups;
    }
    /** claculate Group Totals */
    calculateTotals(totals) {
      const group = totals.group;
      const gi = this.groupingInfos[group?.level ?? 0];
      const isLeafLevel = group.level === this.groupingInfos.length;
      let agg;
      let idx = gi.aggregators.length;
      if (!isLeafLevel && gi.aggregateChildGroups) {
        let i = group.groups?.length ?? 0;
        while (i--) {
          if (!group.groups[i].totals.initialized) {
            this.calculateTotals(group.groups[i].totals);
          }
        }
      }
      while (idx--) {
        agg = gi.aggregators[idx];
        agg.init();
        if (!isLeafLevel && gi.aggregateChildGroups) {
          gi.compiledAccumulators[idx].call(agg, group.groups);
        } else {
          gi.compiledAccumulators[idx].call(agg, group.rows);
        }
        agg.storeResult(totals);
      }
      totals.initialized = true;
    }
    addGroupTotals(group) {
      const gi = this.groupingInfos[group.level];
      const totals = new SlickGroupTotals();
      totals.group = group;
      group.totals = totals;
      if (!gi.lazyTotalsCalculation) {
        this.calculateTotals(totals);
      }
    }
    addTotals(groups, level) {
      level = level || 0;
      const gi = this.groupingInfos[level];
      const groupCollapsed = gi.collapsed;
      const toggledGroups = this.toggledGroupsByLevel[level];
      let idx = groups.length;
      let g2;
      while (idx--) {
        g2 = groups[idx];
        if (g2.collapsed && !gi.aggregateCollapsed) {
          continue;
        }
        if (g2.groups) {
          this.addTotals(g2.groups, level + 1);
        }
        if (gi.aggregators?.length && (gi.aggregateEmpty || g2.rows.length || g2.groups?.length)) {
          this.addGroupTotals(g2);
        }
        g2.collapsed = groupCollapsed ^ toggledGroups[g2.groupingKey];
        g2.title = gi.formatter ? gi.formatter(g2) : g2.value;
      }
    }
    flattenGroupedRows(groups, level) {
      level = level || 0;
      const gi = this.groupingInfos[level];
      const groupedRows = [];
      let rows;
      let gl = 0;
      let g2;
      for (let i = 0, l = groups.length; i < l; i++) {
        g2 = groups[i];
        groupedRows[gl++] = g2;
        if (!g2.collapsed) {
          rows = g2.groups ? this.flattenGroupedRows(g2.groups, level + 1) : g2.rows;
          for (let j2 = 0, jj = rows.length; j2 < jj; j2++) {
            groupedRows[gl++] = rows[j2];
          }
        }
        if (g2.totals && gi.displayTotalsRow && (!g2.collapsed || gi.aggregateCollapsed)) {
          groupedRows[gl++] = g2.totals;
        }
      }
      return groupedRows;
    }
    compileAccumulatorLoopCSPSafe(aggregator) {
      if (aggregator.accumulate) {
        return function(items) {
          let result;
          if (Array.isArray(items)) {
            for (let i = 0; i < items.length; i++) {
              const item = items[i];
              result = aggregator.accumulate.call(aggregator, item);
            }
          }
          return result;
        };
      } else {
        return function noAccumulator() {
        };
      }
    }
    compileFilterCSPSafe(items, args) {
      if (typeof this.filterCSPSafe !== "function") {
        return [];
      }
      const retval = [];
      const il = items.length;
      for (let _i = 0; _i < il; _i++) {
        if (this.filterCSPSafe(items[_i], args)) {
          retval.push(items[_i]);
        }
      }
      return retval;
    }
    compileFilter(stopRunningIfCSPSafeIsActive = false) {
      if (stopRunningIfCSPSafeIsActive) {
        return null;
      }
      const filterInfo = getFunctionDetails(this.filter);
      const filterPath1 = "{ continue _coreloop; }$1";
      const filterPath2 = "{ _retval[_idx++] = $item$; continue _coreloop; }$1";
      const filterBody = filterInfo.body.replace(/return false\s*([;}]|\}|$)/gi, filterPath1).replace(/return!1([;}]|\}|$)/gi, filterPath1).replace(/return true\s*([;}]|\}|$)/gi, filterPath2).replace(/return!0([;}]|\}|$)/gi, filterPath2).replace(/return ([^;}]+?)\s*([;}]|$)/gi, "{ if ($1) { _retval[_idx++] = $item$; }; continue _coreloop; }$2");
      let tpl = [
        // 'function(_items, _args) { ',
        "var _retval = [], _idx = 0; ",
        "var $item$, $args$ = _args; ",
        "_coreloop: ",
        "for (var _i = 0, _il = _items.length; _i < _il; _i++) { ",
        "$item$ = _items[_i]; ",
        "$filter$; ",
        "} ",
        "return _retval; "
        // '}'
      ].join("");
      tpl = tpl.replace(/\$filter\$/gi, filterBody);
      tpl = tpl.replace(/\$item\$/gi, filterInfo.params[0]);
      tpl = tpl.replace(/\$args\$/gi, filterInfo.params[1]);
      const fn = new Function("_items,_args", tpl);
      const fnName = "compiledFilter";
      fn.displayName = fnName;
      fn.name = this.setFunctionName(fn, fnName);
      return fn;
    }
    compileFilterWithCaching(stopRunningIfCSPSafeIsActive = false) {
      if (stopRunningIfCSPSafeIsActive) {
        return null;
      }
      const filterInfo = getFunctionDetails(this.filter);
      const filterPath1 = "{ continue _coreloop; }$1";
      const filterPath2 = "{ _cache[_i] = true;_retval[_idx++] = $item$; continue _coreloop; }$1";
      const filterBody = filterInfo.body.replace(/return false\s*([;}]|\}|$)/gi, filterPath1).replace(/return!1([;}]|\}|$)/gi, filterPath1).replace(/return true\s*([;}]|\}|$)/gi, filterPath2).replace(/return!0([;}]|\}|$)/gi, filterPath2).replace(/return ([^;}]+?)\s*([;}]|$)/gi, "{ if ((_cache[_i] = $1)) { _retval[_idx++] = $item$; }; continue _coreloop; }$2");
      let tpl = [
        // 'function(_items, _args, _cache) { ',
        "var _retval = [], _idx = 0; ",
        "var $item$, $args$ = _args; ",
        "_coreloop: ",
        "for (var _i = 0, _il = _items.length; _i < _il; _i++) { ",
        "$item$ = _items[_i]; ",
        "if (_cache[_i]) { ",
        "_retval[_idx++] = $item$; ",
        "continue _coreloop; ",
        "} ",
        "$filter$; ",
        "} ",
        "return _retval; "
        // '}'
      ].join("");
      tpl = tpl.replace(/\$filter\$/gi, filterBody);
      tpl = tpl.replace(/\$item\$/gi, filterInfo.params[0]);
      tpl = tpl.replace(/\$args\$/gi, filterInfo.params[1]);
      const fn = new Function("_items,_args,_cache", tpl);
      const fnName = "compiledFilterWithCaching";
      fn.displayName = fnName;
      fn.name = this.setFunctionName(fn, fnName);
      return fn;
    }
    compileFilterWithCachingCSPSafe(items, args, filterCache) {
      if (typeof this.filterCSPSafe !== "function") {
        return [];
      }
      const retval = [];
      const il = items.length;
      for (let _i = 0; _i < il; _i++) {
        if (filterCache[_i] || this.filterCSPSafe(items[_i], args)) {
          retval.push(items[_i]);
        }
      }
      return retval;
    }
    /**
     * In ES5 we could set the function name on the fly but in ES6 this is forbidden and we need to set it through differently
     * We can use Object.defineProperty and set it the property to writable, see MDN for reference
     * https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/defineProperty
     * @param {*} fn
     * @param {string} fnName
     */
    setFunctionName(fn, fnName) {
      try {
        Object.defineProperty(fn, "name", { writable: true, value: fnName });
      } catch (err) {
        fn.name = fnName;
      }
    }
    uncompiledFilter(items, args) {
      const retval = [];
      let idx = 0;
      for (let i = 0, ii = items.length; i < ii; i++) {
        if (this.filter?.(items[i], args)) {
          retval[idx++] = items[i];
        }
      }
      return retval;
    }
    uncompiledFilterWithCaching(items, args, cache) {
      const retval = [];
      let idx = 0;
      let item;
      for (let i = 0, ii = items.length; i < ii; i++) {
        item = items[i];
        if (cache[i]) {
          retval[idx++] = item;
        } else if (this.filter?.(item, args)) {
          retval[idx++] = item;
          cache[i] = true;
        }
      }
      return retval;
    }
    getFilteredAndPagedItems(items) {
      if (this._options.useCSPSafeFilter ? this.filterCSPSafe : this.filter) {
        let batchFilter;
        let batchFilterWithCaching;
        if (this._options.useCSPSafeFilter) {
          batchFilter = this._options.inlineFilters ? this.compiledFilterCSPSafe : this.uncompiledFilter;
          batchFilterWithCaching = this._options.inlineFilters ? this.compiledFilterWithCachingCSPSafe : this.uncompiledFilterWithCaching;
        } else {
          batchFilter = this._options.inlineFilters ? this.compiledFilter : this.uncompiledFilter;
          batchFilterWithCaching = this._options.inlineFilters ? this.compiledFilterWithCaching : this.uncompiledFilterWithCaching;
        }
        if (this.refreshHints.isFilterNarrowing) {
          this.filteredItems = batchFilter.call(this, this.filteredItems, this.filterArgs);
        } else if (this.refreshHints.isFilterExpanding) {
          this.filteredItems = batchFilterWithCaching.call(this, items, this.filterArgs, this.filterCache);
        } else if (!this.refreshHints.isFilterUnchanged) {
          this.filteredItems = batchFilter.call(this, items, this.filterArgs);
        }
      } else {
        this.filteredItems = this.pagesize ? items : items.concat();
      }
      let paged;
      if (this.pagesize) {
        if (this.filteredItems.length <= this.pagenum * this.pagesize) {
          if (this.filteredItems.length === 0) {
            this.pagenum = 0;
          } else {
            this.pagenum = Math.floor((this.filteredItems.length - 1) / this.pagesize);
          }
        }
        paged = this.filteredItems.slice(this.pagesize * this.pagenum, this.pagesize * this.pagenum + this.pagesize);
      } else {
        paged = this.filteredItems;
      }
      return { totalRows: this.filteredItems.length, rows: paged };
    }
    getRowDiffs(rows, newRows) {
      let item;
      let r2;
      let eitherIsNonData;
      const diff2 = [];
      let from = 0;
      let to = Math.max(newRows.length, rows.length);
      if (this.refreshHints?.ignoreDiffsBefore) {
        from = Math.max(0, Math.min(newRows.length, this.refreshHints.ignoreDiffsBefore));
      }
      if (this.refreshHints?.ignoreDiffsAfter) {
        to = Math.min(newRows.length, Math.max(0, this.refreshHints.ignoreDiffsAfter));
      }
      for (let i = from, rl = rows.length; i < to; i++) {
        if (i >= rl) {
          diff2[diff2.length] = i;
        } else {
          item = newRows[i];
          r2 = rows[i];
          if (!item || this.groupingInfos.length && (eitherIsNonData = item.__nonDataRow || r2.__nonDataRow) && item.__group !== r2.__group || item.__group && !item.equals(r2) || eitherIsNonData && // no good way to compare totals since they are arbitrary DTOs
          // deep object comparison is pretty expensive
          // always considering them 'dirty' seems easier for the time being
          (item.__groupTotals || r2.__groupTotals) || item[this.idProperty] !== r2[this.idProperty] || this.updated?.[item[this.idProperty]]) {
            diff2[diff2.length] = i;
          }
        }
      }
      return diff2;
    }
    recalc(_items) {
      this.rowsById = void 0;
      if (this.refreshHints.isFilterNarrowing !== this.prevRefreshHints.isFilterNarrowing || this.refreshHints.isFilterExpanding !== this.prevRefreshHints.isFilterExpanding) {
        this.filterCache = [];
      }
      const filteredItems = this.getFilteredAndPagedItems(_items);
      this.totalRows = filteredItems.totalRows;
      let newRows = filteredItems.rows;
      this.groups = [];
      if (this.groupingInfos.length) {
        this.groups = this.extractGroups(newRows);
        if (this.groups.length) {
          newRows = this.flattenGroupedRows(this.groups);
        }
      }
      const diff2 = this.getRowDiffs(this.rows, newRows);
      this.rows = newRows;
      return diff2;
    }
    refresh() {
      if (this.suspend) {
        return;
      }
      const previousPagingInfo = extend(true, {}, this.getPagingInfo());
      const countBefore = this.rows.length;
      const totalRowsBefore = this.totalRows;
      let diff2 = this.recalc(this.items);
      if (this.pagesize && this.totalRows < this.pagenum * this.pagesize) {
        this.pagenum = Math.max(0, Math.ceil(this.totalRows / this.pagesize) - 1);
        diff2 = this.recalc(this.items);
      }
      this.updated = null;
      this.prevRefreshHints = this.refreshHints;
      this.refreshHints = {};
      if (totalRowsBefore !== this.totalRows) {
        if (this.onBeforePagingInfoChanged.notify(previousPagingInfo, null, this).getReturnValue() !== false) {
          this.onPagingInfoChanged.notify(this.getPagingInfo(), null, this);
        }
      }
      if (countBefore !== this.rows.length) {
        this.onRowCountChanged.notify({
          previous: countBefore,
          current: this.rows.length,
          itemCount: this.items.length,
          dataView: this,
          changedRows: diff2,
          callingOnRowsChanged: diff2.length > 0
        }, null, this);
      }
      if (diff2.length > 0) {
        this.onRowsChanged.notify({ rows: diff2, itemCount: this.items.length, dataView: this, calledOnRowCountChanged: countBefore !== this.rows.length }, null, this);
      }
      if (countBefore !== this.rows.length || diff2.length > 0) {
        this.onRowsOrCountChanged.notify({
          rowsDiff: diff2,
          previousRowCount: countBefore,
          currentRowCount: this.rows.length,
          itemCount: this.items.length,
          rowCountChanged: countBefore !== this.rows.length,
          rowsChanged: diff2.length > 0,
          dataView: this
        }, null, this);
      }
    }
    /**
     * Wires the grid and the DataView together to keep row selection tied to item ids.
     * This is useful since, without it, the grid only knows about rows, so if the items
     * move around, the same rows stay selected instead of the selection moving along
     * with the items.
     *
     * NOTE:  This doesn't work with cell selection model.
     *
     * @param {SlickGrid} grid - The grid to sync selection with.
     * @param {Boolean} preserveHidden - Whether to keep selected items that go out of the
     *     view due to them getting filtered out. When used with pagination,
     *     changing selection on different page will cancel previous page selection
     * @param {Boolean} [preserveHiddenOnSelectionChange] - Whether to keep selected items
     *     that are currently out of the view (see preserveHidden) as selected when selection
     *     changes. When used with pagination, changing selection on different page
     *     will keep previous page selection (this is different compared to `preserveHidden`)
     * @return {Event} An event that notifies when an internal list of selected row ids
     *     changes.  This is useful since, in combination with the above two options, it allows
     *     access to the full list selected row ids, and not just the ones visible to the grid.
     * @method syncGridSelection
     */
    syncGridSelection(grid, preserveHidden, preserveHiddenOnSelectionChange) {
      this._grid = grid;
      let inHandler;
      this.selectedRowIds = this.mapRowsToIds(grid.getSelectedRows());
      const gridOptions = grid.getOptions();
      const setSelectedRowIds = (rowIds) => {
        if (rowIds === false) {
          this.selectedRowIds = [];
        } else {
          if (this.selectedRowIds.sort().join(",") !== rowIds.sort().join(",")) {
            this.selectedRowIds = rowIds;
          }
        }
      };
      const update2 = () => {
        if ((this.selectedRowIds || []).length > 0 && !inHandler) {
          inHandler = true;
          const selectedRows = this.mapIdsToRows(this.selectedRowIds || []);
          if (!preserveHidden) {
            const selectedRowsChangedArgs = {
              grid: this._grid,
              ids: this.mapRowsToIds(selectedRows),
              rows: selectedRows,
              dataView: this
            };
            this.preSelectedRowIdsChangeFn(selectedRowsChangedArgs);
            this.onSelectedRowIdsChanged.notify(Object.assign(selectedRowsChangedArgs, {
              selectedRowIds: this.selectedRowIds,
              filteredIds: this.getAllSelectedFilteredIds()
            }), new SlickEventData(), this);
          }
          grid.setSelectedRows(selectedRows);
          inHandler = false;
        }
      };
      grid.onSelectedRowsChanged.subscribe((_e, args) => {
        if (!inHandler) {
          const newSelectedRowIds = this.mapRowsToIds(args.rows);
          const selectedRowsChangedArgs = {
            grid: this._grid,
            ids: newSelectedRowIds,
            rows: args.rows,
            added: true,
            dataView: this
          };
          this.preSelectedRowIdsChangeFn(selectedRowsChangedArgs);
          this.onSelectedRowIdsChanged.notify(Object.assign(selectedRowsChangedArgs, {
            selectedRowIds: this.selectedRowIds,
            filteredIds: this.getAllSelectedFilteredIds()
          }), new SlickEventData(), this);
        }
      });
      this.preSelectedRowIdsChangeFn = (args) => {
        if (!inHandler) {
          inHandler = true;
          const overwrite = typeof args.added === "undefined";
          if (overwrite) {
            setSelectedRowIds(args.ids);
          } else {
            let rowIds;
            if (args.added) {
              if (preserveHiddenOnSelectionChange && gridOptions.multiSelect) {
                const hiddenSelectedRowIds = this.selectedRowIds?.filter((id) => this.getRowById(id) === void 0);
                rowIds = hiddenSelectedRowIds.concat(args.ids);
              } else {
                rowIds = args.ids;
              }
            } else {
              if (preserveHiddenOnSelectionChange && gridOptions.multiSelect) {
                const argsIdsSet = new Set(args.ids);
                rowIds = this.selectedRowIds?.filter((id) => !argsIdsSet.has(id));
              } else {
                rowIds = [];
              }
            }
            setSelectedRowIds(rowIds);
          }
          inHandler = false;
        }
      };
      this.onRowsOrCountChanged.subscribe(update2.bind(this));
      return this.onSelectedRowIdsChanged;
    }
    /**
     * Get all selected IDs
     * Note: when using Pagination it will also include hidden selections assuming `preserveHiddenOnSelectionChange` is set to true.
     */
    getAllSelectedIds() {
      return this.selectedRowIds;
    }
    /**
     * Get all selected filtered IDs (similar to "getAllSelectedIds" but only return filtered data)
     * Note: when using Pagination it will also include hidden selections assuming `preserveHiddenOnSelectionChange` is set to true.
     */
    getAllSelectedFilteredIds() {
      return this.getAllSelectedFilteredItems().map((item) => item[this.idProperty]);
    }
    /**
     * Set current row selected IDs array (regardless of Pagination)
     * NOTE: This will NOT change the selection in the grid, if you need to do that then you still need to call
     * "grid.setSelectedRows(rows)"
     * @param {Array} selectedIds - list of IDs which have been selected for this action
     * @param {Object} options
     *  - `isRowBeingAdded`: defaults to true, are the new selected IDs being added (or removed) as new row selections
     *  - `shouldTriggerEvent`: defaults to true, should we trigger `onSelectedRowIdsChanged` event
     *  - `applyRowSelectionToGrid`: defaults to true, should we apply the row selections to the grid in the UI
     */
    setSelectedIds(selectedIds, options) {
      let isRowBeingAdded = options?.isRowBeingAdded;
      const shouldTriggerEvent = options?.shouldTriggerEvent;
      const applyRowSelectionToGrid = options?.applyRowSelectionToGrid;
      if (isRowBeingAdded !== false) {
        isRowBeingAdded = true;
      }
      const selectedRows = this.mapIdsToRows(selectedIds);
      const selectedRowsChangedArgs = {
        grid: this._grid,
        ids: selectedIds,
        rows: selectedRows,
        added: isRowBeingAdded,
        dataView: this
      };
      this.preSelectedRowIdsChangeFn?.(selectedRowsChangedArgs);
      if (shouldTriggerEvent !== false) {
        this.onSelectedRowIdsChanged.notify(Object.assign(selectedRowsChangedArgs, {
          selectedRowIds: this.selectedRowIds,
          filteredIds: this.getAllSelectedFilteredIds()
        }), new SlickEventData(), this);
      }
      if (applyRowSelectionToGrid !== false && this._grid) {
        this._grid.setSelectedRows(selectedRows);
      }
    }
    /**
     * Get all selected dataContext items
     * Note: when using Pagination it will also include hidden selections assuming `preserveHiddenOnSelectionChange` is set to true.
     */
    getAllSelectedItems() {
      const selectedData = [];
      const selectedIds = this.getAllSelectedIds();
      selectedIds.forEach((id) => {
        selectedData.push(this.getItemById(id));
      });
      return selectedData;
    }
    /**
     * Get all selected filtered dataContext items (similar to "getAllSelectedItems" but only return filtered data)
     * Note: when using Pagination it will also include hidden selections assuming `preserveHiddenOnSelectionChange` is set to true.
     */
    getAllSelectedFilteredItems() {
      if (!Array.isArray(this.selectedRowIds)) {
        return [];
      }
      const selectedRowIdSet = new Set(this.selectedRowIds);
      const intersection = this.filteredItems.filter((a) => selectedRowIdSet.has(a[this.idProperty]));
      return intersection || [];
    }
    syncGridCellCssStyles(grid, key) {
      let hashById;
      let inHandler;
      const storeCellCssStyles = (hash) => {
        hashById = {};
        if (typeof hash === "object") {
          Object.keys(hash).forEach((row) => {
            if (hash && this.rows[row]) {
              const id = this.rows[row][this.idProperty];
              hashById[id] = hash[row];
            }
          });
        }
      };
      storeCellCssStyles(grid.getCellCssStyles(key));
      const update2 = () => {
        if (typeof hashById === "object") {
          inHandler = true;
          this.ensureRowsByIdCache();
          const newHash = {};
          Object.keys(hashById).forEach((id) => {
            const row = this.rowsById?.[id];
            if (isDefined(row)) {
              newHash[row] = hashById[id];
            }
          });
          grid.setCellCssStyles(key, newHash);
          inHandler = false;
        }
      };
      grid.onCellCssStylesChanged.subscribe((_e, args) => {
        if (inHandler || key !== args.key) {
          return;
        }
        if (args.hash) {
          storeCellCssStyles(args.hash);
        } else {
          grid.onCellCssStylesChanged.unsubscribe();
          this.onRowsOrCountChanged.unsubscribe(update2);
        }
      });
      this.onRowsOrCountChanged.subscribe(update2.bind(this));
    }
    // --------------------------
    // Formatted Data Cache
    // --------------------------
    /**
     * Sets the grid reference so the DataView can access columns and options for the formatted data cache.
     * This is called by SlickGrid when it binds a DataView as its data source.
     * @param {SlickGrid} grid - The SlickGrid instance
     */
    setGrid(grid) {
      this._grid = grid;
      this._gridOptions = grid.getOptions();
    }
    /** Set the planner callback used to decide formatted-cache behavior for each column. */
    setFormattedDataCachePlanner(planner, forceRefresh = false) {
      if (!forceRefresh && this.formattedDataCachePlanner === planner) {
        return;
      }
      this.formattedDataCachePlanner = planner;
      if (this._gridOptions?.enableFormattedDataCache) {
        this.clearFormattedDataCache();
        this.populateFormattedDataCacheAsync();
      }
    }
    /**
     * Returns the cached formatted cell value if available, otherwise returns the fallback value.
     * Used by export services (e.g. ExcelExportService) to avoid re-executing expensive formatters.
     * @param {number} rowIdx - The row index in the current view
     * @param {string} columnId - The column ID
     * @param {any} fallbackValue - Value to return when the cache has no entry for this cell
     * @returns {any} The cached formatted value or the fallback value
     */
    getFormattedCellValue(rowIdx, columnId, fallbackValue) {
      if (!this._gridOptions?.enableFormattedDataCache) {
        return fallbackValue;
      }
      const item = this.getItem(rowIdx);
      const itemId = item?.[this.idProperty];
      if (itemId !== void 0) {
        const rowCache = this.formattedDataCache[itemId];
        if (rowCache?.[columnId] !== void 0) {
          return rowCache[columnId];
        }
      }
      return fallbackValue;
    }
    /**
     * Returns the cached UI display formatter result for a cell, or `undefined` when not cached.
     * Used by SlickGrid's `getFormatter` to skip re-executing the formatter during rendering.
     * @param {number} rowIdx - The row index in the current view
     * @param {string} columnId - The column ID
     * @returns {FormatterResult | undefined} The raw cached formatter result, or `undefined` on a cache miss
     */
    getCellDisplayValue(rowIdx, columnId, item) {
      const resolvedItem = item ?? this.getItem(rowIdx);
      const itemId = resolvedItem?.[this.idProperty];
      if (itemId === void 0) {
        return void 0;
      }
      return this.formattedCellCache[itemId]?.[columnId];
    }
    /**
     * Returns a snapshot of the current formatted data cache metadata.
     * @returns {FormattedDataCacheMetadata} The cache metadata
     */
    getCacheStatus() {
      return { ...this.formattedCacheMetadata };
    }
    /**
     * Clears the entire formatted data cache and resets the metadata.
     * Called when columns change or when the dataset is completely replaced.
     */
    clearFormattedDataCache() {
      if (this._populateCacheRafId !== void 0) {
        cancelAnimationFrame(this._populateCacheRafId);
        this._populateCacheRafId = void 0;
      }
      this.formattedDataCache = {};
      this.formattedCellCache = {};
      this.formattedCacheMetadata = {
        isPopulating: false,
        lastProcessedRow: -1,
        totalFormattedCells: 0
      };
    }
    /**
     * Invalidates the formatted data cache for a specific row and immediately re-caches it.
     * Called when a cell value changes so that the cached formatter output stays up to date.
     * @param {number} rowIdx - The row index (in the current view) to invalidate
     */
    invalidateFormattedDataCacheForRow(rowIdx) {
      if (!this._gridOptions?.enableFormattedDataCache) {
        return;
      }
      const item = this.getItem(rowIdx);
      const itemId = item?.[this.idProperty];
      if (itemId !== void 0) {
        this.populateSingleRowCache(rowIdx, this.buildCacheContext());
      }
    }
    /**
     * Handles formatted cache cleanup and invalidation for an updated item.
     * Removes stale cache entries if the item's id changed, and invalidates the row cache.
     * @param oldId - The previous id of the item
     * @param item - The updated item
     */
    invalidateFormattedCacheForUpdatedItem(oldId, item) {
      const gridOptions = this._gridOptions;
      if (!gridOptions?.enableFormattedDataCache) {
        return;
      }
      const newId = item[this.idProperty];
      if (oldId !== newId) {
        delete this.formattedDataCache[oldId];
        delete this.formattedCellCache[oldId];
      }
      const rowIdx = this.getRowById(newId ?? oldId);
      if (rowIdx !== void 0) {
        this.invalidateFormattedDataCacheForRow(rowIdx);
      }
    }
    /**
     * Starts populating the formatted data cache asynchronously in background batches using
     * `requestAnimationFrame` so that the UI remains responsive during population.
     * Does nothing when `enableFormattedDataCache` is not set in the grid options or when
     * a population pass is already in progress.
     * @param {number} [startRow=0] - Row index to start from (defaults to 0)
     */
    populateFormattedDataCacheAsync(startRow = 0) {
      const gridOptions = this._gridOptions;
      if (!gridOptions?.enableFormattedDataCache) {
        return;
      }
      if (this._populateCacheRafId !== void 0) {
        cancelAnimationFrame(this._populateCacheRafId);
        this._populateCacheRafId = void 0;
      }
      const generation = ++this._cachePopulationGeneration;
      this.formattedCacheMetadata.isPopulating = true;
      this.formattedCacheMetadata.lastProcessedRow = startRow - 1;
      this.formattedCacheMetadata.totalFormattedCells = 0;
      this.formattedCacheMetadata.cacheStartTime = Date.now();
      const batchCtx = this.buildCacheContext();
      let scheduleNextBatch;
      let lastProgressFireMs = 0;
      const processBatch = () => {
        if (generation !== this._cachePopulationGeneration) {
          return;
        }
        const frameBudgetMs = batchCtx.gridOptions.formattedDataCacheFrameBudgetMs ?? 8;
        const maxRowsPerFrame = batchCtx.gridOptions.formattedDataCacheBatchSize ?? 300;
        const frameDeadline = performance.now() + frameBudgetMs;
        const totalRows = this.getLength();
        const lastRowIndex = totalRows - 1;
        let processedInBatch = 0;
        let rowsUntilDeadlineCheck = 16;
        while (processedInBatch < maxRowsPerFrame && this.formattedCacheMetadata.lastProcessedRow < lastRowIndex) {
          if (--rowsUntilDeadlineCheck === 0) {
            if (performance.now() >= frameDeadline) {
              break;
            }
            rowsUntilDeadlineCheck = 16;
          }
          this.formattedCacheMetadata.lastProcessedRow++;
          if (this.populateSingleRowCache(this.formattedCacheMetadata.lastProcessedRow, batchCtx)) {
            processedInBatch++;
          }
        }
        const isDone = this.formattedCacheMetadata.lastProcessedRow >= lastRowIndex;
        const nowMs = Date.now();
        if (isDone || nowMs - lastProgressFireMs >= 250) {
          lastProgressFireMs = nowMs;
          const elapsedMs = nowMs - (this.formattedCacheMetadata.cacheStartTime || 0);
          const percentComplete = Math.round((this.formattedCacheMetadata.lastProcessedRow + 1) / totalRows * 100);
          this.onFormattedDataCacheProgress.notify({
            rowsProcessed: this.formattedCacheMetadata.lastProcessedRow + 1,
            totalRows,
            percentComplete,
            elapsedMs
          });
        }
        if (!isDone) {
          scheduleNextBatch();
        } else {
          this._populateCacheRafId = void 0;
          this.formattedCacheMetadata.isPopulating = false;
          const duration = Date.now() - (this.formattedCacheMetadata.cacheStartTime || 0);
          this.onFormattedDataCacheCompleted.notify({
            totalRows,
            totalFormattedCells: this.formattedCacheMetadata.totalFormattedCells,
            durationMs: duration
          });
        }
      };
      if (typeof MessageChannel !== "undefined") {
        const { port1, port2 } = new MessageChannel();
        port1.onmessage = processBatch;
        scheduleNextBatch = () => port2.postMessage(null);
      } else {
        scheduleNextBatch = () => {
          this._populateCacheRafId = requestAnimationFrame(processBatch);
        };
      }
      scheduleNextBatch();
    }
    /** Builds a RowCacheContext from the current grid state. Called once per population run or per single-row invalidation. */
    buildCacheContext() {
      const grid = this._grid;
      const gridOptions = this._gridOptions ?? grid.getOptions();
      const columns = grid.getColumns() ?? [];
      const exportOnlyCacheColumns = [];
      const dualCacheColumns = [];
      const cellOnlyColumns = [];
      for (let ci = 0; ci < columns.length; ci++) {
        const col = columns[ci];
        const plannerConfig = this.formattedDataCachePlanner?.(col, gridOptions);
        const needsExportCache = !!plannerConfig?.shouldCacheExport;
        const canReuseCellFormatterForExport = !!plannerConfig?.useCellFormatterForExport;
        const sanitizeDataExport = !!plannerConfig?.sanitizeDataExport;
        const exportOptions = plannerConfig?.exportOptions;
        const needsCellCache = !!col.formatter;
        if (needsExportCache && needsCellCache && !col.exportCustomFormatter && canReuseCellFormatterForExport) {
          dualCacheColumns.push({
            column: col,
            colIdx: ci,
            columnId: String(col.id),
            field: col.field,
            formatter: col.formatter,
            exportOptions,
            sanitizeDataExport
          });
        } else {
          if (needsExportCache) {
            exportOnlyCacheColumns.push({ column: col, colIdx: ci, columnId: String(col.id), exportOptions, sanitizeDataExport: false });
          }
          if (needsCellCache) {
            cellOnlyColumns.push({
              column: col,
              colIdx: ci,
              columnId: String(col.id),
              field: col.field,
              formatter: col.formatter,
              sanitizeDataExport: false
            });
          }
        }
      }
      const hasMetadataProviders = !!(this._options.globalItemMetadataProvider || this._options.groupItemMetadataProvider);
      return {
        grid,
        gridOptions,
        exportOnlyCacheColumns,
        dualCacheColumns,
        cellOnlyColumns,
        rows: this.rows,
        idProperty: this.idProperty,
        hasMetadataProviders
      };
    }
    populateSingleRowCache(rowIdx, ctx) {
      const item = ctx.rows[rowIdx];
      if (!item || item.__group || item.__groupTotals) {
        return false;
      }
      const itemId = item[ctx.idProperty];
      if (!this.formattedDataCache[itemId]) {
        this.formattedDataCache[itemId] = {};
      }
      if (!this.formattedCellCache[itemId]) {
        this.formattedCellCache[itemId] = {};
      }
      const formattedDataRowCache = this.formattedDataCache[itemId];
      const formattedCellRowCache = this.formattedCellCache[itemId];
      const { grid, exportOnlyCacheColumns, dualCacheColumns, cellOnlyColumns, hasMetadataProviders } = ctx;
      let totalFormattedCells = this.formattedCacheMetadata.totalFormattedCells;
      const rowHasMetadataFormatter = hasMetadataProviders && !!this.getItemMetadata(rowIdx)?.formatter;
      for (let ci = 0; ci < exportOnlyCacheColumns.length; ci++) {
        const entry = exportOnlyCacheColumns[ci];
        try {
          formattedDataRowCache[entry.columnId] = exportWithFormatterWhenDefined(
            rowIdx,
            entry.colIdx,
            entry.column,
            item,
            grid,
            entry.exportOptions,
            true
            // skipSanitization=true: export service handles sanitization uniformly at the end
          );
          totalFormattedCells++;
        } catch {
          formattedDataRowCache[entry.columnId] = void 0;
        }
      }
      for (let ci = 0; ci < dualCacheColumns.length; ci++) {
        const entry = dualCacheColumns[ci];
        try {
          const cellValue = item[entry.field] ?? null;
          const rawResult = entry.formatter(rowIdx, entry.colIdx, cellValue, entry.column, item, grid);
          const cellResult = isPrimitiveOrHTML(rawResult) ? rawResult : rawResult.html || rawResult.text;
          let exportStr = getHtmlStringOutput(cellResult) ?? "";
          if (entry.sanitizeDataExport && exportStr) {
            exportStr = stripTags(exportStr);
          }
          formattedDataRowCache[entry.columnId] = exportStr;
          totalFormattedCells++;
          if (!rowHasMetadataFormatter && !isLiveDomFormatterResult(rawResult)) {
            formattedCellRowCache[entry.columnId] = rawResult;
          }
        } catch {
          formattedDataRowCache[entry.columnId] = void 0;
        }
      }
      if (!rowHasMetadataFormatter) {
        for (let ci = 0; ci < cellOnlyColumns.length; ci++) {
          const entry = cellOnlyColumns[ci];
          try {
            const cellValue = item[entry.field] ?? null;
            const rawResult = entry.formatter(rowIdx, entry.colIdx, cellValue, entry.column, item, grid);
            if (!isLiveDomFormatterResult(rawResult)) {
              formattedCellRowCache[entry.columnId] = rawResult;
            }
          } catch {
          }
        }
      }
      this.formattedCacheMetadata.totalFormattedCells = totalFormattedCells;
      return true;
    }
  };

  // node_modules/sortablejs/modular/sortable.core.esm.js
  function _defineProperty(e2, r2, t) {
    return (r2 = _toPropertyKey(r2)) in e2 ? Object.defineProperty(e2, r2, {
      value: t,
      enumerable: true,
      configurable: true,
      writable: true
    }) : e2[r2] = t, e2;
  }
  function _extends() {
    return _extends = Object.assign ? Object.assign.bind() : function(n) {
      for (var e2 = 1; e2 < arguments.length; e2++) {
        var t = arguments[e2];
        for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
      }
      return n;
    }, _extends.apply(null, arguments);
  }
  function ownKeys(e2, r2) {
    var t = Object.keys(e2);
    if (Object.getOwnPropertySymbols) {
      var o = Object.getOwnPropertySymbols(e2);
      r2 && (o = o.filter(function(r3) {
        return Object.getOwnPropertyDescriptor(e2, r3).enumerable;
      })), t.push.apply(t, o);
    }
    return t;
  }
  function _objectSpread2(e2) {
    for (var r2 = 1; r2 < arguments.length; r2++) {
      var t = null != arguments[r2] ? arguments[r2] : {};
      r2 % 2 ? ownKeys(Object(t), true).forEach(function(r3) {
        _defineProperty(e2, r3, t[r3]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e2, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function(r3) {
        Object.defineProperty(e2, r3, Object.getOwnPropertyDescriptor(t, r3));
      });
    }
    return e2;
  }
  function _objectWithoutProperties(e2, t) {
    if (null == e2) return {};
    var o, r2, i = _objectWithoutPropertiesLoose(e2, t);
    if (Object.getOwnPropertySymbols) {
      var n = Object.getOwnPropertySymbols(e2);
      for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t.indexOf(o) && {}.propertyIsEnumerable.call(e2, o) && (i[o] = e2[o]);
    }
    return i;
  }
  function _objectWithoutPropertiesLoose(r2, e2) {
    if (null == r2) return {};
    var t = {};
    for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
      if (-1 !== e2.indexOf(n)) continue;
      t[n] = r2[n];
    }
    return t;
  }
  function _toPrimitive(t, r2) {
    if ("object" != typeof t || !t) return t;
    var e2 = t[Symbol.toPrimitive];
    if (void 0 !== e2) {
      var i = e2.call(t, r2 || "default");
      if ("object" != typeof i) return i;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r2 ? String : Number)(t);
  }
  function _toPropertyKey(t) {
    var i = _toPrimitive(t, "string");
    return "symbol" == typeof i ? i : i + "";
  }
  function _typeof(o) {
    "@babel/helpers - typeof";
    return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o2) {
      return typeof o2;
    } : function(o2) {
      return o2 && "function" == typeof Symbol && o2.constructor === Symbol && o2 !== Symbol.prototype ? "symbol" : typeof o2;
    }, _typeof(o);
  }
  var version = "1.15.7";
  function userAgent(pattern) {
    if (typeof window !== "undefined" && window.navigator) {
      return !!/* @__PURE__ */ navigator.userAgent.match(pattern);
    }
  }
  var IE11OrLess = userAgent(/(?:Trident.*rv[ :]?11\.|msie|iemobile|Windows Phone)/i);
  var Edge = userAgent(/Edge/i);
  var FireFox = userAgent(/firefox/i);
  var Safari = userAgent(/safari/i) && !userAgent(/chrome/i) && !userAgent(/android/i);
  var IOS = userAgent(/iP(ad|od|hone)/i);
  var ChromeForAndroid = userAgent(/chrome/i) && userAgent(/android/i);
  var captureMode = {
    capture: false,
    passive: false
  };
  function on(el, event, fn) {
    el.addEventListener(event, fn, !IE11OrLess && captureMode);
  }
  function off(el, event, fn) {
    el.removeEventListener(event, fn, !IE11OrLess && captureMode);
  }
  function matches(el, selector) {
    if (!selector) return;
    selector[0] === ">" && (selector = selector.substring(1));
    if (el) {
      try {
        if (el.matches) {
          return el.matches(selector);
        } else if (el.msMatchesSelector) {
          return el.msMatchesSelector(selector);
        } else if (el.webkitMatchesSelector) {
          return el.webkitMatchesSelector(selector);
        }
      } catch (_2) {
        return false;
      }
    }
    return false;
  }
  function getParentOrHost(el) {
    return el.host && el !== document && el.host.nodeType && el.host !== el ? el.host : el.parentNode;
  }
  function closest(el, selector, ctx, includeCTX) {
    if (el) {
      ctx = ctx || document;
      do {
        if (selector != null && (selector[0] === ">" ? el.parentNode === ctx && matches(el, selector) : matches(el, selector)) || includeCTX && el === ctx) {
          return el;
        }
        if (el === ctx) break;
      } while (el = getParentOrHost(el));
    }
    return null;
  }
  var R_SPACE = /\s+/g;
  function toggleClass(el, name, state2) {
    if (el && name) {
      if (el.classList) {
        el.classList[state2 ? "add" : "remove"](name);
      } else {
        var className = (" " + el.className + " ").replace(R_SPACE, " ").replace(" " + name + " ", " ");
        el.className = (className + (state2 ? " " + name : "")).replace(R_SPACE, " ");
      }
    }
  }
  function css(el, prop, val) {
    var style = el && el.style;
    if (style) {
      if (val === void 0) {
        if (document.defaultView && document.defaultView.getComputedStyle) {
          val = document.defaultView.getComputedStyle(el, "");
        } else if (el.currentStyle) {
          val = el.currentStyle;
        }
        return prop === void 0 ? val : val[prop];
      } else {
        if (!(prop in style) && prop.indexOf("webkit") === -1) {
          prop = "-webkit-" + prop;
        }
        style[prop] = val + (typeof val === "string" ? "" : "px");
      }
    }
  }
  function matrix(el, selfOnly) {
    var appliedTransforms = "";
    if (typeof el === "string") {
      appliedTransforms = el;
    } else {
      do {
        var transform = css(el, "transform");
        if (transform && transform !== "none") {
          appliedTransforms = transform + " " + appliedTransforms;
        }
      } while (!selfOnly && (el = el.parentNode));
    }
    var matrixFn = window.DOMMatrix || window.WebKitCSSMatrix || window.CSSMatrix || window.MSCSSMatrix;
    return matrixFn && new matrixFn(appliedTransforms);
  }
  function find(ctx, tagName, iterator) {
    if (ctx) {
      var list = ctx.getElementsByTagName(tagName), i = 0, n = list.length;
      if (iterator) {
        for (; i < n; i++) {
          iterator(list[i], i);
        }
      }
      return list;
    }
    return [];
  }
  function getWindowScrollingElement() {
    var scrollingElement = document.scrollingElement;
    if (scrollingElement) {
      return scrollingElement;
    } else {
      return document.documentElement;
    }
  }
  function getRect(el, relativeToContainingBlock, relativeToNonStaticParent, undoScale, container) {
    if (!el.getBoundingClientRect && el !== window) return;
    var elRect, top, left, bottom, right, height, width;
    if (el !== window && el.parentNode && el !== getWindowScrollingElement()) {
      elRect = el.getBoundingClientRect();
      top = elRect.top;
      left = elRect.left;
      bottom = elRect.bottom;
      right = elRect.right;
      height = elRect.height;
      width = elRect.width;
    } else {
      top = 0;
      left = 0;
      bottom = window.innerHeight;
      right = window.innerWidth;
      height = window.innerHeight;
      width = window.innerWidth;
    }
    if ((relativeToContainingBlock || relativeToNonStaticParent) && el !== window) {
      container = container || el.parentNode;
      if (!IE11OrLess) {
        do {
          if (container && container.getBoundingClientRect && (css(container, "transform") !== "none" || relativeToNonStaticParent && css(container, "position") !== "static")) {
            var containerRect = container.getBoundingClientRect();
            top -= containerRect.top + parseInt(css(container, "border-top-width"));
            left -= containerRect.left + parseInt(css(container, "border-left-width"));
            bottom = top + elRect.height;
            right = left + elRect.width;
            break;
          }
        } while (container = container.parentNode);
      }
    }
    if (undoScale && el !== window) {
      var elMatrix = matrix(container || el), scaleX = elMatrix && elMatrix.a, scaleY = elMatrix && elMatrix.d;
      if (elMatrix) {
        top /= scaleY;
        left /= scaleX;
        width /= scaleX;
        height /= scaleY;
        bottom = top + height;
        right = left + width;
      }
    }
    return {
      top,
      left,
      bottom,
      right,
      width,
      height
    };
  }
  function isScrolledPast(el, elSide, parentSide) {
    var parent = getParentAutoScrollElement(el, true), elSideVal = getRect(el)[elSide];
    while (parent) {
      var parentSideVal = getRect(parent)[parentSide], visible = void 0;
      if (parentSide === "top" || parentSide === "left") {
        visible = elSideVal >= parentSideVal;
      } else {
        visible = elSideVal <= parentSideVal;
      }
      if (!visible) return parent;
      if (parent === getWindowScrollingElement()) break;
      parent = getParentAutoScrollElement(parent, false);
    }
    return false;
  }
  function getChild(el, childNum, options, includeDragEl) {
    var currentChild = 0, i = 0, children = el.children;
    while (i < children.length) {
      if (children[i].style.display !== "none" && children[i] !== Sortable.ghost && (includeDragEl || children[i] !== Sortable.dragged) && closest(children[i], options.draggable, el, false)) {
        if (currentChild === childNum) {
          return children[i];
        }
        currentChild++;
      }
      i++;
    }
    return null;
  }
  function lastChild(el, selector) {
    var last = el.lastElementChild;
    while (last && (last === Sortable.ghost || css(last, "display") === "none" || selector && !matches(last, selector))) {
      last = last.previousElementSibling;
    }
    return last || null;
  }
  function index(el, selector) {
    var index2 = 0;
    if (!el || !el.parentNode) {
      return -1;
    }
    while (el = el.previousElementSibling) {
      if (el.nodeName.toUpperCase() !== "TEMPLATE" && el !== Sortable.clone && (!selector || matches(el, selector))) {
        index2++;
      }
    }
    return index2;
  }
  function getRelativeScrollOffset(el) {
    var offsetLeft = 0, offsetTop = 0, winScroller = getWindowScrollingElement();
    if (el) {
      do {
        var elMatrix = matrix(el), scaleX = elMatrix.a, scaleY = elMatrix.d;
        offsetLeft += el.scrollLeft * scaleX;
        offsetTop += el.scrollTop * scaleY;
      } while (el !== winScroller && (el = el.parentNode));
    }
    return [offsetLeft, offsetTop];
  }
  function indexOfObject(arr, obj) {
    for (var i in arr) {
      if (!arr.hasOwnProperty(i)) continue;
      for (var key in obj) {
        if (obj.hasOwnProperty(key) && obj[key] === arr[i][key]) return Number(i);
      }
    }
    return -1;
  }
  function getParentAutoScrollElement(el, includeSelf) {
    if (!el || !el.getBoundingClientRect) return getWindowScrollingElement();
    var elem = el;
    var gotSelf = false;
    do {
      if (elem.clientWidth < elem.scrollWidth || elem.clientHeight < elem.scrollHeight) {
        var elemCSS = css(elem);
        if (elem.clientWidth < elem.scrollWidth && (elemCSS.overflowX == "auto" || elemCSS.overflowX == "scroll") || elem.clientHeight < elem.scrollHeight && (elemCSS.overflowY == "auto" || elemCSS.overflowY == "scroll")) {
          if (!elem.getBoundingClientRect || elem === document.body) return getWindowScrollingElement();
          if (gotSelf || includeSelf) return elem;
          gotSelf = true;
        }
      }
    } while (elem = elem.parentNode);
    return getWindowScrollingElement();
  }
  function extend2(dst, src) {
    if (dst && src) {
      for (var key in src) {
        if (src.hasOwnProperty(key)) {
          dst[key] = src[key];
        }
      }
    }
    return dst;
  }
  function isRectEqual(rect1, rect2) {
    return Math.round(rect1.top) === Math.round(rect2.top) && Math.round(rect1.left) === Math.round(rect2.left) && Math.round(rect1.height) === Math.round(rect2.height) && Math.round(rect1.width) === Math.round(rect2.width);
  }
  var _throttleTimeout;
  function throttle(callback, ms) {
    return function() {
      if (!_throttleTimeout) {
        var args = arguments, _this = this;
        if (args.length === 1) {
          callback.call(_this, args[0]);
        } else {
          callback.apply(_this, args);
        }
        _throttleTimeout = setTimeout(function() {
          _throttleTimeout = void 0;
        }, ms);
      }
    };
  }
  function scrollBy(el, x2, y2) {
    el.scrollLeft += x2;
    el.scrollTop += y2;
  }
  function clone(el) {
    var Polymer = window.Polymer;
    var $ = window.jQuery || window.Zepto;
    if (Polymer && Polymer.dom) {
      return Polymer.dom(el).cloneNode(true);
    } else if ($) {
      return $(el).clone(true)[0];
    } else {
      return el.cloneNode(true);
    }
  }
  function getChildContainingRectFromElement(container, options, ghostEl2) {
    var rect = {};
    Array.from(container.children).forEach(function(child) {
      var _rect$left, _rect$top, _rect$right, _rect$bottom;
      if (!closest(child, options.draggable, container, false) || child.animated || child === ghostEl2) return;
      var childRect = getRect(child);
      rect.left = Math.min((_rect$left = rect.left) !== null && _rect$left !== void 0 ? _rect$left : Infinity, childRect.left);
      rect.top = Math.min((_rect$top = rect.top) !== null && _rect$top !== void 0 ? _rect$top : Infinity, childRect.top);
      rect.right = Math.max((_rect$right = rect.right) !== null && _rect$right !== void 0 ? _rect$right : -Infinity, childRect.right);
      rect.bottom = Math.max((_rect$bottom = rect.bottom) !== null && _rect$bottom !== void 0 ? _rect$bottom : -Infinity, childRect.bottom);
    });
    rect.width = rect.right - rect.left;
    rect.height = rect.bottom - rect.top;
    rect.x = rect.left;
    rect.y = rect.top;
    return rect;
  }
  var expando = "Sortable" + (/* @__PURE__ */ new Date()).getTime();
  function AnimationStateManager() {
    var animationStates = [], animationCallbackId;
    return {
      captureAnimationState: function captureAnimationState() {
        animationStates = [];
        if (!this.options.animation) return;
        var children = [].slice.call(this.el.children);
        children.forEach(function(child) {
          if (css(child, "display") === "none" || child === Sortable.ghost) return;
          animationStates.push({
            target: child,
            rect: getRect(child)
          });
          var fromRect = _objectSpread2({}, animationStates[animationStates.length - 1].rect);
          if (child.thisAnimationDuration) {
            var childMatrix = matrix(child, true);
            if (childMatrix) {
              fromRect.top -= childMatrix.f;
              fromRect.left -= childMatrix.e;
            }
          }
          child.fromRect = fromRect;
        });
      },
      addAnimationState: function addAnimationState(state2) {
        animationStates.push(state2);
      },
      removeAnimationState: function removeAnimationState(target) {
        animationStates.splice(indexOfObject(animationStates, {
          target
        }), 1);
      },
      animateAll: function animateAll(callback) {
        var _this = this;
        if (!this.options.animation) {
          clearTimeout(animationCallbackId);
          if (typeof callback === "function") callback();
          return;
        }
        var animating = false, animationTime = 0;
        animationStates.forEach(function(state2) {
          var time = 0, target = state2.target, fromRect = target.fromRect, toRect = getRect(target), prevFromRect = target.prevFromRect, prevToRect = target.prevToRect, animatingRect = state2.rect, targetMatrix = matrix(target, true);
          if (targetMatrix) {
            toRect.top -= targetMatrix.f;
            toRect.left -= targetMatrix.e;
          }
          target.toRect = toRect;
          if (target.thisAnimationDuration) {
            if (isRectEqual(prevFromRect, toRect) && !isRectEqual(fromRect, toRect) && // Make sure animatingRect is on line between toRect & fromRect
            (animatingRect.top - toRect.top) / (animatingRect.left - toRect.left) === (fromRect.top - toRect.top) / (fromRect.left - toRect.left)) {
              time = calculateRealTime(animatingRect, prevFromRect, prevToRect, _this.options);
            }
          }
          if (!isRectEqual(toRect, fromRect)) {
            target.prevFromRect = fromRect;
            target.prevToRect = toRect;
            if (!time) {
              time = _this.options.animation;
            }
            _this.animate(target, animatingRect, toRect, time);
          }
          if (time) {
            animating = true;
            animationTime = Math.max(animationTime, time);
            clearTimeout(target.animationResetTimer);
            target.animationResetTimer = setTimeout(function() {
              target.animationTime = 0;
              target.prevFromRect = null;
              target.fromRect = null;
              target.prevToRect = null;
              target.thisAnimationDuration = null;
            }, time);
            target.thisAnimationDuration = time;
          }
        });
        clearTimeout(animationCallbackId);
        if (!animating) {
          if (typeof callback === "function") callback();
        } else {
          animationCallbackId = setTimeout(function() {
            if (typeof callback === "function") callback();
          }, animationTime);
        }
        animationStates = [];
      },
      animate: function animate(target, currentRect, toRect, duration) {
        if (duration) {
          css(target, "transition", "");
          css(target, "transform", "");
          var elMatrix = matrix(this.el), scaleX = elMatrix && elMatrix.a, scaleY = elMatrix && elMatrix.d, translateX = (currentRect.left - toRect.left) / (scaleX || 1), translateY = (currentRect.top - toRect.top) / (scaleY || 1);
          target.animatingX = !!translateX;
          target.animatingY = !!translateY;
          css(target, "transform", "translate3d(" + translateX + "px," + translateY + "px,0)");
          this.forRepaintDummy = repaint(target);
          css(target, "transition", "transform " + duration + "ms" + (this.options.easing ? " " + this.options.easing : ""));
          css(target, "transform", "translate3d(0,0,0)");
          typeof target.animated === "number" && clearTimeout(target.animated);
          target.animated = setTimeout(function() {
            css(target, "transition", "");
            css(target, "transform", "");
            target.animated = false;
            target.animatingX = false;
            target.animatingY = false;
          }, duration);
        }
      }
    };
  }
  function repaint(target) {
    return target.offsetWidth;
  }
  function calculateRealTime(animatingRect, fromRect, toRect, options) {
    return Math.sqrt(Math.pow(fromRect.top - animatingRect.top, 2) + Math.pow(fromRect.left - animatingRect.left, 2)) / Math.sqrt(Math.pow(fromRect.top - toRect.top, 2) + Math.pow(fromRect.left - toRect.left, 2)) * options.animation;
  }
  var plugins = [];
  var defaults = {
    initializeByDefault: true
  };
  var PluginManager = {
    mount: function mount(plugin) {
      for (var option2 in defaults) {
        if (defaults.hasOwnProperty(option2) && !(option2 in plugin)) {
          plugin[option2] = defaults[option2];
        }
      }
      plugins.forEach(function(p) {
        if (p.pluginName === plugin.pluginName) {
          throw "Sortable: Cannot mount plugin ".concat(plugin.pluginName, " more than once");
        }
      });
      plugins.push(plugin);
    },
    pluginEvent: function pluginEvent(eventName, sortable, evt) {
      var _this = this;
      this.eventCanceled = false;
      evt.cancel = function() {
        _this.eventCanceled = true;
      };
      var eventNameGlobal = eventName + "Global";
      plugins.forEach(function(plugin) {
        if (!sortable[plugin.pluginName]) return;
        if (sortable[plugin.pluginName][eventNameGlobal]) {
          sortable[plugin.pluginName][eventNameGlobal](_objectSpread2({
            sortable
          }, evt));
        }
        if (sortable.options[plugin.pluginName] && sortable[plugin.pluginName][eventName]) {
          sortable[plugin.pluginName][eventName](_objectSpread2({
            sortable
          }, evt));
        }
      });
    },
    initializePlugins: function initializePlugins(sortable, el, defaults2, options) {
      plugins.forEach(function(plugin) {
        var pluginName = plugin.pluginName;
        if (!sortable.options[pluginName] && !plugin.initializeByDefault) return;
        var initialized = new plugin(sortable, el, sortable.options);
        initialized.sortable = sortable;
        initialized.options = sortable.options;
        sortable[pluginName] = initialized;
        _extends(defaults2, initialized.defaults);
      });
      for (var option2 in sortable.options) {
        if (!sortable.options.hasOwnProperty(option2)) continue;
        var modified = this.modifyOption(sortable, option2, sortable.options[option2]);
        if (typeof modified !== "undefined") {
          sortable.options[option2] = modified;
        }
      }
    },
    getEventProperties: function getEventProperties(name, sortable) {
      var eventProperties = {};
      plugins.forEach(function(plugin) {
        if (typeof plugin.eventProperties !== "function") return;
        _extends(eventProperties, plugin.eventProperties.call(sortable[plugin.pluginName], name));
      });
      return eventProperties;
    },
    modifyOption: function modifyOption(sortable, name, value) {
      var modifiedValue;
      plugins.forEach(function(plugin) {
        if (!sortable[plugin.pluginName]) return;
        if (plugin.optionListeners && typeof plugin.optionListeners[name] === "function") {
          modifiedValue = plugin.optionListeners[name].call(sortable[plugin.pluginName], value);
        }
      });
      return modifiedValue;
    }
  };
  function dispatchEvent(_ref) {
    var sortable = _ref.sortable, rootEl2 = _ref.rootEl, name = _ref.name, targetEl = _ref.targetEl, cloneEl2 = _ref.cloneEl, toEl = _ref.toEl, fromEl = _ref.fromEl, oldIndex2 = _ref.oldIndex, newIndex2 = _ref.newIndex, oldDraggableIndex2 = _ref.oldDraggableIndex, newDraggableIndex2 = _ref.newDraggableIndex, originalEvent = _ref.originalEvent, putSortable2 = _ref.putSortable, extraEventProperties = _ref.extraEventProperties;
    sortable = sortable || rootEl2 && rootEl2[expando];
    if (!sortable) return;
    var evt, options = sortable.options, onName = "on" + name.charAt(0).toUpperCase() + name.substr(1);
    if (window.CustomEvent && !IE11OrLess && !Edge) {
      evt = new CustomEvent(name, {
        bubbles: true,
        cancelable: true
      });
    } else {
      evt = document.createEvent("Event");
      evt.initEvent(name, true, true);
    }
    evt.to = toEl || rootEl2;
    evt.from = fromEl || rootEl2;
    evt.item = targetEl || rootEl2;
    evt.clone = cloneEl2;
    evt.oldIndex = oldIndex2;
    evt.newIndex = newIndex2;
    evt.oldDraggableIndex = oldDraggableIndex2;
    evt.newDraggableIndex = newDraggableIndex2;
    evt.originalEvent = originalEvent;
    evt.pullMode = putSortable2 ? putSortable2.lastPutMode : void 0;
    var allEventProperties = _objectSpread2(_objectSpread2({}, extraEventProperties), PluginManager.getEventProperties(name, sortable));
    for (var option2 in allEventProperties) {
      evt[option2] = allEventProperties[option2];
    }
    if (rootEl2) {
      rootEl2.dispatchEvent(evt);
    }
    if (options[onName]) {
      options[onName].call(sortable, evt);
    }
  }
  var _excluded = ["evt"];
  var pluginEvent2 = function pluginEvent3(eventName, sortable) {
    var _ref = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {}, originalEvent = _ref.evt, data = _objectWithoutProperties(_ref, _excluded);
    PluginManager.pluginEvent.bind(Sortable)(eventName, sortable, _objectSpread2({
      dragEl,
      parentEl,
      ghostEl,
      rootEl,
      nextEl,
      lastDownEl,
      cloneEl,
      cloneHidden,
      dragStarted: moved,
      putSortable,
      activeSortable: Sortable.active,
      originalEvent,
      oldIndex,
      oldDraggableIndex,
      newIndex,
      newDraggableIndex,
      hideGhostForTarget: _hideGhostForTarget,
      unhideGhostForTarget: _unhideGhostForTarget,
      cloneNowHidden: function cloneNowHidden() {
        cloneHidden = true;
      },
      cloneNowShown: function cloneNowShown() {
        cloneHidden = false;
      },
      dispatchSortableEvent: function dispatchSortableEvent(name) {
        _dispatchEvent({
          sortable,
          name,
          originalEvent
        });
      }
    }, data));
  };
  function _dispatchEvent(info) {
    dispatchEvent(_objectSpread2({
      putSortable,
      cloneEl,
      targetEl: dragEl,
      rootEl,
      oldIndex,
      oldDraggableIndex,
      newIndex,
      newDraggableIndex
    }, info));
  }
  var dragEl;
  var parentEl;
  var ghostEl;
  var rootEl;
  var nextEl;
  var lastDownEl;
  var cloneEl;
  var cloneHidden;
  var oldIndex;
  var newIndex;
  var oldDraggableIndex;
  var newDraggableIndex;
  var activeGroup;
  var putSortable;
  var awaitingDragStarted = false;
  var ignoreNextClick = false;
  var sortables = [];
  var tapEvt;
  var touchEvt;
  var lastDx;
  var lastDy;
  var tapDistanceLeft;
  var tapDistanceTop;
  var moved;
  var lastTarget;
  var lastDirection;
  var pastFirstInvertThresh = false;
  var isCircumstantialInvert = false;
  var targetMoveDistance;
  var ghostRelativeParent;
  var ghostRelativeParentInitialScroll = [];
  var _silent = false;
  var savedInputChecked = [];
  var documentExists = typeof document !== "undefined";
  var PositionGhostAbsolutely = IOS;
  var CSSFloatProperty = Edge || IE11OrLess ? "cssFloat" : "float";
  var supportDraggable = documentExists && !ChromeForAndroid && !IOS && "draggable" in document.createElement("div");
  var supportCssPointerEvents = (function() {
    if (!documentExists) return;
    if (IE11OrLess) {
      return false;
    }
    var el = document.createElement("x");
    el.style.cssText = "pointer-events:auto";
    return el.style.pointerEvents === "auto";
  })();
  var _detectDirection = function _detectDirection2(el, options) {
    var elCSS = css(el), elWidth = parseInt(elCSS.width) - parseInt(elCSS.paddingLeft) - parseInt(elCSS.paddingRight) - parseInt(elCSS.borderLeftWidth) - parseInt(elCSS.borderRightWidth), child1 = getChild(el, 0, options), child2 = getChild(el, 1, options), firstChildCSS = child1 && css(child1), secondChildCSS = child2 && css(child2), firstChildWidth = firstChildCSS && parseInt(firstChildCSS.marginLeft) + parseInt(firstChildCSS.marginRight) + getRect(child1).width, secondChildWidth = secondChildCSS && parseInt(secondChildCSS.marginLeft) + parseInt(secondChildCSS.marginRight) + getRect(child2).width;
    if (elCSS.display === "flex") {
      return elCSS.flexDirection === "column" || elCSS.flexDirection === "column-reverse" ? "vertical" : "horizontal";
    }
    if (elCSS.display === "grid") {
      return elCSS.gridTemplateColumns.split(" ").length <= 1 ? "vertical" : "horizontal";
    }
    if (child1 && firstChildCSS["float"] && firstChildCSS["float"] !== "none") {
      var touchingSideChild2 = firstChildCSS["float"] === "left" ? "left" : "right";
      return child2 && (secondChildCSS.clear === "both" || secondChildCSS.clear === touchingSideChild2) ? "vertical" : "horizontal";
    }
    return child1 && (firstChildCSS.display === "block" || firstChildCSS.display === "flex" || firstChildCSS.display === "table" || firstChildCSS.display === "grid" || firstChildWidth >= elWidth && elCSS[CSSFloatProperty] === "none" || child2 && elCSS[CSSFloatProperty] === "none" && firstChildWidth + secondChildWidth > elWidth) ? "vertical" : "horizontal";
  };
  var _dragElInRowColumn = function _dragElInRowColumn2(dragRect, targetRect, vertical) {
    var dragElS1Opp = vertical ? dragRect.left : dragRect.top, dragElS2Opp = vertical ? dragRect.right : dragRect.bottom, dragElOppLength = vertical ? dragRect.width : dragRect.height, targetS1Opp = vertical ? targetRect.left : targetRect.top, targetS2Opp = vertical ? targetRect.right : targetRect.bottom, targetOppLength = vertical ? targetRect.width : targetRect.height;
    return dragElS1Opp === targetS1Opp || dragElS2Opp === targetS2Opp || dragElS1Opp + dragElOppLength / 2 === targetS1Opp + targetOppLength / 2;
  };
  var _detectNearestEmptySortable = function _detectNearestEmptySortable2(x2, y2) {
    var ret;
    sortables.some(function(sortable) {
      var threshold = sortable[expando].options.emptyInsertThreshold;
      if (!threshold || lastChild(sortable)) return;
      var rect = getRect(sortable), insideHorizontally = x2 >= rect.left - threshold && x2 <= rect.right + threshold, insideVertically = y2 >= rect.top - threshold && y2 <= rect.bottom + threshold;
      if (insideHorizontally && insideVertically) {
        return ret = sortable;
      }
    });
    return ret;
  };
  var _prepareGroup = function _prepareGroup2(options) {
    function toFn(value, pull) {
      return function(to, from, dragEl2, evt) {
        var sameGroup = to.options.group.name && from.options.group.name && to.options.group.name === from.options.group.name;
        if (value == null && (pull || sameGroup)) {
          return true;
        } else if (value == null || value === false) {
          return false;
        } else if (pull && value === "clone") {
          return value;
        } else if (typeof value === "function") {
          return toFn(value(to, from, dragEl2, evt), pull)(to, from, dragEl2, evt);
        } else {
          var otherGroup = (pull ? to : from).options.group.name;
          return value === true || typeof value === "string" && value === otherGroup || value.join && value.indexOf(otherGroup) > -1;
        }
      };
    }
    var group = {};
    var originalGroup = options.group;
    if (!originalGroup || _typeof(originalGroup) != "object") {
      originalGroup = {
        name: originalGroup
      };
    }
    group.name = originalGroup.name;
    group.checkPull = toFn(originalGroup.pull, true);
    group.checkPut = toFn(originalGroup.put);
    group.revertClone = originalGroup.revertClone;
    options.group = group;
  };
  var _hideGhostForTarget = function _hideGhostForTarget2() {
    if (!supportCssPointerEvents && ghostEl) {
      css(ghostEl, "display", "none");
    }
  };
  var _unhideGhostForTarget = function _unhideGhostForTarget2() {
    if (!supportCssPointerEvents && ghostEl) {
      css(ghostEl, "display", "");
    }
  };
  if (documentExists && !ChromeForAndroid) {
    document.addEventListener("click", function(evt) {
      if (ignoreNextClick) {
        evt.preventDefault();
        evt.stopPropagation && evt.stopPropagation();
        evt.stopImmediatePropagation && evt.stopImmediatePropagation();
        ignoreNextClick = false;
        return false;
      }
    }, true);
  }
  var nearestEmptyInsertDetectEvent = function nearestEmptyInsertDetectEvent2(evt) {
    if (dragEl) {
      evt = evt.touches ? evt.touches[0] : evt;
      var nearest = _detectNearestEmptySortable(evt.clientX, evt.clientY);
      if (nearest) {
        var event = {};
        for (var i in evt) {
          if (evt.hasOwnProperty(i)) {
            event[i] = evt[i];
          }
        }
        event.target = event.rootEl = nearest;
        event.preventDefault = void 0;
        event.stopPropagation = void 0;
        nearest[expando]._onDragOver(event);
      }
    }
  };
  var _checkOutsideTargetEl = function _checkOutsideTargetEl2(evt) {
    if (dragEl) {
      dragEl.parentNode[expando]._isOutsideThisEl(evt.target);
    }
  };
  function Sortable(el, options) {
    if (!(el && el.nodeType && el.nodeType === 1)) {
      throw "Sortable: `el` must be an HTMLElement, not ".concat({}.toString.call(el));
    }
    this.el = el;
    this.options = options = _extends({}, options);
    el[expando] = this;
    var defaults2 = {
      group: null,
      sort: true,
      disabled: false,
      store: null,
      handle: null,
      draggable: /^[uo]l$/i.test(el.nodeName) ? ">li" : ">*",
      swapThreshold: 1,
      // percentage; 0 <= x <= 1
      invertSwap: false,
      // invert always
      invertedSwapThreshold: null,
      // will be set to same as swapThreshold if default
      removeCloneOnHide: true,
      direction: function direction() {
        return _detectDirection(el, this.options);
      },
      ghostClass: "sortable-ghost",
      chosenClass: "sortable-chosen",
      dragClass: "sortable-drag",
      ignore: "a, img",
      filter: null,
      preventOnFilter: true,
      animation: 0,
      easing: null,
      setData: function setData(dataTransfer, dragEl2) {
        dataTransfer.setData("Text", dragEl2.textContent);
      },
      dropBubble: false,
      dragoverBubble: false,
      dataIdAttr: "data-id",
      delay: 0,
      delayOnTouchOnly: false,
      touchStartThreshold: (Number.parseInt ? Number : window).parseInt(window.devicePixelRatio, 10) || 1,
      forceFallback: false,
      fallbackClass: "sortable-fallback",
      fallbackOnBody: false,
      fallbackTolerance: 0,
      fallbackOffset: {
        x: 0,
        y: 0
      },
      // Disabled on Safari: #1571; Enabled on Safari IOS: #2244
      supportPointer: Sortable.supportPointer !== false && "PointerEvent" in window && (!Safari || IOS),
      emptyInsertThreshold: 5
    };
    PluginManager.initializePlugins(this, el, defaults2);
    for (var name in defaults2) {
      !(name in options) && (options[name] = defaults2[name]);
    }
    _prepareGroup(options);
    for (var fn in this) {
      if (fn.charAt(0) === "_" && typeof this[fn] === "function") {
        this[fn] = this[fn].bind(this);
      }
    }
    this.nativeDraggable = options.forceFallback ? false : supportDraggable;
    if (this.nativeDraggable) {
      this.options.touchStartThreshold = 1;
    }
    if (options.supportPointer) {
      on(el, "pointerdown", this._onTapStart);
    } else {
      on(el, "mousedown", this._onTapStart);
      on(el, "touchstart", this._onTapStart);
    }
    if (this.nativeDraggable) {
      on(el, "dragover", this);
      on(el, "dragenter", this);
    }
    sortables.push(this.el);
    options.store && options.store.get && this.sort(options.store.get(this) || []);
    _extends(this, AnimationStateManager());
  }
  Sortable.prototype = /** @lends Sortable.prototype */
  {
    constructor: Sortable,
    _isOutsideThisEl: function _isOutsideThisEl(target) {
      if (!this.el.contains(target) && target !== this.el) {
        lastTarget = null;
      }
    },
    _getDirection: function _getDirection(evt, target) {
      return typeof this.options.direction === "function" ? this.options.direction.call(this, evt, target, dragEl) : this.options.direction;
    },
    _onTapStart: function _onTapStart(evt) {
      if (!evt.cancelable) return;
      var _this = this, el = this.el, options = this.options, preventOnFilter = options.preventOnFilter, type = evt.type, touch = evt.touches && evt.touches[0] || evt.pointerType && evt.pointerType === "touch" && evt, target = (touch || evt).target, originalTarget = evt.target.shadowRoot && (evt.path && evt.path[0] || evt.composedPath && evt.composedPath()[0]) || target, filter = options.filter;
      _saveInputCheckedState(el);
      if (dragEl) {
        return;
      }
      if (/mousedown|pointerdown/.test(type) && evt.button !== 0 || options.disabled) {
        return;
      }
      if (originalTarget.isContentEditable) {
        return;
      }
      if (!this.nativeDraggable && Safari && target && target.tagName.toUpperCase() === "SELECT") {
        return;
      }
      target = closest(target, options.draggable, el, false);
      if (target && target.animated) {
        return;
      }
      if (lastDownEl === target) {
        return;
      }
      oldIndex = index(target);
      oldDraggableIndex = index(target, options.draggable);
      if (typeof filter === "function") {
        if (filter.call(this, evt, target, this)) {
          _dispatchEvent({
            sortable: _this,
            rootEl: originalTarget,
            name: "filter",
            targetEl: target,
            toEl: el,
            fromEl: el
          });
          pluginEvent2("filter", _this, {
            evt
          });
          preventOnFilter && evt.preventDefault();
          return;
        }
      } else if (filter) {
        filter = filter.split(",").some(function(criteria) {
          criteria = closest(originalTarget, criteria.trim(), el, false);
          if (criteria) {
            _dispatchEvent({
              sortable: _this,
              rootEl: criteria,
              name: "filter",
              targetEl: target,
              fromEl: el,
              toEl: el
            });
            pluginEvent2("filter", _this, {
              evt
            });
            return true;
          }
        });
        if (filter) {
          preventOnFilter && evt.preventDefault();
          return;
        }
      }
      if (options.handle && !closest(originalTarget, options.handle, el, false)) {
        return;
      }
      this._prepareDragStart(evt, touch, target);
    },
    _prepareDragStart: function _prepareDragStart(evt, touch, target) {
      var _this = this, el = _this.el, options = _this.options, ownerDocument = el.ownerDocument, dragStartFn;
      if (target && !dragEl && target.parentNode === el) {
        var dragRect = getRect(target);
        rootEl = el;
        dragEl = target;
        parentEl = dragEl.parentNode;
        nextEl = dragEl.nextSibling;
        lastDownEl = target;
        activeGroup = options.group;
        Sortable.dragged = dragEl;
        tapEvt = {
          target: dragEl,
          clientX: (touch || evt).clientX,
          clientY: (touch || evt).clientY
        };
        tapDistanceLeft = tapEvt.clientX - dragRect.left;
        tapDistanceTop = tapEvt.clientY - dragRect.top;
        this._lastX = (touch || evt).clientX;
        this._lastY = (touch || evt).clientY;
        dragEl.style["will-change"] = "all";
        dragStartFn = function dragStartFn2() {
          pluginEvent2("delayEnded", _this, {
            evt
          });
          if (Sortable.eventCanceled) {
            _this._onDrop();
            return;
          }
          _this._disableDelayedDragEvents();
          if (!FireFox && _this.nativeDraggable) {
            dragEl.draggable = true;
          }
          _this._triggerDragStart(evt, touch);
          _dispatchEvent({
            sortable: _this,
            name: "choose",
            originalEvent: evt
          });
          toggleClass(dragEl, options.chosenClass, true);
        };
        options.ignore.split(",").forEach(function(criteria) {
          find(dragEl, criteria.trim(), _disableDraggable);
        });
        on(ownerDocument, "dragover", nearestEmptyInsertDetectEvent);
        on(ownerDocument, "mousemove", nearestEmptyInsertDetectEvent);
        on(ownerDocument, "touchmove", nearestEmptyInsertDetectEvent);
        if (options.supportPointer) {
          on(ownerDocument, "pointerup", _this._onDrop);
          !this.nativeDraggable && on(ownerDocument, "pointercancel", _this._onDrop);
        } else {
          on(ownerDocument, "mouseup", _this._onDrop);
          on(ownerDocument, "touchend", _this._onDrop);
          on(ownerDocument, "touchcancel", _this._onDrop);
        }
        if (FireFox && this.nativeDraggable) {
          this.options.touchStartThreshold = 4;
          dragEl.draggable = true;
        }
        pluginEvent2("delayStart", this, {
          evt
        });
        if (options.delay && (!options.delayOnTouchOnly || touch) && (!this.nativeDraggable || !(Edge || IE11OrLess))) {
          if (Sortable.eventCanceled) {
            this._onDrop();
            return;
          }
          if (options.supportPointer) {
            on(ownerDocument, "pointerup", _this._disableDelayedDrag);
            on(ownerDocument, "pointercancel", _this._disableDelayedDrag);
          } else {
            on(ownerDocument, "mouseup", _this._disableDelayedDrag);
            on(ownerDocument, "touchend", _this._disableDelayedDrag);
            on(ownerDocument, "touchcancel", _this._disableDelayedDrag);
          }
          on(ownerDocument, "mousemove", _this._delayedDragTouchMoveHandler);
          on(ownerDocument, "touchmove", _this._delayedDragTouchMoveHandler);
          options.supportPointer && on(ownerDocument, "pointermove", _this._delayedDragTouchMoveHandler);
          _this._dragStartTimer = setTimeout(dragStartFn, options.delay);
        } else {
          dragStartFn();
        }
      }
    },
    _delayedDragTouchMoveHandler: function _delayedDragTouchMoveHandler(e2) {
      var touch = e2.touches ? e2.touches[0] : e2;
      if (Math.max(Math.abs(touch.clientX - this._lastX), Math.abs(touch.clientY - this._lastY)) >= Math.floor(this.options.touchStartThreshold / (this.nativeDraggable && window.devicePixelRatio || 1))) {
        this._disableDelayedDrag();
      }
    },
    _disableDelayedDrag: function _disableDelayedDrag() {
      dragEl && _disableDraggable(dragEl);
      clearTimeout(this._dragStartTimer);
      this._disableDelayedDragEvents();
    },
    _disableDelayedDragEvents: function _disableDelayedDragEvents() {
      var ownerDocument = this.el.ownerDocument;
      off(ownerDocument, "mouseup", this._disableDelayedDrag);
      off(ownerDocument, "touchend", this._disableDelayedDrag);
      off(ownerDocument, "touchcancel", this._disableDelayedDrag);
      off(ownerDocument, "pointerup", this._disableDelayedDrag);
      off(ownerDocument, "pointercancel", this._disableDelayedDrag);
      off(ownerDocument, "mousemove", this._delayedDragTouchMoveHandler);
      off(ownerDocument, "touchmove", this._delayedDragTouchMoveHandler);
      off(ownerDocument, "pointermove", this._delayedDragTouchMoveHandler);
    },
    _triggerDragStart: function _triggerDragStart(evt, touch) {
      touch = touch || evt.pointerType == "touch" && evt;
      if (!this.nativeDraggable || touch) {
        if (this.options.supportPointer) {
          on(document, "pointermove", this._onTouchMove);
        } else if (touch) {
          on(document, "touchmove", this._onTouchMove);
        } else {
          on(document, "mousemove", this._onTouchMove);
        }
      } else {
        on(dragEl, "dragend", this);
        on(rootEl, "dragstart", this._onDragStart);
      }
      try {
        if (document.selection) {
          _nextTick(function() {
            document.selection.empty();
          });
        } else {
          window.getSelection().removeAllRanges();
        }
      } catch (err) {
      }
    },
    _dragStarted: function _dragStarted(fallback, evt) {
      awaitingDragStarted = false;
      if (rootEl && dragEl) {
        pluginEvent2("dragStarted", this, {
          evt
        });
        if (this.nativeDraggable) {
          on(document, "dragover", _checkOutsideTargetEl);
        }
        var options = this.options;
        !fallback && toggleClass(dragEl, options.dragClass, false);
        toggleClass(dragEl, options.ghostClass, true);
        Sortable.active = this;
        fallback && this._appendGhost();
        _dispatchEvent({
          sortable: this,
          name: "start",
          originalEvent: evt
        });
      } else {
        this._nulling();
      }
    },
    _emulateDragOver: function _emulateDragOver() {
      if (touchEvt) {
        this._lastX = touchEvt.clientX;
        this._lastY = touchEvt.clientY;
        _hideGhostForTarget();
        var target = document.elementFromPoint(touchEvt.clientX, touchEvt.clientY);
        var parent = target;
        while (target && target.shadowRoot) {
          target = target.shadowRoot.elementFromPoint(touchEvt.clientX, touchEvt.clientY);
          if (target === parent) break;
          parent = target;
        }
        dragEl.parentNode[expando]._isOutsideThisEl(target);
        if (parent) {
          do {
            if (parent[expando]) {
              var inserted = void 0;
              inserted = parent[expando]._onDragOver({
                clientX: touchEvt.clientX,
                clientY: touchEvt.clientY,
                target,
                rootEl: parent
              });
              if (inserted && !this.options.dragoverBubble) {
                break;
              }
            }
            target = parent;
          } while (parent = getParentOrHost(parent));
        }
        _unhideGhostForTarget();
      }
    },
    _onTouchMove: function _onTouchMove(evt) {
      if (tapEvt) {
        var options = this.options, fallbackTolerance = options.fallbackTolerance, fallbackOffset = options.fallbackOffset, touch = evt.touches ? evt.touches[0] : evt, ghostMatrix = ghostEl && matrix(ghostEl, true), scaleX = ghostEl && ghostMatrix && ghostMatrix.a, scaleY = ghostEl && ghostMatrix && ghostMatrix.d, relativeScrollOffset = PositionGhostAbsolutely && ghostRelativeParent && getRelativeScrollOffset(ghostRelativeParent), dx = (touch.clientX - tapEvt.clientX + fallbackOffset.x) / (scaleX || 1) + (relativeScrollOffset ? relativeScrollOffset[0] - ghostRelativeParentInitialScroll[0] : 0) / (scaleX || 1), dy = (touch.clientY - tapEvt.clientY + fallbackOffset.y) / (scaleY || 1) + (relativeScrollOffset ? relativeScrollOffset[1] - ghostRelativeParentInitialScroll[1] : 0) / (scaleY || 1);
        if (!Sortable.active && !awaitingDragStarted) {
          if (fallbackTolerance && Math.max(Math.abs(touch.clientX - this._lastX), Math.abs(touch.clientY - this._lastY)) < fallbackTolerance) {
            return;
          }
          this._onDragStart(evt, true);
        }
        if (ghostEl) {
          if (ghostMatrix) {
            ghostMatrix.e += dx - (lastDx || 0);
            ghostMatrix.f += dy - (lastDy || 0);
          } else {
            ghostMatrix = {
              a: 1,
              b: 0,
              c: 0,
              d: 1,
              e: dx,
              f: dy
            };
          }
          var cssMatrix = "matrix(".concat(ghostMatrix.a, ",").concat(ghostMatrix.b, ",").concat(ghostMatrix.c, ",").concat(ghostMatrix.d, ",").concat(ghostMatrix.e, ",").concat(ghostMatrix.f, ")");
          css(ghostEl, "webkitTransform", cssMatrix);
          css(ghostEl, "mozTransform", cssMatrix);
          css(ghostEl, "msTransform", cssMatrix);
          css(ghostEl, "transform", cssMatrix);
          lastDx = dx;
          lastDy = dy;
          touchEvt = touch;
        }
        evt.cancelable && evt.preventDefault();
      }
    },
    _appendGhost: function _appendGhost() {
      if (!ghostEl) {
        var container = this.options.fallbackOnBody ? document.body : rootEl, rect = getRect(dragEl, true, PositionGhostAbsolutely, true, container), options = this.options;
        if (PositionGhostAbsolutely) {
          ghostRelativeParent = container;
          while (css(ghostRelativeParent, "position") === "static" && css(ghostRelativeParent, "transform") === "none" && ghostRelativeParent !== document) {
            ghostRelativeParent = ghostRelativeParent.parentNode;
          }
          if (ghostRelativeParent !== document.body && ghostRelativeParent !== document.documentElement) {
            if (ghostRelativeParent === document) ghostRelativeParent = getWindowScrollingElement();
            rect.top += ghostRelativeParent.scrollTop;
            rect.left += ghostRelativeParent.scrollLeft;
          } else {
            ghostRelativeParent = getWindowScrollingElement();
          }
          ghostRelativeParentInitialScroll = getRelativeScrollOffset(ghostRelativeParent);
        }
        ghostEl = dragEl.cloneNode(true);
        toggleClass(ghostEl, options.ghostClass, false);
        toggleClass(ghostEl, options.fallbackClass, true);
        toggleClass(ghostEl, options.dragClass, true);
        css(ghostEl, "transition", "");
        css(ghostEl, "transform", "");
        css(ghostEl, "box-sizing", "border-box");
        css(ghostEl, "margin", 0);
        css(ghostEl, "top", rect.top);
        css(ghostEl, "left", rect.left);
        css(ghostEl, "width", rect.width);
        css(ghostEl, "height", rect.height);
        css(ghostEl, "opacity", "0.8");
        css(ghostEl, "position", PositionGhostAbsolutely ? "absolute" : "fixed");
        css(ghostEl, "zIndex", "100000");
        css(ghostEl, "pointerEvents", "none");
        Sortable.ghost = ghostEl;
        container.appendChild(ghostEl);
        css(ghostEl, "transform-origin", tapDistanceLeft / parseInt(ghostEl.style.width) * 100 + "% " + tapDistanceTop / parseInt(ghostEl.style.height) * 100 + "%");
      }
    },
    _onDragStart: function _onDragStart(evt, fallback) {
      var _this = this;
      var dataTransfer = evt.dataTransfer;
      var options = _this.options;
      pluginEvent2("dragStart", this, {
        evt
      });
      if (Sortable.eventCanceled) {
        this._onDrop();
        return;
      }
      pluginEvent2("setupClone", this);
      if (!Sortable.eventCanceled) {
        cloneEl = clone(dragEl);
        cloneEl.removeAttribute("id");
        cloneEl.draggable = false;
        cloneEl.style["will-change"] = "";
        this._hideClone();
        toggleClass(cloneEl, this.options.chosenClass, false);
        Sortable.clone = cloneEl;
      }
      _this.cloneId = _nextTick(function() {
        pluginEvent2("clone", _this);
        if (Sortable.eventCanceled) return;
        if (!_this.options.removeCloneOnHide) {
          rootEl.insertBefore(cloneEl, dragEl);
        }
        _this._hideClone();
        _dispatchEvent({
          sortable: _this,
          name: "clone"
        });
      });
      !fallback && toggleClass(dragEl, options.dragClass, true);
      if (fallback) {
        ignoreNextClick = true;
        _this._loopId = setInterval(_this._emulateDragOver, 50);
      } else {
        off(document, "mouseup", _this._onDrop);
        off(document, "touchend", _this._onDrop);
        off(document, "touchcancel", _this._onDrop);
        if (dataTransfer) {
          dataTransfer.effectAllowed = "move";
          options.setData && options.setData.call(_this, dataTransfer, dragEl);
        }
        on(document, "drop", _this);
        css(dragEl, "transform", "translateZ(0)");
      }
      awaitingDragStarted = true;
      _this._dragStartId = _nextTick(_this._dragStarted.bind(_this, fallback, evt));
      on(document, "selectstart", _this);
      moved = true;
      window.getSelection().removeAllRanges();
      if (Safari) {
        css(document.body, "user-select", "none");
      }
    },
    // Returns true - if no further action is needed (either inserted or another condition)
    _onDragOver: function _onDragOver(evt) {
      var el = this.el, target = evt.target, dragRect, targetRect, revert, options = this.options, group = options.group, activeSortable = Sortable.active, isOwner = activeGroup === group, canSort = options.sort, fromSortable = putSortable || activeSortable, vertical, _this = this, completedFired = false;
      if (_silent) return;
      function dragOverEvent(name, extra) {
        pluginEvent2(name, _this, _objectSpread2({
          evt,
          isOwner,
          axis: vertical ? "vertical" : "horizontal",
          revert,
          dragRect,
          targetRect,
          canSort,
          fromSortable,
          target,
          completed,
          onMove: function onMove(target2, after2) {
            return _onMove(rootEl, el, dragEl, dragRect, target2, getRect(target2), evt, after2);
          },
          changed
        }, extra));
      }
      function capture() {
        dragOverEvent("dragOverAnimationCapture");
        _this.captureAnimationState();
        if (_this !== fromSortable) {
          fromSortable.captureAnimationState();
        }
      }
      function completed(insertion) {
        dragOverEvent("dragOverCompleted", {
          insertion
        });
        if (insertion) {
          if (isOwner) {
            activeSortable._hideClone();
          } else {
            activeSortable._showClone(_this);
          }
          if (_this !== fromSortable) {
            toggleClass(dragEl, putSortable ? putSortable.options.ghostClass : activeSortable.options.ghostClass, false);
            toggleClass(dragEl, options.ghostClass, true);
          }
          if (putSortable !== _this && _this !== Sortable.active) {
            putSortable = _this;
          } else if (_this === Sortable.active && putSortable) {
            putSortable = null;
          }
          if (fromSortable === _this) {
            _this._ignoreWhileAnimating = target;
          }
          _this.animateAll(function() {
            dragOverEvent("dragOverAnimationComplete");
            _this._ignoreWhileAnimating = null;
          });
          if (_this !== fromSortable) {
            fromSortable.animateAll();
            fromSortable._ignoreWhileAnimating = null;
          }
        }
        if (target === dragEl && !dragEl.animated || target === el && !target.animated) {
          lastTarget = null;
        }
        if (!options.dragoverBubble && !evt.rootEl && target !== document) {
          dragEl.parentNode[expando]._isOutsideThisEl(evt.target);
          !insertion && nearestEmptyInsertDetectEvent(evt);
        }
        !options.dragoverBubble && evt.stopPropagation && evt.stopPropagation();
        return completedFired = true;
      }
      function changed() {
        newIndex = index(dragEl);
        newDraggableIndex = index(dragEl, options.draggable);
        _dispatchEvent({
          sortable: _this,
          name: "change",
          toEl: el,
          newIndex,
          newDraggableIndex,
          originalEvent: evt
        });
      }
      if (evt.preventDefault !== void 0) {
        evt.cancelable && evt.preventDefault();
      }
      target = closest(target, options.draggable, el, true);
      dragOverEvent("dragOver");
      if (Sortable.eventCanceled) return completedFired;
      if (dragEl.contains(evt.target) || target.animated && target.animatingX && target.animatingY || _this._ignoreWhileAnimating === target) {
        return completed(false);
      }
      ignoreNextClick = false;
      if (activeSortable && !options.disabled && (isOwner ? canSort || (revert = parentEl !== rootEl) : putSortable === this || (this.lastPutMode = activeGroup.checkPull(this, activeSortable, dragEl, evt)) && group.checkPut(this, activeSortable, dragEl, evt))) {
        vertical = this._getDirection(evt, target) === "vertical";
        dragRect = getRect(dragEl);
        dragOverEvent("dragOverValid");
        if (Sortable.eventCanceled) return completedFired;
        if (revert) {
          parentEl = rootEl;
          capture();
          this._hideClone();
          dragOverEvent("revert");
          if (!Sortable.eventCanceled) {
            if (nextEl) {
              rootEl.insertBefore(dragEl, nextEl);
            } else {
              rootEl.appendChild(dragEl);
            }
          }
          return completed(true);
        }
        var elLastChild = lastChild(el, options.draggable);
        if (!elLastChild || _ghostIsLast(evt, vertical, this) && !elLastChild.animated) {
          if (elLastChild === dragEl) {
            return completed(false);
          }
          if (elLastChild && el === evt.target) {
            target = elLastChild;
          }
          if (target) {
            targetRect = getRect(target);
          }
          if (_onMove(rootEl, el, dragEl, dragRect, target, targetRect, evt, !!target) !== false) {
            capture();
            if (elLastChild && elLastChild.nextSibling) {
              el.insertBefore(dragEl, elLastChild.nextSibling);
            } else {
              el.appendChild(dragEl);
            }
            parentEl = el;
            changed();
            return completed(true);
          }
        } else if (elLastChild && _ghostIsFirst(evt, vertical, this)) {
          var firstChild = getChild(el, 0, options, true);
          if (firstChild === dragEl) {
            return completed(false);
          }
          target = firstChild;
          targetRect = getRect(target);
          if (_onMove(rootEl, el, dragEl, dragRect, target, targetRect, evt, false) !== false) {
            capture();
            el.insertBefore(dragEl, firstChild);
            parentEl = el;
            changed();
            return completed(true);
          }
        } else if (target.parentNode === el) {
          targetRect = getRect(target);
          var direction = 0, targetBeforeFirstSwap, differentLevel = dragEl.parentNode !== el, differentRowCol = !_dragElInRowColumn(dragEl.animated && dragEl.toRect || dragRect, target.animated && target.toRect || targetRect, vertical), side1 = vertical ? "top" : "left", scrolledPastTop = isScrolledPast(target, "top", "top") || isScrolledPast(dragEl, "top", "top"), scrollBefore = scrolledPastTop ? scrolledPastTop.scrollTop : void 0;
          if (lastTarget !== target) {
            targetBeforeFirstSwap = targetRect[side1];
            pastFirstInvertThresh = false;
            isCircumstantialInvert = !differentRowCol && options.invertSwap || differentLevel;
          }
          direction = _getSwapDirection(evt, target, targetRect, vertical, differentRowCol ? 1 : options.swapThreshold, options.invertedSwapThreshold == null ? options.swapThreshold : options.invertedSwapThreshold, isCircumstantialInvert, lastTarget === target);
          var sibling;
          if (direction !== 0) {
            var dragIndex = index(dragEl);
            do {
              dragIndex -= direction;
              sibling = parentEl.children[dragIndex];
            } while (sibling && (css(sibling, "display") === "none" || sibling === ghostEl));
          }
          if (direction === 0 || sibling === target) {
            return completed(false);
          }
          lastTarget = target;
          lastDirection = direction;
          var nextSibling = target.nextElementSibling, after = false;
          after = direction === 1;
          var moveVector = _onMove(rootEl, el, dragEl, dragRect, target, targetRect, evt, after);
          if (moveVector !== false) {
            if (moveVector === 1 || moveVector === -1) {
              after = moveVector === 1;
            }
            _silent = true;
            setTimeout(_unsilent, 30);
            capture();
            if (after && !nextSibling) {
              el.appendChild(dragEl);
            } else {
              target.parentNode.insertBefore(dragEl, after ? nextSibling : target);
            }
            if (scrolledPastTop) {
              scrollBy(scrolledPastTop, 0, scrollBefore - scrolledPastTop.scrollTop);
            }
            parentEl = dragEl.parentNode;
            if (targetBeforeFirstSwap !== void 0 && !isCircumstantialInvert) {
              targetMoveDistance = Math.abs(targetBeforeFirstSwap - getRect(target)[side1]);
            }
            changed();
            return completed(true);
          }
        }
        if (el.contains(dragEl)) {
          return completed(false);
        }
      }
      return false;
    },
    _ignoreWhileAnimating: null,
    _offMoveEvents: function _offMoveEvents() {
      off(document, "mousemove", this._onTouchMove);
      off(document, "touchmove", this._onTouchMove);
      off(document, "pointermove", this._onTouchMove);
      off(document, "dragover", nearestEmptyInsertDetectEvent);
      off(document, "mousemove", nearestEmptyInsertDetectEvent);
      off(document, "touchmove", nearestEmptyInsertDetectEvent);
    },
    _offUpEvents: function _offUpEvents() {
      var ownerDocument = this.el.ownerDocument;
      off(ownerDocument, "mouseup", this._onDrop);
      off(ownerDocument, "touchend", this._onDrop);
      off(ownerDocument, "pointerup", this._onDrop);
      off(ownerDocument, "pointercancel", this._onDrop);
      off(ownerDocument, "touchcancel", this._onDrop);
      off(document, "selectstart", this);
    },
    _onDrop: function _onDrop(evt) {
      var el = this.el, options = this.options;
      newIndex = index(dragEl);
      newDraggableIndex = index(dragEl, options.draggable);
      pluginEvent2("drop", this, {
        evt
      });
      parentEl = dragEl && dragEl.parentNode;
      newIndex = index(dragEl);
      newDraggableIndex = index(dragEl, options.draggable);
      if (Sortable.eventCanceled) {
        this._nulling();
        return;
      }
      awaitingDragStarted = false;
      isCircumstantialInvert = false;
      pastFirstInvertThresh = false;
      clearInterval(this._loopId);
      clearTimeout(this._dragStartTimer);
      _cancelNextTick(this.cloneId);
      _cancelNextTick(this._dragStartId);
      if (this.nativeDraggable) {
        off(document, "drop", this);
        off(el, "dragstart", this._onDragStart);
      }
      this._offMoveEvents();
      this._offUpEvents();
      if (Safari) {
        css(document.body, "user-select", "");
      }
      css(dragEl, "transform", "");
      if (evt) {
        if (moved) {
          evt.cancelable && evt.preventDefault();
          !options.dropBubble && evt.stopPropagation();
        }
        ghostEl && ghostEl.parentNode && ghostEl.parentNode.removeChild(ghostEl);
        if (rootEl === parentEl || putSortable && putSortable.lastPutMode !== "clone") {
          cloneEl && cloneEl.parentNode && cloneEl.parentNode.removeChild(cloneEl);
        }
        if (dragEl) {
          if (this.nativeDraggable) {
            off(dragEl, "dragend", this);
          }
          _disableDraggable(dragEl);
          dragEl.style["will-change"] = "";
          if (moved && !awaitingDragStarted) {
            toggleClass(dragEl, putSortable ? putSortable.options.ghostClass : this.options.ghostClass, false);
          }
          toggleClass(dragEl, this.options.chosenClass, false);
          _dispatchEvent({
            sortable: this,
            name: "unchoose",
            toEl: parentEl,
            newIndex: null,
            newDraggableIndex: null,
            originalEvent: evt
          });
          if (rootEl !== parentEl) {
            if (newIndex >= 0) {
              _dispatchEvent({
                rootEl: parentEl,
                name: "add",
                toEl: parentEl,
                fromEl: rootEl,
                originalEvent: evt
              });
              _dispatchEvent({
                sortable: this,
                name: "remove",
                toEl: parentEl,
                originalEvent: evt
              });
              _dispatchEvent({
                rootEl: parentEl,
                name: "sort",
                toEl: parentEl,
                fromEl: rootEl,
                originalEvent: evt
              });
              _dispatchEvent({
                sortable: this,
                name: "sort",
                toEl: parentEl,
                originalEvent: evt
              });
            }
            putSortable && putSortable.save();
          } else {
            if (newIndex !== oldIndex) {
              if (newIndex >= 0) {
                _dispatchEvent({
                  sortable: this,
                  name: "update",
                  toEl: parentEl,
                  originalEvent: evt
                });
                _dispatchEvent({
                  sortable: this,
                  name: "sort",
                  toEl: parentEl,
                  originalEvent: evt
                });
              }
            }
          }
          if (Sortable.active) {
            if (newIndex == null || newIndex === -1) {
              newIndex = oldIndex;
              newDraggableIndex = oldDraggableIndex;
            }
            _dispatchEvent({
              sortable: this,
              name: "end",
              toEl: parentEl,
              originalEvent: evt
            });
            this.save();
          }
        }
      }
      this._nulling();
    },
    _nulling: function _nulling() {
      pluginEvent2("nulling", this);
      rootEl = dragEl = parentEl = ghostEl = nextEl = cloneEl = lastDownEl = cloneHidden = tapEvt = touchEvt = moved = newIndex = newDraggableIndex = oldIndex = oldDraggableIndex = lastTarget = lastDirection = putSortable = activeGroup = Sortable.dragged = Sortable.ghost = Sortable.clone = Sortable.active = null;
      var el = this.el;
      savedInputChecked.forEach(function(checkEl) {
        if (el.contains(checkEl)) {
          checkEl.checked = true;
        }
      });
      savedInputChecked.length = lastDx = lastDy = 0;
    },
    handleEvent: function handleEvent(evt) {
      switch (evt.type) {
        case "drop":
        case "dragend":
          this._onDrop(evt);
          break;
        case "dragenter":
        case "dragover":
          if (dragEl) {
            this._onDragOver(evt);
            _globalDragOver(evt);
          }
          break;
        case "selectstart":
          evt.preventDefault();
          break;
      }
    },
    /**
     * Serializes the item into an array of string.
     * @returns {String[]}
     */
    toArray: function toArray() {
      var order = [], el, children = this.el.children, i = 0, n = children.length, options = this.options;
      for (; i < n; i++) {
        el = children[i];
        if (closest(el, options.draggable, this.el, false)) {
          order.push(el.getAttribute(options.dataIdAttr) || _generateId(el));
        }
      }
      return order;
    },
    /**
     * Sorts the elements according to the array.
     * @param  {String[]}  order  order of the items
     */
    sort: function sort(order, useAnimation) {
      var items = {}, rootEl2 = this.el;
      this.toArray().forEach(function(id, i) {
        var el = rootEl2.children[i];
        if (closest(el, this.options.draggable, rootEl2, false)) {
          items[id] = el;
        }
      }, this);
      useAnimation && this.captureAnimationState();
      order.forEach(function(id) {
        if (items[id]) {
          rootEl2.removeChild(items[id]);
          rootEl2.appendChild(items[id]);
        }
      });
      useAnimation && this.animateAll();
    },
    /**
     * Save the current sorting
     */
    save: function save() {
      var store = this.options.store;
      store && store.set && store.set(this);
    },
    /**
     * For each element in the set, get the first element that matches the selector by testing the element itself and traversing up through its ancestors in the DOM tree.
     * @param   {HTMLElement}  el
     * @param   {String}       [selector]  default: `options.draggable`
     * @returns {HTMLElement|null}
     */
    closest: function closest$1(el, selector) {
      return closest(el, selector || this.options.draggable, this.el, false);
    },
    /**
     * Set/get option
     * @param   {string} name
     * @param   {*}      [value]
     * @returns {*}
     */
    option: function option(name, value) {
      var options = this.options;
      if (value === void 0) {
        return options[name];
      } else {
        var modifiedValue = PluginManager.modifyOption(this, name, value);
        if (typeof modifiedValue !== "undefined") {
          options[name] = modifiedValue;
        } else {
          options[name] = value;
        }
        if (name === "group") {
          _prepareGroup(options);
        }
      }
    },
    /**
     * Destroy
     */
    destroy: function destroy2() {
      pluginEvent2("destroy", this);
      var el = this.el;
      el[expando] = null;
      off(el, "mousedown", this._onTapStart);
      off(el, "touchstart", this._onTapStart);
      off(el, "pointerdown", this._onTapStart);
      if (this.nativeDraggable) {
        off(el, "dragover", this);
        off(el, "dragenter", this);
      }
      Array.prototype.forEach.call(el.querySelectorAll("[draggable]"), function(el2) {
        el2.removeAttribute("draggable");
      });
      this._onDrop();
      this._disableDelayedDragEvents();
      sortables.splice(sortables.indexOf(this.el), 1);
      this.el = el = null;
    },
    _hideClone: function _hideClone() {
      if (!cloneHidden) {
        pluginEvent2("hideClone", this);
        if (Sortable.eventCanceled) return;
        css(cloneEl, "display", "none");
        if (this.options.removeCloneOnHide && cloneEl.parentNode) {
          cloneEl.parentNode.removeChild(cloneEl);
        }
        cloneHidden = true;
      }
    },
    _showClone: function _showClone(putSortable2) {
      if (putSortable2.lastPutMode !== "clone") {
        this._hideClone();
        return;
      }
      if (cloneHidden) {
        pluginEvent2("showClone", this);
        if (Sortable.eventCanceled) return;
        if (dragEl.parentNode == rootEl && !this.options.group.revertClone) {
          rootEl.insertBefore(cloneEl, dragEl);
        } else if (nextEl) {
          rootEl.insertBefore(cloneEl, nextEl);
        } else {
          rootEl.appendChild(cloneEl);
        }
        if (this.options.group.revertClone) {
          this.animate(dragEl, cloneEl);
        }
        css(cloneEl, "display", "");
        cloneHidden = false;
      }
    }
  };
  function _globalDragOver(evt) {
    if (evt.dataTransfer) {
      evt.dataTransfer.dropEffect = "move";
    }
    evt.cancelable && evt.preventDefault();
  }
  function _onMove(fromEl, toEl, dragEl2, dragRect, targetEl, targetRect, originalEvent, willInsertAfter) {
    var evt, sortable = fromEl[expando], onMoveFn = sortable.options.onMove, retVal;
    if (window.CustomEvent && !IE11OrLess && !Edge) {
      evt = new CustomEvent("move", {
        bubbles: true,
        cancelable: true
      });
    } else {
      evt = document.createEvent("Event");
      evt.initEvent("move", true, true);
    }
    evt.to = toEl;
    evt.from = fromEl;
    evt.dragged = dragEl2;
    evt.draggedRect = dragRect;
    evt.related = targetEl || toEl;
    evt.relatedRect = targetRect || getRect(toEl);
    evt.willInsertAfter = willInsertAfter;
    evt.originalEvent = originalEvent;
    fromEl.dispatchEvent(evt);
    if (onMoveFn) {
      retVal = onMoveFn.call(sortable, evt, originalEvent);
    }
    return retVal;
  }
  function _disableDraggable(el) {
    el.draggable = false;
  }
  function _unsilent() {
    _silent = false;
  }
  function _ghostIsFirst(evt, vertical, sortable) {
    var firstElRect = getRect(getChild(sortable.el, 0, sortable.options, true));
    var childContainingRect = getChildContainingRectFromElement(sortable.el, sortable.options, ghostEl);
    var spacer = 10;
    return vertical ? evt.clientX < childContainingRect.left - spacer || evt.clientY < firstElRect.top && evt.clientX < firstElRect.right : evt.clientY < childContainingRect.top - spacer || evt.clientY < firstElRect.bottom && evt.clientX < firstElRect.left;
  }
  function _ghostIsLast(evt, vertical, sortable) {
    var lastElRect = getRect(lastChild(sortable.el, sortable.options.draggable));
    var childContainingRect = getChildContainingRectFromElement(sortable.el, sortable.options, ghostEl);
    var spacer = 10;
    return vertical ? evt.clientX > childContainingRect.right + spacer || evt.clientY > lastElRect.bottom && evt.clientX > lastElRect.left : evt.clientY > childContainingRect.bottom + spacer || evt.clientX > lastElRect.right && evt.clientY > lastElRect.top;
  }
  function _getSwapDirection(evt, target, targetRect, vertical, swapThreshold, invertedSwapThreshold, invertSwap, isLastTarget) {
    var mouseOnAxis = vertical ? evt.clientY : evt.clientX, targetLength = vertical ? targetRect.height : targetRect.width, targetS1 = vertical ? targetRect.top : targetRect.left, targetS2 = vertical ? targetRect.bottom : targetRect.right, invert = false;
    if (!invertSwap) {
      if (isLastTarget && targetMoveDistance < targetLength * swapThreshold) {
        if (!pastFirstInvertThresh && (lastDirection === 1 ? mouseOnAxis > targetS1 + targetLength * invertedSwapThreshold / 2 : mouseOnAxis < targetS2 - targetLength * invertedSwapThreshold / 2)) {
          pastFirstInvertThresh = true;
        }
        if (!pastFirstInvertThresh) {
          if (lastDirection === 1 ? mouseOnAxis < targetS1 + targetMoveDistance : mouseOnAxis > targetS2 - targetMoveDistance) {
            return -lastDirection;
          }
        } else {
          invert = true;
        }
      } else {
        if (mouseOnAxis > targetS1 + targetLength * (1 - swapThreshold) / 2 && mouseOnAxis < targetS2 - targetLength * (1 - swapThreshold) / 2) {
          return _getInsertDirection(target);
        }
      }
    }
    invert = invert || invertSwap;
    if (invert) {
      if (mouseOnAxis < targetS1 + targetLength * invertedSwapThreshold / 2 || mouseOnAxis > targetS2 - targetLength * invertedSwapThreshold / 2) {
        return mouseOnAxis > targetS1 + targetLength / 2 ? 1 : -1;
      }
    }
    return 0;
  }
  function _getInsertDirection(target) {
    if (index(dragEl) < index(target)) {
      return 1;
    } else {
      return -1;
    }
  }
  function _generateId(el) {
    var str = el.tagName + el.className + el.src + el.href + el.textContent, i = str.length, sum = 0;
    while (i--) {
      sum += str.charCodeAt(i);
    }
    return sum.toString(36);
  }
  function _saveInputCheckedState(root) {
    savedInputChecked.length = 0;
    var inputs = root.getElementsByTagName("input");
    var idx = inputs.length;
    while (idx--) {
      var el = inputs[idx];
      el.checked && savedInputChecked.push(el);
    }
  }
  function _nextTick(fn) {
    return setTimeout(fn, 0);
  }
  function _cancelNextTick(id) {
    return clearTimeout(id);
  }
  if (documentExists) {
    on(document, "touchmove", function(evt) {
      if ((Sortable.active || awaitingDragStarted) && evt.cancelable) {
        evt.preventDefault();
      }
    });
  }
  Sortable.utils = {
    on,
    off,
    css,
    find,
    is: function is(el, selector) {
      return !!closest(el, selector, el, false);
    },
    extend: extend2,
    throttle,
    closest,
    toggleClass,
    clone,
    index,
    nextTick: _nextTick,
    cancelNextTick: _cancelNextTick,
    detectDirection: _detectDirection,
    getChild,
    expando
  };
  Sortable.get = function(element) {
    return element[expando];
  };
  Sortable.mount = function() {
    for (var _len = arguments.length, plugins2 = new Array(_len), _key = 0; _key < _len; _key++) {
      plugins2[_key] = arguments[_key];
    }
    if (plugins2[0].constructor === Array) plugins2 = plugins2[0];
    plugins2.forEach(function(plugin) {
      if (!plugin.prototype || !plugin.prototype.constructor) {
        throw "Sortable: Mounted plugin must be a constructor function, not ".concat({}.toString.call(plugin));
      }
      if (plugin.utils) Sortable.utils = _objectSpread2(_objectSpread2({}, Sortable.utils), plugin.utils);
      PluginManager.mount(plugin);
    });
  };
  Sortable.create = function(el, options) {
    return new Sortable(el, options);
  };
  Sortable.version = version;
  var autoScrolls = [];
  var scrollEl;
  var scrollRootEl;
  var scrolling = false;
  var touchEvt$1;
  function clearAutoScrolls() {
    autoScrolls.forEach(function(autoScroll2) {
      clearInterval(autoScroll2.pid);
    });
    autoScrolls = [];
  }
  var autoScroll = throttle(function(evt, options, rootEl2, isFallback) {
    if (!options.scroll) return;
    var x2 = (evt.touches ? evt.touches[0] : evt).clientX, y2 = (evt.touches ? evt.touches[0] : evt).clientY, sens = options.scrollSensitivity, speed = options.scrollSpeed, winScroller = getWindowScrollingElement();
    var scrollThisInstance = false, scrollCustomFn;
    if (scrollRootEl !== rootEl2) {
      scrollRootEl = rootEl2;
      clearAutoScrolls();
      scrollEl = options.scroll;
      scrollCustomFn = options.scrollFn;
      if (scrollEl === true) {
        scrollEl = getParentAutoScrollElement(rootEl2, true);
      }
    }
    var layersOut = 0;
    var currentParent = scrollEl;
    do {
      var el = currentParent, rect = getRect(el), top = rect.top, bottom = rect.bottom, left = rect.left, right = rect.right, width = rect.width, height = rect.height, canScrollX = void 0, canScrollY = void 0, scrollWidth = el.scrollWidth, scrollHeight = el.scrollHeight, elCSS = css(el), scrollPosX = el.scrollLeft, scrollPosY = el.scrollTop;
      if (el === winScroller) {
        canScrollX = width < scrollWidth && (elCSS.overflowX === "auto" || elCSS.overflowX === "scroll" || elCSS.overflowX === "visible");
        canScrollY = height < scrollHeight && (elCSS.overflowY === "auto" || elCSS.overflowY === "scroll" || elCSS.overflowY === "visible");
      } else {
        canScrollX = width < scrollWidth && (elCSS.overflowX === "auto" || elCSS.overflowX === "scroll");
        canScrollY = height < scrollHeight && (elCSS.overflowY === "auto" || elCSS.overflowY === "scroll");
      }
      var vx = canScrollX && (Math.abs(right - x2) <= sens && scrollPosX + width < scrollWidth) - (Math.abs(left - x2) <= sens && !!scrollPosX);
      var vy = canScrollY && (Math.abs(bottom - y2) <= sens && scrollPosY + height < scrollHeight) - (Math.abs(top - y2) <= sens && !!scrollPosY);
      if (!autoScrolls[layersOut]) {
        for (var i = 0; i <= layersOut; i++) {
          if (!autoScrolls[i]) {
            autoScrolls[i] = {};
          }
        }
      }
      if (autoScrolls[layersOut].vx != vx || autoScrolls[layersOut].vy != vy || autoScrolls[layersOut].el !== el) {
        autoScrolls[layersOut].el = el;
        autoScrolls[layersOut].vx = vx;
        autoScrolls[layersOut].vy = vy;
        clearInterval(autoScrolls[layersOut].pid);
        if (vx != 0 || vy != 0) {
          scrollThisInstance = true;
          autoScrolls[layersOut].pid = setInterval(function() {
            if (isFallback && this.layer === 0) {
              Sortable.active._onTouchMove(touchEvt$1);
            }
            var scrollOffsetY = autoScrolls[this.layer].vy ? autoScrolls[this.layer].vy * speed : 0;
            var scrollOffsetX = autoScrolls[this.layer].vx ? autoScrolls[this.layer].vx * speed : 0;
            if (typeof scrollCustomFn === "function") {
              if (scrollCustomFn.call(Sortable.dragged.parentNode[expando], scrollOffsetX, scrollOffsetY, evt, touchEvt$1, autoScrolls[this.layer].el) !== "continue") {
                return;
              }
            }
            scrollBy(autoScrolls[this.layer].el, scrollOffsetX, scrollOffsetY);
          }.bind({
            layer: layersOut
          }), 24);
        }
      }
      layersOut++;
    } while (options.bubbleScroll && currentParent !== winScroller && (currentParent = getParentAutoScrollElement(currentParent, false)));
    scrolling = scrollThisInstance;
  }, 30);
  var drop = function drop2(_ref) {
    var originalEvent = _ref.originalEvent, putSortable2 = _ref.putSortable, dragEl2 = _ref.dragEl, activeSortable = _ref.activeSortable, dispatchSortableEvent = _ref.dispatchSortableEvent, hideGhostForTarget = _ref.hideGhostForTarget, unhideGhostForTarget = _ref.unhideGhostForTarget;
    if (!originalEvent) return;
    var toSortable = putSortable2 || activeSortable;
    hideGhostForTarget();
    var touch = originalEvent.changedTouches && originalEvent.changedTouches.length ? originalEvent.changedTouches[0] : originalEvent;
    var target = document.elementFromPoint(touch.clientX, touch.clientY);
    unhideGhostForTarget();
    if (toSortable && !toSortable.el.contains(target)) {
      dispatchSortableEvent("spill");
      this.onSpill({
        dragEl: dragEl2,
        putSortable: putSortable2
      });
    }
  };
  function Revert() {
  }
  Revert.prototype = {
    startIndex: null,
    dragStart: function dragStart(_ref2) {
      var oldDraggableIndex2 = _ref2.oldDraggableIndex;
      this.startIndex = oldDraggableIndex2;
    },
    onSpill: function onSpill(_ref3) {
      var dragEl2 = _ref3.dragEl, putSortable2 = _ref3.putSortable;
      this.sortable.captureAnimationState();
      if (putSortable2) {
        putSortable2.captureAnimationState();
      }
      var nextSibling = getChild(this.sortable.el, this.startIndex, this.options);
      if (nextSibling) {
        this.sortable.el.insertBefore(dragEl2, nextSibling);
      } else {
        this.sortable.el.appendChild(dragEl2);
      }
      this.sortable.animateAll();
      if (putSortable2) {
        putSortable2.animateAll();
      }
    },
    drop
  };
  _extends(Revert, {
    pluginName: "revertOnSpill"
  });
  function Remove() {
  }
  Remove.prototype = {
    onSpill: function onSpill2(_ref4) {
      var dragEl2 = _ref4.dragEl, putSortable2 = _ref4.putSortable;
      var parentSortable = putSortable2 || this.sortable;
      parentSortable.captureAnimationState();
      dragEl2.parentNode && dragEl2.parentNode.removeChild(dragEl2);
      parentSortable.animateAll();
    },
    drop
  };
  _extends(Remove, {
    pluginName: "removeOnSpill"
  });
  var sortable_core_esm_default = Sortable;

  // node_modules/@slickgrid-universal/common/dist/core/slickInteractions.js
  function Draggable(options) {
    let { containerElement } = options;
    const { onDragInit, onDragStart, onDrag, onDragEnd, preventDragFromKeys } = options;
    let element;
    let startX;
    let startY;
    let deltaX;
    let deltaY;
    let dragStarted;
    let matchClassTag;
    if (!containerElement) {
      containerElement = document.body;
    }
    let originaldd = {
      dragSource: containerElement,
      dragHandle: null
    };
    function init2() {
      if (containerElement) {
        containerElement.addEventListener("mousedown", userPressed);
        containerElement.addEventListener("touchstart", userPressed);
      }
    }
    function executeDragCallbackWhenDefined(callback, evt, dd) {
      if (typeof callback === "function") {
        return callback(evt, dd);
      }
    }
    function destroy3() {
      if (containerElement) {
        containerElement.removeEventListener("mousedown", userPressed);
        containerElement.removeEventListener("touchstart", userPressed);
      }
    }
    function preventDrag(event) {
      let eventPrevented = false;
      if (preventDragFromKeys) {
        preventDragFromKeys.forEach((key) => {
          if (event[key]) {
            eventPrevented = true;
          }
        });
      }
      return eventPrevented;
    }
    function userPressed(event) {
      element = event.target;
      if (!preventDrag(event)) {
        const targetEvent = event?.touches?.[0] ?? event;
        const { target } = targetEvent;
        if (!options.allowDragFrom || options.allowDragFrom && element.matches(options.allowDragFrom) || options.allowDragFromClosest && element.closest(options.allowDragFromClosest)) {
          originaldd.dragHandle = element;
          matchClassTag = "";
          if (options.dragFromClassDetectArr) {
            for (let o, i = 0; i < options.dragFromClassDetectArr.length; i++) {
              o = options.dragFromClassDetectArr[i];
              if (o.id && element.id === o.id || o.cssSelector && element.matches(o.cssSelector)) {
                matchClassTag = o.tag;
                break;
              }
            }
          }
          const winScrollPos = windowScrollPosition();
          startX = winScrollPos.left + targetEvent.clientX;
          startY = winScrollPos.top + targetEvent.clientY;
          deltaX = targetEvent.clientX - targetEvent.clientX;
          deltaY = targetEvent.clientY - targetEvent.clientY;
          originaldd = Object.assign(originaldd, { deltaX, deltaY, startX, startY, target, matchClassTag });
          const result = executeDragCallbackWhenDefined(onDragInit, event, originaldd);
          if (result !== false) {
            document.body.addEventListener("mousemove", userMoved);
            document.body.addEventListener("touchmove", userMoved);
            window.addEventListener("mouseup", userReleased);
            window.addEventListener("touchend", userReleased);
            window.addEventListener("touchcancel", userReleased);
          }
        }
      }
    }
    function userMoved(event) {
      const targetEvent = event?.touches?.[0] ?? event;
      if (!preventDrag(event)) {
        deltaX = targetEvent.clientX - startX;
        deltaY = targetEvent.clientY - startY;
        const { target } = targetEvent;
        if (!dragStarted) {
          originaldd = Object.assign(originaldd, { deltaX, deltaY, startX, startY, target, matchClassTag });
          executeDragCallbackWhenDefined(onDragStart, event, originaldd);
          dragStarted = true;
        }
        originaldd = Object.assign(originaldd, { deltaX, deltaY, startX, startY, target, matchClassTag });
        executeDragCallbackWhenDefined(onDrag, event, originaldd);
      }
    }
    function userReleased(event) {
      document.body.removeEventListener("mousemove", userMoved);
      document.body.removeEventListener("touchmove", userMoved);
      window.removeEventListener("mouseup", userReleased);
      window.removeEventListener("touchend", userReleased);
      window.removeEventListener("touchcancel", userReleased);
      if (dragStarted) {
        const { target } = event;
        originaldd = Object.assign(originaldd, { target });
        executeDragCallbackWhenDefined(onDragEnd, event, originaldd);
        dragStarted = false;
      }
    }
    init2();
    return { destroy: destroy3 };
  }
  function MouseWheel(options) {
    const { element, onMouseWheel } = options;
    function destroy3() {
      element.removeEventListener("wheel", wheelHandler);
      element.removeEventListener("mousewheel", wheelHandler);
    }
    function init2() {
      element.addEventListener("wheel", wheelHandler);
      element.addEventListener("mousewheel", wheelHandler);
    }
    function wheelHandler(event) {
      const orgEvent = event || window.event;
      let delta = 0;
      let deltaX = 0;
      let deltaY = 0;
      if (orgEvent.wheelDelta) {
        delta = orgEvent.wheelDelta / 120;
      }
      if (orgEvent.detail) {
        delta = -orgEvent.detail / 3;
      }
      deltaY = delta;
      if (orgEvent.axis !== void 0 && orgEvent.axis === orgEvent.HORIZONTAL_AXIS) {
        deltaY = 0;
        deltaX = -1 * delta;
      }
      if (orgEvent.wheelDeltaY !== void 0) {
        deltaY = orgEvent.wheelDeltaY / 120;
      }
      if (orgEvent.wheelDeltaX !== void 0) {
        deltaX = -1 * orgEvent.wheelDeltaX / 120;
      }
      if (typeof onMouseWheel === "function") {
        onMouseWheel(event, delta, deltaX, deltaY);
      }
    }
    init2();
    return { destroy: destroy3 };
  }
  function Resizable(options) {
    const { resizeableElement, resizeableHandleElement, onResizeStart, onResize, onResizeEnd } = options;
    const supportsPointerEvents = typeof window !== "undefined" && "PointerEvent" in window;
    let activePointerId;
    if (!resizeableHandleElement || typeof resizeableHandleElement.addEventListener !== "function") {
      throw new Error("[SlickResizable] You did not provide a valid html element that will be used for the handle to resize.");
    }
    function init2() {
      if (supportsPointerEvents) {
        resizeableHandleElement.addEventListener("pointerdown", pointerResizeStartHandler);
      }
      resizeableHandleElement.addEventListener("mousedown", resizeStartHandler);
      resizeableHandleElement.addEventListener("touchstart", resizeStartHandler);
    }
    function destroy3() {
      if (typeof resizeableHandleElement?.removeEventListener === "function") {
        if (supportsPointerEvents) {
          resizeableHandleElement.removeEventListener("pointerdown", pointerResizeStartHandler);
          removePointerListeners();
        }
        resizeableHandleElement.removeEventListener("mousedown", resizeStartHandler);
        resizeableHandleElement.removeEventListener("touchstart", resizeStartHandler);
      }
    }
    function addPointerListeners() {
      resizeableHandleElement.addEventListener("pointermove", pointerResizingHandler);
      resizeableHandleElement.addEventListener("pointerup", pointerResizeEndHandler);
      resizeableHandleElement.addEventListener("pointercancel", pointerResizeEndHandler);
      resizeableHandleElement.addEventListener("lostpointercapture", pointerResizeEndHandler);
    }
    function removePointerListeners() {
      resizeableHandleElement.removeEventListener("pointermove", pointerResizingHandler);
      resizeableHandleElement.removeEventListener("pointerup", pointerResizeEndHandler);
      resizeableHandleElement.removeEventListener("pointercancel", pointerResizeEndHandler);
      resizeableHandleElement.removeEventListener("lostpointercapture", pointerResizeEndHandler);
    }
    function executeResizeCallbackWhenDefined(callback, e2) {
      if (typeof callback === "function") {
        return callback(e2, { resizeableElement, resizeableHandleElement });
      }
    }
    function resizeStartHandler(e2) {
      if (supportsPointerEvents && activePointerId !== void 0) {
        return;
      }
      e2.preventDefault();
      const event = e2.touches ? e2.changedTouches[0] : e2;
      const result = executeResizeCallbackWhenDefined(onResizeStart, event);
      if (result !== false) {
        document.body.addEventListener("mousemove", resizingHandler);
        document.body.addEventListener("mouseup", resizeEndHandler);
        document.body.addEventListener("touchmove", resizingHandler);
        document.body.addEventListener("touchend", resizeEndHandler);
      }
    }
    function pointerResizeStartHandler(e2) {
      if (e2.pointerType === "mouse" && e2.button !== 0) {
        return;
      }
      e2.preventDefault();
      activePointerId = e2.pointerId;
      if (typeof resizeableHandleElement.setPointerCapture === "function") {
        resizeableHandleElement.setPointerCapture(e2.pointerId);
      }
      const result = executeResizeCallbackWhenDefined(onResizeStart, e2);
      if (result !== false) {
        addPointerListeners();
      } else if (typeof resizeableHandleElement.releasePointerCapture === "function") {
        resizeableHandleElement.releasePointerCapture(e2.pointerId);
        activePointerId = void 0;
      }
    }
    function resizingHandler(e2) {
      if (e2.preventDefault && e2.type !== "touchmove") {
        e2.preventDefault();
      }
      const event = e2.touches ? e2.changedTouches[0] : e2;
      if (typeof onResize === "function") {
        onResize(event, { resizeableElement, resizeableHandleElement });
      }
    }
    function pointerResizingHandler(e2) {
      if (activePointerId !== void 0 && e2.pointerId !== activePointerId) {
        return;
      }
      if (e2.preventDefault && e2.pointerType !== "touch") {
        e2.preventDefault();
      }
      if (typeof onResize === "function") {
        onResize(e2, { resizeableElement, resizeableHandleElement });
      }
    }
    function resizeEndHandler(e2) {
      const event = e2.touches ? e2.changedTouches[0] : e2;
      executeResizeCallbackWhenDefined(onResizeEnd, event);
      document.body.removeEventListener("mousemove", resizingHandler);
      document.body.removeEventListener("mouseup", resizeEndHandler);
      document.body.removeEventListener("touchmove", resizingHandler);
      document.body.removeEventListener("touchend", resizeEndHandler);
    }
    function pointerResizeEndHandler(e2) {
      if (activePointerId !== void 0 && e2.pointerId !== activePointerId) {
        return;
      }
      executeResizeCallbackWhenDefined(onResizeEnd, e2);
      removePointerListeners();
      if (activePointerId !== void 0 && typeof resizeableHandleElement.hasPointerCapture === "function" && resizeableHandleElement.hasPointerCapture(activePointerId) && typeof resizeableHandleElement.releasePointerCapture === "function") {
        resizeableHandleElement.releasePointerCapture(activePointerId);
      }
      activePointerId = void 0;
    }
    init2();
    return { destroy: destroy3 };
  }

  // node_modules/@slickgrid-universal/common/dist/core/utils.js
  function applyHtmlToElement(target, val = "", options) {
    if (target) {
      if (val instanceof HTMLElement || val instanceof DocumentFragment) {
        const emptyTarget = options?.emptyTarget !== false;
        if (emptyTarget) {
          emptyElement(target);
        }
        target.appendChild(val);
      } else {
        const skipEmptyReassignment = options?.skipEmptyReassignment !== false;
        if (skipEmptyReassignment && !isDefined(val) && !target.innerHTML) {
          return;
        }
        if (typeof val === "number" || typeof val === "boolean") {
          target.textContent = String(val);
        } else {
          const sanitizeHtmlString = (dirtyHtml) => {
            if (typeof options?.sanitizer !== "function" || !dirtyHtml || typeof dirtyHtml !== "string") {
              return dirtyHtml;
            }
            return options.sanitizer(dirtyHtml);
          };
          const sanitizedText = sanitizeHtmlString(val);
          if (options?.enableHtmlRendering && sanitizedText) {
            target.innerHTML = sanitizedText;
          } else {
            target.textContent = sanitizedText;
          }
        }
      }
    }
  }
  function runOptionalHtmlSanitizer(dirtyHtml, sanitizer) {
    if (typeof sanitizer !== "function" || !dirtyHtml || typeof dirtyHtml !== "string") {
      return dirtyHtml;
    }
    return sanitizer(dirtyHtml);
  }

  // node_modules/@slickgrid-universal/common/dist/core/slickGrid.js
  var RESIZE_AUTOSCROLL_MIN_INTERVAL_MS = 30;
  var RESIZE_AUTOSCROLL_MAX_INTERVAL_MS = 600;
  var RESIZE_AUTOSCROLL_ACCELERATE_INTERVAL = 5;
  var RESIZE_AUTOSCROLL_BROWSER_EDGE_PX = 1;
  var RESIZE_AUTOSCROLL_BROWSER_EDGE_LEFT_DELAY_MS = 300;
  var RESIZE_AUTOSCROLL_BROWSER_EDGE_RIGHT_DELAY_MS = 1200;
  var SlickGrid = class {
    /**
     * Creates a new instance of the grid.
     * @class SlickGrid
     * @constructor
     * @param {Node} container - Container node to create the grid in.
     * @param {Array|Object} data - An array of objects for databinding or an external DataView.
     * @param {Array<C>} columns - An array of column definitions.
     * @param {Object} [options] - Grid Options
     * @param {Object} [externalPubSub] - optional External PubSub Service to use by SlickEvent
     **/
    constructor(container, data, columns, options, externalPubSub) {
      this.container = container;
      this.data = data;
      this.columns = columns;
      this.externalPubSub = externalPubSub;
      this.canvas = null;
      this.canvas_context = null;
      this._isResizingColumn = false;
      this._lastColumnGridMenuCompensation = 2;
      this._defaults = {
        invalidColumnFreezePickerCallback: (error) => alert(error),
        invalidColumnFreezeWidthCallback: (error) => alert(error),
        invalidColumnFreezeWidthMessage: "[SlickGrid] You are trying to freeze/pin more columns than the grid can support. Make sure to have less columns pinned (on the left) than the actual visible grid width.",
        invalidColumnFreezePickerMessage: '[SlickGrid] Action not allowed and aborted, you need to have at least one or more column on the right section of the column freeze/pining. You could alternatively "Unfreeze all the columns" before trying again.',
        skipFreezeColumnValidation: false,
        allowDragFromClosest: "div.slick-cell.dnd, div.slick-cell.cell-reorder",
        alwaysShowVerticalScroll: false,
        alwaysAllowHorizontalScroll: false,
        explicitInitialization: false,
        rowHeight: 25,
        defaultColumnWidth: 80,
        enableHtmlRendering: true,
        enableAddRow: false,
        leaveSpaceForNewRows: false,
        editable: false,
        autoEdit: true,
        autoEditNewRow: true,
        autoCommitEdit: false,
        suppressActiveCellChangeOnEdit: false,
        enableCellNavigation: true,
        enableColumnReorder: true,
        unorderableColumnCssClass: "unorderable",
        asyncEditorLoading: false,
        asyncEditorLoadDelay: 100,
        forceFitColumns: false,
        autoScrollOnColumnResize: true,
        autoScrollResizeLeftDelay: RESIZE_AUTOSCROLL_BROWSER_EDGE_LEFT_DELAY_MS,
        autoScrollResizeRightDelay: RESIZE_AUTOSCROLL_BROWSER_EDGE_RIGHT_DELAY_MS,
        enableAsyncPostRender: false,
        asyncPostRenderDelay: 50,
        enableAsyncPostRenderCleanup: false,
        asyncPostRenderCleanupDelay: 40,
        columnResizingDelay: 300,
        nonce: "",
        editorLock: SlickGlobalEditorLock,
        showColumnHeader: true,
        showHeaderRow: false,
        headerRowHeight: 25,
        createFooterRow: false,
        showFooterRow: false,
        footerRowHeight: 25,
        createPreHeaderPanel: false,
        createTopHeaderPanel: false,
        showPreHeaderPanel: false,
        showTopHeaderPanel: false,
        preHeaderPanelHeight: 25,
        preHeaderPanelWidth: "auto",
        // mostly useful for Draggable Grouping dropzone to take full width
        topHeaderPanelHeight: 25,
        topHeaderPanelWidth: "auto",
        // mostly useful for Draggable Grouping dropzone to take full width
        showTopPanel: false,
        topPanelHeight: 25,
        formatterFactory: null,
        editorFactory: null,
        cellFlashingCssClass: "flashing",
        rowHighlightCssClass: "highlight-animate",
        rowHighlightDuration: 400,
        selectedCellCssClass: "selected",
        multiSelect: true,
        enableCellRowSpan: false,
        enableTextSelectionOnCells: false,
        dataItemColumnValueExtractor: null,
        frozenBottom: false,
        frozenColumn: -1,
        frozenRow: -1,
        frozenRightViewportMinWidth: 100,
        fullWidthRows: false,
        multiColumnSort: false,
        numberedMultiColumnSort: false,
        tristateMultiColumnSort: false,
        sortColNumberInSeparateSpan: false,
        defaultFormatter: this.defaultFormatter,
        forceSyncScrolling: false,
        addNewRowCssClass: "new-row",
        preserveCopiedSelectionOnPaste: false,
        preventDragFromKeys: ["ctrlKey", "metaKey"],
        showCellSelection: true,
        viewportClass: void 0,
        minRowBuffer: 3,
        emulatePagingWhenScrolling: true,
        // when scrolling off bottom of viewport, place new row at top of viewport
        editorCellNavOnLRKeys: false,
        enableMouseWheelScrollHandler: true,
        doPaging: true,
        rowTopOffsetRenderType: "top",
        scrollRenderThrottling: 10,
        suppressCssChangesOnHiddenInit: false,
        ffMaxSupportedCssHeight: 6e6,
        maxSupportedCssHeight: 1e9,
        maxPartialRowSpanRemap: 5e3,
        sanitizer: void 0,
        // sanitize function
        mixinDefaults: false,
        shadowRoot: void 0
      };
      this._columnDefaults = {
        name: "",
        headerCssClass: null,
        defaultSortAsc: true,
        focusable: true,
        hidden: false,
        minWidth: 30,
        maxWidth: void 0,
        rerenderOnResize: false,
        reorderable: true,
        resizable: true,
        sortable: false,
        selectable: true
      };
      this.page = 0;
      this.offset = 0;
      this.vScrollDir = 1;
      this._bindingEventService = new BindingEventService();
      this.initialized = false;
      this.uid = `slickgrid_${Math.round(1e6 * Math.random())}`;
      this.dragReplaceEl = new SlickDragExtendHandle(this.uid);
      this._groupHeaders = [];
      this._headerScroller = [];
      this._headers = [];
      this._boundAncestors = [];
      this.viewportH = 0;
      this.viewportW = 0;
      this.canvasWidth = 0;
      this.canvasWidthL = 0;
      this.canvasWidthR = 0;
      this.headersWidth = 0;
      this.headersWidthL = 0;
      this.headersWidthR = 0;
      this.viewportHasHScroll = false;
      this.viewportHasVScroll = false;
      this.headerColumnWidthDiff = 0;
      this.headerColumnHeightDiff = 0;
      this.cellWidthDiff = 0;
      this.cellHeightDiff = 0;
      this.hasFrozenRows = false;
      this.frozenRowsHeight = 0;
      this.actualFrozenRow = -1;
      this._prevFrozenColumnIdx = -1;
      this._invalidfrozenAlerted = false;
      this.paneTopH = 0;
      this.paneBottomH = 0;
      this.viewportTopH = 0;
      this.viewportBottomH = 0;
      this.topPanelH = 0;
      this.headerRowH = 0;
      this.footerRowH = 0;
      this.tabbingDirection = 1;
      this.activeCellNode = null;
      this.currentEditor = null;
      this._prevDataLength = 0;
      this._prevInvalidatedRowsCount = 0;
      this._rowSpanIsCached = false;
      this._colsWithRowSpanCache = {};
      this.rowsCache = {};
      this.renderedRows = 0;
      this.numVisibleRows = 0;
      this.prevScrollTop = 0;
      this.scrollHeight = 0;
      this.scrollTop = 0;
      this.lastRenderedScrollTop = 0;
      this.lastRenderedScrollLeft = 0;
      this.prevScrollLeft = 0;
      this.scrollLeft = 0;
      this.selectedRows = [];
      this.selectedRanges = [];
      this.plugins = [];
      this.cellCssClasses = {};
      this.columnsById = {};
      this.visibleColumnsById = {};
      this.sortColumns = [];
      this.columnPosLeft = [];
      this.columnPosRight = [];
      this.pagingActive = false;
      this.pagingIsLastPage = false;
      this.postProcessedRows = {};
      this.postProcessToRow = null;
      this.postProcessFromRow = null;
      this.postProcessedCleanupQueue = [];
      this.postProcessgroupId = 0;
      this.counter_rows_rendered = 0;
      this.counter_rows_removed = 0;
      this.cssShow = { position: "absolute", visibility: "hidden", display: "block" };
      this._hiddenParents = [];
      this.oldProps = [];
      this.enforceFrozenRowHeightRecalc = false;
      this.columnResizeDragging = false;
      this.slickDraggableInstance = null;
      this.slickMouseWheelInstances = [];
      this.slickResizableInstances = [];
      this.formattedDataCachePlanner = (column, gridOptions) => {
        const optionCandidates = [gridOptions.excelExportOptions, gridOptions.textExportOptions, gridOptions.pdfExportOptions];
        const hasExportCustomFormatter = typeof column.exportCustomFormatter === "function";
        const hasColumnExportWithFormatter = !!column.exportWithFormatter;
        let shouldCacheExport = hasColumnExportWithFormatter || hasExportCustomFormatter;
        let useCellFormatterForExport = hasColumnExportWithFormatter;
        let sanitizeDataExport = !!column.sanitizeDataExport;
        for (let i = 0; i < optionCandidates.length; i++) {
          const exportOptions = optionCandidates[i];
          if (!exportOptions) {
            continue;
          }
          const hasExportWithFormatter = column.exportWithFormatter !== void 0 ? !!column.exportWithFormatter : !!exportOptions.exportWithFormatter;
          if (!hasExportWithFormatter && !hasExportCustomFormatter) {
            continue;
          }
          shouldCacheExport = true;
          useCellFormatterForExport = useCellFormatterForExport || hasExportWithFormatter;
          sanitizeDataExport = sanitizeDataExport || !!column.sanitizeDataExport || !!exportOptions.sanitizeDataExport;
        }
        if (!shouldCacheExport) {
          return void 0;
        }
        return {
          shouldCacheExport,
          useCellFormatterForExport,
          sanitizeDataExport,
          exportOptions: {
            exportWithFormatter: useCellFormatterForExport,
            sanitizeDataExport
          }
        };
      };
      this._container = typeof this.container === "string" ? document.querySelector(this.container) : this.container;
      if (!this._container) {
        throw new Error(`SlickGrid requires a valid container, ${this.container} does not exist in the DOM.`);
      }
      this._pubSubService = externalPubSub;
      this.onActiveCellChanged = new SlickEvent("onActiveCellChanged", externalPubSub);
      this.onActiveCellPositionChanged = new SlickEvent("onActiveCellPositionChanged", externalPubSub);
      this.onAddNewRow = new SlickEvent("onAddNewRow", externalPubSub);
      this.onAfterSetColumns = new SlickEvent("onAfterSetColumns", externalPubSub);
      this.onAutosizeColumns = new SlickEvent("onAutosizeColumns", externalPubSub);
      this.onBeforeAppendCell = new SlickEvent("onBeforeAppendCell", externalPubSub);
      this.onBeforeCellEditorDestroy = new SlickEvent("onBeforeCellEditorDestroy", externalPubSub);
      this.onBeforeColumnsResize = new SlickEvent("onBeforeColumnsResize", externalPubSub);
      this.onBeforeDestroy = new SlickEvent("onBeforeDestroy", externalPubSub);
      this.onBeforeEditCell = new SlickEvent("onBeforeEditCell", externalPubSub);
      this.onBeforeFooterRowCellDestroy = new SlickEvent("onBeforeFooterRowCellDestroy", externalPubSub);
      this.onBeforeHeaderCellDestroy = new SlickEvent("onBeforeHeaderCellDestroy", externalPubSub);
      this.onBeforeHeaderRowCellDestroy = new SlickEvent("onBeforeHeaderRowCellDestroy", externalPubSub);
      this.onBeforeRemoveCachedRow = new SlickEvent("onRowRemovedFromCache", externalPubSub);
      this.onBeforeSetColumns = new SlickEvent("onBeforeSetColumns", externalPubSub);
      this.onBeforeSort = new SlickEvent("onBeforeSort", externalPubSub);
      this.onBeforeUpdateColumns = new SlickEvent("onBeforeUpdateColumns", externalPubSub);
      this.onAfterUpdateColumns = new SlickEvent("onBeforeUpdateColumns", externalPubSub);
      this.onCellChange = new SlickEvent("onCellChange", externalPubSub);
      this.onCellCssStylesChanged = new SlickEvent("onCellCssStylesChanged", externalPubSub);
      this.onClick = new SlickEvent("onClick", externalPubSub);
      this.onColumnsReordered = new SlickEvent("onColumnsReordered", externalPubSub);
      this.onColumnsDrag = new SlickEvent("onColumnsDrag", externalPubSub);
      this.onColumnsResized = new SlickEvent("onColumnsResized", externalPubSub);
      this.onColumnsResizeDblClick = new SlickEvent("onColumnsResizeDblClick", externalPubSub);
      this.onCompositeEditorChange = new SlickEvent("onCompositeEditorChange", externalPubSub);
      this.onContextMenu = new SlickEvent("onContextMenu", externalPubSub);
      this.onDblClick = new SlickEvent("onDblClick", externalPubSub);
      this.onDrag = new SlickEvent("onDrag", externalPubSub);
      this.onDragInit = new SlickEvent("onDragInit", externalPubSub);
      this.onDragStart = new SlickEvent("onDragStart", externalPubSub);
      this.onDragEnd = new SlickEvent("onDragEnd", externalPubSub);
      this.onFooterClick = new SlickEvent("onFooterClick", externalPubSub);
      this.onFooterContextMenu = new SlickEvent("onFooterContextMenu", externalPubSub);
      this.onFooterRowCellRendered = new SlickEvent("onFooterRowCellRendered", externalPubSub);
      this.onHeaderCellRendered = new SlickEvent("onHeaderCellRendered", externalPubSub);
      this.onHeaderClick = new SlickEvent("onHeaderClick", externalPubSub);
      this.onHeaderContextMenu = new SlickEvent("onHeaderContextMenu", externalPubSub);
      this.onHeaderMouseEnter = new SlickEvent("onHeaderMouseEnter", externalPubSub);
      this.onHeaderMouseLeave = new SlickEvent("onHeaderMouseLeave", externalPubSub);
      this.onHeaderMouseOver = new SlickEvent("onHeaderMouseOver", externalPubSub);
      this.onHeaderMouseOut = new SlickEvent("onHeaderMouseOut", externalPubSub);
      this.onHeaderRowMouseOver = new SlickEvent("onHeaderRowMouseOver", externalPubSub);
      this.onHeaderRowMouseOut = new SlickEvent("onHeaderRowMouseOut", externalPubSub);
      this.onHeaderKeyDown = new SlickEvent("onHeaderKeyDown", externalPubSub);
      this.onHeaderRowCellRendered = new SlickEvent("onHeaderRowCellRendered", externalPubSub);
      this.onHeaderRowMouseEnter = new SlickEvent("onHeaderRowMouseEnter", externalPubSub);
      this.onHeaderRowMouseLeave = new SlickEvent("onHeaderRowMouseLeave", externalPubSub);
      this.onKeyDown = new SlickEvent("onKeyDown", externalPubSub);
      this.onMouseEnter = new SlickEvent("onMouseEnter", externalPubSub);
      this.onMouseLeave = new SlickEvent("onMouseLeave", externalPubSub);
      this.onPreHeaderClick = new SlickEvent("onPreHeaderClick", externalPubSub);
      this.onPreHeaderContextMenu = new SlickEvent("onPreHeaderContextMenu", externalPubSub);
      this.onRendered = new SlickEvent("onRendered", externalPubSub);
      this.onScroll = new SlickEvent("onScroll", externalPubSub);
      this.onSelectedRowsChanged = new SlickEvent("onSelectedRowsChanged", externalPubSub);
      this.onSetOptions = new SlickEvent("onSetOptions", externalPubSub);
      this.onActivateChangedOptions = new SlickEvent("onActivateChangedOptions", externalPubSub);
      this.onSort = new SlickEvent("onSort", externalPubSub);
      this.onValidationError = new SlickEvent("onValidationError", externalPubSub);
      this.onViewportChanged = new SlickEvent("onViewportChanged", externalPubSub);
      this.onDragReplaceCells = new SlickEvent("onDragReplaceCells", externalPubSub);
      this.initialize(options);
      this.syncDataViewFormattedCachePlanner();
    }
    // Initialization
    /** Initializes the grid. */
    init() {
      if (!this._options.silenceWarnings && document.body.style.zoom && document.body.style.zoom !== "100%") {
        console.warn("[Slickgrid] Zoom level other than 100% is not supported by the library and will give subpar experience. SlickGrid relies on the `rowHeight` grid option to do row positioning & calculation and when zoom is not 100% then calculation becomes all offset.");
      }
      if (this._options.rowTopOffsetRenderType === "transform" && (this._options.enableCellRowSpan || this._options.enableRowDetailView)) {
        console.warn('[Slickgrid-Universal] `rowTopOffsetRenderType` should be set to "top" when using either RowDetail and/or RowSpan since "transform" is known to have UI issues.');
      }
      this.finishInitialization();
    }
    initialize(options) {
      if (options?.mixinDefaults) {
        if (!this._options) {
          this._options = options;
        }
        Utils.applyDefaults(this._options, this._defaults);
      } else {
        this._options = extend(true, {}, this._defaults, options);
      }
      this.scrollThrottle = this.actionThrottle(this.render.bind(this), this._options.scrollRenderThrottling);
      this.maxSupportedCssHeight = this.maxSupportedCssHeight || this.getMaxSupportedCssHeight();
      this.validateAndEnforceOptions();
      this._columnDefaults.width = this._options.defaultColumnWidth;
      this._prevFrozenColumnIdx = this.getFrozenColumnIdx();
      if (!this._options.suppressCssChangesOnHiddenInit) {
        this.cacheCssForHiddenInit();
      }
      this.updateColumnProps();
      this.editController = {
        commitCurrentEdit: this.commitCurrentEdit.bind(this),
        cancelCurrentEdit: this.cancelCurrentEdit.bind(this)
      };
      emptyElement(this._container);
      this._container.style.outline = String(0);
      this._container.classList.add(this.uid);
      this._container.classList.add("slick-widget");
      this._container.setAttribute("role", "grid");
      this._container.setAttribute("aria-colcount", this.columns.length.toString());
      this._container.setAttribute("aria-rowcount", Array.isArray(this.data) ? this.data.length.toString() : "0");
      const containerStyles = getComputedStyle(this._container);
      if (!/relative|absolute|fixed/.test(containerStyles.position)) {
        this._container.style.position = "relative";
      }
      this._focusSink = createDomElement("div", { tabIndex: -1, style: { position: "fixed", width: "0px", height: "0px", top: "0px", left: "0px", outline: "0px" } }, this._container);
      if (this._options.createTopHeaderPanel) {
        this._topHeaderPanelScroller = createDomElement("div", { className: "slick-topheader-panel slick-state-default", style: { overflow: "hidden", position: "relative" } }, this._container);
        this._topHeaderPanelScroller.appendChild(document.createElement("div"));
        this._topHeaderPanel = createDomElement("div", null, this._topHeaderPanelScroller);
        this._topHeaderPanelSpacer = createDomElement("div", { style: { display: "block", height: "1px", position: "absolute", top: "0px", left: "0px" } }, this._topHeaderPanelScroller);
        if (!this._options.showTopHeaderPanel) {
          Utils.hide(this._topHeaderPanelScroller);
        }
      }
      this._paneHeaderL = createDomElement("div", { className: "slick-pane slick-pane-header slick-pane-left" }, this._container);
      this._paneHeaderR = createDomElement("div", { className: "slick-pane slick-pane-header slick-pane-right" }, this._container);
      this._paneTopL = createDomElement("div", { className: "slick-pane slick-pane-top slick-pane-left" }, this._container);
      this._paneTopR = createDomElement("div", { className: "slick-pane slick-pane-top slick-pane-right" }, this._container);
      this._paneBottomL = createDomElement("div", { className: "slick-pane slick-pane-bottom slick-pane-left" }, this._container);
      this._paneBottomR = createDomElement("div", { className: "slick-pane slick-pane-bottom slick-pane-right" }, this._container);
      if (this._options.createPreHeaderPanel) {
        const headerContainer = createDomElement("div", { className: "slick-preheader-container" }, this._paneHeaderL);
        this._preHeaderPanelScroller = createDomElement("div", { className: "slick-preheader-panel slick-state-default", style: { overflow: "hidden", position: "relative" } }, headerContainer);
        this._preHeaderPanelScroller.appendChild(document.createElement("div"));
        this._preHeaderPanel = createDomElement("div", null, this._preHeaderPanelScroller);
        this._preHeaderPanelSpacer = createDomElement("div", { style: { display: "block", height: "1px", position: "absolute", top: "0px", left: "0px" } }, this._preHeaderPanelScroller);
        this._preHeaderPanelScrollerR = createDomElement("div", { className: "slick-preheader-panel slick-state-default", style: { overflow: "hidden", position: "relative" } }, this._paneHeaderR);
        this._preHeaderPanelR = createDomElement("div", null, this._preHeaderPanelScrollerR);
        this._preHeaderPanelSpacerR = createDomElement("div", { style: { display: "block", height: "1px", position: "absolute", top: "0px", left: "0px" } }, this._preHeaderPanelScrollerR);
        if (!this._options.showPreHeaderPanel) {
          Utils.hide(this._preHeaderPanelScroller);
          Utils.hide(this._preHeaderPanelScrollerR);
        }
      }
      const headerContainerL = createDomElement("div", { className: "slick-header-container" }, this._paneHeaderL);
      const headerContainerR = createDomElement("div", { className: "slick-header-container" }, this._paneHeaderR);
      this._headerScrollerL = createDomElement("div", { className: "slick-header slick-state-default slick-header-left" }, headerContainerL);
      this._headerScrollerR = createDomElement("div", { className: "slick-header slick-state-default slick-header-right" }, headerContainerR);
      this._bindingEventService.bind(this._headerScrollerR, "scroll", (e2) => {
        this.scrollToX(e2.target.scrollLeft);
      });
      this._headerScroller.push(this._headerScrollerL);
      this._headerScroller.push(this._headerScrollerR);
      this._headerL = createDomElement("div", { className: "slick-header-columns slick-header-columns-left", style: { left: "-1000px" } }, this._headerScrollerL);
      this._headerR = createDomElement("div", { className: "slick-header-columns slick-header-columns-right", style: { left: "-1000px" } }, this._headerScrollerR);
      this._headers = [this._headerL, this._headerR];
      this._headerRowScrollerL = createDomElement("div", { className: "slick-headerrow slick-state-default" }, this._paneTopL);
      this._headerRowScrollerR = createDomElement("div", { className: "slick-headerrow slick-state-default" }, this._paneTopR);
      this._headerRowScroller = [this._headerRowScrollerL, this._headerRowScrollerR];
      this._headerRowSpacerL = createDomElement("div", { style: { display: "block", height: "1px", position: "absolute", top: "0px", left: "0px" } }, this._headerRowScrollerL);
      this._headerRowSpacerR = createDomElement("div", { style: { display: "block", height: "1px", position: "absolute", top: "0px", left: "0px" } }, this._headerRowScrollerR);
      this._headerRowL = createDomElement("div", { className: "slick-headerrow-columns slick-headerrow-columns-left" }, this._headerRowScrollerL);
      this._headerRowR = createDomElement("div", { className: "slick-headerrow-columns slick-headerrow-columns-right" }, this._headerRowScrollerR);
      this._headerRows = [this._headerRowL, this._headerRowR];
      this._topPanelScrollerL = createDomElement("div", { className: "slick-top-panel-scroller slick-state-default" }, this._paneTopL);
      this._topPanelScrollerR = createDomElement("div", { className: "slick-top-panel-scroller slick-state-default" }, this._paneTopR);
      this._topPanelScrollers = [this._topPanelScrollerL, this._topPanelScrollerR];
      this._topPanelL = createDomElement("div", { className: "slick-top-panel", style: { width: "10000px" } }, this._topPanelScrollerL);
      this._topPanelR = createDomElement("div", { className: "slick-top-panel", style: { width: "10000px" } }, this._topPanelScrollerR);
      this._topPanels = [this._topPanelL, this._topPanelR];
      if (!this._options.showColumnHeader) {
        this._headerScroller.forEach((el) => {
          Utils.hide(el);
        });
      }
      if (!this._options.showTopPanel) {
        this._topPanelScrollers.forEach((scroller) => {
          Utils.hide(scroller);
        });
      }
      if (!this._options.showHeaderRow) {
        this._headerRowScroller.forEach((scroller) => {
          Utils.hide(scroller);
        });
      }
      this._viewportTopL = createDomElement("div", { className: "slick-viewport slick-viewport-top slick-viewport-left" }, this._paneTopL);
      this._viewportTopR = createDomElement("div", { className: "slick-viewport slick-viewport-top slick-viewport-right" }, this._paneTopR);
      this._viewportBottomL = createDomElement("div", { className: "slick-viewport slick-viewport-bottom slick-viewport-left" }, this._paneBottomL);
      this._viewportBottomR = createDomElement("div", { className: "slick-viewport slick-viewport-bottom slick-viewport-right" }, this._paneBottomR);
      this._viewport = [this._viewportTopL, this._viewportTopR, this._viewportBottomL, this._viewportBottomR];
      if (this._options.viewportClass) {
        this._viewport.forEach((view) => {
          view.classList.add(...classNameToList(this._options.viewportClass));
        });
      }
      this._activeViewportNode = this._viewportTopL;
      this._canvasTopL = createDomElement("div", { className: "grid-canvas grid-canvas-top grid-canvas-left" }, this._viewportTopL);
      this._canvasTopR = createDomElement("div", { className: "grid-canvas grid-canvas-top grid-canvas-right" }, this._viewportTopR);
      this._canvasBottomL = createDomElement("div", { className: "grid-canvas grid-canvas-bottom grid-canvas-left" }, this._viewportBottomL);
      this._canvasBottomR = createDomElement("div", { className: "grid-canvas grid-canvas-bottom grid-canvas-right" }, this._viewportBottomR);
      this._canvas = [this._canvasTopL, this._canvasTopR, this._canvasBottomL, this._canvasBottomR];
      this.scrollbarDimensions = this.scrollbarDimensions || this.measureScrollbar();
      const canvasWithScrollbarWidth = this.getCanvasWidth() + this.scrollbarDimensions.width;
      this._activeCanvasNode = this._canvasTopL;
      if (this._topHeaderPanelSpacer) {
        Utils.width(this._topHeaderPanelSpacer, canvasWithScrollbarWidth);
      }
      if (this._preHeaderPanelSpacer) {
        Utils.width(this._preHeaderPanelSpacer, canvasWithScrollbarWidth);
      }
      this._headers.forEach((el) => {
        Utils.width(el, this.getHeadersWidth());
      });
      Utils.width(this._headerRowSpacerL, canvasWithScrollbarWidth);
      Utils.width(this._headerRowSpacerR, canvasWithScrollbarWidth);
      if (this._options.createFooterRow) {
        this._footerRowScrollerR = createDomElement("div", { className: "slick-footerrow slick-state-default" }, this._paneTopR);
        this._footerRowScrollerL = createDomElement("div", { className: "slick-footerrow slick-state-default" }, this._paneTopL);
        this._footerRowScroller = [this._footerRowScrollerL, this._footerRowScrollerR];
        this._footerRowSpacerL = createDomElement("div", { style: { display: "block", height: "1px", position: "absolute", top: "0px", left: "0px" } }, this._footerRowScrollerL);
        Utils.width(this._footerRowSpacerL, canvasWithScrollbarWidth);
        this._footerRowSpacerR = createDomElement("div", { style: { display: "block", height: "1px", position: "absolute", top: "0px", left: "0px" } }, this._footerRowScrollerR);
        Utils.width(this._footerRowSpacerR, canvasWithScrollbarWidth);
        this._footerRowL = createDomElement("div", { className: "slick-footerrow-columns slick-footerrow-columns-left" }, this._footerRowScrollerL);
        this._footerRowR = createDomElement("div", { className: "slick-footerrow-columns slick-footerrow-columns-right" }, this._footerRowScrollerR);
        this._footerRow = [this._footerRowL, this._footerRowR];
        if (!this._options.showFooterRow) {
          this._footerRowScroller.forEach((scroller) => {
            Utils.hide(scroller);
          });
        }
      }
      this._focusSink2 = this._focusSink.cloneNode(true);
      this._container.appendChild(this._focusSink2);
      if (!this._options.explicitInitialization) {
        this.finishInitialization();
      }
    }
    finishInitialization() {
      if (!this.initialized) {
        this.initialized = true;
        this.getViewportWidth();
        this.getViewportHeight();
        this.measureCellPaddingAndBorder();
        this.disableSelection(this._headers);
        if (!this._options.enableTextSelectionOnCells) {
          this._viewport.forEach((view) => {
            this._bindingEventService.bind(view, "selectstart", (event) => {
              if (event.target instanceof HTMLInputElement || event.target instanceof HTMLTextAreaElement) {
                return;
              }
              event.preventDefault();
            });
          });
        }
        this.setFrozenOptions();
        this.setPaneFrozenClasses();
        this.setPaneVisibility();
        this.setScroller();
        this.setOverflow();
        this.updateColumnCaches();
        this.createColumnHeaders();
        this.createColumnFooter();
        this.setupColumnSort();
        this.createCssRules();
        this.resizeCanvas();
        this.bindAncestorScrollEvents();
        this._bindingEventService.bind(this._container, "resize", this.resizeCanvas.bind(this));
        this._bindingEventService.bind(this._viewport, "scroll", this.handleScroll.bind(this));
        this._bindingEventService.bind(this._viewport, "focus", () => {
          this._options.enableCellNavigation && this.focusGridCell();
        });
        if (this._options.enableMouseWheelScrollHandler) {
          this._viewport.forEach((view) => {
            this.slickMouseWheelInstances.push(MouseWheel({
              element: view,
              onMouseWheel: this.handleMouseWheel.bind(this)
            }));
          });
        }
        this._bindingEventService.bind(this._headerScroller, "contextmenu", this.handleHeaderContextMenu.bind(this));
        this._bindingEventService.bind(this._headerScroller, "click", this.handleHeaderClick.bind(this));
        this._bindingEventService.bind(this._headerRowScroller, "scroll", this.handleHeaderRowScroll.bind(this));
        if (this._options.createFooterRow) {
          this._bindingEventService.bind(this._footerRow, "contextmenu", this.handleFooterContextMenu.bind(this));
          this._bindingEventService.bind(this._footerRow, "click", this.handleFooterClick.bind(this));
          this._bindingEventService.bind(this._footerRowScroller, "scroll", this.handleFooterRowScroll.bind(this));
        }
        if (this._options.createTopHeaderPanel) {
          this._bindingEventService.bind(this._topHeaderPanelScroller, "scroll", this.handleTopHeaderPanelScroll.bind(this));
        }
        if (this._options.createPreHeaderPanel) {
          this._bindingEventService.bind(this._preHeaderPanelScroller, "scroll", this.handlePreHeaderPanelScroll.bind(this));
          this._bindingEventService.bind([this._preHeaderPanelScroller, this._preHeaderPanelScrollerR], "contextmenu", this.handlePreHeaderContextMenu.bind(this));
          this._bindingEventService.bind([this._preHeaderPanelScroller, this._preHeaderPanelScrollerR], "click", this.handlePreHeaderClick.bind(this));
        }
        this._bindingEventService.bind(this._focusSink, "keydown", this.handleGridKeyDown.bind(this));
        this._bindingEventService.bind(this._focusSink2, "keydown", this.handleGridKeyDown.bind(this));
        this._bindingEventService.bind(this._canvas, "keydown", this.handleGridKeyDown.bind(this));
        this._bindingEventService.bind(this._canvas, "click", this.handleClick.bind(this));
        this._bindingEventService.bind(this._canvas, "dblclick", this.handleDblClick.bind(this));
        this._bindingEventService.bind(this._canvas, "contextmenu", this.handleContextMenu.bind(this));
        this._bindingEventService.bind(this._canvas, "mouseover", this.handleCellMouseOver.bind(this));
        this._bindingEventService.bind(this._canvas, "mouseout", this.handleCellMouseOut.bind(this));
        this._bindingEventService.bind(this._container, "keydown", this.handleContainerKeyDown.bind(this));
        if (Draggable) {
          this.slickDraggableInstance = Draggable({
            containerElement: this._container,
            allowDragFrom: `div.slick-cell, div.${this.dragReplaceEl.cssClass}`,
            dragFromClassDetectArr: [{ tag: "dragReplaceHandle", id: this.dragReplaceEl.id }],
            // the slick cell parent must always contain `.dnd` and/or `.cell-reorder` class to be identified as draggable
            allowDragFromClosest: this._options.allowDragFromClosest,
            preventDragFromKeys: this._options.preventDragFromKeys,
            onDragInit: this.handleDragInit.bind(this),
            onDragStart: this.handleDragStart.bind(this),
            onDrag: this.handleDrag.bind(this),
            onDragEnd: this.handleDragEnd.bind(this)
          });
        }
        if (!this._options.suppressCssChangesOnHiddenInit) {
          this.restoreCssFromHiddenInit();
        }
      }
    }
    /** handles "display:none" on container or container parents, related to issue: https://github.com/6pac/SlickGrid/issues/568 */
    cacheCssForHiddenInit() {
      this._hiddenParents = Utils.parents(this._container, ":hidden");
      this.oldProps = [];
      this._hiddenParents.forEach((el) => {
        const old = {};
        Object.keys(this.cssShow).forEach((name) => {
          if (this.cssShow) {
            old[name] = el.style[name];
            el.style[name] = this.cssShow[name];
          }
        });
        this.oldProps.push(old);
      });
    }
    restoreCssFromHiddenInit() {
      let i = 0;
      if (this._hiddenParents) {
        this._hiddenParents.forEach((el) => {
          const old = this.oldProps[i++];
          Object.keys(this.cssShow).forEach((name) => {
            if (this.cssShow) {
              el.style[name] = old[name];
            }
          });
        });
        this._hiddenParents.length = 0;
      }
    }
    hasFrozenColumns() {
      return this._options.frozenColumn > -1;
    }
    /** Register an external Plugin */
    registerPlugin(plugin) {
      this.plugins.unshift(plugin);
      plugin.init(this);
    }
    /** Unregister (destroy) an external Plugin */
    unregisterPlugin(plugin) {
      for (let i = this.plugins.length; i >= 0; i--) {
        if (this.plugins[i] === plugin) {
          this.plugins[i]?.destroy();
          this.plugins.splice(i, 1);
          break;
        }
      }
    }
    /** Get a Plugin (addon) by its name */
    getPluginByName(name) {
      for (let i = this.plugins.length - 1; i >= 0; i--) {
        if (this.plugins[i]?.pluginName === name) {
          return this.plugins[i];
        }
      }
      return void 0;
    }
    getPubSubService() {
      return this._pubSubService;
    }
    /**
     * Unregisters a current selection model and registers a new one. See the definition of SelectionModel for more information.
     * @param {Object} selectionModel A SelectionModel.
     */
    setSelectionModel(model) {
      if (this.selectionModel) {
        this.selectionModel.onSelectedRangesChanged.unsubscribe(this.handleSelectedRangesChanged.bind(this));
        this.selectionModel.destroy?.();
      }
      this.selectionModel = model;
      if (this.selectionModel) {
        this.selectionModel.init(this);
        this.selectionModel.onSelectedRangesChanged.subscribe(this.handleSelectedRangesChanged.bind(this));
      }
    }
    /** Returns the current SelectionModel. See here for more information about SelectionModels. */
    getSelectionModel() {
      return this.selectionModel;
    }
    /** Get Grid Canvas Node DOM Element */
    getCanvasNode(columnIdOrIdx, rowIndex) {
      return this._getContainerElement(this.getCanvases(), columnIdOrIdx, rowIndex);
    }
    /** Get the canvas DOM element */
    getActiveCanvasNode(e2) {
      if (e2 === void 0) {
        return this._activeCanvasNode;
      }
      if (e2 instanceof SlickEventData) {
        e2 = e2.getNativeEvent();
      }
      this._activeCanvasNode = e2?.target?.closest(".grid-canvas");
      return this._activeCanvasNode;
    }
    /** Get the canvas DOM element */
    getCanvases() {
      return this._canvas;
    }
    /** Get the Viewport DOM node element */
    getViewportNode(columnIdOrIdx, rowIndex) {
      return this._getContainerElement(this.getViewports(), columnIdOrIdx, rowIndex);
    }
    /** Get all the Viewport node elements */
    getViewports() {
      return this._viewport;
    }
    getActiveViewportNode(e2) {
      this.setActiveViewportNode(e2);
      return this._activeViewportNode;
    }
    /** Sets an active viewport node */
    setActiveViewportNode(e2) {
      if (e2 instanceof SlickEventData) {
        e2 = e2.getNativeEvent();
      }
      this._activeViewportNode = e2?.target?.closest(".slick-viewport");
      return this._activeViewportNode;
    }
    _getContainerElement(targetContainers, columnIdOrIdx, rowIndex) {
      if (!targetContainers) {
        return;
      }
      if (!columnIdOrIdx) {
        columnIdOrIdx = 0;
      }
      if (!rowIndex) {
        rowIndex = 0;
      }
      const idx = typeof columnIdOrIdx === "number" ? columnIdOrIdx : this.getColumnIndex(columnIdOrIdx);
      const isBottomSide = this.hasFrozenRows && rowIndex >= this.actualFrozenRow + (this._options.frozenBottom ? 0 : 1);
      const isRightSide = this.hasFrozenColumns() && idx > this._options.frozenColumn;
      return targetContainers[(isBottomSide ? 2 : 0) + (isRightSide ? 1 : 0)];
    }
    measureScrollbar() {
      let className = "";
      this._viewport.forEach((v2) => className += v2.className);
      const outerdiv = createDomElement("div", {
        className,
        style: { position: "absolute", top: "-10000px", left: "-10000px", overflow: "auto", width: "100px", height: "100px" }
      }, document.body);
      const innerdiv = createDomElement("div", { style: { width: "200px", height: "200px", overflow: "auto" } }, outerdiv);
      const dim = {
        width: outerdiv.offsetWidth - outerdiv.clientWidth,
        height: outerdiv.offsetHeight - outerdiv.clientHeight
      };
      innerdiv.remove();
      outerdiv.remove();
      return dim;
    }
    /** Get the headers width in pixel */
    getHeadersWidth() {
      this.headersWidth = this.headersWidthL = this.headersWidthR = 0;
      const includeScrollbar = !this._options.autoHeight;
      let i = 0;
      const ii = this.columns.length;
      for (i = 0; i < ii; i++) {
        if (!this.columns[i] || this.columns[i].hidden) {
          continue;
        }
        const width = this.columns[i].width;
        if (this._options.frozenColumn > -1 && i > this._options.frozenColumn) {
          this.headersWidthR += width || 0;
        } else {
          this.headersWidthL += width || 0;
        }
      }
      if (includeScrollbar) {
        if (this._options.frozenColumn > -1 && i > this._options.frozenColumn) {
          this.headersWidthR += this.scrollbarDimensions?.width || 0;
        } else {
          this.headersWidthL += this.scrollbarDimensions?.width || 0;
        }
      }
      if (this.hasFrozenColumns()) {
        this.headersWidthL = this.headersWidthL + 1e3;
        this.headersWidthR = Math.max(this.headersWidthR, this.viewportW) + this.headersWidthL;
        this.headersWidthR += this.scrollbarDimensions?.width || 0;
      } else {
        this.headersWidthL += this.scrollbarDimensions?.width || 0;
        this.headersWidthL = Math.max(this.headersWidthL, this.viewportW) + 1e3;
      }
      this.headersWidth = this.headersWidthL + this.headersWidthR;
      return Math.max(this.headersWidth, this.viewportW) + 1e3;
    }
    /** Get the grid canvas width */
    getCanvasWidth() {
      const availableWidth = this.getViewportInnerWidth();
      let i = this.columns.length;
      this.canvasWidthL = this.canvasWidthR = 0;
      while (i--) {
        if (!this.columns[i] || this.columns[i].hidden) {
          continue;
        }
        if (this.hasFrozenColumns() && i > this._options.frozenColumn) {
          this.canvasWidthR += this.columns[i].width || 0;
        } else {
          this.canvasWidthL += this.columns[i].width || 0;
        }
      }
      let totalRowWidth = this.canvasWidthL + this.canvasWidthR;
      if (this._options.fullWidthRows) {
        const extraWidth = Math.max(totalRowWidth, availableWidth) - totalRowWidth;
        if (extraWidth > 0) {
          totalRowWidth += extraWidth;
          if (this.hasFrozenColumns()) {
            this.canvasWidthR += extraWidth;
          } else {
            this.canvasWidthL += extraWidth;
          }
        }
      }
      return totalRowWidth;
    }
    /**
     * Validate that the column freeze is allowed in the browser by making sure that the frozen column is not exceeding the available and visible left canvas width.
     * Note that it will only validate when `invalidColumnFreezeWidthCallback` grid option is enabled.
     * @param {Number} frozenColumn the column index to freeze at
     *  - if `undefined` it will do the condition check and never alert more than once
     *  - if `true` it will do the condition check and always alert even if it was called before
     *  - if `false` it will do the condition check but always skip the alert
     */
    validateColumnFreezeWidth(frozenColumn = -1) {
      if (frozenColumn >= 0) {
        let canvasWidthL = 0;
        this.columns.forEach((col, i) => {
          if (!col.hidden && i <= frozenColumn) {
            const { minWidth = 0, maxWidth = 0, width = this._options.defaultColumnWidth } = col;
            let fwidth = width < minWidth ? minWidth : width;
            if (maxWidth > 0 && fwidth > maxWidth) {
              fwidth = maxWidth;
            }
            canvasWidthL += fwidth;
          }
        });
        const cWidth = Utils.width(this._container) || 0;
        if (cWidth > 0 && canvasWidthL > cWidth && !this._options.skipFreezeColumnValidation) {
          if (this._options.invalidColumnFreezeWidthCallback) {
            this._options.invalidColumnFreezeWidthCallback?.(this._options.invalidColumnFreezeWidthMessage);
            this._invalidfrozenAlerted = true;
          }
          return false;
        }
      }
      return true;
    }
    /**
     * Validate that the frozen column is allowed by verifying there is at least 1, or more, column to the right of the frozen column otherwise show an error
     * Note that it will only validate when `invalidColumnFreezePickerCallback` grid option is enabled.
     * @param {Number|String} [columnId] column id
     * @param {Boolean} [forceAlert] tri-state flag to alert when frozen column is invalid
     * @param {Array<Column>} [colums] optionally provide new columns to validate
     *  - if `undefined` it will do the condition check and never alert more than once
     *  - if `true` it will do the condition check and always alert even if it was called before
     *  - if `false` it will do the condition check but always skip the alert
     */
    validateColumnFreeze(columnId, forceAlert = false, columns) {
      const hasColummnIdArg = columnId !== void 0;
      columns ??= this.columns;
      if (columnId === void 0 && (this._prevFrozenColumnIdx >= 0 || this._options.frozenColumn >= 0)) {
        const column = columns[this._prevFrozenColumnIdx] ?? columns[this._options.frozenColumn];
        columnId = column?.id ?? "";
      }
      const currentFrozenIdx = this._options.frozenColumn;
      const frozenColumnId = currentFrozenIdx >= 0 && currentFrozenIdx <= columns.length ? columns[currentFrozenIdx].id : "";
      if (!frozenColumnId || !columnId) {
        return true;
      }
      const visibleColumns = columns.filter((col) => !col.hidden);
      const colIdx = visibleColumns.findIndex((c) => c.id === columnId);
      const frozenColIdx = visibleColumns.findIndex((c) => c.id === frozenColumnId);
      let currentFrozenColumn = this._options.frozenColumn;
      if (frozenColIdx > colIdx && colIdx <= currentFrozenColumn || currentFrozenColumn === -1) {
        return true;
      }
      if (currentFrozenColumn >= 0 && currentFrozenColumn >= visibleColumns.length - 2 && !this._options.skipFreezeColumnValidation || hasColummnIdArg && currentFrozenColumn === 0 && columnId !== void 0 && this.columns[0].id === columnId) {
        if (forceAlert !== false && !this._invalidfrozenAlerted || forceAlert === true) {
          this._options.invalidColumnFreezePickerCallback?.(this._options.invalidColumnFreezePickerMessage);
          this._invalidfrozenAlerted = true;
        }
        return false;
      }
      return true;
    }
    updateCanvasWidth(forceColumnWidthsUpdate) {
      const oldCanvasWidth = this.canvasWidth;
      const oldCanvasWidthL = this.canvasWidthL;
      const oldCanvasWidthR = this.canvasWidthR;
      this.canvasWidth = this.getCanvasWidth();
      if (this._options.createTopHeaderPanel && !this._isResizingColumn) {
        const panelWidth = this._options.topHeaderPanelWidth ?? this.canvasWidth;
        this._topHeaderPanel.style.width = typeof panelWidth === "string" ? panelWidth : `${panelWidth}px`;
      }
      const widthChanged = this.canvasWidth !== oldCanvasWidth || this.canvasWidthL !== oldCanvasWidthL || this.canvasWidthR !== oldCanvasWidthR;
      if (widthChanged || this.hasFrozenColumns() || this.hasFrozenRows) {
        Utils.width(this._canvasTopL, this.canvasWidthL);
        this.getHeadersWidth();
        Utils.width(this._headerL, this.headersWidthL);
        Utils.width(this._headerR, this.headersWidthR);
        if (this.hasFrozenColumns()) {
          Utils.width(this._canvasTopR, this.canvasWidthR);
          Utils.width(this._paneHeaderL, this.canvasWidthL);
          Utils.setStyleSize(this._paneHeaderR, "left", this.canvasWidthL);
          Utils.setStyleSize(this._paneHeaderR, "width", this.viewportW - this.canvasWidthL);
          Utils.width(this._paneTopL, this.canvasWidthL);
          Utils.setStyleSize(this._paneTopR, "left", this.canvasWidthL);
          Utils.width(this._paneTopR, this.viewportW - this.canvasWidthL);
          Utils.width(this._headerRowScrollerL, this.canvasWidthL);
          Utils.width(this._headerRowScrollerR, this.viewportW - this.canvasWidthL);
          Utils.width(this._headerRowL, this.canvasWidthL);
          Utils.width(this._headerRowR, this.canvasWidthR);
          if (this._options.createFooterRow) {
            Utils.width(this._footerRowScrollerL, this.canvasWidthL);
            Utils.width(this._footerRowScrollerR, this.viewportW - this.canvasWidthL);
            Utils.width(this._footerRowL, this.canvasWidthL);
            Utils.width(this._footerRowR, this.canvasWidthR);
          }
          if (this._options.createPreHeaderPanel && !this._isResizingColumn) {
            const panelWidth = this._options.preHeaderPanelWidth ?? this.canvasWidth;
            this._preHeaderPanel.style.width = typeof panelWidth === "string" ? panelWidth : `${panelWidth}px`;
          }
          Utils.width(this._viewportTopL, this.canvasWidthL);
          Utils.width(this._viewportTopR, this.viewportW - this.canvasWidthL);
          if (this.hasFrozenRows) {
            Utils.width(this._paneBottomL, this.canvasWidthL);
            Utils.setStyleSize(this._paneBottomR, "left", this.canvasWidthL);
            Utils.width(this._viewportBottomL, this.canvasWidthL);
            Utils.width(this._viewportBottomR, this.viewportW - this.canvasWidthL);
            Utils.width(this._canvasBottomL, this.canvasWidthL);
            Utils.width(this._canvasBottomR, this.canvasWidthR);
          }
        } else {
          Utils.width(this._paneHeaderL, "100%");
          Utils.width(this._paneTopL, "100%");
          Utils.width(this._headerRowScrollerL, "100%");
          Utils.width(this._headerRowL, this.canvasWidth);
          if (this._options.createFooterRow) {
            Utils.width(this._footerRowScrollerL, "100%");
            Utils.width(this._footerRowL, this.canvasWidth);
          }
          if (this._options.createPreHeaderPanel && !this._isResizingColumn) {
            const panelWidth = this._options.preHeaderPanelWidth ?? this.canvasWidth;
            this._preHeaderPanel.style.width = typeof panelWidth === "string" ? panelWidth : `${panelWidth}px`;
          }
          Utils.width(this._viewportTopL, "100%");
          if (this.hasFrozenRows) {
            Utils.width(this._viewportBottomL, "100%");
            Utils.width(this._canvasBottomL, this.canvasWidthL);
          }
        }
      }
      this.viewportHasHScroll = this.canvasWidth >= this.viewportW - (this.scrollbarDimensions?.width || 0);
      Utils.width(this._headerRowSpacerL, this.canvasWidth + (this.viewportHasVScroll ? this.scrollbarDimensions?.width || 0 : 0));
      Utils.width(this._headerRowSpacerR, this.canvasWidth + (this.viewportHasVScroll ? this.scrollbarDimensions?.width || 0 : 0));
      if (this._options.createFooterRow) {
        Utils.width(this._footerRowSpacerL, this.canvasWidth + (this.viewportHasVScroll ? this.scrollbarDimensions?.width || 0 : 0));
        Utils.width(this._footerRowSpacerR, this.canvasWidth + (this.viewportHasVScroll ? this.scrollbarDimensions?.width || 0 : 0));
      }
      if (widthChanged || forceColumnWidthsUpdate) {
        this.applyColumnWidths();
      }
    }
    disableSelection(target) {
      target.forEach((el) => {
        el.setAttribute("unselectable", "on");
        el.style.mozUserSelect = "none";
        this._bindingEventService.bind(el, "selectstart", () => false);
      });
    }
    getMaxSupportedCssHeight() {
      let supportedHeight = 1e6;
      const testUpTo = navigator.userAgent.toLowerCase().match(/firefox/) ? this._options.ffMaxSupportedCssHeight : this._options.maxSupportedCssHeight;
      const div = createDomElement("div", { style: { display: "hidden" } }, document.body);
      let condition = true;
      while (condition) {
        const test = supportedHeight * 2;
        Utils.height(div, test);
        const height = Utils.height(div);
        if (test > testUpTo || height !== test) {
          condition = false;
          break;
        } else {
          supportedHeight = test;
        }
      }
      div.remove();
      return supportedHeight;
    }
    /** Get grid unique identifier */
    getUID() {
      return this.uid;
    }
    /** Get Header Column Width Difference in pixel */
    getHeaderColumnWidthDiff() {
      return this.headerColumnWidthDiff;
    }
    /** Get scrollbar dimensions */
    getScrollbarDimensions() {
      return this.scrollbarDimensions;
    }
    /** Get the displayed scrollbar dimensions */
    getDisplayedScrollbarDimensions() {
      return {
        width: this.viewportHasVScroll && this.scrollbarDimensions?.width ? this.scrollbarDimensions.width : 0,
        height: this.viewportHasHScroll && this.scrollbarDimensions?.height ? this.scrollbarDimensions.height : 0
      };
    }
    /** Get the absolute column minimum width */
    getAbsoluteColumnMinWidth() {
      return this.absoluteColumnMinWidth;
    }
    // TODO:  this is static. we need to handle page mutation.
    bindAncestorScrollEvents() {
      let elem = this.hasFrozenRows && !this._options.frozenBottom ? this._canvasBottomL : this._canvasTopL;
      while ((elem = elem.parentNode) !== document.body && elem) {
        if (elem === this._viewportTopL || elem.scrollWidth !== elem.clientWidth || elem.scrollHeight !== elem.clientHeight) {
          this._boundAncestors.push(elem);
          this._bindingEventService.bind(elem, "scroll", this.handleActiveCellPositionChange.bind(this));
        }
      }
    }
    /**
     * Updates an existing column definition and a corresponding header DOM element with the new title and tooltip.
     * @param {Number|String} columnId Column id.
     * @param {string | HTMLElement | DocumentFragment} [title] New column name.
     * @param {String} [toolTip] New column tooltip.
     */
    updateColumnHeader(columnId, title, toolTip) {
      if (this.initialized) {
        const idx = this.getColumnIndex(columnId);
        if (!isDefined(idx)) {
          return;
        }
        const columnDef = this.columns[idx];
        const header = this.getColumnHeaderByIndex(idx);
        if (header) {
          if (title !== void 0) {
            this.columns[idx].name = title;
          }
          if (toolTip !== void 0) {
            this.columns[idx].toolTip = toolTip;
          }
          this.triggerEvent(this.onBeforeHeaderCellDestroy, {
            node: header,
            column: columnDef,
            grid: this
          });
          header.setAttribute("title", toolTip || "");
          if (title !== void 0) {
            applyHtmlToElement(header.children[0], title, this._options);
          }
          this.triggerEvent(this.onHeaderCellRendered, {
            node: header,
            column: columnDef,
            grid: this
          });
        }
        return header;
      }
    }
    /**
     * Get the Header DOM element
     * @param {C} columnDef - column definition
     */
    getHeader(columnDef) {
      if (!columnDef) {
        return this.hasFrozenColumns() ? this._headers : this._headerL;
      }
      const idx = this.getColumnIndex(columnDef.id);
      return this.hasFrozenColumns() ? idx <= this._options.frozenColumn ? this._headerL : this._headerR : this._headerL;
    }
    /**
     * Get a specific Header Column DOM element by its column Id or index
     * @param {Number|String} columnIdOrIdx - column Id or index
     */
    getHeaderColumn(columnIdOrIdx) {
      const idx = typeof columnIdOrIdx === "number" ? columnIdOrIdx : this.getColumnIndex(columnIdOrIdx);
      const targetHeader = this.hasFrozenColumns() ? idx <= this._options.frozenColumn ? this._headerL : this._headerR : this._headerL;
      const targetIndex = this.hasFrozenColumns() ? idx <= this._options.frozenColumn ? idx : idx - this._options.frozenColumn - 1 : idx;
      const directMatch = targetHeader.children[targetIndex];
      const targetColumnId = String(this.columns[idx]?.id ?? columnIdOrIdx);
      const directMatchColumn = Utils.storage.get(directMatch, "column");
      if (directMatch && (directMatch.dataset?.id === targetColumnId || String(directMatchColumn?.id) === targetColumnId)) {
        return directMatch;
      }
      return Array.from(targetHeader.children).find((child) => child.dataset?.id === targetColumnId) || void 0;
    }
    /** Get the Header Row DOM element */
    getHeaderRow() {
      return this.hasFrozenColumns() ? this._headerRows : this._headerRows?.[0];
    }
    /** Get the Footer DOM element */
    getFooterRow() {
      return this.hasFrozenColumns() ? this._footerRow : this._footerRow?.[0];
    }
    /** @alias `getPreHeaderPanelLeft` */
    getPreHeaderPanel() {
      return this._preHeaderPanel;
    }
    /** Get the Pre-Header Panel Left DOM node element */
    getPreHeaderPanelLeft() {
      return this._preHeaderPanel;
    }
    /** Get the Pre-Header Panel Right DOM node element */
    getPreHeaderPanelRight() {
      return this._preHeaderPanelR;
    }
    /** Get the Top-Header Panel DOM node element */
    getTopHeaderPanel() {
      return this._topHeaderPanel;
    }
    /**
     * Get Header Row Column DOM element by its column Id or index
     * @param {Number|String} columnIdOrIdx - column Id or index
     */
    getHeaderRowColumn(columnIdOrIdx) {
      let idx = typeof columnIdOrIdx === "number" ? columnIdOrIdx : this.getColumnIndex(columnIdOrIdx);
      let headerRowTarget;
      if (this.hasFrozenColumns()) {
        if (idx <= this._options.frozenColumn) {
          headerRowTarget = this._headerRowL;
        } else {
          headerRowTarget = this._headerRowR;
          idx -= this._options.frozenColumn + 1;
        }
      } else {
        headerRowTarget = this._headerRowL;
      }
      return headerRowTarget.children[idx];
    }
    /**
     * Get the Footer Row Column DOM element by its column Id or index
     * @param {Number|String} columnIdOrIdx - column Id or index
     */
    getFooterRowColumn(columnIdOrIdx) {
      let idx = typeof columnIdOrIdx === "number" ? columnIdOrIdx : this.getColumnIndex(columnIdOrIdx);
      let footerRowTarget;
      if (this.hasFrozenColumns()) {
        if (idx <= this._options.frozenColumn) {
          footerRowTarget = this._footerRowL;
        } else {
          footerRowTarget = this._footerRowR;
          idx -= this._options.frozenColumn + 1;
        }
      } else {
        footerRowTarget = this._footerRowL;
      }
      return footerRowTarget?.children[idx];
    }
    createColumnFooter() {
      if (this._options.createFooterRow) {
        this._footerRow.forEach((footer) => {
          const columnElements = footer.querySelectorAll(".slick-footerrow-column");
          columnElements.forEach((column) => {
            const columnDef = Utils.storage.get(column, "column");
            this.triggerEvent(this.onBeforeFooterRowCellDestroy, {
              node: column,
              column: columnDef,
              grid: this
            });
          });
        });
        emptyElement(this._footerRowL);
        emptyElement(this._footerRowR);
        for (let i = 0; i < this.columns.length; i++) {
          const m = this.columns[i];
          if (!m || m.hidden) {
            continue;
          }
          const footerRowCell = createDomElement("div", { className: `slick-state-default slick-footerrow-column l${i} r${i}` }, this.hasFrozenColumns() && i > this._options.frozenColumn ? this._footerRowR : this._footerRowL);
          const className = this.hasFrozenColumns() && i <= this._options.frozenColumn ? "frozen" : null;
          if (className) {
            footerRowCell.classList.add(className);
          }
          Utils.storage.put(footerRowCell, "column", m);
          this.triggerEvent(this.onFooterRowCellRendered, {
            node: footerRowCell,
            column: m,
            grid: this
          });
        }
      }
    }
    handleHeaderMouseHoverOn(e2) {
      e2?.target.classList.add("slick-state-hover");
    }
    handleHeaderMouseHoverOff(e2) {
      e2?.target.classList.remove("slick-state-hover");
    }
    createColumnHeaders() {
      this._bindingEventService.unbindAll("colheaders");
      this._headers.forEach((header) => {
        const columnElements = header.querySelectorAll(".slick-header-column");
        columnElements.forEach((column) => {
          const columnDef = Utils.storage.get(column, "column");
          if (columnDef) {
            this.triggerEvent(this.onBeforeHeaderCellDestroy, {
              node: column,
              column: columnDef,
              grid: this
            });
          }
        });
      });
      emptyElement(this._headerL);
      emptyElement(this._headerR);
      this.getHeadersWidth();
      Utils.width(this._headerL, this.headersWidthL);
      Utils.width(this._headerR, this.headersWidthR);
      this._headerRows.forEach((row) => {
        const columnElements = row.querySelectorAll(".slick-headerrow-column");
        columnElements.forEach((column) => {
          const columnDef = Utils.storage.get(column, "column");
          if (columnDef) {
            this.triggerEvent(this.onBeforeHeaderRowCellDestroy, {
              node: this,
              column: columnDef,
              grid: this
            });
          }
        });
      });
      emptyElement(this._headerRowL);
      emptyElement(this._headerRowR);
      if (this._options.createFooterRow) {
        const footerRowLColumnElements = this._footerRowL.querySelectorAll(".slick-footerrow-column");
        footerRowLColumnElements.forEach((column) => {
          const columnDef = Utils.storage.get(column, "column");
          if (columnDef) {
            this.triggerEvent(this.onBeforeFooterRowCellDestroy, {
              node: this,
              column: columnDef,
              grid: this
            });
          }
        });
        emptyElement(this._footerRowL);
        if (this.hasFrozenColumns()) {
          const footerRowRColumnElements = this._footerRowR.querySelectorAll(".slick-footerrow-column");
          footerRowRColumnElements.forEach((column) => {
            const columnDef = Utils.storage.get(column, "column");
            if (columnDef) {
              this.triggerEvent(this.onBeforeFooterRowCellDestroy, {
                node: this,
                column: columnDef,
                grid: this
              });
            }
          });
          emptyElement(this._footerRowR);
        }
      }
      for (let i = 0, ln = this.columns.length; i < ln; i++) {
        const m = this.columns[i];
        if (!m || m.hidden) {
          continue;
        }
        const headerTarget = this.hasFrozenColumns() ? i <= this._options.frozenColumn ? this._headerL : this._headerR : this._headerL;
        const headerRowTarget = this.hasFrozenColumns() ? i <= this._options.frozenColumn ? this._headerRowL : this._headerRowR : this._headerRowL;
        const header = createDomElement("div", {
          id: `${this.uid + m.id}`,
          dataset: { id: String(m.id) },
          role: "columnheader",
          className: "slick-state-default slick-header-column",
          tabIndex: 0
        }, headerTarget);
        if (m.toolTip) {
          header.title = m.toolTip;
        }
        if (!m.reorderable) {
          header.classList.add(this._options.unorderableColumnCssClass);
        }
        const colNameElm = createDomElement("span", { className: "slick-column-name" }, header);
        applyHtmlToElement(colNameElm, m.name, this._options);
        let colWidth = m.width - this.headerColumnWidthDiff;
        if (this._options.enableGridMenu && i === ln - 1) {
          colWidth -= this._lastColumnGridMenuCompensation;
          if (!this.scrollbarDimensions?.width) {
            colWidth -= this._options.gridMenu?.menuWidth ?? 18;
          }
        }
        Utils.width(header, colWidth);
        let classname = m.headerCssClass || null;
        if (classname) {
          header.classList.add(...classNameToList(classname));
        }
        classname = this.hasFrozenColumns() && i <= this._options.frozenColumn ? "frozen" : null;
        if (classname) {
          header.classList.add(classname);
        }
        this._bindingEventService.bind(header, "mouseenter", this.handleHeaderMouseEnter.bind(this), {}, "colheaders");
        this._bindingEventService.bind(header, "mouseleave", this.handleHeaderMouseLeave.bind(this), {}, "colheaders");
        this._bindingEventService.bind(header, "mouseover", this.handleHeaderMouseOver.bind(this), {}, "colheaders");
        this._bindingEventService.bind(header, "mouseout", this.handleHeaderMouseOut.bind(this), {}, "colheaders");
        Utils.storage.put(header, "column", m);
        if (this._options.enableColumnReorder || m.sortable) {
          this._bindingEventService.bind(header, "mouseenter", this.handleHeaderMouseHoverOn.bind(this), {}, "colheaders");
          this._bindingEventService.bind(header, "mouseleave", this.handleHeaderMouseHoverOff.bind(this), {}, "colheaders");
        }
        if (m.hasOwnProperty("headerCellAttrs") && m.headerCellAttrs instanceof Object) {
          Object.keys(m.headerCellAttrs).forEach((key) => {
            if (m.headerCellAttrs.hasOwnProperty(key)) {
              header.setAttribute(key, m.headerCellAttrs[key]);
            }
          });
        }
        if (m.sortable) {
          header.classList.add("slick-header-sortable");
          createDomElement("div", {
            className: `slick-sort-indicator ${this._options.numberedMultiColumnSort && !this._options.sortColNumberInSeparateSpan ? " slick-sort-indicator-numbered" : ""}`
          }, header);
          if (this._options.numberedMultiColumnSort && this._options.sortColNumberInSeparateSpan) {
            createDomElement("div", { className: "slick-sort-indicator-numbered" }, header);
          }
        }
        this.triggerEvent(this.onHeaderCellRendered, {
          node: header,
          column: m,
          grid: this
        });
        if (this._options.showHeaderRow) {
          const headerRowCell = createDomElement("div", { className: `slick-state-default slick-headerrow-column l${i} r${i}` }, headerRowTarget);
          const frozenClasses = this.hasFrozenColumns() && i <= this._options.frozenColumn ? "frozen" : null;
          if (frozenClasses) {
            headerRowCell.classList.add(frozenClasses);
          }
          this._bindingEventService.bind(headerRowCell, "mouseenter", this.handleHeaderRowMouseEnter.bind(this), {}, "colheaders");
          this._bindingEventService.bind(headerRowCell, "mouseleave", this.handleHeaderRowMouseLeave.bind(this), {}, "colheaders");
          this._bindingEventService.bind(headerRowCell, "mouseover", this.handleHeaderRowMouseOver.bind(this), {}, "colheaders");
          this._bindingEventService.bind(headerRowCell, "mouseout", this.handleHeaderRowMouseOut.bind(this), {}, "colheaders");
          Utils.storage.put(headerRowCell, "column", m);
          this.triggerEvent(this.onHeaderRowCellRendered, {
            node: headerRowCell,
            column: m,
            grid: this
          });
        }
        if (this._options.createFooterRow && this._options.showFooterRow) {
          const footerRowTarget = this.hasFrozenColumns() ? i <= this._options.frozenColumn ? this._footerRow[0] : this._footerRow[1] : this._footerRow[0];
          const footerRowCell = createDomElement("div", { className: `slick-state-default slick-footerrow-column l${i} r${i}` }, footerRowTarget);
          Utils.storage.put(footerRowCell, "column", m);
          this.triggerEvent(this.onFooterRowCellRendered, {
            node: footerRowCell,
            column: m,
            grid: this
          });
        }
      }
      this.setSortColumns(this.sortColumns);
      this.setupColumnResize();
      if (this._options.enableColumnReorder) {
        if (typeof this._options.enableColumnReorder === "function") {
          this._options.enableColumnReorder(this, this._headers, this.headerColumnWidthDiff, this.setColumns, this.setupColumnResize, this.columns, this.getColumnIndex, this.uid, this.triggerEvent);
        } else {
          this.setupColumnReorder();
        }
      }
    }
    setupColumnSort() {
      this._bindingEventService.unbindAll("colsorts");
      this._headers.forEach((header) => {
        const sortCallback = (e2) => {
          if (this.columnResizeDragging || e2.target.classList.contains("slick-resizable-handle")) {
            return;
          }
          const coll = e2.target.closest(".slick-header-column");
          if (!coll) {
            return;
          }
          const column = Utils.storage.get(coll, "column");
          if (column?.sortable) {
            if (!this.getEditorLock()?.commitCurrentEdit()) {
              return;
            }
            const previousSortColumns = this.sortColumns.slice();
            let sortColumn = null;
            let i = 0;
            for (; i < this.sortColumns.length; i++) {
              if (this.sortColumns[i].columnId === column.id) {
                sortColumn = this.sortColumns[i];
                sortColumn.sortAsc = !sortColumn.sortAsc;
                break;
              }
            }
            const hadSortCol = !!sortColumn;
            if (this._options.tristateMultiColumnSort) {
              if (!sortColumn) {
                sortColumn = { columnId: column.id, sortAsc: column.defaultSortAsc, sortCol: column };
              }
              if (hadSortCol && sortColumn.sortAsc) {
                this.sortColumns.splice(i, 1);
                sortColumn = null;
              }
              if (!this._options.multiColumnSort) {
                this.sortColumns = [];
              }
              if (sortColumn && (!hadSortCol || !this._options.multiColumnSort)) {
                this.sortColumns.push(sortColumn);
              }
            } else {
              if (e2.metaKey && this._options.multiColumnSort) {
                if (sortColumn) {
                  this.sortColumns.splice(i, 1);
                }
              } else {
                if (!e2.shiftKey && !e2.metaKey || !this._options.multiColumnSort) {
                  this.sortColumns = [];
                }
                if (!sortColumn) {
                  sortColumn = { columnId: column.id, sortAsc: column.defaultSortAsc, sortCol: column };
                  this.sortColumns.push(sortColumn);
                } else if (this.sortColumns.length === 0) {
                  this.sortColumns.push(sortColumn);
                }
              }
            }
            let onSortArgs;
            if (!this._options.multiColumnSort) {
              onSortArgs = {
                multiColumnSort: false,
                previousSortColumns,
                columnId: this.sortColumns.length > 0 ? column.id : null,
                sortCol: this.sortColumns.length > 0 ? column : null,
                sortAsc: this.sortColumns.length > 0 ? this.sortColumns[0].sortAsc : true
              };
            } else {
              onSortArgs = {
                multiColumnSort: true,
                previousSortColumns,
                sortCols: this.sortColumns.map((col) => {
                  const tempCol = this.getColumnById(col.columnId);
                  return tempCol && !tempCol.hidden ? { columnId: tempCol.id, sortCol: tempCol, sortAsc: col.sortAsc } : null;
                }).filter((el) => el)
              };
            }
            if (this.triggerEvent(this.onBeforeSort, onSortArgs, e2).getReturnValue() !== false) {
              this.setSortColumns(this.sortColumns);
              this.triggerEvent(this.onSort, onSortArgs, e2);
            }
          }
        };
        this._bindingEventService.bind(header, "keydown", ((e2) => {
          this.triggerEvent(this.onHeaderKeyDown, { event: e2, column: Utils.storage.get(e2.target, "column"), grid: this });
          if (e2.key === "Enter" || e2.key === " ") {
            sortCallback(e2);
          }
        }), {}, "colsorts");
        this._bindingEventService.bind(header, "click", ((e2) => sortCallback(e2)), {}, "colsorts");
      });
    }
    setupColumnReorder() {
      this.sortableSideLeftInstance?.destroy();
      this.sortableSideRightInstance?.destroy();
      let columnScrollTimer;
      const scrollColumnsRight = () => this._viewportScrollContainerX.scrollLeft += 10;
      const scrollColumnsLeft = () => this._viewportScrollContainerX.scrollLeft -= 10;
      let prevColumnIds = [];
      let columnMap;
      let canDragScroll = false;
      const sortableOptions = {
        animation: 50,
        direction: "horizontal",
        ghostClass: "slick-sortable-placeholder",
        draggable: ".slick-header-column",
        dragoverBubble: false,
        // Fixes broken Firefox-Linux dragging
        forceFallback: /firefox/i.test(navigator.userAgent) && /linux/i.test(navigator.userAgent),
        // allow column to be resized even when they are not orderable
        preventOnFilter: false,
        revertClone: true,
        scroll: !this.hasFrozenColumns(),
        // enable auto-scroll
        // lock unorderable columns by using a combo of filter + onMove
        filter: `.${this._options.unorderableColumnCssClass}`,
        onMove: (event) => {
          return !event.related.classList.contains(this._options.unorderableColumnCssClass);
        },
        onStart: (e2) => {
          e2.item.classList.add("slick-header-column-active");
          canDragScroll = !this.hasFrozenColumns() || getOffset(e2.item).left > getOffset(this._viewportScrollContainerX).left;
          if (canDragScroll && e2.originalEvent.pageX > this._container.clientWidth) {
            if (!columnScrollTimer) {
              columnScrollTimer = setInterval(scrollColumnsRight, 100);
            }
          } else if (canDragScroll && e2.originalEvent.pageX < getOffset(this._viewportScrollContainerX).left) {
            if (!columnScrollTimer) {
              columnScrollTimer = setInterval(scrollColumnsLeft, 100);
            }
          } else {
            clearInterval(columnScrollTimer);
          }
          prevColumnIds = this.columns.map((c) => c.id);
          columnMap = /* @__PURE__ */ new Map();
          this.columns.forEach((col, idx) => {
            columnMap.set(col.id, { index: idx, hidden: !!col.hidden, column: col });
          });
        },
        onEnd: (e2) => {
          e2.item.classList.remove("slick-header-column-active");
          clearInterval(columnScrollTimer);
          const prevScrollLeft = this.scrollLeft;
          if (!this.getEditorLock()?.commitCurrentEdit()) {
            return;
          }
          let reorderedIds = this.sortableSideLeftInstance?.toArray() ?? [];
          reorderedIds = reorderedIds.concat(this.sortableSideRightInstance?.toArray() ?? []);
          const reorderedColumns = [];
          for (let i = 0; i < reorderedIds.length; i++) {
            reorderedColumns.push(this.columns[this.getColumnIndex(reorderedIds[i])]);
          }
          const finalColumns = [];
          let visibleIdx = 0;
          for (let i = 0; i < this.columns.length; i++) {
            const colInfo = columnMap.get(this.columns[i].id);
            if (colInfo?.hidden) {
              finalColumns.push(colInfo.column);
            } else {
              finalColumns.push(reorderedColumns[visibleIdx++]);
            }
          }
          e2.stopPropagation();
          if (!this.arrayEquals(prevColumnIds, reorderedIds)) {
            this.setColumns(finalColumns);
            this.scrollToX(prevScrollLeft);
            this.triggerEvent(this.onColumnsReordered, { impactedColumns: this.columns, previousColumnOrder: prevColumnIds });
            this.setupColumnResize();
          }
          if (this.activeCellNode) {
            this.setFocus();
          }
        }
      };
      this.sortableSideLeftInstance = sortable_core_esm_default.create(this._headerL, sortableOptions);
      this.sortableSideRightInstance = sortable_core_esm_default.create(this._headerR, sortableOptions);
    }
    getHeaderChildren() {
      const a = Array.from(this._headers[0].children);
      const b = Array.from(this._headers[1].children);
      return a.concat(b);
    }
    handleResizeableDoubleClick(evt) {
      const triggeredByColumn = evt.target.parentElement.id.replace(this.uid, "");
      this.triggerEvent(this.onColumnsResizeDblClick, { triggeredByColumn });
    }
    setupColumnResize() {
      let j2, k2, c;
      let pageX, minPageX, maxPageX;
      let firstResizable;
      let lastResizable = -1;
      let frozenLeftColMaxWidth = 0;
      let resizeAutoScrollDeltaX = 0;
      let autoScrollClientX;
      let autoScrollOffsetX = 0;
      this._bindingEventService.unbindAll("colresizes");
      this.clearAutoScrollTimer();
      const children = this.getHeaderChildren();
      const vc = this.getVisibleColumns();
      for (let i = 0; i < children.length; i++) {
        const child = children[i];
        const handles = child.querySelectorAll(".slick-resizable-handle");
        handles.forEach((handle) => handle.remove());
        if (i < vc.length && vc[i]?.resizable) {
          if (firstResizable === void 0) {
            firstResizable = i;
          }
          lastResizable = i;
        }
      }
      if (firstResizable === void 0) {
        return;
      }
      const stopColumnResizeAutoScroll = () => {
        this.clearAutoScrollTimer();
        autoScrollOffsetX = 0;
      };
      const scheduleColumnResizeAutoScroll = (resizeCallback) => {
        if (this._columnResizeAutoScrollTimer || !autoScrollOffsetX)
          return;
        let elapsed = 0;
        this._columnResizeAutoScrollTimer = setInterval(() => {
          if (!autoScrollOffsetX) {
            elapsed = 0;
            return;
          }
          const isBrowserEdge = Math.abs(autoScrollOffsetX) === 1;
          const viewportOffset = getOffset(this._viewportScrollContainerX);
          const moveDistance = isBrowserEdge ? 4 * autoScrollOffsetX : this.getAbsoluteColumnMinWidth() / 2 * Math.sign(autoScrollOffsetX);
          if (!isBrowserEdge) {
            const delay = RESIZE_AUTOSCROLL_MAX_INTERVAL_MS - Math.abs(autoScrollOffsetX) * RESIZE_AUTOSCROLL_ACCELERATE_INTERVAL;
            elapsed += RESIZE_AUTOSCROLL_MIN_INTERVAL_MS;
            if (elapsed < delay)
              return;
            elapsed = 0;
          }
          const targetPageX = autoScrollOffsetX > 0 ? viewportOffset.left + this._viewportScrollContainerX.clientWidth + moveDistance : viewportOffset.left - moveDistance;
          resizeCallback(targetPageX + resizeAutoScrollDeltaX);
        }, RESIZE_AUTOSCROLL_MIN_INTERVAL_MS);
      };
      const updateColumnResizeAutoScroll = (clientX, targetPageX, resizeCallback) => {
        autoScrollClientX = isDefinedNumber(clientX) ? clientX : autoScrollClientX;
        const viewportOffset = getOffset(this._viewportScrollContainerX);
        const left = viewportOffset.left;
        const right = left + this._viewportScrollContainerX.clientWidth;
        const browserW = window.innerWidth || document.documentElement.clientWidth || 0;
        if (targetPageX <= left) {
          autoScrollOffsetX = targetPageX - left;
        } else if (targetPageX >= right) {
          autoScrollOffsetX = targetPageX - right;
        } else if (isDefinedNumber(autoScrollClientX) && browserW > 0) {
          if (autoScrollClientX <= RESIZE_AUTOSCROLL_BROWSER_EDGE_PX) {
            autoScrollOffsetX = -1;
          } else if (autoScrollClientX >= browserW - RESIZE_AUTOSCROLL_BROWSER_EDGE_PX) {
            autoScrollOffsetX = 1;
          } else {
            stopColumnResizeAutoScroll();
            return;
          }
        } else {
          stopColumnResizeAutoScroll();
          return;
        }
        scheduleColumnResizeAutoScroll(resizeCallback);
      };
      for (let i = 0; i < children.length; i++) {
        const colElm = children[i];
        if (i >= vc.length || !vc[i]) {
          continue;
        }
        if (i < firstResizable || this._options.forceFitColumns && i >= lastResizable) {
          continue;
        }
        const resizeableHandle = createDomElement("div", { className: "slick-resizable-handle", role: "separator", ariaOrientation: "horizontal" }, colElm);
        this._bindingEventService.bind(resizeableHandle, "dblclick", this.handleResizeableDoubleClick.bind(this), {}, "colresizes");
        const applyColumnResize = (targetPageX, resizeElms) => {
          this.columnResizeDragging = true;
          let actualMinWidth;
          const d2 = Math.min(maxPageX, Math.max(minPageX, targetPageX)) - pageX;
          let x2;
          let newCanvasWidthL = 0;
          let newCanvasWidthR = 0;
          const viewportWidth = this.getViewportInnerWidth();
          if (d2 < 0) {
            x2 = d2;
            for (j2 = i; j2 >= 0; j2--) {
              c = vc[j2];
              if (c && c.resizable && !c.hidden) {
                actualMinWidth = Math.max(c.minWidth || 0, this.absoluteColumnMinWidth);
                if (x2 && (c.previousWidth || 0) + x2 < actualMinWidth) {
                  x2 += (c.previousWidth || 0) - actualMinWidth;
                  c.width = actualMinWidth;
                } else {
                  c.width = (c.previousWidth || 0) + x2;
                  x2 = 0;
                }
              }
            }
            for (k2 = 0; k2 <= i; k2++) {
              c = vc[k2];
              if (c && !c.hidden) {
                if (this.hasFrozenColumns() && k2 > this._options.frozenColumn) {
                  newCanvasWidthR += c.width || 0;
                } else {
                  newCanvasWidthL += c.width || 0;
                }
              }
            }
            if (this._options.forceFitColumns) {
              x2 = -d2;
              for (j2 = i + 1; j2 < vc.length; j2++) {
                c = vc[j2];
                if (c && !c.hidden) {
                  if (c.resizable) {
                    if (x2 && c.maxWidth && c.maxWidth - (c.previousWidth || 0) < x2) {
                      x2 -= c.maxWidth - (c.previousWidth || 0);
                      c.width = c.maxWidth;
                    } else {
                      c.width = (c.previousWidth || 0) + x2;
                      x2 = 0;
                    }
                    if (this.hasFrozenColumns() && j2 > this._options.frozenColumn) {
                      newCanvasWidthR += c.width || 0;
                    } else {
                      newCanvasWidthL += c.width || 0;
                    }
                  }
                }
              }
            } else {
              for (j2 = i + 1; j2 < vc.length; j2++) {
                c = vc[j2];
                if (c && !c.hidden) {
                  if (this.hasFrozenColumns() && j2 > this._options.frozenColumn) {
                    newCanvasWidthR += c.width || 0;
                  } else {
                    newCanvasWidthL += c.width || 0;
                  }
                }
              }
            }
            if (this._options.forceFitColumns) {
              x2 = -d2;
              for (j2 = i + 1; j2 < vc.length; j2++) {
                c = vc[j2];
                if (c && !c.hidden && c.resizable) {
                  if (x2 && c.maxWidth && c.maxWidth - (c.previousWidth || 0) < x2) {
                    x2 -= c.maxWidth - (c.previousWidth || 0);
                    c.width = c.maxWidth;
                  } else {
                    c.width = (c.previousWidth || 0) + x2;
                    x2 = 0;
                  }
                }
              }
            }
          } else {
            x2 = d2;
            newCanvasWidthL = 0;
            newCanvasWidthR = 0;
            for (j2 = i; j2 >= 0; j2--) {
              c = vc[j2];
              if (c && !c.hidden && c.resizable) {
                if (x2 && c.maxWidth && c.maxWidth - (c.previousWidth || 0) < x2) {
                  x2 -= c.maxWidth - (c.previousWidth || 0);
                  c.width = c.maxWidth;
                } else {
                  const newWidth = (c.previousWidth || 0) + x2;
                  const resizedCanvasWidthL = this.canvasWidthL + x2;
                  if (this.hasFrozenColumns() && j2 <= this._options.frozenColumn) {
                    if (newWidth > frozenLeftColMaxWidth && resizedCanvasWidthL < viewportWidth - this._options.frozenRightViewportMinWidth) {
                      frozenLeftColMaxWidth = newWidth;
                    }
                    c.width = resizedCanvasWidthL + this._options.frozenRightViewportMinWidth > viewportWidth ? frozenLeftColMaxWidth : newWidth;
                  } else {
                    c.width = newWidth;
                  }
                  x2 = 0;
                }
              }
            }
            for (k2 = 0; k2 <= i; k2++) {
              c = vc[k2];
              if (c && !c.hidden) {
                if (this.hasFrozenColumns() && k2 > this._options.frozenColumn) {
                  newCanvasWidthR += c.width || 0;
                } else {
                  newCanvasWidthL += c.width || 0;
                }
              }
            }
            if (this._options.forceFitColumns) {
              x2 = -d2;
              for (j2 = i + 1; j2 < vc.length; j2++) {
                c = vc[j2];
                if (c && !c.hidden && c.resizable) {
                  actualMinWidth = Math.max(c.minWidth || 0, this.absoluteColumnMinWidth);
                  if (x2 && (c.previousWidth || 0) + x2 < actualMinWidth) {
                    x2 += (c.previousWidth || 0) - actualMinWidth;
                    c.width = actualMinWidth;
                  } else {
                    c.width = (c.previousWidth || 0) + x2;
                    x2 = 0;
                  }
                  if (this.hasFrozenColumns() && j2 > this._options.frozenColumn) {
                    newCanvasWidthR += c.width || 0;
                  } else {
                    newCanvasWidthL += c.width || 0;
                  }
                }
              }
            } else {
              for (j2 = i + 1; j2 < vc.length; j2++) {
                c = vc[j2];
                if (c && !c.hidden) {
                  if (this.hasFrozenColumns() && j2 > this._options.frozenColumn) {
                    newCanvasWidthR += c.width || 0;
                  } else {
                    newCanvasWidthL += c.width || 0;
                  }
                }
              }
            }
          }
          if (this.hasFrozenColumns() && newCanvasWidthL !== this.canvasWidthL) {
            Utils.width(this._headerL, newCanvasWidthL + 1e3);
            Utils.setStyleSize(this._paneHeaderR, "left", newCanvasWidthL);
          }
          this.applyColumnHeaderWidths();
          if (this._options.syncColumnCellResize) {
            this.applyColumnWidths();
          }
          this.updateCanvasWidth();
          if (this._options.autoScrollOnColumnResize && !this._options.forceFitColumns) {
            const columnRight = this.columnPosRight[i];
            const scrollLeft = this._viewportScrollContainerX.scrollLeft;
            const viewportWidth2 = this._viewportScrollContainerX.clientWidth;
            const isLastVisibleColumn = i === vc.length - 1;
            const previousScrollLeft = this._viewportScrollContainerX.scrollLeft;
            if (isLastVisibleColumn) {
              this._isResizingColumn = true;
              const maxScrollLeft = Math.max(0, this._viewportScrollContainerX.scrollWidth - this._viewportScrollContainerX.clientWidth);
              this.scrollToX(maxScrollLeft);
            } else if (columnRight > scrollLeft + viewportWidth2) {
              this._isResizingColumn = true;
              this.scrollToX(columnRight - viewportWidth2);
            }
            resizeAutoScrollDeltaX += this._viewportScrollContainerX.scrollLeft - previousScrollLeft;
          }
          this.triggerEvent(this.onColumnsDrag, {
            triggeredByColumn: resizeElms.resizeableElement,
            resizeHandle: resizeElms.resizeableHandleElement
          });
        };
        this.slickResizableInstances.push(Resizable({
          resizeableElement: colElm,
          resizeableHandleElement: resizeableHandle,
          onResizeStart: (e2, resizeElms) => {
            const targetEvent = e2.touches ? e2.changedTouches[0] : e2;
            if (!this.getEditorLock()?.commitCurrentEdit()) {
              return false;
            }
            pageX = targetEvent.pageX;
            frozenLeftColMaxWidth = 0;
            resizeElms.resizeableElement.classList.add("slick-header-column-active");
            let shrinkLeewayOnRight = null;
            let stretchLeewayOnRight = null;
            for (let pw = 0; pw < children.length; pw++) {
              if (pw < vc.length && vc[pw]) {
                vc[pw].previousWidth = children[pw].offsetWidth;
              }
            }
            if (this._options.forceFitColumns) {
              shrinkLeewayOnRight = 0;
              stretchLeewayOnRight = 0;
              for (j2 = i + 1; j2 < vc.length; j2++) {
                c = vc[j2];
                if (c?.resizable) {
                  if (stretchLeewayOnRight !== null) {
                    if (c.maxWidth) {
                      stretchLeewayOnRight += c.maxWidth - (c.previousWidth || 0);
                    } else {
                      stretchLeewayOnRight = null;
                    }
                  }
                  shrinkLeewayOnRight += (c.previousWidth || 0) - Math.max(c.minWidth || 0, this.absoluteColumnMinWidth);
                }
              }
            }
            let shrinkLeewayOnLeft = 0;
            let stretchLeewayOnLeft = 0;
            for (j2 = 0; j2 <= i; j2++) {
              c = vc[j2];
              if (c?.resizable) {
                if (stretchLeewayOnLeft !== null) {
                  if (c.maxWidth) {
                    stretchLeewayOnLeft += c.maxWidth - (c.previousWidth || 0);
                  } else {
                    stretchLeewayOnLeft = null;
                  }
                }
                shrinkLeewayOnLeft += (c.previousWidth || 0) - Math.max(c.minWidth || 0, this.absoluteColumnMinWidth);
              }
            }
            maxPageX = pageX + Math.min(shrinkLeewayOnRight ?? 1e5, stretchLeewayOnLeft ?? 1e5);
            minPageX = pageX - Math.min(shrinkLeewayOnLeft ?? 1e5, stretchLeewayOnRight ?? 1e5);
            resizeAutoScrollDeltaX = 0;
            autoScrollClientX = isDefinedNumber(targetEvent.clientX) ? targetEvent.clientX : void 0;
            stopColumnResizeAutoScroll();
          },
          onResize: (e2, resizeElms) => {
            const targetEvent = e2.touches ? e2.changedTouches[0] : e2;
            const targetPageX = targetEvent.pageX;
            updateColumnResizeAutoScroll(targetEvent.clientX, targetPageX, (resizePageX) => {
              applyColumnResize(resizePageX, {
                resizeableElement: resizeElms.resizeableElement,
                resizeableHandleElement: resizeableHandle
              });
            });
            applyColumnResize(targetPageX + resizeAutoScrollDeltaX, resizeElms);
          },
          onResizeEnd: (_e, resizeElms) => {
            stopColumnResizeAutoScroll();
            resizeAutoScrollDeltaX = 0;
            this._isResizingColumn = false;
            resizeElms.resizeableElement.classList.remove("slick-header-column-active");
            const triggeredByColumn = resizeElms.resizeableElement.id.replace(this.uid, "");
            if (this.triggerEvent(this.onBeforeColumnsResize, { triggeredByColumn }).getReturnValue() === true) {
              this.applyColumnHeaderWidths();
            }
            let newWidth;
            for (j2 = 0; j2 < vc.length; j2++) {
              c = vc[j2];
              if (c && !c.hidden && children[j2]) {
                newWidth = children[j2].offsetWidth;
                if (c.previousWidth !== newWidth && c.rerenderOnResize) {
                  this.invalidateAllRows();
                }
              }
            }
            this.updateCanvasWidth(true);
            this.render();
            this.scrollToX(this._viewportScrollContainerX.scrollLeft);
            this.triggerEvent(this.onColumnsResized, { triggeredByColumn });
            clearTimeout(this._columnResizeTimer);
            this._columnResizeTimer = setTimeout(() => this.columnResizeDragging = false, this._options.columnResizingDelay);
          }
        }));
      }
    }
    /**
     * Calculates the vertical box sizes (the sum of top/bottom borders and paddings)
     * for a given element by reading its computed style.
     * @param el
     * @returns number
     */
    getVBoxDelta(el) {
      const p = ["borderTopWidth", "borderBottomWidth", "paddingTop", "paddingBottom"];
      const styles3 = getComputedStyle(el);
      let delta = 0;
      p.forEach((val) => delta += Utils.toFloat(styles3[val]));
      return delta;
    }
    setFrozenOptions() {
      this._options.frozenColumn = this._options.frozenColumn >= 0 && this._options.frozenColumn < this.columns.length ? parseInt(this._options.frozenColumn, 10) : -1;
      if (this._options.frozenRow > -1) {
        this.hasFrozenRows = true;
        this.frozenRowsHeight = this._options.frozenRow * this._options.rowHeight;
        const dataLength = this.getDataLength();
        this.actualFrozenRow = this._options.frozenBottom ? dataLength - this._options.frozenRow : this._options.frozenRow;
      } else {
        this.hasFrozenRows = false;
      }
    }
    /** add/remove frozen class to left headers/footer when defined */
    setPaneFrozenClasses() {
      const classAction = this.hasFrozenColumns() ? "add" : "remove";
      for (const elm of [this._paneHeaderL, this._paneTopL, this._paneBottomL]) {
        elm.classList[classAction]("frozen");
      }
    }
    setPaneVisibility() {
      if (this.hasFrozenColumns()) {
        Utils.show(this._paneHeaderR);
        Utils.show(this._paneTopR);
        if (this.hasFrozenRows) {
          Utils.show(this._paneBottomL);
          Utils.show(this._paneBottomR);
        } else {
          Utils.hide(this._paneBottomR);
          Utils.hide(this._paneBottomL);
        }
      } else {
        Utils.hide(this._paneHeaderR);
        Utils.hide(this._paneTopR);
        Utils.hide(this._paneBottomR);
        if (this.hasFrozenRows) {
          Utils.show(this._paneBottomL);
        } else {
          Utils.hide(this._paneBottomR);
          Utils.hide(this._paneBottomL);
        }
      }
    }
    setOverflow() {
      this._viewportTopL.style.overflowX = this.hasFrozenColumns() ? this.hasFrozenRows && !this._options.alwaysAllowHorizontalScroll ? "hidden" : "scroll" : this.hasFrozenRows && !this._options.alwaysAllowHorizontalScroll ? "hidden" : "auto";
      this._viewportTopL.style.overflowY = !this.hasFrozenColumns() && this._options.alwaysShowVerticalScroll ? "scroll" : this.hasFrozenColumns() ? this.hasFrozenRows ? "hidden" : "hidden" : this.hasFrozenRows ? "scroll" : "auto";
      this._viewportTopR.style.overflowX = this.hasFrozenColumns() ? this.hasFrozenRows && !this._options.alwaysAllowHorizontalScroll ? "hidden" : "scroll" : this.hasFrozenRows && !this._options.alwaysAllowHorizontalScroll ? "hidden" : "auto";
      this._viewportTopR.style.overflowY = this._options.alwaysShowVerticalScroll ? "scroll" : this.hasFrozenColumns() ? this.hasFrozenRows ? "scroll" : "auto" : this.hasFrozenRows ? "scroll" : "auto";
      this._viewportBottomL.style.overflowX = this.hasFrozenColumns() ? this.hasFrozenRows && !this._options.alwaysAllowHorizontalScroll ? "scroll" : "auto" : this.hasFrozenRows && !this._options.alwaysAllowHorizontalScroll ? "auto" : "auto";
      this._viewportBottomL.style.overflowY = !this.hasFrozenColumns() && this._options.alwaysShowVerticalScroll ? "scroll" : this.hasFrozenColumns() ? this.hasFrozenRows ? "hidden" : "hidden" : this.hasFrozenRows ? "scroll" : "auto";
      this._viewportBottomR.style.overflowX = this.hasFrozenColumns() ? this.hasFrozenRows && !this._options.alwaysAllowHorizontalScroll ? "scroll" : "auto" : this.hasFrozenRows && !this._options.alwaysAllowHorizontalScroll ? "auto" : "auto";
      this._viewportBottomR.style.overflowY = this._options.alwaysShowVerticalScroll ? "scroll" : this.hasFrozenColumns() ? this.hasFrozenRows ? "auto" : "auto" : this.hasFrozenRows ? "auto" : "auto";
      if (this._options.viewportClass) {
        const viewportClasses = classNameToList(this._options.viewportClass);
        this._viewportTopL.classList.add(...viewportClasses);
        this._viewportTopR.classList.add(...viewportClasses);
        this._viewportBottomL.classList.add(...viewportClasses);
        this._viewportBottomR.classList.add(...viewportClasses);
      }
    }
    setScroller() {
      if (this.hasFrozenColumns()) {
        this._headerScrollContainer = this._headerScrollerR;
        this._headerRowScrollContainer = this._headerRowScrollerR;
        this._footerRowScrollContainer = this._footerRowScrollerR;
        if (this.hasFrozenRows) {
          if (this._options.frozenBottom) {
            this._viewportScrollContainerX = this._viewportBottomR;
            this._viewportScrollContainerY = this._viewportTopR;
          } else {
            this._viewportScrollContainerX = this._viewportScrollContainerY = this._viewportBottomR;
          }
        } else {
          this._viewportScrollContainerX = this._viewportScrollContainerY = this._viewportTopR;
        }
      } else {
        this._headerScrollContainer = this._headerScrollerL;
        this._headerRowScrollContainer = this._headerRowScrollerL;
        this._footerRowScrollContainer = this._footerRowScrollerL;
        if (this.hasFrozenRows) {
          if (this._options.frozenBottom) {
            this._viewportScrollContainerX = this._viewportBottomL;
            this._viewportScrollContainerY = this._viewportTopL;
          } else {
            this._viewportScrollContainerX = this._viewportScrollContainerY = this._viewportBottomL;
          }
        } else {
          this._viewportScrollContainerX = this._viewportScrollContainerY = this._viewportTopL;
        }
      }
    }
    measureCellPaddingAndBorder() {
      const h2 = ["borderLeftWidth", "borderRightWidth", "paddingLeft", "paddingRight"];
      const v2 = ["borderTopWidth", "borderBottomWidth", "paddingTop", "paddingBottom"];
      const header = this._headers[0];
      this.headerColumnWidthDiff = this.headerColumnHeightDiff = 0;
      this.cellWidthDiff = this.cellHeightDiff = 0;
      let el = createDomElement("div", { className: "slick-state-default slick-header-column", style: { visibility: "hidden" }, textContent: "-" }, header);
      let style = getComputedStyle(el);
      if (style.boxSizing !== "border-box") {
        h2.forEach((val) => this.headerColumnWidthDiff += Utils.toFloat(style[val]));
        v2.forEach((val) => this.headerColumnHeightDiff += Utils.toFloat(style[val]));
      }
      el.remove();
      const r2 = createDomElement("div", { className: "slick-row" }, this._canvas[0]);
      el = createDomElement("div", { className: "slick-cell", id: "", style: { visibility: "hidden" }, textContent: "-" }, r2);
      style = getComputedStyle(el);
      if (style.boxSizing !== "border-box") {
        h2.forEach((val) => this.cellWidthDiff += Utils.toFloat(style[val]));
        v2.forEach((val) => this.cellHeightDiff += Utils.toFloat(style[val]));
      }
      r2.remove();
      this.absoluteColumnMinWidth = Math.max(this.headerColumnWidthDiff, this.cellWidthDiff);
    }
    createCssRules() {
      this._style = document.createElement("style");
      if (this._options.nonce) {
        this._style.nonce = this._options.nonce;
      }
      (this._options.shadowRoot || document.head).appendChild(this._style);
      const rowHeight = this._options.rowHeight - this.cellHeightDiff;
      const rules = [
        `.${this.uid} .slick-group-header-column { left: 1000px; }`,
        `.${this.uid} .slick-header-column { left: 1000px; }`,
        `.${this.uid} .slick-top-panel { height: ${this._options.topPanelHeight}px; }`,
        `.${this.uid} .slick-preheader-panel { height: ${this._options.preHeaderPanelHeight}px; }`,
        `.${this.uid} .slick-topheader-panel { height: ${this._options.topHeaderPanelHeight}px; }`,
        `.${this.uid} .slick-headerrow-columns { height: ${this._options.headerRowHeight}px; }`,
        `.${this.uid} .slick-footerrow-columns { height: ${this._options.footerRowHeight}px; }`,
        `.${this.uid} .slick-cell { height: ${rowHeight}px; }`,
        `.${this.uid} .slick-row { height: ${this._options.rowHeight}px; }`
      ];
      const sheet = this._style.sheet;
      if (sheet) {
        rules.forEach((rule) => sheet.insertRule(rule));
        for (let i = 0; i < this.columns.length; i++) {
          if (this.columns[i]) {
            sheet.insertRule(`.${this.uid} .l${i} { }`);
            sheet.insertRule(`.${this.uid} .r${i} { }`);
          }
        }
      } else {
        this.createCssRulesAlternative(rules);
      }
    }
    /** Create CSS rules via template in case the first approach with createElement('style') doesn't work */
    /* v8 ignore next */
    createCssRulesAlternative(rules) {
      const template = document.createElement("template");
      template.innerHTML = '<style type="text/css" rel="stylesheet" />';
      this._style = template.content.firstChild;
      (this._options.shadowRoot || document.head).appendChild(this._style);
      for (let i = 0; i < this.columns.length; i++) {
        if (this.columns[i] && !this.columns[i].hidden) {
          rules.push(`.${this.uid} .l${i} { }`);
          rules.push(`.${this.uid} .r${i} { }`);
        }
      }
      if (this._style.styleSheet) {
        this._style.styleSheet.cssText = rules.join(" ");
      } else {
        this._style.appendChild(document.createTextNode(rules.join(" ")));
      }
    }
    getColumnCssRules(idx) {
      let i;
      if (!this.stylesheet) {
        const sheets = (this._options.shadowRoot || document).styleSheets;
        if (this._options.devMode && typeof this._options.devMode.ownerNodeIndex === "number" && this._options.devMode.ownerNodeIndex >= 0) {
          sheets[this._options.devMode.ownerNodeIndex].ownerNode = this._style;
        }
        for (i = 0; i < sheets.length; i++) {
          const sheet = sheets[i];
          if ((sheet.ownerNode || sheet.owningElement) === this._style) {
            this.stylesheet = sheet;
            break;
          }
        }
        if (!this.stylesheet) {
          throw new Error("SlickGrid Cannot find stylesheet.");
        }
        this.columnCssRulesL = [];
        this.columnCssRulesR = [];
        const cssRules = this.stylesheet.cssRules || this.stylesheet.rules;
        let matches2;
        let columnIdx;
        for (i = 0; i < cssRules.length; i++) {
          const selector = cssRules[i].selectorText;
          if (matches2 = /\.l\d+/.exec(selector)) {
            columnIdx = parseInt(matches2[0].substr(2, matches2[0].length - 2), 10);
            this.columnCssRulesL[columnIdx] = cssRules[i];
          } else if (matches2 = /\.r\d+/.exec(selector)) {
            columnIdx = parseInt(matches2[0].substr(2, matches2[0].length - 2), 10);
            this.columnCssRulesR[columnIdx] = cssRules[i];
          }
        }
      }
      return {
        left: this.columnCssRulesL[idx],
        right: this.columnCssRulesR[idx]
      };
    }
    removeCssRules() {
      this._style?.remove();
      this.stylesheet = null;
    }
    /** Clear all highlight timers that might have been left opened */
    clearAllTimers() {
      this.clearAutoScrollTimer();
      [
        this._columnResizeTimer,
        this._executionBlockTimer,
        this._flashCellTimer,
        this._highlightRowTimer,
        this.h_editorLoader,
        this.h_postrender,
        this.h_postrenderCleanup
      ].forEach((timer) => {
        if (timer) {
          clearTimeout(timer);
        }
      });
    }
    clearAutoScrollTimer() {
      if (this._columnResizeAutoScrollTimer) {
        clearInterval(this._columnResizeAutoScrollTimer);
        this._columnResizeAutoScrollTimer = void 0;
      }
    }
    /**
     * Destroy (dispose) of SlickGrid
     * @param {boolean} shouldDestroyAllElements - do we want to destroy (nullify) all DOM elements as well? This help in avoiding mem leaks
     */
    destroy(shouldDestroyAllElements) {
      this.clearAllTimers();
      this.slickDraggableInstance = this.destroyAllInstances(this.slickDraggableInstance);
      this.slickMouseWheelInstances = this.destroyAllInstances(this.slickMouseWheelInstances);
      this.slickResizableInstances = this.destroyAllInstances(this.slickResizableInstances);
      this.getEditorLock()?.cancelCurrentEdit();
      this.clearInternalDomCaches();
      this.triggerEvent(this.onBeforeDestroy, {});
      this._bindingEventService.unbindAll();
      this._pubSubService?.unsubscribeAll();
      let i = this.plugins.length;
      while (i--) {
        this.unregisterPlugin(this.plugins[i]);
      }
      if (this.sortableSideRightInstance?.el && typeof this.sortableSideRightInstance?.destroy === "function") {
        this.sortableSideRightInstance.destroy();
      }
      if (this.sortableSideLeftInstance?.el && typeof this.sortableSideLeftInstance?.destroy === "function") {
        this.sortableSideLeftInstance.destroy();
      }
      this._boundAncestors.length = 0;
      emptyElement(this._container);
      this.removeCssRules();
      if (shouldDestroyAllElements) {
        destroyAllElementProps(this);
      }
    }
    clearInternalDomCaches() {
      this.activeCellNode = null;
      this.rowsCache = {};
      this.postProcessedRows = {};
      this.postProcessedCleanupQueue.length = 0;
    }
    /**
     * call destroy method, when exists, on all the instance(s) it found
     * @params instances - can be a single instance or a an array of instances
     */
    destroyAllInstances(inputInstances) {
      if (inputInstances) {
        const instances = Array.isArray(inputInstances) ? inputInstances : [inputInstances];
        let instance;
        while (isDefined(instance = instances.pop())) {
          if (instance && typeof instance.destroy === "function") {
            instance.destroy();
          }
        }
      }
      inputInstances = Array.isArray(inputInstances) ? [] : null;
      return inputInstances;
    }
    // Column Autosizing
    autosizeColumns() {
      this.legacyAutosizeColumns();
    }
    /**
     * legacy autosizeColumns() method that was used before 6pac/SlickGrid reimplemented autosizeColumns().
     * We could simply rename the method to autosizeColumns() but let's keep separate for now
     * to make it easier to compare against 6pac/SlickGrid fork
     */
    legacyAutosizeColumns() {
      let i;
      let c;
      let shrinkLeeway = 0;
      let total = 0;
      let prevTotal = 0;
      const widths = [];
      const availWidth = this.getViewportInnerWidth();
      for (i = 0; i < this.columns.length; i++) {
        c = this.columns[i];
        if (!c || c.hidden) {
          widths.push(0);
          continue;
        }
        widths.push(c.width || 0);
        total += c.width || 0;
        if (c.resizable) {
          shrinkLeeway += (c.width || 0) - Math.max(c.minWidth || 0, this.absoluteColumnMinWidth);
        }
      }
      prevTotal = total;
      while (total > availWidth && shrinkLeeway) {
        const shrinkProportion = (total - availWidth) / shrinkLeeway;
        for (i = 0; i < this.columns.length && total > availWidth; i++) {
          c = this.columns[i];
          if (c && !c.hidden) {
            const width = widths[i];
            if (!c.resizable || width <= c.minWidth || width <= this.absoluteColumnMinWidth) {
              continue;
            }
            const absMinWidth = Math.max(c.minWidth, this.absoluteColumnMinWidth);
            let shrinkSize = Math.floor(shrinkProportion * (width - absMinWidth)) || 1;
            shrinkSize = Math.min(shrinkSize, width - absMinWidth);
            total -= shrinkSize;
            shrinkLeeway -= shrinkSize;
            widths[i] -= shrinkSize;
          }
        }
        if (prevTotal <= total) {
          break;
        }
        prevTotal = total;
      }
      prevTotal = total;
      while (total < availWidth) {
        const growProportion = availWidth / total;
        for (i = 0; i < this.columns.length && total < availWidth; i++) {
          c = this.columns[i];
          if (c && !c.hidden) {
            const currentWidth = widths[i];
            let growSize;
            if (!c.resizable || c.maxWidth <= currentWidth) {
              growSize = 0;
            } else {
              growSize = Math.min(Math.floor(growProportion * currentWidth) - currentWidth, c.maxWidth - currentWidth || 1e6) || 1;
            }
            total += growSize;
            widths[i] += total <= availWidth ? growSize : 0;
          }
        }
        if (prevTotal >= total) {
          break;
        }
        prevTotal = total;
      }
      let reRender = false;
      for (i = 0; i < this.columns.length; i++) {
        c = this.columns[i];
        if (c && !c.hidden) {
          if (this.columns[i].rerenderOnResize && this.columns[i].width !== widths[i]) {
            reRender = true;
          }
          this.columns[i].width = widths[i];
        }
      }
      this.reRenderColumns(reRender);
    }
    /**
     * Apply Columns Widths in the UI and optionally invalidate & re-render the columns when specified
     * @param {Boolean} shouldReRender - should we invalidate and re-render the grid?
     */
    reRenderColumns(reRender) {
      this.applyColumnHeaderWidths();
      this.updateCanvasWidth(true);
      this.triggerEvent(this.onAutosizeColumns, { columns: this.columns });
      if (reRender) {
        this.invalidateAllRows();
        this.render();
      }
    }
    getVisibleColumns() {
      return this.columns.filter((c) => !c.hidden);
    }
    // General
    triggerEvent(evt, args, e2) {
      const sed = e2 || new SlickEventData(e2, args);
      const eventArgs = args || {};
      eventArgs.grid = this;
      return evt.notify(eventArgs, sed, this);
    }
    /** Get Editor lock */
    getEditorLock() {
      return this._options.editorLock;
    }
    /** Get Editor Controller */
    getEditController() {
      return this.editController;
    }
    /**
     * Returns the index of a column with a given id. Since columns can be reordered by the user, this can be used to get the column definition independent of the order:
     * @param {String | Number} id A column id.
     */
    getColumnIndex(id) {
      return this.columnsById[id];
    }
    /**
     * Returns the index of a visible column with a given id. Since columns can be reordered by the user, this can be used to get the column definition independent of the order:
     * @param {String | Number} id A column id.
     */
    getVisibleColumnIndex(id) {
      return this.visibleColumnsById[id];
    }
    /** Returns the column object by its ID */
    getColumnById(id) {
      const index2 = this.getColumnIndex(id);
      if (isDefined(index2)) {
        return this.columns[index2];
      }
      return null;
    }
    /** Update any column properties by their finding column by ID */
    updateColumnById(columnId, props, forceColumnUpdate = false) {
      const column = this.getColumnById(columnId);
      if (isDefined(column)) {
        Object.assign(column, props);
      }
      if (forceColumnUpdate) {
        this.updateColumns();
      }
    }
    applyColumnHeaderWidths() {
      if (this.initialized) {
        let columnIndex = 0;
        const vc = this.getVisibleColumns();
        this._headers.forEach((header) => {
          for (let i = 0, ln = header.children.length; i < ln; i++, columnIndex++) {
            const h2 = header.children[i];
            const col = vc[columnIndex] || {};
            let width = (col.width || 0) - this.headerColumnWidthDiff;
            if (this._options.enableGridMenu && i === ln - 1) {
              const totalColumnsWidth = vc.reduce((sum, col2) => sum + (col2.width || 0), 0);
              const canvasWidth = this.getViewportInnerWidth();
              if (totalColumnsWidth >= canvasWidth) {
                width -= this._lastColumnGridMenuCompensation;
                if (!this.scrollbarDimensions?.width) {
                  width -= this._options.gridMenu?.menuWidth ?? 18;
                }
              }
            }
            if (Utils.width(h2) !== width) {
              Utils.width(h2, width);
            }
          }
        });
        this.updateColumnCaches();
      }
    }
    applyColumnWidths() {
      let x2 = 0;
      let w2 = 0;
      let rule;
      for (let i = 0; i < this.columns.length; i++) {
        if (this.columns[i]) {
          w2 = this.columns[i].hidden ? 0 : this.columns[i].width || 0;
          rule = this.getColumnCssRules(i);
          if (rule.left) {
            rule.left.style.left = `${x2}px`;
          }
          if (rule.right) {
            rule.right.style.right = (this._options.frozenColumn !== -1 && i > this._options.frozenColumn ? this.canvasWidthR : this.canvasWidthL) - x2 - w2 + "px";
          }
          if (this._options.frozenColumn !== i && !this.columns[i].hidden) {
            x2 += this.columns[i].width;
          }
        }
        if (this._options.frozenColumn === i) {
          x2 = 0;
        }
      }
    }
    /**
     * Get column header by index
     * @param {Number} idx - column index
     * @returns - column header HTML element
     */
    getColumnHeaderByIndex(idx) {
      return this.getColumnByIndex(idx);
    }
    /** @deprecated @alias `getColumnHeaderByIndex` Get column header by index */
    getColumnByIndex(idx) {
      let result;
      this._headers.every((header) => {
        const length = header.children.length;
        if (idx < length) {
          result = header.children[idx];
          return false;
        }
        idx -= length;
        return true;
      });
      return result;
    }
    /**
     * Get column by index
     * @param {Number} idx - column index
     * @returns - column object
     */
    getColumnByIdx(idx) {
      return this.columns[idx];
    }
    /**
     * Accepts a columnId string and an ascending boolean. Applies a sort glyph in either ascending or descending form to the header of the column. Note that this does not actually sort the column. It only adds the sort glyph to the header.
     * @param {String | Number} columnId
     * @param {Boolean} ascending
     */
    setSortColumn(columnId, ascending) {
      this.setSortColumns([{ columnId, sortAsc: ascending }]);
    }
    /**
     * Accepts an array of objects in the form [ { columnId: [string], sortAsc: [boolean] }, ... ]. When called, this will apply a sort glyph in either ascending or descending form to the header of each column specified in the array. Note that this does not actually sort the column. It only adds the sort glyph to the header
     * @param {ColumnSort[]} cols - column sort
     */
    setSortColumns(cols) {
      this.sortColumns = cols;
      const numberCols = this._options.numberedMultiColumnSort && this.sortColumns.length > 1;
      this._headers.forEach((header) => {
        let indicators = header.querySelectorAll(".slick-header-column-sorted");
        indicators.forEach((indicator) => indicator.classList.remove("slick-header-column-sorted"));
        indicators = header.querySelectorAll(".slick-sort-indicator");
        indicators.forEach((indicator) => indicator.classList.remove("slick-sort-indicator-asc", "slick-sort-indicator-desc"));
        indicators = header.querySelectorAll(".slick-sort-indicator-numbered");
        indicators.forEach((el) => el.textContent = "");
      });
      let i = 1;
      this.sortColumns.forEach((col) => {
        if (!isDefined(col.sortAsc)) {
          col.sortAsc = true;
        }
        const columnIndex = this.getVisibleColumnIndex(col.columnId);
        if (isDefined(columnIndex)) {
          const column = this.getColumnHeaderByIndex(columnIndex);
          if (column) {
            column.classList.add("slick-header-column-sorted");
            let indicator = column.querySelector(".slick-sort-indicator");
            indicator?.classList.add(col.sortAsc ? "slick-sort-indicator-asc" : "slick-sort-indicator-desc");
            if (numberCols) {
              indicator = column.querySelector(".slick-sort-indicator-numbered");
              if (indicator) {
                indicator.textContent = String(i);
              }
            }
          }
        }
        i++;
      });
    }
    /** Get sorted columns **/
    getSortColumns() {
      return this.sortColumns;
    }
    handleSelectedRangesChanged(e2, ranges) {
      const ne = e2.getNativeEvent();
      const selectionMode = ne?.detail?.selectionMode ?? "";
      let addDragHandle = !!ne?.detail?.addDragHandle;
      const selectionType = this.getSelectionModel()?.getOptions()?.selectionType;
      addDragHandle = selectionType === "cell" || selectionType === "mixed";
      const prevSelectedRanges = this.selectedRanges.slice(0);
      this.selectedRanges = ranges;
      if (selectionMode === "REP" && prevSelectedRanges?.length === 1 && this.selectedRanges?.length === 1) {
        const prevSelectedRange = prevSelectedRanges[0];
        const selectedRange = this.selectedRanges[0];
        if (SlickSelectionUtils.copyRangeIsLarger(prevSelectedRange, selectedRange)) {
          this.triggerEvent(this.onDragReplaceCells, { prevSelectedRange, selectedRange });
          this.invalidate();
        }
      }
      const previousSelectedRows = this.selectedRows.slice(0);
      this.selectionBottomRow = -1;
      this.selectionRightCell = -1;
      this.dragReplaceEl.removeEl();
      this.selectedRows = [];
      const hash = {};
      for (let i = 0; i < ranges.length; i++) {
        for (let j2 = ranges[i].fromRow; j2 <= ranges[i].toRow; j2++) {
          if (!hash[j2]) {
            this.selectedRows.push(j2);
            hash[j2] = {};
          }
          for (let k2 = ranges[i].fromCell; k2 <= ranges[i].toCell; k2++) {
            if (this.canCellBeSelected(j2, k2)) {
              hash[j2][this.columns[k2].id] = this._options.selectedCellCssClass;
            }
          }
        }
        if (this.selectionBottomRow < ranges[i].toRow) {
          this.selectionBottomRow = ranges[i].toRow;
        }
        if (this.selectionRightCell < ranges[i].toCell) {
          this.selectionRightCell = ranges[i].toCell;
        }
      }
      this.setCellCssStyles(this._options.selectedCellCssClass || "", hash);
      if (this.selectionBottomRow >= 0 && this.selectionRightCell >= 0 && addDragHandle) {
        const lowerRightCell = this.getCellNode(this.selectionBottomRow, this.selectionRightCell);
        this.dragReplaceEl.createEl(lowerRightCell);
      }
      if (!this.arrayEquals(previousSelectedRows.sort(), this.selectedRows.sort())) {
        const caller = ne?.detail?.caller ?? "click";
        const selectedRowsSet = new Set(this.getSelectedRows());
        const previousSelectedRowsSet = new Set(previousSelectedRows);
        const newSelectedAdditions = Array.from(selectedRowsSet).filter((i) => !previousSelectedRowsSet.has(i));
        const newSelectedDeletions = Array.from(previousSelectedRowsSet).filter((i) => !selectedRowsSet.has(i));
        this.triggerEvent(this.onSelectedRowsChanged, {
          rows: this.getSelectedRows(),
          previousSelectedRows,
          caller,
          changedSelectedRows: newSelectedAdditions,
          changedUnselectedRows: newSelectedDeletions
        }, e2);
      }
    }
    // compare 2 primitive type arrays, do not use to compare object arrays)
    arrayEquals(arr1, arr2) {
      return Array.isArray(arr1) && Array.isArray(arr2) && arr2.toString() === arr1.toString();
    }
    /** Returns an array of column definitions. */
    getColumns() {
      return this.columns;
    }
    updateColumnCaches() {
      this.columnPosLeft = [];
      this.columnPosRight = [];
      let x2 = 0;
      for (let i = 0, ii = this.columns.length; i < ii; i++) {
        if (this.columns[i]) {
          this.columnPosLeft[i] = x2;
          this.columnPosRight[i] = x2 + (this.columns[i].hidden ? 0 : this.columns[i].width || 0);
          if (this._options.frozenColumn === i) {
            x2 = 0;
          } else {
            x2 += this.columns[i].hidden ? 0 : this.columns[i].width || 0;
          }
        }
      }
    }
    updateColumnProps() {
      this.columnsById = {};
      this.visibleColumnsById = {};
      for (let i = 0; i < this.columns.length; i++) {
        let m = this.columns[i] || {};
        if (m.width) {
          m.widthRequest = m.width;
        }
        if (this._options.mixinDefaults) {
          Utils.applyDefaults(m, this._columnDefaults);
        } else {
          m = this.columns[i] = extend({}, this._columnDefaults, m);
        }
        this.columnsById[m.id] = i;
        if (m.minWidth && (m.width || 0) < m.minWidth) {
          m.width = m.minWidth;
        }
        if (m.maxWidth && (m.width || 0) > m.maxWidth) {
          m.width = m.maxWidth;
        }
      }
      this.getVisibleColumns().forEach((col, idx) => {
        this.visibleColumnsById[col.id] = idx;
      });
    }
    /**
     * Sets grid columns. Column headers will be recreated and all rendered rows will be removed. To rerender the grid (if necessary), call render().
     * @param {Column[]} newColumns An array of column definitions.
     * @param {boolean} [waitNextCycle=false] - should we wait for a microtask cycle before updating column headers
     */
    setColumns(newColumns, waitNextCycle = false) {
      this.triggerEvent(this.onBeforeSetColumns, { previousColumns: this.columns, newColumns, grid: this });
      if (!this.validateColumnFreeze(void 0, true)) {
        return;
      }
      this.columns = newColumns;
      this._container.setAttribute("aria-colcount", this.columns.length.toString());
      const updateCols = () => {
        this.updateColumns();
        this.triggerEvent(this.onAfterSetColumns, { newColumns, grid: this });
      };
      waitNextCycle ? queueMicrotaskPolyfill(() => updateCols()) : updateCols();
    }
    /** Update columns for when a hidden property has changed but the column list itself has not changed. */
    updateColumns() {
      this.triggerEvent(this.onBeforeUpdateColumns, { columns: this.columns, grid: this });
      this.updateColumnsInternal();
      this.triggerEvent(this.onAfterUpdateColumns, { columns: this.columns, grid: this });
    }
    updateColumnsInternal() {
      this.updateColumnProps();
      this.updateColumnCaches();
      if (this.initialized) {
        this.setPaneFrozenClasses();
        this.setPaneVisibility();
        this.setOverflow();
        this.invalidateAllRows();
        this.createColumnHeaders();
        this.createColumnFooter();
        this.removeCssRules();
        this.createCssRules();
        this.resizeCanvas();
        this.updateCanvasWidth();
        this.applyColumnWidths();
        this.handleScroll();
        this.getSelectionModel()?.refreshSelections();
      }
    }
    /** Returns an object containing all of the Grid options set on the grid. See a list of Grid Options here.  */
    getOptions() {
      return this._options;
    }
    /**
     * Get the Column ID of the currently frozen column or `null` when not frozen
     * @returns {String|Number|null} Frozen Column ID
     */
    getFrozenColumnId() {
      const frozenColIndex = this.getFrozenColumnIdx();
      if (frozenColIndex >= 0 && this.columns[frozenColIndex]) {
        return this.columns[frozenColIndex].id;
      }
      return null;
    }
    getFrozenColumnIdx() {
      return this._options.frozenColumn ?? -1;
    }
    /**
     * Extends grid options with a given hash. If an there is an active edit, the grid will attempt to commit the changes and only continue if the attempt succeeds.
     * @param {Object} options - an object with configuration options.
     * @param {Boolean} [suppressRender] - do we want to supress the grid re-rendering? (defaults to false)
     * @param {Boolean} [suppressColumnSet] - do we want to supress the columns set, via "setColumns()" method? (defaults to false)
     * @param {Boolean} [suppressSetOverflow] - do we want to suppress the call to `setOverflow`
     */
    setOptions(newOptions, suppressRender, suppressColumnSet, suppressSetOverflow) {
      this.prepareForOptionsChange();
      if (this._options.enableAddRow !== newOptions.enableAddRow) {
        this.invalidateRow(this.getDataLength());
      }
      if (newOptions.frozenColumn !== void 0 && newOptions.frozenColumn >= 0) {
        this._prevFrozenColumnIdx = this.getFrozenColumnIdx();
        if (!suppressColumnSet) {
          this._invalidfrozenAlerted = false;
        }
        if (this.validateColumnFreezeWidth(newOptions.frozenColumn)) {
          this.getViewports().forEach((vp) => vp.scrollLeft = 0);
          this.handleScroll();
        } else {
          newOptions.frozenColumn = this._prevFrozenColumnIdx < newOptions.frozenColumn ? this._prevFrozenColumnIdx : -1;
        }
      }
      const originalOptions = extend(true, {}, this._options);
      this._options = extend(true, this._options, newOptions);
      this.triggerEvent(this.onSetOptions, { optionsBefore: originalOptions, optionsAfter: this._options });
      if (this.shouldRefreshFormattedCachePlanner(newOptions)) {
        this.syncDataViewFormattedCachePlanner(true);
      }
      this.internal_setOptions(suppressRender, suppressColumnSet, suppressSetOverflow);
    }
    /**
     * If option.mixinDefaults is true then external code maintains a reference to the options object. In this case there is no need
     * to call setOptions() - changes can be made directly to the object. However setOptions() also performs some recalibration of the
     * grid in reaction to changed options. activateChangedOptions call the same recalibration routines as setOptions() would have.
     * @param {Boolean} [suppressRender] - do we want to supress the grid re-rendering? (defaults to false)
     * @param {Boolean} [suppressColumnSet] - do we want to supress the columns set, via "setColumns()" method? (defaults to false)
     * @param {Boolean} [suppressSetOverflow] - do we want to suppress the call to `setOverflow`
     */
    activateChangedOptions(suppressRender, suppressColumnSet, suppressSetOverflow) {
      this.prepareForOptionsChange();
      this.invalidateRow(this.getDataLength());
      this.triggerEvent(this.onActivateChangedOptions, { options: this._options });
      this.syncDataViewFormattedCachePlanner(true);
      this.internal_setOptions(suppressRender, suppressColumnSet, suppressSetOverflow);
    }
    prepareForOptionsChange() {
      if (!this.getEditorLock()?.commitCurrentEdit()) {
        return;
      }
      this.makeActiveCellNormal();
    }
    internal_setOptions(suppressRender, suppressColumnSet, suppressSetOverflow) {
      if (this._options.showColumnHeader !== void 0) {
        this.setColumnHeaderVisibility(this._options.showColumnHeader);
      }
      this.validateAndEnforceOptions();
      this.setFrozenOptions();
      if (this._options.frozenBottom !== void 0) {
        this.enforceFrozenRowHeightRecalc = true;
      }
      this._viewport.forEach((view) => {
        view.style.overflowY = this._options.autoHeight ? "hidden" : "auto";
      });
      if (!suppressRender) {
        this.render();
      }
      this.setScroller();
      if (!suppressSetOverflow) {
        this.setOverflow();
      }
      if (!suppressColumnSet) {
        this.setColumns(this.columns);
      }
      if (this._options.enableMouseWheelScrollHandler && this._viewport && (!this.slickMouseWheelInstances || this.slickMouseWheelInstances.length === 0)) {
        this._viewport.forEach((view) => {
          this.slickMouseWheelInstances.push(MouseWheel({
            element: view,
            onMouseWheel: this.handleMouseWheel.bind(this)
          }));
        });
      } else if (this._options.enableMouseWheelScrollHandler === false) {
        this.destroyAllInstances(this.slickMouseWheelInstances);
      }
    }
    validateAndEnforceOptions() {
      if (this._options.autoHeight) {
        this._options.leaveSpaceForNewRows = false;
      }
      if (!this.validateColumnFreezeWidth(this._options.frozenColumn)) {
        this._options.frozenColumn = this._prevFrozenColumnIdx < this._options.frozenColumn ? this._prevFrozenColumnIdx : -1;
      }
    }
    /**
     * Sets a new source for databinding and removes all rendered rows. Note that this doesn't render the new rows - you can follow it with a call to render() to do that.
     * @param {CustomDataView|Array<*>} newData New databinding source using a regular JavaScript array.. or a custom object exposing getItem(index) and getLength() functions.
     * @param {Number} [scrollToTop] If true, the grid will reset the vertical scroll position to the top of the grid.
     */
    setData(newData, scrollToTop) {
      this.data = newData;
      this.syncDataViewFormattedCachePlanner();
      this.invalidateAllRows();
      this.updateRowCount();
      if (scrollToTop) {
        this.scrollTo(0);
      }
    }
    /** Returns an array of every data object, unless you're using DataView in which case it returns a DataView object. */
    getData() {
      return this.data;
    }
    /** Returns the size of the databinding source. */
    getDataLength() {
      if (this.data.getLength) {
        return this.data.getLength();
      }
      return this.data?.length || 0;
    }
    getDataLengthIncludingAddNew() {
      return this.getDataLength() + (!this._options.enableAddRow ? 0 : !this.pagingActive || this.pagingIsLastPage ? 1 : 0);
    }
    /**
     * Returns the databinding item at a given position.
     * @param {Number} index Item row index.
     */
    getDataItem(i) {
      if (this.data.getItem) {
        return this.data.getItem(i);
      }
      return this.data[i];
    }
    /**
     * Returns item metadata by a row index when it exists
     * @param {Number} row
     * @returns {ItemMetadata | null}
     */
    getItemMetadaWhenExists(row) {
      return "getItemMetadata" in this.data ? this.data.getItemMetadata(row) : null;
    }
    /** Get Top Panel DOM element */
    getTopPanel() {
      return this._topPanels[0];
    }
    /** Get Top Panels (left/right) DOM element */
    getTopPanels() {
      return this._topPanels;
    }
    /** Are we using a DataView? */
    hasDataView() {
      return !Array.isArray(this.data);
    }
    shouldRefreshFormattedCachePlanner(newOptions) {
      return "enableFormattedDataCache" in newOptions || "excelExportOptions" in newOptions || "textExportOptions" in newOptions || "pdfExportOptions" in newOptions;
    }
    syncDataViewFormattedCachePlanner(forceRefresh = false) {
      if (!this.hasDataView() || !this._options.enableFormattedDataCache) {
        return;
      }
      const dataView = this.getData();
      if (typeof dataView.setFormattedDataCachePlanner === "function") {
        dataView.setFormattedDataCachePlanner(this.formattedDataCachePlanner, forceRefresh);
      }
    }
    togglePanelVisibility(option2, container, visible) {
      if (this._options[option2] !== visible) {
        this._options[option2] = visible;
        if (visible) {
          Utils.show(container);
        } else {
          Utils.hide(container);
        }
        this.resizeCanvas();
      }
    }
    /**
     * Set the Top Panel Visibility
     * @param {Boolean} [visible] - optionally set if top panel is visible or not
     */
    setTopPanelVisibility(visible) {
      this.togglePanelVisibility("showTopPanel", this._topPanelScrollers, visible);
    }
    /**
     * Set the Header Row Visibility
     * @param {Boolean} [visible] - optionally set if header row panel is visible or not
     */
    setHeaderRowVisibility(visible) {
      this.togglePanelVisibility("showHeaderRow", this._headerRowScroller, visible);
    }
    /**
     * Set the Column Header Visibility
     * @param {Boolean} [visible] - optionally set if column header is visible or not
     */
    setColumnHeaderVisibility(visible) {
      this.togglePanelVisibility("showColumnHeader", this._headerScroller, visible);
    }
    /**
     * Set the Footer Visibility
     * @param {Boolean} [visible] - optionally set if footer row panel is visible or not
     */
    setFooterRowVisibility(visible) {
      this.togglePanelVisibility("showFooterRow", this._footerRowScroller, visible);
    }
    /**
     * Set the Pre-Header Visibility
     * @param {Boolean} [visible] - optionally set if pre-header panel is visible or not
     */
    setPreHeaderPanelVisibility(visible) {
      this.togglePanelVisibility("showPreHeaderPanel", [this._preHeaderPanelScroller, this._preHeaderPanelScrollerR], visible);
    }
    /**
     * Set the Top-Header Visibility
     * @param {Boolean} [visible] - optionally set if top-header panel is visible or not
     */
    setTopHeaderPanelVisibility(visible) {
      this.togglePanelVisibility("showTopHeaderPanel", this._topHeaderPanelScroller, visible);
    }
    /** Get Grid Canvas Node DOM Element */
    getContainerNode() {
      return this._container;
    }
    // Rendering / Scrolling
    getRowHeight() {
      return this._options.rowHeight;
    }
    getRowTop(row) {
      return Math.round(this._options.rowHeight * row - this.offset);
    }
    getRowBottom(row) {
      return this.getRowTop(row) + this._options.rowHeight;
    }
    getRowFromPosition(y2) {
      return Math.floor((y2 + this.offset) / this._options.rowHeight);
    }
    /**
     * Scroll to an Y coordinate position in the grid
     * @param {Number} y
     */
    scrollTo(y2) {
      y2 = Math.max(y2, 0);
      y2 = Math.min(y2, (this.th || 0) - Utils.height(this._viewportScrollContainerY) + (this.viewportHasHScroll || this.hasFrozenColumns() ? this.scrollbarDimensions?.height || 0 : 0));
      const oldOffset = this.offset;
      this.offset = Math.round(this.page * (this.cj || 0));
      this.page = Math.min((this.n || 0) - 1, Math.floor(y2 / (this.ph || 0)));
      const newScrollTop = y2 - this.offset;
      if (this.offset !== oldOffset) {
        const range2 = this.getVisibleRange(newScrollTop);
        this.cleanupRows(range2);
        this.updateRowPositions();
      }
      if (this.prevScrollTop !== newScrollTop) {
        this.vScrollDir = this.prevScrollTop + oldOffset < newScrollTop + this.offset ? 1 : -1;
        this.lastRenderedScrollTop = this.scrollTop = this.prevScrollTop = newScrollTop;
        if (this.hasFrozenColumns()) {
          this._viewportTopL.scrollTop = newScrollTop;
        }
        if (this.hasFrozenRows) {
          this._viewportBottomL.scrollTop = this._viewportBottomR.scrollTop = newScrollTop;
        }
        if (this._viewportScrollContainerY) {
          this._viewportScrollContainerY.scrollTop = newScrollTop;
        }
        this.triggerEvent(this.onViewportChanged, {});
      }
    }
    /**
     * Scroll to an X coordinate position in the grid
     * @param {Number} x
     */
    scrollToX(x2) {
      this._viewportScrollContainerX.scrollLeft = x2;
      this._headerScrollContainer.scrollLeft = x2;
      this._topPanelScrollers[0].scrollLeft = x2;
      if (this._options.createFooterRow) {
        this._footerRowScrollContainer.scrollLeft = x2;
      }
      if (this._options.createPreHeaderPanel) {
        if (this.hasFrozenColumns()) {
          this._preHeaderPanelScrollerR.scrollLeft = x2;
        } else {
          this._preHeaderPanelScroller.scrollLeft = x2;
        }
      }
      if (this._options.createTopHeaderPanel) {
        this._topHeaderPanelScroller.scrollLeft = x2;
      }
      if (this.hasFrozenColumns()) {
        if (this.hasFrozenRows) {
          this._viewportTopR.scrollLeft = x2;
        }
        this._headerRowScrollerR.scrollLeft = x2;
      } else {
        if (this.hasFrozenRows) {
          this._viewportTopL.scrollLeft = x2;
        }
        this._headerRowScrollerL.scrollLeft = x2;
      }
    }
    defaultFormatter(_row, _cell, value) {
      if (!isDefined(value)) {
        return "";
      }
      return (value + "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
    }
    getFormatter(row, column) {
      const rowMetadata = this.data?.getItemMetadata?.(row);
      const columnOverrides = rowMetadata?.columns && (rowMetadata.columns[column.id] || rowMetadata.columns[this.getColumnIndex(column.id)]);
      const formatter = columnOverrides?.formatter || rowMetadata?.formatter || column.formatter || this._options.formatterFactory?.getFormatter(column) || this._options.defaultFormatter;
      const canUseDisplayCache = this._options.enableFormattedDataCache && !rowMetadata?.formatter && !columnOverrides?.formatter && this.hasDataView();
      const dataView = canUseDisplayCache ? this.getData() : void 0;
      let resolvedFormatter = formatter;
      if (typeof dataView?.getCellDisplayValue === "function") {
        resolvedFormatter = (rowIdx, cell, value, columnDef, dataContext, grid) => {
          const cached = dataView.getCellDisplayValue(rowIdx, String(columnDef.id), dataContext);
          const resolvedValue = cached !== void 0 ? cached : formatter(rowIdx, cell, value, columnDef, dataContext, grid);
          return resolvedValue;
        };
      }
      return resolvedFormatter;
    }
    getEditor(row, cell) {
      const column = this.columns[cell];
      const rowMetadata = this.getItemMetadaWhenExists(row);
      const columnMetadata = rowMetadata?.columns;
      if (columnMetadata?.[column.id]?.editorClass !== void 0) {
        return columnMetadata[column.id].editorClass;
      }
      if (columnMetadata?.[cell]?.editorClass !== void 0) {
        return columnMetadata[cell].editorClass;
      }
      return column.editorClass || this._options?.editorFactory?.getEditor(column);
    }
    getDataItemValueForColumn(item, columnDef) {
      if (this._options.dataItemColumnValueExtractor) {
        return this._options.dataItemColumnValueExtractor(item, columnDef);
      }
      return item[columnDef.field];
    }
    appendRowHtml(divArrayL, divArrayR, row, range2, dataLength) {
      const d2 = this.getDataItem(row);
      const dataLoading = row < dataLength && !d2;
      let rowCss = "slick-row" + (this.hasFrozenRows && row <= this._options.frozenRow ? " frozen" : "") + (dataLoading ? " loading" : "") + (row === this.activeRow && this._options.showCellSelection ? " active" : "") + (row % 2 === 1 ? " odd" : " even");
      if (!d2) {
        rowCss += ` ${this._options.addNewRowCssClass}`;
      }
      const metadata = this.getItemMetadaWhenExists(row);
      if (metadata?.cssClasses) {
        rowCss += ` ${metadata.cssClasses}`;
      }
      const rowDiv = createDomElement("div", {
        className: `ui-widget-content ${rowCss}`,
        role: "row",
        dataset: { row: `${row}` }
      });
      const frozenRowOffset = this.getFrozenRowOffset(row);
      const topOffset = this.getRowTop(row) - frozenRowOffset;
      if (this._options.rowTopOffsetRenderType === "transform") {
        rowDiv.style.transform = `translateY(${topOffset}px)`;
      } else {
        rowDiv.style.top = `${topOffset}px`;
      }
      let rowDivR;
      divArrayL.push(rowDiv);
      if (this.hasFrozenColumns()) {
        rowDivR = rowDiv.cloneNode(true);
        divArrayR.push(rowDivR);
      }
      const columnCount = this.columns.length;
      let columnData;
      let colspan;
      let rowspan;
      let m;
      let isRenderCell = true;
      let isFullColspan = false;
      for (let i = 0, ii = columnCount; i < ii; i++) {
        isRenderCell = true;
        m = this.columns[i];
        if (m && (!m.hidden || metadata?.isGroup)) {
          colspan = 1;
          rowspan = 1;
          columnData = null;
          if (metadata?.columns) {
            columnData = metadata.columns[m.id] || metadata.columns[i];
            colspan = columnData?.colspan || 1;
            rowspan = columnData?.rowspan || 1;
            if (colspan === "*") {
              isFullColspan = true;
              colspan = ii - i;
            }
            if (rowspan > dataLength - row) {
              rowspan = dataLength - row;
            }
          }
          if (!this._options.enableCellRowSpan && rowspan > 1) {
            console.warn('[SlickGrid] Cell "rowspan" is an opt-in grid option because of its small perf hit, you must enable it via the "enableCellRowSpan" grid option.');
          }
          let ncolspan = colspan;
          if (this.getParentRowSpanByCell(row, i)) {
            continue;
          }
          if (this.columnPosRight[Math.min(ii - 1, i + ncolspan - 1)] > range2.leftPx) {
            if (!m.alwaysRenderColumn && this.columnPosLeft[i] > range2.rightPx) {
              isRenderCell = false;
            }
            if (!isFullColspan && this._options.spreadHiddenColspan) {
              ncolspan = this.increaseHiddenColspan(ncolspan, i);
            }
            if (isRenderCell) {
              const targetedRowDiv = this.hasFrozenColumns() && i > this._options.frozenColumn ? rowDivR : rowDiv;
              this.appendCellHtml(targetedRowDiv, row, i, ncolspan, rowspan, columnData, d2);
            }
          } else if (m.alwaysRenderColumn || this.hasFrozenColumns() && i <= this._options.frozenColumn) {
            this.appendCellHtml(rowDiv, row, i, ncolspan, rowspan, columnData, d2);
          }
          if (ncolspan > 1) {
            i += ncolspan - 1;
          }
        }
      }
    }
    appendCellHtml(divRow, row, cell, colspan, rowspan, columnMetadata, item) {
      const m = this.columns[cell];
      let cellCss = `slick-cell l${cell} r${Math.min(this.columns.length - 1, cell + colspan - 1)}` + (m.cssClass ? ` ${m.cssClass}` : "") + (rowspan > 1 ? " rowspan" : "") + (columnMetadata?.cssClass ? ` ${columnMetadata.cssClass}` : "");
      if (this.hasFrozenColumns() && cell <= this._options.frozenColumn) {
        cellCss += " frozen";
      }
      if (row === this.activeRow && cell === this.activeCell && this._options.showCellSelection) {
        cellCss += " active";
      }
      Object.keys(this.cellCssClasses).forEach((key) => {
        if (this.cellCssClasses[key][row]?.[m.id]) {
          cellCss += ` ${this.cellCssClasses[key][row][m.id]}`;
        }
      });
      let value = null;
      let formatterResult = "";
      if (item) {
        value = this.getDataItemValueForColumn(item, m);
        formatterResult = this.getFormatter(row, m)(row, cell, value, m, item, this);
        if (formatterResult === null || formatterResult === void 0) {
          formatterResult = "";
        }
      }
      const evt = this.triggerEvent(this.onBeforeAppendCell, { row, cell, value, dataContext: item });
      const appendCellResult = evt.getReturnValue();
      let addlCssClasses = typeof appendCellResult === "string" ? appendCellResult : "";
      if (formatterResult?.addClasses) {
        addlCssClasses += classNameToList((addlCssClasses ? " " : "") + formatterResult.addClasses).join(" ");
      }
      const toolTipText = formatterResult?.toolTip ? `${formatterResult.toolTip}` : "";
      const cellDiv = createDomElement("div", {
        className: classNameToList(`${cellCss} ${addlCssClasses || ""}`).join(" "),
        role: "gridcell",
        tabIndex: -1
      });
      cellDiv.setAttribute("aria-describedby", this.uid + m.id);
      if (toolTipText) {
        cellDiv.setAttribute("title", toolTipText);
      }
      const cellHeight = this.getCellHeight(row, rowspan);
      if (rowspan > 1 && cellHeight !== this._options.rowHeight - this.cellHeightDiff) {
        cellDiv.style.height = `${cellHeight || 0}px`;
      }
      if (m.hasOwnProperty("cellAttrs") && m.cellAttrs instanceof Object) {
        Object.keys(m.cellAttrs).forEach((key) => {
          if (m.cellAttrs.hasOwnProperty(key)) {
            cellDiv.setAttribute(key, m.cellAttrs[key]);
          }
        });
      }
      if (item) {
        const cellResult = isPrimitiveOrHTML(formatterResult) ? formatterResult : formatterResult.html || formatterResult.text;
        applyHtmlToElement(cellDiv, cellResult, this._options);
        const selectionType = this.getSelectionModel()?.getOptions()?.selectionType;
        const addDragHandle = selectionType === "cell" || selectionType === "mixed";
        if (row === this.selectionBottomRow && cell === this.selectionRightCell && this._options.showCellSelection && addDragHandle) {
          this.dragReplaceEl.createEl(cellDiv);
        }
      }
      divRow.appendChild(cellDiv);
      if (formatterResult.insertElementAfterTarget) {
        insertAfterElement(cellDiv, formatterResult.insertElementAfterTarget);
      }
      this.rowsCache[row].cellRenderQueue.push(cell);
      this.rowsCache[row].cellColSpans[cell] = colspan;
    }
    cleanupRows(rangeToKeep) {
      const mandatoryRows = /* @__PURE__ */ new Set();
      if (this._options.enableCellRowSpan) {
        for (let i = rangeToKeep.top, ln = rangeToKeep.bottom; i <= ln; i++) {
          const parentRowSpan = this.getRowSpanIntersect(i);
          if (parentRowSpan !== null) {
            mandatoryRows.add(parentRowSpan);
          }
        }
      }
      Object.keys(this.rowsCache).forEach((rowId) => {
        if (this.rowsCache) {
          let i = +rowId;
          let removeFrozenRow = true;
          if (this.hasFrozenRows && (this._options.frozenBottom && i >= this.actualFrozenRow || // Frozen bottom rows
          !this._options.frozenBottom && i <= this.actualFrozenRow)) {
            removeFrozenRow = false;
          }
          if ((i = parseInt(rowId, 10)) !== this.activeRow && (i < rangeToKeep.top || i > rangeToKeep.bottom) && removeFrozenRow && !mandatoryRows.has(i)) {
            this.removeRowFromCache(i);
          }
        }
      });
      if (this._options.enableAsyncPostRenderCleanup) {
        this.startPostProcessingCleanup();
      }
    }
    /**
     * from a row number, return any column indexes that intersected with the grid row including the cell
     * @param {Number} row - grid row index
     */
    getRowSpanColumnIntersects(row) {
      return this.getRowSpanIntersection(row, "columns");
    }
    /**
     * from a row number, check if the rowspan is intersecting with any rowspan and return it when found,
     * otherwise return `null` when nothing is found or when the rowspan feature is disabled.
     * @param {Number} row - grid row index
     */
    getRowSpanIntersect(row) {
      return this.getRowSpanIntersection(row);
    }
    getRowSpanIntersection(row, outputType) {
      const columnIntersects = [];
      let rowStartIntersect = null;
      for (let col = 0, cln = this.columns.length; col < cln; col++) {
        const rmeta = this._colsWithRowSpanCache[col];
        if (rmeta) {
          for (const range2 of Array.from(rmeta)) {
            const [start, end] = range2.split(":").map(Number);
            if (row >= start && row <= end) {
              if (outputType === "columns") {
                columnIntersects.push(col);
              } else {
                rowStartIntersect = start;
                break;
              }
            }
          }
        }
      }
      return outputType === "columns" ? columnIntersects : rowStartIntersect;
    }
    /**
     * Returns the parent rowspan details when child cell are spanned from a rowspan or `null` when it's not spanned.
     * By default it will exclude the parent cell that holds the rowspan, and return `null`, that initiated the rowspan unless the 3rd argument is disabled.
     * The exclusion is helpful to find out when we're dealing with a child cell of a rowspan
     * @param {Number} row - grid row index
     * @param {Number} cell - grid cell/column index
     * @param {Boolean} [excludeParentRow] - should we exclude the parent who initiated the rowspan in the search (defaults to true)?
     */
    getParentRowSpanByCell(row, cell, excludeParentRow = true) {
      let spanDetail = null;
      const rowspanRange = this._colsWithRowSpanCache[cell] || /* @__PURE__ */ new Set();
      for (const range2 of Array.from(rowspanRange)) {
        const [start, end] = range2.split(":").map(Number);
        const startCondition = excludeParentRow ? row > start : row >= start;
        if (startCondition && row <= end) {
          spanDetail = { start, end, range: range2 };
          break;
        }
      }
      return spanDetail;
    }
    /**
     * Remap all the rowspan metadata by looping through all dataset rows and keep a cache of rowspan by column indexes
     * For example:
     *  1- if 2nd row of the 1st column has a metadata.rowspan of 3 then the cache will be: `{ 0: '1:4' }`
     *  2- if 2nd row if the 1st column has a metadata.rowspan of 3 AND a colspan of 2 then the cache will be: `{ 0: '1:4', 1: '1:4' }`
     */
    remapAllColumnsRowSpan() {
      const ln = this.getDataLength();
      if (ln > 0) {
        this._colsWithRowSpanCache = {};
        for (let row = 0; row < ln; row++) {
          this.remapRowSpanMetadataByRow(row);
        }
        this._rowSpanIsCached = true;
      }
    }
    remapRowSpanMetadataByRow(row) {
      const colMeta = this.getItemMetadaWhenExists(row);
      if (colMeta?.columns) {
        Object.keys(colMeta.columns).forEach((col) => {
          const colIdx = +col;
          if (this.columns[colIdx] && !this.columns[colIdx].hidden) {
            const columnMeta = colMeta.columns[colIdx];
            const colspan = +(columnMeta?.colspan || 1);
            const rowspan = +(columnMeta?.rowspan || 1);
            this.remapRowSpanMetadata(row, colIdx, colspan, rowspan);
          }
        });
      }
    }
    remapRowSpanMetadata(row, cell, colspan, rowspan) {
      if (rowspan > 1) {
        const rspan = `${row}:${row + rowspan - 1}`;
        this._colsWithRowSpanCache[cell] ??= /* @__PURE__ */ new Set();
        this._colsWithRowSpanCache[cell].add(rspan);
        if (colspan > 1) {
          for (let i = 1; i < colspan; i++) {
            this._colsWithRowSpanCache[cell + i] ??= /* @__PURE__ */ new Set();
            this._colsWithRowSpanCache[cell + i].add(rspan);
          }
        }
      }
    }
    /** Invalidate all grid rows and re-render the visible grid rows */
    invalidate() {
      this.updateRowCount();
      this.invalidateAllRows();
      this.render();
    }
    /** Invalidate all grid rows */
    invalidateAllRows() {
      if (this.currentEditor) {
        this.makeActiveCellNormal();
      }
      if (typeof this.rowsCache === "object") {
        Object.keys(this.rowsCache).forEach((row) => {
          if (this.rowsCache) {
            this.removeRowFromCache(+row);
          }
        });
      }
      if (this._options.enableAsyncPostRenderCleanup) {
        this.startPostProcessingCleanup();
      }
    }
    /**
     * Invalidate a specific set of row numbers
     * @param {Number[]} rows
     */
    invalidateRows(rows) {
      if (!rows || !rows.length) {
        return;
      }
      let row;
      this.vScrollDir = 0;
      const rl = rows.length;
      const invalidatedRows = /* @__PURE__ */ new Set();
      const requiredRemapRows = /* @__PURE__ */ new Set();
      const isRowSpanFullRemap = rows.length > this._options.maxPartialRowSpanRemap || rows.length === this.getDataLength() || this._prevInvalidatedRowsCount + rows.length === this.getDataLength();
      for (let i = 0; i < rl; i++) {
        row = rows[i];
        if (this.currentEditor && this.activeRow === row) {
          this.makeActiveCellNormal();
        }
        if (this.rowsCache[row]) {
          this.removeRowFromCache(row);
        }
        if (this._options.enableCellRowSpan && !isRowSpanFullRemap) {
          invalidatedRows.add(row);
          const parentRowSpan = this.getRowSpanIntersect(row);
          if (parentRowSpan !== null) {
            invalidatedRows.add(parentRowSpan);
          }
        }
      }
      if (this._options.enableCellRowSpan && !isRowSpanFullRemap) {
        for (const ir of Array.from(invalidatedRows)) {
          const colIdxs = this.getRowSpanColumnIntersects(ir);
          for (const cidx of colIdxs) {
            const prs = this.getParentRowSpanByCell(ir, cidx);
            if (prs && this._colsWithRowSpanCache[cidx]) {
              this._colsWithRowSpanCache[cidx].delete(prs.range);
              requiredRemapRows.add(prs.range.split(":").map(Number)[0]);
            }
          }
        }
        for (const row2 of Array.from(requiredRemapRows)) {
          this.remapRowSpanMetadataByRow(row2);
        }
      }
      if (this._options.enableAsyncPostRenderCleanup) {
        this.startPostProcessingCleanup();
      }
      this._prevInvalidatedRowsCount = rows.length;
    }
    /**
     * Invalidate a specific row number
     * @param {Number} row
     */
    invalidateRow(row) {
      if (row >= 0) {
        const rows = [row];
        if (this._options.enableCellRowSpan) {
          const intersectedRow = this.getRowSpanIntersect(row);
          if (intersectedRow !== null) {
            rows.push(intersectedRow);
          }
        }
        this.invalidateRows(rows);
      }
    }
    queuePostProcessedRowForCleanup(cacheEntry, postProcessedRow, rowIdx) {
      this.postProcessgroupId++;
      if (typeof postProcessedRow === "object") {
        Object.keys(postProcessedRow).forEach((columnIdx) => {
          if (postProcessedRow.hasOwnProperty(columnIdx)) {
            this.postProcessedCleanupQueue.push({
              actionType: "C",
              groupId: this.postProcessgroupId,
              node: cacheEntry.cellNodesByColumnIdx[+columnIdx],
              columnIdx: +columnIdx,
              rowIdx
            });
          }
        });
      }
      if (!cacheEntry.rowNode) {
        cacheEntry.rowNode = [];
      }
      this.postProcessedCleanupQueue.push({
        actionType: "R",
        groupId: this.postProcessgroupId,
        node: cacheEntry.rowNode
      });
      cacheEntry.rowNode?.forEach((node) => node.remove());
    }
    /* v8 ignore next */
    queuePostProcessedCellForCleanup(cellnode, columnIdx, rowIdx) {
      this.postProcessedCleanupQueue.push({
        actionType: "C",
        groupId: this.postProcessgroupId,
        node: cellnode,
        columnIdx,
        rowIdx
      });
      cellnode.remove();
    }
    removeRowFromCache(row) {
      const cacheEntry = this.rowsCache[row];
      if (cacheEntry?.rowNode) {
        this.triggerEvent(this.onBeforeRemoveCachedRow, { row });
        if (this._options.enableAsyncPostRenderCleanup && this.postProcessedRows[row]) {
          this.queuePostProcessedRowForCleanup(cacheEntry, this.postProcessedRows[row], row);
        } else {
          cacheEntry.rowNode?.forEach((node) => node.parentElement?.removeChild(node));
        }
        delete this.rowsCache[row];
        delete this.postProcessedRows[row];
        this.renderedRows--;
        this.counter_rows_removed++;
      }
    }
    /** Apply a Formatter Result to a Cell DOM Node */
    applyFormatResultToCellNode(formatterResult, cellNode, suppressRemove) {
      if (formatterResult === null || formatterResult === void 0) {
        formatterResult = "";
      }
      if (isPrimitiveOrHTML(formatterResult)) {
        applyHtmlToElement(cellNode, formatterResult, this._options);
        return;
      }
      const formatterVal = formatterResult.html || formatterResult.text;
      applyHtmlToElement(cellNode, formatterVal, this._options);
      if (formatterResult.removeClasses && !suppressRemove) {
        cellNode.classList.remove(...classNameToList(formatterResult.removeClasses));
      }
      if (formatterResult.addClasses) {
        cellNode.classList.add(...classNameToList(formatterResult.addClasses));
      }
      if (formatterResult.toolTip) {
        cellNode.setAttribute("title", formatterResult.toolTip);
      }
    }
    /**
     * Update a specific cell by its row and column index
     * @param {Number} row - grid row number
     * @param {Number} cell - grid cell column number
     */
    updateCell(row, cell) {
      const cellNode = this.getCellNode(row, cell);
      if (cellNode) {
        const m = this.columns[cell];
        const d2 = this.getDataItem(row);
        if (this.currentEditor && this.activeRow === row && this.activeCell === cell) {
          this.currentEditor.loadValue(d2);
        } else {
          const formatterResult = d2 ? this.getFormatter(row, m)(row, cell, this.getDataItemValueForColumn(d2, m), m, d2, this) : "";
          this.applyFormatResultToCellNode(formatterResult, cellNode);
          this.invalidatePostProcessingResults(row);
        }
      }
    }
    /**
     * Update a specific row by its row index
     * @param {Number} row - grid row number
     */
    updateRow(row) {
      const cacheEntry = this.rowsCache[row];
      if (!cacheEntry) {
        return;
      }
      this.ensureCellNodesInRowsCache(row);
      let formatterResult;
      const d2 = this.getDataItem(row);
      Object.keys(cacheEntry.cellNodesByColumnIdx).forEach((colIdx) => {
        if (cacheEntry.cellNodesByColumnIdx.hasOwnProperty(colIdx)) {
          const columnIdx = +colIdx;
          const m = this.columns[columnIdx];
          const node = cacheEntry.cellNodesByColumnIdx[columnIdx];
          if (this.currentEditor && row === this.activeRow && columnIdx === this.activeCell) {
            this.currentEditor.loadValue(d2);
          } else if (d2) {
            formatterResult = this.getFormatter(row, m)(row, columnIdx, this.getDataItemValueForColumn(d2, m), m, d2, this);
            this.applyFormatResultToCellNode(formatterResult, node);
          } else {
            emptyElement(node);
          }
        }
      });
      this.invalidatePostProcessingResults(row);
    }
    getCellHeight(row, rowspan) {
      let cellHeight = this._options.rowHeight || 0;
      if (rowspan > 1) {
        const rowSpanBottomIdx = row + rowspan - 1;
        cellHeight = this.getRowBottom(rowSpanBottomIdx) - this.getRowTop(row);
      } else {
        const rowHeight = this.getRowHeight();
        if (rowHeight !== cellHeight - this.cellHeightDiff) {
          cellHeight = rowHeight;
        }
      }
      cellHeight -= this.cellHeightDiff;
      return Math.ceil(cellHeight);
    }
    /**
     * Get the number of rows displayed in the viewport
     * Note that the row count is an approximation because it is a calculated value using this formula (viewport / rowHeight = rowCount),
     * the viewport must also be displayed for this calculation to work.
     * @return {Number} rowCount
     */
    getViewportRowCount() {
      const vh = this.getViewportHeight();
      const scrollbarHeight = this.getScrollbarDimensions()?.height || 0;
      return Math.floor((vh - scrollbarHeight) / this._options.rowHeight);
    }
    getViewportHeight() {
      if (!this._options.autoHeight || this._options.frozenColumn !== -1) {
        this.topPanelH = this._options.showTopPanel ? this._options.topPanelHeight + this.getVBoxDelta(this._topPanelScrollers[0]) : 0;
        this.headerRowH = this._options.showHeaderRow ? this._options.headerRowHeight + this.getVBoxDelta(this._headerRowScroller[0]) : 0;
        this.footerRowH = this._options.showFooterRow ? this._options.footerRowHeight + this.getVBoxDelta(this._footerRowScroller[0]) : 0;
      }
      if (this._options.autoHeight) {
        let fullHeight = this._paneHeaderL.offsetHeight;
        fullHeight += this._options.showPreHeaderPanel ? this._options.preHeaderPanelHeight + this.getVBoxDelta(this._preHeaderPanelScroller) : 0;
        fullHeight += this._options.showHeaderRow ? this._options.headerRowHeight + this.getVBoxDelta(this._headerRowScroller[0]) : 0;
        fullHeight += this._options.showFooterRow ? this._options.footerRowHeight + this.getVBoxDelta(this._footerRowScroller[0]) : 0;
        fullHeight += this.getCanvasWidth() > this.viewportW ? this.scrollbarDimensions?.height || 0 : 0;
        this.viewportH = this._options.rowHeight * this.getDataLengthIncludingAddNew() + (this._options.frozenColumn === -1 ? fullHeight : 0);
      } else {
        const style = getComputedStyle(this._container);
        const containerBoxH = style.boxSizing !== "content-box" ? this.getVBoxDelta(this._container) : 0;
        const topHeaderH = this._options.createTopHeaderPanel && this._options.showTopHeaderPanel ? this._options.topHeaderPanelHeight + this.getVBoxDelta(this._topHeaderPanelScroller) : 0;
        const preHeaderH = this._options.createPreHeaderPanel && this._options.showPreHeaderPanel ? this._options.preHeaderPanelHeight + this.getVBoxDelta(this._preHeaderPanelScroller) : 0;
        const columnNamesH = this._options.showColumnHeader ? Utils.toFloat(Utils.height(this._headerScroller[0])) : 0;
        this.viewportH = Utils.toFloat(style.height) - Utils.toFloat(style.paddingTop) - Utils.toFloat(style.paddingBottom) - this.topPanelH - topHeaderH - preHeaderH - this.headerRowH - columnNamesH - this.footerRowH - containerBoxH;
      }
      this.numVisibleRows = Math.ceil(this.viewportH / this._options.rowHeight);
      return this.viewportH;
    }
    /** returns the available viewport inner width, that is the viewport width minus the scrollbar when shown */
    getViewportInnerWidth() {
      return this.viewportHasVScroll ? this.viewportW - (this.scrollbarDimensions?.width || 0) : this.viewportW;
    }
    getViewportWidth() {
      this.viewportW = parseFloat(getInnerSize(this._container, "width")) || this._options.devMode && this._options.devMode.containerClientWidth || 0;
      return this.viewportW;
    }
    /** Execute a Resize of the Grid Canvas */
    resizeCanvas() {
      if (this.initialized) {
        this.paneTopH = 0;
        this.paneBottomH = 0;
        this.viewportTopH = 0;
        this.viewportBottomH = 0;
        this.getViewportWidth();
        this.getViewportHeight();
        if (this.hasFrozenRows) {
          if (this._options.frozenBottom) {
            this.paneTopH = this.viewportH - this.frozenRowsHeight - (this.scrollbarDimensions?.height || 0);
            this.paneBottomH = this.frozenRowsHeight + (this.scrollbarDimensions?.height || 0);
          } else {
            this.paneTopH = this.frozenRowsHeight;
            this.paneBottomH = this.viewportH - this.frozenRowsHeight;
          }
        } else {
          this.paneTopH = this.viewportH;
        }
        this.paneTopH += this.topPanelH + this.headerRowH + this.footerRowH;
        if (this.hasFrozenColumns() && this._options.autoHeight) {
          this.paneTopH += this.scrollbarDimensions?.height || 0;
        }
        this.viewportTopH = this.paneTopH - this.topPanelH - this.headerRowH - this.footerRowH;
        if (this._options.autoHeight) {
          if (this.hasFrozenColumns()) {
            let fullHeight = this.paneTopH + this._headerScrollerL.offsetHeight;
            fullHeight += this.getVBoxDelta(this._container);
            if (this._options.showPreHeaderPanel) {
              fullHeight += this._options.preHeaderPanelHeight;
            }
            Utils.height(this._container, fullHeight);
          }
          this._paneTopL.style.position = "relative";
        }
        let topHeightOffset = Utils.height(this._paneHeaderL);
        if (topHeightOffset) {
          topHeightOffset += this._options.showTopHeaderPanel ? this._options.topHeaderPanelHeight : 0;
        } else {
          topHeightOffset = (this._options.showHeaderRow ? this._options.headerRowHeight : 0) + (this._options.showPreHeaderPanel ? this._options.preHeaderPanelHeight : 0);
        }
        Utils.setStyleSize(this._paneTopL, "top", topHeightOffset);
        Utils.height(this._paneTopL, this.paneTopH);
        const paneBottomTop = this._paneTopL.offsetTop + this.paneTopH;
        if (!this._options.autoHeight) {
          Utils.height(this._viewportTopL, this.viewportTopH);
        }
        if (this.hasFrozenColumns()) {
          let topHeightOffset2 = Utils.height(this._paneHeaderL);
          if (topHeightOffset2) {
            topHeightOffset2 += this._options.showTopHeaderPanel ? this._options.topHeaderPanelHeight : 0;
          }
          Utils.setStyleSize(this._paneTopR, "top", topHeightOffset2);
          Utils.height(this._paneTopR, this.paneTopH);
          Utils.height(this._viewportTopR, this.viewportTopH);
          if (this.hasFrozenRows) {
            Utils.setStyleSize(this._paneBottomL, "top", paneBottomTop);
            Utils.height(this._paneBottomL, this.paneBottomH);
            Utils.setStyleSize(this._paneBottomR, "top", paneBottomTop);
            Utils.height(this._paneBottomR, this.paneBottomH);
            Utils.height(this._viewportBottomR, this.paneBottomH);
          }
        } else {
          if (this.hasFrozenRows) {
            Utils.width(this._paneBottomL, "100%");
            Utils.height(this._paneBottomL, this.paneBottomH);
            Utils.setStyleSize(this._paneBottomL, "top", paneBottomTop);
          }
        }
        if (this.hasFrozenRows) {
          Utils.height(this._viewportBottomL, this.paneBottomH);
          if (this._options.frozenBottom) {
            Utils.height(this._canvasBottomL, this.frozenRowsHeight);
            if (this.hasFrozenColumns()) {
              Utils.height(this._canvasBottomR, this.frozenRowsHeight);
            }
          } else {
            Utils.height(this._canvasTopL, this.frozenRowsHeight);
            if (this.hasFrozenColumns()) {
              Utils.height(this._canvasTopR, this.frozenRowsHeight);
            }
          }
        } else {
          Utils.height(this._viewportTopR, this.viewportTopH);
        }
        if (!this.scrollbarDimensions || !this.scrollbarDimensions.width) {
          this.scrollbarDimensions = this.measureScrollbar();
        }
        if (this._options.forceFitColumns) {
          this.legacyAutosizeColumns();
        }
        this.updateRowCount();
        this.handleScroll();
        this.lastRenderedScrollLeft = -1;
        this.render();
      }
    }
    /**
     * Update paging information status from the View
     * @param {PagingInfo} pagingInfo
     */
    updatePagingStatusFromView(pagingInfo) {
      this.pagingActive = pagingInfo.pageSize !== 0;
      this.pagingIsLastPage = pagingInfo.pageNum === pagingInfo.totalPages - 1;
    }
    /** Update the dataset row count */
    updateRowCount() {
      if (this.initialized) {
        const dataLength = this.getDataLength();
        this._container.setAttribute("aria-rowcount", dataLength.toString());
        if (dataLength > 0 && dataLength !== this._prevDataLength) {
          this._rowSpanIsCached = false;
        }
        if (this._options.enableCellRowSpan && !this._rowSpanIsCached) {
          this.remapAllColumnsRowSpan();
        }
        this._prevDataLength = dataLength;
        const dataLengthIncludingAddNew = this.getDataLengthIncludingAddNew();
        let numberOfRows = 0;
        let oldH = this.hasFrozenRows && !this._options.frozenBottom ? Utils.height(this._canvasBottomL) : Utils.height(this._canvasTopL);
        if (this.hasFrozenRows) {
          numberOfRows = this.getDataLength() - this._options.frozenRow;
        } else {
          numberOfRows = dataLengthIncludingAddNew + (this._options.leaveSpaceForNewRows ? this.numVisibleRows - 1 : 0);
        }
        const tempViewportH = Utils.height(this._viewportScrollContainerY);
        const oldViewportHasVScroll = this.viewportHasVScroll;
        this.viewportHasVScroll = this._options.alwaysShowVerticalScroll || !this._options.autoHeight && numberOfRows * this._options.rowHeight > tempViewportH;
        this.makeActiveCellNormal();
        const r1 = dataLength - 1;
        if (typeof this.rowsCache === "object") {
          Object.keys(this.rowsCache).forEach((row) => {
            const cachedRow = +row;
            if (cachedRow > r1) {
              this.removeRowFromCache(cachedRow);
            }
          });
        }
        if (this._options.enableAsyncPostRenderCleanup) {
          this.startPostProcessingCleanup();
        }
        if (this.activeCellNode && this.activeRow > r1) {
          this.resetActiveCell();
        }
        oldH = this.h;
        if (this._options.autoHeight) {
          this.h = this._options.rowHeight * numberOfRows;
        } else {
          this.th = Math.max(this._options.rowHeight * numberOfRows, tempViewportH - (this.scrollbarDimensions?.height || 0));
          if (this.th < this.maxSupportedCssHeight) {
            this.h = this.ph = this.th;
            this.n = 1;
            this.cj = 0;
          } else {
            this.h = this.maxSupportedCssHeight;
            this.ph = this.h / 100;
            this.n = Math.floor(this.th / this.ph);
            this.cj = (this.th - this.h) / (this.n - 1);
          }
        }
        if (this.h !== oldH || this.enforceFrozenRowHeightRecalc) {
          if (this.hasFrozenRows && !this._options.frozenBottom) {
            Utils.height(this._canvasBottomL, this.h);
            if (this.hasFrozenColumns()) {
              Utils.height(this._canvasBottomR, this.h);
            }
          } else {
            Utils.height(this._canvasTopL, this.h);
            Utils.height(this._canvasTopR, this.h);
          }
          this.scrollTop = this._viewportScrollContainerY.scrollTop;
          this.scrollHeight = this._viewportScrollContainerY.scrollHeight;
          this.enforceFrozenRowHeightRecalc = false;
        }
        const oldScrollTopInRange = this.scrollTop + this.offset <= this.th - tempViewportH;
        if (this.th === 0 || this.scrollTop === 0) {
          this.page = this.offset = 0;
        } else if (oldScrollTopInRange) {
          this.scrollTo(this.scrollTop + this.offset);
        } else {
          this.scrollTo(this.th - tempViewportH + (this.scrollbarDimensions?.height || 0));
        }
        if (this.h !== oldH && this._options.autoHeight) {
          this.resizeCanvas();
        }
        if (this._options.forceFitColumns && oldViewportHasVScroll !== this.viewportHasVScroll) {
          this.legacyAutosizeColumns();
        }
        this.updateCanvasWidth(false);
      }
    }
    /** @alias `getVisibleRange` */
    getViewport(viewportTop, viewportLeft) {
      return this.getVisibleRange(viewportTop, viewportLeft);
    }
    /**
     * Returns an object with the top and bottom row indices that are visible in the viewport, as well
     * as the left and right pixel boundaries.
     * It uses the current (or provided) scroll positions and viewport dimensions.
     *
     * @param {number} [viewportTop] - The top scroll position.
     * @param {number} [viewportLeft] - The left scroll position.
     * @returns {{ top: number; bottom: number; leftPx: number; rightPx: number }} The visible range.
     */
    getVisibleRange(viewportTop, viewportLeft) {
      viewportTop ??= this.scrollTop;
      viewportLeft ??= this.scrollLeft;
      return {
        top: this.getRowFromPosition(viewportTop),
        bottom: this.getRowFromPosition(viewportTop + this.viewportH) + 1,
        leftPx: viewportLeft,
        rightPx: viewportLeft + this.viewportW
      };
    }
    /**
     * Computes the range of rows (and horizontal pixel boundaries) that should be rendered,
     * including an additional buffer (based on row height and a minimum buffer) determined by
     * the current vertical scroll direction.
     * This range is used to decide which rows and cells to render.
     *
     * @param {number} [viewportTop] - The top scroll position.
     * @param {number} [viewportLeft] - The left scroll position.
     * @returns {{ top: number; bottom: number; leftPx: number; rightPx: number }} The rendered range.
     */
    getRenderedRange(viewportTop, viewportLeft) {
      const range2 = this.getVisibleRange(viewportTop, viewportLeft);
      const buffer = Math.round(this.viewportH / this._options.rowHeight);
      const minBuffer = this._options.minRowBuffer;
      if (this.vScrollDir === -1) {
        range2.top -= buffer;
        range2.bottom += minBuffer;
      } else if (this.vScrollDir === 1) {
        range2.top -= minBuffer;
        range2.bottom += buffer;
      } else {
        range2.top -= minBuffer;
        range2.bottom += minBuffer;
      }
      range2.top = Math.max(0, range2.top);
      range2.bottom = Math.min(this.getDataLengthIncludingAddNew() - 1, range2.bottom);
      range2.leftPx -= this.viewportW;
      range2.rightPx += this.viewportW;
      range2.leftPx = Math.max(0, range2.leftPx);
      range2.rightPx = Math.min(this.canvasWidth, range2.rightPx);
      return range2;
    }
    /**
     * Returns the rows cache that are currently rendered in the DOM,
     * the cache includes certain properties like the row div element, cell rendered queue and the row colspan when defined.
     */
    getRowCache() {
      return this.rowsCache;
    }
    ensureCellNodesInRowsCache(row) {
      const cacheEntry = this.rowsCache[row];
      if (cacheEntry?.cellRenderQueue.length && cacheEntry.rowNode?.length) {
        const rowNode = cacheEntry.rowNode;
        let children = Array.from(rowNode[0].children);
        if (rowNode.length > 1) {
          children = children.concat(Array.from(rowNode[1].children));
        }
        let i = children.length - 1;
        while (cacheEntry.cellRenderQueue.length) {
          const columnIdx = cacheEntry.cellRenderQueue.pop();
          cacheEntry.cellNodesByColumnIdx[columnIdx] = children[i--];
        }
      }
    }
    cleanUpCells(range2, row) {
      if (this.hasFrozenRows && (this._options.frozenBottom && row > this.actualFrozenRow || // Frozen bottom rows
      row <= this.actualFrozenRow)) {
        return;
      }
      const cacheEntry = this.rowsCache[row];
      const cellsToRemove = [];
      Object.keys(cacheEntry.cellNodesByColumnIdx).forEach((cellNodeIdx) => {
        if (!cacheEntry.cellNodesByColumnIdx.hasOwnProperty(cellNodeIdx)) {
          return;
        }
        const i = +cellNodeIdx;
        if (i <= this._options.frozenColumn) {
          return;
        }
        if (Array.isArray(this.columns) && this.columns[i]?.alwaysRenderColumn) {
          return;
        }
        const colspan = cacheEntry.cellColSpans[i];
        if (this.columnPosLeft[i] > range2.rightPx || this.columnPosRight[Math.min(this.columns.length - 1, (i || 0) + colspan - 1)] < range2.leftPx) {
          if (!(row === this.activeRow && Number(i) === this.activeCell)) {
            cellsToRemove.push(i);
          }
        }
      });
      let cellToRemove;
      let cellNode;
      while (isDefined(cellToRemove = cellsToRemove.pop())) {
        cellNode = cacheEntry.cellNodesByColumnIdx[cellToRemove];
        if (this._options.enableAsyncPostRenderCleanup && this.postProcessedRows[row]?.[cellToRemove]) {
          this.queuePostProcessedCellForCleanup(cellNode, cellToRemove, row);
        } else {
          cellNode.parentElement?.removeChild(cellNode);
        }
        delete cacheEntry.cellColSpans[cellToRemove];
        delete cacheEntry.cellNodesByColumnIdx[cellToRemove];
        if (this.postProcessedRows[row]) {
          delete this.postProcessedRows[row][cellToRemove];
        }
      }
    }
    cleanUpAndRenderCells(range2) {
      let cacheEntry;
      const divRow = document.createElement("div");
      const processedRows = [];
      let cellsAdded;
      let colspan;
      let columnData;
      const columnCount = this.columns.length;
      for (let row = range2.top, btm = range2.bottom; row <= btm; row++) {
        cacheEntry = this.rowsCache[row];
        if (cacheEntry) {
          this.ensureCellNodesInRowsCache(row);
          if (!this._options.enableCellRowSpan || this.getRowSpanIntersect(row) === null) {
            this.cleanUpCells(range2, row);
          }
          cellsAdded = 0;
          const metadata = this.getItemMetadaWhenExists(row);
          const metadataCol = metadata?.columns;
          const d2 = this.getDataItem(row);
          let isFullColspan = false;
          for (let i = 0, ii = columnCount; i < ii; i++) {
            if (this.columns[i] && (!this.columns[i].hidden || metadata?.isGroup)) {
              if (this.columnPosLeft[i] > range2.rightPx) {
                break;
              }
              if (isDefined(colspan = cacheEntry.cellColSpans[i])) {
                i += colspan > 1 ? colspan - 1 : 0;
                continue;
              }
              colspan = 1;
              columnData = null;
              if (metadataCol) {
                columnData = metadataCol[this.columns[i].id] || metadataCol[i];
                colspan = columnData?.colspan ?? 1;
                if (colspan === "*") {
                  colspan = ii - i;
                  isFullColspan = true;
                }
              }
              let ncolspan = colspan;
              if (!isFullColspan && this._options.spreadHiddenColspan) {
                ncolspan = this.increaseHiddenColspan(ncolspan, i);
              }
              if (this.getParentRowSpanByCell(row, i)) {
                continue;
              }
              if (this.columnPosRight[Math.min(ii - 1, i + ncolspan - 1)] > range2.leftPx) {
                const rowspan = this.getRowspan(row, i);
                this.appendCellHtml(divRow, row, i, ncolspan, rowspan, columnData, d2);
                cellsAdded++;
              }
              i += ncolspan > 1 ? ncolspan - 1 : 0;
            }
          }
          if (cellsAdded) {
            processedRows.push(row);
          }
        }
      }
      if (!divRow.children.length) {
        return;
      }
      let processedRow;
      let node;
      while (isDefined(processedRow = processedRows.pop())) {
        cacheEntry = this.rowsCache[processedRow];
        let columnIdx;
        while (isDefined(columnIdx = cacheEntry.cellRenderQueue.pop())) {
          node = divRow.lastChild;
          if (node) {
            if (this.hasFrozenColumns() && columnIdx > this._options.frozenColumn) {
              cacheEntry.rowNode[1].appendChild(node);
            } else {
              cacheEntry.rowNode[0].appendChild(node);
            }
            cacheEntry.cellNodesByColumnIdx[columnIdx] = node;
          }
        }
      }
    }
    createEmptyCachingRow() {
      return {
        rowNode: null,
        // ColSpans of rendered cells (by column idx).
        // Can also be used for checking whether a cell has been rendered.
        cellColSpans: [],
        // Cell nodes (by column idx).  Lazy-populated by ensureCellNodesInRowsCache().
        cellNodesByColumnIdx: [],
        // Column indices of cell nodes that have been rendered, but not yet indexed in
        // cellNodesByColumnIdx.  These are in the same order as cell nodes added at the
        // end of the row.
        cellRenderQueue: []
      };
    }
    renderRows(range2) {
      const divArrayL = [];
      const divArrayR = [];
      const rows = [];
      let needToReselectCell = false;
      const dataLength = this.getDataLength();
      const mustRenderRows = /* @__PURE__ */ new Set();
      const renderingRows = /* @__PURE__ */ new Set();
      for (let i = range2.top, ii = range2.bottom; i <= ii; i++) {
        if (this.rowsCache[i] || this.hasFrozenRows && this._options.frozenBottom && i === this.getDataLength()) {
          continue;
        }
        this.renderedRows++;
        rows.push(i);
        renderingRows.add(i);
        this.rowsCache[i] = this.createEmptyCachingRow();
        if (this._options.enableCellRowSpan) {
          const parentRowSpan = this.getRowSpanIntersect(i);
          if (parentRowSpan !== null) {
            renderingRows.add(parentRowSpan);
          }
        }
        this.appendRowHtml(divArrayL, divArrayR, i, range2, dataLength);
        mustRenderRows.add(i);
        if (this.activeCellNode && this.activeRow === i) {
          needToReselectCell = true;
        }
        this.counter_rows_rendered++;
      }
      const mandatorySpanRows = this.setDifference(renderingRows, mustRenderRows);
      if (mandatorySpanRows.size > 0) {
        mandatorySpanRows.forEach((r2) => {
          this.removeRowFromCache(r2);
          rows.push(r2);
          this.rowsCache[r2] = this.createEmptyCachingRow();
          this.appendRowHtml(divArrayL, divArrayR, r2, range2, dataLength);
        });
      }
      if (rows.length) {
        const x2 = document.createElement("div");
        const xRight = document.createElement("div");
        divArrayL.forEach((elm) => x2.appendChild(elm));
        divArrayR.forEach((elm) => xRight.appendChild(elm));
        for (let i = 0, ii = rows.length; i < ii; i++) {
          if (this.hasFrozenRows && rows[i] >= this.actualFrozenRow) {
            if (this.hasFrozenColumns()) {
              if (this.rowsCache?.hasOwnProperty(rows[i]) && x2.firstChild && xRight.firstChild) {
                this.rowsCache[rows[i]].rowNode = [x2.firstChild, xRight.firstChild];
                this._canvasBottomL.appendChild(x2.firstChild);
                this._canvasBottomR.appendChild(xRight.firstChild);
              }
            } else {
              if (this.rowsCache?.hasOwnProperty(rows[i]) && x2.firstChild) {
                this.rowsCache[rows[i]].rowNode = [x2.firstChild];
                this._canvasBottomL.appendChild(x2.firstChild);
              }
            }
          } else if (this.hasFrozenColumns()) {
            if (this.rowsCache?.hasOwnProperty(rows[i]) && x2.firstChild && xRight.firstChild) {
              this.rowsCache[rows[i]].rowNode = [x2.firstChild, xRight.firstChild];
              this._canvasTopL.appendChild(x2.firstChild);
              this._canvasTopR.appendChild(xRight.firstChild);
            }
          } else {
            if (this.rowsCache?.hasOwnProperty(rows[i]) && x2.firstChild) {
              this.rowsCache[rows[i]].rowNode = [x2.firstChild];
              this._canvasTopL.appendChild(x2.firstChild);
            }
          }
        }
        if (needToReselectCell) {
          this.activeCellNode = this.getCellNode(this.activeRow, this.activeCell);
        }
      }
    }
    /** polyfill if the new Set.difference() added in ES2024 */
    setDifference(a, b) {
      return new Set(Array.from(a).filter((item) => !b.has(item)));
    }
    startPostProcessing() {
      if (this._options.enableAsyncPostRender) {
        clearTimeout(this.h_postrender);
        this.h_postrender = setTimeout(this.asyncPostProcessRows.bind(this), this._options.asyncPostRenderDelay);
      }
    }
    startPostProcessingCleanup() {
      if (this._options.enableAsyncPostRenderCleanup) {
        clearTimeout(this.h_postrenderCleanup);
        this.h_postrenderCleanup = setTimeout(this.asyncPostProcessCleanupRows.bind(this), this._options.asyncPostRenderCleanupDelay);
      }
    }
    invalidatePostProcessingResults(row) {
      if (typeof this.postProcessedRows[row] === "object") {
        Object.keys(this.postProcessedRows[row]).forEach((columnIdx) => {
          if (this.postProcessedRows[row].hasOwnProperty(columnIdx)) {
            this.postProcessedRows[row][columnIdx] = "C";
          }
        });
      }
      this.postProcessFromRow = Math.min(this.postProcessFromRow, row);
      this.postProcessToRow = Math.max(this.postProcessToRow, row);
      this.startPostProcessing();
    }
    updateRowPositions() {
      if (this.rowsCache && typeof this.rowsCache === "object") {
        Object.keys(this.rowsCache).forEach((row) => {
          const rowNumber = row ? parseInt(row, 10) : 0;
          const rowNode = this.rowsCache[rowNumber].rowNode[0];
          if (this._options.rowTopOffsetRenderType === "transform") {
            rowNode.style.transform = `translateY(${this.getRowTop(rowNumber)}px)`;
          } else {
            rowNode.style.top = `${this.getRowTop(rowNumber)}px`;
          }
        });
      }
    }
    /** (re)Render the grid */
    render() {
      if (this.initialized) {
        this.scrollThrottle.dequeue();
        const visible = this.getVisibleRange();
        const rendered = this.getRenderedRange();
        this.cleanupRows(rendered);
        if (this.lastRenderedScrollLeft !== this.scrollLeft) {
          if (this.hasFrozenRows) {
            const renderedFrozenRows = extend(true, {}, rendered);
            if (this._options.frozenBottom) {
              renderedFrozenRows.top = this.actualFrozenRow;
              renderedFrozenRows.bottom = this.getDataLength();
            } else {
              renderedFrozenRows.top = 0;
              renderedFrozenRows.bottom = this._options.frozenRow;
            }
            this.cleanUpAndRenderCells(renderedFrozenRows);
          }
          this.cleanUpAndRenderCells(rendered);
        }
        this.renderRows(rendered);
        if (this.hasFrozenRows) {
          if (this._options.frozenBottom) {
            this.renderRows({
              top: this.actualFrozenRow,
              bottom: this.getDataLength() - 1,
              leftPx: rendered.leftPx,
              rightPx: rendered.rightPx
            });
          } else {
            this.renderRows({
              top: 0,
              bottom: this._options.frozenRow - 1,
              leftPx: rendered.leftPx,
              rightPx: rendered.rightPx
            });
          }
        }
        this.postProcessFromRow = visible.top;
        this.postProcessToRow = Math.min(this.getDataLengthIncludingAddNew() - 1, visible.bottom);
        this.startPostProcessing();
        this.lastRenderedScrollTop = this.scrollTop;
        this.lastRenderedScrollLeft = this.scrollLeft;
        this.triggerEvent(this.onRendered, { startRow: visible.top, endRow: visible.bottom, grid: this });
      }
    }
    handleHeaderRowScroll() {
      const scrollLeft = this._headerRowScrollContainer.scrollLeft;
      if (scrollLeft !== this._viewportScrollContainerX.scrollLeft) {
        this._viewportScrollContainerX.scrollLeft = scrollLeft;
      }
    }
    handleFooterRowScroll() {
      const scrollLeft = this._footerRowScrollContainer.scrollLeft;
      if (scrollLeft !== this._viewportScrollContainerX.scrollLeft) {
        this._viewportScrollContainerX.scrollLeft = scrollLeft;
      }
    }
    handlePreHeaderPanelScroll() {
      this.handleElementScroll(this._preHeaderPanelScroller);
    }
    handleTopHeaderPanelScroll() {
      this.handleElementScroll(this._topHeaderPanelScroller);
    }
    handleElementScroll(element) {
      const scrollLeft = element.scrollLeft;
      if (scrollLeft !== this._viewportScrollContainerX.scrollLeft) {
        this._viewportScrollContainerX.scrollLeft = scrollLeft;
      }
    }
    handleScroll(e2) {
      this.scrollHeight = this._viewportScrollContainerY.scrollHeight;
      this.scrollTop = this._viewportScrollContainerY.scrollTop;
      this.scrollLeft = this._viewportScrollContainerX.scrollLeft;
      return this._handleScroll(e2 ? "scroll" : "system");
    }
    _handleScroll(eventType = "system") {
      let maxScrollDistanceY = this._viewportScrollContainerY.scrollHeight - this._viewportScrollContainerY.clientHeight;
      let maxScrollDistanceX = this._viewportScrollContainerY.scrollWidth - this._viewportScrollContainerY.clientWidth;
      maxScrollDistanceY = Math.max(0, maxScrollDistanceY);
      maxScrollDistanceX = Math.max(0, maxScrollDistanceX);
      if (this.scrollTop > maxScrollDistanceY) {
        this.scrollTop = maxScrollDistanceY;
        this.scrollHeight = maxScrollDistanceY;
      }
      if (this.scrollLeft > maxScrollDistanceX) {
        this.scrollLeft = maxScrollDistanceX;
      }
      const vScrollDist = Math.abs(this.scrollTop - this.prevScrollTop);
      const hScrollDist = Math.abs(this.scrollLeft - this.prevScrollLeft);
      if (hScrollDist) {
        this.prevScrollLeft = this.scrollLeft;
        this.scrollToX(this.scrollLeft);
      }
      if (vScrollDist && !this._options.autoHeight) {
        this.vScrollDir = this.prevScrollTop < this.scrollTop ? 1 : -1;
        this.prevScrollTop = this.scrollTop;
        if (eventType === "mousewheel") {
          this._viewportScrollContainerY.scrollTop = this.scrollTop;
        }
        if (this.hasFrozenColumns()) {
          if (this.hasFrozenRows && !this._options.frozenBottom) {
            this._viewportBottomL.scrollTop = this.scrollTop;
          } else {
            this._viewportTopL.scrollTop = this.scrollTop;
          }
        }
        if (vScrollDist < this.viewportH) {
          this.scrollTo(this.scrollTop + this.offset);
        } else {
          if (this.h === this.viewportH) {
            this.page = 0;
          } else {
            this.page = Math.min(this.n - 1, Math.floor(this.scrollTop * ((this.th - this.viewportH) / (this.h - this.viewportH)) * (1 / this.ph)));
          }
          this.offset = Math.round(this.page * this.cj);
        }
      }
      if (hScrollDist || vScrollDist) {
        const dx = Math.abs(this.lastRenderedScrollLeft - this.scrollLeft);
        const dy = Math.abs(this.lastRenderedScrollTop - this.scrollTop);
        if (dx > 20 || dy > 20) {
          if (this._isResizingColumn && hScrollDist && !vScrollDist) {
            this.lastRenderedScrollLeft = this.scrollLeft;
            this.triggerEvent(this.onViewportChanged, {});
            return true;
          }
          if (this._options.forceSyncScrolling || dy < this.viewportH && dx < this.viewportW) {
            this.render();
          } else {
            this.scrollThrottle.enqueue();
          }
          this.triggerEvent(this.onViewportChanged, {});
        }
      }
      this.triggerEvent(this.onScroll, {
        triggeredBy: eventType,
        scrollHeight: this.scrollHeight,
        scrollLeft: this.scrollLeft,
        scrollTop: this.scrollTop
      });
      if (hScrollDist || vScrollDist) {
        return true;
      }
      return false;
    }
    handleActiveCellPositionChange() {
      if (this.activeCellNode) {
        this.triggerEvent(this.onActiveCellPositionChanged, {});
        if (this.currentEditor) {
          const cellBox = this.getActiveCellPosition();
          if (this.currentEditor.show && this.currentEditor.hide) {
            if (!cellBox.visible) {
              this.currentEditor.hide();
            } else {
              this.currentEditor.show();
            }
          }
          if (this.currentEditor.position) {
            this.currentEditor.position(cellBox);
          }
        }
      }
    }
    /**
     * limits the frequency at which the provided action is executed.
     * call enqueue to execute the action - it will execute either immediately or, if it was executed less than minPeriod_ms in the past, as soon as minPeriod_ms has expired.
     * call dequeue to cancel any pending action.
     */
    actionThrottle(action, minPeriod_ms) {
      let blocked = false;
      let queued = false;
      const enqueue = () => {
        if (!blocked) {
          blockAndExecute();
        } else {
          queued = true;
        }
      };
      const dequeue = () => queued = false;
      const blockAndExecute = () => {
        blocked = true;
        clearTimeout(this._executionBlockTimer);
        this._executionBlockTimer = setTimeout(unblock, minPeriod_ms);
        action.call(this);
      };
      const unblock = () => {
        if (queued) {
          dequeue();
          blockAndExecute();
        } else {
          blocked = false;
        }
      };
      return {
        enqueue: enqueue.bind(this),
        dequeue: dequeue.bind(this)
      };
    }
    asyncPostProcessRows() {
      const dataLength = this.getDataLength();
      while (this.postProcessFromRow <= this.postProcessToRow) {
        const row = this.vScrollDir >= 0 ? this.postProcessFromRow++ : this.postProcessToRow--;
        const cacheEntry = this.rowsCache[row];
        if (!cacheEntry || row >= dataLength) {
          continue;
        }
        if (!this.postProcessedRows[row]) {
          this.postProcessedRows[row] = {};
        }
        this.ensureCellNodesInRowsCache(row);
        Object.keys(cacheEntry.cellNodesByColumnIdx).forEach((colIdx) => {
          if (cacheEntry.cellNodesByColumnIdx.hasOwnProperty(colIdx)) {
            const columnIdx = +colIdx;
            const m = this.columns[columnIdx];
            const processedStatus = this.postProcessedRows[row][columnIdx];
            if (m.asyncPostRender && processedStatus !== "R") {
              const node = cacheEntry.cellNodesByColumnIdx[columnIdx];
              if (node) {
                m.asyncPostRender(node, row, this.getDataItem(row), m, processedStatus === "C");
              }
              this.postProcessedRows[row][columnIdx] = "R";
            }
          }
        });
        this.h_postrender = setTimeout(this.asyncPostProcessRows.bind(this), this._options.asyncPostRenderDelay);
        return;
      }
    }
    asyncPostProcessCleanupRows() {
      if (this.postProcessedCleanupQueue.length > 0) {
        const groupId = this.postProcessedCleanupQueue[0].groupId;
        while (this.postProcessedCleanupQueue.length > 0 && this.postProcessedCleanupQueue[0].groupId === groupId) {
          const entry = this.postProcessedCleanupQueue.shift();
          if (entry?.actionType === "R") {
            entry.node.forEach((node) => {
              node.remove();
            });
          }
          if (entry?.actionType === "C") {
            const column = this.columns[entry.columnIdx];
            if (column.asyncPostRenderCleanup && entry.node) {
              column.asyncPostRenderCleanup(entry.node, entry.rowIdx, column);
            }
          }
        }
        this.h_postrenderCleanup = setTimeout(this.asyncPostProcessCleanupRows.bind(this), this._options.asyncPostRenderCleanupDelay);
      }
    }
    updateCellCssStylesOnRenderedRows(addedHash, removedHash) {
      let node;
      let addedRowHash;
      let removedRowHash;
      if (typeof this.rowsCache === "object") {
        Object.keys(this.rowsCache).forEach((row) => {
          if (this.rowsCache) {
            removedRowHash = removedHash?.[row];
            addedRowHash = addedHash?.[row];
            if (removedRowHash) {
              Object.keys(removedRowHash).forEach((columnId) => {
                if (!addedRowHash || removedRowHash[columnId] !== addedRowHash[columnId]) {
                  node = this.getCellNode(+row, this.getColumnIndex(columnId));
                  if (node) {
                    node.classList.remove(removedRowHash[columnId]);
                  }
                }
              });
            }
            if (addedRowHash) {
              Object.keys(addedRowHash).forEach((columnId) => {
                if (!removedRowHash || removedRowHash[columnId] !== addedRowHash[columnId]) {
                  node = this.getCellNode(+row, this.getColumnIndex(columnId));
                  if (node) {
                    node.classList.add(addedRowHash[columnId]);
                  }
                }
              });
            }
          }
        });
      }
    }
    /**
     * Adds an "overlay" of CSS classes to cell DOM elements. SlickGrid can have many such overlays associated with different keys and they are frequently used by plugins. For example, SlickGrid uses this method internally to decorate selected cells with selectedCellCssClass (see options).
     * @param {String} key A unique key you can use in calls to setCellCssStyles and removeCellCssStyles. If a hash with that key has already been set, an exception will be thrown.
     * @param {CssStyleHash} hash A hash of additional cell CSS classes keyed by row number and then by column id. Multiple CSS classes can be specified and separated by space.
     * @example
     * `{
     * 	 0: { number_column: SlickEvent; title_column: SlickEvent;	},
     * 	 4: { percent_column: SlickEvent; }
     * }`
     */
    addCellCssStyles(key, hash) {
      if (this.cellCssClasses[key]) {
        throw new Error(`SlickGrid addCellCssStyles: cell CSS hash with key "${key}" already exists.`);
      }
      this.cellCssClasses[key] = hash;
      this.updateCellCssStylesOnRenderedRows(hash, null);
      this.triggerEvent(this.onCellCssStylesChanged, { key, hash, grid: this });
    }
    /**
     * Removes an "overlay" of CSS classes from cell DOM elements. See setCellCssStyles for more.
     * @param {String} key A string key.
     */
    removeCellCssStyles(key) {
      if (this.cellCssClasses[key]) {
        this.updateCellCssStylesOnRenderedRows(null, this.cellCssClasses[key]);
        delete this.cellCssClasses[key];
        this.triggerEvent(this.onCellCssStylesChanged, { key, hash: null, grid: this });
      }
    }
    /**
     * Removes an "overlay" of CSS classes from cell DOM elements matching predicated entries.
     * Useful when you have multiple keys and want to remove them based on a certain criteria.
     * @param {Function} predicate A callback function that receives the key and hash as arguments and should return true if the entry should be removed.
     * @example
     * grid.removeCellCssStylesBatch((key, hash) => key.startsWith('unsaved-changes') && hash[0].includes('highlight'));
     */
    removeCellCssStylesBatch(predicate) {
      Object.entries(this.cellCssClasses).forEach(([k2, v2]) => predicate(k2, v2) && this.removeCellCssStyles(k2));
    }
    /**
     * Sets CSS classes to specific grid cells by calling removeCellCssStyles(key) followed by addCellCssStyles(key, hash). key is name for this set of styles so you can reference it later - to modify it or remove it, for example. hash is a per-row-index, per-column-name nested hash of CSS classes to apply.
     * Suppose you have a grid with columns:
     * ["login", "name", "birthday", "age", "likes_icecream", "favorite_cake"]
     * ...and you'd like to highlight the "birthday" and "age" columns for people whose birthday is today, in this case, rows at index 0 and 9. (The first and tenth row in the grid).
     * @param {String} key A string key. Will overwrite any data already associated with this key.
     * @param {Object} hash A hash of additional cell CSS classes keyed by row number and then by column id. Multiple CSS classes can be specified and separated by space.
     */
    setCellCssStyles(key, hash) {
      const prevHash = this.cellCssClasses[key];
      this.cellCssClasses[key] = hash;
      this.updateCellCssStylesOnRenderedRows(hash, prevHash);
      this.triggerEvent(this.onCellCssStylesChanged, { key, hash, grid: this });
    }
    /**
     * Accepts a key name, returns the group of CSS styles defined under that name. See setCellCssStyles for more info.
     * @param {String} key A string.
     */
    getCellCssStyles(key) {
      return this.cellCssClasses[key];
    }
    /**
     * Flashes the cell twice by toggling the CSS class 4 times.
     * @param {Number} row A row index.
     * @param {Number} cell A column index.
     * @param {Number} [speed] (optional) - The milliseconds delay between the toggling calls. Defaults to 250 ms.
     */
    flashCell(row, cell, speed = 250) {
      const toggleCellClass = (cellNode, times) => {
        if (times > 0) {
          clearTimeout(this._flashCellTimer);
          this._flashCellTimer = setTimeout(() => {
            cellNode.classList.toggle(this._options.cellFlashingCssClass || "", times % 2 === 0);
            toggleCellClass(cellNode, times - 1);
          }, speed);
        }
      };
      if (this.rowsCache[row]) {
        const cellNode = this.getCellNode(row, cell);
        if (cellNode) {
          toggleCellClass(cellNode, 5);
        }
      }
    }
    /**
     * Highlight a row for a certain duration (ms) of time.
     * @param {Number} row - grid row number
     * @param {Number} [duration] - duration (ms), defaults to 400ms
     */
    highlightRow(row, duration) {
      const rowCache = this.rowsCache[row];
      duration ||= this._options.rowHighlightDuration;
      if (Array.isArray(rowCache?.rowNode) && this._options.rowHighlightCssClass) {
        rowCache.rowNode.forEach((node) => node.classList.add(...classNameToList(this._options.rowHighlightCssClass)));
        clearTimeout(this._highlightRowTimer);
        this._highlightRowTimer = setTimeout(() => {
          rowCache.rowNode?.forEach((node) => node.classList.remove(...classNameToList(this._options.rowHighlightCssClass)));
        }, duration);
      }
    }
    /**
     * Programmatically focus a header column by index (default: first visible column).
     * @param index - Column index to focus (defaults to 0)
     */
    focusHeaderColumn(index2 = 0) {
      this.getHeaderColumn(index2)?.focus();
    }
    /**
     * Programmatically focus a header menu (when found) or fallback to header column if menu is not found or not visible.
     * @param index - Column index to focus (defaults to 0)
     */
    focusHeaderMenuOrColumn(index2 = 0) {
      const [headerMenuElm] = this.getVisibleElements(this.getHeaderColumn(index2), '.slick-header-menu-button[tabIndex="0"]');
      if (headerMenuElm) {
        headerMenuElm.focus();
      } else {
        this.focusHeaderColumn(index2);
      }
    }
    /**
     * Focus on first header row filter element it finds, unless focusOnLast is set to true in which case it will start backward and focus on the last one.
     * If header row filter isn't shown, it will focus on the first grid cell (or grid menu/header menu if focusOnLast is true) instead.
     * @param focusOnLast
     * @returns true when a header row filter element was found and focused otherwise false
     */
    focusHeaderRowFilter(focusOnLast = false) {
      const headerRow = this.getHeaderRow();
      if (this._options.showHeaderRow && headerRow) {
        const headerRows = headerRow instanceof HTMLElement ? [headerRow] : [...headerRow];
        const allFilterElms = headerRows.flatMap((row) => Array.from(row.querySelectorAll('.slick-headerrow-column *[tabIndex="0"]')));
        const filterLn = allFilterElms.length;
        let closestVisibleFilter = null;
        if (filterLn > 0) {
          const start = focusOnLast ? filterLn - 1 : 0;
          const end = focusOnLast ? -1 : filterLn;
          const step = focusOnLast ? -1 : 1;
          for (let i = start; i !== end; i += step) {
            const elm = allFilterElms[i];
            if (elm && elm.offsetParent !== null) {
              closestVisibleFilter = elm;
              break;
            }
          }
        }
        if (closestVisibleFilter) {
          closestVisibleFilter.focus();
          return true;
        }
      }
      !focusOnLast ? this.focusGridCell() : this.focusGridMenu();
      return false;
    }
    /** focus on the active cell when it exists, otherwise focus on first cell */
    focusGridCell() {
      this.setFocus();
      if (!this.getActiveCell()) {
        this.setActiveCell(0, 0);
      }
    }
    /** focus on grid menu button when enabled or fallback to last header menu or column */
    focusGridMenu() {
      const gridMenuBtn = this._container?.querySelector('.slick-grid-menu-button[tabIndex="0"]');
      if (gridMenuBtn) {
        gridMenuBtn.focus();
      } else {
        this.focusHeaderMenuOrColumn(this.getVisibleColumns().length - 1);
      }
    }
    // Interactivity
    /** focus element and stop event bubbling (for keyboard events) */
    focusElementWithoutBubbling(e2, target) {
      if (target) {
        target.focus();
        this.stopFullBubbling(e2);
      }
    }
    /** get only visible elemnts from a container and a query selector, e.g. elements with `display: none` will be excluded. */
    getVisibleElements(container, selector) {
      return Array.from(container.querySelectorAll(selector)).filter((el) => el.offsetParent !== null);
    }
    handleMouseWheel(e2, _delta, deltaX, deltaY) {
      this.scrollHeight = this._viewportScrollContainerY.scrollHeight;
      if (e2.shiftKey) {
        this.scrollLeft = this._viewportScrollContainerX.scrollLeft + deltaX * 10;
      } else {
        this.scrollTop = Math.max(0, this._viewportScrollContainerY.scrollTop - deltaY * this._options.rowHeight);
        this.scrollLeft = this._viewportScrollContainerX.scrollLeft + deltaX * 10;
      }
      const handled = this._handleScroll("mousewheel");
      if (handled) {
        e2.stopPropagation();
      }
    }
    handleDragInit(e2, dd) {
      const cell = this.getCellFromEvent(e2);
      if (!cell || !this.cellExists(cell.row, cell.cell)) {
        return false;
      }
      const retval = this.triggerEvent(this.onDragInit, dd, e2);
      if (retval.isImmediatePropagationStopped()) {
        return retval.getReturnValue();
      }
      return false;
    }
    handleDragStart(e2, dd) {
      const cell = this.getCellFromEvent(e2);
      if (!cell || !this.cellExists(cell.row, cell.cell)) {
        return false;
      }
      if (this.currentEditor && !this.getEditorLock().commitCurrentEdit()) {
        return false;
      }
      const retval = this.triggerEvent(this.onDragStart, dd, e2);
      if (retval.isImmediatePropagationStopped()) {
        return retval.getReturnValue();
      }
      return false;
    }
    handleDrag(e2, dd) {
      return this.triggerEvent(this.onDrag, dd, e2).getReturnValue();
    }
    handleDragEnd(e2, dd) {
      this.triggerEvent(this.onDragEnd, dd, e2);
    }
    handleContainerKeyDown(e2) {
      if (e2.target instanceof HTMLElement && e2.key === "Tab" && !e2.ctrlKey && !e2.altKey) {
        const isInHeaderRow = e2.target.closest(".slick-headerrow-columns");
        const headerSelector = `.slick-${isInHeaderRow ? "headerrow-column" : "header-columns"} *[tabIndex="0"]`;
        const allFilterElms = this.getVisibleElements(this._container, headerSelector);
        const allLeftFilterElms = this.getVisibleElements(this._container, `.slick-pane-left ${headerSelector}`);
        const allRightFilterElms = this.getVisibleElements(this._container, `.slick-pane-right ${headerSelector}`);
        const ancestorHeaderRow = e2.target instanceof HTMLElement ? e2.target.closest(headerSelector) : null;
        if (allFilterElms.length > 0) {
          const targetFilterElm = e2.shiftKey ? allFilterElms[0] : allFilterElms[allFilterElms.length - 1];
          if (targetFilterElm === ancestorHeaderRow && isInHeaderRow) {
            this.stopFullBubbling(e2);
            e2.shiftKey ? this.focusGridMenu() : this.focusGridCell();
          } else if (this._options.frozenColumn >= 0) {
            const lastLeftFilterElm = allLeftFilterElms[allLeftFilterElms.length - 1];
            if (e2.shiftKey && e2.target === allRightFilterElms[0]) {
              this.focusElementWithoutBubbling(e2, lastLeftFilterElm);
            } else if (!e2.shiftKey && e2.target === lastLeftFilterElm) {
              this.focusElementWithoutBubbling(e2, allRightFilterElms[0]);
            }
          }
        }
      }
    }
    handleGridKeyDown(e2) {
      const retval = this.triggerEvent(this.onKeyDown, { row: this.activeRow, cell: this.activeCell }, e2);
      let handled = retval.isImmediatePropagationStopped();
      const isGridFocusSinkTarget = e2.target === this._focusSink || e2.target === this._focusSink2;
      const isPlainTab = e2.key === "Tab" && !e2.ctrlKey && !e2.altKey;
      const isActiveCellZeroZero = this.activeRow === 0 && this.activeCell === 0;
      const hasGridCellFocus = this.getActiveCell() !== null;
      if (!handled && isGridFocusSinkTarget && isPlainTab) {
        if (e2.target === this._focusSink && !e2.shiftKey && !hasGridCellFocus) {
          this.focusHeaderMenuOrColumn(0);
          handled = true;
        } else if (e2.target === this._focusSink2 && e2.shiftKey && isActiveCellZeroZero) {
          this.stopFullBubbling(e2);
          if (this._options.showHeaderRow && this.getHeaderRow()) {
            this.focusHeaderRowFilter(true);
          } else {
            this.focusGridMenu();
          }
          handled = true;
        }
      }
      if (!handled) {
        if (this._options.enableCellNavigation && e2.ctrlKey && e2.key.toLowerCase() === "c" && !this._options.enableExcelCopyBuffer) {
          copyCellToClipboard({
            grid: this,
            cell: this.activeCell,
            row: this.activeRow,
            column: this.columns[this.activeCell],
            dataContext: this.getDataItem(this.activeRow)
          });
        } else if (!e2.shiftKey && !e2.altKey) {
          if (this._options.editable && this.currentEditor?.keyCaptureList) {
            if (this.currentEditor.keyCaptureList.indexOf(e2.which) > -1) {
              return;
            }
          }
          if (e2.ctrlKey && e2.key === "Home") {
            this.navigateTopStart();
          } else if (e2.ctrlKey && e2.key === "End") {
            this.navigateBottomEnd();
          } else if (e2.ctrlKey && e2.key === "ArrowUp") {
            this.navigateTop();
          } else if (e2.ctrlKey && e2.key === "ArrowDown") {
            this.navigateBottom();
          } else if (e2.ctrlKey && e2.key === "ArrowLeft" || !e2.ctrlKey && e2.key === "Home") {
            this.navigateRowStart();
          } else if (e2.ctrlKey && e2.key === "ArrowRight" || !e2.ctrlKey && e2.key === "End") {
            this.navigateRowEnd();
          }
        }
      }
      if (!handled) {
        if (e2.key === "Tab" && e2.shiftKey && !e2.ctrlKey && !e2.altKey) {
          if (this.activeRow === 0 && this.activeCell === 0) {
            this.focusHeaderRowFilter(true);
            handled = true;
          } else {
            handled = this.navigatePrev();
          }
        }
        if (!e2.shiftKey && !e2.altKey && !e2.ctrlKey && !handled) {
          if (e2.key === "Escape") {
            if (!this.getEditorLock()?.isActive()) {
              return;
            }
            this.cancelEditAndSetFocus();
          } else if (e2.key === "PageDown") {
            this.navigatePageDown();
            handled = true;
          } else if (e2.key === "PageUp") {
            this.navigatePageUp();
            handled = true;
          } else if (e2.key === "ArrowLeft") {
            handled = this.navigateLeft();
          } else if (e2.key === "ArrowRight") {
            handled = this.navigateRight();
          } else if (e2.key === "ArrowUp") {
            handled = this.navigateUp();
          } else if (e2.key === "ArrowDown") {
            handled = this.navigateDown();
          } else if (e2.key === "Tab") {
            handled = this.navigateNext();
          } else if (e2.key === "Enter") {
            if (this._options.editable) {
              if (this.currentEditor) {
                if (this.activeRow === this.getDataLength()) {
                  this.navigateDown();
                } else {
                  this.commitEditAndSetFocus();
                }
              } else {
                if (this.getEditorLock()?.commitCurrentEdit()) {
                  this.makeActiveCellEditable(void 0, void 0, e2);
                }
              }
            }
            handled = true;
          } else if (e2.key === "F2" && this._options.editable && !this.currentEditor) {
            this.makeActiveCellEditable(void 0, void 0, e2);
            handled = true;
          } else if (e2.key === "F6") {
            this.focusHeaderColumn();
            handled = true;
          }
        }
      }
      const cell = this.getActiveCell();
      const isChar = /^[\p{L}\p{N}\p{P}\p{S}\s]$/u.test(e2.key);
      if (!handled && this._options.autoEditByKeypress && cell && isChar && this.isCellEditable(cell.row, cell.cell) && !this.currentEditor) {
        this.makeActiveCellEditable(void 0, false, e2);
      }
      if (handled) {
        this.stopFullBubbling(e2);
      }
    }
    handleClick(evt) {
      const e2 = evt instanceof SlickEventData ? evt.getNativeEvent() : evt;
      if (!this.currentEditor) {
        if (e2.target !== document.activeElement || e2.target.classList.contains("slick-cell")) {
          const selection = this.getTextSelection();
          this.setFocus();
          this.setTextSelection(selection);
        }
      }
      const cell = this.getCellFromEvent(e2);
      if (!cell || this.currentEditor !== null && this.activeRow === cell.row && this.activeCell === cell.cell) {
        return;
      }
      evt = this.triggerEvent(this.onClick, { row: cell.row, cell: cell.cell }, evt || e2);
      if (evt.isImmediatePropagationStopped() || e2.defaultPrevented) {
        return;
      }
      if (this.canCellBeActive(cell.row, cell.cell)) {
        if (!this.getEditorLock()?.isActive() || this.getEditorLock()?.commitCurrentEdit()) {
          this.scrollRowIntoView(cell.row, false);
          const preClickModeOn = !!e2.target?.classList?.contains(preClickClassName);
          const column = this.columns[cell.cell];
          const suppressActiveCellChangedEvent = !!(this._options.editable && column?.editorClass && this._options.suppressActiveCellChangeOnEdit);
          this.setActiveCellInternal(this.getCellNode(cell.row, cell.cell), null, preClickModeOn, suppressActiveCellChangedEvent, e2);
        }
      }
    }
    handleContextMenu(e2) {
      const cellElm = e2.target.closest(".slick-cell");
      if (this.activeCellNode === cellElm && this.currentEditor !== null) {
        return;
      }
      const cell = this.getCellFromEvent(e2) ?? { cell: -1, row: -1 };
      this.triggerEvent(this.onContextMenu, { row: cell.row, cell: cell.cell }, e2);
    }
    handleDblClick(e2) {
      const cell = this.getCellFromEvent(e2);
      if (!cell || this.currentEditor !== null && this.activeRow === cell.row && this.activeCell === cell.cell) {
        return;
      }
      this.triggerEvent(this.onDblClick, { row: cell.row, cell: cell.cell }, e2);
      if (e2.defaultPrevented) {
        return;
      }
      if (this._options.editable) {
        this.gotoCell(cell.row, cell.cell, true, e2);
      }
    }
    handleHeaderMouseEnter(e2) {
      const column = Utils.storage.get(e2.target.closest(".slick-header-column"), "column");
      if (column) {
        this.triggerEvent(this.onHeaderMouseEnter, { column, grid: this }, e2);
      }
    }
    handleHeaderMouseOver(e2) {
      const column = Utils.storage.get(e2.target.closest(".slick-header-column"), "column");
      if (column) {
        this.triggerEvent(this.onHeaderMouseOver, { column, grid: this }, e2);
      }
    }
    handleHeaderMouseLeave(e2) {
      const column = Utils.storage.get(e2.target.closest(".slick-header-column"), "column");
      if (column) {
        this.triggerEvent(this.onHeaderMouseLeave, { column, grid: this }, e2);
      }
    }
    handleHeaderMouseOut(e2) {
      const column = Utils.storage.get(e2.target.closest(".slick-header-column"), "column");
      if (column) {
        this.triggerEvent(this.onHeaderMouseOut, { column, grid: this }, e2);
      }
    }
    handleHeaderRowMouseEnter(e2) {
      const column = Utils.storage.get(e2.target.closest(".slick-headerrow-column"), "column");
      if (column) {
        this.triggerEvent(this.onHeaderRowMouseEnter, { column, grid: this }, e2);
      }
    }
    handleHeaderRowMouseOver(e2) {
      const column = Utils.storage.get(e2.target.closest(".slick-headerrow-column"), "column");
      if (column) {
        this.triggerEvent(this.onHeaderRowMouseOver, { column, grid: this }, e2);
      }
    }
    handleHeaderRowMouseLeave(e2) {
      const column = Utils.storage.get(e2.target.closest(".slick-headerrow-column"), "column");
      if (column) {
        this.triggerEvent(this.onHeaderRowMouseLeave, { column, grid: this }, e2);
      }
    }
    handleHeaderRowMouseOut(e2) {
      const column = Utils.storage.get(e2.target.closest(".slick-headerrow-column"), "column");
      if (column) {
        this.triggerEvent(this.onHeaderRowMouseOut, { column, grid: this }, e2);
      }
    }
    handleHeaderContextMenu(e2) {
      const header = e2.target.closest(".slick-header-column");
      const column = header && Utils.storage.get(header, "column");
      this.triggerEvent(this.onHeaderContextMenu, { column }, e2);
    }
    handleHeaderClick(e2) {
      if (!this.columnResizeDragging) {
        const header = e2.target.closest(".slick-header-column");
        const column = header && Utils.storage.get(header, "column");
        if (column) {
          this.triggerEvent(this.onHeaderClick, { column }, e2);
        }
      }
    }
    handlePreHeaderContextMenu(e2) {
      this.triggerEvent(this.onPreHeaderContextMenu, { node: e2.target }, e2);
    }
    handlePreHeaderClick(e2) {
      if (!this.columnResizeDragging) {
        this.triggerEvent(this.onPreHeaderClick, { node: e2.target }, e2);
      }
    }
    handleFooterContextMenu(e2) {
      const footer = e2.target.closest(".slick-footerrow-column");
      const column = footer && Utils.storage.get(footer, "column");
      this.triggerEvent(this.onFooterContextMenu, { column }, e2);
    }
    handleFooterClick(e2) {
      const footer = e2.target.closest(".slick-footerrow-column");
      const column = footer && Utils.storage.get(footer, "column");
      this.triggerEvent(this.onFooterClick, { column }, e2);
    }
    handleCellMouseOver(e2) {
      this.triggerEvent(this.onMouseEnter, {}, e2);
    }
    handleCellMouseOut(e2) {
      this.triggerEvent(this.onMouseLeave, {}, e2);
    }
    cellExists(row, cell) {
      return !(row < 0 || row >= this.getDataLength() || cell < 0 || cell >= this.columns.length);
    }
    stopFullBubbling(e2) {
      if (e2) {
        e2.preventDefault();
        e2.stopPropagation();
      }
    }
    /**
     * Returns row and cell indexes by providing x,y coordinates.
     * Coordinates are relative to the top left corner of the grid beginning with the first row (not including the column headers).
     * @param x An x coordinate.
     * @param y A y coordinate.
     */
    getCellFromPoint(x2, y2) {
      let row = this.getRowFromPosition(y2);
      let cell = 0;
      let w2 = 0;
      for (let i = 0; i < this.columns.length && w2 <= x2; i++) {
        if (this.columns[i] && !this.columns[i].hidden) {
          w2 += this.columns[i].width;
          cell = i + 1;
        }
      }
      cell -= 1;
      if (row < -1) {
        row = -1;
      }
      return { row, cell };
    }
    getCellFromNode(cellNode) {
      const cls = /l\d+/.exec(cellNode.className);
      if (!cls) {
        throw new Error(`SlickGrid getCellFromNode: cannot get cell - ${cellNode.className}`);
      }
      return parseInt(cls[0].substr(1, cls[0].length - 1), 10);
    }
    getRowFromNode(rowNode) {
      for (const row in this.rowsCache) {
        if (this.rowsCache) {
          for (const i in this.rowsCache[row].rowNode) {
            if (this.rowsCache[row].rowNode?.[+i] === rowNode) {
              return row ? parseInt(row, 10) : 0;
            }
          }
        }
      }
      return null;
    }
    /**
     * Get frozen (pinned) row offset
     * @param {Number} row - grid row number
     */
    getFrozenRowOffset(row) {
      let offset2 = 0;
      if (this.hasFrozenRows) {
        if (this._options.frozenBottom) {
          if (row >= this.actualFrozenRow) {
            if (this.h < this.viewportTopH) {
              offset2 = this.actualFrozenRow * this._options.rowHeight;
            } else {
              offset2 = this.h;
            }
          } else {
            offset2 = 0;
          }
        } else {
          if (row >= this.actualFrozenRow) {
            offset2 = this.frozenRowsHeight;
          } else {
            offset2 = 0;
          }
        }
      } else {
        offset2 = 0;
      }
      return offset2;
    }
    /**
     * Returns row and cell indexes by providing a standard W3C event.
     * @param {*} event A standard W3C event.
     */
    getCellFromEvent(evt) {
      const e2 = evt instanceof SlickEventData ? evt.getNativeEvent() : evt;
      if (!e2) {
        return null;
      }
      const targetEvent = e2.touches ? e2.touches[0] : e2;
      const cellNode = e2.target.closest(".slick-cell");
      if (!cellNode) {
        return null;
      }
      let row = this.getRowFromNode(cellNode.parentNode);
      if (this.hasFrozenRows) {
        let rowOffset = 0;
        const c = getOffset(Utils.parents(cellNode, ".grid-canvas")[0]);
        const isBottom = Utils.parents(cellNode, ".grid-canvas-bottom").length;
        if (isBottom) {
          rowOffset = this._options.frozenBottom ? Utils.height(this._canvasTopL) : this.frozenRowsHeight;
        }
        const x2 = targetEvent.clientX - c.left;
        const y2 = targetEvent.clientY - c.top + rowOffset + document.documentElement.scrollTop;
        row = this.getCellFromPoint(x2, y2).row;
      }
      const cell = this.getCellFromNode(cellNode);
      if (!isDefinedNumber(row) || !isDefinedNumber(cell)) {
        return null;
      }
      return { row, cell };
    }
    /**
     * Returns an object representing information about a cell's position. All coordinates are absolute and take into consideration the visibility and scrolling position of all ancestors.
     * @param {Number} row - A row number.
     * @param {Number} cell - A column number.
     */
    getCellNodeBox(row, cell) {
      if (!this.cellExists(row, cell)) {
        return null;
      }
      const frozenRowOffset = this.getFrozenRowOffset(row);
      const y1 = this.getRowTop(row) - frozenRowOffset;
      const y2 = y1 + this._options.rowHeight - 1;
      let x1 = 0;
      for (let i = 0; i < cell; i++) {
        if (this.columns[i] && !this.columns[i].hidden) {
          x1 += this.columns[i].width || 0;
          if (this._options.frozenColumn === i) {
            x1 = 0;
          }
        }
      }
      const x2 = x1 + (this.columns[cell]?.width || 0);
      return {
        top: y1,
        left: x1,
        bottom: y2,
        right: x2
      };
    }
    // Cell switching
    /** Resets active cell by making cell normal and other internal resets. */
    resetActiveCell() {
      this.setActiveCellInternal(null, false);
    }
    /** Clear active cell by making cell normal & removing "active" CSS class. */
    unsetActiveCell() {
      if (isDefined(this.activeCellNode)) {
        this.makeActiveCellNormal();
        this.activeCellNode.classList.remove("active");
        this.rowsCache[this.activeRow]?.rowNode?.forEach((node) => node.classList.remove("active"));
      }
    }
    /**
     * Focus the grid.
     * Defaults to `cell`, which focuses the active cell (or first cell when no active cell exists).
     * @param mode - `cell` focuses active/first grid cell, `header` focuses first header menu/column entry point,
     * `internal` keeps legacy focus-sink behavior (primarily for internal/editor flows).
     */
    focus(mode = "cell") {
      if (mode === "header") {
        this.focusHeaderMenuOrColumn(0);
        return;
      } else if (mode === "cell") {
        this.focusGridCell();
        return;
      }
      this.setFocus();
    }
    setFocus() {
      if (this.tabbingDirection === -1) {
        this._focusSink.focus();
      } else {
        this._focusSink2.focus();
      }
    }
    /** Scroll to a specific cell and make it into the view */
    scrollCellIntoView(row, cell, doPaging) {
      this.scrollRowIntoView(row, doPaging);
      if (cell > this._options.frozenColumn) {
        const colspan = this.getColspan(row, cell);
        this.internalScrollColumnIntoView(this.columnPosLeft[cell], this.columnPosRight[cell + (colspan > 1 ? colspan - 1 : 0)]);
      }
    }
    internalScrollColumnIntoView(left, right) {
      const scrollRight = this.scrollLeft + Utils.width(this._viewportScrollContainerX) - (this.viewportHasVScroll ? this.scrollbarDimensions?.width || 0 : 0);
      if (left < this.scrollLeft) {
        this._viewportScrollContainerX.scrollLeft = left;
        this.handleScroll();
        this.render();
      } else if (right > scrollRight) {
        this._viewportScrollContainerX.scrollLeft = Math.min(left, right - this._viewportScrollContainerX.clientWidth);
        this.handleScroll();
        this.render();
      }
    }
    /**
     * Scroll to a specific column and show it into the viewport
     * @param {Number} cell - cell column number
     */
    scrollColumnIntoView(cell) {
      this.internalScrollColumnIntoView(this.columnPosLeft[cell], this.columnPosRight[cell]);
    }
    setActiveCellInternal(newCell, opt_editMode, preClickModeOn, suppressActiveCellChangedEvent, e2) {
      this.unsetActiveCell();
      this.activeCellNode = newCell;
      if (isDefined(this.activeCellNode)) {
        const activeCellOffset = getOffset(this.activeCellNode);
        let rowOffset = Math.floor(getOffset(Utils.parents(this.activeCellNode, ".grid-canvas")[0]).top);
        const isBottom = Utils.parents(this.activeCellNode, ".grid-canvas-bottom").length;
        if (this.hasFrozenRows && isBottom) {
          rowOffset -= this._options.frozenBottom ? Utils.height(this._canvasTopL) : this.frozenRowsHeight;
        }
        const cell = this.getCellFromPoint(activeCellOffset.left, Math.ceil(activeCellOffset.top) - rowOffset);
        this.activeRow = this.activePosY = cell.row;
        this.activeCell = this.activePosX = this.getCellFromNode(this.activeCellNode);
        if (!isDefined(opt_editMode) && this._options.autoEditNewRow) {
          opt_editMode = this.activeRow === this.getDataLength() || this._options.autoEdit;
        }
        if (this._options.showCellSelection) {
          document.querySelectorAll(".slick-cell.active").forEach((node) => node.classList.remove("active"));
          this.activeCellNode.classList.add("active");
          this.rowsCache[this.activeRow]?.rowNode?.forEach((node) => node.classList.add("active"));
        }
        if (opt_editMode && this.isCellEditable(this.activeRow, this.activeCell)) {
          if (this._options.asyncEditorLoading) {
            clearTimeout(this.h_editorLoader);
            this.h_editorLoader = setTimeout(() => {
              this.makeActiveCellEditable(void 0, preClickModeOn, e2);
            }, this._options.asyncEditorLoadDelay);
          } else {
            this.makeActiveCellEditable(void 0, preClickModeOn, e2);
          }
        }
      } else {
        this.activeRow = this.activeCell = null;
      }
      if (!suppressActiveCellChangedEvent) {
        this.triggerEvent(this.onActiveCellChanged, this.getActiveCell());
      }
    }
    /** Check if cell is editable and check if grid is also editable */
    isCellEditable(row, cell) {
      return !!(this._options.editable && this.isCellPotentiallyEditable(row, cell));
    }
    /** Check if cell is potentially editable but without validating that the grid is editable */
    isCellPotentiallyEditable(row, cell) {
      const dataLength = this.getDataLength();
      if (row < dataLength && !this.getDataItem(row)) {
        return false;
      }
      if (this.columns[cell].cannotTriggerInsert && row >= dataLength) {
        return false;
      }
      if (!this.columns[cell] || this.columns[cell].hidden || !this.getEditor(row, cell)) {
        return false;
      }
      return true;
    }
    /**
     * Make the cell normal again (for example after destroying cell editor),
     * we can also optionally refocus on the current active cell (again possibly after closing cell editor)
     * @param {Boolean} [refocusActiveCell]
     */
    makeActiveCellNormal(refocusActiveCell = false) {
      if (this.currentEditor) {
        this.triggerEvent(this.onBeforeCellEditorDestroy, { editor: this.currentEditor });
        this.currentEditor.destroy();
        this.currentEditor = null;
        if (this.activeCellNode) {
          const d2 = this.getDataItem(this.activeRow);
          this.activeCellNode.classList.remove("editable", "invalid");
          if (d2) {
            const column = this.columns[this.activeCell];
            const formatter = this.getFormatter(this.activeRow, column);
            const formatterResult = formatter(this.activeRow, this.activeCell, this.getDataItemValueForColumn(d2, column), column, d2, this);
            this.applyFormatResultToCellNode(formatterResult, this.activeCellNode);
            this.invalidatePostProcessingResults(this.activeRow);
          }
          if (refocusActiveCell) {
            this.setFocus();
          }
        }
        this.getEditorLock()?.deactivate(this.editController);
      }
    }
    editActiveCell(editor, preClickModeOn, e2) {
      this.makeActiveCellEditable(editor, preClickModeOn, e2);
    }
    makeActiveCellEditable(editor, preClickModeOn, e2) {
      if (!this.activeCellNode) {
        return;
      }
      if (!this._options.editable) {
        throw new Error("SlickGrid makeActiveCellEditable : should never get called when grid options.editable is false");
      }
      clearTimeout(this.h_editorLoader);
      if (!this.isCellPotentiallyEditable(this.activeRow, this.activeCell)) {
        return;
      }
      const columnDef = this.columns[this.activeCell];
      const item = this.getDataItem(this.activeRow);
      if (this.triggerEvent(this.onBeforeEditCell, {
        row: this.activeRow,
        cell: this.activeCell,
        item,
        column: columnDef,
        target: "grid"
      }).getReturnValue() === false) {
        this.setFocus();
        return;
      }
      this.getEditorLock()?.activate(this.editController);
      this.activeCellNode.classList.add("editable");
      const useEditor = editor || this.getEditor(this.activeRow, this.activeCell);
      if (typeof useEditor === "function") {
        if (!editor && !useEditor.suppressClearOnEdit) {
          emptyElement(this.activeCellNode);
        }
        let metadata = this.getItemMetadaWhenExists(this.activeRow);
        metadata = metadata?.columns;
        const columnMetaData = metadata && (metadata[columnDef.id] || metadata[this.activeCell]);
        const editorArgs = {
          grid: this,
          gridPosition: this.absBox(this._container),
          position: this.absBox(this.activeCellNode),
          container: this.activeCellNode,
          column: columnDef,
          columnMetaData,
          item: item || {},
          isCompositeEditor: false,
          event: e2,
          commitChanges: this.commitEditAndSetFocus.bind(this),
          cancelChanges: this.cancelEditAndSetFocus.bind(this)
        };
        this.currentEditor = new useEditor(editorArgs);
        if (item && this.currentEditor) {
          this.currentEditor.loadValue(item);
          if (preClickModeOn && typeof this.currentEditor?.preClick === "function") {
            this.currentEditor.preClick();
          }
        }
        this.serializedEditorValue = this.currentEditor?.serializeValue();
        if (this.currentEditor?.position) {
          this.handleActiveCellPositionChange();
        }
      }
    }
    commitEditAndSetFocus(navigateCellDown = true) {
      if (this.getEditorLock()?.commitCurrentEdit()) {
        this.setFocus();
        if (this._options.autoEdit && !this._options.autoCommitEdit && navigateCellDown) {
          this.navigateDown();
        }
      }
    }
    cancelEditAndSetFocus() {
      if (this.getEditorLock()?.cancelCurrentEdit()) {
        this.setFocus();
      }
    }
    /**
     * Computes the absolute position of an element relative to the document,
     * taking into account offsets, scrolling, and visibility within scrollable containers.
     */
    absBox(elem) {
      const rect = elem.getBoundingClientRect();
      const box = {
        top: rect.top,
        left: rect.left,
        bottom: rect.bottom,
        right: rect.right,
        width: rect.width,
        height: rect.height,
        visible: true
      };
      if (rect.bottom === 0 && rect.top === 0) {
        return box;
      }
      const gridRect = this._container?.getBoundingClientRect() || { top: 0, left: 0, bottom: 0, right: 0 };
      box.top = rect.top - gridRect.top;
      box.left = rect.left - gridRect.left;
      box.bottom = rect.bottom - gridRect.top;
      box.right = rect.right - gridRect.left;
      if (box.bottom < 0 || box.top > (this._container?.clientHeight ?? window.innerHeight) || box.right < 0 || box.left > (this._container?.clientWidth ?? window.innerWidth)) {
        box.visible = false;
      }
      return box;
    }
    /** Returns an object representing information about the active cell's position. All coordinates are absolute and take into consideration the visibility and scrolling position of all ancestors. */
    getActiveCellPosition() {
      return this.absBox(this.activeCellNode);
    }
    /** Get the Grid Position */
    getGridPosition() {
      return this.absBox(this._container);
    }
    /** Returns the active cell editor. If there is no actively edited cell, null is returned.   */
    getCellEditor() {
      return this.currentEditor;
    }
    /**
     * Returns an object representing the coordinates of the currently active cell:
     * @example	`{ row: activeRow, cell: activeCell }`
     */
    getActiveCell() {
      if (this.activeCellNode) {
        return { row: this.activeRow, cell: this.activeCell };
      }
      return null;
    }
    /** Returns the DOM element containing the currently active cell. If no cell is active, null is returned. */
    getActiveCellNode() {
      return this.activeCellNode;
    }
    // This get/set methods are used for keeping text-selection. These don't consider IE because they don't loose text-selection.
    // Fix for firefox selection. See https://github.com/mleibman/SlickGrid/pull/746/files
    getTextSelection() {
      let textSelection = null;
      if (window.getSelection) {
        const selection = window.getSelection();
        if ((selection?.rangeCount || 0) > 0) {
          textSelection = selection.getRangeAt(0);
        }
      }
      return textSelection;
    }
    setTextSelection(selection) {
      if (window.getSelection && selection) {
        const target = window.getSelection();
        if (target) {
          target.removeAllRanges();
          target.addRange(selection);
        }
      }
    }
    /**
     * Scroll to a specific row and make it into the view
     * @param {Number} row - grid row number
     * @param {Boolean} doPaging - scroll when pagination is enabled
     */
    scrollRowIntoView(row, doPaging) {
      if (!this.hasFrozenRows || !this._options.frozenBottom && row > this.actualFrozenRow - 1 || this._options.frozenBottom && row < this.actualFrozenRow - 1) {
        const viewportScrollH = Utils.height(this._viewportScrollContainerY);
        const rowNumber = this.hasFrozenRows && !this._options.frozenBottom ? row - this._options.frozenRow : row;
        const rowAtTop = rowNumber * this._options.rowHeight;
        const rowAtBottom = (rowNumber + 1) * this._options.rowHeight - viewportScrollH + (this.viewportHasHScroll ? this.scrollbarDimensions?.height || 0 : 0);
        if ((rowNumber + 1) * this._options.rowHeight > this.scrollTop + viewportScrollH + this.offset) {
          this.scrollTo(doPaging ? rowAtTop : rowAtBottom);
          this.render();
        } else if (rowNumber * this._options.rowHeight < this.scrollTop + this.offset) {
          this.scrollTo(doPaging ? rowAtBottom : rowAtTop);
          this.render();
        }
      }
    }
    /**
     * Scroll to the top row and make it into the view
     * @param {Number} row - grid row number
     */
    scrollRowToTop(row) {
      this.scrollTo(row * this._options.rowHeight);
      this.render();
    }
    scrollPage(dir) {
      const deltaRows = dir * this.numVisibleRows;
      const bottomOfTopmostFullyVisibleRow = this.scrollTop + this._options.rowHeight - 1;
      this.scrollTo((this.getRowFromPosition(bottomOfTopmostFullyVisibleRow) + deltaRows) * this._options.rowHeight);
      this.render();
      if (this._options.enableCellNavigation && isDefined(this.activeRow)) {
        let row = this.activeRow + deltaRows;
        const dataLengthIncludingAddNew = this.getDataLengthIncludingAddNew();
        if (row >= dataLengthIncludingAddNew) {
          row = dataLengthIncludingAddNew - 1;
        }
        if (row < 0) {
          row = 0;
        }
        const pos = dir === 1 ? this.gotoDown(row - 1 || 0, this.activeCell, this.activePosY, this.activePosX) : this.gotoUp(row + 1, this.activeCell, this.activePosY, this.activePosX);
        this.navigateToPos(pos);
      }
    }
    /** Navigate (scroll) by a page down */
    navigatePageDown() {
      this.unsetActiveCell();
      this.scrollPage(1);
    }
    /** Navigate (scroll) by a page up */
    navigatePageUp() {
      this.unsetActiveCell();
      this.scrollPage(-1);
    }
    /** Navigate to the top of the grid */
    navigateTop() {
      this.unsetActiveCell();
      this.navigateToRow(0);
    }
    /** Navigate to the bottom of the grid */
    navigateBottom() {
      const row = this.getDataLength() - 1;
      let tmpRow = this.getParentRowSpanByCell(row, this.activeCell)?.start ?? row;
      do {
        if (this._options.enableCellRowSpan) {
          this.setActiveRow(tmpRow);
        }
        const isValidMode = this.navigateToRow(tmpRow);
        if (isValidMode && this.activeCell === this.activePosX || !isDefined(this.activeCell)) {
          break;
        }
      } while (--tmpRow > 0);
    }
    navigateToRow(row) {
      const num_rows = this.getDataLength();
      if (!num_rows) {
        return false;
      }
      if (row < 0) {
        row = 0;
      } else if (row >= num_rows) {
        row = num_rows - 1;
      }
      this.scrollCellIntoView(row, 0, true);
      let isValidMove = !isDefined(this.activeCell) || !isDefined(this.activeRow);
      if (this._options.enableCellNavigation && isDefined(this.activeRow)) {
        let cell = 0;
        let prevCell = null;
        const prevActivePosX = this.activePosX;
        while (cell <= this.activePosX) {
          if (this.canCellBeActive(row, cell)) {
            prevCell = cell;
            if (!isDefined(this.activeCell) || cell === this.activeCell) {
              isValidMove = true;
            }
          }
          cell += this.getColspan(row, cell);
        }
        if (prevCell !== null) {
          this.setActiveCellInternal(this.getCellNode(row, prevCell));
          this.activePosX = prevActivePosX;
        } else {
          this.resetActiveCell();
        }
      }
      return isValidMove;
    }
    getColspan(row, cell) {
      const metadata = this.getItemMetadaWhenExists(row);
      if (!metadata || !metadata.columns || this.columns[cell]?.hidden) {
        return 1;
      }
      if (cell >= this.columns.length) {
        cell = this.columns.length - 1;
      }
      let isFullColspan = false;
      const columnData = metadata.columns[this.columns[cell].id] || metadata.columns[cell];
      let colspan = columnData?.colspan;
      if (colspan === "*") {
        isFullColspan = true;
        colspan = this.columns.length - cell;
      } else {
        colspan = colspan || 1;
      }
      if (!isFullColspan && this._options.spreadHiddenColspan) {
        return this.increaseHiddenColspan(colspan, cell);
      }
      return colspan;
    }
    getRowspan(row, cell) {
      let rowspan = 1;
      const metadata = this.getItemMetadaWhenExists(row);
      if (metadata?.columns) {
        Object.keys(metadata.columns).forEach((col) => {
          const colIdx = Number(col);
          if (colIdx === cell) {
            const columnMeta = metadata.columns[colIdx];
            rowspan = Number(columnMeta?.rowspan || 1);
          }
        });
      }
      return rowspan;
    }
    findFocusableRow(row, cell, dir) {
      let r2 = row;
      const rowRange = this._colsWithRowSpanCache[cell] || /* @__PURE__ */ new Set();
      let found = false;
      Array.from(rowRange).forEach((rrange) => {
        const [start, end] = rrange.split(":").map(Number);
        if (!found && row >= start && row <= end) {
          r2 = dir === "up" ? start : end;
          if (this.canCellBeActive(r2, cell)) {
            found = true;
          }
        }
      });
      return r2;
    }
    findFirstFocusableCell(row) {
      let cell = 0;
      let focusableRow = row;
      let ff = -1;
      while (cell < this.columns.length) {
        const prs = this.getParentRowSpanByCell(row, cell);
        focusableRow = prs !== null && prs.start !== row ? prs.start : row;
        if (this.canCellBeActive(focusableRow, cell)) {
          ff = cell;
          break;
        }
        cell += this.getColspan(focusableRow, cell);
      }
      return { cell: ff, row: focusableRow };
    }
    findLastFocusableCell(row) {
      let cell = 0;
      let focusableRow = row;
      let lf = -1;
      while (cell < this.columns.length) {
        const prs = this.getParentRowSpanByCell(row, cell);
        focusableRow = prs !== null && prs.start !== row ? prs.start : row;
        if (this.canCellBeActive(focusableRow, cell)) {
          lf = cell;
        }
        cell += this.getColspan(focusableRow, cell);
      }
      return { cell: lf, row: focusableRow };
    }
    /**
     * From any row/cell indexes that might have colspan/rowspan, find its starting indexes
     * For example, if we start at 0,0 and we have colspan/rowspan of 4 for both and our indexes is row:2,cell:3
     * then our starting row/cell is 0,0. If a cell has no spanning at all then row/cell output is same as input
     */
    findSpanStartingCell(row, cell) {
      cell = this.findNextAvailableColumnCell(cell);
      const prs = this.getParentRowSpanByCell(row, cell);
      const focusableRow = prs !== null && prs.start !== row ? prs.start : row;
      let fc = 0;
      let prevCell = 0;
      while (fc < this.columns.length) {
        fc += this.getColspan(focusableRow, fc);
        if (fc > cell) {
          fc = prevCell;
          return { cell: fc, row: focusableRow };
        }
        prevCell = fc;
      }
      return { cell: fc, row: focusableRow };
    }
    gotoRight(_row, cell, posY, _posX) {
      if (cell < this.columns.length) {
        let fc = cell + 1;
        let fr = posY;
        do {
          const sc = this.findSpanStartingCell(posY, fc);
          fr = sc.row;
          fc = this.findNextAvailableColumnCell(sc.cell);
          if (this.canCellBeActive(fr, fc) && fc > cell) {
            break;
          }
          fc += this.getColspan(fr, sc.cell);
        } while (fc < this.columns.length);
        if (fc < this.columns.length) {
          return {
            row: fr,
            cell: fc,
            posX: fc,
            posY
          };
        }
      }
      return null;
    }
    gotoLeft(row, cell, posY, _posX) {
      const ff = this.findFirstFocusableCell(row);
      if (cell <= 0 || ff.cell >= cell) {
        return null;
      }
      let pos;
      let prev = {
        row,
        cell: ff.cell,
        posX: ff.cell,
        posY
      };
      while (true) {
        pos = this.gotoRight(prev.row, prev.cell, prev.posY, prev.posX);
        if (!pos) {
          return null;
        }
        if (pos.cell >= cell) {
          const nextRow = this.findFocusableRow(posY, prev.cell, "up");
          if (nextRow !== prev.row) {
            prev.row = nextRow;
          }
          return prev;
        }
        prev = pos;
      }
    }
    gotoDown(row, cell, _posY, posX) {
      let prevCell;
      const ub = this.getDataLengthIncludingAddNew();
      do {
        row += this.getRowspan(row, posX);
        prevCell = cell = 0;
        while (cell <= posX) {
          prevCell = this.findNextAvailableColumnCell(cell);
          cell += this.getColspan(row, cell);
        }
      } while (row <= ub && !this.canCellBeActive(row, prevCell));
      if (row <= ub) {
        return {
          row,
          cell: prevCell,
          posX,
          posY: row
        };
      }
      return null;
    }
    gotoUp(row, cell, _posY, posX) {
      let prevCell;
      if (row > 0) {
        do {
          row = this.findFocusableRow(row - 1, posX, "up");
          prevCell = cell = 0;
          while (cell <= posX) {
            prevCell = this.findNextAvailableColumnCell(cell);
            cell += this.getColspan(row, cell);
          }
        } while (row >= 0 && !this.canCellBeActive(row, prevCell));
        if (cell <= this.columns.length) {
          return {
            row,
            cell: prevCell,
            posX,
            posY: row
          };
        }
      }
      return null;
    }
    gotoNext(row, cell, posY, posX) {
      if (!isDefinedNumber(row) && !isDefinedNumber(cell)) {
        row = cell = posY = posX = 0;
        if (this.canCellBeActive(row, cell)) {
          return {
            row,
            cell,
            posX: cell,
            posY
          };
        }
      }
      let pos = this.gotoRight(row, cell, posY, posX);
      if (!pos) {
        let ff;
        while (!pos && ++posY < this.getDataLength() + (this._options.enableAddRow ? 1 : 0)) {
          ff = this.findFirstFocusableCell(posY);
          row = this.getParentRowSpanByCell(posY, ff.cell)?.start ?? posY;
          pos = {
            row,
            cell: ff.cell,
            posX: ff.cell,
            posY
          };
        }
      }
      return pos;
    }
    gotoPrev(row, cell, posY, posX) {
      if (!isDefinedNumber(row) && !isDefinedNumber(cell)) {
        row = posY = this.getDataLengthIncludingAddNew() - 1;
        cell = posX = this.columns.length - 1;
        if (this.canCellBeActive(row, cell)) {
          return {
            row,
            cell,
            posX: cell,
            posY
          };
        }
      }
      let pos = this.gotoLeft(row, cell, posY, posX);
      if (!pos) {
        let lf;
        while (!pos && --posY >= 0) {
          lf = this.findLastFocusableCell(posY);
          if (lf.cell > -1) {
            row = this.getParentRowSpanByCell(posY, lf.cell)?.start ?? posY;
            pos = {
              row,
              cell: lf.cell,
              posX: lf.cell,
              posY
            };
          }
        }
      }
      return pos;
    }
    gotoRowStart(row, _cell, _posY, _posX) {
      const ff = this.findFirstFocusableCell(row);
      return {
        row: ff.row,
        cell: ff.cell,
        posX: ff.cell,
        posY: row
      };
    }
    gotoRowEnd(row, _cell, _posY, _posX) {
      const lf = this.findLastFocusableCell(row);
      if (lf.cell === -1) {
        return null;
      }
      return {
        row: lf.row,
        cell: lf.cell,
        posX: lf.cell,
        posY: row
      };
    }
    /** find the next available column to the right */
    findNextAvailableColumnCell(cell) {
      let availableCell = cell;
      if (this.columns[availableCell]) {
        while (this.columns[availableCell].hidden || availableCell > this.columns.length) {
          availableCell++;
        }
      }
      return availableCell;
    }
    // when dealing with colspan, we'll count hidden columns and increase colspan when that happens
    increaseHiddenColspan(colspan, cell) {
      if (colspan > 1) {
        let hiddenCount = 0;
        for (let k2 = cell; k2 < cell + colspan; k2++) {
          if (this.columns[k2]?.hidden) {
            hiddenCount++;
          }
        }
        if (hiddenCount > 0) {
          colspan = colspan + hiddenCount;
        }
      }
      return colspan;
    }
    /** Switches the active cell one cell right skipping unselectable cells. Unline navigateNext, navigateRight stops at the last cell of the row. Returns a boolean saying whether it was able to complete or not. */
    navigateRight() {
      return this.navigate("right");
    }
    /** Switches the active cell one cell left skipping unselectable cells. Unline navigatePrev, navigateLeft stops at the first cell of the row. Returns a boolean saying whether it was able to complete or not. */
    navigateLeft() {
      return this.navigate("left");
    }
    /** Switches the active cell one row down skipping unselectable cells. Returns a boolean saying whether it was able to complete or not. */
    navigateDown() {
      return this.navigate("down");
    }
    /** Switches the active cell one row up skipping unselectable cells. Returns a boolean saying whether it was able to complete or not. */
    navigateUp() {
      return this.navigate("up");
    }
    /** Tabs over active cell to the next selectable cell. Returns a boolean saying whether it was able to complete or not. */
    navigateNext() {
      return this.navigate("next");
    }
    /** Tabs over active cell to the previous selectable cell. Returns a boolean saying whether it was able to complete or not. */
    navigatePrev() {
      return this.navigate("prev");
    }
    /** Navigate to the start row in the grid */
    navigateRowStart() {
      return this.navigate("home");
    }
    /** Navigate to the end row in the grid */
    navigateRowEnd() {
      return this.navigate("end");
    }
    /** Navigate to coordinate 0,0 (top left home) */
    navigateTopStart() {
      this.unsetActiveCell();
      this.navigateToRow(0);
      return this.navigate("home");
    }
    /** Navigate to bottom row end (bottom right end) */
    navigateBottomEnd() {
      this.navigateBottom();
      return this.navigate("end");
    }
    /**
     * @param {string} dir Navigation direction.
     * @return {boolean} Whether navigation resulted in a change of active cell.
     */
    navigate(dir) {
      if (!this._options.enableCellNavigation) {
        return false;
      }
      if (!this.activeCellNode && dir !== "prev" && dir !== "next") {
        return false;
      }
      if (!this.getEditorLock()?.commitCurrentEdit()) {
        return true;
      }
      this.setFocus();
      this.unsetActiveCell();
      const tabbingDirections = {
        up: -1,
        down: 1,
        left: -1,
        right: 1,
        prev: -1,
        next: 1,
        home: -1,
        end: 1
      };
      this.tabbingDirection = tabbingDirections[dir];
      const stepFunctions = {
        up: this.gotoUp,
        down: this.gotoDown,
        left: this.gotoLeft,
        right: this.gotoRight,
        prev: this.gotoPrev,
        next: this.gotoNext,
        home: this.gotoRowStart,
        end: this.gotoRowEnd
      };
      const stepFn = stepFunctions[dir];
      const pos = stepFn.call(this, this.activeRow, this.activeCell, this.activePosY, this.activePosX);
      return this.navigateToPos(pos);
    }
    navigateToPos(pos) {
      if (pos) {
        if (this.hasFrozenRows && this._options.frozenBottom && pos.row === this.getDataLength()) {
          return;
        }
        const isAddNewRow = pos.row === this.getDataLength();
        if (!this._options.frozenBottom && pos.row >= this.actualFrozenRow || this._options.frozenBottom && pos.row < this.actualFrozenRow) {
          this.scrollCellIntoView(pos.row, pos.cell, !isAddNewRow && this._options.emulatePagingWhenScrolling);
        }
        this.setActiveCellInternal(this.getCellNode(pos.row, pos.cell));
        this.activePosX = pos.posX;
        this.activePosY = pos.posY;
        return true;
      } else {
        this.setActiveCellInternal(this.getCellNode(this.activeRow, this.activeCell));
        return false;
      }
    }
    /**
     * Returns a DOM element containing a cell at a given row and cell.
     * @param row A row index.
     * @param cell A column index.
     */
    getCellNode(row, cell) {
      if (this.rowsCache[row]) {
        this.ensureCellNodesInRowsCache(row);
        try {
          if (this.rowsCache[row].cellNodesByColumnIdx.length > cell) {
            return this.rowsCache[row].cellNodesByColumnIdx[cell];
          }
          return null;
        } catch (_e) {
          return this.rowsCache[row].cellNodesByColumnIdx[cell];
        }
      }
      return null;
    }
    /**
     * Sets an active cell.
     * @param {number} row - A row index.
     * @param {number} cell - A column index.
     * @param {boolean} [optionEditMode] Option Edit Mode is Auto-Edit?
     * @param {boolean} [preClickModeOn] Pre-Click Mode is Enabled?
     * @param {boolean} [suppressActiveCellChangedEvent] Are we suppressing Active Cell Changed Event (defaults to false)
     */
    setActiveCell(row, cell, opt_editMode, preClickModeOn, suppressActiveCellChangedEvent) {
      if (!this.initialized || !this._options.enableCellNavigation || row > this.getDataLength() || row < 0 || cell >= this.columns.length || cell < 0) {
        return;
      }
      this.scrollCellIntoView(row, cell, false);
      this.setActiveCellInternal(this.getCellNode(row, cell), opt_editMode, preClickModeOn, suppressActiveCellChangedEvent);
    }
    /**
     * Sets an active cell.
     * @param {number} row - A row index.
     * @param {number} cell - A column index.
     * @param {boolean} [suppressScrollIntoView] - optionally suppress the ScrollIntoView that happens by default (defaults to false)
     */
    setActiveRow(row, cell, suppressScrollIntoView) {
      cell ??= 0;
      if (!this.initialized || row > this.getDataLength() || row < 0 || cell >= this.columns.length || cell < 0) {
        return;
      }
      this.activeRow = row;
      if (!suppressScrollIntoView) {
        this.scrollCellIntoView(row, cell, false);
      }
    }
    /**
     * Returns true if you can click on a given cell and make it the active focus.
     * @param {number} row A row index.
     * @param {number} col A column index.
     */
    canCellBeActive(row, cell) {
      if (!this._options.enableCellNavigation || row >= this.getDataLengthIncludingAddNew() || row < 0 || cell >= this.columns.length || cell < 0) {
        return false;
      }
      if (!this.columns[cell] || this.columns[cell].hidden) {
        return false;
      }
      const spanRow = this.getParentRowSpanByCell(row, cell)?.start ?? row;
      if (spanRow !== row) {
        return false;
      }
      const rowMetadata = this.getItemMetadaWhenExists(row);
      if (rowMetadata?.focusable !== void 0) {
        return !!rowMetadata.focusable;
      }
      const columnMetadata = rowMetadata?.columns;
      if (columnMetadata?.[this.columns[cell].id]?.focusable !== void 0) {
        return !!columnMetadata[this.columns[cell].id].focusable;
      }
      if (columnMetadata?.[cell]?.focusable !== void 0) {
        return !!columnMetadata[cell].focusable;
      }
      return !!this.columns[cell].focusable;
    }
    /**
     * Returns true if selecting the row causes this particular cell to have the selectedCellCssClass applied to it. A cell can be selected if it exists and if it isn't on an empty / "Add New" row and if it is not marked as "unselectable" in the column definition.
     * @param {number} row A row index.
     * @param {number} col A column index.
     */
    canCellBeSelected(row, cell) {
      if (row >= this.getDataLength() || row < 0 || cell >= this.columns.length || cell < 0) {
        return false;
      }
      if (!this.columns[cell] || this.columns[cell].hidden) {
        return false;
      }
      const rowMetadata = this.getItemMetadaWhenExists(row);
      if (rowMetadata?.selectable !== void 0) {
        return !!rowMetadata.selectable;
      }
      const columnMetadata = rowMetadata?.columns && (rowMetadata.columns[this.columns[cell].id] || rowMetadata.columns[cell]);
      if (columnMetadata?.selectable !== void 0) {
        return !!columnMetadata.selectable;
      }
      return !!this.columns[cell].selectable;
    }
    /**
     * Accepts a row integer and a cell integer, scrolling the view to the row where row is its row index, and cell is its cell index. Optionally accepts a forceEdit boolean which, if true, will attempt to initiate the edit dialogue for the field in the specified cell.
     * Unlike setActiveCell, this scrolls the row into the viewport and sets the keyboard focus.
     * @param {Number} row A row index.
     * @param {Number} cell A column index.
     * @param {Boolean} [forceEdit] If true, will attempt to initiate the edit dialogue for the field in the specified cell.
     */
    gotoCell(row, cell, forceEdit, e2) {
      if (this.initialized && this.canCellBeActive(row, cell) && this.getEditorLock()?.commitCurrentEdit()) {
        this.scrollCellIntoView(row, cell, false);
        const newCell = this.getCellNode(row, cell);
        const column = this.columns[cell];
        const suppressActiveCellChangedEvent = !!(this._options.editable && column?.editorClass && this._options.suppressActiveCellChangeOnEdit);
        this.setActiveCellInternal(newCell, forceEdit || row === this.getDataLength() || this._options.autoEdit, null, suppressActiveCellChangedEvent, e2);
        if (!this.currentEditor) {
          this.setFocus();
        }
      }
    }
    // IEditor implementation for the editor lock
    commitCurrentEdit() {
      const self = this;
      const item = self.getDataItem(self.activeRow);
      const column = self.columns[self.activeCell];
      if (self.currentEditor) {
        if (self.currentEditor.isValueChanged()) {
          const validationResults = self.currentEditor.validate(void 0, {
            rowIndex: self.activeRow,
            cellIndex: self.activeCell
          });
          if (validationResults.valid) {
            const row = self.activeRow;
            const cell = self.activeCell;
            const editor = self.currentEditor;
            const serializedValue = self.currentEditor.serializeValue();
            const prevSerializedValue = self.serializedEditorValue;
            if (self.activeRow < self.getDataLength()) {
              const editCommand = {
                row,
                cell,
                editor,
                serializedValue,
                prevSerializedValue,
                execute: () => {
                  editor.applyValue(item, serializedValue);
                  self.updateRow(row);
                  self.triggerEvent(self.onCellChange, { command: "execute", row, cell, item, column });
                },
                undo: () => {
                  editor.applyValue(item, prevSerializedValue);
                  self.updateRow(row);
                  self.triggerEvent(self.onCellChange, { command: "undo", row, cell, item, column });
                }
              };
              if (self._options.editCommandHandler) {
                self.makeActiveCellNormal(true);
                self._options.editCommandHandler(item, column, editCommand);
              } else {
                editCommand.execute();
                self.makeActiveCellNormal(true);
              }
            } else {
              const newItem = {};
              self.currentEditor.applyValue(newItem, self.currentEditor.serializeValue());
              self.makeActiveCellNormal(true);
              self.triggerEvent(self.onAddNewRow, { item: newItem, column });
            }
            return !self.getEditorLock()?.isActive();
          } else {
            if (self.activeCellNode) {
              self.activeCellNode.classList.remove("invalid");
              Utils.width(self.activeCellNode);
              self.activeCellNode.classList.add("invalid");
            }
            self.triggerEvent(self.onValidationError, {
              editor: self.currentEditor,
              cellNode: self.activeCellNode,
              validationResults,
              row: self.activeRow,
              cell: self.activeCell,
              column
            });
            self.currentEditor.focus();
            return false;
          }
        }
        self.makeActiveCellNormal(true);
      }
      return true;
    }
    cancelCurrentEdit() {
      this.makeActiveCellNormal();
      return true;
    }
    rowsToRanges(rows) {
      const ranges = [];
      const lastCell = this.columns.length - 1;
      for (let i = 0; i < rows.length; i++) {
        ranges.push(new SlickRange(rows[i], 0, rows[i], lastCell));
      }
      return ranges;
    }
    /** Returns an array of row indices corresponding to the currently selected rows. */
    getSelectedRows() {
      if (!this.selectionModel) {
        throw new Error("SlickGrid Selection model is not set");
      }
      return this.selectedRows.slice(0);
    }
    /**
     * Accepts an array of row indices and applies the current selectedCellCssClass to the cells in the row, respecting whether cells have been flagged as selectable.
     * @param {Array<number>} rowsArray - an array of row numbers.
     * @param {String} [caller] - an optional string to identify who called the method
     */
    setSelectedRows(rows, caller) {
      if (!this.selectionModel) {
        throw new Error("SlickGrid Selection model is not set");
      }
      const elock = this.getEditorLock();
      if (typeof elock?.isActive === "function" && !elock.isActive()) {
        this.selectionModel.setSelectedRanges(this.rowsToRanges(rows), caller || "SlickGrid.setSelectedRows");
      }
    }
    /**
     * Sanitize possible dirty html string (remove any potential XSS code like scripts and others) when a `sanitizer` is provided via grid options.
     * The logic will only call the sanitizer if it exists and the value is a defined string, anything else will be skipped (number, boolean, TrustedHTML will all be skipped)
     * @param {*} dirtyHtml: dirty html string
     */
    sanitizeHtmlString(dirtyHtml) {
      return runOptionalHtmlSanitizer(dirtyHtml, this._options?.sanitizer);
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/filterUtilities.js
  function buildSelectOperator(optionValues, grid) {
    const selectElm = createDomElement("select", { className: "form-control", tabIndex: 0 });
    for (const option2 of optionValues) {
      const optionElm = document.createElement("option");
      optionElm.value = option2.operator;
      applyHtmlToElement(optionElm, `${htmlEncodeWithPadding(option2.operatorAlt || option2.operator, 3)}${option2.descAlt || option2.desc}`, grid.getOptions());
      selectElm.appendChild(optionElm);
    }
    return selectElm;
  }
  function renderDomElementFromCollectionAsync(collection, columnDef, renderDomElementCallback) {
    const columnFilter = columnDef?.filter ?? {};
    collection = getCollectionFromObjectWhenEnabled(collection, columnFilter);
    if (!Array.isArray(collection)) {
      throw new Error('Something went wrong while trying to pull the collection from the "collectionAsync" call in the Filter, the collection is not a valid array.');
    }
    columnFilter.collection = collection;
    renderDomElementCallback(collection);
  }
  async function renderCollectionOptionsAsync(collectionAsync, columnDef, renderDomElementCallback, rxjs, subscriptions) {
    const columnFilter = columnDef?.filter ?? {};
    let awaitedCollection = null;
    if (collectionAsync) {
      const isObservable = rxjs?.isObservable(collectionAsync) ?? false;
      awaitedCollection = await fetchAsPromise(collectionAsync, rxjs);
      awaitedCollection = getCollectionFromObjectWhenEnabled(awaitedCollection, columnFilter);
      if (!Array.isArray(awaitedCollection)) {
        throw new Error('Something went wrong while trying to pull the collection from the "collectionAsync" call in the Filter, the collection is not a valid array.');
      }
      columnFilter.collection = awaitedCollection;
      renderDomElementCallback(awaitedCollection);
      if (isObservable) {
        createCollectionAsyncSubject(columnDef, renderDomElementCallback, rxjs, subscriptions);
      }
    }
    return awaitedCollection;
  }
  function createCollectionAsyncSubject(columnDef, renderDomElementCallback, rxjs, subscriptions) {
    const columnFilter = columnDef?.filter ?? {};
    const newCollectionAsync = rxjs?.createSubject();
    columnFilter.collectionAsync = newCollectionAsync;
    if (subscriptions && newCollectionAsync) {
      subscriptions.push(newCollectionAsync.subscribe((collection) => renderDomElementFromCollectionAsync(collection, columnDef, renderDomElementCallback)));
    }
  }
  function getOutputText(translationKey, localeText, defaultText, gridOptions, translaterService) {
    if (gridOptions?.enableTranslate && translaterService?.translate) {
      const translationPrefix = getTranslationPrefix(gridOptions);
      return translaterService.translate(`${translationPrefix}${translationKey}`);
    }
    const locales = gridOptions.locales || Constants.locales;
    return locales?.[localeText] ?? defaultText;
  }
  function compoundOperatorString(gridOptions, translaterService) {
    const operatorList = [
      { operator: "", desc: getOutputText("CONTAINS", "TEXT_CONTAINS", "Contains", gridOptions, translaterService) },
      { operator: "<>", desc: getOutputText("NOT_CONTAINS", "TEXT_NOT_CONTAINS", "Not Contains", gridOptions, translaterService) },
      { operator: "=", desc: getOutputText("EQUALS", "TEXT_EQUALS", "Equals", gridOptions, translaterService) },
      { operator: "!=", desc: getOutputText("NOT_EQUAL_TO", "TEXT_NOT_EQUAL_TO", "Not equal to", gridOptions, translaterService) },
      { operator: "a*", desc: getOutputText("STARTS_WITH", "TEXT_STARTS_WITH", "Starts with", gridOptions, translaterService) },
      { operator: "*z", desc: getOutputText("ENDS_WITH", "TEXT_ENDS_WITH", "Ends with", gridOptions, translaterService) }
    ];
    return operatorList;
  }
  function compoundOperatorNumeric(gridOptions, translaterService) {
    const operatorList = [
      { operator: "", desc: "" },
      { operator: "=", desc: getOutputText("EQUAL_TO", "TEXT_EQUAL_TO", "Equal to", gridOptions, translaterService) },
      { operator: "<", desc: getOutputText("LESS_THAN", "TEXT_LESS_THAN", "Less than", gridOptions, translaterService) },
      {
        operator: "<=",
        desc: getOutputText("LESS_THAN_OR_EQUAL_TO", "TEXT_LESS_THAN_OR_EQUAL_TO", "Less than or equal to", gridOptions, translaterService)
      },
      { operator: ">", desc: getOutputText("GREATER_THAN", "TEXT_GREATER_THAN", "Greater than", gridOptions, translaterService) },
      {
        operator: ">=",
        desc: getOutputText("GREATER_THAN_OR_EQUAL_TO", "TEXT_GREATER_THAN_OR_EQUAL_TO", "Greater than or equal to", gridOptions, translaterService)
      },
      { operator: "<>", desc: getOutputText("NOT_EQUAL_TO", "TEXT_NOT_EQUAL_TO", "Not equal to", gridOptions, translaterService) }
    ];
    return operatorList;
  }
  function applyOperatorAltTextWhenExists(gridOptions, operatorDetailList, filterType) {
    if (gridOptions.compoundOperatorAltTexts) {
      for (const opDetail of operatorDetailList) {
        if (gridOptions.compoundOperatorAltTexts.hasOwnProperty(filterType)) {
          const altTexts = gridOptions.compoundOperatorAltTexts[filterType][opDetail.operator];
          opDetail["operatorAlt"] = altTexts?.operatorAlt || "";
          opDetail["descAlt"] = altTexts?.descAlt || "";
        }
      }
    }
  }

  // node_modules/@slickgrid-universal/common/dist/filters/autocompleterFilter.js
  var AutocompleterFilter = class {
    /**
     * Initialize the Filter
     */
    constructor(translaterService, collectionService, rxjs) {
      this.translaterService = translaterService;
      this.collectionService = collectionService;
      this.rxjs = rxjs;
      this._clearFilterTriggered = false;
      this._collectionObservers = [];
      this._shouldTriggerQuery = true;
      this.searchTerms = [];
      this.isFilled = false;
      this.isItemSelected = false;
      this.valueName = "label";
      this.enableTranslateLabel = false;
      this.subscriptions = [];
      this._bindEventService = new BindingEventService();
    }
    /** Getter for the Autocomplete Option */
    get autocompleterOptions() {
      return this._autocompleterOptions || {};
    }
    /** Getter for the Collection Used by the Filter */
    get collection() {
      return this._collection;
    }
    /** Getter for the Filter Operator */
    get columnFilter() {
      return this.columnDef?.filter || {};
    }
    /** Getter for the Editor DOM Element */
    get filterDomElement() {
      return this._filterElm;
    }
    get filterOptions() {
      return { ...this.gridOptions.defaultFilterOptions?.autocompleter, ...this.columnFilter?.options };
    }
    /** Getter for the Custom Structure if exist */
    get customStructure() {
      let customStructure = this.columnFilter?.customStructure;
      const columnType = this.columnFilter?.type ?? this.columnDef?.type;
      if (!customStructure && columnType === "object" && this.columnDef?.dataKey && this.columnDef?.labelKey) {
        customStructure = {
          label: this.columnDef.labelKey,
          value: this.columnDef.dataKey
        };
      }
      return customStructure;
    }
    /** Getter to know what would be the default operator when none is specified */
    get defaultOperator() {
      return "EQ";
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this.grid?.getOptions() ?? {};
    }
    /** Kraaden AutoComplete instance */
    get instance() {
      return this._instance;
    }
    /** Getter of the Operator to use when doing the filter comparing */
    get operator() {
      return this.columnFilter?.operator ?? this.defaultOperator;
    }
    /** Setter for the filter operator */
    set operator(operator) {
      if (this.columnFilter) {
        this.columnFilter.operator = operator;
      }
    }
    /**
     * Initialize the filter template
     */
    init(args) {
      this.grid = args.grid;
      this.callback = args.callback;
      this.columnDef = args.columnDef;
      this.searchTerms = (args.hasOwnProperty("searchTerms") ? args.searchTerms : []) || [];
      this.filterContainerElm = args.filterContainerElm;
      if (!this.grid || !this.columnDef || !this.columnFilter || !this.columnFilter.collection && !this.columnFilter.collectionAsync && isObjectEmpty(this.filterOptions)) {
        throw new Error(`[Slickgrid-Universal] You need to pass a "collection" (or "collectionAsync") for the AutoComplete Filter to work correctly. Also each option should include a value/label pair (or value/labelKey when using Locale). For example:: { filter: model: Filters.autocompleter, collection: [{ value: true, label: 'True' }, { value: false, label: 'False'}] }`);
      }
      this.enableTranslateLabel = this.columnFilter?.enableTranslateLabel ?? false;
      this.labelName = this.customStructure?.label ?? "label";
      this.valueName = this.customStructure?.value ?? "value";
      this.labelPrefixName = this.customStructure?.labelPrefix ?? "labelPrefix";
      this.labelSuffixName = this.customStructure?.labelSuffix ?? "labelSuffix";
      this._locales = this.gridOptions?.locales ?? Constants.locales;
      const newCollection = this.columnFilter.collection;
      this._collection = newCollection;
      this.renderDomElement(newCollection);
      return new Promise(async (resolve, reject) => {
        try {
          const collectionAsync = this.columnFilter.collectionAsync;
          let collectionOutput;
          if (collectionAsync && !this.columnFilter.collection) {
            collectionOutput = renderCollectionOptionsAsync(collectionAsync, this.columnDef, this.renderDomElement.bind(this), this.rxjs, this.subscriptions);
            resolve(collectionOutput);
          } else {
            collectionOutput = newCollection;
            resolve(newCollection);
          }
          if (collectionAsync || this.columnFilter.enableCollectionWatch) {
            await (collectionOutput ?? collectionAsync);
            this.watchCollectionChanges();
          }
        } catch (e2) {
          reject(e2);
        }
      });
    }
    /**
     * Clear the filter value
     */
    clear(shouldTriggerQuery = true) {
      if (this._filterElm) {
        this._clearFilterTriggered = true;
        this._shouldTriggerQuery = shouldTriggerQuery;
        this.searchTerms = [];
        this._filterElm.value = "";
        this._filterElm.dispatchEvent(new CustomEvent("input"));
        this.updateFilterStyle(false);
      }
    }
    /**
     * destroy the filter
     */
    destroy() {
      if (typeof this._instance?.destroy === "function") {
        this._instance.destroy();
      }
      if (this._filterElm) {
      }
      this._filterElm?.remove();
      if (this._collection) {
        this._collection.length = 0;
      }
      this._bindEventService.unbindAll();
      this._collectionObservers.forEach((obs) => obs?.disconnect());
      unsubscribeAll(this.subscriptions);
    }
    getValues() {
      return this._filterElm?.value || "";
    }
    /** Set value(s) on the DOM element  */
    setValues(values, operator, triggerChange = false) {
      if (values && this._filterElm) {
        this._filterElm.value = values;
      }
      this.updateFilterStyle(this.getValues() !== "");
      this.operator = operator || this.defaultOperator;
      if (triggerChange) {
        this.callback(void 0, {
          columnDef: this.columnDef,
          operator: this.operator,
          searchTerms: [this.getValues()],
          shouldTriggerQuery: true
        });
      }
    }
    //
    // protected functions
    // ------------------
    /**
     * Subscribe to both CollectionObserver & PropertyObserver with BindingEngine.
     * They each have their own purpose, the "propertyObserver" will trigger once the collection is replaced entirely
     * while the "collectionObverser" will trigger on collection changes (`push`, `unshift`, `splice`, ...)
     */
    watchCollectionChanges() {
      if (this.columnFilter?.collection) {
        this._collectionObservers.push(collectionObserver(this.columnFilter.collection, (updatedArray) => {
          this.renderDomElement(this.columnFilter.collection || updatedArray || []);
        }));
        propertyObserver(this.columnFilter, "collection", (newValue) => {
          this.renderDomElement(newValue || []);
          if (this.columnFilter.collection) {
            this._collectionObservers.push(collectionObserver(this.columnFilter.collection, (updatedArray) => {
              this.renderDomElement(this.columnFilter.collection || updatedArray || []);
            }));
          }
        });
      }
    }
    renderDomElement(collection) {
      collection = getCollectionFromObjectWhenEnabled(collection, this.columnFilter);
      let newCollection = collection;
      if (newCollection) {
        newCollection = filterCollectionWithOptions(newCollection, this.collectionService, this.columnFilter?.collectionFilterBy, this.columnFilter.collectionOptions);
        newCollection = sortCollectionWithOptions(newCollection, this.columnDef, this.collectionService, this.columnFilter?.collectionSortBy, this.enableTranslateLabel);
      }
      const searchTerm = Array.isArray(this.searchTerms) && this.searchTerms.length >= 0 ? this.searchTerms[0] : "";
      this._collection = newCollection;
      this._filterElm = this.createFilterElement(newCollection, searchTerm);
      this._bindEventService.bind(this._filterElm, "input", this.handleOnInputChange.bind(this));
      this._bindEventService.bind(this._filterElm, "blur", () => {
        if (!this.isItemSelected) {
          this.clear();
        }
      });
    }
    /**
     * Create the autocomplete filter DOM element
     * @param collection
     * @param searchTerm
     * @returns
     */
    createFilterElement(collection, searchTerm) {
      this._collection = collection;
      const columnId = this.columnDef?.id ?? "";
      emptyElement(this.filterContainerElm);
      let placeholder = this.gridOptions?.defaultFilterPlaceholder ?? "";
      if (this.columnFilter?.placeholder) {
        placeholder = this.columnFilter.placeholder;
      }
      this._filterElm = createDomElement("input", {
        type: "text",
        ariaLabel: this.columnFilter?.ariaLabel ?? `${toSentenceCase(columnId + "")} Search Filter`,
        autocomplete: "off",
        ariaAutoComplete: "none",
        placeholder,
        className: `form-control search-filter slick-filter filter-${columnId} slick-autocomplete-container`,
        value: searchTerm ?? "",
        dataset: { columnid: `${columnId}` },
        tabIndex: 0
      });
      const searchTermInput = searchTerm;
      if (Array.isArray(collection)) {
        if (collection.every((x2) => isPrimitiveValue(x2))) {
          collection = collection.map((c) => ({ label: c, value: c }));
        } else {
          collection = collection.map((item) => ({
            label: item?.[this.labelName],
            value: item?.[this.valueName],
            labelPrefix: item?.[this.labelPrefixName] ?? "",
            labelSuffix: item?.[this.labelSuffixName] ?? ""
          }));
        }
      }
      this._autocompleterOptions = {
        input: this._filterElm,
        debounceWaitMs: 200,
        className: `slick-autocomplete ${this.filterOptions?.className ?? ""}`.trim(),
        // prettier-ignore
        emptyMsg: this.gridOptions.enableTranslate && this.translaterService?.translate ? this.translaterService.translate("NO_ELEMENTS_FOUND") : this._locales?.TEXT_NO_ELEMENTS_FOUND ?? "No elements found",
        customize: (_input, _inputRect, container) => {
          container.style.width = "";
        },
        onSelect: (item) => {
          this.isItemSelected = true;
          this.handleSelect(item);
        },
        ...this.filterOptions
      };
      if (this.gridOptions?.darkMode) {
        this._autocompleterOptions.className += " slick-dark-mode";
      }
      this.autocompleterOptions.className = classNameToList(this.autocompleterOptions.className).join(" ");
      if (this._autocompleterOptions.renderItem?.layout) {
        this._autocompleterOptions.className += ` autocomplete-custom-${toKebabCase(this._autocompleterOptions.renderItem.layout)}`;
        this._autocompleterOptions.render = this.renderCustomItem.bind(this);
      } else if (Array.isArray(collection)) {
        this._autocompleterOptions.render = this._autocompleterOptions.render?.bind(this) ?? this.renderCollectionItem.bind(this);
      } else if (!this._autocompleterOptions.render) {
        this._autocompleterOptions.render = this.renderRegularItem.bind(this);
      }
      if (this.filterOptions?.fetch) {
        addAutocompleteLoadingByOverridingFetch(this._filterElm, this._autocompleterOptions);
        this._instance = autocomplete_es_default(this._autocompleterOptions);
      } else {
        this._instance = autocomplete_es_default({
          ...this._autocompleterOptions,
          fetch: (searchText, updateCallback) => {
            if (collection) {
              updateCallback(collection.filter((c) => {
                const label = (typeof c === "string" ? c : c?.label) || "";
                return label.toLowerCase().includes(searchText.toLowerCase());
              }));
            }
          }
        });
      }
      this._filterElm.value = searchTermInput ?? "";
      const filterDivContainerElm = createDomElement("div", { className: "autocomplete-filter-container" });
      filterDivContainerElm.appendChild(this._filterElm);
      filterDivContainerElm.appendChild(createDomElement("span"));
      if (searchTerm) {
        this._filterElm.classList.add("filled");
      }
      this.filterContainerElm.appendChild(filterDivContainerElm);
      this.filterContainerElm.appendChild(document.createElement("span"));
      this.columnFilter.onInstantiated?.(this._instance);
      return this._filterElm;
    }
    //
    // protected functions
    // ------------------
    // this function should be PRIVATE but for unit tests purposes we'll make it public until a better solution is found
    // a better solution would be to get the autocomplete DOM element to work with selection but I couldn't find how to do that in Vitest
    handleSelect(item) {
      if (item !== void 0) {
        const hasCustomRenderItemCallback = this.filterOptions?.renderItem ?? false;
        const itemLabel = typeof item === "string" ? item : hasCustomRenderItemCallback ? item[this.labelName] : item.label;
        let itemValue = typeof item === "string" ? item : hasCustomRenderItemCallback ? item[this.valueName] : item.value;
        itemValue = this.trimWhitespaceWhenEnabled(itemValue);
        this.updateFilterStyle(itemValue !== "");
        this.setValues(itemLabel);
        this.callback(void 0, {
          columnDef: this.columnDef,
          operator: this.operator,
          searchTerms: [itemValue],
          shouldTriggerQuery: this._shouldTriggerQuery
        });
        this._clearFilterTriggered = false;
        this._shouldTriggerQuery = true;
      }
      return false;
    }
    handleOnInputChange(e2) {
      let value = e2?.target?.value ?? "";
      const shouldTriggerOnEveryKeyStroke = this.filterOptions.triggerOnEveryKeyStroke ?? false;
      value = this.trimWhitespaceWhenEnabled(value);
      if (this._clearFilterTriggered || value === "" || shouldTriggerOnEveryKeyStroke) {
        const callbackArgs = { columnDef: this.columnDef, shouldTriggerQuery: this._shouldTriggerQuery };
        if (this._clearFilterTriggered) {
          callbackArgs.clearFilterTriggered = this._clearFilterTriggered;
        } else {
          callbackArgs.operator = this.operator;
          callbackArgs.searchTerms = [value];
        }
        this.updateFilterStyle(value !== "");
        this.callback(e2, callbackArgs);
      }
      this._clearFilterTriggered = false;
      this._shouldTriggerQuery = true;
    }
    renderRegularItem(item) {
      const itemLabel = typeof item === "string" ? item : item?.label ?? "";
      return createDomElement("div", {
        textContent: itemLabel || ""
      });
    }
    renderCustomItem(item) {
      const templateString = this._autocompleterOptions?.renderItem?.templateCallback(item) ?? "";
      const tmpElm = document.createElement("div");
      applyHtmlToElement(tmpElm, templateString, this.gridOptions);
      return tmpElm;
    }
    renderCollectionItem(item) {
      const isRenderHtmlEnabled = this.columnFilter?.enableRenderHtml ?? false;
      const prefixText = item.labelPrefix || "";
      const labelText = item.label || "";
      const suffixText = item.labelSuffix || "";
      const finalText = prefixText + labelText + suffixText;
      const sanitizedText = this.grid.sanitizeHtmlString(finalText) || "";
      const div = document.createElement("div");
      div[isRenderHtmlEnabled ? "innerHTML" : "textContent"] = sanitizedText;
      return div;
    }
    /**
     * Trim whitespaces when option is enabled globally or on the filter itself
     * @param value - value found which could be a string or an object
     * @returns - trimmed value when it is a string and the feature is enabled
     */
    trimWhitespaceWhenEnabled(value) {
      let outputValue = value;
      const enableWhiteSpaceTrim = this.gridOptions.enableFilterTrimWhiteSpace || this.columnFilter.enableTrimWhiteSpace;
      if (typeof value === "string" && enableWhiteSpaceTrim) {
        outputValue = value.trim();
      }
      return outputValue;
    }
    /** add/remove "filled" CSS class */
    updateFilterStyle(isFilled) {
      this.isItemSelected = isFilled;
      this._filterElm.classList.toggle("filled", isFilled);
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/dateFilter.js
  var DateFilter = class {
    constructor(translaterService) {
      this.translaterService = translaterService;
      this._clearFilterTriggered = false;
      this._lastClickIsDate = false;
      this._shouldTriggerQuery = true;
      this.hasTimePicker = false;
      this.inputFilterType = "range";
      this.searchTerms = [];
      this._bindEventService = new BindingEventService();
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this.grid?.getOptions() ?? {};
    }
    /** Getter for the Column Filter */
    get columnFilter() {
      return this.columnDef?.filter || {};
    }
    /** Getter for the Current Date(s) selected */
    get currentDateOrDates() {
      return this._currentDateOrDates;
    }
    /** Getter to know what would be the default operator when none is specified */
    get defaultOperator() {
      return this.inputFilterType === "compound" ? "" : this.gridOptions.defaultFilterRangeOperator || "RangeInclusive";
    }
    /** Getter for the date picker options */
    get pickerOptions() {
      return this._pickerOptions || {};
    }
    get filterOptions() {
      return { ...this.gridOptions.defaultFilterOptions?.date, ...this.columnFilter?.options };
    }
    /** Getter for the Filter Operator */
    get operator() {
      if (this.inputFilterType === "compound") {
        return this._operator || this.columnFilter.operator || this.defaultOperator;
      }
      return this.columnFilter?.operator ?? this.defaultOperator;
    }
    /** Setter for the filter operator */
    set operator(operator) {
      if (this.inputFilterType === "compound") {
        this._operator = operator;
      } else if (this.columnFilter) {
        this.columnFilter.operator = operator;
      }
    }
    /** Initialize the Filter */
    init(args) {
      this.grid = args.grid;
      this.callback = args.callback;
      this.columnDef = args.columnDef;
      if (this.inputFilterType === "compound") {
        this.operator = args.operator || "";
      }
      this.searchTerms = args?.searchTerms ?? [];
      this.filterContainerElm = args.filterContainerElm;
      const searchValues = this.inputFilterType === "compound" ? Array.isArray(this.searchTerms) && this.searchTerms.length >= 0 ? this.searchTerms[0] : "" : this.searchTerms;
      this._filterElm = this.createDomFilterElement(searchValues);
      this.updateFilterStyle(this.searchTerms.length > 0);
      if (this._selectOperatorElm) {
        this._bindEventService.bind(this._selectOperatorElm, "change", this.onTriggerEvent.bind(this));
      }
      this._bindEventService.bind(document.body, "keydown", ((e2) => {
        if (e2.key === "Escape" || e2.key === "Tab") {
          this.hide();
        }
      }));
      this._bindEventService.bind(this._dateInputElm, "keydown", ((e2) => {
        if (e2.key === "Backspace") {
          e2.preventDefault();
          this.clear(true, false);
        } else if (e2.key === "Enter" && e2.target === this._dateInputElm) {
          e2.stopImmediatePropagation();
          this.show();
        }
      }));
    }
    /** Clear the filter value */
    clear(shouldTriggerQuery = true, shouldTriggerClearEvent = true) {
      if (this.calendarInstance) {
        this._clearFilterTriggered = shouldTriggerClearEvent;
        this._shouldTriggerQuery = shouldTriggerQuery;
        this._currentValue = "";
        this.searchTerms = [];
        this._currentDateStrings = [];
        if (this._selectOperatorElm) {
          this._selectOperatorElm.selectedIndex = 0;
        }
        if (this.calendarInstance) {
          resetDatePicker(this.calendarInstance);
        }
      }
      this.onTriggerEvent(new Event("keyup"));
      this.updateFilterStyle(false);
    }
    /** Destroy the filter */
    destroy() {
      this._bindEventService.unbindAll();
      this.calendarInstance?.destroy();
      emptyElement(this.filterContainerElm);
      this.filterContainerElm?.remove();
      this._selectOperatorElm?.remove();
      this._filterElm?.remove();
    }
    hide() {
      this.calendarInstance?.hide();
    }
    show() {
      this.calendarInstance?.show();
    }
    getValues() {
      return this._currentDateOrDates;
    }
    /**
     * Set value(s) on the DOM element
     * @params searchTerms
     */
    setValues(values, operator, triggerChange = false) {
      let pickerValues;
      if (this.inputFilterType === "compound") {
        pickerValues = Array.isArray(values) ? values[0] : values;
      } else {
        if (typeof values === "string" || Array.isArray(values) && typeof values[0] === "string" && values[0].indexOf("..") > 0) {
          pickerValues = typeof values === "string" ? [values] : values[0].split("..");
        } else if (Array.isArray(values)) {
          pickerValues = values;
        }
      }
      if (this.calendarInstance && pickerValues !== void 0) {
        setPickerDates(this.columnFilter, this._dateInputElm, this.calendarInstance, {
          columnDef: this.columnDef,
          newVal: pickerValues
        });
        this._currentDateOrDates = values && pickerValues ? pickerValues : void 0;
      }
      const currentValueOrValues = this.getValues() || [];
      const searchTerms = Array.isArray(currentValueOrValues) ? currentValueOrValues : [currentValueOrValues];
      this.updateFilterStyle(searchTerms.length > 0);
      this.operator = operator || this.defaultOperator;
      if (operator && this._selectOperatorElm) {
        const operatorShorthand = mapOperatorToShorthandDesignation(this.operator);
        this._selectOperatorElm.value = operatorShorthand;
      }
      if (triggerChange) {
        this.callback(void 0, { columnDef: this.columnDef, searchTerms, operator: this.operator, shouldTriggerQuery: true });
      }
    }
    //
    // protected functions
    // ------------------
    buildDatePickerInput(searchTerms) {
      const columnId = this.columnDef?.id ?? "";
      const columnFieldType = this.columnFilter.type || this.columnDef.type || "dateIso";
      const outputFieldType = this.columnDef.outputType || this.columnFilter.type || this.columnDef.type || "dateUtc";
      const outputFormat = mapTempoDateFormatWithFieldType(outputFieldType);
      const inputFieldType = this.columnFilter.type || this.columnDef.type || "dateIso";
      if (outputFormat && this.inputFilterType !== "range" && (outputFormat === "ISO8601" || outputFormat.toLowerCase().includes("h"))) {
        this.hasTimePicker = true;
      }
      const pickerFormat = mapTempoDateFormatWithFieldType(this.hasTimePicker ? "dateTimeIsoAM_PM" : "dateIso");
      const currentLocale = (this.filterOptions?.locale ?? this.translaterService?.getCurrentLanguage?.()) || this.gridOptions.locale || "en";
      let pickerValues;
      if (this.inputFilterType === "compound") {
        if (searchTerms) {
          pickerValues = searchTerms;
          this._currentDateOrDates = searchTerms;
        }
      } else {
        if (typeof searchTerms === "string" || Array.isArray(searchTerms) && typeof searchTerms[0] === "string" && searchTerms[0].indexOf("..") > 0) {
          pickerValues = typeof searchTerms === "string" ? [searchTerms] : searchTerms[0].split("..");
        } else if (Array.isArray(searchTerms)) {
          pickerValues = searchTerms;
        }
        if (Array.isArray(pickerValues)) {
          this._currentDateOrDates = pickerValues;
          this._currentDateStrings = pickerValues.map((date2) => formatDateByFieldType(date2, void 0, inputFieldType));
        }
      }
      const pickerOptions = {
        inputMode: true,
        enableJumpToSelectedDate: true,
        firstWeekday: 0,
        enableDateToggle: true,
        locale: currentLocale,
        selectedTheme: this.gridOptions?.darkMode ? "dark" : "light",
        positionToInput: "auto",
        openOnFocus: false,
        sanitizerHTML: (dirtyHtml) => this.grid.sanitizeHtmlString(dirtyHtml),
        selectedWeekends: [],
        type: this.inputFilterType === "range" ? "multiple" : "default",
        onClickDate: () => {
          this._lastClickIsDate = true;
        },
        onChangeToInput: (self) => {
          if (self.context.inputElement) {
            let outDates = [];
            let firstDate = "";
            let lastDate = "";
            if (self.context.selectedDates[1]) {
              self.context.selectedDates.sort((a, b) => +new Date(a) - +new Date(b));
              firstDate = self.context.selectedDates[0];
              lastDate = self.context.selectedDates[self.context.selectedDates.length - 1];
              const firstDisplayDate = format(self.context.selectedDates[0], outputFormat, "en-US");
              const lastDisplayDate = format(lastDate, outputFormat, "en-US");
              self.context.inputElement.value = `${firstDisplayDate} \u2014 ${lastDisplayDate}`;
              outDates = [firstDate, lastDate];
            } else if (self.context.selectedDates[0]) {
              firstDate = self.context.selectedDates[0];
              self.context.inputElement.value = formatDateByFieldType(firstDate, "dateIso", outputFieldType);
              outDates = self.context.selectedDates;
            } else {
              self.context.inputElement.value = "";
            }
            if (this.hasTimePicker && firstDate) {
              const tempoDate = parse(firstDate, pickerFormat);
              tempoDate.setHours(+(self.context.selectedHours || 0));
              tempoDate.setMinutes(+(self.context.selectedMinutes || 0));
              self.context.inputElement.value = formatDateByFieldType(tempoDate, void 0, outputFieldType);
              outDates = [tempoDate];
            }
            if (this.inputFilterType === "compound") {
              this._currentValue = formatDateByFieldType(outDates[0], void 0, columnFieldType);
            } else {
              if (Array.isArray(outDates)) {
                this._currentDateStrings = outDates.map((date2) => formatDateByFieldType(date2, void 0, columnFieldType));
                this._currentValue = this._currentDateStrings.join("..");
              }
            }
            this._currentDateOrDates = outDates.map((d2) => d2 instanceof Date ? d2 : parse(d2, pickerFormat));
            if (this._currentValue) {
              const newEvent = this.hasTimePicker ? new Event("keyup") : void 0;
              this.onTriggerEvent(newEvent);
            }
            if (this.inputFilterType === "range" && self.context.selectedDates.length < 2) {
              this._lastClickIsDate = false;
            }
            if (this._lastClickIsDate) {
              self.hide();
              this._lastClickIsDate = false;
            }
          }
        },
        onShow: (self) => {
          setPickerFocus(self.context.mainElement);
        }
      };
      if (this.inputFilterType === "range") {
        pickerOptions.type = "multiple";
        pickerOptions.displayMonthsCount = 2;
        pickerOptions.monthsToSwitch = 2;
        pickerOptions.enableEdgeDatesOnly = true;
        pickerOptions.selectionDatesMode = "multiple-ranged";
        pickerOptions.displayDatesOutside = false;
      }
      if (this.hasTimePicker) {
        pickerOptions.selectionTimeMode = 24;
      }
      this._pickerOptions = extend(true, {}, pickerOptions, this.filterOptions);
      let placeholder = this.gridOptions?.defaultFilterPlaceholder ?? "";
      if (this.columnFilter?.placeholder) {
        placeholder = this.columnFilter.placeholder;
      }
      this._dateInputElm = createDomElement("input", {
        type: "text",
        className: "form-control date-picker",
        placeholder,
        readOnly: true,
        dataset: { input: "", columnid: `${columnId}` },
        tabIndex: 0
      });
      this.calendarInstance = new Calendar(this._dateInputElm, this._pickerOptions);
      this.calendarInstance.init();
      if (this._pickerOptions?.selectedDates) {
        pickerValues = this._pickerOptions.selectedDates;
      }
      if (pickerValues) {
        setPickerDates(this.columnFilter, this._dateInputElm, this.calendarInstance, {
          columnDef: this.columnDef,
          oldVal: void 0,
          newVal: pickerValues,
          updatePickerUI: false
        });
      }
      this.columnFilter.onInstantiated?.(this.calendarInstance);
    }
    /** Get the available operator option values to populate the operator select dropdown list */
    getOperatorOptionValues() {
      let operatorList;
      if (this.columnFilter?.compoundOperatorList) {
        operatorList = this.columnFilter.compoundOperatorList;
      } else {
        operatorList = compoundOperatorNumeric(this.gridOptions, this.translaterService);
      }
      applyOperatorAltTextWhenExists(this.gridOptions, operatorList, "numeric");
      return operatorList;
    }
    /**
     * Create the DOM element
     * @params searchTerms
     */
    createDomFilterElement(searchTerms) {
      const columnId = this.columnDef?.id ?? "";
      emptyElement(this.filterContainerElm);
      this.buildDatePickerInput(searchTerms);
      if (this.inputFilterType === "range") {
        const inputContainerElm = createDomElement("div", {
          className: `date-picker form-group search-filter slick-filter filter-${columnId}`
        });
        if (Array.isArray(searchTerms) && searchTerms.length > 0 && searchTerms[0] !== "") {
          this._currentDateOrDates = searchTerms;
          this._currentValue = searchTerms[0];
        }
        inputContainerElm.appendChild(this._dateInputElm);
        if (inputContainerElm) {
          this.filterContainerElm.appendChild(inputContainerElm);
        }
        return inputContainerElm;
      } else {
        this._selectOperatorElm = buildSelectOperator(this.getOperatorOptionValues(), this.grid);
        const filterContainerElm = createDomElement("div", {
          className: `date-picker form-group search-filter slick-filter filter-${columnId}`
        });
        const containerInputGroupElm = createDomElement("div", { className: "input-group date-picker" }, filterContainerElm);
        const operatorInputGroupAddonElm = createDomElement("div", { className: "input-group-addon input-group-prepend operator" }, containerInputGroupElm);
        operatorInputGroupAddonElm.appendChild(this._selectOperatorElm);
        containerInputGroupElm.appendChild(this._dateInputElm);
        if (this.operator) {
          const operatorShorthand = mapOperatorToShorthandDesignation(this.operator);
          this._selectOperatorElm.value = operatorShorthand;
        }
        this._currentDateOrDates = searchTerms;
        this._currentValue = searchTerms;
        if (filterContainerElm) {
          this.filterContainerElm.appendChild(filterContainerElm);
        }
        return filterContainerElm;
      }
    }
    onTriggerEvent(e2) {
      if (this._clearFilterTriggered) {
        this.callback(e2, {
          columnDef: this.columnDef,
          clearFilterTriggered: this._clearFilterTriggered,
          shouldTriggerQuery: this._shouldTriggerQuery
        });
        this.updateFilterStyle(false);
      } else {
        if (this.inputFilterType === "range") {
          const searchTerms = this._currentDateStrings ? this._currentDateStrings : [this._currentValue];
          this.updateFilterStyle(searchTerms.length > 0);
          this.callback(e2, {
            columnDef: this.columnDef,
            searchTerms,
            operator: this.operator || "",
            shouldTriggerQuery: this._shouldTriggerQuery
          });
        } else if (this.inputFilterType === "compound" && this._selectOperatorElm) {
          const selectedOperator = this._selectOperatorElm.value;
          this.updateFilterStyle(!!this._currentValue);
          const skipNullInput = this.columnFilter.skipCompoundOperatorFilterWithNullInput ?? this.gridOptions.skipCompoundOperatorFilterWithNullInput ?? this.gridOptions.skipCompoundOperatorFilterWithNullInput === void 0;
          const hasSkipNullValChanged = skipNullInput && isDefined(this._currentDateOrDates) || this._currentDateOrDates === "" && isDefined(this._lastSearchValue);
          if (!skipNullInput || hasSkipNullValChanged) {
            this.callback(e2, {
              columnDef: this.columnDef,
              searchTerms: this._currentValue ? [this._currentValue] : null,
              operator: selectedOperator || "",
              shouldTriggerQuery: this._shouldTriggerQuery
            });
          }
        }
      }
      this._clearFilterTriggered = false;
      this._shouldTriggerQuery = true;
      this._lastSearchValue = this._currentValue;
    }
    /** add/remove "filled" CSS class */
    updateFilterStyle(isFilled) {
      this._filterElm.classList.toggle("filled", isFilled);
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/compoundDateFilter.js
  var CompoundDateFilter = class extends DateFilter {
    /** Initialize the Filter */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this.inputFilterType = "compound";
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/collection.service.js
  var CollectionService = class {
    constructor(translaterService) {
      this.translaterService = translaterService;
    }
    /**
     * Filter 1 or more items from a collection
     * @param collection
     * @param filterByOptions
     */
    filterCollection(collection, filterByOptions, filterResultBy = "chain") {
      let filteredCollection = [];
      if (Array.isArray(filterByOptions)) {
        filteredCollection = filterResultBy === "merge" ? [] : [...collection];
        filterByOptions.forEach((filter) => {
          if (filterResultBy === "merge") {
            const filteredPass = this.singleFilterCollection(collection, filter);
            filteredCollection = uniqueArray([...filteredCollection, ...filteredPass]);
          } else {
            filteredCollection = this.singleFilterCollection(filteredCollection, filter);
          }
        });
      } else {
        filteredCollection = this.singleFilterCollection(collection, filterByOptions);
      }
      return filteredCollection;
    }
    /** Pre-parse date items as `Date` object to improve Date Sort considerably */
    preParseByMutationDateItems(items, grid, preParseDateColumns) {
      const parsingProps = [];
      grid.getColumns().forEach((col) => {
        const parseInfo = this.getParseDateInfo(col, preParseDateColumns);
        if (parseInfo) {
          parsingProps.push(parseInfo);
        }
      });
      items.forEach((item) => {
        parsingProps.forEach(({ columnId, dateFormat, queryFieldName }) => {
          this.reassignDateWhenValid(item, columnId, dateFormat, queryFieldName);
        });
      });
    }
    parseSingleDateItem(item, grid, preParseDateColumns) {
      if (preParseDateColumns) {
        grid.getColumns().forEach((col) => {
          const parseInfo = this.getParseDateInfo(col, preParseDateColumns);
          if (parseInfo) {
            this.reassignDateWhenValid(item, col.id, parseInfo.dateFormat, parseInfo.queryFieldName);
          }
        });
      }
    }
    /**
     * Filter an item from a collection
     * @param collection
     * @param filterBy
     */
    singleFilterCollection(collection, filterBy) {
      let filteredCollection = [];
      if (filterBy) {
        const objectProperty = filterBy.property;
        const operator = filterBy.operator || "EQ";
        const value = typeof filterBy.value === "undefined" ? "" : filterBy.value;
        switch (operator) {
          case "=":
          case "EQ":
            if (objectProperty) {
              filteredCollection = collection.filter((item) => item[objectProperty] === value);
            } else {
              filteredCollection = collection.filter((item) => item === value);
            }
            break;
          case "Contains":
          case "CONTAINS":
            if (objectProperty) {
              filteredCollection = collection.filter((item) => item[objectProperty]?.toString().indexOf(value.toString()) !== -1);
            } else {
              filteredCollection = collection.filter((item) => item !== null && item !== void 0 && item.toString().indexOf(value.toString()) !== -1);
            }
            break;
          case "Not_Contains":
          case "NOT_CONTAINS":
            if (objectProperty) {
              filteredCollection = collection.filter((item) => item[objectProperty]?.toString().indexOf(value.toString()) === -1);
            } else {
              filteredCollection = collection.filter((item) => item !== null && item !== void 0 && item.toString().indexOf(value.toString()) === -1);
            }
            break;
          default:
            if (objectProperty) {
              filteredCollection = collection.filter((item) => item[objectProperty] !== value);
            } else {
              filteredCollection = collection.filter((item) => item !== value);
            }
        }
      }
      return filteredCollection;
    }
    /**
     * Sort 1 or more items in a collection
     * @param column definition
     * @param collection
     * @param sortByOptions
     * @param enableTranslateLabel
     */
    sortCollection(columnDef, collection, sortByOptions, enableTranslateLabel) {
      if (enableTranslateLabel && (!this.translaterService || !this.translaterService.translate)) {
        throw new Error('[Slickgrid-Universal] requires a Translate Service to be installed and configured when the grid option "enableTranslate" is enabled.');
      }
      let sortedCollection = [];
      if (sortByOptions) {
        if (Array.isArray(sortByOptions)) {
          sortedCollection = collection.sort((dataRow1, dataRow2) => {
            for (let i = 0, l = sortByOptions.length; i < l; i++) {
              const sortBy = sortByOptions[i];
              if (sortBy?.property) {
                const sortDirection = sortBy.sortDesc ? SortDirectionNumber.desc : SortDirectionNumber.asc;
                const objectProperty = sortBy.property;
                const fieldType = sortBy?.fieldType ?? columnDef?.type ?? "string";
                const value1 = enableTranslateLabel ? this.translaterService?.translate?.(dataRow1[objectProperty] || " ") : dataRow1[objectProperty];
                const value2 = enableTranslateLabel ? this.translaterService?.translate?.(dataRow2[objectProperty] || " ") : dataRow2[objectProperty];
                const sortResult = sortByFieldType(fieldType, value1, value2, sortDirection, columnDef);
                if (sortResult !== SortDirectionNumber.neutral) {
                  return sortResult;
                }
              }
            }
            return SortDirectionNumber.neutral;
          });
        } else if (sortByOptions?.property) {
          const objectProperty = sortByOptions.property;
          const sortDirection = sortByOptions.sortDesc ? SortDirectionNumber.desc : SortDirectionNumber.asc;
          const fieldType = sortByOptions?.fieldType ?? columnDef?.type ?? "string";
          sortedCollection = collection.sort((dataRow1, dataRow2) => {
            const value1 = enableTranslateLabel ? this.translaterService?.translate && this.translaterService.translate(dataRow1[objectProperty] || " ") : dataRow1[objectProperty];
            const value2 = enableTranslateLabel ? this.translaterService?.translate && this.translaterService.translate(dataRow2[objectProperty] || " ") : dataRow2[objectProperty];
            const sortResult = sortByFieldType(fieldType, value1, value2, sortDirection, columnDef);
            if (sortResult !== SortDirectionNumber.neutral) {
              return sortResult;
            }
            return SortDirectionNumber.neutral;
          });
        } else if (sortByOptions && !sortByOptions.property) {
          const sortDirection = sortByOptions.sortDesc ? SortDirectionNumber.desc : SortDirectionNumber.asc;
          const fieldType = sortByOptions?.fieldType ?? columnDef?.type ?? "string";
          sortedCollection = collection.sort((dataRow1, dataRow2) => {
            const value1 = enableTranslateLabel ? this.translaterService?.translate && this.translaterService.translate(dataRow1 || " ") : dataRow1;
            const value2 = enableTranslateLabel ? this.translaterService?.translate && this.translaterService.translate(dataRow2 || " ") : dataRow2;
            const sortResult = sortByFieldType(fieldType, value1, value2, sortDirection, columnDef);
            if (sortResult !== SortDirectionNumber.neutral) {
              return sortResult;
            }
            return SortDirectionNumber.neutral;
          });
        }
      }
      return sortedCollection;
    }
    // --
    // protected functions
    // -------------------
    getParseDateInfo(col, preParseDateColumns) {
      const fieldType = col.type || "string";
      const dateFormat = mapTempoDateFormatWithFieldType(fieldType);
      if (isColumnDateType(fieldType) && preParseDateColumns) {
        const queryFieldName = typeof preParseDateColumns === "string" ? `${preParseDateColumns}${col.id}` : `${col.id}`;
        return { columnId: col.id, dateFormat, queryFieldName };
      }
    }
    reassignDateWhenValid(item, columnId, dateFormat, queryFieldName) {
      const date2 = tryParseDate(item[columnId], dateFormat, false);
      if (date2) {
        item[queryFieldName] = date2;
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/dataWrapperService.js
  var DataWrapperService = class {
    init(grid) {
      this._grid = grid;
      if (grid.hasDataView()) {
        this._dataView = grid.getData();
      }
    }
    getDataItem(row) {
      return this._dataView?.getItem(row) ?? this._grid.getDataItem(row);
    }
    getDataItems() {
      return this._dataView?.getItems() ?? this._grid.getData();
    }
    getDataLength() {
      return this._dataView?.getItemCount() ?? this._grid.getDataLength();
    }
    setDataItems(items) {
      this._dataView?.setItems(items) ?? this._grid.setData(items);
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/domUtilities.js
  function buildMsSelectCollectionList(type, collection, columnDef, grid, isMultiSelect = false, translaterService, searchTerms) {
    const columnId = columnDef?.id ?? "";
    const gridOptions = grid.getOptions();
    const columnFilterOrEditor = (type === "editor" ? columnDef?.editor : columnDef?.filter) ?? {};
    const collectionOptions = columnFilterOrEditor?.collectionOptions ?? {};
    const separatorBetweenLabels = collectionOptions?.separatorBetweenTextLabels ?? "";
    const enableTranslateLabel = columnFilterOrEditor?.enableTranslateLabel ?? false;
    const isTranslateEnabled = gridOptions?.enableTranslate ?? false;
    const isRenderHtmlEnabled = columnFilterOrEditor?.enableRenderHtml ?? false;
    const labelName = columnFilterOrEditor?.customStructure?.label ?? "label";
    const labelPrefixName = columnFilterOrEditor?.customStructure?.labelPrefix ?? "labelPrefix";
    const labelSuffixName = columnFilterOrEditor?.customStructure?.labelSuffix ?? "labelSuffix";
    const optionLabel = columnFilterOrEditor?.customStructure?.optionLabel ?? "value";
    const valueName = columnFilterOrEditor?.customStructure?.value ?? "value";
    const selectElement = createDomElement("select", { className: "ms-filter" });
    const extraCssClasses = type === "filter" ? ["search-filter", `filter-${columnId}`] : ["select-editor", `editor-${columnId}`];
    selectElement.classList.add(...extraCssClasses);
    selectElement.multiple = isMultiSelect;
    const dataCollection = [];
    let hasFoundSearchTerm = false;
    if (Array.isArray(collection)) {
      if (collection.every((x2) => typeof x2 === "number" || typeof x2 === "string")) {
        collection.forEach((option2) => {
          const selectOption = { text: String(option2), value: option2 };
          if (type === "filter" && Array.isArray(searchTerms)) {
            selectOption.selected = searchTerms.findIndex((term) => term === option2) >= 0;
          }
          dataCollection.push(selectOption);
          if (selectOption.selected && isMultiSelect || selectOption.selected && !isMultiSelect && option2 !== "") {
            hasFoundSearchTerm = true;
          }
        });
      } else {
        collection.forEach((option2) => {
          if (option2 === void 0 || typeof option2 === "object" && option2[labelName] === void 0 && option2.labelKey === void 0) {
            throw new Error(`[Slickgrid-Universal] Select Filter/Editor collection with value/label (or value/labelKey when using Locale) is required to populate the Select list, for example:: { filter: model: Filters.multipleSelect, collection: [ { value: '1', label: 'One' } ]')`);
          }
          const labelKey = option2.labelKey || option2[labelName];
          const labelText = (option2.labelKey || enableTranslateLabel && translaterService) && labelKey && isTranslateEnabled ? translaterService?.translate(labelKey || " ") : labelKey;
          let prefixText = option2[labelPrefixName] || "";
          let suffixText = option2[labelSuffixName] || "";
          let selectOptionLabel = option2.hasOwnProperty(optionLabel) ? option2[optionLabel] : "";
          if (selectOptionLabel?.toString) {
            selectOptionLabel = selectOptionLabel.toString().replace(/"/g, "'");
          }
          prefixText = translateWhenEnabled(prefixText, enableTranslateLabel, translaterService);
          suffixText = translateWhenEnabled(suffixText, enableTranslateLabel, translaterService);
          selectOptionLabel = translateWhenEnabled(selectOptionLabel, enableTranslateLabel, translaterService);
          const tmpOptionArray = [
            prefixText,
            typeof labelText === "string" || typeof labelText === "number" ? labelText.toString() : labelText,
            suffixText
          ].filter((text) => text);
          let optionText = tmpOptionArray.join(separatorBetweenLabels);
          const selectOption = { text: "", value: "" };
          if (isRenderHtmlEnabled) {
            optionText = runOptionalHtmlSanitizer(optionText, gridOptions.sanitizer);
          }
          selectOption.text = optionText;
          let selectOptionValue = option2[valueName];
          if (selectOptionValue === void 0 || selectOptionValue === null) {
            selectOptionValue = "";
          }
          if (type === "filter" && Array.isArray(searchTerms)) {
            selectOption.selected = searchTerms.findIndex((term) => `${term}` === `${option2[valueName]}`) >= 0;
          }
          selectOption.value = `${selectOptionValue ?? ""}`;
          dataCollection.push(selectOption);
          if (selectOption.selected && isMultiSelect || selectOption.selected && !isMultiSelect && option2[valueName] !== "") {
            hasFoundSearchTerm = true;
          }
        });
      }
    }
    return { selectElement, dataCollection, hasFoundSearchTerm };
  }
  function translateWhenEnabled(input, enableTranslateLabel, translaterService) {
    return enableTranslateLabel && translaterService && input && typeof input === "string" ? translaterService.translate(input || " ") : input;
  }

  // node_modules/@slickgrid-universal/common/dist/extensions/extensionUtility.js
  var ExtensionUtility = class {
    constructor(sharedService, backendUtilities, translaterService) {
      this.sharedService = sharedService;
      this.backendUtilities = backendUtilities;
      this.translaterService = translaterService;
    }
    /**
     * From a Grid Menu object property name, we will return the correct title output string following this order
     * 1- if user provided a title, use it as the output title
     * 2- else if user provided a title key, use it to translate the output title
     * 3- else if nothing is provided use text defined as constants
     */
    getPickerTitleOutputString(propName, pickerName) {
      if (this.sharedService.gridOptions?.enableTranslate && !this.translaterService?.translate) {
        throw new Error('[Slickgrid-Universal] requires a Translate Service to be installed and configured when the grid option "enableTranslate" is enabled.');
      }
      let output = "";
      const picker = this.sharedService.gridOptions?.[pickerName] ?? {};
      const title = picker?.[propName];
      const titleKey = picker?.[`${propName}Key`];
      const gridOptions = this.sharedService.gridOptions;
      const translationPrefix = getTranslationPrefix(gridOptions);
      if (!title && titleKey && this.translaterService?.translate) {
        output = this.translaterService.translate(titleKey || " ");
      } else {
        let transKey = "";
        switch (propName) {
          case "commandTitle":
            transKey = "COMMANDS";
            break;
          case "columnTitle":
            transKey = "COLUMNS";
            break;
          case "forceFitTitle":
            transKey = "FORCE_FIT_COLUMNS";
            break;
          case "syncResizeTitle":
            transKey = "SYNCHRONOUS_RESIZE";
            break;
        }
        output = transKey ? this.translateWhenEnabledAndServiceExist(`${translationPrefix}${transKey}`, `TEXT_${transKey}`, title) : title;
      }
      return output;
    }
    /** Refresh the dataset through the Backend Service */
    refreshBackendDataset(inputGridOptions) {
      let gridOptions = this.sharedService.gridOptions;
      if (inputGridOptions) {
        gridOptions = { ...this.sharedService.gridOptions, ...inputGridOptions };
        this.sharedService.gridOptions = gridOptions;
      }
      this.backendUtilities?.refreshBackendDataset(gridOptions);
    }
    /** Run the Override function when it exists, if it returns True then it is usable/visible */
    runOverrideFunctionWhenExists(overrideFn, args) {
      if (typeof overrideFn === "function") {
        return !!overrideFn.call(this, args);
      }
      return true;
    }
    /**
     * @deprecated Sort items (by pointers) in an array by a property name
     * @param {Array<Object>} items array
     * @param {String} property name to sort with
     */
    sortItems(items, propertyName) {
      if (Array.isArray(items)) {
        items.sort((itemA, itemB) => {
          if (itemA && itemB && itemA.hasOwnProperty(propertyName) && itemB.hasOwnProperty(propertyName)) {
            return itemA[propertyName] - itemB[propertyName];
          }
          return 0;
        });
      }
    }
    /** Translate the array of items from an input key and assign them to their output key */
    translateItems(items, inputKey, outputKey) {
      if (Array.isArray(items)) {
        for (const item of items) {
          if (item.hasOwnProperty(inputKey)) {
            item[outputKey] = this.translaterService?.translate?.(item[inputKey]);
          }
        }
      }
    }
    /**
     * Loop through all Menu Command Items and use `titleKey`, `subMenuTitleKey` properties to translate (or use Locale) appropriate `title` property
     * @param {Array<MenuCommandItem | String>} items - Menu Command Items array
     * @param {Object} gridOptions - Grid Options
     */
    translateMenuItemsFromTitleKey(items, subMenuItemsKey = "commandItems") {
      for (const item of items) {
        if (typeof item === "object") {
          if (item.titleKey) {
            item.title = this.translateWhenEnabledAndServiceExist(`${item.titleKey}`, `TEXT_${item.titleKey}`, item._orgTitle);
          }
          if (item.subMenuTitleKey) {
            item.subMenuTitle = this.translateWhenEnabledAndServiceExist(`${item.subMenuTitleKey}`, `TEXT_${item.subMenuTitleKey}`);
          }
        }
        if (Array.isArray(item?.[subMenuItemsKey])) {
          this.translateMenuItemsFromTitleKey(item?.[subMenuItemsKey]);
        }
      }
    }
    /**
     * When "enabledTranslate" is set to True, we will try to translate if the Translate Service exist or use the Locales when not
     * @param {String} translationKey
     * @param {String} localeKey
     * @param {String} textToUse - optionally provide a static text to use (that will completely override the other arguments of the method)
     */
    translateWhenEnabledAndServiceExist(translationKey, localeKey, textToUse) {
      let text = "";
      const gridOptions = this.sharedService?.gridOptions;
      const locales = gridOptions?.locales ?? Constants.locales;
      if (textToUse) {
        text = textToUse;
      } else if (gridOptions.enableTranslate && this.translaterService?.translate) {
        text = this.translaterService.translate(translationKey || " ");
      } else if (localeKey in locales) {
        text = locales[localeKey];
      } else {
        text = localeKey;
      }
      return text;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/keyboardNavigation.js
  function bindKeyboardNavigation(containerElm, bindEventService, options) {
    const { focusedItemSelector, allItemsSelector, filterFn, onActivate, onEscape, onTab, eventServiceKey = "keyboard-navigation" } = options;
    const getVisibleItems = (contextElm) => {
      const menuContainer = contextElm || containerElm;
      let items = Array.from(menuContainer.querySelectorAll(allItemsSelector));
      if (filterFn) {
        items = items.filter(filterFn);
      }
      return items.filter((item) => item.offsetParent !== null);
    };
    bindEventService.bind(containerElm, "keydown", ((evt) => {
      const isNavigationKey = ["ArrowUp", "ArrowDown", "ArrowRight", "ArrowLeft", "Enter", " ", "Escape", "Tab"].includes(evt.key);
      if (!isNavigationKey) {
        return;
      }
      let focusedItem = containerElm.querySelector(focusedItemSelector);
      if (focusedItem) {
        let menuContainer = focusedItem.closest('[role="menu"], .slick-submenu') || containerElm;
        if (containerElm.classList.contains("slick-grid-menu") && containerElm.classList.contains("slick-menu-level-0")) {
          menuContainer = containerElm;
        }
        let allItems = getVisibleItems(menuContainer);
        const currentIndex = allItems.indexOf(focusedItem);
        const stopBubbling = () => {
          evt.preventDefault();
          evt.stopPropagation();
        };
        const isInSubMenu = focusedItem.closest(".slick-submenu");
        switch (evt.key) {
          case "Tab":
            if (onTab) {
              onTab(evt, focusedItem);
            }
            break;
          case "ArrowDown": {
            stopBubbling();
            const nextIndex = currentIndex < allItems.length - 1 ? currentIndex + 1 : 0;
            allItems[nextIndex]?.focus();
            break;
          }
          case "ArrowUp": {
            stopBubbling();
            const prevIndex = currentIndex > 0 ? currentIndex - 1 : allItems.length - 1;
            allItems[prevIndex]?.focus();
            break;
          }
          case "ArrowRight": {
            const isSubMenuTrigger = focusedItem.classList.contains("slick-submenu-item");
            if (isSubMenuTrigger && typeof options.onOpenSubMenu === "function") {
              stopBubbling();
              options.onOpenSubMenu(focusedItem);
            }
            break;
          }
          case "ArrowLeft": {
            if (isInSubMenu && typeof options.onCloseSubMenu === "function") {
              stopBubbling();
              options.onCloseSubMenu(focusedItem);
            }
            break;
          }
          case "Enter":
          case " ": {
            const isSubMenuTrigger = focusedItem.classList.contains("slick-submenu-item");
            if (isSubMenuTrigger && typeof options.onOpenSubMenu === "function") {
              stopBubbling();
              options.onOpenSubMenu(focusedItem);
            } else {
              stopBubbling();
              if (onActivate) {
                onActivate(focusedItem);
              }
            }
            break;
          }
          case "Escape":
            stopBubbling();
            if (isInSubMenu && typeof options.onCloseSubMenu === "function") {
              options.onCloseSubMenu(focusedItem);
            } else if (onEscape) {
              onEscape();
            }
            break;
        }
      }
    }), void 0, eventServiceKey);
    bindEventService.bind(containerElm, "mouseover", ((evt) => {
      const target = evt.target;
      const menuContainer = target.closest('[role="menu"], .slick-submenu') || containerElm;
      const allItems = getVisibleItems(menuContainer);
      const menuItem = target.closest(allItemsSelector);
      if (menuItem && allItems.includes(menuItem)) {
        menuItem.focus();
      }
    }), void 0, eventServiceKey);
  }
  function wireMenuKeyboardNavigation(menuElm, bindEventService, options) {
    const defaultSelector = '[role="menuitem"]:not(.disabled, .hidden, .slick-menu-item-disabled, .slick-menu-item-divider, .slick-menu-item-hidden)';
    const allItemsSelector = options?.allItemsSelector || defaultSelector;
    const focusedItemSelector = (options?.focusedItemSelector || defaultSelector) + ":focus";
    if (!menuElm.dataset.keyboardNavBound && typeof bindKeyboardNavigation === "function") {
      bindKeyboardNavigation(menuElm, bindEventService, {
        focusedItemSelector,
        allItemsSelector,
        filterFn: (item) => item.offsetParent !== null,
        onActivate: options?.onActivate,
        onEscape: options?.onEscape,
        onTab: options?.onTab,
        onOpenSubMenu: options?.onOpenSubMenu,
        onCloseSubMenu: options?.onCloseSubMenu,
        eventServiceKey: options?.eventServiceKey ?? "menu-keyboard"
      });
      menuElm.dataset.keyboardNavBound = "true";
    }
  }

  // node_modules/@slickgrid-universal/common/dist/extensions/menuBaseClass.js
  var MenuBaseClass = class {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(extensionUtility, pubSubService, sharedService) {
      this.extensionUtility = extensionUtility;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this._addonOptions = {};
      this._areVisibleColumnDifferent = false;
      this._camelPluginName = "";
      this._columnCheckboxes = [];
      this._columns = [];
      this._gridUid = "";
      this._menuCssPrefix = "";
      this._menuPluginCssPrefix = "";
      this.pluginName = "";
      this._bindEventService = new BindingEventService();
      this._eventHandler = new SlickEventHandler();
    }
    get addonOptions() {
      return this._addonOptions;
    }
    set addonOptions(newOptions) {
      this._addonOptions = newOptions;
    }
    get eventHandler() {
      return this._eventHandler;
    }
    get grid() {
      return this.sharedService.slickGrid;
    }
    get gridOptions() {
      return this.sharedService.gridOptions ?? {};
    }
    /** Getter for the grid uid */
    get gridUid() {
      return this._gridUid || (this.grid?.getUID() ?? "");
    }
    get gridUidSelector() {
      return this.gridUid ? `.${this.gridUid}` : "";
    }
    get menuCssClass() {
      return this._menuPluginCssPrefix || this._menuCssPrefix;
    }
    get menuElement() {
      return this._menuElm || document.querySelector(`.${this.menuCssClass}${this.gridUidSelector}`);
    }
    /** Dispose (destroy) of the plugin */
    dispose() {
      clearTimeout(this._timer);
      this._eventHandler?.unsubscribeAll();
      this._bindEventService.unbindAll();
      this.pubSubService.unsubscribeAll();
      this._commandTitleElm?.remove();
      this._optionTitleElm?.remove();
      this.disposeAllMenus();
      emptyElement(this._menuElm);
      this.menuElement?.remove();
      this._menuElm?.remove();
    }
    /** Remove/dispose all parent menus and any sub-menu(s) */
    disposeAllMenus() {
      this.disposeSubMenus();
      this._bindEventService.unbindAll("parent-menu");
      this._bindEventService.unbindAll("keyboard-navigation");
      document.querySelectorAll(`.${this.menuCssClass}${this.gridUidSelector}`).forEach((subElm) => subElm.remove());
      this._menuTriggerElement?.focus();
    }
    /**
     * Remove/dispose all previously opened sub-menu(s),
     * it will first remove all sub-menu listeners then remove sub-menus from the DOM
     */
    disposeSubMenus() {
      this._bindEventService.unbindAll("sub-menu");
      document.querySelectorAll(`.${this.menuCssClass}.slick-submenu${this.gridUidSelector}`).forEach((subElm) => subElm.remove());
    }
    setOptions(newOptions) {
      this._addonOptions = { ...this._addonOptions, ...newOptions };
    }
    get columnPickerMenuAddonOptions() {
      return this.addonOptions;
    }
    /** Create a Close button element and add it to the Menu element */
    addCloseButtomElement(menuElm) {
      const dismiss = this._camelPluginName === "gridMenu" ? "slick-grid-menu" : "slick-column-picker";
      const closePickerButtonElm = createDomElement("button", {
        type: "button",
        className: "close",
        ariaLabel: "Close",
        textContent: "\xD7",
        dataset: { dismiss }
      });
      menuElm.appendChild(closePickerButtonElm);
    }
    /** When columnTitle is provided, create a title element for the columns list. */
    addColumnTitleElementWhenDefined(menuElm) {
      if (this.columnPickerMenuAddonOptions?.columnTitle) {
        this._columnTitleElm = createDomElement("div", { className: "slick-menu-title", textContent: this.columnPickerMenuAddonOptions.columnTitle }, menuElm);
      }
    }
    /**
     * Handle column picker item click for both SlickColumnPicker and SlickGridMenu controls.
     * @param event - input checkbox event
     */
    handleColumnPickerItemClick(event) {
      const controlType = this._camelPluginName || "columnPicker";
      const iconContainerElm = event.target?.closest(".icon-checkbox-container");
      const iconElm = iconContainerElm?.querySelector(".sgi");
      const isChecked = !!event.target.checked;
      event.target.ariaChecked = String(isChecked);
      this.togglePickerCheckbox(iconElm, isChecked);
      if (event.target.dataset.option === "autoresize") {
        this.grid.setOptions({ forceFitColumns: isChecked });
        this.grid.updateColumns();
        return;
      }
      if (event.target.dataset.option === "syncresize") {
        this.grid.setOptions({ syncColumnCellResize: isChecked });
        return;
      }
      if (event.target.type === "checkbox") {
        this._areVisibleColumnDifferent = true;
        const columnId = event.target.dataset.columnid || "";
        const isFrozenAllowed = this.grid.validateColumnFreeze(columnId, true);
        let visibleColumns = this.grid.getVisibleColumns();
        if (!isFrozenAllowed || visibleColumns.length - 1 < 1 && !isChecked) {
          event.target.checked = true;
          this.togglePickerCheckbox(iconElm, true);
          return;
        }
        this.grid.updateColumnById(columnId, { hidden: !isChecked });
        if (!isChecked && this.gridOptions.enableCellRowSpan) {
          this.grid.remapAllColumnsRowSpan();
        }
        this.grid.updateColumns();
        visibleColumns = this.grid.getVisibleColumns();
        if (this.gridOptions.enableSelection && isChecked) {
          const rowSelection = this.grid.getSelectedRows();
          this.grid.setSelectedRows(rowSelection);
        }
        const callbackArgs = {
          columnId,
          showing: isChecked,
          allColumns: this.grid.getColumns(),
          visibleColumns,
          columns: visibleColumns,
          grid: this.grid
        };
        this.pubSubService.publish(`on${titleCase(controlType)}ColumnsChanged`, callbackArgs);
        if (typeof this.columnPickerMenuAddonOptions.onColumnsChanged === "function") {
          this.columnPickerMenuAddonOptions.onColumnsChanged(event, callbackArgs);
        }
        this.onColumnsChanged.notify(callbackArgs, null, this);
      }
      const liElm = event.target?.closest("li");
      if (liElm && typeof liElm.focus === "function") {
        setTimeout(() => liElm?.focus(), 0);
      }
    }
    /**
     * Because columns can be reordered, we have to update the `columns` to reflect the new order, however we can't just take `grid.getColumns()`,
     * as it does not include columns currently hidden by the picker. We create a new `columns` structure by leaving currently-hidden
     * columns in their original ordinal position and interleaving the results of the current column sort.
     */
    updateColumnPickerOrder() {
      const current = this.grid.getColumns().slice(0);
      const ordered = new Array(this._columns.length);
      for (let i = 0, ln = ordered.length; i < ln; i++) {
        const columnIdx = this.grid.getColumnIndex(this._columns[i].id);
        ordered[i] = columnIdx === void 0 ? this._columns[i] : current.shift();
      }
      this._columns = ordered;
    }
    populateColumnPicker(addonOptions, defaultExtractor) {
      const isGridMenu = this._camelPluginName === "gridMenu";
      const menuPrefix = isGridMenu ? "gridmenu-" : "";
      let processedColumns = this._columns;
      if (typeof addonOptions?.columnSort === "function") {
        processedColumns = [...this._columns].sort(addonOptions.columnSort);
      }
      if (typeof addonOptions.columnListBuilder === "function") {
        processedColumns = addonOptions.columnListBuilder(processedColumns);
      }
      for (const column of processedColumns) {
        const columnId = column.id;
        const columnLiElm = createDomElement("li", { tabIndex: -1 });
        if (column.excludeFromColumnPicker && !isGridMenu || column.excludeFromGridMenu && isGridMenu) {
          columnLiElm.className = "hidden";
        }
        const inputId = `${this._gridUid}-${menuPrefix}colpicker-${columnId}`;
        const isChecked = this.grid.getVisibleColumns().some((col) => col.id === columnId && !col.hidden);
        const { inputElm, labelElm, labelSpanElm } = this.generatePickerCheckbox(columnLiElm, inputId, { columnid: `${columnId}` }, isChecked);
        this._columnCheckboxes.push(inputElm);
        const headerColumnValueExtractorFn = typeof addonOptions?.headerColumnValueExtractor === "function" ? addonOptions.headerColumnValueExtractor : defaultExtractor;
        const columnLabel = headerColumnValueExtractorFn(column, this.gridOptions);
        applyHtmlToElement(labelSpanElm, columnLabel, this.gridOptions);
        columnLiElm.appendChild(labelElm);
        this._listElm.appendChild(columnLiElm);
      }
      if (!addonOptions.hideForceFitButton || !addonOptions.hideSyncResizeButton) {
        this._listElm.appendChild(document.createElement("hr"));
      }
      if (!addonOptions?.hideForceFitButton) {
        const fitLiElm = createDomElement("li", { tabIndex: -1 });
        const inputId = `${this._gridUid}-${menuPrefix}colpicker-forcefit`;
        const { labelSpanElm } = this.generatePickerCheckbox(fitLiElm, inputId, { option: "autoresize" }, this.gridOptions.forceFitColumns);
        labelSpanElm.textContent = addonOptions?.forceFitTitle ?? "";
        this._listElm.appendChild(fitLiElm);
      }
      if (!addonOptions?.hideSyncResizeButton) {
        const syncLiElm = createDomElement("li", { tabIndex: -1 });
        const inputId = `${this._gridUid}-${menuPrefix}colpicker-syncresize`;
        const { labelSpanElm } = this.generatePickerCheckbox(syncLiElm, inputId, { option: "syncresize" }, this.gridOptions.forceFitColumns);
        labelSpanElm.textContent = addonOptions?.syncResizeTitle ?? "";
        this._listElm.appendChild(syncLiElm);
      }
    }
    // --
    // protected functions
    // ------------------
    /**
     * add Menu Item Command when not found and also make sure that we have an `action` callback
     * (could be missing when provided by user) if not use built-in `action` callback when missing
     * @param originalMenuItems
     * @param builtInMenuItem
     * @param showCommand - is command hidden from menu option (deprecated)
     * @returns - returns true when added to the commands array
     */
    addMissingCommandOrAction(builtInMenuItem, hideCommands = [], targetMenuItems, originalMenuItems) {
      let skip = false;
      if (hideCommands.length) {
        skip = builtInMenuItem !== "divider" && new Set(hideCommands).has(builtInMenuItem.command);
      }
      if (builtInMenuItem !== "divider" && !skip) {
        const cmdName = builtInMenuItem.command;
        const cmd = (originalMenuItems ?? targetMenuItems).find((item) => item !== "divider" && item.command === cmdName);
        if (!cmd) {
          targetMenuItems.push(builtInMenuItem);
        } else if (!cmd.action) {
          cmd.action = builtInMenuItem.action;
        }
      }
    }
    /** Focus the first focusable menu item after menu is opened */
    focusFirstMenuItem(menuElm) {
      const menuList = menuElm.querySelector('[role="menu"]');
      if (menuList) {
        const menuItems = Array.from(menuList.querySelectorAll('[role="menuitem"]'));
        const firstMenuItem = menuItems.find((item) => {
          return !item.classList.contains("disabled") && !item.classList.contains("slick-menu-item-disabled") && !item.classList.contains("slick-menu-item-divider") && !item.classList.contains("slick-menu-item-hidden") && item.offsetParent !== null;
        });
        firstMenuItem?.focus();
      }
    }
    /** Set the menu trigger element for focus restoration when menu closes */
    setMenuTriggerElement(triggerElement) {
      this._menuTriggerElement = triggerElement;
    }
    togglePickerCheckbox(iconElm, checked = false) {
      if (iconElm) {
        iconElm.className = `sgi ${checked ? "sgi-icon-picker-check" : "sgi-icon-picker-uncheck"}`;
      }
    }
    generatePickerCheckbox(columnLiElm, inputId, inputData, checked = false) {
      const labelElm = createDomElement("label", { className: "checkbox-picker-label", htmlFor: inputId });
      const divElm = createDomElement("div", { className: "icon-checkbox-container" });
      const inputElm = createDomElement("input", {
        id: inputId,
        type: "checkbox",
        dataset: inputData,
        tabIndex: -1
      });
      const colInputDivElm = createDomElement("div", { className: `sgi ${checked ? "sgi-icon-picker-check" : "sgi-icon-picker-uncheck"}` });
      const labelSpanElm = createDomElement("span", { className: "checkbox-label" });
      divElm.appendChild(inputElm);
      divElm.appendChild(colInputDivElm);
      labelElm.appendChild(divElm);
      labelElm.appendChild(labelSpanElm);
      columnLiElm.appendChild(labelElm);
      if (checked) {
        inputElm.ariaChecked = "true";
        inputElm.checked = true;
      }
      return { inputElm, labelElm, labelSpanElm };
    }
    /**
     * Render slot content using a renderer callback.
     * The renderer receives the menu item and args for full context access.
     * @param parentElm - The parent element (LI) to insert the slot content into
     * @param slotRenderer - A callback function that receives (item, args) and returns string or HTMLElement
     * @param item - The menu item object (passed to callback)
     * @param args - The callback args providing access to grid, column, dataContext, etc.
     */
    renderSlotRenderer(parentElm, slotRenderer, item, args) {
      const result = slotRenderer(item, args);
      if (typeof result === "string") {
        parentElm.innerHTML = this.grid.sanitizeHtmlString(result);
      } else if (result instanceof HTMLElement) {
        parentElm.appendChild(result);
      }
    }
    addSubMenuTitleWhenExists(item, commandOrOptionMenu) {
      if (item !== "divider" && item?.subMenuTitle) {
        const subMenuTitleElm = document.createElement("div");
        subMenuTitleElm.className = "slick-menu-title";
        subMenuTitleElm.textContent = item.subMenuTitle;
        const subMenuTitleClass = item.subMenuTitleCssClass;
        if (subMenuTitleClass) {
          subMenuTitleElm.classList.add(...classNameToList(subMenuTitleClass));
        }
        commandOrOptionMenu.appendChild(subMenuTitleElm);
      }
    }
    /**
     * @deprecated
     * not entirely deprecated, but I just want to rename the function since we'll be deprecating/removing `optionItems` in v11
     * Construct the Command/Options Items section.
     */
    populateCommandOrOptionItems(itemType, menuOptions, commandOrOptionMenuElm, commandOrOptionItems, args, triggeredByElm, itemClickCallback, itemMouseoverCallback, keyboardNavOptions) {
      if (args && commandOrOptionItems && menuOptions) {
        for (const item of commandOrOptionItems) {
          const li = this.populateSingleCommandOrOptionItem(itemType, menuOptions, commandOrOptionMenuElm, item, args, triggeredByElm, itemClickCallback, itemMouseoverCallback);
          if (li && (item?.commandItems || item?.optionItems)) {
            const isCommand = typeof item.command === "string";
            const key = isCommand ? item.command : item.option;
            const keyStr = typeof key === "string" ? key.replace(/\s/g, "") : String(key);
            if (keyStr) {
              const selector = `.slick-submenu[data-sub-menu-parent="${keyStr}"]`;
              const subMenuElm = document.body.querySelector(selector);
              if (subMenuElm && !subMenuElm.dataset.keyboardNavBound) {
                this.wireMenuKeyboardNavigation(subMenuElm, keyboardNavOptions);
              }
            }
          }
        }
      }
    }
    /** @deprecated (same as above and rename the function in v11) Add the Command/Options Title when necessary. */
    populateCommandOrOptionTitle(itemType, menuOptions, commandOrOptionMenuElm, level) {
      if (menuOptions) {
        const isSubMenu = level > 0;
        const menuHeaderElm = this._menuElm?.querySelector(`.slick-${itemType}-header`) ?? createDomElement("div", { className: `slick-${itemType}-header` });
        const titleProp = `${itemType}Title`;
        if (!isSubMenu) {
          if (menuOptions?.[titleProp]) {
            emptyElement(menuHeaderElm);
            this[`_${itemType}TitleElm`] = createDomElement("span", {
              className: "slick-menu-title",
              textContent: menuOptions[titleProp]
            });
            menuHeaderElm.appendChild(this[`_${itemType}TitleElm`]);
            menuHeaderElm.classList.add("with-title");
          } else {
            menuHeaderElm.classList.add("no-title");
          }
          commandOrOptionMenuElm.appendChild(menuHeaderElm);
        }
      }
    }
    /** @deprecated (same as above and rename the function in v11) Construct the Command/Options Items section. */
    populateSingleCommandOrOptionItem(itemType, menuOptions, commandOrOptionMenuElm, item, args, triggeredByElm, itemClickCallback, itemMouseoverCallback) {
      let commandLiElm = null;
      const isHeaderButton = this._camelPluginName === "headerButtons";
      if (args && item && menuOptions) {
        const level = args?.level || 0;
        const pluginMiddleName = isHeaderButton ? "" : "-item";
        const menuCssPrefix = `${this._menuCssPrefix}${pluginMiddleName}`;
        let isItemVisible = true;
        let isItemUsable = true;
        if (typeof item === "object") {
          isItemVisible = this.extensionUtility.runOverrideFunctionWhenExists(item.itemVisibilityOverride, args);
          isItemUsable = this.extensionUtility.runOverrideFunctionWhenExists(item.itemUsabilityOverride, args);
        }
        if (!isItemVisible) {
          return null;
        }
        if (typeof item === "object" && item.itemUsabilityOverride) {
          item.disabled = isItemUsable ? false : true;
        }
        commandLiElm = createDomElement("li", { className: menuCssPrefix, role: "menuitem" });
        if (item !== "divider" && !item.divider) {
          commandLiElm.tabIndex = -1;
        }
        if (typeof item === "object" && isDefined(item[itemType])) {
          commandLiElm.dataset[itemType] = item?.[itemType];
        }
        if (commandOrOptionMenuElm) {
          commandOrOptionMenuElm.appendChild(commandLiElm);
        }
        if (typeof item === "object" && item.divider || item === "divider") {
          commandLiElm.classList.add(`${menuCssPrefix}-divider`);
          commandLiElm.setAttribute("role", "separator");
          return commandLiElm;
        }
        if (item.disabled) {
          commandLiElm.classList.add(`${menuCssPrefix}-disabled`);
          commandLiElm.ariaDisabled = "true";
        }
        if (item.hidden || item.showOnHover) {
          commandLiElm.classList.add(`${menuCssPrefix}-hidden`);
        }
        if (item.cssClass) {
          if (isHeaderButton) {
            commandLiElm.appendChild(createDomElement("span", { className: item.cssClass }));
          } else {
            commandLiElm.classList.add(...classNameToList(item.cssClass));
          }
        }
        if (item.tooltip) {
          commandLiElm.title = item.tooltip;
        }
        if (!isHeaderButton) {
          const slotRenderer = item.slotRenderer || this._addonOptions.defaultMenuItemRenderer;
          if (slotRenderer) {
            this.renderSlotRenderer(commandLiElm, slotRenderer, item, args);
          } else {
            const iconElm = createDomElement("div", { className: `${this._menuCssPrefix}-icon` });
            commandLiElm.appendChild(iconElm);
            if (item.iconCssClass) {
              iconElm.classList.add(...classNameToList(item.iconCssClass));
            } else if (!item.commandItems && !item.optionItems) {
              iconElm.textContent = "\u25E6";
            }
            const textElm = createDomElement("span", {
              className: `${this._menuCssPrefix}-content`,
              textContent: typeof item === "object" && item.title || ""
            }, commandLiElm);
            if (item.textCssClass) {
              textElm.classList.add(...classNameToList(item.textCssClass));
            }
          }
        }
        const eventGroupName = level > 0 ? "sub-menu" : "parent-menu";
        if (commandLiElm) {
          this._bindEventService.bind(commandLiElm, "click", ((e2) => {
            const slotRenderer = item.slotRenderer || this._addonOptions.defaultMenuItemRenderer;
            if (slotRenderer) {
              slotRenderer(item, args, e2);
            }
            if (e2.defaultPrevented) {
              return;
            }
            itemClickCallback.call(this, e2, itemType, item, level, args?.column);
            triggeredByElm?.focus();
            if (this.pluginName === "CellMenu" && !document.activeElement?.closest(".slick-cell")) {
              this.grid.focus("cell");
            }
          }), void 0, eventGroupName);
        }
        if (this._addonOptions?.subMenuOpenByEvent === "mouseover" && typeof itemMouseoverCallback === "function") {
          this._bindEventService.bind(commandLiElm, "mouseover", ((e2) => itemMouseoverCallback.call(this, e2, itemType, item, level, args?.column)), void 0, eventGroupName);
        }
        if (item.commandItems || item.optionItems) {
          const chevronElm = document.createElement("span");
          chevronElm.className = "sub-item-chevron";
          if (this._addonOptions.subItemChevronClass) {
            chevronElm.classList.add(...classNameToList(this._addonOptions.subItemChevronClass));
          } else {
            chevronElm.textContent = "\u2B9E";
          }
          commandLiElm.classList.add("slick-submenu-item");
          commandLiElm.ariaHasPopup = "true";
          commandLiElm.ariaExpanded = "false";
          commandLiElm.appendChild(chevronElm);
        }
      }
      return commandLiElm;
    }
    /**
     * Wire up keyboard navigation for the menu container using shared utility.
     * Should be called after menu DOM is created for all non-GridMenu plugins.
     * Handles sub-menu open/close and focus transfer for a11y.
     */
    wireMenuKeyboardNavigation(menuElm, options) {
      wireMenuKeyboardNavigation(menuElm, this._bindEventService, {
        ...options,
        onActivate: options?.onActivate ?? ((focusedItem) => {
          if (focusedItem) {
            focusedItem.dispatchEvent(new MouseEvent("click", { bubbles: true, cancelable: true }));
          }
        }),
        onEscape: options?.onEscape ?? (() => this.disposeAllMenus()),
        onTab: options?.onTab ?? ((evt) => {
          evt.preventDefault();
          evt.stopPropagation();
        }),
        allItemsSelector: options?.allItemsSelector,
        focusedItemSelector: options?.focusedItemSelector,
        onOpenSubMenu: (focusedItem) => {
          const command = focusedItem.dataset.command;
          let subMenuSelector = "";
          if (command) {
            const cmdStr = typeof command === "string" ? command.replace(/\s/g, "") : String(command);
            subMenuSelector = `.slick-submenu[data-sub-menu-parent="${cmdStr}"]`;
          }
          let subMenuElm = subMenuSelector ? document.body.querySelector(subMenuSelector) : null;
          if (!subMenuElm) {
            focusedItem.dispatchEvent(new MouseEvent("mouseover", { bubbles: true, cancelable: true }));
            if (subMenuSelector) {
              subMenuElm = document.body.querySelector(subMenuSelector);
            }
          }
          if (subMenuElm) {
            if (!subMenuElm.dataset.keyboardNavBound) {
              this.wireMenuKeyboardNavigation(subMenuElm, options);
            }
            focusedItem.ariaExpanded = "true";
            this.focusFirstMenuItem(subMenuElm);
          }
        },
        onCloseSubMenu: (focusedItem) => {
          const currentSubMenu = focusedItem.closest(".slick-submenu");
          if (currentSubMenu) {
            currentSubMenu.remove();
            const parentMenu = document.body.querySelector(`.slick-menu-level-${parseInt(currentSubMenu.className.match(/slick-menu-level-(\d+)/)?.[1] || "1", 10) - 1}`);
            let triggerSelector = "";
            const subMenuParentId = currentSubMenu.dataset.subMenuParent;
            if (subMenuParentId) {
              triggerSelector = `.slick-submenu-item[data-command="${subMenuParentId}"], .slick-submenu-item[data-option="${subMenuParentId}"]`;
            }
            let triggerItem = parentMenu && triggerSelector ? parentMenu.querySelector(triggerSelector) : null;
            if (!triggerItem && parentMenu) {
              triggerItem = parentMenu.querySelector('.slick-submenu-item, [role="menuitem"]');
            }
            if (triggerItem) {
              triggerItem.ariaExpanded = "false";
              triggerItem.focus();
            }
          }
        }
      });
    }
    /**
     * Reposition any of the menu plugins (CellMenu, ContextMenu, GridMenu, HeaderMenu) to where the user clicked,
     * it will calculate the best position depending on available space in the viewport and the menu type.
     */
    repositionMenu(e2, menuElm, buttonElm, addonOptions) {
      const targetElm = e2.target;
      const targetEvent = e2?.touches?.[0] ?? e2;
      const isSubMenu = menuElm.classList.contains("slick-submenu");
      const rowHeight = this.gridOptions.rowHeight || 0;
      const parentElm = isSubMenu ? e2.target.closest(".slick-menu-item") : this.pluginName === "CellMenu" || this.pluginName === "ContextMenu" ? e2.target.closest(".slick-cell") : targetEvent.target;
      if (menuElm && parentElm) {
        if (this.pluginName === "CellMenu" || this.pluginName === "ContextMenu") {
          menuElm.style.top = `0px`;
          menuElm.style.left = `0px`;
        }
        const containerElm = this.sharedService.gridContainerElement.classList.contains("slickgrid-container") ? this.sharedService.gridContainerElement : this.sharedService.gridContainerElement.querySelector(".slickgrid-container") ?? this.sharedService.gridContainerElement;
        const relativePos = getOffsetRelativeToParent(containerElm, targetElm);
        const menuWidth = menuElm.offsetWidth;
        const parentOffset = getOffset(parentElm);
        let menuOffsetLeft = 0;
        let menuOffsetTop = 0;
        let dropOffset = 0;
        let sideOffset = 0;
        let availableSpaceBottom = 0;
        let availableSpaceTop = 0;
        const { bottom: parentSpaceBottom, top: parentSpaceTop } = calculateAvailableSpace(parentElm);
        if (this.pluginName === "GridMenu" && buttonElm) {
          if (!isSubMenu) {
            const buttonComptStyle = getComputedStyle(buttonElm);
            const buttonWidth = parseInt(buttonComptStyle?.width ?? addonOptions?.menuWidth, 10);
            const contentMinWidth = addonOptions?.contentMinWidth ?? 0;
            const currentMenuWidth = (contentMinWidth > menuWidth ? contentMinWidth : menuWidth) || 0;
            if (contentMinWidth > 0) {
              menuElm.style.minWidth = `${contentMinWidth}px`;
            }
            const menuIconOffset = getOffset(buttonElm);
            const nextPositionLeft = menuIconOffset.right;
            menuOffsetTop = menuIconOffset.top + buttonElm.offsetHeight;
            menuOffsetLeft = addonOptions?.dropSide === "right" ? nextPositionLeft - buttonWidth : nextPositionLeft - currentMenuWidth;
          }
        } else if (this.pluginName === "CellMenu" || this.pluginName === "ContextMenu") {
          menuOffsetLeft = parentElm && this.pluginName === "CellMenu" ? parentOffset.left : targetEvent.pageX;
          menuOffsetTop = parentElm && this.pluginName === "CellMenu" ? parentOffset.top : targetEvent.pageY;
          dropOffset = Number(addonOptions?.autoAdjustDropOffset || 0);
          sideOffset = Number(addonOptions?.autoAlignSideOffset || 0);
        } else {
          menuOffsetLeft = isSubMenu ? parentOffset.left : relativePos?.left ?? 0;
          menuOffsetTop = isSubMenu ? parentOffset.top : (relativePos?.top ?? 0) + (addonOptions?.menuOffsetTop ?? 0) + targetElm.clientHeight;
        }
        if ((this.pluginName === "ContextMenu" || this.pluginName === "GridMenu") && isSubMenu) {
          menuOffsetLeft = parentOffset.left;
          menuOffsetTop = parentOffset.top;
        }
        const menuHeight = menuElm?.offsetHeight || 0;
        if ((this.pluginName === "GridMenu" || this.pluginName === "HeaderMenu") && isSubMenu) {
          availableSpaceBottom = parentSpaceBottom;
          availableSpaceTop = parentSpaceTop;
        } else if (this.pluginName === "CellMenu" || this.pluginName === "ContextMenu" || addonOptions?.autoAdjustDrop || addonOptions?.dropDirection) {
          availableSpaceBottom = parentSpaceBottom + dropOffset - rowHeight;
          availableSpaceTop = parentSpaceTop - dropOffset + rowHeight;
        }
        const dropPosition = availableSpaceBottom < menuHeight && availableSpaceTop > availableSpaceBottom ? "top" : "bottom";
        if (dropPosition === "top" || addonOptions?.dropDirection === "top") {
          menuElm.classList.remove("dropdown");
          menuElm.classList.add("dropup");
          if (isSubMenu) {
            menuOffsetTop -= menuHeight - dropOffset - parentElm.clientHeight;
          } else {
            menuOffsetTop -= menuHeight - dropOffset;
          }
        } else {
          menuElm.classList.remove("dropup");
          menuElm.classList.add("dropdown");
          if (this.pluginName === "CellMenu" || this.pluginName === "ContextMenu") {
            menuOffsetTop = menuOffsetTop + dropOffset;
            if (this.pluginName === "CellMenu") {
              if (isSubMenu) {
                menuOffsetTop += dropOffset;
              } else {
                menuOffsetTop += rowHeight + dropOffset;
              }
            }
          }
        }
        if (addonOptions?.autoAlign || addonOptions?.autoAlignSide || addonOptions?.dropSide === "left") {
          let subMenuPosCalc = menuOffsetLeft + Number(menuWidth);
          if (isSubMenu) {
            subMenuPosCalc += parentElm.clientWidth;
          }
          const gridPos = this.grid.getGridPosition();
          const browserWidth = document.documentElement.clientWidth;
          const dropSide = subMenuPosCalc >= gridPos.width || subMenuPosCalc >= browserWidth ? "left" : "right";
          let needHeaderMenuOffsetLeftRecalc = false;
          if (dropSide === "left" || !isSubMenu && addonOptions?.dropSide === "left") {
            menuElm.classList.remove("dropright");
            menuElm.classList.add("dropleft");
            if (this.pluginName === "HeaderMenu") {
              if (isSubMenu) {
                menuOffsetLeft -= menuWidth;
              } else {
                needHeaderMenuOffsetLeftRecalc = true;
              }
            } else if (this.pluginName === "CellMenu" && !isSubMenu) {
              const parentCellWidth = parentElm.offsetWidth || 0;
              menuOffsetLeft -= Number(menuWidth) - parentCellWidth - sideOffset;
            } else if (this.pluginName !== "GridMenu" || this.pluginName === "GridMenu" && isSubMenu) {
              menuOffsetLeft -= Number(menuWidth) - sideOffset;
            }
          } else {
            menuElm.classList.remove("dropleft");
            menuElm.classList.add("dropright");
            if (isSubMenu) {
              menuOffsetLeft += sideOffset + parentElm.offsetWidth;
            } else {
              if (this.pluginName === "HeaderMenu") {
                needHeaderMenuOffsetLeftRecalc = true;
              } else {
                menuOffsetLeft += sideOffset;
              }
            }
          }
          if (needHeaderMenuOffsetLeftRecalc) {
            menuOffsetLeft = relativePos?.left ?? 0;
            if (addonOptions?.autoAlign && gridPos?.width && menuOffsetLeft + (menuElm.clientWidth ?? 0) >= gridPos.width) {
              menuOffsetLeft = menuOffsetLeft + targetElm.clientWidth - menuElm.clientWidth + (addonOptions?.autoAlignOffset || 0);
            }
          }
        }
        menuElm.style.top = `${menuOffsetTop}px`;
        menuElm.style.left = `${menuOffsetLeft}px`;
        if (this.pluginName === "GridMenu") {
          menuElm.style.opacity = "1";
          menuElm.style.display = "block";
        }
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickAutoTooltip.js
  var SlickAutoTooltip = class {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(options) {
      this.pluginName = "AutoTooltips";
      this._defaults = {
        enableForCells: true,
        enableForHeaderCells: false,
        maxToolTipLength: void 0,
        replaceExisting: true
      };
      this._eventHandler = new SlickEventHandler();
      this._addonOptions = options;
    }
    get addonOptions() {
      return this._addonOptions;
    }
    get eventHandler() {
      return this._eventHandler;
    }
    /** Initialize plugin. */
    init(grid) {
      this._addonOptions = { ...this._defaults, ...this.addonOptions };
      this._grid = grid;
      if (this._addonOptions.enableForCells) {
        this._eventHandler.subscribe(this._grid.onMouseEnter, this.handleMouseEnter.bind(this));
      }
      if (this._addonOptions.enableForHeaderCells) {
        this._eventHandler.subscribe(this._grid.onHeaderMouseEnter, this.handleHeaderMouseEnter.bind(this));
      }
    }
    destroy() {
      this.dispose();
    }
    /** Dispose (destroy) the SlickGrid 3rd party plugin */
    dispose() {
      this._eventHandler?.unsubscribeAll();
    }
    // --
    // protected functions
    // ------------------
    /**
     * Handle mouse entering grid cell to add/remove tooltip.
     * @param {SlickEventData} event - The event
     */
    handleMouseEnter(event) {
      const cell = this._grid.getCellFromEvent(event);
      if (cell) {
        let node = this._grid.getCellNode(cell.row, cell.cell);
        let text;
        if (this._addonOptions && node && (!node.title || this._addonOptions?.replaceExisting)) {
          if (node.clientWidth < node.scrollWidth) {
            text = node.textContent?.trim() ?? "";
            if (this._addonOptions?.maxToolTipLength && text.length > this._addonOptions?.maxToolTipLength) {
              text = text.substring(0, this._addonOptions.maxToolTipLength - 3) + "...";
            }
          } else {
            text = "";
          }
          node.title = text;
        }
        node = null;
      }
    }
    /**
     * Handle mouse entering header cell to add/remove tooltip.
     * @param {SlickEventData} event - The event
     * @param {Object} args.column - The column definition
     */
    handleHeaderMouseEnter(event, args) {
      const column = args.column;
      let node;
      const targetElm = event.target;
      if (targetElm) {
        node = targetElm.closest(".slick-header-column");
        if (node && !column?.toolTip) {
          const titleVal = targetElm.clientWidth < node.clientWidth ? column?.name ?? "" : "";
          node.title = stripTags(getHtmlStringOutput(titleVal, "innerHTML"));
        }
      }
      node = null;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickCellExcelCopyManager.js
  var SlickCellExcelCopyManager = class {
    constructor() {
      this.pluginName = "CellExcelCopyManager";
      this._eventHandler = new SlickEventHandler();
      this._bindingEventService = new BindingEventService();
    }
    get addonOptions() {
      return this._addonOptions;
    }
    get eventHandler() {
      return this._eventHandler;
    }
    get commandQueue() {
      return this._commandQueue;
    }
    get gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    get undoRedoBuffer() {
      return this._undoRedoBuffer;
    }
    init(grid, options) {
      this._grid = grid;
      this.createUndoRedoBuffer();
      const selectionType = this.gridOptions.selectionOptions?.selectionType || "cell";
      this._cellSelectionModel = grid.getSelectionModel() ?? new SlickHybridSelectionModel({ ...this.gridOptions.selectionOptions, selectionType });
      this._cellSelectionModel.setOptions({ ...this.gridOptions.selectionOptions, selectionType });
      this._grid.setSelectionModel(this._cellSelectionModel);
      this._bindingEventService.bind(document.body, "keydown", this.handleBodyKeyDown.bind(this));
      this._addonOptions = { ...this.getDefaultOptions(), ...options };
      this._cellExternalCopyManagerPlugin = new SlickCellExternalCopyManager();
      this._cellExternalCopyManagerPlugin.init(this._grid, this._addonOptions);
      this._eventHandler.subscribe(this._cellExternalCopyManagerPlugin.onCopyCells, (e2, args) => {
        if (this._addonOptions && typeof this._addonOptions.onCopyCells === "function") {
          this._addonOptions.onCopyCells(e2, args);
        }
      });
      this._eventHandler.subscribe(this._cellExternalCopyManagerPlugin.onCopyCancelled, (e2, args) => {
        if (this._addonOptions && typeof this._addonOptions.onCopyCancelled === "function") {
          this._addonOptions.onCopyCancelled(e2, args);
        }
      });
      this._eventHandler.subscribe(this._cellExternalCopyManagerPlugin.onPasteCells, (e2, args) => {
        if (this._addonOptions && typeof this._addonOptions.onPasteCells === "function") {
          this._addonOptions.onPasteCells(e2, args);
        }
      });
    }
    /** Dispose of the 3rd party addon (plugin) */
    dispose() {
      this._eventHandler.unsubscribeAll();
      this._bindingEventService.unbindAll();
      this._cellSelectionModel?.dispose();
      this._cellExternalCopyManagerPlugin?.dispose();
    }
    //
    // protected functions
    // ---------------------
    /** Create an undo redo buffer used by the Excel like copy */
    createUndoRedoBuffer() {
      let commandCtr = 0;
      this._commandQueue = [];
      this._undoRedoBuffer = {
        queueAndExecuteCommand: (editCommand) => {
          this._commandQueue[commandCtr] = editCommand;
          commandCtr++;
          editCommand.execute();
        },
        undo: () => {
          if (commandCtr === 0) {
            return;
          }
          commandCtr--;
          const command = this._commandQueue[commandCtr];
          if (command && SlickGlobalEditorLock.cancelCurrentEdit()) {
            command.undo();
          }
        },
        redo: () => {
          if (commandCtr >= this._commandQueue.length) {
            return;
          }
          const command = this._commandQueue[commandCtr];
          commandCtr++;
          if (command && SlickGlobalEditorLock.cancelCurrentEdit()) {
            command.execute();
          }
        }
      };
    }
    /** @return default plugin (addon) options */
    getDefaultOptions() {
      let newRowIds = 0;
      return {
        clipboardCommandHandler: (editCommand) => {
          this._undoRedoBuffer.queueAndExecuteCommand.call(this._undoRedoBuffer, editCommand);
        },
        dataItemColumnValueExtractor: (item, columnDef, row = 0, cell = 0) => {
          const activeCell = this._grid.getActiveCell();
          const isActiveEditorCurrentCell = this._grid.getCellEditor() && activeCell?.row === row && activeCell?.cell === cell;
          const copyActiveEditorCell = this.addonOptions?.copyActiveEditorCell || false;
          if (!this.gridOptions.editable || !columnDef.editor || !isActiveEditorCurrentCell || copyActiveEditorCell) {
            const isEvaluatingFormatter = columnDef.exportWithFormatter !== void 0 ? columnDef.exportWithFormatter : this.gridOptions.textExportOptions?.exportWithFormatter;
            if (columnDef.formatter && isEvaluatingFormatter) {
              const formattedOutput = columnDef.formatter(row, cell, item[columnDef.field], columnDef, item, this._grid);
              const cellResult = isPrimitiveOrHTML(formattedOutput) ? formattedOutput : formattedOutput.html || formattedOutput.text;
              if (columnDef.sanitizeDataExport || this.gridOptions.textExportOptions?.sanitizeDataExport) {
                const outputString = cellResult instanceof HTMLElement ? cellResult.innerHTML : cellResult;
                return stripTags(outputString ?? "");
              }
              return formattedOutput;
            }
          }
          return null;
        },
        readOnlyMode: false,
        includeHeaderWhenCopying: false,
        newRowCreator: (count) => {
          for (let i = 0; i < count; i++) {
            this._grid.getData().addItem({ [this.gridOptions.datasetIdPropertyName || "id"]: `newRow_${newRowIds++}` });
          }
        },
        replaceNewlinesWith: false,
        removeDoubleQuotesOnPaste: false
      };
    }
    /** Hook an undo shortcut key hook that will redo/undo the copy buffer using Ctrl+(Shift)+Z keyboard events */
    handleBodyKeyDown(e2) {
      if (e2.key === "Z" && (e2.ctrlKey || e2.metaKey)) {
        if (e2.shiftKey) {
          this._undoRedoBuffer.redo();
        } else {
          this._undoRedoBuffer.undo();
        }
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickCellExternalCopyManager.js
  var CLEAR_COPY_SELECTION_DELAY = 2e3;
  var noop = () => {
  };
  var SlickCellExternalCopyManager = class {
    constructor() {
      this.pluginName = "CellExternalCopyManager";
      this._copiedCellStyle = "copied";
      this._copiedCellStyleLayerKey = "copy-manager";
      this._copiedRanges = null;
      this._dataWrapper = new DataWrapperService();
      this.onCopyCells = new SlickEvent("onCopyCells");
      this.onCopyCancelled = new SlickEvent("onCopyCancelled");
      this.onPasteCells = new SlickEvent("onPasteCells");
      this.onBeforePasteCell = new SlickEvent("onBeforePasteCell");
      this._eventHandler = new SlickEventHandler();
    }
    get addonOptions() {
      return this._addonOptions;
    }
    get eventHandler() {
      return this._eventHandler;
    }
    init(grid, options) {
      this._grid = grid;
      this._dataWrapper.init(grid);
      this._addonOptions = { ...this._addonOptions, ...options };
      this._copiedCellStyleLayerKey = this._addonOptions.copiedCellStyleLayerKey || "copy-manager";
      this._copiedCellStyle = this._addonOptions.copiedCellStyle || "copied";
      this._onCopyInit = this._addonOptions.onCopyInit || void 0;
      this._onCopySuccess = this._addonOptions.onCopySuccess || void 0;
      const pubSub = grid.getPubSubService();
      if (pubSub) {
        Utils.addSlickEventPubSubWhenDefined(pubSub, this);
      }
      this._eventHandler.subscribe(this._grid.onKeyDown, this.handleKeyDown.bind(this));
      const cellSelectionModel = grid.getSelectionModel();
      if (!cellSelectionModel) {
        throw new Error(`Selection model is mandatory for this plugin. Please set a selection model on the grid before adding this plugin: grid.setSelectionModel(new SlickHybridSelectionModel())`);
      }
      this._eventHandler.subscribe(cellSelectionModel.onSelectedRangesChanged, () => {
        if (!this._grid.getEditorLock().isActive() && !document.activeElement?.classList.contains("slick-filter")) {
          this._grid.focus("internal");
        }
      });
      if (grid && typeof this._addonOptions?.onBeforePasteCell === "function") {
        this._eventHandler.subscribe(this.onBeforePasteCell, (e2, args) => {
          const column = grid.getColumnByIdx(args.cell);
          const returnedArgs = {
            row: args.row,
            cell: args.cell,
            dataView: grid.getData(),
            grid,
            columnDef: column,
            dataContext: grid.getDataItem(args.row)
          };
          return this._addonOptions.onBeforePasteCell?.(e2, returnedArgs);
        });
      }
    }
    dispose() {
      clearTimeout(this._clearCopyTI);
      this._eventHandler.unsubscribeAll();
    }
    clearCopySelection() {
      this._grid.removeCellCssStyles(this._copiedCellStyleLayerKey);
    }
    getHeaderValueForColumn(columnDef) {
      if (typeof this._addonOptions.headerColumnValueExtractor === "function") {
        const val = getHtmlStringOutput(this._addonOptions.headerColumnValueExtractor(columnDef), "innerHTML");
        if (val) {
          return stripTags(val);
        }
      }
      return getHtmlStringOutput(columnDef.name || "", "innerHTML");
    }
    getDataItemValueForColumn(item, columnDef, row, cell, event) {
      if (typeof this._addonOptions.dataItemColumnValueExtractor === "function") {
        const val = this._addonOptions.dataItemColumnValueExtractor(item, columnDef, row, cell);
        if (val) {
          return val instanceof HTMLElement ? stripTags(val.innerHTML) : val;
        }
      }
      let retVal = "";
      if (columnDef) {
        if (columnDef.editorClass) {
          const tmpP = document.createElement("p");
          const editor = new columnDef.editorClass({
            container: tmpP,
            // a dummy container
            column: columnDef,
            event,
            position: { top: 0, left: 0 },
            // a dummy position required by some editors
            gridPosition: { top: 0, left: 0 },
            // a dummy position required by some editors
            grid: this._grid,
            cancelChanges: noop,
            commitChanges: noop
          });
          editor.loadValue(item);
          retVal = editor.serializeValue();
          editor.destroy();
          tmpP.remove();
        } else {
          retVal = item[columnDef.field || ""];
        }
      }
      return retVal;
    }
    setDataItemValueForColumn(item, columnDef, value) {
      if (!columnDef?.denyPaste) {
        if (this._addonOptions.dataItemColumnValueSetter) {
          const setterResult = this._addonOptions.dataItemColumnValueSetter(item, columnDef, value);
          if (setterResult !== true) {
            return setterResult;
          }
        }
        if (columnDef.editorClass) {
          const tmpDiv = document.createElement("div");
          const editor = new columnDef.editorClass({
            container: tmpDiv,
            // a dummy container
            column: columnDef,
            event: null,
            position: { top: 0, left: 0 },
            // a dummy position required by some editors
            gridPosition: { top: 0, left: 0 },
            // a dummy position required by some editors
            grid: this._grid,
            cancelChanges: noop,
            commitChanges: noop
          });
          editor.loadValue({ ...item, [columnDef.field]: value });
          const validationResults = editor.validate(void 0, value);
          if (!validationResults.valid) {
            const activeCell = this._grid.getActiveCell();
            this._grid.onValidationError.notify({
              editor,
              cellNode: this._grid.getActiveCellNode(),
              validationResults,
              row: activeCell?.row,
              cell: activeCell?.cell,
              column: columnDef,
              grid: this._grid
            });
          }
          editor.applyValue(item, value);
          editor.destroy();
          tmpDiv.remove();
        } else {
          item[columnDef.field] = value;
        }
      }
    }
    setIncludeHeaderWhenCopying(includeHeaderWhenCopying) {
      this._addonOptions.includeHeaderWhenCopying = includeHeaderWhenCopying;
    }
    //
    // protected functions
    // ---------------------
    decodeTabularData(grid, clipText) {
      const columns = grid.getColumns();
      const clipRows = clipText.replaceAll("\r\n", "\n").split(/[\n\f\r](?=(?:[^"]*"[^"]*")*[^"]*$)/);
      if (clipRows[clipRows.length - 1] === "") {
        clipRows.pop();
      }
      let j2 = 0;
      const clippedRange = [];
      for (const clipRow of clipRows) {
        const jNext = j2++;
        const splitRegex = /\t(?=(?:[^"]*"[^"]*")*[^"]*$)/;
        clippedRange[jNext] = clipRow.split(splitRegex).map((item) => item.replaceAll("\n", this._addonOptions.replaceNewlinesWith || "\n").replaceAll("\r", "").replaceAll('"', this._addonOptions.removeDoubleQuotesOnPaste ? "" : '"'));
      }
      const selectedCell = this._grid.getActiveCell();
      const ranges = this._grid.getSelectionModel()?.getSelectedRanges();
      const selectedRange = ranges?.length ? ranges[0] : null;
      let activeRow;
      let activeCell;
      if (selectedRange) {
        activeRow = selectedRange.fromRow;
        activeCell = selectedRange.fromCell;
      } else if (selectedCell) {
        activeRow = selectedCell.row;
        activeCell = selectedCell.cell;
      } else {
        return;
      }
      let oneCellToMultiple = false;
      let destH = clippedRange.length;
      let destW = clippedRange.length ? clippedRange[0].length : 0;
      if (clippedRange.length === 1 && clippedRange[0].length === 1 && selectedRange) {
        oneCellToMultiple = true;
        destH = selectedRange.toRow - selectedRange.fromRow + 1;
        destW = selectedRange.toCell - selectedRange.fromCell + 1;
      }
      const availableRows = this._dataWrapper.getDataLength() - activeRow;
      if (availableRows < destH && typeof this._addonOptions.newRowCreator === "function") {
        const rowsToAdd = destH - availableRows;
        const rowsBeforePaste = this._dataWrapper.getDataLength();
        this._addonOptions.newRowCreator(rowsToAdd);
        const rowsAfterPaste = this._dataWrapper.getDataLength();
        if (rowsAfterPaste !== rowsBeforePaste + rowsToAdd) {
          console.warn(`[Slickgrid-Universal] The "newRowCreator" did not add the correct amount of rows, it should add "${rowsToAdd}" rows but it added "${rowsAfterPaste - rowsBeforePaste}" rows`);
        }
        this._grid.render();
      }
      const overflowsBottomOfGrid = activeRow + destH > this._dataWrapper.getDataLength();
      if (overflowsBottomOfGrid && typeof this._addonOptions.newRowCreator === "function") {
        const newRowsNeeded = activeRow + destH - this._dataWrapper.getDataLength();
        this._addonOptions.newRowCreator(newRowsNeeded);
      }
      let lastDestX = activeCell;
      const clipCommand = {
        isClipboardCommand: true,
        clippedRange,
        oldValues: [],
        cellExternalCopyManager: this,
        _options: this._addonOptions,
        setDataItemValueForColumn: this.setDataItemValueForColumn,
        markCopySelection: this.markCopySelection,
        oneCellToMultiple,
        activeRow,
        activeCell,
        destH,
        destW,
        maxDestY: this._dataWrapper.getDataLength(),
        maxDestX: this._grid.getColumns().length,
        h: 0,
        w: 0,
        execute: () => {
          clipCommand.h = 0;
          lastDestX = activeCell;
          for (let y2 = 0; y2 < clipCommand.destH; y2++) {
            clipCommand.oldValues[y2] = [];
            clipCommand.w = 0;
            clipCommand.h++;
            let xOffset = 0;
            for (let x2 = 0; x2 < clipCommand.destW; x2++) {
              const desty = activeRow + y2;
              const destx = activeCell + x2;
              const column = columns[destx];
              if (!column) {
                break;
              }
              if (column.hidden) {
                clipCommand.destW++;
                xOffset++;
                continue;
              }
              clipCommand.w++;
              lastDestX = destx;
              if (desty < clipCommand.maxDestY && destx < clipCommand.maxDestX) {
                const dt = this._dataWrapper.getDataItem(desty);
                if (this._grid.triggerEvent(this.onBeforePasteCell, { row: desty, cell: destx, dt, column, target: "grid" }).getReturnValue() === false) {
                  continue;
                }
                clipCommand.oldValues[y2][x2 - xOffset] = dt[column["field"]];
                if (oneCellToMultiple) {
                  this.setDataItemValueForColumn(dt, column, clippedRange[0][0]);
                } else {
                  this.setDataItemValueForColumn(dt, column, clippedRange[y2] ? clippedRange[y2][x2 - xOffset] : "");
                }
                this._grid.updateCell(desty, destx);
                this._grid.onCellChange.notify({
                  row: desty,
                  cell: destx,
                  item: dt,
                  grid: this._grid,
                  column: {}
                });
              }
            }
          }
          const bRange = new SlickRange(activeRow, activeCell, activeRow + clipCommand.h - 1, lastDestX);
          this.markCopySelection([bRange]);
          this._grid.getSelectionModel()?.setSelectedRanges([bRange]);
          this.onPasteCells.notify({ ranges: [bRange] });
        },
        undo: () => {
          for (let y2 = 0; y2 < clipCommand.destH; y2++) {
            let xOffset = 0;
            for (let x2 = 0; x2 < clipCommand.destW; x2++) {
              const desty = activeRow + y2;
              const destx = activeCell + x2;
              const column = columns[destx];
              if (column.hidden) {
                xOffset++;
                continue;
              }
              if (desty < clipCommand.maxDestY && destx < clipCommand.maxDestX) {
                const dt = this._dataWrapper.getDataItem(desty);
                this.setDataItemValueForColumn(dt, column, clipCommand.oldValues[y2][x2 - xOffset]);
                this._grid.updateCell(desty, destx);
                this._grid.onCellChange.notify({
                  row: desty,
                  cell: destx,
                  item: dt,
                  grid: this._grid,
                  column: {}
                });
              }
            }
          }
          const bRange = new SlickRange(activeRow, activeCell, activeRow + clipCommand.h - 1, lastDestX);
          this.markCopySelection([bRange]);
          this._grid.getSelectionModel()?.setSelectedRanges([bRange]);
          this.onPasteCells.notify({ ranges: [bRange] });
          if (typeof this._addonOptions.onPasteCells === "function") {
            this._addonOptions.onPasteCells(new SlickEventData(), { ranges: [bRange] });
          }
        }
      };
      if (this._addonOptions.clipboardCommandHandler) {
        this._addonOptions.clipboardCommandHandler(clipCommand);
      } else {
        clipCommand.execute();
      }
    }
    async handleKeyDown(e2) {
      try {
        let ranges;
        if (!this._grid.getEditorLock().isActive() || this._grid.getOptions().autoEdit) {
          if (e2.key === "Escape") {
            if (this._copiedRanges) {
              e2.preventDefault();
              this.clearCopySelection();
              this.onCopyCancelled.notify({ ranges: this._copiedRanges });
              if (typeof this._addonOptions.onCopyCancelled === "function") {
                this._addonOptions.onCopyCancelled(e2, { ranges: this._copiedRanges });
              }
              this._copiedRanges = null;
            }
          }
          if ((e2.key === "c" || e2.key === "Insert") && (e2.ctrlKey || e2.metaKey) && !e2.shiftKey) {
            if (typeof this._onCopyInit === "function") {
              this._onCopyInit.call(this);
            }
            ranges = this._grid.getSelectionModel()?.getSelectedRanges() ?? [];
            if (ranges.length !== 0) {
              this._copiedRanges = ranges;
              this.markCopySelection(ranges);
              this.onCopyCells.notify({ ranges });
              if (typeof this._addonOptions.onCopyCells === "function") {
                this._addonOptions.onCopyCells(e2, { ranges });
              }
              const columns = this._grid.getColumns();
              let clipText = "";
              for (let rg = 0; rg < ranges.length; rg++) {
                const range2 = ranges[rg];
                const clipTextRows = [];
                for (let i = range2.fromRow; i < range2.toRow + 1; i++) {
                  const clipTextCells = [];
                  const dt = this._dataWrapper.getDataItem(i);
                  if (clipTextRows.length === 0 && this._addonOptions.includeHeaderWhenCopying) {
                    const clipTextHeaders = [];
                    for (let j2 = range2.fromCell; j2 < range2.toCell + 1; j2++) {
                      if (columns[j2]) {
                        const colName = columns[j2].name instanceof HTMLElement ? stripTags(columns[j2].name.innerHTML) : columns[j2].name;
                        if (colName.length > 0 && !columns[j2].hidden) {
                          clipTextHeaders.push(this.getHeaderValueForColumn(columns[j2]));
                        }
                      }
                    }
                    clipTextRows.push(clipTextHeaders.join("	"));
                  }
                  for (let j2 = range2.fromCell; j2 < range2.toCell + 1; j2++) {
                    if (columns[j2]) {
                      const colName = columns[j2].name instanceof HTMLElement ? stripTags(columns[j2].name.innerHTML) : columns[j2].name;
                      if (colName.length > 0 && !columns[j2].hidden) {
                        clipTextCells.push(this.getDataItemValueForColumn(dt, columns[j2], i, j2, e2));
                      }
                    }
                  }
                  clipTextRows.push(clipTextCells.join("	"));
                }
                clipText += clipTextRows.join("\r\n") + "\r\n";
              }
              const clipboardOverrideFn = this._grid.getOptions().clipboardWriteOverride;
              clipboardOverrideFn ? clipboardOverrideFn(clipText) : await navigator.clipboard.writeText(clipText);
              if (typeof this._onCopySuccess === "function") {
                const rowCount = ranges.length === 1 ? ranges[0].toRow + 1 - ranges[0].fromRow : ranges.length;
                this._onCopySuccess(rowCount);
              }
              return false;
            }
          }
          if (!this._addonOptions.readOnlyMode && (e2.key === "v" && (e2.ctrlKey || e2.metaKey) && !e2.shiftKey || e2.key === "Insert" && e2.shiftKey && !e2.ctrlKey)) {
            const pastedText = await navigator.clipboard.readText();
            this.decodeTabularData(this._grid, pastedText);
          }
        }
      } catch (err) {
        console.error(`Unable to read/write to clipboard. Please check your browser settings or permissions. Error: ${err}`);
      }
    }
    markCopySelection(ranges) {
      this.clearCopySelection();
      const columns = this._grid.getColumns();
      const hash = {};
      for (const range2 of ranges) {
        for (let j2 = range2.fromRow; j2 <= range2.toRow; j2++) {
          hash[j2] = {};
          for (let k2 = range2.fromCell; k2 <= range2.toCell && k2 < columns.length; k2++) {
            hash[j2][columns[k2].id] = this._copiedCellStyle;
          }
        }
      }
      this._grid.setCellCssStyles(this._copiedCellStyleLayerKey, hash);
      clearTimeout(this._clearCopyTI);
      this._clearCopyTI = setTimeout(() => this.clearCopySelection(), this.addonOptions?.clearCopySelectionDelay || CLEAR_COPY_SELECTION_DELAY);
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/menuFromCellBaseClass.js
  var MenuFromCellBaseClass = class extends MenuBaseClass {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(extensionUtility, pubSubService, sharedService) {
      super(extensionUtility, pubSubService, sharedService);
      this.extensionUtility = extensionUtility;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this._currentCell = -1;
      this._currentRow = -1;
      this._lastMenuTypeClicked = "";
      this._subMenuParentId = "";
    }
    createParentMenu(event, cell) {
      this.menuElement?.remove();
      this._menuElm = void 0;
      if (cell) {
        this._currentCell = cell.cell;
        this._currentRow = cell.row;
        const commandItems = this._addonOptions?.commandItems || [];
        const optionItems = this._addonOptions?.optionItems || [];
        const callbackArgs = {
          cell: this._currentCell,
          row: this._currentRow,
          grid: this.grid
          // menu: this._pluginOptions,
        };
        this.closeMenu(event, callbackArgs);
        if (typeof event.stopPropagation === "function") {
          this.pubSubService.publish(`on${titleCase(this._camelPluginName)}BeforeMenuShow`, callbackArgs);
          if (typeof this.addonOptions?.onBeforeMenuShow === "function" && this.addonOptions.onBeforeMenuShow(event, callbackArgs) === false) {
            return;
          }
        }
        const triggerElement = event.target || this.grid.getActiveCellNode();
        if (triggerElement) {
          this.setMenuTriggerElement(triggerElement);
        }
        this._menuElm = this.createMenu(triggerElement, commandItems, optionItems);
        if (this._menuElm) {
          this._menuElm.style.top = `${(event.pageY || 0) + 5}px`;
          this._menuElm.style.left = `${event.pageX}px`;
          this._menuElm.style.display = "block";
          document.body.appendChild(this._menuElm);
          this.focusFirstMenuItem(this._menuElm);
          this.wireMenuKeyboardNavigation(this._menuElm);
        }
        this.pubSubService.publish(`on${titleCase(this._camelPluginName)}AfterMenuShow`, callbackArgs);
        if (typeof this.addonOptions?.onAfterMenuShow === "function" && this.addonOptions.onAfterMenuShow(event, callbackArgs) === false) {
          return;
        }
      }
      return this._menuElm;
    }
    /**
     * Create parent menu or sub-menu(s), a parent menu will start at level 0 while sub-menu(s) will be incremented
     * @param commandItems - array of optional commands or dividers
     * @param optionItems - array of optional options or dividers
     * @param level - menu level
     * @param item - command, option or divider
     * @returns menu DOM element
     */
    createMenu(triggeredByElm, commandItems, optionItems, level = 0, item) {
      const columnDef = this.grid.getColumnByIdx(this._currentCell) ?? {};
      const dataContext = this.grid.getDataItem(this._currentRow) ?? {};
      const subMenuCommandOrOption = item?.command || item?.option;
      const subMenuId = subMenuCommandOrOption ? String(subMenuCommandOrOption).replace(/\s/g, "") : "";
      let isColumnOptionAllowed = true;
      let isColumnCommandAllowed = true;
      if (this._camelPluginName === "contextMenu") {
        isColumnOptionAllowed = this.checkIsColumnAllowed(this._addonOptions?.optionShownOverColumnIds ?? [], columnDef.id);
        isColumnCommandAllowed = this.checkIsColumnAllowed(this._addonOptions?.commandShownOverColumnIds ?? [], columnDef.id);
        if ((!isColumnCommandAllowed || !commandItems.length) && (!isColumnOptionAllowed || !optionItems.length)) {
          this.hideMenu();
          return;
        }
      } else {
        if (!columnDef.cellMenu || !commandItems.length && !optionItems.length) {
          return;
        }
      }
      const menuClasses = `${this.menuCssClass} slick-menu-level-${level} ${this.gridUid}`;
      const bodyMenuElm = document.body.querySelector(`.${this.menuCssClass}.slick-menu-level-${level}${this.gridUidSelector}`);
      if (bodyMenuElm) {
        if (bodyMenuElm.dataset.subMenuParent === subMenuId) {
          return bodyMenuElm;
        }
        this.disposeSubMenus();
      }
      const menuElm = document.createElement("div");
      menuElm.className = menuClasses;
      if (level > 0) {
        menuElm.classList.add("slick-submenu");
        if (this.gridOptions?.darkMode) {
          menuElm.classList.add("slick-dark-mode");
        }
        if (subMenuId) {
          menuElm.dataset.subMenuParent = subMenuId;
        }
      }
      const maxHeight = isNaN(this.addonOptions.maxHeight) ? this.addonOptions.maxHeight : `${this.addonOptions.maxHeight ?? 0}px`;
      const maxWidth = isNaN(this.addonOptions.maxWidth) ? this.addonOptions.maxWidth : `${this.addonOptions.maxWidth ?? 0}px`;
      if (maxHeight) {
        menuElm.style.maxHeight = maxHeight;
      }
      if (maxWidth) {
        menuElm.style.maxWidth = maxWidth;
      }
      if (this.addonOptions?.width) {
        menuElm.style.width = findWidthOrDefault(this.addonOptions?.width);
      }
      const closeButtonElm = createDomElement("button", {
        ariaLabel: "Close",
        className: "close",
        type: "button",
        textContent: "\xD7",
        dataset: { dismiss: this._menuCssPrefix }
      });
      if (!this.addonOptions.hideOptionSection && isColumnOptionAllowed && optionItems.length > 0) {
        const optionMenuElm = createDomElement("div", { className: `${this._menuCssPrefix}-option-list`, role: "menu" }, menuElm);
        this.populateCommandOrOptionTitle("option", this.addonOptions, optionMenuElm, level);
        if (!this.addonOptions.hideCloseButton && level < 1) {
          this.populateCommandOrOptionCloseBtn("option", closeButtonElm, optionMenuElm);
        }
        if (item && level > 0) {
          this.addSubMenuTitleWhenExists(item, optionMenuElm);
        }
        this.populateCommandOrOptionItems("option", this.addonOptions, optionMenuElm, optionItems, {
          cell: this._currentCell,
          row: this._currentRow,
          column: columnDef,
          dataContext,
          grid: this.grid,
          level
        }, triggeredByElm, this.handleMenuItemCommandClick, this.handleMenuItemMouseOver);
      }
      if (!this.addonOptions.hideCommandSection && isColumnCommandAllowed && commandItems.length > 0) {
        const commandMenuElm = createDomElement("div", { className: `${this._menuCssPrefix}-command-list`, role: "menu" }, menuElm);
        this.populateCommandOrOptionTitle("command", this.addonOptions, commandMenuElm, level);
        if (!this.addonOptions.hideCloseButton && level < 1 && (!isColumnOptionAllowed || optionItems.length === 0 || this.addonOptions.hideOptionSection)) {
          this.populateCommandOrOptionCloseBtn("command", closeButtonElm, commandMenuElm);
        }
        if (item && level > 0) {
          this.addSubMenuTitleWhenExists(item, commandMenuElm);
        }
        this.populateCommandOrOptionItems("command", this.addonOptions, commandMenuElm, commandItems, {
          cell: this._currentCell,
          row: this._currentRow,
          column: columnDef,
          dataContext,
          grid: this.grid,
          level
        }, triggeredByElm, this.handleMenuItemCommandClick, this.handleMenuItemMouseOver);
      }
      level++;
      return menuElm;
    }
    closeMenu(e2, args) {
      if (this.menuElement) {
        if (typeof this.addonOptions?.onBeforeMenuClose === "function" && this.addonOptions.onBeforeMenuClose(e2, args) === false) {
          return;
        }
        this.hideMenu();
      }
    }
    /** Hide the Menu */
    hideMenu() {
      this.menuElement?.remove();
      this._menuElm = null;
      this.disposeSubMenus();
    }
    // --
    // protected functions
    // ------------------
    checkIsColumnAllowed(columnIds, columnId) {
      if (columnIds?.length > 0) {
        return columnIds.findIndex((colId) => colId === columnId) >= 0;
      }
      return true;
    }
    /** Mouse down handler when clicking anywhere in the DOM body */
    handleBodyMouseDown(e2) {
      if (this.menuElement) {
        let isMenuClicked = false;
        const parentMenuElm = e2.target.closest(`.${this.menuCssClass}`);
        if (this.menuElement.contains(e2.target) || parentMenuElm) {
          isMenuClicked = true;
        }
        if (this.menuElement !== e2.target && !isMenuClicked && !e2.defaultPrevented || e2.target.className === "close" && parentMenuElm) {
          this.closeMenu(e2, { cell: this._currentCell, row: this._currentRow, grid: this.grid });
        }
      }
    }
    handleCloseButtonClicked(e2) {
      if (!e2.defaultPrevented) {
        this.closeMenu(e2, { cell: 0, row: 0, grid: this.grid });
      }
    }
    handleMenuItemMouseOver(e2, type, item, level = 0) {
      if (item?.[type] !== void 0 && item !== "divider" && !item.disabled && !item.divider) {
        if (item.commandItems || item.optionItems) {
          this.repositionSubMenu(item, type, level, e2);
          this._lastMenuTypeClicked = type;
        } else if (level === 0) {
          this.disposeSubMenus();
        }
      }
    }
    handleMenuItemCommandClick(event, type, item, level = 0) {
      if (item?.[type] !== void 0 && item !== "divider" && !item.disabled && !item.divider && this._currentCell !== void 0 && this._currentRow !== void 0) {
        if (type === "option" && !this.grid.getEditorLock().commitCurrentEdit()) {
          return;
        }
        const cell = this._currentCell;
        const row = this._currentRow;
        const columnDef = this.grid.getColumnByIdx(this._currentCell);
        const dataContext = this.grid.getDataItem(this._currentRow);
        const optionOrCommand = item[type] !== void 0 ? item[type] : "";
        if (optionOrCommand !== void 0 && !item[`${type}Items`]) {
          const callbackArgs = {
            cell: this._currentCell,
            row: this._currentRow,
            grid: this.grid,
            [type]: optionOrCommand,
            item,
            column: columnDef,
            dataContext
          };
          const eventType = type === "command" ? "onCommand" : "onOptionSelected";
          const eventName = `${this._camelPluginName}:${eventType}`;
          this.pubSubService.publish(eventName, callbackArgs);
          if (typeof this._addonOptions?.[eventType] === "function") {
            this._addonOptions[eventType](event, callbackArgs);
          }
          if (typeof item.action === "function") {
            item.action.call(this, event, callbackArgs);
          }
          if (!event.defaultPrevented) {
            this.closeMenu(event, { cell, row, grid: this.grid });
          }
        } else if (item.commandItems || item.optionItems) {
          this.repositionSubMenu(item, type, level, event);
        }
        this._lastMenuTypeClicked = type;
      }
    }
    populateCommandOrOptionCloseBtn(itemType, closeButtonElm, commandOrOptionMenuElm) {
      this._bindEventService.bind(closeButtonElm, "click", ((e2) => this.handleCloseButtonClicked(e2)), void 0, "parent-menu");
      const commandOrOptionMenuHeaderElm = commandOrOptionMenuElm.querySelector(`.slick-${itemType}-header`) ?? createDomElement("div", { className: `slick-${itemType}-header` });
      commandOrOptionMenuHeaderElm?.appendChild(closeButtonElm);
      commandOrOptionMenuElm.appendChild(commandOrOptionMenuHeaderElm);
      commandOrOptionMenuHeaderElm.classList.add("with-close");
    }
    repositionSubMenu(item, type, level, e2) {
      if (e2.target.classList.contains("slick-cell") || this._lastMenuTypeClicked !== type) {
        this.disposeSubMenus();
      }
      const subMenuElm = this.createMenu(e2.target, item?.commandItems || [], item?.optionItems || [], level + 1, item);
      if (subMenuElm) {
        subMenuElm.style.display = "block";
        document.body.appendChild(subMenuElm);
        this.repositionMenu(e2, subMenuElm, void 0, this._addonOptions);
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickCellMenu.js
  var SlickCellMenu = class extends MenuFromCellBaseClass {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(extensionUtility, pubSubService, sharedService) {
      super(extensionUtility, pubSubService, sharedService);
      this.extensionUtility = extensionUtility;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this.pluginName = "CellMenu";
      this._defaults = {
        autoAdjustDrop: true,
        // dropup/dropdown
        autoAlignSide: true,
        // left/right
        autoAdjustDropOffset: 0,
        autoAlignSideOffset: 0,
        hideMenuOnScroll: true,
        subMenuOpenByEvent: "mouseover"
      };
      this._camelPluginName = "cellMenu";
      this._menuCssPrefix = "slick-menu";
      this._menuPluginCssPrefix = "slick-cell-menu";
      this.init(sharedService.gridOptions.cellMenu);
    }
    /** Initialize plugin. */
    init(cellMenuOptions) {
      this._addonOptions = { ...this._defaults, ...cellMenuOptions };
      this.sortMenuItems(this.grid.getColumns());
      this._eventHandler.subscribe(this.grid.onClick, this.handleCellClick.bind(this));
      this._eventHandler.subscribe(this.grid.onKeyDown, this.handleCellKeyDown.bind(this));
      if (this._addonOptions.hideMenuOnScroll) {
        this._eventHandler.subscribe(this.grid.onScroll, this.closeMenu.bind(this));
      }
    }
    /** Translate the Cell Menu titles, we need to loop through all column definition to re-translate all list titles & all commands/options */
    translateCellMenu() {
      const gridOptions = this.sharedService?.gridOptions;
      const columns = this.grid.getColumns();
      if (gridOptions?.enableTranslate && Array.isArray(columns)) {
        columns.forEach((columnDef) => {
          if (columnDef?.cellMenu && (Array.isArray(columnDef.cellMenu.commandItems) || Array.isArray(columnDef.cellMenu.optionItems))) {
            const columnCellMenuCommandItems = columnDef.cellMenu.commandItems || [];
            const columnCellMenuOptionItems = columnDef.cellMenu.optionItems || [];
            if (columnDef.cellMenu.commandTitleKey) {
              columnDef.cellMenu.commandTitle = this.extensionUtility.translateWhenEnabledAndServiceExist(columnDef.cellMenu.commandTitleKey, "TEXT_COMMANDS") || columnDef.cellMenu.commandTitle;
            }
            if (columnDef.cellMenu.optionTitleKey) {
              columnDef.cellMenu.optionTitle = this.extensionUtility.translateWhenEnabledAndServiceExist(columnDef.cellMenu.optionTitleKey, "TEXT_OPTIONS") || columnDef.cellMenu.optionTitle;
            }
            this.extensionUtility.translateMenuItemsFromTitleKey(columnCellMenuCommandItems, "commandItems");
            this.extensionUtility.translateMenuItemsFromTitleKey(columnCellMenuOptionItems, "optionItems");
          }
        });
      }
    }
    // --
    // event handlers
    // ------------------
    handleCellClick(event, args) {
      this.disposeAllMenus();
      const dataContext = this.grid.getDataItem(args.row) ?? {};
      const columnDef = this.grid.getColumnByIdx(args.cell) ?? {};
      if (columnDef && dataContext) {
        if (columnDef.cellMenu && !this.gridOptions.cellMenu?.activateCellOnMenuClick) {
          event.preventDefault();
        }
        this._addonOptions = { ...this._addonOptions, ...columnDef.cellMenu };
        const menuArgs = args || {};
        menuArgs.column = columnDef;
        menuArgs.dataContext = dataContext;
        menuArgs.grid = this.grid;
        if (!this.extensionUtility.runOverrideFunctionWhenExists(this._addonOptions.menuUsabilityOverride, menuArgs)) {
          return;
        }
        this._menuElm = this.createParentMenu(event, args);
        if (this._menuElm) {
          this.repositionMenu(event, this._menuElm, void 0, this._addonOptions);
          this._menuElm.style.display = "block";
          if (this.gridOptions.darkMode) {
            this._menuElm.classList.add("slick-dark-mode");
          }
          this._bindEventService.bind(document.body, "mousedown", this.handleBodyMouseDown.bind(this));
        }
      }
    }
    /** Open the Cell Menu from root grid cell context on Enter key, sub-menu Enter key presses are handled separately and ignored here. */
    handleCellKeyDown(event, args) {
      const keyboardEvent = event.getNativeEvent();
      const columnDef = this.grid.getColumns()[args.cell];
      if (keyboardEvent?.key === "Enter" && columnDef?.cellMenu && !this.grid.getEditorLock()?.isActive?.()) {
        const parentCell = this.grid.getCellNode(args.row, args.cell)?.closest(".slick-cell");
        if (parentCell) {
          this.handleCellClick({
            ...keyboardEvent,
            preventDefault: keyboardEvent.preventDefault.bind(keyboardEvent),
            stopPropagation: keyboardEvent.stopPropagation.bind(keyboardEvent),
            defaultPrevented: keyboardEvent.defaultPrevented,
            target: parentCell
          }, args);
        }
      }
    }
    // --
    // protected functions
    // ------------------
    /** @deprecated Sort items (by pointers) in an array by a property name */
    sortMenuItems(columns) {
      columns.forEach((columnDef) => {
        if (columnDef?.cellMenu?.commandItems) {
          this.extensionUtility.sortItems(columnDef.cellMenu.commandItems || [], "positionOrder");
        }
        if (columnDef?.cellMenu?.optionItems) {
          this.extensionUtility.sortItems(columnDef.cellMenu.optionItems || [], "positionOrder");
        }
      });
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickCellRangeDecorator.js
  var SlickCellRangeDecorator = class {
    constructor(grid, options) {
      this.grid = grid;
      this.pluginName = "CellRangeDecorator";
      this._defaults = {
        selectionCssClass: "slick-range-decorator",
        selectionCss: {
          border: "2px dashed red",
          zIndex: "9999"
        },
        copyToSelectionCss: {
          border: "2px dashed blue",
          zIndex: "9999"
        },
        offset: { top: 0, left: 0, height: 1, width: 1 }
      };
      this._options = deepMerge(this._defaults, options);
      this._selectionCss = this._options?.selectionCss || {};
    }
    get addonOptions() {
      return this._options;
    }
    get addonElement() {
      return this._elem;
    }
    /** Dispose the plugin. */
    destroy() {
      this.hide();
    }
    init() {
    }
    getSelectionCss() {
      return this._selectionCss;
    }
    setSelectionCss(cssProps) {
      this._selectionCss = cssProps;
    }
    hide() {
      this._elem?.remove();
      this._elem = null;
    }
    show(range2, isCopyTo) {
      if (!this._elem) {
        this._elem = createDomElement("div", { className: this._options.selectionCssClass });
        this._elem.style.position = "absolute";
        this.grid.getActiveCanvasNode()?.appendChild(this._elem);
      }
      const css2 = isCopyTo && this._options.copyToSelectionCss ? this._options.copyToSelectionCss : this._selectionCss;
      setStyles(this._elem, css2);
      const from = this.grid.getCellNodeBox(range2.fromRow, range2.fromCell);
      const to = this.grid.getCellNodeBox(range2.toRow, range2.toCell);
      if (from && to && this._options?.offset) {
        this._elem.style.top = `${from.top + this._options.offset.top}px`;
        this._elem.style.left = `${from.left + this._options.offset.left}px`;
        this._elem.style.height = `${to.bottom - from.top + this._options.offset.height}px`;
        this._elem.style.width = `${to.right - from.left + this._options.offset.width}px`;
      }
      return this._elem;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickCellRangeSelector.js
  var SlickCellRangeSelector = class {
    constructor(options) {
      this.pluginName = "CellRangeSelector";
      this._currentlySelectedRange = null;
      this._previousSelectedRange = null;
      this._canvas = null;
      this._dragging = false;
      this._gridUid = "";
      this._dragReplaceHandleCell = null;
      this._selectionMode = "SEL";
      this._dragReplaceHandleActive = false;
      this._columnOffset = 0;
      this._rowOffset = 0;
      this._isRightCanvas = false;
      this._isBottomCanvas = false;
      this._xDelayForNextCell = 0;
      this._yDelayForNextCell = 0;
      this._viewportHeight = 0;
      this._viewportWidth = 0;
      this._isRowMoveRegistered = false;
      this._scrollLeft = 0;
      this._scrollTop = 0;
      this._defaults = {
        autoScroll: true,
        minIntervalToShowNextCell: 30,
        maxIntervalToShowNextCell: 600,
        // better to a multiple of minIntervalToShowNextCell
        accelerateInterval: 5,
        // increase 5ms when cursor 1px outside the viewport.
        selectionCss: {
          border: "2px dashed blue"
        }
      };
      this.onBeforeCellRangeSelected = new SlickEvent("onBeforeCellRangeSelected");
      this.onCellRangeSelecting = new SlickEvent("onCellRangeSelecting");
      this.onCellRangeSelected = new SlickEvent("onCellRangeSelected");
      this._eventHandler = new SlickEventHandler();
      this._options = deepMerge(this._defaults, options);
    }
    get addonOptions() {
      return this._options;
    }
    get eventHandler() {
      return this._eventHandler;
    }
    /** Getter for the grid uid */
    get gridUid() {
      return this._gridUid || (this._grid?.getUID() ?? "");
    }
    get gridUidSelector() {
      return this.gridUid ? `.${this.gridUid}` : "";
    }
    init(grid) {
      this._grid = grid;
      this._decorator = this._options.cellDecorator || new SlickCellRangeDecorator(grid, this._options);
      this._canvas = grid.getCanvasNode();
      this._gridOptions = grid.getOptions();
      this._gridUid = grid.getUID();
      const pubSub = grid.getPubSubService();
      if (pubSub) {
        Utils.addSlickEventPubSubWhenDefined(pubSub, this);
      }
      this._eventHandler.subscribe(this._grid.onDrag, this.handleDrag.bind(this)).subscribe(this._grid.onDragInit, this.handleDragInit.bind(this)).subscribe(this._grid.onDragStart, this.handleDragStart.bind(this)).subscribe(this._grid.onDragEnd, this.handleDragEnd.bind(this)).subscribe(this._grid.onScroll, this.handleScroll.bind(this));
    }
    destroy() {
      this.dispose();
    }
    /** Dispose the plugin. */
    dispose() {
      this._eventHandler?.unsubscribeAll();
      emptyElement(this._activeCanvas);
      emptyElement(this._canvas);
      this._decorator?.destroy();
      this.stopIntervalTimer();
    }
    getCellDecorator() {
      return this._decorator;
    }
    getCurrentRange() {
      return this._currentlySelectedRange;
    }
    getMouseOffsetViewport(e2, dd) {
      const targetEvent = e2?.touches?.[0] ?? e2;
      const viewportLeft = this._activeViewport.scrollLeft;
      const viewportTop = this._activeViewport.scrollTop;
      const viewportRight = viewportLeft + this._viewportWidth;
      const viewportBottom = viewportTop + this._viewportHeight;
      const viewportOffset = getOffset(this._activeViewport);
      const viewportOffsetLeft = viewportOffset.left;
      const viewportOffsetTop = viewportOffset.top;
      const viewportOffsetRight = viewportOffsetLeft + this._viewportWidth;
      const viewportOffsetBottom = viewportOffsetTop + this._viewportHeight;
      const result = {
        e: e2,
        dd,
        viewport: {
          left: viewportLeft,
          top: viewportTop,
          right: viewportRight,
          bottom: viewportBottom,
          offset: { left: viewportOffsetLeft, top: viewportOffsetTop, right: viewportOffsetRight, bottom: viewportOffsetBottom }
        },
        // Consider the viewport as the origin, the `offset` is based on the coordinate system:
        // the cursor is on the viewport's left/bottom when it is less than 0, and on the right/top when greater than 0.
        offset: { x: 0, y: 0 },
        isOutsideViewport: false
      };
      if (targetEvent.pageX < viewportOffsetLeft) {
        result.offset.x = targetEvent.pageX - viewportOffsetLeft;
      } else if (targetEvent.pageX > viewportOffsetRight) {
        result.offset.x = targetEvent.pageX - viewportOffsetRight;
      }
      if (targetEvent.pageY < viewportOffsetTop) {
        result.offset.y = viewportOffsetTop - targetEvent.pageY;
      } else if (targetEvent.pageY > viewportOffsetBottom) {
        result.offset.y = viewportOffsetBottom - targetEvent.pageY;
      }
      result.isOutsideViewport = !!result.offset.x || !!result.offset.y;
      return result;
    }
    stopIntervalTimer() {
      if (this._autoScrollTimerId) {
        clearInterval(this._autoScrollTimerId);
        this._autoScrollTimerId = void 0;
      }
    }
    //
    // protected functions
    // ---------------------
    handleDrag(evt, dd) {
      if (!this._dragging && !this._gridOptions.enableRowMoveManager) {
        return;
      }
      if (!this._gridOptions.enableRowMoveManager) {
        evt.stopImmediatePropagation();
      }
      const e2 = evt.getNativeEvent();
      if (this.addonOptions.autoScroll) {
        this._draggingMouseOffset = this.getMouseOffsetViewport(e2, dd);
        if (this._draggingMouseOffset.isOutsideViewport) {
          return this.handleDragOutsideViewport();
        }
      }
      this.stopIntervalTimer();
      this.handleDragTo(e2, dd);
    }
    handleDragOutsideViewport() {
      this._xDelayForNextCell = this.addonOptions.maxIntervalToShowNextCell - Math.abs(this._draggingMouseOffset.offset.x) * this.addonOptions.accelerateInterval;
      this._yDelayForNextCell = this.addonOptions.maxIntervalToShowNextCell - Math.abs(this._draggingMouseOffset.offset.y) * this.addonOptions.accelerateInterval;
      if (!this._autoScrollTimerId) {
        let xTotalDelay = 0;
        let yTotalDelay = 0;
        this._autoScrollTimerId = setInterval(() => {
          let xNeedUpdate = false;
          let yNeedUpdate = false;
          if (this._draggingMouseOffset.offset.x) {
            xTotalDelay += this.addonOptions.minIntervalToShowNextCell;
            xNeedUpdate = xTotalDelay >= this._xDelayForNextCell;
          } else {
            xTotalDelay = 0;
          }
          if (this._draggingMouseOffset.offset.y) {
            yTotalDelay += this.addonOptions.minIntervalToShowNextCell;
            yNeedUpdate = yTotalDelay >= this._yDelayForNextCell;
          } else {
            yTotalDelay = 0;
          }
          if (xNeedUpdate || yNeedUpdate) {
            if (xNeedUpdate) {
              xTotalDelay = 0;
            }
            if (yNeedUpdate) {
              yTotalDelay = 0;
            }
            this.handleDragToNewPosition(xNeedUpdate, yNeedUpdate);
          }
        }, this.addonOptions.minIntervalToShowNextCell);
      }
    }
    handleDragToNewPosition(xNeedUpdate, yNeedUpdate) {
      let pageX = this._draggingMouseOffset.e.pageX;
      let pageY = this._draggingMouseOffset.e.pageY;
      const mouseOffsetX = this._draggingMouseOffset.offset.x;
      const mouseOffsetY = this._draggingMouseOffset.offset.y;
      const viewportOffset = this._draggingMouseOffset.viewport.offset;
      if (xNeedUpdate && mouseOffsetX) {
        if (mouseOffsetX > 0) {
          pageX = viewportOffset.right + this._moveDistanceForOneCell.x;
        } else {
          pageX = viewportOffset.left - this._moveDistanceForOneCell.x;
        }
      }
      if (yNeedUpdate && mouseOffsetY) {
        if (mouseOffsetY > 0) {
          pageY = viewportOffset.top - this._moveDistanceForOneCell.y;
        } else {
          pageY = viewportOffset.bottom + this._moveDistanceForOneCell.y;
        }
      }
      this.handleDragTo({ pageX, pageY }, this._draggingMouseOffset.dd);
    }
    handleDragTo(e2, dd) {
      const targetEvent = e2?.touches?.[0] ?? e2;
      const end = this._grid.getCellFromPoint(targetEvent.pageX - getOffset(this._activeCanvas).left + this._columnOffset, targetEvent.pageY - getOffset(this._activeCanvas).top + this._rowOffset);
      if (end !== void 0) {
        if (this._gridOptions.frozenColumn >= 0 && (!this._isRightCanvas && end.cell > this._gridOptions.frozenColumn || this._isRightCanvas && end.cell <= this._gridOptions.frozenColumn)) {
          return;
        }
        if (this._gridOptions.frozenRow >= 0 && (!this._isBottomCanvas && end.row >= this._gridOptions.frozenRow || this._isBottomCanvas && end.row < this._gridOptions.frozenRow)) {
          return;
        }
        if (this.addonOptions.autoScroll && this._draggingMouseOffset) {
          const endCellBox = this._grid.getCellNodeBox(end.row, end.cell);
          if (endCellBox) {
            const viewport = this._draggingMouseOffset.viewport;
            if (endCellBox.left < viewport.left || endCellBox.right > viewport.right || endCellBox.top < viewport.top || endCellBox.bottom > viewport.bottom) {
              this._grid.scrollCellIntoView(end.row, end.cell);
            }
          }
        }
        if (!this._grid.canCellBeSelected(end.row, end.cell)) {
          return;
        }
        if (dd?.range) {
          dd.range.end = end;
          const cornerCell = !this._previousSelectedRange ? dd.range.start : SlickSelectionUtils.normalRangeOppositeCellFromCopy(this._previousSelectedRange, end);
          this._currentlySelectedRange = dd.range;
          const range2 = new SlickRange(cornerCell.row, cornerCell.cell, end.row, end.cell);
          this._decorator.show(range2, this._dragReplaceHandleActive);
          this.onCellRangeSelecting.notify({
            range: range2,
            selectionMode: "",
            allowAutoEdit: false
          });
        }
      }
    }
    handleDragEnd(e2, dd) {
      this._decorator.hide();
      if (!this._dragging || !dd.range) {
        if (this._autoScrollTimerId) {
          this.stopIntervalTimer();
        }
        return;
      }
      this._dragging = false;
      e2.stopImmediatePropagation();
      this.stopIntervalTimer();
      const targetEvent = e2?.touches?.[0] ?? e2;
      const canvasOffset = getOffset(this._activeCanvas);
      const end = this._grid.getCellFromPoint(targetEvent.pageX - (canvasOffset?.left ?? 0) + this._columnOffset, targetEvent.pageY - (canvasOffset?.top ?? 0) + this._rowOffset);
      const cornerCell = !this._dragReplaceHandleActive || !this._previousSelectedRange ? dd.range.start : SlickSelectionUtils.normalRangeOppositeCellFromCopy(this._previousSelectedRange, end);
      const r2 = new SlickRange(cornerCell.row ?? 0, cornerCell.cell ?? 0, dd.range.end.row, dd.range.end.cell);
      this.onCellRangeSelected.notify({
        range: r2,
        selectionMode: this._selectionMode,
        allowAutoEdit: this._selectionMode === "SEL" && r2.isSingleCell()
      });
      this._previousSelectedRange = SlickSelectionUtils.normaliseDragRange(dd.range);
    }
    handleDragInit(e2, dd) {
      this._activeCanvas = this._grid.getActiveCanvasNode(e2);
      this._activeViewport = this._grid.getActiveViewportNode(e2);
      const scrollbarDimensions = this._grid.getDisplayedScrollbarDimensions();
      this._viewportWidth = this._activeViewport.offsetWidth - scrollbarDimensions.width;
      this._viewportHeight = this._activeViewport.offsetHeight - scrollbarDimensions.height;
      this._moveDistanceForOneCell = {
        x: this._grid.getAbsoluteColumnMinWidth() / 2,
        y: this._gridOptions.rowHeight / 2
      };
      this._rowOffset = 0;
      this._columnOffset = 0;
      this._isBottomCanvas = this._activeCanvas.classList.contains("grid-canvas-bottom");
      if (this._gridOptions.frozenRow > -1 && this._isBottomCanvas) {
        const canvasSelector = `${this.gridUidSelector} .grid-canvas-${this._gridOptions.frozenBottom ? "bottom" : "top"}`;
        this._rowOffset = document.querySelector(canvasSelector)?.clientHeight ?? 0;
      }
      this._isRightCanvas = this._activeCanvas.classList.contains("grid-canvas-right");
      if (this._gridOptions.frozenColumn > -1 && this._isRightCanvas) {
        this._columnOffset = document.querySelector(`${this.gridUidSelector} .grid-canvas-left`)?.clientWidth ?? 0;
      }
      this._dragReplaceHandleActive = dd.matchClassTag === "dragReplaceHandle";
      if (this._dragReplaceHandleActive) {
        this._dragReplaceHandleCell = this._grid.getCellFromEvent(e2);
      } else {
        this._previousSelectedRange = null;
      }
      const cell = this._grid.getCellFromEvent(e2);
      const activeCell = this._grid.getActiveCell();
      if (!this._grid.getEditorLock().isActive() || !(activeCell && cell && activeCell.row === cell.row && activeCell.cell === cell.cell)) {
        e2.stopImmediatePropagation();
        e2.preventDefault();
      }
    }
    handleDragStart(e2, dd) {
      let cellObj = this._grid.getCellFromEvent(e2);
      if (this._dragReplaceHandleActive) {
        cellObj = this._dragReplaceHandleCell;
      }
      if (cellObj && this.onBeforeCellRangeSelected.notify(cellObj).getReturnValue() !== false && this._grid.canCellBeSelected(cellObj.row, cellObj.cell)) {
        this._dragging = true;
        e2.stopImmediatePropagation();
      }
      if (!this._dragging) {
        return;
      }
      this._grid.focus("internal");
      const canvasOffset = getOffset(this._canvas);
      let startX = dd.startX - canvasOffset.left;
      if (this._gridOptions.frozenColumn >= 0 && this._isRightCanvas) {
        startX += this._scrollLeft;
      }
      let startY = dd.startY - canvasOffset.top;
      if (this._gridOptions.frozenRow >= 0 && this._isBottomCanvas) {
        startY += this._scrollTop;
      }
      let start;
      this._selectionMode = this._dragReplaceHandleActive ? "REP" : "SEL";
      if (!this._dragReplaceHandleActive) {
        start = this._grid.getCellFromPoint(startX, startY);
      } else {
        start = this._grid.getActiveCell() || { row: void 0, cell: void 0 };
      }
      dd.range = { start, end: {} };
      this._currentlySelectedRange = dd.range;
      return this._decorator.show(new SlickRange(start.row ?? 0, start.cell ?? 0), this._dragReplaceHandleActive);
    }
    handleScroll(_e, args) {
      this._scrollTop = args.scrollTop;
      this._scrollLeft = args.scrollLeft;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickHybridSelectionModel.js
  var SlickHybridSelectionModel = class {
    constructor(options) {
      this.pluginName = "HybridSelectionModel";
      this._cachedPageRowCount = 0;
      this._prevKeyDown = "";
      this._ranges = [];
      this._activeSelectionIsRow = false;
      this._defaults = {
        selectActiveCell: true,
        selectActiveRow: true,
        dragToSelect: false,
        autoScrollWhenDrag: true,
        handleRowMoveManagerColumn: true,
        // Row Selection on RowMoveManager column
        rowSelectColumnIds: [],
        // Row Selection on these columns
        rowSelectOverride: void 0,
        // function to toggle Row Selection Models
        cellRangeSelector: void 0,
        selectionType: "mixed"
      };
      this.onSelectedRangesChanged = new SlickEvent("onSelectedRangesChanged");
      this._eventHandler = new SlickEventHandler();
      this._options = { ...this._defaults, ...options };
    }
    get eventHandler() {
      return this._eventHandler;
    }
    get gridOptions() {
      return this._grid?.getOptions();
    }
    set activeSelectionIsRow(value) {
      this._activeSelectionIsRow = value;
    }
    // Region: Setup
    // -----------------------------------------------------------------------------
    init(grid) {
      this._grid = grid;
      this._options = { ...this._defaults, ...this._options };
      this._selector = this._options.cellRangeSelector;
      if (this._options.selectionType === "cell") {
        this._activeSelectionIsRow = false;
      } else if (this._options.selectionType === "row") {
        this._activeSelectionIsRow = true;
      }
      const pubSub = grid.getPubSubService();
      if (pubSub) {
        this.onSelectedRangesChanged.setPubSubService(pubSub);
      }
      if (!this._selector && (!this._activeSelectionIsRow || this._activeSelectionIsRow && this._options.dragToSelect)) {
        this._selector = new SlickCellRangeSelector(this._options?.dragToSelect ? {
          selectionCss: { border: "none" },
          autoScroll: this._options?.autoScrollWhenDrag
        } : {
          selectionCss: { border: "2px solid gray" },
          copyToSelectionCss: { border: "2px solid purple" }
        });
        this._options.cellRangeSelector = this._selector;
      } else if (!this._selector && !this._options.dragToSelect) {
        this._eventHandler.subscribe(this._grid.onDragInit, (e2) => {
          e2.stopImmediatePropagation();
          e2.preventDefault();
        });
      }
      if (grid.hasDataView()) {
        this._dataView = grid.getData();
      }
      this._eventHandler.subscribe(this._grid.onActiveCellChanged, this.handleActiveCellChange.bind(this)).subscribe(this._grid.onClick, this.handleClick.bind(this)).subscribe(this._grid.onKeyDown, this.handleKeyDown.bind(this));
      if (this._selector) {
        this._grid.registerPlugin(this._selector);
        this._eventHandler.subscribe(this._selector.onCellRangeSelecting, (e2, args) => this.handleCellRangeSelected(e2, { ...args, caller: "onCellRangeSelecting" })).subscribe(this._selector.onCellRangeSelected, (e2, args) => this.handleCellRangeSelected(e2, { ...args, caller: "onCellRangeSelected" })).subscribe(this._selector.onBeforeCellRangeSelected, this.handleBeforeCellRangeSelected.bind(this));
      }
      this._grid.invalidate();
    }
    destroy() {
      this.dispose();
    }
    dispose() {
      this._eventHandler.unsubscribeAll();
      if (this._selector) {
        this._grid?.unregisterPlugin(this._selector);
      }
      this._selector?.destroy();
      this._selector?.dispose();
    }
    getOptions() {
      return this._options;
    }
    setOptions(options) {
      this._options = { ...this._options, ...options };
    }
    // Region: CellSelectionModel Members
    // -----------------------------------------------------------------------------
    removeInvalidRanges(ranges) {
      const result = [];
      for (let i = 0; i < ranges.length; i++) {
        const r2 = ranges[i];
        if (this._grid.canCellBeSelected(r2.fromRow, r2.fromCell) && this._grid.canCellBeSelected(r2.toRow, r2.toCell)) {
          result.push(r2);
        }
      }
      return result;
    }
    rangesAreEqual(range1, range2) {
      let areDifferent = range1.length !== range2.length;
      if (!areDifferent) {
        for (let i = 0; i < range1.length; i++) {
          if (range1[i].fromCell !== range2[i].fromCell || range1[i].fromRow !== range2[i].fromRow || range1[i].toCell !== range2[i].toCell || range1[i].toRow !== range2[i].toRow) {
            areDifferent = true;
            break;
          }
        }
      }
      return !areDifferent;
    }
    // Region: RowSelectionModel Members
    // -----------------------------------------------------------------------------
    rangesToRows(ranges) {
      const rows = [];
      for (let i = 0; i < ranges.length; i++) {
        for (let j2 = ranges[i].fromRow; j2 <= ranges[i].toRow; j2++) {
          rows.push(j2);
        }
      }
      return rows;
    }
    rowsToRanges(rows) {
      const ranges = [];
      const lastCell = this._grid.getColumns().length - 1;
      rows.forEach((row) => ranges.push(new SlickRange(row, 0, row, lastCell)));
      return ranges;
    }
    getRowsRange(from, to) {
      let i;
      const rows = [];
      for (i = from; i <= to; i++) {
        rows.push(i);
      }
      for (i = to; i < from; i++) {
        rows.push(i);
      }
      return rows;
    }
    getCellRangeSelector() {
      return this._selector;
    }
    getSelectedRanges() {
      return this._ranges;
    }
    getSelectedRows() {
      return this.rangesToRows(this._ranges);
    }
    setSelectedRows(rows) {
      this.setSelectedRanges(this.rowsToRanges(rows), "SlickHybridSelectionModel.setSelectedRows", "");
    }
    // Region: Shared Members
    // -----------------------------------------------------------------------------
    /** Provide a way to force a recalculation of page row count (for example on grid resize) */
    resetPageRowCount() {
      this._cachedPageRowCount = 0;
    }
    setSelectedRanges(ranges, caller = "SlickHybridSelectionModel.setSelectedRanges", selectionMode = "") {
      if ((!this._ranges || this._ranges.length === 0) && (!ranges || ranges.length === 0)) {
        return;
      }
      if (this._activeSelectionIsRow) {
        this._ranges = ranges;
        const eventData = new SlickEventData(new CustomEvent("click", { detail: { caller, selectionMode } }), this._ranges);
        this.onSelectedRangesChanged.notify(this._ranges, eventData);
      } else {
        const rangeHasChanged = !this.rangesAreEqual(this._ranges, ranges);
        this._ranges = this.removeInvalidRanges(ranges);
        if (rangeHasChanged) {
          const eventData = new SlickEventData(new CustomEvent("click", { detail: { caller, selectionMode, addDragHandle: true } }), this._ranges);
          this.onSelectedRangesChanged.notify(this._ranges, eventData);
        }
      }
    }
    currentSelectionModeIsRow() {
      return this._activeSelectionIsRow;
    }
    refreshSelections() {
      if (this._activeSelectionIsRow) {
        this.setSelectedRows(this.getSelectedRows());
      } else {
        this.setSelectedRanges(this.getSelectedRanges(), void 0, "");
      }
    }
    rowSelectionModelIsActive(data) {
      if (this._options.selectionType === "cell") {
        return false;
      } else if (this._options.selectionType === "row") {
        return true;
      }
      if (this._options?.rowSelectOverride) {
        return this._options?.rowSelectOverride(data, this, this._grid);
      }
      if (!isDefined(data.cell)) {
        return false;
      }
      if (this.gridOptions.enableRowMoveManager && this.isHandlerColumn(data.cell)) {
        return true;
      }
      const targetColumn = this._grid.getVisibleColumns()[data.cell];
      if (targetColumn) {
        return this._options?.rowSelectColumnIds?.includes(`${targetColumn.id}`) || false;
      }
      return false;
    }
    handleActiveCellChange(_e, args) {
      this._prevSelectedRow = void 0;
      const isCellDefined = isDefined(args.cell);
      const isRowDefined = isDefined(args.row);
      this._activeSelectionIsRow = this.rowSelectionModelIsActive(args);
      if (this._activeSelectionIsRow) {
        if (this._options?.selectActiveRow && isRowDefined) {
          this.setSelectedRanges([new SlickRange(args.row, 0, args.row, this._grid.getColumns().length - 1)], void 0, "");
        }
      } else {
        if (this._options?.selectActiveCell && isRowDefined && isCellDefined) {
          if (this._options.selectActiveRow) {
            this.setSelectedRanges([new SlickRange(args.row, args.cell)], void 0, "");
          }
        } else if (!this._options?.selectActiveCell || !isRowDefined && !isCellDefined) {
          this.setSelectedRanges([], void 0, "");
        }
      }
    }
    isKeyAllowed(key, isShiftKeyPressed) {
      return [
        "ArrowLeft",
        "ArrowRight",
        "ArrowUp",
        "ArrowDown",
        "PageDown",
        "PageUp",
        "Home",
        "End",
        ...!isShiftKeyPressed ? ["a", "A"] : []
      ].some((k2) => k2 === key);
    }
    handleKeyDown(e2) {
      if (!this._activeSelectionIsRow) {
        let ranges;
        let last;
        const colLn = this._grid.getColumns().length;
        const active = this._grid.getActiveCell();
        let dataLn = 0;
        if (this._dataView && "getPagingInfo" in this._dataView) {
          dataLn = this._dataView?.getPagingInfo().pageSize || this._dataView.getLength();
        } else {
          dataLn = this._grid.getDataLength();
        }
        if (active && (e2.shiftKey || e2.ctrlKey) && !e2.altKey && this.isKeyAllowed(e2.key, e2.shiftKey)) {
          ranges = this.getSelectedRanges().slice();
          if (!ranges.length) {
            ranges.push(new SlickRange(active.row, active.cell));
          }
          last = ranges.pop();
          if (typeof last?.contains === "function") {
            if (!last.contains(active.row, active.cell)) {
              last = new SlickRange(active.row, active.cell);
            }
            let dRow = last.toRow - last.fromRow;
            let dCell = last.toCell - last.fromCell;
            let toCell;
            let toRow = 0;
            if (e2.ctrlKey && e2.key?.toLowerCase() === "a") {
              this._grid.setActiveCell(0, 0, false, false, true);
              active.row = 0;
              active.cell = 0;
              toCell = colLn - 1;
              toRow = dataLn - 1;
            }
            const dirRow = active.row === last.fromRow ? 1 : -1;
            const dirCell = active.cell === last.fromCell ? 1 : -1;
            const isSingleKeyMove = e2.key.startsWith("Arrow");
            if (isSingleKeyMove && !e2.ctrlKey) {
              if (e2.key === "ArrowLeft") {
                dCell -= dirCell;
              } else if (e2.key === "ArrowRight") {
                dCell += dirCell;
              } else if (e2.key === "ArrowUp") {
                dRow -= dirRow;
              } else if (e2.key === "ArrowDown") {
                dRow += dirRow;
              }
              toRow = active.row + dirRow * dRow;
            } else {
              if (this._cachedPageRowCount < 1) {
                this._cachedPageRowCount = this._grid.getViewportRowCount();
              }
              if (this._prevSelectedRow === void 0) {
                this._prevSelectedRow = active.row;
              }
              if (!e2.ctrlKey && e2.shiftKey && e2.key === "Home" || e2.ctrlKey && e2.shiftKey && e2.key === "ArrowLeft") {
                toCell = 0;
                toRow = active.row;
              } else if (!e2.ctrlKey && e2.shiftKey && e2.key === "End" || e2.ctrlKey && e2.shiftKey && e2.key === "ArrowRight") {
                toCell = colLn - 1;
                toRow = active.row;
              } else if (e2.ctrlKey && e2.shiftKey && e2.key === "ArrowUp") {
                toRow = 0;
              } else if (e2.ctrlKey && e2.shiftKey && e2.key === "ArrowDown") {
                toRow = dataLn - 1;
              } else if (e2.ctrlKey && e2.shiftKey && e2.key === "Home") {
                toCell = 0;
                toRow = 0;
              } else if (e2.ctrlKey && e2.shiftKey && e2.key === "End") {
                toCell = colLn - 1;
                toRow = dataLn - 1;
              } else if (e2.key === "PageUp") {
                if (this._prevSelectedRow >= 0) {
                  toRow = this._prevSelectedRow - this._cachedPageRowCount;
                }
                if (toRow < 0) {
                  toRow = 0;
                }
              } else if (e2.key === "PageDown") {
                if (this._prevSelectedRow <= dataLn - 1) {
                  toRow = this._prevSelectedRow + this._cachedPageRowCount;
                }
                if (toRow > dataLn - 1) {
                  toRow = dataLn - 1;
                }
              }
              this._prevSelectedRow = toRow;
            }
            toCell ??= active.cell + dirCell * dCell;
            const newLast = new SlickRange(active.row, active.cell, toRow, toCell);
            if (this.removeInvalidRanges([newLast]).length) {
              ranges.push(newLast);
              const viewRow = dirRow > 0 ? newLast.toRow : newLast.fromRow;
              const viewCell = dirCell > 0 ? newLast.toCell : newLast.fromCell;
              if (isSingleKeyMove) {
                this._grid.scrollRowIntoView(viewRow);
                this._grid.scrollCellIntoView(viewRow, viewCell, false);
              } else {
                this._grid.scrollRowIntoView(toRow);
                this._grid.scrollCellIntoView(toRow, viewCell, false);
              }
            } else {
              ranges.push(last);
            }
            this.setSelectedRanges(ranges, void 0, "");
            e2.preventDefault();
            e2.stopPropagation();
            this._prevKeyDown = e2.key;
          }
        }
      } else {
        const activeRow = this._grid.getActiveCell();
        const isMultiSelect = this.gridOptions.multiSelect !== false;
        if (activeRow && e2.shiftKey && !e2.ctrlKey && !e2.altKey && !e2.metaKey && (e2.key === "ArrowUp" || e2.key === "ArrowDown")) {
          let selectedRows = this.getSelectedRows();
          selectedRows.sort((x2, y2) => x2 - y2);
          if (!selectedRows.length) {
            selectedRows = [activeRow.row];
          }
          let active;
          let top = selectedRows[0];
          let bottom = selectedRows[selectedRows.length - 1];
          if (e2.key === "ArrowDown") {
            active = activeRow.row < bottom || top === bottom ? ++bottom : ++top;
          } else {
            active = activeRow.row < bottom ? --bottom : --top;
          }
          if (active >= 0 && active < this._grid.getDataLength()) {
            this._grid.scrollRowIntoView(active);
            if (!isMultiSelect) {
              top = active;
              bottom = active;
            }
            const tempRanges = this.rowsToRanges(this.getRowsRange(top, bottom));
            this.setSelectedRanges(tempRanges);
          }
          e2.preventDefault();
          e2.stopPropagation();
        }
      }
    }
    handleClick(e2) {
      if (this._activeSelectionIsRow) {
        const cell = this._grid.getCellFromEvent(e2);
        if (!cell || !this._grid.canCellBeActive(cell.row, cell.cell)) {
          return false;
        }
        if (!this.gridOptions.multiSelect || !e2.ctrlKey && !e2.shiftKey && !e2.metaKey) {
          return false;
        }
        let selection = this.rangesToRows(this._ranges);
        const idx = selection.indexOf(cell.row);
        if (idx === -1 && (e2.ctrlKey || e2.metaKey)) {
          selection.push(cell.row);
          this._grid.setActiveCell(cell.row, cell.cell);
        } else if (idx !== -1 && (e2.ctrlKey || e2.metaKey)) {
          selection = selection.filter((o) => o !== cell.row);
          this._grid.setActiveCell(cell.row, cell.cell);
        } else if (selection.length && e2.shiftKey) {
          const last = selection.pop();
          const from = Math.min(cell.row, last);
          const to = Math.max(cell.row, last);
          selection = [];
          for (let i = from; i <= to; i++) {
            if (i !== last) {
              selection.push(i);
            }
          }
          selection.push(last);
          this._grid.setActiveCell(cell.row, cell.cell);
        }
        const tempRanges = this.rowsToRanges(selection);
        this.setSelectedRanges(tempRanges);
        e2.stopImmediatePropagation();
        return true;
      }
    }
    /** is the column a column Row Move OR Select Row Move */
    isHandlerColumn(columnIndex) {
      const columns = this._grid.getColumns();
      const col = columns[columnIndex].behavior || "";
      return /move|selectAndMove/.test(col);
    }
    handleBeforeCellRangeSelected(e2, cell) {
      if (this._activeSelectionIsRow) {
        let isRowMoveColumn = false;
        if (this.gridOptions.enableRowMoveManager) {
          isRowMoveColumn = this.isHandlerColumn(cell.cell) ?? false;
        }
        if (this._grid.getEditorLock().isActive() || isRowMoveColumn) {
          e2.stopPropagation();
          return false;
        }
        this._grid.setActiveCell(cell.row, cell.cell);
      } else {
        const cell2 = this._grid.getCellFromEvent(e2);
        const activeCell = this._grid.getActiveCell();
        if (this._grid.getEditorLock().isActive() && activeCell && cell2 && activeCell.row === cell2.row && activeCell.cell === cell2.cell) {
          e2.stopPropagation();
          return false;
        }
      }
    }
    handleCellRangeSelected(_e, args) {
      if (this._activeSelectionIsRow) {
        if (!this.gridOptions.multiSelect || !this._options?.selectActiveRow && !this._options.dragToSelect) {
          return false;
        }
        this.setSelectedRanges([new SlickRange(args.range.fromRow, 0, args.range.toRow, this._grid.getColumns().length - 1)], void 0, args.selectionMode);
      } else {
        if (args.caller === "onCellRangeSelecting") {
          return false;
        }
        this._grid.setActiveCell(args.range.fromRow, args.range.fromCell, args.allowAutoEdit ? void 0 : false, false, true);
        this.setSelectedRanges([args.range], void 0, args.selectionMode);
      }
      return true;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickCheckboxSelectColumn.js
  var CHECK_ICON = "sgi-icon-check";
  var UNCHECK_ICON = "sgi-icon-uncheck";
  var PARTIAL_CHECK_ICON = "sgi-icon-partial-check";
  var DEFAULT_COLUMN_ID = "_checkbox_selector";
  var SlickCheckboxSelectColumn = class {
    constructor(pubSubService, options) {
      this.pubSubService = pubSubService;
      this.pluginName = "CheckboxSelectColumn";
      this._defaults = {
        columnId: DEFAULT_COLUMN_ID,
        cssClass: null,
        field: DEFAULT_COLUMN_ID,
        hideSelectAllCheckbox: false,
        name: "",
        toolTip: "Select/Deselect All",
        width: 30,
        reorderable: false,
        applySelectOnAllPages: true,
        // when that is enabled the "Select All" will be applied to all pages (when using Pagination)
        hideInColumnTitleRow: false,
        hideInFilterHeaderRow: true
      };
      this._addonOptions = this._defaults;
      this._checkboxColumnCellIndex = null;
      this._isSelectAllChecked = false;
      this._isPartialSelectAllChecked = false;
      this._isUsingDataView = false;
      this._selectedRowsLookup = {};
      this._selectAll_UID = this.createUID();
      this._bindEventService = new BindingEventService();
      this._eventHandler = new SlickEventHandler();
      this._addonOptions = { ...this._defaults, ...options };
    }
    get addonOptions() {
      return this._addonOptions;
    }
    get headerRowNode() {
      return this._headerRowNode;
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    get selectAllUid() {
      return this._selectAll_UID;
    }
    set selectedRowsLookup(selectedRows) {
      this._selectedRowsLookup = selectedRows;
    }
    init(grid) {
      this._grid = grid;
      this._isUsingDataView = !Array.isArray(grid.getData());
      if (this._isUsingDataView) {
        this._dataView = grid.getData();
      }
      if (this.gridOptions.backendServiceApi) {
        this._addonOptions.applySelectOnAllPages = false;
      }
      this._eventHandler.subscribe(grid.onSelectedRowsChanged, this.handleSelectedRowsChanged.bind(this)).subscribe(grid.onClick, this.handleClick.bind(this)).subscribe(grid.onKeyDown, this.handleKeyDown.bind(this)).subscribe(grid.onAfterSetColumns, this.handleDataViewSelectedIdsChanged.bind(this)).subscribe(grid.onAfterUpdateColumns, this.handleDataViewSelectedIdsChanged.bind(this));
      if (this._isUsingDataView && this._dataView && this._addonOptions.applySelectOnAllPages) {
        this._eventHandler.subscribe(this._dataView.onSelectedRowIdsChanged, this.handleDataViewSelectedIdsChanged.bind(this)).subscribe(this._dataView.onPagingInfoChanged, this.handleDataViewSelectedIdsChanged.bind(this));
      }
      if (!this._addonOptions.hideInFilterHeaderRow) {
        this.addCheckboxToFilterHeaderRow(grid);
      }
      if (!this._addonOptions.hideInColumnTitleRow) {
        this._eventHandler.subscribe(this._grid.onHeaderClick, this.handleHeaderClick.bind(this)).subscribe(this._grid.onHeaderKeyDown, (_e, args) => this.handleHeaderKeyDown(args.event, args));
      }
      if (!this._rowSelectionModel || !this._grid.getSelectionModel()) {
        const selectionType = this.gridOptions.selectionOptions?.selectionType || "row";
        this._rowSelectionModel = grid.getSelectionModel() ?? new SlickHybridSelectionModel({ ...this.gridOptions.selectionOptions, selectionType });
        this._grid.setSelectionModel(this._rowSelectionModel);
      }
      if (this.gridOptions.preselectedRows && this._rowSelectionModel && this._grid.getSelectionModel()) {
        clearTimeout(this._timer);
        this._timer = setTimeout(() => this.selectRows(this.gridOptions.preselectedRows || []));
      }
      if (typeof this._addonOptions.selectableOverride === "function") {
        this.selectableOverride(this._addonOptions.selectableOverride);
      }
    }
    dispose() {
      clearTimeout(this._timer);
      this._bindEventService.unbindAll();
      this._eventHandler.unsubscribeAll();
    }
    /**
     * Create the plugin before the Grid creation to avoid having odd behaviors.
     * Mostly because the column definitions might change after the grid creation, so we want to make sure to add it before then
     */
    create(columns, gridOptions) {
      this._addonOptions = { ...this._defaults, ...gridOptions.checkboxSelector };
      if (Array.isArray(columns) && gridOptions) {
        const selectionColumn = this.getColumnDefinition();
        if (!columns.some((col) => col.id === selectionColumn.id)) {
          const columnPosition = gridOptions?.checkboxSelector?.columnIndexPosition ?? 0;
          if (columnPosition > 0) {
            columns.splice(columnPosition, 0, selectionColumn);
          } else {
            columns.unshift(selectionColumn);
          }
          this.pubSubService.publish("onPluginColumnsChanged", {
            columns,
            pluginName: this.pluginName
          });
        }
      }
      return this;
    }
    getOptions() {
      return this._addonOptions;
    }
    setOptions(options) {
      this._addonOptions = { ...this._addonOptions, ...options };
      if (this._addonOptions.hideSelectAllCheckbox) {
        this.hideSelectAllFromColumnHeaderTitleRow();
        this.hideSelectAllFromColumnHeaderFilterRow();
      } else {
        if (!this._addonOptions.hideInColumnTitleRow) {
          this.renderSelectAllCheckbox(this._isSelectAllChecked, this._isPartialSelectAllChecked);
          this._eventHandler.subscribe(this._grid.onHeaderClick, this.handleHeaderClick.bind(this));
        } else {
          this.hideSelectAllFromColumnHeaderTitleRow();
          if (this._addonOptions.name) {
            this._grid.updateColumnHeader(this._addonOptions.columnId || "", this._addonOptions.name, "");
          }
        }
        if (!this._addonOptions.hideInFilterHeaderRow) {
          const selectAllContainerElm = this.headerRowNode?.querySelector("#filter-checkbox-selectall-container");
          if (selectAllContainerElm) {
            selectAllContainerElm.style.display = "flex";
            selectAllContainerElm.ariaChecked = String(this._isSelectAllChecked);
            const selectAllInputElm = selectAllContainerElm.querySelector('input[type="checkbox"]');
            if (selectAllInputElm) {
              selectAllInputElm.ariaChecked = String(this._isSelectAllChecked);
              selectAllInputElm.checked = this._isSelectAllChecked;
            }
          }
        } else {
          this.hideSelectAllFromColumnHeaderFilterRow();
        }
      }
    }
    deSelectRows(rowArray) {
      const removeRows = [];
      for (const row of rowArray) {
        if (this._selectedRowsLookup[row]) {
          removeRows[removeRows.length] = row;
        }
      }
      this._grid.setSelectedRows(this._grid.getSelectedRows().filter((n) => removeRows.indexOf(n) < 0), "SlickCheckboxSelectColumn.deSelectRows");
    }
    selectRows(rowArray) {
      const addRows = [];
      for (const row of rowArray) {
        if (this._selectedRowsLookup[row]) {
          addRows[addRows.length] = row;
        }
      }
      const newSelectedRows = this._grid.getSelectedRows()?.concat(addRows);
      this._grid.setSelectedRows(newSelectedRows);
    }
    /**
     * use a DocumentFragment to return a fragment including an <input> then a <label> as siblings,
     * the label is using `for` to link it to the input `id`
     * @param {String} inputId - id to link the label
     * @param {Boolean} isChecked - is the input checkbox checked?
     * @param {Boolean} isPartialChecked - is the input checkbox checked?
     * @returns
     */
    createCheckboxElement(inputId, isChecked = false, isPartialChecked = false) {
      const checkboxElm = createDocumentFragmentOrElement(this.gridOptions);
      const labelElm = createDomElement("label", { className: "checkbox-selector-label selectall", htmlFor: inputId });
      const divElm = createDomElement("div", { className: "icon-checkbox-container" });
      divElm.classList[isChecked ? "add" : "remove"]("checked");
      divElm.appendChild(createDomElement("input", { id: inputId, type: "checkbox", checked: isChecked, ariaChecked: String(isChecked), tabIndex: -1 }));
      divElm.appendChild(createDomElement("div", { className: this.getCheckboxIcon(isChecked, isPartialChecked) }));
      labelElm.appendChild(divElm);
      checkboxElm.appendChild(labelElm);
      return checkboxElm;
    }
    getColumnDefinition() {
      const columnId = String(this._addonOptions?.columnId ?? this._defaults.columnId);
      return {
        id: columnId,
        name: this._addonOptions.hideSelectAllCheckbox || this._addonOptions.hideInColumnTitleRow ? this._addonOptions.name || "" : this.createCheckboxElement(`header-selector${this._selectAll_UID}`),
        toolTip: this._addonOptions.hideSelectAllCheckbox || this._addonOptions.hideInColumnTitleRow ? "" : this._addonOptions.toolTip,
        field: columnId,
        cssClass: this._addonOptions.cssClass,
        excludeFromExport: true,
        excludeFromColumnPicker: true,
        excludeFromGridMenu: true,
        excludeFromQuery: true,
        excludeFromHeaderMenu: true,
        hideSelectAllCheckbox: this._addonOptions.hideSelectAllCheckbox,
        resizable: false,
        reorderable: this._addonOptions.reorderable,
        sortable: false,
        width: this._addonOptions.width || 30,
        maxWidth: this._addonOptions.width || 30,
        formatter: this.checkboxSelectionFormatter.bind(this)
      };
    }
    hideSelectAllFromColumnHeaderTitleRow() {
      this._grid.updateColumnHeader(this._addonOptions.columnId || "", this._addonOptions.name || "", "");
    }
    hideSelectAllFromColumnHeaderFilterRow() {
      const selectAllContainerElm = this.headerRowNode?.querySelector("#filter-checkbox-selectall-container");
      if (selectAllContainerElm) {
        selectAllContainerElm.style.display = "none";
      }
    }
    /**
     * Toggle a row selection by providing a row number
     * @param {Number} row - grid row number to toggle
     */
    toggleRowSelection(row) {
      this.toggleRowSelectionWithEvent(null, row);
    }
    /**
     *  Toggle a row selection and also provide the event that triggered it
     * @param {Object} event - event that triggered the row selection change
     * @param {Number} row - grid row number to toggle
     * @returns
     */
    toggleRowSelectionWithEvent(event, row) {
      const dataContext = this._grid.getDataItem(row);
      if (!this.checkSelectableOverride(row, dataContext, this._grid)) {
        return;
      }
      const previousSelectedRows = this._grid.getSelectedRows();
      if (typeof this._addonOptions.onRowToggleStart === "function") {
        this._addonOptions.onRowToggleStart(event, { row, previousSelectedRows });
      }
      const isMultiSelect = this._grid.getOptions().multiSelect !== false;
      const newSelectedRows = isMultiSelect ? this._selectedRowsLookup[row] ? this._grid.getSelectedRows().filter((n) => n !== row) : this._grid.getSelectedRows().concat(row) : this._selectedRowsLookup[row] ? [] : [row];
      this._grid.setSelectedRows(newSelectedRows, "click.toggle");
      this._grid.setActiveCell(row, this.getCheckboxColumnCellIndex());
      if (this._addonOptions.onRowToggleEnd) {
        this._addonOptions.onRowToggleEnd(event, { row, previousSelectedRows });
      }
    }
    /**
     * Method that user can pass to override the default behavior or making every row a selectable row.
     * In order word, user can choose which rows to be selectable or not by providing his own logic.
     * @param overrideFn: override function callback
     */
    selectableOverride(overrideFn) {
      this._selectableOverride = overrideFn;
    }
    //
    // protected functions
    // ---------------------
    addCheckboxToFilterHeaderRow(grid) {
      this._eventHandler.subscribe(grid.onHeaderRowCellRendered, (_e, args) => {
        if (args.column.field === (this._addonOptions.field || DEFAULT_COLUMN_ID)) {
          emptyElement(args.node);
          const inputId = `header-filter-selector${this._selectAll_UID}`;
          const labelElm = createDomElement("label", { id: "filter-checkbox-selectall-container", htmlFor: inputId });
          const divElm = createDomElement("div", { className: "icon-checkbox-container", tabIndex: 0 });
          divElm.appendChild(createDomElement("input", { id: inputId, type: "checkbox", ariaChecked: "false", tabIndex: -1 }));
          divElm.appendChild(createDomElement("div", { className: `sgi ${UNCHECK_ICON}` }));
          labelElm.appendChild(divElm);
          args.node.appendChild(labelElm);
          this._headerRowNode = args.node;
          this._headerRowNode.classList.add("checkbox-header");
          this._bindEventService.bind(labelElm, "click", ((e2) => this.handleHeaderClick(e2, args)));
          this._bindEventService.bind(divElm, "keydown", ((e2) => this.handleHeaderKeyDown(e2, args)));
        }
      });
    }
    checkboxSelectionFormatter(row, _cell, _val, _columnDef, dataContext, grid) {
      if (dataContext && this.checkSelectableOverride(row, dataContext, grid)) {
        const UID = this.createUID() + row;
        return this.createCheckboxElement(`selector${UID}`, !!this._selectedRowsLookup[row]);
      }
      return null;
    }
    checkSelectableOverride(row, dataContext, grid) {
      if (typeof this._selectableOverride === "function") {
        return this._selectableOverride(row, dataContext, grid);
      }
      return true;
    }
    createUID() {
      return Math.round(1e7 * Math.random());
    }
    getCheckboxColumnCellIndex() {
      if (this._checkboxColumnCellIndex === null) {
        this._checkboxColumnCellIndex = 0;
        const colArr = this._grid.getColumns();
        for (let i = 0; i < colArr.length; i++) {
          if (colArr[i].id === this._addonOptions.columnId) {
            this._checkboxColumnCellIndex = i;
          }
        }
      }
      return this._checkboxColumnCellIndex;
    }
    getCheckboxIcon(isChecked, isPartialChecked) {
      const iconClass = isChecked ? CHECK_ICON : isPartialChecked && !this._addonOptions.hidePartialCheckbox ? PARTIAL_CHECK_ICON : UNCHECK_ICON;
      return `sgi ${iconClass}`;
    }
    handleDataViewSelectedIdsChanged() {
      const selectedIds = this._dataView.getAllSelectedFilteredIds();
      const filteredItems = this._dataView.getFilteredItems();
      let disabledCount = 0;
      if (typeof this._selectableOverride === "function" && selectedIds.length > 0) {
        for (let k2 = 0; k2 < this._dataView.getItemCount(); k2++) {
          const dataItem = this._dataView.getItemByIdx(k2);
          const idProperty = this._dataView.getIdPropertyName();
          const dataItemId = dataItem[idProperty];
          const foundItemIdx = filteredItems.findIndex((item) => item[idProperty] === dataItemId);
          if (foundItemIdx >= 0 && !this.checkSelectableOverride(k2, dataItem, this._grid)) {
            disabledCount++;
          }
        }
      }
      this._isSelectAllChecked = selectedIds.length + disabledCount >= filteredItems.length;
      this._isPartialSelectAllChecked = selectedIds.length > 0;
      if (!this._addonOptions.hideInColumnTitleRow && !this._addonOptions.hideSelectAllCheckbox) {
        this.renderSelectAllCheckbox(this._isSelectAllChecked, this._isPartialSelectAllChecked);
      }
      if (!this._addonOptions.hideInFilterHeaderRow) {
        const selectAllContainerElm = this.headerRowNode?.querySelector(".icon-checkbox-container");
        const selectAllElm = selectAllContainerElm?.querySelector(`#header-filter-selector${this._selectAll_UID}`);
        const selectAllIconElm = selectAllContainerElm?.querySelector(".sgi");
        if (selectAllContainerElm) {
          const actionFn = this._isSelectAllChecked ? "add" : "remove";
          selectAllContainerElm.classList[actionFn]("checked");
          if (selectAllElm) {
            selectAllElm.ariaChecked = String(this._isSelectAllChecked);
            selectAllElm.checked = this._isSelectAllChecked;
          }
          if (selectAllIconElm) {
            selectAllIconElm.className = this.getCheckboxIcon(this._isSelectAllChecked, this._isPartialSelectAllChecked);
          }
        }
      }
    }
    handleClick(e2, args) {
      const column = this._grid.getColumnByIdx(args.cell);
      if (column && column.id === this._addonOptions.columnId && e2.target.type === "checkbox") {
        e2.target.ariaChecked = String(e2.target.checked);
        if (this._grid.getEditorLock().isActive() && !this._grid.getEditorLock().commitCurrentEdit()) {
          e2.preventDefault();
          e2.stopImmediatePropagation();
          return;
        }
        this.toggleRowSelectionWithEvent(e2, args.row);
        e2.stopPropagation();
        e2.stopImmediatePropagation();
      }
    }
    handleHeaderClick(e2, args, forceToggle = false) {
      let inputChkElm = e2.target;
      if (e2.target instanceof HTMLDivElement) {
        inputChkElm = e2.target.querySelector("input[type=checkbox]");
        if (inputChkElm && forceToggle) {
          inputChkElm.checked = !inputChkElm.checked;
        }
      }
      if (inputChkElm && args.column.id === this._addonOptions.columnId && inputChkElm.type === "checkbox") {
        inputChkElm.ariaChecked = String(inputChkElm.checked);
        if (this._grid.getEditorLock().isActive() && !this._grid.getEditorLock().commitCurrentEdit()) {
          e2.preventDefault();
          e2.stopImmediatePropagation();
          return;
        }
        let isAllSelected = inputChkElm.checked;
        const caller = isAllSelected ? "click.selectAll" : "click.unselectAll";
        const previousSelectedRows = this._grid.getSelectedRows();
        if (this._addonOptions.onSelectAllToggleStart) {
          this._addonOptions.onSelectAllToggleStart(e2, { previousSelectedRows, caller });
        }
        let newSelectedRows = [];
        if (isAllSelected) {
          const rows = [];
          for (let i = 0; i < this._grid.getDataLength(); i++) {
            const rowItem = this._grid.getDataItem(i);
            if (!rowItem.__group && !rowItem.__groupTotals && this.checkSelectableOverride(i, rowItem, this._grid)) {
              rows.push(i);
            }
          }
          newSelectedRows = rows;
          isAllSelected = true;
        }
        if (this._isUsingDataView && this._dataView && this._addonOptions.applySelectOnAllPages) {
          const ids = [];
          const filteredItems = this._dataView.getFilteredItems();
          for (let j2 = 0; j2 < filteredItems.length; j2++) {
            const dataviewRowItem = filteredItems[j2];
            if (this.checkSelectableOverride(j2, dataviewRowItem, this._grid)) {
              ids.push(dataviewRowItem[this._dataView.getIdPropertyName()]);
            }
          }
          this._dataView.setSelectedIds(ids, { isRowBeingAdded: isAllSelected });
        }
        this._grid.setSelectedRows(newSelectedRows, caller);
        this._isPartialSelectAllChecked = newSelectedRows.length > 0;
        if (this._addonOptions.onSelectAllToggleEnd) {
          this._addonOptions.onSelectAllToggleEnd(e2, { rows: newSelectedRows, previousSelectedRows, caller });
        }
        e2.stopPropagation();
        e2.stopImmediatePropagation();
      }
    }
    handleKeyDown(e2, args) {
      const column = this._grid.getColumnByIdx(args.cell);
      if (e2.key === " " && column && column.id === this._addonOptions.columnId) {
        if (!this._grid.getEditorLock().isActive() || this._grid.getEditorLock().commitCurrentEdit()) {
          this.toggleRowSelectionWithEvent(e2, args.row);
        }
        e2.preventDefault();
        e2.stopImmediatePropagation();
      }
    }
    handleHeaderKeyDown(e2, args) {
      if (e2.key === "Enter" || e2.key === " ") {
        this.handleHeaderClick(e2, args, true);
      }
    }
    handleSelectedRowsChanged() {
      const selectedRows = this._grid.getSelectedRows();
      const lookup = {};
      let row = 0;
      let i = 0;
      let k2 = 0;
      let disabledCount = 0;
      if (typeof this._selectableOverride === "function") {
        for (k2 = 0; k2 < this._grid.getDataLength(); k2++) {
          const dataItem = this._grid.getDataItem(k2);
          if (!this.checkSelectableOverride(i, dataItem, this._grid)) {
            disabledCount++;
          }
        }
      }
      const removeList = [];
      for (i = 0; i < selectedRows.length; i++) {
        row = selectedRows[i];
        const rowItem = this._grid.getDataItem(row);
        if (this.checkSelectableOverride(i, rowItem, this._grid)) {
          lookup[row] = true;
          if (lookup[row] !== this._selectedRowsLookup[row]) {
            this._grid.invalidateRow(row);
            delete this._selectedRowsLookup[row];
          }
        } else {
          removeList.push(row);
        }
      }
      if (typeof this._selectedRowsLookup === "object") {
        Object.keys(this._selectedRowsLookup).forEach((selectedRow) => {
          if (selectedRow !== void 0) {
            this._grid.invalidateRow(+selectedRow);
          }
        });
      }
      this._selectedRowsLookup = lookup;
      this._grid.render();
      this._isSelectAllChecked = (selectedRows?.length ?? 0) + disabledCount >= this._grid.getDataLength();
      this._isPartialSelectAllChecked = (selectedRows?.length ?? 0) > 0;
      if (!this._isUsingDataView || !this._addonOptions.applySelectOnAllPages) {
        if (!this._addonOptions.hideInColumnTitleRow && !this._addonOptions.hideSelectAllCheckbox) {
          this.renderSelectAllCheckbox(this._isSelectAllChecked, this._isPartialSelectAllChecked);
        }
        if (!this._addonOptions.hideInFilterHeaderRow) {
          const selectAllElm = this.headerRowNode?.querySelector(`#header-filter-selector${this._selectAll_UID}`);
          if (selectAllElm) {
            selectAllElm.ariaChecked = String(this._isSelectAllChecked);
            selectAllElm.checked = this._isSelectAllChecked;
          }
        }
      }
      if (removeList.length > 0) {
        for (const itemToRemove of removeList) {
          const remIdx = selectedRows.indexOf(itemToRemove);
          selectedRows.splice(remIdx, 1);
        }
        this._grid.setSelectedRows(selectedRows, "click.toggle");
      }
    }
    renderSelectAllCheckbox(isSelectAllChecked, isPartialSelectAllChecked) {
      const colHeaderElm = this._grid.updateColumnHeader(this._addonOptions.columnId || "", this.createCheckboxElement(`header-selector${this._selectAll_UID}`, !!isSelectAllChecked, !!isPartialSelectAllChecked), this._addonOptions.toolTip);
      colHeaderElm?.classList.add("header-checkbox-selectall");
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickColumnPicker.js
  var SlickColumnPicker = class extends MenuBaseClass {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(extensionUtility, pubSubService, sharedService) {
      super(extensionUtility, pubSubService, sharedService);
      this._defaults = {
        // the last 2 checkboxes titles
        hideForceFitButton: false,
        hideSyncResizeButton: false,
        forceFitTitle: "Force fit columns",
        minHeight: 150,
        syncResizeTitle: "Synchronous resize",
        headerColumnValueExtractor: (columnDef) => {
          return getHtmlStringOutput(columnDef.columnPickerLabel || columnDef.name || "", "innerHTML");
        }
      };
      this._camelPluginName = "columnPicker";
      this._menuPluginCssPrefix = "slick-column-picker";
      this.onColumnsChanged = new SlickEvent("onColumnsChanged");
      this._columns = this.grid?.getColumns() ?? [];
      this._gridUid = this.grid?.getUID() ?? "";
      this.init();
    }
    get addonOptions() {
      return this.gridOptions.columnPicker || {};
    }
    get columns() {
      return this._columns;
    }
    set columns(newColumns) {
      this._columns = newColumns;
    }
    get gridOptions() {
      return this.sharedService.gridOptions ?? {};
    }
    get grid() {
      return this.sharedService.slickGrid;
    }
    get menuElement() {
      return this._menuElm || null;
    }
    /** Initialize plugin. */
    init() {
      this._gridUid = this.grid.getUID() ?? "";
      this._originalPickerOptions = extend(true, {}, this.sharedService.gridOptions.columnPicker);
      this.gridOptions.columnPicker = { ...this._defaults, ...this.gridOptions.columnPicker };
      this.onColumnsChanged.setPubSubService(this.pubSubService);
      this.translateColumnPickerTitles();
      this._eventHandler.subscribe(this.grid.onPreHeaderContextMenu, (e2) => {
        if (["slick-column-name", "slick-header-column"].some((className) => e2.target?.classList.contains(className))) {
          this.handleHeaderContextMenu(e2);
        }
      });
      this._eventHandler.subscribe(this.grid.onHeaderContextMenu, this.handleHeaderContextMenu.bind(this));
      this._eventHandler.subscribe(this.grid.onColumnsReordered, this.updateColumnPickerOrder.bind(this));
      this._eventHandler.subscribe(this.grid.onClick, this.disposeMenu.bind(this));
      this._bindEventService.bind(document.body, "mousedown", this.handleBodyMouseDown.bind(this), void 0, "body");
      this._bindEventService.bind(document.body, "beforeunload", this.dispose.bind(this), void 0, "body");
    }
    /** Dispose (destroy) the SlickGrid 3rd party plugin */
    dispose() {
      this._eventHandler.unsubscribeAll();
      this._bindEventService.unbindAll();
      this.disposeMenu();
    }
    disposeMenu() {
      this._listElm?.remove();
      this._menuElm?.remove();
      this._menuElm = null;
    }
    createPickerMenu() {
      const menuElm = createDomElement("div", {
        className: `slick-column-picker ${this._gridUid}`,
        role: "menu"
      });
      this.updateColumnPickerOrder();
      this.addColumnTitleElementWhenDefined(menuElm);
      this.addCloseButtomElement(menuElm);
      this._listElm = createDomElement("div", { className: "slick-column-picker-list", role: "menu" });
      this._bindEventService.bind(menuElm, "click", this.handleColumnPickerItemClick.bind(this), void 0, "parent-menu");
      document.body.appendChild(menuElm);
      return menuElm;
    }
    /**
     * Get all columns including hidden columns.
     * @returns {Array<Object>} - all columns array
     */
    getAllColumns() {
      return this._columns;
    }
    /**
     * Get only the visible columns.
     * @returns {Array<Object>} - all columns array
     */
    getVisibleColumns() {
      return this.grid.getVisibleColumns();
    }
    /** Translate the Column Picker headers and also the last 2 checkboxes */
    translateColumnPicker() {
      this.translateColumnPickerTitles();
      this.extensionUtility.translateItems(this._columns, "nameKey", "name");
    }
    // update the properties by pointers, that is the only way to get Column Picker Control to see the new values
    translateColumnPickerTitles() {
      if (this.addonOptions) {
        this.addonOptions.columnTitle = this._originalPickerOptions.columnTitle || "";
        this.addonOptions.forceFitTitle = this._originalPickerOptions.forceFitTitle || "";
        this.addonOptions.syncResizeTitle = this._originalPickerOptions.syncResizeTitle || "";
        this.addonOptions.columnTitle = this.extensionUtility.getPickerTitleOutputString("columnTitle", "columnPicker");
        this.addonOptions.forceFitTitle = this.extensionUtility.getPickerTitleOutputString("forceFitTitle", "columnPicker");
        this.addonOptions.syncResizeTitle = this.extensionUtility.getPickerTitleOutputString("syncResizeTitle", "columnPicker");
      }
    }
    // --
    // protected functions
    // ------------------
    /** Mouse down handler when clicking anywhere in the DOM body */
    handleBodyMouseDown(e2) {
      if (this._menuElm !== e2.target && !this._menuElm?.contains(e2.target) || e2.target.className === "close" && e2.target.closest(".slick-column-picker")) {
        this.disposeMenu();
      }
    }
    /** Mouse header context handler when doing a right+click on any of the header column title */
    handleHeaderContextMenu(e2) {
      e2.preventDefault();
      emptyElement(this._menuElm);
      this._columnCheckboxes = [];
      this._menuElm = this.createPickerMenu();
      if (this.gridOptions.darkMode) {
        this._menuElm.classList.add("slick-dark-mode");
      }
      this.populateColumnPicker(this.addonOptions, this._defaults.headerColumnValueExtractor);
      document.body.appendChild(this._menuElm);
      this.repositionMenu(e2);
      this.focusFirstMenuItem(this._menuElm);
      wireMenuKeyboardNavigation(this._menuElm, this._bindEventService, {
        allItemsSelector: ".slick-column-picker-list li:not(.hidden)",
        focusedItemSelector: ".slick-column-picker-list li:not(.hidden)",
        onActivate: (focusedItem) => {
          if (focusedItem) {
            const iconContainer = focusedItem.querySelector(".icon-checkbox-container");
            if (iconContainer && typeof iconContainer.click === "function") {
              iconContainer.click();
            }
          }
        },
        onEscape: () => {
          this.disposeMenu();
          this.grid.focus("header");
        },
        onTab: (evt) => {
          evt.preventDefault();
          evt.stopPropagation();
        },
        eventServiceKey: "column-picker-keyboard"
      });
    }
    focusFirstMenuItem(menuElm) {
      const menuItems = Array.from(menuElm.querySelectorAll(".slick-column-picker-list li:not(.hidden)"));
      const firstMenuItem = menuItems.find((item) => item.offsetParent !== null);
      firstMenuItem?.focus();
    }
    repositionMenu(event) {
      const targetEvent = event?.touches?.[0] ?? event;
      if (this._menuElm) {
        const gridPos = this.grid.getGridPosition();
        const menuWidth = this._menuElm.clientWidth || 0;
        let menuOffsetLeft = targetEvent.pageX || 0;
        if (gridPos?.width && menuOffsetLeft + menuWidth >= gridPos.width) {
          menuOffsetLeft = menuOffsetLeft - menuWidth;
        }
        this._menuElm.style.top = `${targetEvent.pageY - 10}px`;
        this._menuElm.style.left = `${menuOffsetLeft}px`;
        this._menuElm.style.minHeight = findWidthOrDefault(this.addonOptions.minHeight, "");
        this._menuElm.style.maxHeight = findWidthOrDefault(this.addonOptions.maxHeight, `${window.innerHeight - targetEvent.clientY}px`);
        this._menuElm.style.display = "block";
        this._menuElm.appendChild(this._listElm);
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickContextMenu.js
  var SlickContextMenu = class extends MenuFromCellBaseClass {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(extensionUtility, pubSubService, sharedService, treeDataService) {
      super(extensionUtility, pubSubService, sharedService);
      this.extensionUtility = extensionUtility;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this.treeDataService = treeDataService;
      this._defaults = {
        autoAdjustDrop: true,
        // dropup/dropdown
        autoAlignSide: true,
        // left/right
        autoAdjustDropOffset: 0,
        autoAlignSideOffset: 0,
        hideMenuOnScroll: false,
        optionShownOverColumnIds: [],
        commandShownOverColumnIds: [],
        subMenuOpenByEvent: "mouseover"
      };
      this.pluginName = "ContextMenu";
      this._camelPluginName = "contextMenu";
      this._menuCssPrefix = "slick-menu";
      this._menuPluginCssPrefix = "slick-context-menu";
      this.init(sharedService.gridOptions.contextMenu);
    }
    /** Initialize plugin. */
    init(contextMenuOptions) {
      this._originalContextMenu = extend(true, {}, this.sharedService.gridOptions.contextMenu);
      this._addonOptions = { ...this._defaults, ...contextMenuOptions };
      const originalCommandItems = Array.isArray(this._addonOptions?.commandItems) ? this._addonOptions.commandItems : [];
      const initialCommandItems = [...originalCommandItems, ...this.addMenuCustomCommands(originalCommandItems)];
      this._addonOptions.commandItems = this._addonOptions.commandListBuilder?.(initialCommandItems) ?? initialCommandItems;
      this._addonOptions = { ...this._addonOptions };
      this.sharedService.gridOptions.contextMenu = this._addonOptions;
      this.sortMenuItems();
      this._eventHandler.subscribe(this.grid.onContextMenu, this.handleOnContextMenu.bind(this));
      this._eventHandler.subscribe(this.grid.onClick, this.hideMenu.bind(this));
      if (this._addonOptions.hideMenuOnScroll) {
        this._eventHandler.subscribe(this.grid.onScroll, this.closeMenu.bind(this));
      }
    }
    /** Translate the Context Menu titles, we need to loop through all column definition to re-translate all list titles & all commands/options */
    translateContextMenu() {
      const gridOptions = this.sharedService?.gridOptions ?? {};
      const contextMenu = this.sharedService.gridOptions.contextMenu;
      if (contextMenu) {
        contextMenu.commandTitle = this._originalContextMenu.commandTitle || "";
        contextMenu.optionTitle = this._originalContextMenu.optionTitle || "";
      }
      if (contextMenu && gridOptions?.enableTranslate) {
        const columnContextMenuCommandItems = contextMenu.commandItems || [];
        const columnContextMenuOptionItems = contextMenu.optionItems || [];
        if (contextMenu.commandTitleKey) {
          contextMenu.commandTitle ||= this.extensionUtility.translateWhenEnabledAndServiceExist(contextMenu.commandTitleKey, "TEXT_COMMANDS") || contextMenu.commandTitle;
        }
        if (contextMenu.optionTitleKey) {
          contextMenu.optionTitle ||= this.extensionUtility.translateWhenEnabledAndServiceExist(contextMenu.optionTitleKey, "TEXT_OPTIONS") || contextMenu.optionTitle;
        }
        this.extensionUtility.translateMenuItemsFromTitleKey(columnContextMenuCommandItems, "commandItems");
        this.extensionUtility.translateMenuItemsFromTitleKey(columnContextMenuOptionItems, "optionItems");
      }
    }
    // --
    // event handlers
    // ------------------
    handleOnContextMenu(event, args) {
      this.disposeAllMenus();
      const menuArgs = args || {};
      menuArgs.cell = args.cell;
      menuArgs.row = args.row;
      menuArgs.column = this.grid.getColumnByIdx(args.cell) ?? {};
      menuArgs.dataContext = this.grid.getDataItem(args.row) ?? {};
      menuArgs.grid = this.grid;
      if (!this.extensionUtility.runOverrideFunctionWhenExists(this._addonOptions.menuUsabilityOverride, menuArgs)) {
        return;
      }
      this._menuElm = this.createParentMenu(event, args);
      if (this._menuElm) {
        event.preventDefault();
        if (this.gridOptions.darkMode) {
          this._menuElm.classList.add("slick-dark-mode");
        }
        this.repositionMenu(event, this._menuElm, void 0, this._addonOptions);
        this._menuElm.style.display = "block";
        this._bindEventService.bind(document.body, "mousedown", this.handleBodyMouseDown.bind(this));
      }
    }
    // --
    // protected functions
    // ------------------
    /** Create Context Menu with Custom Commands (copy cell value, export) */
    addMenuCustomCommands(originalCommandItems) {
      const menuCommandItems = [];
      const gridOptions = this.sharedService && this.sharedService.gridOptions || {};
      const contextMenu = gridOptions?.contextMenu;
      const commandLabels = this._addonOptions?.commandLabels;
      const dataView = this.sharedService?.dataView;
      const translationPrefix = getTranslationPrefix(gridOptions);
      if (contextMenu && !contextMenu.hideCopyCellValueCommand) {
        this.addMissingCommandOrAction({
          _orgTitle: commandLabels?.copyCellValueCommand || "",
          iconCssClass: contextMenu.iconCopyCellValueCommand || "mdi mdi-content-copy",
          titleKey: `${translationPrefix}COPY`,
          disabled: false,
          command: "copy",
          positionOrder: 50,
          action: (_e, args) => copyCellToClipboard(args),
          itemUsabilityOverride: (args) => isDefined(getCopyCellValue(args))
        }, contextMenu.hideCommands, menuCommandItems, originalCommandItems);
      }
      if (gridOptions?.enableTextExport && contextMenu && !contextMenu.hideExportCsvCommand) {
        this.addMissingCommandOrAction({
          _orgTitle: commandLabels?.exportCsvCommand || "",
          iconCssClass: contextMenu.iconExportCsvCommand || "mdi mdi-file-document-outline",
          titleKey: `${translationPrefix}EXPORT_TO_CSV`,
          disabled: false,
          command: "export-csv",
          positionOrder: 51,
          action: () => {
            const registedServices = this.sharedService?.externalRegisteredResources || [];
            const excelService = registedServices.find((service) => service.pluginName === "TextExportService");
            if (excelService?.exportToFile) {
              excelService.exportToFile({ delimiter: ",", format: "csv" });
            } else {
              throw new Error(`[Slickgrid-Universal] You must register the TextExportService to properly use Export to File in the Context Menu. Example:: this.gridOptions = { enableTextExport: true, externalResources: [new TextExportService()] };`);
            }
          }
        }, contextMenu.hideCommands, menuCommandItems, originalCommandItems);
      }
      if (gridOptions && gridOptions.enableExcelExport && contextMenu && !contextMenu.hideExportExcelCommand) {
        this.addMissingCommandOrAction({
          _orgTitle: commandLabels?.exportExcelCommand || "",
          iconCssClass: contextMenu.iconExportExcelCommand || "mdi mdi-file-excel-outline text-success",
          titleKey: `${translationPrefix}EXPORT_TO_EXCEL`,
          disabled: false,
          command: "export-excel",
          positionOrder: 52,
          action: () => {
            const registedServices = this.sharedService?.externalRegisteredResources || [];
            const excelService = registedServices.find((service) => service.pluginName === "ExcelExportService");
            if (excelService?.exportToExcel) {
              excelService.exportToExcel();
            } else {
              throw new Error(`[Slickgrid-Universal] You must register the ExcelExportService to properly use Export to Excel in the Context Menu. Example:: this.gridOptions = { enableExcelExport: true, externalResources: [new ExcelExportService()] };`);
            }
          }
        }, contextMenu.hideCommands, menuCommandItems, originalCommandItems);
      }
      if (gridOptions && gridOptions.enablePdfExport && contextMenu && !contextMenu.hideExportPdfCommand) {
        this.addMissingCommandOrAction({
          _orgTitle: commandLabels?.exportPdfCommand || "",
          iconCssClass: contextMenu.iconExportPdfCommand || "mdi mdi-file-pdf-outline text-danger",
          titleKey: `${translationPrefix}EXPORT_TO_PDF`,
          disabled: false,
          command: "export-pdf",
          positionOrder: 53,
          action: () => {
            const registedServices = this.sharedService?.externalRegisteredResources || [];
            const pdfService = registedServices.find((service) => service.pluginName === "PdfExportService");
            if (pdfService?.exportToPdf) {
              pdfService.exportToPdf();
            } else {
              throw new Error(`[Slickgrid-Universal] You must register the PdfExportService to properly use Export to PDF in the Context Menu. Example:: this.gridOptions = { enablePdfExport: true, externalResources: [new PdfExportService()] };`);
            }
          }
        }, contextMenu.hideCommands, menuCommandItems, originalCommandItems);
      }
      if (gridOptions?.enableTextExport && contextMenu && !contextMenu.hideExportTextDelimitedCommand) {
        this.addMissingCommandOrAction({
          _orgTitle: commandLabels?.exportTextDelimitedCommand || "",
          iconCssClass: contextMenu.iconExportTextDelimitedCommand || "mdi mdi-file-document-outline",
          titleKey: `${translationPrefix}EXPORT_TO_TAB_DELIMITED`,
          disabled: false,
          command: "export-text-delimited",
          positionOrder: 54,
          action: () => {
            const registedServices = this.sharedService?.externalRegisteredResources || [];
            const excelService = registedServices.find((service) => service.pluginName === "TextExportService");
            if (excelService?.exportToFile) {
              excelService.exportToFile({ delimiter: "	", format: "txt" });
            } else {
              throw new Error(`[Slickgrid-Universal] You must register the TextExportService to properly use Export to File in the Context Menu. Example:: this.gridOptions = { enableTextExport: true, externalResources: [new TextExportService()] };`);
            }
          }
        }, contextMenu.hideCommands, menuCommandItems, originalCommandItems);
      }
      if (gridOptions && (gridOptions.enableGrouping || gridOptions.enableDraggableGrouping || gridOptions.enableTreeData)) {
        if (contextMenu && !contextMenu.hideCopyCellValueCommand && !contextMenu.hideCommands?.includes("copy")) {
          menuCommandItems.push({ divider: true, command: "divider-1", positionOrder: 54 });
        }
        if (gridOptions && !gridOptions.enableTreeData && contextMenu && !contextMenu.hideClearAllGrouping) {
          this.addMissingCommandOrAction({
            _orgTitle: commandLabels?.clearGroupingCommand || "",
            iconCssClass: contextMenu.iconClearGroupingCommand || "mdi mdi-close",
            titleKey: `${translationPrefix}CLEAR_ALL_GROUPING`,
            disabled: false,
            command: "clear-grouping",
            positionOrder: 56,
            action: () => {
              dataView.setGrouping([]);
              this.pubSubService.publish("onContextMenuClearGrouping");
            },
            itemUsabilityOverride: () => {
              const groupingArray = dataView && dataView.getGrouping && dataView.getGrouping();
              return Array.isArray(groupingArray) && groupingArray.length > 0;
            }
          }, contextMenu.hideCommands, menuCommandItems, originalCommandItems);
        }
        if (gridOptions && contextMenu && !contextMenu.hideCollapseAllGroups) {
          this.addMissingCommandOrAction({
            _orgTitle: commandLabels?.collapseAllGroupsCommand || "",
            iconCssClass: contextMenu.iconCollapseAllGroupsCommand || "mdi mdi-arrow-collapse",
            titleKey: `${translationPrefix}COLLAPSE_ALL_GROUPS`,
            disabled: false,
            command: "collapse-all-groups",
            positionOrder: 57,
            action: () => {
              if (gridOptions.enableTreeData) {
                this.treeDataService.toggleTreeDataCollapse(true);
              } else {
                dataView.collapseAllGroups();
              }
              this.pubSubService.publish("onContextMenuCollapseAllGroups");
            },
            itemUsabilityOverride: () => {
              if (gridOptions.enableTreeData) {
                return true;
              }
              const groupingArray = dataView && dataView.getGrouping && dataView.getGrouping();
              return Array.isArray(groupingArray) && groupingArray.length > 0;
            }
          }, contextMenu.hideCommands, menuCommandItems, originalCommandItems);
        }
        if (gridOptions && contextMenu && !contextMenu.hideExpandAllGroups) {
          this.addMissingCommandOrAction({
            _orgTitle: commandLabels?.expandAllGroupsCommand || "",
            iconCssClass: contextMenu.iconExpandAllGroupsCommand || "mdi mdi-arrow-expand",
            titleKey: `${translationPrefix}EXPAND_ALL_GROUPS`,
            disabled: false,
            command: "expand-all-groups",
            positionOrder: 58,
            action: () => {
              if (gridOptions.enableTreeData) {
                this.treeDataService.toggleTreeDataCollapse(false);
              } else {
                dataView.expandAllGroups();
              }
              this.pubSubService.publish("onContextMenuExpandAllGroups");
            },
            itemUsabilityOverride: () => {
              if (gridOptions.enableTreeData) {
                return true;
              }
              const groupingArray = dataView && dataView.getGrouping && dataView.getGrouping();
              return Array.isArray(groupingArray) && groupingArray.length > 0;
            }
          }, contextMenu.hideCommands, menuCommandItems, originalCommandItems);
        }
      }
      this.extensionUtility.translateMenuItemsFromTitleKey(menuCommandItems);
      return menuCommandItems;
    }
    /** @deprecated sort all menu items by their position order when defined */
    sortMenuItems() {
      const contextMenu = this.sharedService?.gridOptions?.contextMenu;
      if (contextMenu) {
        this.extensionUtility.sortItems(contextMenu.commandItems || [], "positionOrder");
        this.extensionUtility.sortItems(contextMenu.optionItems || [], "positionOrder");
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickDraggableGrouping.js
  var SlickDraggableGrouping = class {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(extensionUtility, pubSubService, sharedService) {
      this.extensionUtility = extensionUtility;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this.columnsGroupBy = [];
      this.pluginName = "DraggableGrouping";
      this._gridColumns = [];
      this._gridUid = "";
      this._isInitialized = false;
      this._reorderedColumns = [];
      this._subscriptions = [];
      this._defaults = {
        dropPlaceHolderText: "Drop a column header here to group by the column",
        hideGroupSortIcons: false,
        hideToggleAllButton: false,
        toggleAllButtonText: "",
        toggleAllPlaceholderText: "Toggle all Groups"
      };
      this._bindingEventService = new BindingEventService();
      this.onGroupChanged = new SlickEvent("onGroupChanged");
      this._eventHandler = new SlickEventHandler();
    }
    get addonOptions() {
      return this._addonOptions;
    }
    /** Getter of SlickGrid DataView object */
    get dataView() {
      return this.grid?.getData() ?? {};
    }
    get dropboxElement() {
      return this._dropzoneElm;
    }
    get droppableInstance() {
      return this._droppableInstance;
    }
    get sortableLeftInstance() {
      return this._sortableLeftInstance;
    }
    get sortableRightInstance() {
      return this._sortableRightInstance;
    }
    get eventHandler() {
      return this._eventHandler;
    }
    get grid() {
      return this._grid ?? this.sharedService.slickGrid ?? {};
    }
    get gridOptions() {
      return this.sharedService.gridOptions ?? {};
    }
    /** Getter for the grid uid */
    get gridUid() {
      return this._gridUid || (this.grid?.getUID() ?? "");
    }
    get gridContainer() {
      return this.grid.getContainerNode();
    }
    set isInitialized(state2) {
      this._isInitialized = state2;
    }
    /** Initialize plugin. */
    init(grid, groupingOptions) {
      this._addonOptions = { ...this._defaults, ...groupingOptions };
      this._grid = grid;
      if (grid) {
        this._gridUid = grid.getUID();
        this._gridColumns = grid.getColumns();
        this._dropzoneElm = grid.getTopHeaderPanel() || grid.getPreHeaderPanel();
        if (!this._dropzoneElm) {
          throw new Error("[Slickgrid-Universal] Draggable Grouping requires the pre-header to be created and shown for the plugin to work correctly (use `createPreHeaderPanel` and `showPreHeaderPanel`).");
        }
        this._dropzoneElm.classList.add("slick-dropzone");
        this.onGroupChanged.setPubSubService(this.pubSubService);
        if (!this._addonOptions.hideToggleAllButton) {
          this._groupToggler = createDomElement("div", {
            className: "slick-group-toggle-all",
            title: this._addonOptions.toggleAllPlaceholderText ?? "",
            style: { display: "none" }
          });
          const groupTogglerIconElm = createDomElement("span", { className: "sgi slick-group-toggle-all-icon" }, this._groupToggler);
          if (this.gridOptions.enableTranslate && this._addonOptions.toggleAllButtonTextKey) {
            this._addonOptions.toggleAllButtonText = this.extensionUtility.translateWhenEnabledAndServiceExist(this._addonOptions.toggleAllButtonTextKey, "TEXT_TOGGLE_ALL_GROUPS");
          }
          if (this.gridOptions.enableTranslate && this._addonOptions.toggleAllPlaceholderTextKey) {
            this._addonOptions.toggleAllPlaceholderText = this.extensionUtility.translateWhenEnabledAndServiceExist(this._addonOptions.toggleAllPlaceholderTextKey, "TEXT_TOGGLE_ALL_GROUPS");
          }
          this._groupToggler.title = this._addonOptions.toggleAllPlaceholderText ?? "";
          if (this._addonOptions.toggleAllButtonText) {
            this._groupToggler.appendChild(createDomElement("span", {
              className: "slick-group-toggle-all-text",
              textContent: this._addonOptions.toggleAllButtonText || ""
            }));
          }
          this._dropzoneElm.appendChild(this._groupToggler);
          this._subscriptions.push(this.pubSubService.subscribe("onContextMenuCollapseAllGroups", () => this.toggleGroupToggler(groupTogglerIconElm, true, false)), this.pubSubService.subscribe("onContextMenuExpandAllGroups", () => this.toggleGroupToggler(groupTogglerIconElm, false, false)));
        }
        this._dropzonePlaceholderElm = createDomElement("div", { className: "slick-draggable-dropzone-placeholder" }, this._dropzoneElm);
        if (this.gridOptions.enableTranslate && this._addonOptions?.dropPlaceHolderTextKey) {
          this._addonOptions.dropPlaceHolderText = this.extensionUtility.translateWhenEnabledAndServiceExist(this._addonOptions.dropPlaceHolderTextKey, "TEXT_TOGGLE_ALL_GROUPS");
        }
        this._dropzonePlaceholderElm.textContent = this._addonOptions?.dropPlaceHolderText ?? this._defaults.dropPlaceHolderText ?? "";
        this.setupColumnDropbox();
        this._eventHandler.subscribe(grid.onHeaderCellRendered, (_e, args) => {
          const node = args.node;
          if (!isEmptyObject(args.column?.grouping) && node) {
            node.style.cursor = "pointer";
            if (this._addonOptions.groupIconCssClass) {
              const groupableIconElm = createDomElement("span", { className: "slick-column-groupable" }, node);
              if (this._addonOptions.groupIconCssClass) {
                groupableIconElm.classList.add(...classNameToList(this._addonOptions.groupIconCssClass));
              }
            }
          }
        });
        this._subscriptions.push(this.pubSubService.subscribe("onContextMenuClearGrouping", () => this.clearDroppedGroups()));
        for (const col of this._gridColumns) {
          const columnId = col.field;
          grid.updateColumnHeader(columnId);
        }
      }
      return this;
    }
    /** Dispose the plugin. */
    dispose() {
      this.destroySortableInstances();
      if (this._droppableInstance?.el) {
        this._droppableInstance?.destroy();
      }
      this.onGroupChanged.unsubscribe();
      this._eventHandler.unsubscribeAll();
      this.pubSubService.unsubscribeAll(this._subscriptions);
      this._bindingEventService.unbindAll();
      emptyElement(this.gridContainer.querySelector(`.${this.gridUid} .slick-preheader-panel,.${this.gridUid} .slick-topheader-panel`));
    }
    clearDroppedGroups() {
      this.columnsGroupBy = [];
      this.updateGroupBy("clear-all");
      const allDroppedGroupingElms = this._dropzoneElm.querySelectorAll(".slick-dropped-grouping");
      for (const groupElm of Array.from(allDroppedGroupingElms)) {
        const groupRemoveBtnElm = this._dropzoneElm.querySelector(".slick-groupby-remove");
        groupRemoveBtnElm?.remove();
        groupElm?.remove();
      }
      this._dropzonePlaceholderElm.style.display = "inline-block";
      if (this._groupToggler) {
        this._groupToggler.style.display = "none";
      }
    }
    destroySortableInstances() {
      if (this._sortableLeftInstance?.el) {
        this._sortableLeftInstance?.destroy();
      }
      if (this._sortableRightInstance?.el) {
        this._sortableRightInstance?.destroy();
      }
    }
    setAddonOptions(options) {
      this._addonOptions = { ...this._addonOptions, ...options };
    }
    setColumns(cols) {
      this._gridColumns = cols;
    }
    setDroppedGroups(groupingInfo) {
      this._dropzonePlaceholderElm.style.display = "none";
      const groupingInfos = Array.isArray(groupingInfo) ? groupingInfo : [groupingInfo];
      for (const groupInfo of groupingInfos) {
        const columnElm = this.grid.getHeaderColumn(groupInfo);
        this.handleGroupByDrop(this._dropzoneElm, columnElm);
      }
    }
    /**
     * Setup the column reordering
     * NOTE: this function is a standalone function and is called externally and does not have access to `this` instance
     * @param grid - slick grid object
     * @param headers - slick grid column header elements
     * @param _headerColumnWidthDiff - header column width difference
     * @param setColumns - callback to reassign columns
     * @param setupColumnResize - callback to setup the column resize
     * @param columns - columns array
     * @param getColumnIndex - callback to find index of a column
     * @param uid - grid UID
     * @param trigger - callback to execute when triggering a column grouping
     */
    setupColumnReorder(grid, headers, _headerColumnWidthDiff, setColumns, setupColumnResize, _columns, getColumnIndex, _uid, trigger) {
      this.destroySortableInstances();
      const dropzoneElm = grid.getTopHeaderPanel() || grid.getPreHeaderPanel();
      const draggablePlaceholderElm = dropzoneElm.querySelector(".slick-draggable-dropzone-placeholder");
      const groupTogglerElm = dropzoneElm.querySelector(".slick-group-toggle-all");
      const sortableOptions = {
        animation: 50,
        ghostClass: "slick-sortable-placeholder",
        draggable: ".slick-header-column",
        dataIdAttr: "data-id",
        group: {
          name: "shared",
          pull: "clone",
          put: false
        },
        // Fixes broken Firefox-Linux dragging
        forceFallback: /firefox/i.test(navigator.userAgent) && /linux/i.test(navigator.userAgent),
        revertClone: true,
        // filter: function (_e, target) {
        //   // block column from being able to be dragged if it's already a grouped column
        //   // NOTE: need to disable for now since it also blocks the column reordering
        //   return columnsGroupBy.some(c => c.id === target.getAttribute('data-id'));
        // },
        onStart: (e2) => {
          e2.item.classList.add("slick-header-column-active");
          if (draggablePlaceholderElm) {
            draggablePlaceholderElm.style.display = "inline-block";
          }
          const droppedGroupingElms = dropzoneElm.querySelectorAll(".slick-dropped-grouping");
          droppedGroupingElms.forEach((droppedGroupingElm) => droppedGroupingElm.style.display = "none");
          if (groupTogglerElm) {
            groupTogglerElm.style.display = "none";
          }
        },
        onEnd: (e2) => {
          e2.item.classList.remove("slick-header-column-active");
          dropzoneElm?.classList.remove("slick-dropzone-hover");
          draggablePlaceholderElm?.parentElement?.classList.remove("slick-dropzone-placeholder-hover");
          const droppedGroupingElms = dropzoneElm.querySelectorAll(".slick-dropped-grouping");
          droppedGroupingElms.forEach((droppedGroupingElm) => droppedGroupingElm.style.display = "flex");
          if (droppedGroupingElms.length) {
            if (draggablePlaceholderElm) {
              draggablePlaceholderElm.style.display = "none";
            }
            if (groupTogglerElm) {
              groupTogglerElm.style.display = "inline-flex";
            }
          }
          if (!grid.getEditorLock().commitCurrentEdit()) {
            return;
          }
          const reorderedIds = this.sortableLeftInstance?.toArray() ?? [];
          if (headers.length > 1) {
            const ids = this._sortableRightInstance?.toArray() ?? [];
            for (const id of ids) {
              reorderedIds.push(id);
            }
          }
          const finalReorderedColumns = [];
          const reorderedColumns = grid.getColumns();
          for (const reorderedId of reorderedIds) {
            finalReorderedColumns.push(reorderedColumns[getColumnIndex.call(grid, reorderedId)]);
          }
          setColumns.call(grid, finalReorderedColumns);
          trigger.call(grid, grid.onColumnsReordered, { grid, impactedColumns: finalReorderedColumns });
          e2.stopPropagation();
          setupColumnResize.call(grid);
        }
      };
      this._sortableLeftInstance = sortable_core_esm_default.create(this.gridContainer.querySelector(`.${grid.getUID()} .slick-header-columns.slick-header-columns-left`), sortableOptions);
      this._sortableRightInstance = sortable_core_esm_default.create(this.gridContainer.querySelector(`.${grid.getUID()} .slick-header-columns.slick-header-columns-right`), sortableOptions);
      const initialGroupIds = this._addonOptions.initialGroupBy ?? this.gridOptions.presets?.grouping;
      if (initialGroupIds && !this._isInitialized) {
        this.setDroppedGroups(initialGroupIds);
      }
      this._isInitialized = true;
      return {
        sortableLeftInstance: this._sortableLeftInstance,
        sortableRightInstance: this._sortableRightInstance
      };
    }
    //
    // protected functions
    // ------------------
    addColumnGroupBy(column) {
      this.columnsGroupBy.push(column);
      this.updateGroupBy("add-group");
    }
    addGroupByRemoveClickHandler(id, groupRemoveIconElm, headerColumnElm, entry) {
      this._bindingEventService.bind(groupRemoveIconElm, "click", () => {
        const boundedElms = this._bindingEventService.boundedEvents.filter((boundedEvent) => boundedEvent.element === groupRemoveIconElm);
        for (const boundedEvent of boundedElms) {
          this._bindingEventService.unbind(boundedEvent.element, "click", boundedEvent.listener);
        }
        this.removeGroupBy(id, headerColumnElm, entry);
      });
    }
    addGroupSortClickHandler(col, groupSortContainerElm) {
      const { grouping, type } = col;
      this._bindingEventService.bind(groupSortContainerElm, "click", () => {
        this.toggleGroupAll(col, false);
        if (grouping) {
          const nextSortDirection = grouping.sortAsc ? SortDirectionNumber.desc : SortDirectionNumber.asc;
          grouping.comparer = (a, b) => sortByFieldType(type || "text", a.value, b.value, nextSortDirection, col, this.gridOptions);
          this.getGroupBySortIcon(groupSortContainerElm, !grouping.sortAsc);
          this.updateGroupBy("sort-group");
          grouping.sortAsc = !grouping.sortAsc;
          this.grid.invalidate();
        }
      });
    }
    getGroupBySortIcon(groupSortContainerElm, sortAsc = true) {
      if (sortAsc) {
        if (this._addonOptions.sortAscIconCssClass) {
          groupSortContainerElm.classList.remove(...classNameToList(this._addonOptions.sortDescIconCssClass));
          groupSortContainerElm.classList.add(...classNameToList(this._addonOptions.sortAscIconCssClass));
        } else {
          groupSortContainerElm.classList.add("slick-groupby-sort-asc-icon");
          groupSortContainerElm.classList.remove("slick-groupby-sort-desc-icon");
        }
      } else {
        if (this._addonOptions.sortDescIconCssClass) {
          groupSortContainerElm.classList.remove(...classNameToList(this._addonOptions.sortAscIconCssClass));
          groupSortContainerElm.classList.add(...classNameToList(this._addonOptions.sortDescIconCssClass));
        } else {
          if (!this._addonOptions.sortDescIconCssClass) {
            groupSortContainerElm.classList.add("slick-groupby-sort-desc-icon");
            groupSortContainerElm.classList.remove("slick-groupby-sort-asc-icon");
          }
        }
      }
    }
    handleGroupByDrop(containerElm, headerColumnElm) {
      const columnId = headerColumnElm.getAttribute("data-id")?.replace(this._gridUid, "");
      let columnAllowed = true;
      for (const colGroupBy of this.columnsGroupBy) {
        if (colGroupBy.id === columnId) {
          columnAllowed = false;
        }
      }
      if (columnAllowed) {
        for (const col of this._gridColumns) {
          if (col.id === columnId && col.grouping && !isEmptyObject(col.grouping)) {
            const columnNameElm = headerColumnElm.querySelector(".slick-column-name");
            const entryElm = createDomElement("div", {
              id: `${this._gridUid}_${col.id}_entry`,
              className: "slick-dropped-grouping",
              dataset: { id: `${col.id}` }
            });
            createDomElement("div", {
              className: "slick-dropped-grouping-title",
              style: { display: "inline-flex" },
              textContent: columnNameElm ? columnNameElm.textContent : headerColumnElm.textContent
            }, entryElm);
            const groupRemoveIconElm = createDomElement("div", { className: "slick-groupby-remove" });
            if (this._addonOptions.deleteIconCssClass) {
              groupRemoveIconElm.classList.add(...classNameToList(this._addonOptions.deleteIconCssClass));
            }
            if (!this._addonOptions.deleteIconCssClass) {
              groupRemoveIconElm.classList.add("slick-groupby-remove-icon");
            }
            let groupSortContainerElm;
            if (this._addonOptions?.hideGroupSortIcons !== true && col.sortable) {
              if (col.grouping?.sortAsc === void 0) {
                col.grouping.sortAsc = true;
              }
              groupSortContainerElm = createDomElement("div", { className: "slick-groupby-sort" }, entryElm);
              this.getGroupBySortIcon(groupSortContainerElm, col.grouping.sortAsc);
            }
            entryElm.appendChild(groupRemoveIconElm);
            entryElm.appendChild(document.createElement("div"));
            containerElm.appendChild(entryElm);
            if (this._groupToggler && this.columnsGroupBy.length === 0) {
              this.toggleGroupAll(col);
            }
            this.addColumnGroupBy(col);
            this.addGroupByRemoveClickHandler(col.id, groupRemoveIconElm, headerColumnElm, entryElm);
            if (groupSortContainerElm) {
              this.addGroupSortClickHandler(col, groupSortContainerElm);
            }
          }
        }
        if (this._groupToggler && this.columnsGroupBy.length > 0) {
          this._groupToggler.style.display = "inline-flex";
        }
      }
    }
    toggleGroupAll({ grouping }, collapsed) {
      const togglerIcon = this._groupToggler?.querySelector(".slick-group-toggle-all-icon");
      const isCollapsed = collapsed === true || grouping?.collapsed;
      togglerIcon?.classList.toggle("collapsed", isCollapsed);
      togglerIcon?.classList.toggle("expanded", !isCollapsed);
    }
    removeFromArray(arrayToModify, itemToRemove) {
      if (Array.isArray(arrayToModify)) {
        const itemIdx = arrayToModify.findIndex((a) => a.id === itemToRemove.id);
        if (itemIdx >= 0) {
          arrayToModify.splice(itemIdx, 1);
        }
      }
      return arrayToModify;
    }
    removeGroupBy(id, _hdrColumnElm, entry) {
      entry.remove();
      const groupByColumns = [];
      this._gridColumns.forEach((col) => groupByColumns[col.id] = col);
      this.removeFromArray(this.columnsGroupBy, groupByColumns[id]);
      if (this.columnsGroupBy.length === 0) {
        this._dropzonePlaceholderElm.style.display = "inline-block";
        if (this._groupToggler) {
          this._groupToggler.style.display = "none";
        }
      }
      this.updateGroupBy("remove-group");
    }
    addDragOverDropzoneListeners() {
      const draggablePlaceholderElm = this._dropzoneElm.querySelector(".slick-draggable-dropzone-placeholder");
      if (draggablePlaceholderElm && this._dropzoneElm) {
        this._bindingEventService.bind(draggablePlaceholderElm, "dragover", (e2) => e2.preventDefault());
        this._bindingEventService.bind(draggablePlaceholderElm, "dragenter", () => this._dropzoneElm.classList.add("slick-dropzone-hover"));
        this._bindingEventService.bind(draggablePlaceholderElm, "dragleave", () => this._dropzoneElm.classList.remove("slick-dropzone-hover"));
      }
    }
    setupColumnDropbox() {
      const dropzoneElm = this._dropzoneElm;
      this._droppableInstance = sortable_core_esm_default.create(dropzoneElm, {
        group: "shared",
        ghostClass: "slick-droppable-sortitem-hover",
        draggable: ".slick-dropped-grouping",
        dragoverBubble: true,
        onAdd: (evt) => {
          const el = evt.item;
          if (el.getAttribute("id")?.replace(this._gridUid, "")) {
            const clonedElm = sortable_core_esm_default.utils.clone(evt.item);
            this.handleGroupByDrop(dropzoneElm, clonedElm);
          }
          el.parentNode?.removeChild(el);
        },
        onUpdate: () => {
          const sortArray = this._droppableInstance?.toArray() ?? [];
          const newGroupingOrder = [];
          for (const sortGroupId of sortArray) {
            for (const groupByColumn of this.columnsGroupBy) {
              if (groupByColumn.id === sortGroupId) {
                newGroupingOrder.push(groupByColumn);
                break;
              }
            }
          }
          this.columnsGroupBy = newGroupingOrder;
          this.updateGroupBy("sort-group");
        }
      });
      this.addDragOverDropzoneListeners();
      if (this._groupToggler) {
        this._bindingEventService.bind(this._groupToggler, "click", ((event) => {
          const target = event.target.classList.contains("slick-group-toggle-all-icon") ? event.target : event.currentTarget.querySelector(".slick-group-toggle-all-icon");
          this.toggleGroupToggler(target, target?.classList.contains("expanded"));
        }));
      }
    }
    toggleGroupToggler(targetElm, collapsing = true, shouldExecuteDataViewCommand = true) {
      if (targetElm) {
        const isCollapsing = collapsing === true;
        targetElm.classList.toggle("collapsed", isCollapsing);
        targetElm.classList.toggle("expanded", !isCollapsing);
        if (shouldExecuteDataViewCommand) {
          isCollapsing ? this.dataView.collapseAllGroups() : this.dataView.expandAllGroups();
        }
      }
    }
    updateGroupBy(originator) {
      if (this.columnsGroupBy.length === 0) {
        this.dataView.setGrouping([]);
        this._dropzonePlaceholderElm.style.display = "inline-block";
        this.triggerOnGroupChangedEvent({ caller: originator, groupColumns: [] });
        return;
      }
      const groupingArray = [];
      this.columnsGroupBy.forEach((element) => groupingArray.push(element.grouping));
      this.dataView.setGrouping(groupingArray);
      this._dropzonePlaceholderElm.style.display = "none";
      this.triggerOnGroupChangedEvent({ caller: originator, groupColumns: groupingArray });
    }
    /** call notify on slickgrid event and execute onGroupChanged callback when defined as a function by the user */
    triggerOnGroupChangedEvent(args) {
      if (this._addonOptions && typeof this._addonOptions.onGroupChanged === "function") {
        this._addonOptions.onGroupChanged(new SlickEventData(), args);
      }
      this.onGroupChanged.notify(args);
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickGridMenu.js
  var SlickGridMenu = class extends MenuBaseClass {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(extensionUtility, filterService, pubSubService, sharedService, sortService) {
      super(extensionUtility, pubSubService, sharedService);
      this.extensionUtility = extensionUtility;
      this.filterService = filterService;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this.sortService = sortService;
      this._commandMenuElm = null;
      this._gridMenuButtonElm = null;
      this._headerElm = null;
      this._isMenuOpen = false;
      this._subMenuParentId = "";
      this._defaults = {
        autoAlignSide: true,
        dropSide: "left",
        showButton: true,
        hideForceFitButton: false,
        hideSyncResizeButton: false,
        forceFitTitle: "Force fit columns",
        marginBottom: 15,
        menuWidth: 14,
        minHeight: 150,
        contentMinWidth: 0,
        resizeOnShowHeaderRow: false,
        syncResizeTitle: "Synchronous resize",
        subMenuOpenByEvent: "mouseover",
        headerColumnValueExtractor: (columnDef) => getHtmlStringOutput(columnDef.columnPickerLabel || columnDef.name || "", "innerHTML")
      };
      this.pluginName = "GridMenu";
      this._menuCssPrefix = "slick-menu";
      this._menuPluginCssPrefix = "slick-grid-menu";
      this._camelPluginName = "gridMenu";
      this._columns = this.grid?.getColumns() ?? [];
      this._gridUid = this.grid?.getUID() ?? "";
      this.onAfterMenuShow = new SlickEvent("onAfterMenuShow");
      this.onBeforeMenuShow = new SlickEvent("onBeforeMenuShow");
      this.onMenuClose = new SlickEvent("onMenuClose");
      this.onCommand = new SlickEvent("onCommand");
      this.onColumnsChanged = new SlickEvent("onColumnsChanged");
      this.initEventHandlers();
      this.init();
    }
    get addonOptions() {
      return this._addonOptions || {};
    }
    get columns() {
      return this._columns;
    }
    set columns(newColumns) {
      this._columns = newColumns;
    }
    get gridOptions() {
      return this.grid?.getOptions() || {};
    }
    get gridUidSelector() {
      return this.gridUid ? `.${this.gridUid}` : "";
    }
    initEventHandlers() {
      this._eventHandler.subscribe(this.grid.onColumnsReordered, this.updateColumnPickerOrder.bind(this));
      this._eventHandler.subscribe(this.grid.onClick, (e2) => this.hideMenu(e2));
      this._eventHandler.subscribe(this.grid.onBeforeDestroy, this.dispose.bind(this));
      this._eventHandler.subscribe(this.grid.onSetOptions, (_e, args) => {
        if (args && args.optionsBefore && args.optionsAfter) {
          const switchedFromRegularToFrozen = args.optionsBefore.frozenColumn >= 0 && args.optionsAfter.frozenColumn === -1;
          const switchedFromFrozenToRegular = args.optionsBefore.frozenColumn === -1 && args.optionsAfter.frozenColumn >= 0;
          if (switchedFromRegularToFrozen || switchedFromFrozenToRegular) {
            this.recreateGridMenu();
          }
        }
      });
    }
    /** Initialize plugin. */
    init(isFirstLoad = true) {
      this._gridUid = this.grid.getUID() ?? "";
      Utils.addSlickEventPubSubWhenDefined(this.pubSubService, this);
      if (isFirstLoad) {
        this._originalGridMenu = extend(true, {}, this.sharedService.gridOptions.gridMenu);
      }
      this._userOriginalGridMenu = { ...this.sharedService.gridOptions.gridMenu };
      this._addonOptions = { ...this._defaults, ...this.getDefaultGridMenuOptions(), ...this.sharedService.gridOptions.gridMenu };
      this.sharedService.gridOptions.gridMenu = this._addonOptions;
      const gridMenuCommandItems = this._userOriginalGridMenu.commandItems;
      const originalCommandItems = this._userOriginalGridMenu && Array.isArray(gridMenuCommandItems) ? gridMenuCommandItems : [];
      const initialCommandItems = [...originalCommandItems, ...this.addGridMenuCustomCommands(originalCommandItems)];
      this._addonOptions.commandItems = this._addonOptions.commandListBuilder?.(initialCommandItems) ?? initialCommandItems;
      this.extensionUtility.translateMenuItemsFromTitleKey(this._addonOptions.commandItems || [], "commandItems");
      this.extensionUtility.sortItems(this._addonOptions.commandItems, "positionOrder");
      this.createGridMenu();
    }
    /** Dispose (destroy) the SlickGrid 3rd party plugin */
    dispose() {
      this.deleteMenu();
      super.dispose();
    }
    deleteMenu() {
      this._bindEventService.unbindAll();
      emptyElement(this._gridMenuButtonElm, true);
      emptyElement(this._menuElm, true);
      this._gridMenuButtonElm = null;
      this._menuElm = null;
      this._headerElm?.parentElement?.querySelector(".slick-grid-menu-container")?.remove();
    }
    createColumnPickerContainer() {
      if (this._menuElm) {
        this.addColumnTitleElementWhenDefined(this._menuElm);
        this._listElm = createDomElement("div", { className: "slick-column-picker-list", role: "menu" });
        this._bindEventService.bind(this._menuElm, "click", this.handleColumnPickerItemClick.bind(this), void 0, "parent-menu");
      }
    }
    /** Create parent grid menu container */
    createGridMenu() {
      const gridMenuWidth = this._addonOptions?.menuWidth || this._defaults.menuWidth;
      const gridContainer = this.grid.getContainerNode();
      const headerSide = this.gridOptions.hasOwnProperty("frozenColumn") && this.gridOptions.frozenColumn >= 0 ? "right" : "left";
      this._headerElm = this._addonOptions?.iconButtonContainer === "preheader" ? gridContainer.querySelector(".slick-preheader-panel") : gridContainer.querySelector(`.slick-header-${headerSide}`);
      if (this._headerElm?.parentElement && this._addonOptions) {
        if (this._addonOptions.showButton ?? this._defaults.showButton) {
          const buttonPadding = 2;
          const gridMenuContainerElm = createDomElement("div", {
            className: "slick-grid-menu-container",
            style: { width: `${gridMenuWidth + buttonPadding}px` }
          });
          this._gridMenuButtonElm = createDomElement("button", { className: "slick-grid-menu-button", ariaLabel: "Grid Menu", tabIndex: 0 });
          const iconSpan = createDomElement("span", { className: this._addonOptions?.iconCssClass || "mdi mdi-menu" });
          this._gridMenuButtonElm.appendChild(iconSpan);
          gridMenuContainerElm.appendChild(this._gridMenuButtonElm);
          this._headerElm.parentElement.appendChild(gridMenuContainerElm);
          this._bindEventService.bind(gridMenuContainerElm, "click", this.showGridMenu.bind(this));
          this._bindEventService.bind(gridMenuContainerElm, "keydown", this.handleKeyDown.bind(this));
        }
        this.sharedService.gridOptions.gridMenu = { ...this._defaults, ...this._addonOptions };
        this.translateTitleLabels(this._addonOptions);
        this.translateTitleLabels(this.sharedService.gridOptions.gridMenu);
        this._bindEventService.bind(document.body, "mousedown", this.handleBodyMouseDown.bind(this));
        this._bindEventService.bind(document.body, "beforeunload", this.dispose.bind(this));
      }
    }
    /** Create the menu or sub-menu(s) but without the column picker which is a separate single process */
    createCommandMenu(triggeredByElm, commandItems, level = 0, item) {
      const subMenuCommand = item?.command;
      const subMenuId = subMenuCommand ? subMenuCommand.replace(/\s/g, "") : "";
      const menuClasses = `${this.menuCssClass} slick-menu-level-${level} ${this._gridUid}`;
      const bodyMenuElm = document.body.querySelector(`.${this.menuCssClass}.slick-menu-level-${level}${this.gridUidSelector}`);
      if (bodyMenuElm) {
        if (bodyMenuElm.dataset.subMenuParent === subMenuId) {
          return bodyMenuElm;
        }
        this.disposeSubMenus();
      }
      const menuElm = createDomElement("div", {
        role: "menu",
        className: menuClasses,
        ariaLabel: level > 1 ? "SubMenu" : "Grid Menu"
      });
      if (level > 0) {
        menuElm.classList.add("slick-submenu");
        if (subMenuId) {
          menuElm.dataset.subMenuParent = subMenuId;
        }
      }
      const callbackArgs = {
        grid: this.grid,
        menu: this._menuElm,
        columns: this.columns,
        allColumns: this.getAllColumns(),
        visibleColumns: this.getVisibleColumns(),
        level
      };
      this._commandMenuElm = this.recreateCommandList(triggeredByElm, commandItems, menuElm, callbackArgs, item);
      level++;
      return menuElm;
    }
    /**
     * Get all columns including hidden columns.
     * @returns {Array<Object>} - all columns array
     */
    getAllColumns() {
      return this._columns;
    }
    /**
     * Get only the visible columns.
     * @returns {Array<Object>} - only the visible columns array
     */
    getVisibleColumns() {
      return this.grid.getVisibleColumns();
    }
    /**
     * Hide the Grid Menu but only if it does detect as open prior to executing anything.
     * @param event
     * @returns
     */
    hideMenu(event) {
      const callbackArgs = {
        grid: this.grid,
        menu: this._menuElm,
        allColumns: this.columns,
        visibleColumns: this.getVisibleColumns()
      };
      this.pubSubService.publish("onGridMenuMenuClose", callbackArgs);
      if (typeof this._addonOptions?.onMenuClose === "function" && this._addonOptions.onMenuClose(event, callbackArgs) === false || this.onMenuClose.notify(callbackArgs, null, this).getReturnValue() === false) {
        return;
      }
      this._isMenuOpen = false;
      if (typeof this.grid?.autosizeColumns === "function" && this._addonOptions?.autoResizeColumns !== false) {
        const gridUid = this.grid.getUID() || "";
        if (this._areVisibleColumnDifferent && gridUid && document.querySelector(`.${gridUid}`) !== null) {
          if (this.gridOptions.enableAutoSizeColumns) {
            this.grid.autosizeColumns();
          }
          this._areVisibleColumnDifferent = false;
        }
      }
      this.disposeSubMenus();
      this._menuElm?.remove();
      this._menuElm = null;
    }
    /** destroy and recreate the Grid Menu in the DOM */
    recreateGridMenu() {
      this.deleteMenu();
      this.init(false);
    }
    /** Open the Grid Menu */
    openGridMenu() {
      const clickEvent = new MouseEvent("click", { bubbles: true, cancelable: true, composed: false });
      Object.defineProperty(clickEvent, "target", {
        writable: true,
        configurable: true,
        value: createDomElement("button", { className: "slick-grid-menu-button" })
      });
      this.showGridMenu(clickEvent);
    }
    /** show Grid Menu from the click event, which in theory will recreate the grid menu in the DOM */
    showGridMenu(e2, options) {
      const targetEvent = e2?.touches?.[0] ?? e2;
      this.stopFullBubbling(e2);
      emptyElement(this._menuElm);
      this._menuElm?.remove();
      if (this._addonOptions) {
        const callbackArgs = {
          grid: this.grid,
          menu: this._menuElm,
          columns: this.columns,
          allColumns: this.getAllColumns(),
          visibleColumns: this.getVisibleColumns()
        };
        const addonOptions = { ...this._addonOptions, ...options };
        if (addonOptions && !this.extensionUtility.runOverrideFunctionWhenExists(addonOptions.menuUsabilityOverride, callbackArgs)) {
          return;
        }
        if (typeof e2.stopPropagation === "function") {
          this.pubSubService.publish("onGridMenuBeforeMenuShow", callbackArgs);
          if (typeof addonOptions?.onBeforeMenuShow === "function" && addonOptions.onBeforeMenuShow(e2, callbackArgs) === false || this.onBeforeMenuShow.notify(callbackArgs, null, this).getReturnValue() === false) {
            return;
          }
        }
        this._menuElm = this.createCommandMenu(e2.target, this._addonOptions?.commandItems ?? []);
        if (!this.addonOptions.hideColumnPickerSection) {
          this.createColumnPickerContainer();
          this.updateColumnPickerOrder();
          this._columnCheckboxes = [];
          this.populateColumnPicker(addonOptions, this._defaults.headerColumnValueExtractor);
          this._menuElm.appendChild(this._listElm);
        }
        if (this.gridOptions.darkMode) {
          this._menuElm.classList.add("slick-dark-mode");
        }
        this._menuElm.style.opacity = "0";
        const menuMarginBottom = (addonOptions?.marginBottom !== void 0 ? addonOptions.marginBottom : this._defaults.marginBottom) || 0;
        this._menuElm.style.minHeight = findWidthOrDefault(addonOptions?.minHeight, "");
        if (addonOptions?.height !== void 0) {
          this._menuElm.style.height = findWidthOrDefault(addonOptions.height, "");
        } else {
          this._menuElm.style.maxHeight = findWidthOrDefault(addonOptions?.maxHeight, `${window.innerHeight - targetEvent.clientY - menuMarginBottom}px`);
        }
        let buttonElm = e2.target.nodeName === "BUTTON" ? e2.target : e2.target.querySelector("button");
        if (!buttonElm) {
          buttonElm = e2.target.parentElement;
        }
        document.body.appendChild(this._menuElm);
        this.repositionMenu(e2, this._menuElm, buttonElm, addonOptions);
        if (buttonElm) {
          this.setMenuTriggerElement(buttonElm);
        }
        const allItems = this.getAllFocusableMenuItems();
        if (allItems.length > 0) {
          allItems[0].focus();
        }
        const itemSelector = ".slick-grid-menu li[tabindex]:not(.disabled, .hidden, .slick-menu-item-disabled, .slick-menu-item-divider, .slick-menu-item-hidden)";
        this.wireMenuKeyboardNavigation(this._menuElm, {
          allItemsSelector: itemSelector,
          focusedItemSelector: itemSelector,
          onActivate: (focusedItem) => {
            if (focusedItem) {
              if (focusedItem.closest(".slick-column-picker-list")) {
                const iconContainer = focusedItem.querySelector(".icon-checkbox-container");
                if (iconContainer && typeof iconContainer.click === "function") {
                  iconContainer.click();
                }
              } else {
                focusedItem.dispatchEvent(new MouseEvent("click", { bubbles: true, cancelable: true }));
              }
            }
          },
          onEscape: () => {
            this.disposeAllMenus();
          },
          onTab: (evt) => {
            this.stopFullBubbling(evt);
          },
          eventServiceKey: "grid-menu-keyboard"
        });
        this._isMenuOpen = true;
        this.pubSubService.publish("onGridMenuAfterMenuShow", callbackArgs);
        if (typeof addonOptions?.onAfterMenuShow === "function") {
          addonOptions.onAfterMenuShow(e2, callbackArgs);
        }
        this.onAfterMenuShow.notify(callbackArgs, null, this);
      }
    }
    /** Translate the Grid Menu titles and column picker */
    translateGridMenu() {
      if (this.sharedService.gridOptions.gridMenu) {
        this.sharedService.gridOptions.gridMenu.commandItems = [];
        this.sharedService.gridOptions.gridMenu.commandTitle = this._originalGridMenu.commandTitle || "";
        this.sharedService.gridOptions.gridMenu.columnTitle = this._originalGridMenu.columnTitle || "";
        this.sharedService.gridOptions.gridMenu.forceFitTitle = this._originalGridMenu.forceFitTitle || "";
        this.sharedService.gridOptions.gridMenu.syncResizeTitle = this._originalGridMenu.syncResizeTitle || "";
        const originalCommandItems = this._userOriginalGridMenu && Array.isArray(this._userOriginalGridMenu.commandItems) ? this._userOriginalGridMenu.commandItems : [];
        this.sharedService.gridOptions.gridMenu.commandItems = [
          ...originalCommandItems,
          ...this.addGridMenuCustomCommands(originalCommandItems)
        ];
        this.extensionUtility.translateMenuItemsFromTitleKey(this._addonOptions?.commandItems || [], "commandItems");
        this.extensionUtility.sortItems(this.sharedService.gridOptions.gridMenu.commandItems, "positionOrder");
        this.translateTitleLabels(this.sharedService.gridOptions.gridMenu);
        this.translateTitleLabels(this._addonOptions);
        this.extensionUtility.translateItems(this._columns, "nameKey", "name");
      }
    }
    translateTitleLabels(gridMenuOptions) {
      if (gridMenuOptions) {
        gridMenuOptions.commandTitle = this.extensionUtility.getPickerTitleOutputString("commandTitle", "gridMenu");
        gridMenuOptions.columnTitle = this.extensionUtility.getPickerTitleOutputString("columnTitle", "gridMenu");
        gridMenuOptions.forceFitTitle = this.extensionUtility.getPickerTitleOutputString("forceFitTitle", "gridMenu");
        gridMenuOptions.syncResizeTitle = this.extensionUtility.getPickerTitleOutputString("syncResizeTitle", "gridMenu");
      }
    }
    // --
    // protected functions
    // ------------------
    /** Create Grid Menu with Custom Commands if user has enabled Filters and/or uses a Backend Service (OData, GraphQL) */
    addGridMenuCustomCommands(originalCommandItems) {
      const backendApi = this.gridOptions.backendServiceApi || null;
      const gridMenuCommandItems = [];
      const gridOptions = this.gridOptions;
      const translationPrefix = getTranslationPrefix(gridOptions);
      const commandLabels = this._addonOptions?.commandLabels;
      if (this._addonOptions && this.gridOptions) {
        if (!this._addonOptions.hideClearFrozenColumnsCommand) {
          this.addMissingCommandOrAction({
            iconCssClass: this._addonOptions.iconClearFrozenColumnsCommand || "mdi mdi-pin-off-outline",
            _orgTitle: commandLabels?.clearFrozenColumnsCommand || "",
            titleKey: `${translationPrefix}${commandLabels?.clearFrozenColumnsCommandKey ?? "CLEAR_PINNING"}`,
            disabled: false,
            command: "clear-pinning",
            positionOrder: 52,
            action: this.clearPinning.bind(this)
          }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
        }
        if (this.gridOptions.enableFiltering && !this.sharedService.hideHeaderRowAfterPageLoad) {
          if (!this._addonOptions.hideClearAllFiltersCommand) {
            this.addMissingCommandOrAction({
              iconCssClass: this._addonOptions.iconClearAllFiltersCommand || "mdi mdi-filter-remove-outline",
              _orgTitle: commandLabels?.clearAllFiltersCommand || "",
              titleKey: `${translationPrefix}${commandLabels?.clearAllFiltersCommandKey ?? "CLEAR_ALL_FILTERS"}`,
              disabled: false,
              command: "clear-filter",
              positionOrder: 50,
              action: () => {
                this.filterService.clearFilters();
                this.sharedService.dataView.refresh();
                this.pubSubService.publish("onGridMenuClearAllFilters");
              }
            }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
          }
          if (!this._addonOptions.hideToggleFilterCommand) {
            this.addMissingCommandOrAction({
              iconCssClass: this._addonOptions.iconToggleFilterCommand || "mdi mdi-flip-vertical",
              _orgTitle: commandLabels?.toggleFilterCommand || "",
              titleKey: `${translationPrefix}${commandLabels?.toggleFilterCommandKey ?? "TOGGLE_FILTER_ROW"}`,
              disabled: false,
              command: "toggle-filter",
              positionOrder: 53,
              action: this.toggleFilterBar.bind(this)
            }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
          }
          if (backendApi && !this._addonOptions.hideRefreshDatasetCommand) {
            this.addMissingCommandOrAction({
              iconCssClass: this._addonOptions.iconRefreshDatasetCommand || "mdi mdi-sync",
              _orgTitle: commandLabels?.refreshDatasetCommand || "",
              titleKey: `${translationPrefix}${commandLabels?.refreshDatasetCommandKey ?? "REFRESH_DATASET"}`,
              disabled: false,
              command: "refresh-dataset",
              positionOrder: 58,
              action: () => this.extensionUtility.refreshBackendDataset()
            }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
          }
        }
        if (!this._addonOptions.hideToggleDarkModeCommand) {
          this.addMissingCommandOrAction({
            iconCssClass: this._addonOptions.iconToggleDarkModeCommand || "mdi mdi-brightness-4",
            _orgTitle: commandLabels?.toggleDarkModeCommand || "",
            titleKey: `${translationPrefix}${commandLabels?.toggleDarkModeCommandKey ?? "TOGGLE_DARK_MODE"}`,
            disabled: false,
            command: "toggle-dark-mode",
            positionOrder: 54,
            action: () => {
              const currentDarkMode = this.sharedService.gridOptions.darkMode;
              this.grid.setOptions({ darkMode: !currentDarkMode });
              this.sharedService.gridOptions.darkMode = !currentDarkMode;
            }
          }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
        }
        if (this.gridOptions.showPreHeaderPanel) {
          if (!this._addonOptions.hideTogglePreHeaderCommand) {
            this.addMissingCommandOrAction({
              iconCssClass: this._addonOptions.iconTogglePreHeaderCommand || "mdi mdi-flip-vertical",
              _orgTitle: commandLabels?.togglePreHeaderCommand || "",
              titleKey: `${translationPrefix}${commandLabels?.togglePreHeaderCommandKey ?? "TOGGLE_PRE_HEADER_ROW"}`,
              disabled: false,
              command: "toggle-preheader",
              positionOrder: 53,
              action: () => {
                const showPreHeaderPanel = this.gridOptions?.showPreHeaderPanel ?? false;
                this.grid.setPreHeaderPanelVisibility(!showPreHeaderPanel);
              }
            }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
          }
        }
        if (this.gridOptions.enableSorting) {
          if (!this._addonOptions.hideClearAllSortingCommand) {
            this.addMissingCommandOrAction({
              iconCssClass: this._addonOptions.iconClearAllSortingCommand || "mdi mdi-sort-variant-off",
              _orgTitle: commandLabels?.clearAllSortingCommand || "",
              titleKey: `${translationPrefix}${commandLabels?.clearAllSortingCommandKey ?? "CLEAR_ALL_SORTING"}`,
              disabled: false,
              command: "clear-sorting",
              positionOrder: 51,
              action: () => {
                this.sortService.clearSorting();
                this.sharedService.dataView.refresh();
                this.pubSubService.publish("onGridMenuClearAllSorting");
              }
            }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
          }
        }
        if (this.gridOptions.enableTextExport && !this._addonOptions.hideExportCsvCommand) {
          this.addMissingCommandOrAction({
            iconCssClass: this._addonOptions.iconExportCsvCommand || "mdi mdi-download",
            _orgTitle: commandLabels?.exportCsvCommand || "",
            titleKey: `${translationPrefix}${commandLabels?.exportCsvCommandKey ?? "EXPORT_TO_CSV"}`,
            disabled: false,
            command: "export-csv",
            positionOrder: 55,
            action: this.exportCsv.bind(this)
          }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
        }
        if (this.gridOptions.enableExcelExport && !this._addonOptions.hideExportExcelCommand) {
          this.addMissingCommandOrAction({
            iconCssClass: this._addonOptions.iconExportExcelCommand || "mdi mdi-file-excel-outline text-success",
            _orgTitle: commandLabels?.exportExcelCommand || "",
            titleKey: `${translationPrefix}${commandLabels?.exportExcelCommandKey ?? "EXPORT_TO_EXCEL"}`,
            disabled: false,
            command: "export-excel",
            positionOrder: 56,
            action: this.exportExcel.bind(this)
          }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
        }
        if (this.gridOptions.enablePdfExport && !this._addonOptions.hideExportPdfCommand) {
          this.addMissingCommandOrAction({
            iconCssClass: this._addonOptions.iconExportPdfCommand || "mdi mdi-file-pdf-outline text-danger",
            _orgTitle: commandLabels?.exportPdfCommand || "",
            titleKey: `${translationPrefix}${commandLabels?.exportPdfCommandKey ?? "EXPORT_TO_PDF"}`,
            disabled: false,
            command: "export-pdf",
            positionOrder: 57,
            action: this.exportPdf.bind(this)
          }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
        }
        if (this.gridOptions.enableTextExport && !this._addonOptions.hideExportTextDelimitedCommand) {
          this.addMissingCommandOrAction({
            iconCssClass: this._addonOptions.iconExportTextDelimitedCommand || "mdi mdi-download",
            _orgTitle: commandLabels?.exportTextDelimitedCommand || "",
            titleKey: `${translationPrefix}${commandLabels?.exportTextDelimitedCommandKey ?? "EXPORT_TO_TAB_DELIMITED"}`,
            disabled: false,
            command: "export-text-delimited",
            positionOrder: 58,
            action: this.exportTextDelimited.bind(this)
          }, this._addonOptions.hideCommands, gridMenuCommandItems, originalCommandItems);
        }
        const commandItems = this._addonOptions?.commandItems || [];
        if (Array.isArray(gridMenuCommandItems) && gridMenuCommandItems.length > 0 || Array.isArray(commandItems) && commandItems.length > 0) {
          this._addonOptions.commandTitle ||= this.extensionUtility.getPickerTitleOutputString("commandTitle", "gridMenu");
        }
      }
      return gridMenuCommandItems;
    }
    clearPinning() {
      const newGridOptions = {
        frozenColumn: -1,
        frozenRow: -1,
        frozenBottom: false,
        enableMouseWheelScrollHandler: false
      };
      this.grid.setOptions(newGridOptions);
      Object.keys(newGridOptions).forEach((c) => this.sharedService.gridOptions[c] = newGridOptions[c]);
      this.grid.updateColumns();
      if (this.gridOptions.enableAutoSizeColumns) {
        this.grid.autosizeColumns();
      }
      this.pubSubService.publish("onGridMenuClearAllPinning");
    }
    exportCsv() {
      const registeredResources = this.sharedService?.externalRegisteredResources || [];
      const exportCsvService = registeredResources.find((service) => service.pluginName === "TextExportService");
      if (exportCsvService?.exportToFile) {
        exportCsvService.exportToFile({ delimiter: ",", format: "csv" });
      } else {
        console.error(`[Slickgrid-Universal] You must register the TextExportService to properly use Export to File in the Grid Menu. Example:: this.gridOptions = { enableTextExport: true, externalResources: [new TextExportService()] };`);
      }
    }
    exportExcel() {
      const registeredResources = this.sharedService?.externalRegisteredResources || [];
      const excelService = registeredResources.find((service) => service.pluginName === "ExcelExportService");
      if (excelService?.exportToExcel) {
        excelService.exportToExcel();
      } else {
        console.error(`[Slickgrid-Universal] You must register the ExcelExportService to properly use Export to Excel in the Grid Menu. Example:: this.gridOptions = { enableExcelExport: true, externalResources: [new ExcelExportService()] };`);
      }
    }
    exportPdf() {
      const registeredResources = this.sharedService?.externalRegisteredResources || [];
      const pdfService = registeredResources.find((service) => service.pluginName === "PdfExportService");
      if (pdfService?.exportToPdf) {
        pdfService.exportToPdf();
      } else {
        console.error(`[Slickgrid-Universal] You must register the PdfExportService to properly use Export to PDF in the Grid Menu. Example:: this.gridOptions = { enablePdfExport: true, externalResources: [new PdfExportService()] };`);
      }
    }
    exportTextDelimited() {
      const registeredResources = this.sharedService?.externalRegisteredResources || [];
      const exportTxtService = registeredResources.find((service) => service.pluginName === "TextExportService");
      if (exportTxtService?.exportToFile) {
        exportTxtService.exportToFile({ delimiter: "	", format: "txt" });
      } else {
        console.error(`[Slickgrid-Universal] You must register the TextExportService to properly use Export to File in the Grid Menu. Example:: this.gridOptions = { enableTextExport: true, externalResources: [new TextExportService()] };`);
      }
    }
    toggleFilterBar() {
      let showHeaderRow = this.gridOptions?.showHeaderRow ?? false;
      showHeaderRow = !showHeaderRow;
      this.grid.setHeaderRowVisibility(showHeaderRow);
      if (showHeaderRow === true) {
        this.grid.updateColumns();
        this.grid.scrollColumnIntoView(0);
      }
    }
    /** @return default Grid Menu options */
    getDefaultGridMenuOptions() {
      return {
        commandTitle: void 0,
        columnTitle: this.extensionUtility.getPickerTitleOutputString("columnTitle", "gridMenu"),
        forceFitTitle: this.extensionUtility.getPickerTitleOutputString("forceFitTitle", "gridMenu"),
        syncResizeTitle: this.extensionUtility.getPickerTitleOutputString("syncResizeTitle", "gridMenu"),
        iconCssClass: "mdi mdi-menu",
        menuWidth: 16,
        commandItems: [],
        hideClearAllFiltersCommand: false,
        hideRefreshDatasetCommand: false,
        hideToggleFilterCommand: false
      };
    }
    /** Returns the CSS selectors for all focusable menu items (commands and column picker), skipping dividers/disabled/hidden. */
    getFocusableMenuSelectors() {
      return [
        '.slick-menu-command-list[role="menu"] > li[role="menuitem"]:not(.slick-menu-item-divider):not(.disabled):not(.slick-menu-item-hidden)',
        '.slick-column-picker-list[role="menu"] > li:not(.hidden)'
      ];
    }
    /** Returns all focusable menu items in DOM order, skipping hidden/disabled/dividers. */
    getAllFocusableMenuItems() {
      const selectors = this.getFocusableMenuSelectors();
      return Array.from(this._menuElm?.querySelectorAll(selectors.join(",")) ?? []).filter((item) => item.offsetParent !== null);
    }
    /** Mouse down handler when clicking anywhere in the DOM body */
    handleBodyMouseDown(e2) {
      if (this.menuElement) {
        let isMenuClicked = false;
        const parentMenuElm = e2.target.closest(`.${this.menuCssClass}`);
        if (this.menuElement.contains(e2.target) || parentMenuElm) {
          isMenuClicked = true;
        }
        if (this._isMenuOpen && this.menuElement !== e2.target && !isMenuClicked && !e2.defaultPrevented || e2.target.className === "close" && parentMenuElm) {
          this.hideMenu(e2);
        }
      }
    }
    stopFullBubbling(e2) {
      if (e2) {
        e2.preventDefault();
        e2.stopPropagation();
      }
    }
    handleKeyDown(e2) {
      if (e2.key === "Enter" || e2.key === " ") {
        this.stopFullBubbling(e2);
        this.showGridMenu(e2);
      } else if (e2.key === "Tab") {
        this.stopFullBubbling(e2);
        if (e2.shiftKey) {
          this.grid.focusHeaderMenuOrColumn(this.getVisibleColumns().length - 1);
        } else {
          this.grid.focusHeaderRowFilter();
        }
      }
    }
    handleMenuItemCommandClick(event, _type, item, level = 0) {
      if (item !== "divider" && !item.disabled && !item.divider) {
        const command = item.command || "";
        if (command && !item.commandItems) {
          const callbackArgs = {
            grid: this.grid,
            command: item.command,
            item,
            allColumns: this.columns,
            visibleColumns: this.getVisibleColumns()
          };
          this.pubSubService.publish("onGridMenuCommand", callbackArgs);
          if (typeof this._addonOptions?.onCommand === "function") {
            this._addonOptions.onCommand(event, callbackArgs);
          }
          this.onCommand.notify(callbackArgs, null, this);
          if (typeof item.action === "function") {
            item.action.call(this, event, callbackArgs);
          }
          if (!this._addonOptions?.leaveOpen && !event.defaultPrevented) {
            this.hideMenu(event);
          }
          this.stopFullBubbling(event);
        } else if (item.commandItems) {
          this.repositionSubMenu(event, item, level);
        }
      }
    }
    handleMenuItemMouseOver(e2, _type, item, level = 0) {
      if (item !== "divider" && !item.disabled && !item.divider) {
        if (item.commandItems) {
          this.repositionSubMenu(e2, item, level);
        } else if (level === 0) {
          this.disposeSubMenus();
        }
      }
    }
    /** Re/Create Command List by adding title, close & list of commands */
    recreateCommandList(triggeredByElm, commandItems, menuElm, callbackArgs, item) {
      const level = callbackArgs.level || 0;
      if (commandItems.length > 0) {
        const commandMenuElm = createDomElement("div", { className: `${this._menuCssPrefix}-command-list`, role: "menu" }, menuElm);
        if (level === 0) {
          this.populateCommandOrOptionTitle("command", this.addonOptions, commandMenuElm, level);
          const commandMenuHeaderElm = menuElm.querySelector(`.slick-command-header`) ?? createDomElement("div", { className: "slick-command-header" });
          commandMenuHeaderElm.classList.add("with-close");
          this.addCloseButtomElement(commandMenuHeaderElm);
          commandMenuElm.appendChild(commandMenuHeaderElm);
        }
        if (item && level > 0) {
          this.addSubMenuTitleWhenExists(item, commandMenuElm);
        }
        this.populateCommandOrOptionItems("command", this._addonOptions, commandMenuElm, commandItems, callbackArgs, triggeredByElm, this.handleMenuItemCommandClick, this.handleMenuItemMouseOver);
        return commandMenuElm;
      }
      return null;
    }
    repositionSubMenu(e2, item, level) {
      const commandItems = item?.commandItems || [];
      const subMenuElm = this.createCommandMenu(e2.target, commandItems, level + 1, item);
      subMenuElm.style.display = "block";
      document.body.appendChild(subMenuElm);
      this._gridMenuButtonElm && this.repositionMenu(e2, subMenuElm, this._gridMenuButtonElm, this._addonOptions);
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickHeaderButtons.js
  var SlickHeaderButtons = class extends MenuBaseClass {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(extensionUtility, pubSubService, sharedService) {
      super(extensionUtility, pubSubService, sharedService);
      this.extensionUtility = extensionUtility;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this._buttonElms = [];
      this._defaults = {
        buttonCssClass: "slick-header-button"
      };
      this.pluginName = "HeaderButtons";
      this._menuCssPrefix = "slick-header-button";
      this._camelPluginName = "headerButtons";
      this.init(sharedService.gridOptions.headerButton);
    }
    get addonOptions() {
      return this._addonOptions;
    }
    set addonOptions(newOptions) {
      this._addonOptions = newOptions;
    }
    get eventHandler() {
      return this._eventHandler;
    }
    get grid() {
      return this.sharedService.slickGrid;
    }
    /** Initialize plugin. */
    init(headerButtonOptions) {
      this._addonOptions = { ...this._defaults, ...headerButtonOptions };
      this._eventHandler.subscribe(this.grid.onHeaderCellRendered, this.handleHeaderCellRendered.bind(this));
      this._eventHandler.subscribe(this.grid.onBeforeHeaderCellDestroy, this.handleBeforeHeaderCellDestroy.bind(this));
      this.grid.updateColumns();
    }
    /** Dispose (destroy) the SlickGrid 3rd party plugin */
    dispose() {
      super.dispose();
      this._buttonElms.forEach((elm) => elm.remove());
    }
    // --
    // event handlers
    // ------------------
    /**
     * Event handler when column title header are being rendered
     * @param {Object} event - The event
     * @param {Object} args - object arguments
     */
    handleHeaderCellRendered(e2, args) {
      const column = args.column;
      if (column.header?.buttons && Array.isArray(column.header.buttons)) {
        let i = column.header.buttons.length;
        while (i--) {
          const buttonItem = column.header.buttons[i];
          const itemElm = this.populateSingleCommandOrOptionItem("command", this.addonOptions, null, buttonItem, args, e2.target, this.handleButtonClick.bind(this));
          if (itemElm && !buttonItem.disabled) {
            itemElm.tabIndex = 0;
          }
          if (itemElm && buttonItem.handler && !buttonItem.disabled) {
            this._bindEventService.bind(itemElm, "click", ((e3) => buttonItem.handler.call(this, e3)));
          }
          if (itemElm && !buttonItem.disabled) {
            this._bindEventService.bind(itemElm, "keydown", ((e3) => {
              if (e3.key === "Enter" || e3.key === " ") {
                e3.preventDefault();
                const clickEvent = new MouseEvent("click", { bubbles: true, cancelable: true });
                itemElm.dispatchEvent(clickEvent);
              }
            }));
          }
          if (itemElm) {
            this._buttonElms.push(itemElm);
            args.node.appendChild(itemElm);
          }
        }
      }
    }
    /**
     * Event handler before the header cell is being destroyed
     * @param {Object} event - The event
     * @param {Object} args.column - The column definition
     */
    handleBeforeHeaderCellDestroy(_e, args) {
      const column = args.column;
      if (column.header?.buttons && this._addonOptions?.buttonCssClass) {
        const buttonCssClass = (this._addonOptions?.buttonCssClass || "").replace(/(\s+)/g, ".");
        if (buttonCssClass) {
          args.node.querySelectorAll(`.${buttonCssClass}`).forEach((elm) => elm.remove());
        }
      }
    }
    handleButtonClick(event, _type, button, level, columnDef) {
      if (button.command && !button.disabled) {
        const command = button.command || "";
        const callbackArgs = {
          grid: this.grid,
          column: columnDef,
          button
        };
        if (command) {
          callbackArgs.command = command;
        }
        if (typeof button.action === "function" && !button.disabled) {
          button.action.call(this, event, callbackArgs);
        }
        if (command !== null && !button.disabled && this._addonOptions?.onCommand) {
          this.pubSubService.publish("onHeaderButtonCommand", callbackArgs);
          this._addonOptions.onCommand(event, callbackArgs);
          if (columnDef?.id) {
            this.grid.updateColumnHeader(columnDef.id);
          }
        }
      }
      event.preventDefault();
      event.stopPropagation();
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickHeaderMenu.js
  var SlickHeaderMenu = class extends MenuBaseClass {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(extensionUtility, filterService, pubSubService, sharedService, sortService) {
      super(extensionUtility, pubSubService, sharedService);
      this.extensionUtility = extensionUtility;
      this.filterService = filterService;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this.sortService = sortService;
      this._subMenuParentId = "";
      this._defaults = {
        autoAlign: true,
        autoAlignOffset: 0,
        buttonCssClass: null,
        buttonImage: null,
        minWidth: 100,
        hideColumnHideCommand: false,
        hideSortCommands: false,
        title: "",
        subMenuOpenByEvent: "mouseover"
      };
      this.pluginName = "HeaderMenu";
      this._menuCssPrefix = "slick-menu";
      this._menuPluginCssPrefix = "slick-header-menu";
      this._camelPluginName = "headerMenu";
      this.init(sharedService.gridOptions.headerMenu);
    }
    /** Initialize plugin. */
    init(headerMenuOptions) {
      this._addonOptions = { ...this._defaults, ...headerMenuOptions };
      this._eventHandler.subscribe(this.grid.onBeforeSetColumns, (_e, args) => this.recreateHeaderMenu(args.newColumns));
      this._eventHandler.subscribe(this.grid.onHeaderCellRendered, this.handleHeaderCellRendered.bind(this));
      this._eventHandler.subscribe(this.grid.onBeforeHeaderCellDestroy, this.handleBeforeHeaderCellDestroy.bind(this));
      this._eventHandler.subscribe(this.grid.onClick, this.hideMenu.bind(this));
      this.grid.setColumns(this.grid.getColumns());
      this._bindEventService.bind(document.body, "mousedown", this.handleBodyMouseDown.bind(this));
    }
    /** Dispose (destroy) of the plugin */
    dispose() {
      emptyElement(this._headerButtonDivElm, true);
      this._headerButtonDivElm = null;
      super.dispose();
      this._activeHeaderColumnElm = void 0;
    }
    /** Hide a column from the grid */
    hideColumn(column) {
      if (this.grid) {
        const isFrozenAllowed = this.grid.validateColumnFreeze(column.id, true);
        if (isFrozenAllowed) {
          this.grid.updateColumnById(column.id, { hidden: true });
          this.grid.updateColumns();
          this.pubSubService.publish("onHideColumns", { columns: this.grid.getVisibleColumns(), hiddenColumn: column });
        }
      }
    }
    /** Hide the Header Menu */
    hideMenu() {
      this.disposeSubMenus();
      this._menuElm?.remove();
      this._menuElm = void 0;
    }
    repositionSubMenu(e2, item, level, columnDef) {
      const subMenuElm = this.createCommandMenu(e2.target, item.commandItems || [], columnDef, level + 1, item);
      document.body.appendChild(subMenuElm);
      this.repositionMenu(e2, subMenuElm, void 0, this.addonOptions);
    }
    /** Translate the Header Menu titles, we need to loop through all column definition to re-translate them */
    translateHeaderMenu() {
      if (this.sharedService.gridOptions?.headerMenu) {
        this.resetHeaderMenuTranslations(this.grid.getColumns());
      }
    }
    // --
    // event handlers
    // ------------------
    /**
     * Event handler when column title header are being rendered
     * @param {Object} event - The event
     * @param {Object} args - object arguments
     */
    handleHeaderCellRendered(_e, args) {
      const column = args.column;
      const menu = column.header?.menu;
      if (menu && args.node) {
        if (!this.extensionUtility.runOverrideFunctionWhenExists(this.addonOptions.menuUsabilityOverride, args)) {
          return;
        }
        this._headerButtonDivElm = createDomElement("div", { className: "slick-header-menu-button", ariaLabel: "Header Menu", tabIndex: 0 }, args.node);
        this._headerButtonDivElm.appendChild(createDomElement("span", { className: "sgi slick-header-menu-icon" }));
        if (this.addonOptions.buttonCssClass) {
          this._headerButtonDivElm.classList.add(...classNameToList(this.addonOptions.buttonCssClass));
        }
        if (this.addonOptions.tooltip) {
          this._headerButtonDivElm.title = this.addonOptions.tooltip;
        }
        this._bindEventService.bind(this._headerButtonDivElm, "click", ((e2) => {
          this.disposeAllMenus();
          this.createParentMenu(e2, args.column, menu);
        }));
        this._bindEventService.bind(this._headerButtonDivElm, "keydown", ((e2) => {
          if (e2.key === "Enter" || e2.key === " ") {
            e2.preventDefault();
            this.disposeAllMenus();
            this.createParentMenu(e2, args.column, menu);
          }
        }));
      }
    }
    /**
     * Event handler before the header cell is being destroyed
     * @param {Object} event - The event
     * @param {Object} args.column - The column definition
     */
    handleBeforeHeaderCellDestroy(_e, args) {
      const column = args.column;
      if (column.header?.menu) {
        args.node.querySelectorAll(".slick-header-menu-button").forEach((elm) => elm.remove());
      }
    }
    /** Mouse down handler when clicking anywhere in the DOM body */
    handleBodyMouseDown(e2) {
      if (this.menuElement) {
        let isMenuClicked = false;
        const parentMenuElm = e2.target.closest(`.${this.menuCssClass}`);
        if (this.menuElement.contains(e2.target) || parentMenuElm) {
          isMenuClicked = true;
        }
        if (this._menuElm !== e2.target && !isMenuClicked && !e2.defaultPrevented || e2.target.className === "close" && parentMenuElm) {
          this.hideMenu();
        }
      }
    }
    handleMenuItemCommandClick(event, _type, item, level = 0, columnDef) {
      if (item !== "divider" && !item.disabled && !item.divider) {
        const command = item.command || "";
        if (command && !item.commandItems) {
          const callbackArgs = {
            grid: this.grid,
            command: item.command,
            column: columnDef,
            item
          };
          this.pubSubService.publish("onHeaderMenuCommand", callbackArgs);
          if (typeof this.addonOptions?.onCommand === "function") {
            this.addonOptions.onCommand(event, callbackArgs);
          }
          if (typeof item.action === "function") {
            item.action.call(this, event, callbackArgs);
          }
          if (!event.defaultPrevented) {
            this.hideMenu();
          }
          event.preventDefault();
          event.stopPropagation();
        } else if (item.commandItems) {
          this.repositionSubMenu(event, item, level, columnDef);
        }
      }
    }
    handleMenuItemMouseOver(e2, _type, item, level = 0, columnDef) {
      if (item !== "divider" && !item.disabled && !item.divider) {
        if (item.commandItems) {
          this.repositionSubMenu(e2, item, level, columnDef);
        } else if (level === 0) {
          this.disposeSubMenus();
        }
      }
    }
    // --
    // protected functions
    // ------------------
    /**
     * Create Header Menu with Custom Commands if user has enabled Header Menu
     * @param gridOptions
     * @param columns
     * @return header menu
     */
    addHeaderMenuCustomCommands(columns) {
      const gridOptions = this.sharedService.gridOptions;
      const headerMenuOptions = gridOptions.headerMenu || {};
      const translationPrefix = getTranslationPrefix(gridOptions);
      const commandLabels = this._addonOptions?.commandLabels;
      if (Array.isArray(columns) && gridOptions.enableHeaderMenu) {
        columns.forEach((columnDef) => {
          if (columnDef && !columnDef.excludeFromHeaderMenu) {
            if (!columnDef.header) {
              columnDef.header = {
                menu: {
                  commandItems: []
                }
              };
            } else if (!columnDef.header.menu) {
              columnDef.header.menu = { commandItems: [] };
            }
            let columnHeaderMenuItems = columnDef?.header?.menu?.commandItems ?? [];
            let hasFrozenOrResizeCommand = false;
            if (headerMenuOptions && !headerMenuOptions.hideFreezeColumnsCommand) {
              hasFrozenOrResizeCommand = true;
              if (gridOptions.frozenColumn === columns.findIndex((col) => col.id === columnDef.id)) {
                this.removeCommandWhenFound(columnHeaderMenuItems, "freeze-columns");
                const cmdUnfreeze = "unfreeze-columns";
                this.addMissingCommandOrAction({
                  _orgTitle: commandLabels?.unfreezeColumnsCommand || "",
                  iconCssClass: headerMenuOptions.iconUnfreezeColumns || "mdi mdi-pin-off-outline",
                  titleKey: `${translationPrefix}UNFREEZE_COLUMNS`,
                  command: cmdUnfreeze,
                  positionOrder: 45,
                  action: (_e, args) => this.freezeOrUnfreezeColumns(args.column, cmdUnfreeze)
                }, headerMenuOptions.hideCommands, columnHeaderMenuItems);
              } else {
                this.removeCommandWhenFound(columnHeaderMenuItems, "unfreeze-columns");
                const cmdFreeze = "freeze-columns";
                this.addMissingCommandOrAction({
                  _orgTitle: commandLabels?.freezeColumnsCommand || "",
                  iconCssClass: headerMenuOptions.iconFreezeColumns || "mdi mdi-pin-outline",
                  titleKey: `${translationPrefix}FREEZE_COLUMNS`,
                  command: "freeze-columns",
                  positionOrder: 45,
                  action: (_e, args) => this.freezeOrUnfreezeColumns(args.column, cmdFreeze)
                }, headerMenuOptions.hideCommands, columnHeaderMenuItems);
              }
            }
            if (headerMenuOptions && !headerMenuOptions.hideColumnResizeByContentCommand && !headerMenuOptions.hideCommands?.includes("column-resize-by-content") && this.sharedService.gridOptions.enableColumnResizeOnDoubleClick) {
              hasFrozenOrResizeCommand = true;
              this.addMissingCommandOrAction({
                _orgTitle: commandLabels?.columnResizeByContentCommand || "",
                iconCssClass: headerMenuOptions.iconColumnResizeByContentCommand || "mdi mdi-arrow-expand-horizontal",
                titleKey: `${translationPrefix}COLUMN_RESIZE_BY_CONTENT`,
                command: "column-resize-by-content",
                positionOrder: 47,
                action: (_e, args) => this.pubSubService.publish("onHeaderMenuColumnResizeByContent", { columnId: args.column.id })
              }, headerMenuOptions.hideCommands, columnHeaderMenuItems);
            }
            if (hasFrozenOrResizeCommand && !columnHeaderMenuItems.some((item) => item !== "divider" && item.positionOrder === 48)) {
              columnHeaderMenuItems.push({ divider: true, command: "divider-1", positionOrder: 48 });
            }
            if (gridOptions.enableSorting && columnDef.sortable && headerMenuOptions && !headerMenuOptions.hideSortCommands && // @deprecated use boolean value of addMissingCommandOrAction() in next major instead
            !(headerMenuOptions.hideCommands?.includes("sort-asc") && headerMenuOptions.hideCommands?.includes("sort-desc"))) {
              this.addMissingCommandOrAction({
                _orgTitle: commandLabels?.sortAscCommand || "",
                iconCssClass: headerMenuOptions.iconSortAscCommand || "mdi mdi-sort-ascending",
                titleKey: `${translationPrefix}SORT_ASCENDING`,
                command: "sort-asc",
                positionOrder: 50,
                action: (e2, args) => this.sortColumn(e2, args, true)
              }, headerMenuOptions.hideCommands, columnHeaderMenuItems);
              this.addMissingCommandOrAction({
                _orgTitle: commandLabels?.sortDescCommand || "",
                iconCssClass: headerMenuOptions.iconSortDescCommand || "mdi mdi-sort-descending",
                titleKey: `${translationPrefix}SORT_DESCENDING`,
                command: "sort-desc",
                positionOrder: 51,
                action: (e2, args) => this.sortColumn(e2, args, false)
              }, headerMenuOptions.hideCommands, columnHeaderMenuItems);
              if (!columnHeaderMenuItems.some((item) => item !== "divider" && item.positionOrder === 52)) {
                columnHeaderMenuItems.push({ divider: true, command: "divider-2", positionOrder: 52 });
              }
              if (!headerMenuOptions.hideClearSortCommand) {
                this.addMissingCommandOrAction({
                  _orgTitle: commandLabels?.clearSortCommand || "",
                  iconCssClass: headerMenuOptions.iconClearSortCommand || "mdi mdi-sort-variant-off",
                  titleKey: `${translationPrefix}REMOVE_SORT`,
                  command: "clear-sort",
                  positionOrder: 58,
                  action: (e2, args) => this.clearColumnSort(e2, args)
                }, headerMenuOptions.hideCommands, columnHeaderMenuItems);
              }
            }
            const cmdShortcutName = "filter-shortcuts-root-menu";
            if (columnDef.filter?.filterShortcuts && !columnHeaderMenuItems.some((item) => item !== "divider" && "command" in item && item.command === cmdShortcutName)) {
              const shortcutSubItems = [];
              columnDef.filter.filterShortcuts.forEach((fs) => {
                const command = fs.title ? toKebabCase(fs.title) : (fs.titleKey || "").toLowerCase().replaceAll("_", "-");
                shortcutSubItems.push({
                  ...fs,
                  command,
                  action: (_e, args) => {
                    const filterRef = this.filterService.getFiltersMetadata().find((f2) => f2.columnDef.id === args.column.id);
                    filterRef?.setValues(fs.searchTerms, fs.operator, true);
                  }
                });
              });
              const filterShortcutsPositionOrder = headerMenuOptions.filterShortcutsPositionOrder ?? 55;
              columnHeaderMenuItems.push({
                _orgTitle: commandLabels?.filterShortcutsCommand || "",
                iconCssClass: headerMenuOptions.iconFilterShortcutSubMenu || "mdi mdi-filter-outline",
                titleKey: `${translationPrefix}FILTER_SHORTCUTS`,
                command: cmdShortcutName,
                positionOrder: filterShortcutsPositionOrder,
                commandItems: shortcutSubItems
              });
              if (hasFrozenOrResizeCommand && !columnHeaderMenuItems.some((item) => item !== "divider" && item.positionOrder === filterShortcutsPositionOrder + 1)) {
                columnHeaderMenuItems.push({ divider: true, command: "divider-3", positionOrder: filterShortcutsPositionOrder + 1 });
              }
            }
            if (gridOptions.enableFiltering && columnDef.filterable && headerMenuOptions && !headerMenuOptions.hideFilterCommand && !headerMenuOptions.hideCommands?.includes("clear-filter")) {
              if (!headerMenuOptions.hideClearFilterCommand) {
                this.addMissingCommandOrAction({
                  _orgTitle: commandLabels?.clearFilterCommand || "",
                  iconCssClass: headerMenuOptions.iconClearFilterCommand || "mdi mdi-filter-remove-outline",
                  titleKey: `${translationPrefix}REMOVE_FILTER`,
                  command: "clear-filter",
                  positionOrder: 57,
                  action: (e2, args) => {
                    this.clearColumnFilter(e2, args);
                  }
                }, headerMenuOptions.hideCommands, columnHeaderMenuItems);
              }
            }
            if (headerMenuOptions && !headerMenuOptions.hideColumnHideCommand && !headerMenuOptions.hideCommands?.includes("hide-column")) {
              this.addMissingCommandOrAction({
                _orgTitle: commandLabels?.hideColumnCommand || "",
                iconCssClass: headerMenuOptions.iconColumnHideCommand || "mdi mdi-close",
                titleKey: `${translationPrefix}HIDE_COLUMN`,
                command: "hide-column",
                positionOrder: 59,
                action: (_e, args) => {
                  this.hideColumn(args.column);
                  if (this.sharedService.gridOptions?.enableAutoSizeColumns) {
                    this.grid.autosizeColumns();
                  }
                }
              }, headerMenuOptions.hideCommands, columnHeaderMenuItems);
            }
            this.extensionUtility.translateMenuItemsFromTitleKey(columnHeaderMenuItems);
            this.extensionUtility.sortItems(columnHeaderMenuItems, "positionOrder");
          }
        });
      }
      return headerMenuOptions;
    }
    /** Clear the Filter on the current column (if it's actually filtered) */
    clearColumnFilter(event, args) {
      if (args?.column) {
        this.filterService.clearFilterByColumnId(event, args.column.id);
      }
    }
    /** Clear the Sort on the current column (if it's actually sorted) */
    clearColumnSort(event, args) {
      if (args?.column && this.sharedService) {
        this.sortService.clearSortByColumnId(event, args.column.id);
      }
    }
    removeCommandWhenFound(columnHeaderMenuItems, command) {
      const commandIdx = columnHeaderMenuItems.findIndex((item) => item !== "divider" && item?.command === command);
      if (commandIdx >= 0) {
        columnHeaderMenuItems.splice(commandIdx, 1);
      }
    }
    /** freeze or unfreeze columns command */
    freezeOrUnfreezeColumns(column, command) {
      const columnPosition = this.grid.getVisibleColumns().findIndex((col) => col.id === column.id);
      const newGridOptions = {
        frozenColumn: command === "unfreeze-columns" ? -1 : columnPosition,
        enableMouseWheelScrollHandler: true
      };
      if (this.grid.validateColumnFreezeWidth(newGridOptions.frozenColumn)) {
        this.grid.setOptions(newGridOptions, false, true);
        const columnHeaderMenuItems = column?.header?.menu?.commandItems ?? [];
        this.removeCommandWhenFound(columnHeaderMenuItems, command);
        this.sharedService.gridOptions.frozenColumn = newGridOptions.frozenColumn;
        this.sharedService.gridOptions.enableMouseWheelScrollHandler = newGridOptions.enableMouseWheelScrollHandler;
        this.sharedService.frozenVisibleColumnId = this.grid.getFrozenColumnId();
        this.grid.updateColumns();
        this.recreateHeaderMenu(this.grid.getColumns());
        const gridOptions = this.grid.getOptions();
        if (gridOptions.enableAutoSizeColumns) {
          this.grid.autosizeColumns();
        }
      }
    }
    createParentMenu(e2, columnDef, menu) {
      const callbackArgs = {
        grid: this.grid,
        column: columnDef,
        menu
      };
      if (typeof e2.stopPropagation === "function") {
        this.pubSubService.publish("onHeaderMenuBeforeMenuShow", callbackArgs);
        if (typeof this.addonOptions?.onBeforeMenuShow === "function" && this.addonOptions?.onBeforeMenuShow(e2, callbackArgs) === false) {
          return;
        }
      }
      const triggerElement = e2.target.closest('[class*="-menu-button"]') ?? e2.target;
      this.setMenuTriggerElement(triggerElement);
      this._menuElm = this.createCommandMenu(triggerElement, menu.commandItems, columnDef);
      this.grid.getContainerNode()?.appendChild(this._menuElm);
      this.repositionMenu(e2, this._menuElm, void 0, this.addonOptions);
      this.focusFirstMenuItem(this._menuElm);
      this.wireMenuKeyboardNavigation(this._menuElm);
      this.pubSubService.publish("onHeaderMenuAfterMenuShow", callbackArgs);
      if (typeof this.addonOptions?.onAfterMenuShow === "function" && this.addonOptions?.onAfterMenuShow(e2, callbackArgs) === false) {
        return;
      }
      e2.preventDefault();
      e2.stopPropagation();
    }
    /** Create the menu or sub-menu(s) but without the column picker which is a separate single process */
    createCommandMenu(triggeredByElm, commandItems, columnDef, level = 0, item) {
      const subMenuCommand = item?.command;
      const subMenuId = subMenuCommand ? subMenuCommand.replace(/\s/g, "") : "";
      const menuClasses = `${this.menuCssClass} slick-menu-level-${level} ${this.gridUid}`;
      const bodyMenuElm = document.body.querySelector(`.${this.menuCssClass}.slick-menu-level-${level}${this.gridUidSelector}`);
      if (bodyMenuElm) {
        if (bodyMenuElm.dataset.subMenuParent === subMenuId) {
          return bodyMenuElm;
        }
        this.disposeSubMenus();
      }
      const menuElm = createDomElement("div", {
        ariaLabel: level > 1 ? "SubMenu" : "Header Menu",
        role: "menu",
        className: menuClasses,
        style: { minWidth: `${this.addonOptions.minWidth}px` }
      });
      if (level > 0) {
        menuElm.classList.add("slick-submenu");
        if (subMenuId) {
          menuElm.dataset.subMenuParent = subMenuId;
        }
        if (this.gridOptions?.darkMode) {
          menuElm.classList.add("slick-dark-mode");
        }
      }
      const commandMenuElm = createDomElement("div", { className: `${this._menuCssPrefix}-command-list`, role: "menu" }, menuElm);
      const callbackArgs = {
        grid: this.grid,
        column: columnDef,
        level,
        menu: { commandItems }
      };
      if (item && level > 0) {
        this.addSubMenuTitleWhenExists(item, commandMenuElm);
      }
      this.populateCommandOrOptionItems("command", this.addonOptions, commandMenuElm, commandItems, callbackArgs, triggeredByElm, this.handleMenuItemCommandClick, this.handleMenuItemMouseOver);
      level++;
      return menuElm;
    }
    /**
     * Reset all the internal Menu options which have text to translate
     * @param header menu object
     */
    resetHeaderMenuTranslations(columns) {
      columns.forEach((columnDef) => {
        if (columnDef?.header?.menu?.commandItems && !columnDef.excludeFromHeaderMenu) {
          const columnHeaderMenuItems = columnDef.header.menu.commandItems || [];
          this.extensionUtility.translateMenuItemsFromTitleKey(columnHeaderMenuItems);
        }
      });
    }
    /** Sort the current column */
    sortColumn(event, args, isSortingAsc = true) {
      if (args?.column) {
        const columnDef = args.column;
        const tmpSortedColumns = !this.sharedService.gridOptions.multiColumnSort ? [] : this.sortService.getCurrentColumnSorts(columnDef.id + "");
        let emitterType = "local";
        tmpSortedColumns.push({ columnId: columnDef.id, sortCol: columnDef, sortAsc: isSortingAsc });
        if (this.sharedService.gridOptions.backendServiceApi) {
          this.sortService.onBackendSortChanged(event, {
            multiColumnSort: true,
            sortCols: tmpSortedColumns,
            grid: this.grid
          });
          emitterType = "remote";
        } else if (this.sharedService.dataView) {
          this.sortService.onLocalSortChanged(this.grid, tmpSortedColumns);
          emitterType = "local";
        } else {
          args.grid.onSort.notify(tmpSortedColumns);
        }
        const newSortColumns = tmpSortedColumns.map((col) => {
          return {
            columnId: col?.sortCol?.id ?? "",
            sortAsc: col?.sortAsc ?? true
          };
        });
        this.grid.setSortColumns(newSortColumns);
        if (emitterType) {
          const currentLocalSorters = [];
          newSortColumns.forEach((sortCol) => {
            currentLocalSorters.push({
              columnId: `${sortCol.columnId}`,
              direction: sortCol.sortAsc ? "ASC" : "DESC"
            });
          });
          this.sortService.emitSortChanged(emitterType, currentLocalSorters);
        }
      }
    }
    recreateHeaderMenu(newColumns) {
      this.sharedService.gridOptions.headerMenu = this.addHeaderMenuCustomCommands(newColumns);
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/slickRowBasedEdit.js
  var ROW_BASED_EDIT_ROW_HIGHLIGHT_CLASS = "slick-rbe-editmode";
  var ROW_BASED_EDIT_UNSAVED_CELL = "slick-rbe-unsaved-cell";
  var ROW_BASED_EDIT_UNSAVED_HIGHLIGHT_PREFIX = "slick-rbe-unsaved-highlight";
  var BTN_ACTION_DELETE = "action-btns--delete";
  var BTN_ACTION_EDIT = "action-btns--edit";
  var BTN_ACTION_UPDATE = "action-btns--update";
  var BTN_ACTION_CANCEL = "action-btns--cancel";
  var SlickRowBasedEdit = class {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(extensionUtility, pubSubService, options) {
      this.extensionUtility = extensionUtility;
      this.pubSubService = pubSubService;
      this.pluginName = "RowBasedEdit";
      this._defaults = {
        actionsColumnLabel: "Actions",
        allowMultipleRows: false,
        columnId: "_slick_rowbasededit_action",
        columnIndexPosition: -1,
        reorderable: false
      };
      this._editedRows = /* @__PURE__ */ new Map();
      this._currentLang = "en";
      this._translations = {};
      this.onBeforeEditCellHandler = (_e, args) => {
        return this._editedRows.has(args.item?.[this.gridOptions.datasetIdPropertyName ?? "id"]);
      };
      this._eventHandler = new SlickEventHandler();
      this._addonOptions = options;
    }
    get addonOptions() {
      return this._addonOptions;
    }
    get gridOptions() {
      return this._grid.getOptions() || {};
    }
    get eventHandler() {
      return this._eventHandler;
    }
    /** Initialize plugin. */
    init(grid, gridService) {
      this._grid = grid;
      this._gridService = gridService;
      this._addonOptions = { ...this._defaults, ...this.addonOptions };
      const dataView = this._grid.getData();
      this._eventHandler.subscribe(this._grid.onBeforeEditCell, this.onBeforeEditCellHandler);
      this.checkOptionsRequirements(this.gridOptions);
      if (!this.gridOptions.autoEdit) {
        this._grid.setOptions({ autoEdit: true });
        console.warn('[Slickgrid-Universal] The Row Based Edit Plugin works best with the gridOption "autoEdit" enabled, the option has now been set automatically for you.');
      }
      this._existingEditCommandHandler = this.gridOptions.editCommandHandler;
      this._grid.setOptions({
        editCommandHandler: this.rowBasedEditCommandHandler.bind(this)
      });
      if (this.gridOptions.enableExcelCopyBuffer === true) {
        const existingBeforePasteCellHandler = this.gridOptions.excelCopyBufferOptions?.onBeforePasteCell;
        this._grid.setOptions({
          excelCopyBufferOptions: {
            ...this.gridOptions.excelCopyBufferOptions,
            onBeforePasteCell: (e2, args) => {
              let userResult = true;
              if (existingBeforePasteCellHandler) {
                userResult = existingBeforePasteCellHandler(e2, args);
                if (userResult === false) {
                  return false;
                }
              }
              const item = dataView.getItem(args.row);
              const idProperty = this.gridOptions.datasetIdPropertyName ?? "id";
              if (this._editedRows.has(item[idProperty]) && userResult === true) {
                return true;
              }
              return false;
            }
          }
        });
      }
      const originalGetItemMetadata = dataView.getItemMetadata;
      dataView.getItemMetadata = this.updateItemMetadata(originalGetItemMetadata?.bind?.(dataView));
      this._eventHandler.subscribe(this._grid.onSetOptions, this.optionsUpdatedHandler.bind(this));
      this._eventHandler.subscribe(dataView.onRowsOrCountChanged, this.handleAllRowRerender.bind(this));
      this.translate();
    }
    destroy() {
      this.dispose();
    }
    /** Dispose (destroy) the SlickGrid 3rd party plugin */
    dispose() {
      this._eventHandler?.unsubscribeAll();
      this.pubSubService?.unsubscribeAll();
    }
    create(columns, gridOptions) {
      this._addonOptions = {
        ...this._defaults,
        ...gridOptions.rowBasedEditOptions
      };
      if (Array.isArray(columns) && gridOptions) {
        const selectionColumn = this.getColumnDefinition();
        if (!columns.some((col) => col.id === selectionColumn.id)) {
          const columnPosition = gridOptions?.rowBasedEditOptions?.columnIndexPosition ?? -1;
          if (columnPosition === -1) {
            columns.push(selectionColumn);
          } else if (columnPosition > 0 && columnPosition < columns.length) {
            columns.splice(columnPosition, 0, selectionColumn);
          } else {
            columns.unshift(selectionColumn);
          }
          this.pubSubService.publish(`onPluginColumnsChanged`, {
            columns,
            pluginName: this.pluginName
          });
        }
      }
      return this;
    }
    getColumnDefinition() {
      const columnId = String(this._addonOptions?.columnId ?? this._defaults.columnId);
      return {
        id: columnId,
        name: this._addonOptions?.actionsColumnLabel,
        field: "action",
        minWidth: 70,
        width: 75,
        maxWidth: 75,
        excludeFromExport: true,
        reorderable: this._addonOptions?.reorderable,
        formatter: this.actionColumnFormatter.bind(this),
        onCellClick: this.onCellClickHandler.bind(this),
        ...this._addonOptions?.actionColumnConfig ?? {}
      };
    }
    rowBasedEditCommandHandler(item, column, editCommand) {
      if (this._existingEditCommandHandler) {
        this._existingEditCommandHandler(item, column, editCommand);
      }
      const prevSerializedValues = Array.isArray(editCommand.prevSerializedValue) ? editCommand.prevSerializedValue : [editCommand.prevSerializedValue];
      const serializedValues = Array.isArray(editCommand.serializedValue) ? editCommand.serializedValue : [editCommand.serializedValue];
      const editorColumns = this._gridService?.getAllColumnDefinitions().filter((col) => col.editor !== void 0);
      const modifiedColumns = [];
      const idProperty = this.gridOptions.datasetIdPropertyName ?? "id";
      prevSerializedValues.forEach((_val, index2) => {
        const prevSerializedValue = prevSerializedValues[index2];
        const serializedValue = serializedValues[index2];
        if (prevSerializedValue !== serializedValue || serializedValue === "") {
          const finalColumn = Array.isArray(editCommand.prevSerializedValue) ? editorColumns?.[index2] : column;
          if (!finalColumn) {
            return;
          }
          this._grid.invalidate();
          editCommand.execute();
          this.renderUnsavedCellStyling(item[idProperty], finalColumn);
          modifiedColumns.push(finalColumn);
        }
      });
      const editedRow = this._editedRows.get(item[idProperty]);
      const newCommands = [...editedRow?.editCommands || []];
      if (modifiedColumns.length > 0) {
        newCommands.push(editCommand);
      }
      this._editedRows.set(item[idProperty], {
        columns: [...editedRow?.columns || [], ...modifiedColumns],
        editCommands: newCommands,
        cssStyleKeys: editedRow?.cssStyleKeys || []
      });
    }
    /**
     * Translate with current locale when `enableTranslate` is set or use title texts provided by user.
     * We want to translate only once to avoid having to translate on each dataset item, we'll just reuse the translations if it was already translated earlier.
     * User could optionally force a retranslate even if it was already translated
     * @param {Boolean} [forceRetranslate] - even if it was already translate, force a retranslate
     */
    translate(forceRetranslate = false) {
      this._currentLang = this.extensionUtility.translaterService?.getCurrentLanguage() ?? "en";
      if (!this._translations[this._currentLang] || forceRetranslate) {
        this._translations[this._currentLang] = {
          btnUpdateTitle: this.getTitleOrDefault("updateButtonTitle", "Update the row"),
          btnEditTitle: this.getTitleOrDefault("editButtonTitle", "Edit the Row"),
          btnDeleteTitle: this.getTitleOrDefault("deleteButtonTitle", "Delete the Row"),
          btnCancelTitle: this.getTitleOrDefault("cancelButtonTitle", "Cancel changes of the Row")
        };
      }
      return this._translations[this._currentLang];
    }
    checkOptionsRequirements(options) {
      if (!options?.enableCellNavigation) {
        throw new Error(`[Slickgrid-Universal] Row Based Edit Plugin requires the gridOption cell navigation (enableCellNavigation = true)`);
      }
      if (!options?.editable) {
        throw new Error(`[Slickgrid-Universal] Row Based Edit Plugin requires the gridOption editable (editable = true)`);
      }
    }
    undoRowEdit(item) {
      const idProperty = this.gridOptions.datasetIdPropertyName ?? "id";
      const targetRow = this._editedRows.get(item[idProperty]);
      const row = this._grid.getData().getRowByItem(item);
      if (row !== void 0 && targetRow?.editCommands && targetRow.editCommands.length || SlickGlobalEditorLock.cancelCurrentEdit()) {
        while (targetRow.editCommands.length > 0) {
          const lastEdit = targetRow.editCommands.pop();
          if (lastEdit) {
            lastEdit.undo();
          }
        }
        targetRow.columns.forEach((column) => {
          this.removeUnsavedStylingFromCell(column, row);
        });
        targetRow.columns = [];
        this._grid.invalidate();
      }
    }
    renderUnsavedCellStyling(id, column) {
      if (column) {
        const row = this._grid.getData()?.getRowById(id);
        if (row !== void 0 && row >= 0) {
          const hash = { [row]: { [column.id]: ROW_BASED_EDIT_UNSAVED_CELL } };
          const cssStyleKey = `${ROW_BASED_EDIT_UNSAVED_HIGHLIGHT_PREFIX}_${[column.id]}${row}`;
          this._grid.setCellCssStyles(cssStyleKey, hash);
          this._editedRows.get(id)?.cssStyleKeys.push(cssStyleKey);
        }
      }
    }
    handleAllRowRerender(_e, _args) {
      this._editedRows.forEach((editedRow, key) => {
        editedRow.cssStyleKeys.forEach((cssStyleKey) => {
          this._grid.removeCellCssStyles(cssStyleKey);
        });
        editedRow.cssStyleKeys = [];
        editedRow.columns.forEach((column) => {
          this.renderUnsavedCellStyling(key, column);
        });
      });
    }
    removeUnsavedStylingFromCell(column, row) {
      const cssStyleKey = `${ROW_BASED_EDIT_UNSAVED_HIGHLIGHT_PREFIX}_${[column.id]}${row}`;
      this._grid.removeCellCssStyles(cssStyleKey);
    }
    removeUnsavedStylingFromRow(row) {
      this._grid.getColumns().forEach((column) => {
        this.removeUnsavedStylingFromCell(column, row);
      });
    }
    optionsUpdatedHandler(_e, args) {
      this._addonOptions = {
        ...this._defaults,
        ...args.optionsAfter.rowBasedEditOptions
      };
    }
    async onCellClickHandler(event, args) {
      const dataContext = args.dataContext;
      const target = event.target;
      const idProperty = this.gridOptions.datasetIdPropertyName ?? "id";
      const targetRow = this._editedRows.get(dataContext[idProperty]);
      if ((target.classList.contains(BTN_ACTION_DELETE) || target.parentElement?.classList.contains(BTN_ACTION_DELETE)) && this._gridService) {
        if (this._addonOptions?.actionButtons?.deleteButtonPrompt && !window.confirm(this._addonOptions.actionButtons.deleteButtonPrompt)) {
          return;
        }
        this.toggleEditmode(dataContext, false);
        this._gridService.deleteItem(dataContext);
        if (typeof this._addonOptions?.onAfterDelete === "function") {
          this._addonOptions.onAfterDelete(args);
        }
      } else if (target.classList.contains(BTN_ACTION_EDIT) || target.parentElement?.classList.contains(BTN_ACTION_EDIT)) {
        if (!this._addonOptions?.allowMultipleRows && this._editedRows.size > 0) {
          return;
        }
        if (typeof this._addonOptions?.onBeforeEditMode === "function") {
          this._addonOptions.onBeforeEditMode(args);
        }
        this.toggleEditmode(dataContext, true);
      } else if (target.classList.contains(BTN_ACTION_UPDATE) || target.parentElement?.classList.contains(BTN_ACTION_UPDATE)) {
        if (this._addonOptions?.actionButtons?.updateButtonPrompt && (targetRow?.editCommands.length || 0) > 0 && !window.confirm(this._addonOptions.actionButtons.updateButtonPrompt)) {
          return;
        }
        if (this._grid.getCellEditor() && this._grid.getActiveCell()?.row === args.row) {
          this._grid.getEditController()?.commitCurrentEdit();
        }
        if (this._addonOptions?.onBeforeRowUpdated) {
          const result = await this._addonOptions.onBeforeRowUpdated(args);
          if (result !== true) {
            return;
          }
        }
        this.removeUnsavedStylingFromRow(args.row);
        this.toggleEditmode(dataContext, false);
      } else if (target.classList.contains(BTN_ACTION_CANCEL) || target.parentElement?.classList.contains(BTN_ACTION_CANCEL)) {
        if (this._addonOptions?.actionButtons?.cancelButtonPrompt && (targetRow?.editCommands.length || 0) > 0 && !window.confirm(this._addonOptions.actionButtons.cancelButtonPrompt)) {
          return;
        }
        this.undoRowEdit(dataContext);
        this.toggleEditmode(dataContext, false);
      }
    }
    actionColumnFormatter(_row, _cell, _value, _columnDef, dataContext) {
      const options = this.gridOptions;
      const isInEditMode = this._editedRows.has(dataContext?.[options.datasetIdPropertyName ?? "id"]);
      const buttonTitles = this._translations[this._currentLang] ?? this.translate();
      const actionFragment = document.createDocumentFragment();
      actionFragment.appendChild(createDomElement("span", {
        className: `${options.rowBasedEditOptions?.actionButtons?.editButtonClassName || "button-style padding-1px mr-2"} action-btns ` + BTN_ACTION_EDIT,
        title: buttonTitles.btnEditTitle,
        style: { display: isInEditMode ? "none" : "" }
      })).appendChild(createDomElement("span", {
        className: options.rowBasedEditOptions?.actionButtons?.iconEditButtonClassName || "mdi mdi-table-edit color-primary"
      }));
      actionFragment.appendChild(createDomElement("span", {
        className: `${options.rowBasedEditOptions?.actionButtons?.deleteButtonClassName || "button-style padding-1px"} action-btns ` + BTN_ACTION_DELETE,
        title: buttonTitles.btnDeleteTitle,
        style: { display: isInEditMode ? "none" : "" }
      })).appendChild(createDomElement("span", {
        className: options.rowBasedEditOptions?.actionButtons?.iconDeleteButtonClassName || "mdi mdi-close color-danger"
      }));
      actionFragment.appendChild(createDomElement("span", {
        className: `${options.rowBasedEditOptions?.actionButtons?.updateButtonClassName || "button-style padding-1px mr-2"} action-btns ` + BTN_ACTION_UPDATE,
        title: buttonTitles.btnUpdateTitle,
        style: { display: !isInEditMode ? "none" : "" }
      })).appendChild(createDomElement("span", {
        className: options.rowBasedEditOptions?.actionButtons?.iconUpdateButtonClassName || "mdi mdi-check-bold color-success"
      }));
      actionFragment.appendChild(createDomElement("span", {
        className: `${options.rowBasedEditOptions?.actionButtons?.cancelButtonClassName || "button-style padding-1px"} action-btns ` + BTN_ACTION_CANCEL,
        title: buttonTitles.btnCancelTitle,
        style: { display: !isInEditMode ? "none" : "" }
      })).appendChild(createDomElement("span", {
        className: options.rowBasedEditOptions?.actionButtons?.iconCancelButtonClassName || "mdi mdi-cancel color-danger"
      }));
      return actionFragment;
    }
    toggleEditmode(dataContext, editMode) {
      const idProperty = this.gridOptions.datasetIdPropertyName ?? "id";
      if (editMode) {
        this._editedRows.set(dataContext[idProperty], {
          columns: [],
          editCommands: [],
          cssStyleKeys: []
        });
      } else {
        this._editedRows.delete(dataContext[idProperty]);
      }
      this._grid.invalidate();
    }
    updateItemMetadata(previousItemMetadata) {
      return (rowNumber) => {
        const item = this._grid.getData().getItem(rowNumber);
        let meta = {
          cssClasses: ""
        };
        if (typeof previousItemMetadata === "function") {
          const previousMeta = previousItemMetadata(rowNumber);
          if (previousMeta) {
            meta = previousMeta;
          }
        }
        if (meta && item) {
          const idProperty = this.gridOptions.datasetIdPropertyName ?? "id";
          if (this._editedRows.has(item[idProperty]) && !meta.cssClasses.includes(ROW_BASED_EDIT_ROW_HIGHLIGHT_CLASS)) {
            meta.cssClasses = (meta.cssClasses || "") + " " + ROW_BASED_EDIT_ROW_HIGHLIGHT_CLASS;
          } else if (!this._editedRows.has(item[idProperty]) && meta.cssClasses.includes(ROW_BASED_EDIT_ROW_HIGHLIGHT_CLASS)) {
            meta.cssClasses = meta.cssClasses.replace(ROW_BASED_EDIT_ROW_HIGHLIGHT_CLASS, "");
          }
        }
        return meta;
      };
    }
    /**
     * Get button tooltip text 1 of these 3 options:
     * 1. from `btnAbcTitleKey` when `enableTranslate` is set
     * 2. from `btnAbcTitle` when no title Key is found and/or `enableTranslate` is disabled
     * 3. or finally use default title fallback provided
     * @param key - button title key
     * @param defaultTitle - fallback title
     * @returns - final tooltip title text
     */
    getTitleOrDefault(key, defaultTitle) {
      const actionBtnOptions = this.gridOptions.rowBasedEditOptions?.actionButtons;
      return actionBtnOptions?.[key + "Key"] && this.extensionUtility.translaterService?.translate?.(actionBtnOptions?.[key + "Key"] || "") || actionBtnOptions?.[key] || defaultTitle;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/extensions/rowMoveUtils.js
  function defaultOnBeforeMoveRows(e2, args) {
    const dataView = args.grid.getData();
    if (dataView?.getItemCount) {
      const itemCount = dataView.getItemCount();
      for (const rowIdx of args.rows) {
        if (rowIdx === args.insertBefore || rowIdx === args.insertBefore - 1 && args.insertBefore - 1 !== itemCount) {
          e2.stopPropagation();
          return false;
        }
      }
      return true;
    }
    return false;
  }
  function defaultOnMoveRows(_e, args) {
    const dataView = args.grid.getData();
    if (dataView?.getItemCount) {
      const rows = args.rows;
      const insertBefore = args.insertBefore;
      const extractedRows = [];
      dataView?.sort(void 0, true);
      const tmpDataset = dataView?.getItems();
      const filteredItems = dataView?.getFilteredItems();
      const itemOnRight = dataView?.getItem(insertBefore);
      const insertBeforeFilteredIdx = itemOnRight ? dataView?.getIdxById(itemOnRight.id) : dataView?.getItemCount();
      const filteredRowItems = [];
      rows.forEach((row) => filteredRowItems.push(filteredItems[row]));
      const filteredRows = filteredRowItems.map((item) => dataView?.getIdxById(item.id));
      const left = tmpDataset.slice(0, insertBeforeFilteredIdx);
      const right = tmpDataset.slice(insertBeforeFilteredIdx, tmpDataset.length);
      rows.sort((a, b) => a - b);
      for (const filteredRow of filteredRows) {
        extractedRows.push(tmpDataset[filteredRow]);
      }
      filteredRows.reverse();
      for (const row of filteredRows) {
        if (row < insertBeforeFilteredIdx) {
          left.splice(row, 1);
        } else {
          right.splice(row - insertBeforeFilteredIdx, 1);
        }
      }
      args.grid?.resetActiveCell();
      const finalDataset = left.concat(extractedRows.concat(right));
      dataView.setItems(finalDataset);
      args.grid.invalidate();
    } else {
      console.error("Sorry `defaultOnMoveRows()` only works with SlickDataView");
    }
  }

  // node_modules/@slickgrid-universal/common/dist/extensions/slickRowMoveManager.js
  var SlickRowMoveManager = class {
    /** Constructor of the SlickGrid 3rd party plugin, it can optionally receive options */
    constructor(pubSubService) {
      this.pubSubService = pubSubService;
      this.pluginName = "RowMoveManager";
      this._dragging = false;
      this._defaults = {
        autoScroll: true,
        columnId: "_move",
        cssClass: "slick-row-move-column",
        cancelEditOnDrag: false,
        disableRowSelection: false,
        hideRowMoveShadow: true,
        reorderable: false,
        rowMoveShadowMarginTop: 0,
        rowMoveShadowMarginLeft: 0,
        rowMoveShadowOpacity: 0.9,
        rowMoveShadowScale: 0.75,
        singleRowMove: false,
        width: 44
      };
      this.onBeforeMoveRows = new SlickEvent("onBeforeMoveRows");
      this.onMoveRows = new SlickEvent("onMoveRows");
      this._eventHandler = new SlickEventHandler();
    }
    get addonOptions() {
      return this._addonOptions;
    }
    get eventHandler() {
      return this._eventHandler;
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    /** Initialize plugin. */
    init(grid, options) {
      this._addonOptions = { ...this._defaults, ...options };
      this._grid = grid;
      this._canvas = this._grid.getCanvasNode();
      Utils.addSlickEventPubSubWhenDefined(this.pubSubService, this);
      if (typeof this._addonOptions?.usabilityOverride === "function") {
        this.usabilityOverride(this._addonOptions.usabilityOverride);
      }
      this._eventHandler.subscribe(this._grid.onDragInit, this.handleDragInit.bind(this)).subscribe(this._grid.onDragStart, this.handleDragStart.bind(this)).subscribe(this._grid.onDrag, this.handleDrag.bind(this)).subscribe(this._grid.onDragEnd, this.handleDragEnd.bind(this));
    }
    destroy() {
      this.dispose();
    }
    /** Dispose (destroy) the SlickGrid 3rd party plugin */
    dispose() {
      this._eventHandler?.unsubscribeAll();
    }
    /**
     * Create the plugin before the Grid creation to avoid having odd behaviors.
     * Mostly because the column definitions might change after the grid creation, so we want to make sure to add it before then
     */
    create(columns, gridOptions) {
      this._addonOptions = { ...this._defaults, ...gridOptions.rowMoveManager };
      if (Array.isArray(columns) && gridOptions) {
        const newRowMoveColumn = this.getColumnDefinition();
        if (!columns.some((col) => col.id === newRowMoveColumn.id)) {
          const rowMoveColDef = Array.isArray(columns) && columns.find((col) => col?.behavior === "selectAndMove");
          const finalRowMoveColumn = rowMoveColDef ? rowMoveColDef : newRowMoveColumn;
          const columnPosition = gridOptions?.rowMoveManager?.columnIndexPosition ?? 0;
          if (columnPosition > 0) {
            columns.splice(columnPosition, 0, finalRowMoveColumn);
          } else {
            columns.unshift(finalRowMoveColumn);
          }
          this.pubSubService.publish(`onPluginColumnsChanged`, {
            columns,
            pluginName: this.pluginName
          });
        }
      }
      return this;
    }
    getColumnDefinition() {
      const columnId = String(this._addonOptions?.columnId ?? this._defaults.columnId);
      const columnWidth = this._addonOptions.width || this._defaults.width;
      return {
        id: columnId,
        name: "",
        behavior: "selectAndMove",
        excludeFromExport: true,
        excludeFromColumnPicker: true,
        excludeFromGridMenu: true,
        excludeFromQuery: true,
        excludeFromHeaderMenu: true,
        field: columnId,
        reorderable: this._addonOptions.reorderable,
        resizable: false,
        width: columnWidth,
        minWidth: columnWidth,
        maxWidth: columnWidth,
        formatter: this.moveIconFormatter.bind(this)
      };
    }
    /**
     * Method that user can pass to override the default behavior or making every row moveable.
     * In order word, user can choose which rows to be an available as moveable (or not) by providing his own logic show/hide icon and usability.
     * @param overrideFn: override function callback
     */
    usabilityOverride(overrideFn) {
      this._usabilityOverride = overrideFn;
    }
    setOptions(newOptions) {
      this._addonOptions = { ...this._addonOptions, ...newOptions };
    }
    isHandlerColumn(columnIndex) {
      return /move|selectAndMove/.test(this._grid.getColumnByIdx(+columnIndex)?.behavior || "");
    }
    // --
    // protected functions
    // ------------------
    handleDragInit(e2) {
      e2.stopImmediatePropagation();
    }
    handleDragEnd(e2, dd) {
      if (!this._dragging) {
        return;
      }
      this._dragging = false;
      e2.stopImmediatePropagation();
      dd.guide?.remove();
      dd.selectionProxy?.remove();
      dd.clonedSlickRow?.remove();
      if (dd.canMove) {
        const eventData = {
          grid: this._grid,
          rows: dd.selectedRows,
          insertBefore: dd.insertBefore
        };
        const evt = e2 instanceof SlickEventData ? e2.getNativeEvent() : e2;
        typeof this._addonOptions.onMoveRows === "function" ? this._addonOptions.onMoveRows(evt, eventData) : defaultOnMoveRows(evt, eventData);
        this.onMoveRows.notify(eventData);
        if (typeof this._addonOptions.onAfterMoveRows === "function") {
          const updatedItems = this._grid.hasDataView() ? this._grid.getData().getItems() : this._grid.getData();
          this._addonOptions.onAfterMoveRows(evt, { ...eventData, updatedItems });
        }
      }
    }
    handleDrag(evt, dd) {
      if (this._dragging) {
        evt.stopImmediatePropagation();
        const e2 = evt.getNativeEvent();
        const targetEvent = e2?.touches?.[0] ?? e2;
        const top = targetEvent.pageY - getOffset(this._canvas).top;
        dd.selectionProxy.style.top = `${top - 5}px`;
        dd.selectionProxy.style.display = "block";
        if (dd.clonedSlickRow) {
          dd.clonedSlickRow.style.top = `${top - 6}px`;
          dd.clonedSlickRow.style.display = "block";
        }
        const insertBefore = Math.max(0, Math.min(Math.round(top / (this.gridOptions.rowHeight || 0)), this._grid.getDataLength()));
        if (insertBefore !== dd.insertBefore) {
          const eventData = {
            grid: this._grid,
            rows: dd.selectedRows,
            insertBefore
          };
          const evt2 = e2 instanceof SlickEventData ? e2.getNativeEvent() : e2;
          let beforeMoveRowResult = typeof this._addonOptions.onBeforeMoveRows === "function" ? this._addonOptions.onBeforeMoveRows(evt2, eventData) : defaultOnBeforeMoveRows(evt2, eventData);
          dd.canMove = !(beforeMoveRowResult === false || this.onBeforeMoveRows.notify(eventData).getReturnValue() === false);
          if (this._usabilityOverride && dd.canMove) {
            const insertBeforeDataContext = this._grid.getDataItem(insertBefore);
            dd.canMove = this.checkUsabilityOverride(insertBefore, insertBeforeDataContext, this._grid);
          }
          if (!dd.canMove) {
            dd.guide.style.top = "-1000px";
          } else {
            dd.guide.style.top = `${insertBefore * (this.gridOptions.rowHeight || 0)}px`;
          }
          dd.insertBefore = insertBefore;
        }
      }
    }
    handleDragStart(event, dd) {
      const cell = this._grid.getCellFromEvent(event);
      if (isDefined(cell)) {
        const dataContext = this._grid.getDataItem(cell.row);
        if (this.checkUsabilityOverride(cell.row, dataContext, this._grid)) {
          if (this._addonOptions.cancelEditOnDrag && this._grid.getEditorLock().isActive()) {
            this._grid.getEditorLock().cancelCurrentEdit();
          }
          if (this._grid.getEditorLock().isActive() || !this.isHandlerColumn(cell.cell)) {
            return false;
          }
          this._dragging = true;
          event.stopImmediatePropagation();
          if (!this.addonOptions.hideRowMoveShadow) {
            const slickRowElm = this._grid.getCellNode(cell.row, cell.cell)?.closest(".slick-row");
            if (slickRowElm) {
              dd.clonedSlickRow = slickRowElm.cloneNode(true);
              dd.clonedSlickRow.classList.add("slick-reorder-shadow-row");
              dd.clonedSlickRow.style.display = "none";
              dd.clonedSlickRow.style.marginLeft = findWidthOrDefault(this._addonOptions?.rowMoveShadowMarginLeft, "0px");
              dd.clonedSlickRow.style.marginTop = findWidthOrDefault(this._addonOptions?.rowMoveShadowMarginTop, "0px");
              dd.clonedSlickRow.style.opacity = `${this._addonOptions?.rowMoveShadowOpacity ?? 0.95}`;
              dd.clonedSlickRow.style.transform = `scale(${this.addonOptions?.rowMoveShadowScale ?? 0.75})`;
              this._canvas.appendChild(dd.clonedSlickRow);
            }
          }
          let selectedRows = this._addonOptions.singleRowMove ? [cell.row] : this._grid.getSelectedRows();
          if (selectedRows.length === 0 || !selectedRows.some((selectedRow) => selectedRow === cell.row)) {
            selectedRows = [cell.row];
            if (!this._addonOptions.disableRowSelection) {
              this._grid.setSelectedRows(selectedRows);
            }
          }
          const rowHeight = this.gridOptions.rowHeight;
          dd.selectedRows = selectedRows;
          dd.selectionProxy = createDomElement("div", {
            className: "slick-reorder-proxy",
            style: {
              display: "none",
              position: "absolute",
              zIndex: "99999",
              width: `${this._canvas.clientWidth}px`,
              height: `${rowHeight * selectedRows.length}px`
            }
          }, this._canvas);
          dd.guide = createDomElement("div", {
            className: "slick-reorder-guide",
            style: {
              position: "absolute",
              zIndex: "99999",
              width: `${this._canvas.clientWidth}px`,
              top: `-1000px`
            }
          }, this._canvas);
          dd.insertBefore = -1;
        }
      }
    }
    checkUsabilityOverride(row, dataContext, grid) {
      if (typeof this._usabilityOverride === "function") {
        return this._usabilityOverride(row, dataContext, grid);
      }
      return true;
    }
    moveIconFormatter(row, _cell, _val, _col, dataContext, grid) {
      if (!this.checkUsabilityOverride(row, dataContext, grid)) {
        return "";
      } else {
        return {
          addClasses: `cell-reorder dnd`,
          html: createDomElement("div", { className: `sgi ${this._addonOptions.cssClass || ""}` })
        };
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/extension.service.js
  var ExtensionService = class {
    get extensionList() {
      return this._extensionList;
    }
    get gridOptions() {
      return this.sharedService.gridOptions || {};
    }
    constructor(extensionUtility, filterService, pubSubService, sharedService, sortService, treeDataService, translaterService, lazyGridService) {
      this.extensionUtility = extensionUtility;
      this.filterService = filterService;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this.sortService = sortService;
      this.treeDataService = treeDataService;
      this.translaterService = translaterService;
      this.lazyGridService = lazyGridService;
      this._extensionCreatedList = {};
      this._extensionList = {};
      this._requireInitExternalExtensions = [];
    }
    /** Dispose of all the controls & plugins */
    dispose() {
      if (typeof this._extensionList === "object") {
        const extensionNames = Object.keys(this._extensionList);
        extensionNames.forEach((extensionName) => {
          if (this._extensionList.hasOwnProperty(extensionName)) {
            const extension = this._extensionList[extensionName];
            if (typeof extension?.instance?.dispose === "function") {
              extension.instance.dispose();
            }
          }
        });
        extensionNames.forEach((key) => {
          delete this._extensionList[key];
        });
      }
      this._cellMenuPlugin = null;
      this._cellExcelCopyManagerPlugin = null;
      this._checkboxSelectColumn = null;
      this._contextMenuPlugin = null;
      this._columnPickerControl = null;
      this._draggleGroupingPlugin = null;
      this._gridMenuControl = null;
      this._groupItemMetadataProviderService = null;
      this._headerMenuPlugin = null;
      this._rowMoveManagerPlugin = null;
      this._selectionModel = null;
      this._extensionCreatedList = null;
      this._extensionList = {};
    }
    /**
     * Get an external plugin Extension
     * @param {String} name
     * @param {Object} extension
     */
    addExtensionToList(name, extension) {
      this._extensionList[name] = extension;
    }
    /** Get all columns (includes visible and non-visible) */
    getAllColumns() {
      return this.sharedService.allColumns || [];
    }
    /** Get only visible columns */
    getVisibleColumns() {
      return this.sharedService.slickGrid?.getVisibleColumns();
    }
    /**
     * Get an Extension that was created by calling its "create" method (there are only 3 extensions which uses this method)
     *  @param name
     */
    getCreatedExtensionByName(name) {
      if (this._extensionCreatedList?.hasOwnProperty(name)) {
        return this._extensionCreatedList[name];
      }
      return void 0;
    }
    /**
     * Get an Extension by it's name.
     * NOTE: it's preferable to @use `getExtensionInstanceByName` if you just want the instance since it will automatically infer the extension.
     * @param name
     */
    getExtensionByName(name) {
      return this._extensionList?.[name];
    }
    /**
     * Get Extension Instance by its name.
     * @param name
     */
    getExtensionInstanceByName(name) {
      return this.getExtensionByName(name)?.instance;
    }
    /** Auto-resize all the column in the grid to fit the grid width */
    autoResizeColumns() {
      this.sharedService.slickGrid?.autosizeColumns();
    }
    /** Bind/Create different Controls or Plugins after the Grid is created */
    bindDifferentExtensions() {
      if (this.gridOptions && this.sharedService.slickGrid) {
        if (this.gridOptions.enableTranslate) {
          this.translateItems(this.sharedService.allColumns, "nameKey", "name");
        }
        if (this.gridOptions.enableRowBasedEdit) {
          this._rowBasedEdit = this._rowBasedEdit || new SlickRowBasedEdit(this.extensionUtility, this.pubSubService, this.gridOptions.rowBasedEditOptions);
          const gridService = this.lazyGridService?.();
          if (!gridService) {
            throw new Error("[Slickgrid-Universal] the RowBasedEdit Plugin requires a GridService to be configured and available");
          }
          this._rowBasedEdit.init(this.sharedService.slickGrid, gridService);
          if (this.gridOptions.rowBasedEditOptions?.onExtensionRegistered) {
            this.gridOptions.rowBasedEditOptions.onExtensionRegistered(this._rowBasedEdit);
          }
          this._extensionList["rowBasedEdit"] = { name: "rowBasedEdit", instance: this._rowBasedEdit };
        }
        if (this.gridOptions.enableAutoTooltip) {
          const instance = new SlickAutoTooltip(this.gridOptions?.autoTooltipOptions);
          this.sharedService.slickGrid.registerPlugin(instance);
          this._extensionList["autoTooltip"] = { name: "autoTooltip", instance };
        }
        if (this.gridOptions.enableExcelCopyBuffer) {
          this._cellExcelCopyManagerPlugin = new SlickCellExcelCopyManager();
          this._cellExcelCopyManagerPlugin.init(this.sharedService.slickGrid, this.sharedService.gridOptions.excelCopyBufferOptions);
          if (this.gridOptions.excelCopyBufferOptions?.onExtensionRegistered) {
            this.gridOptions.excelCopyBufferOptions.onExtensionRegistered(this._cellExcelCopyManagerPlugin);
          }
          this._extensionList["cellExternalCopyManager"] = {
            name: "cellExternalCopyManager",
            instance: this._cellExcelCopyManagerPlugin
          };
        }
        if (this.gridOptions.enableCellMenu) {
          this._cellMenuPlugin = new SlickCellMenu(this.extensionUtility, this.pubSubService, this.sharedService);
          if (this.gridOptions.cellMenu?.onExtensionRegistered) {
            this.gridOptions.cellMenu.onExtensionRegistered(this._cellMenuPlugin);
          }
          this._extensionList["cellMenu"] = { name: "cellMenu", instance: this._cellMenuPlugin };
        }
        if (!this._selectionModel && (this.gridOptions.enableSelection || this.gridOptions.enableCheckboxSelector || this.gridOptions.enableRowDetailView || this.gridOptions.enableRowMoveManager)) {
          if (!this._selectionModel || !this.sharedService.slickGrid.getSelectionModel()) {
            const selectionOptions = this.gridOptions.selectionOptions ?? {};
            const selectionType = this.gridOptions.selectionOptions?.selectionType || "row";
            this._selectionModel = new SlickHybridSelectionModel({ ...selectionOptions, selectionType });
            this.sharedService.slickGrid.setSelectionModel(this._selectionModel);
          }
          this._extensionList["hybridSelection"] = { name: "hybridSelection", instance: this._selectionModel };
        }
        if (this.gridOptions.enableCheckboxSelector) {
          this._checkboxSelectColumn = this._checkboxSelectColumn || new SlickCheckboxSelectColumn(this.pubSubService, this.gridOptions.checkboxSelector);
          this._checkboxSelectColumn.init(this.sharedService.slickGrid);
          const createdExtension = this.getCreatedExtensionByName("checkboxSelector");
          const instance = createdExtension?.instance;
          if (instance) {
            if (this.gridOptions.checkboxSelector?.onExtensionRegistered) {
              this.gridOptions.checkboxSelector.onExtensionRegistered(instance);
            }
            this._extensionList["checkboxSelector"] = {
              name: "checkboxSelector",
              instance: this._checkboxSelectColumn
            };
          }
        }
        if (this.gridOptions.enableColumnPicker) {
          this._columnPickerControl = new SlickColumnPicker(this.extensionUtility, this.pubSubService, this.sharedService);
          if (this.gridOptions.columnPicker?.onExtensionRegistered) {
            this.gridOptions.columnPicker.onExtensionRegistered(this._columnPickerControl);
          }
          this._extensionList["columnPicker"] = {
            name: "columnPicker",
            instance: this._columnPickerControl
          };
        }
        if (this.gridOptions.enableContextMenu) {
          this._contextMenuPlugin = new SlickContextMenu(this.extensionUtility, this.pubSubService, this.sharedService, this.treeDataService);
          if (this.gridOptions.contextMenu?.onExtensionRegistered) {
            this.gridOptions.contextMenu.onExtensionRegistered(this._contextMenuPlugin);
          }
          this._extensionList["contextMenu"] = { name: "contextMenu", instance: this._contextMenuPlugin };
        }
        if (this.gridOptions.enableDraggableGrouping) {
          if (this._draggleGroupingPlugin) {
            this._draggleGroupingPlugin.init(this.sharedService.slickGrid, this.gridOptions.draggableGrouping);
            if (this.gridOptions.draggableGrouping?.onExtensionRegistered) {
              this.gridOptions.draggableGrouping.onExtensionRegistered(this._draggleGroupingPlugin);
            }
            this._extensionList["contextMenu"] = {
              name: "contextMenu",
              instance: this._draggleGroupingPlugin
            };
          }
          this._extensionList["draggableGrouping"] = {
            name: "draggableGrouping",
            instance: this._draggleGroupingPlugin
          };
        }
        if (this.gridOptions.enableGridMenu) {
          this._gridMenuControl = new SlickGridMenu(this.extensionUtility, this.filterService, this.pubSubService, this.sharedService, this.sortService);
          if (this.gridOptions.gridMenu?.onExtensionRegistered) {
            this.gridOptions.gridMenu.onExtensionRegistered(this._gridMenuControl);
          }
          this._extensionList["gridMenu"] = { name: "gridMenu", instance: this._gridMenuControl };
        }
        if (this.gridOptions.enableHeaderButton) {
          const headerButtonPlugin = new SlickHeaderButtons(this.extensionUtility, this.pubSubService, this.sharedService);
          if (this.gridOptions.headerButton?.onExtensionRegistered) {
            this.gridOptions.headerButton.onExtensionRegistered(headerButtonPlugin);
          }
          this._extensionList["headerButton"] = { name: "headerButton", instance: headerButtonPlugin };
        }
        if (this.gridOptions.enableHeaderMenu) {
          this._headerMenuPlugin = new SlickHeaderMenu(this.extensionUtility, this.filterService, this.pubSubService, this.sharedService, this.sortService);
          if (this.gridOptions.headerMenu?.onExtensionRegistered) {
            this.gridOptions.headerMenu.onExtensionRegistered(this._headerMenuPlugin);
          }
          this._extensionList["headerMenu"] = { name: "headerMenu", instance: this._headerMenuPlugin };
        }
        if (this.gridOptions.enableRowMoveManager) {
          this._rowMoveManagerPlugin = this._rowMoveManagerPlugin || new SlickRowMoveManager(this.pubSubService);
          this._rowMoveManagerPlugin.init(this.sharedService.slickGrid, this.gridOptions.rowMoveManager);
          const createdExtension = this.getCreatedExtensionByName("rowMoveManager");
          const instance = createdExtension?.instance;
          if (instance) {
            this._extensionList["rowMoveManager"] = {
              name: "rowMoveManager",
              instance: this._rowMoveManagerPlugin
            };
          }
        }
        if (this._requireInitExternalExtensions.length) {
          this._requireInitExternalExtensions.forEach((extension) => {
            extension.instance.init(this.sharedService.slickGrid, void 0);
          });
        }
      }
    }
    /**
     * Bind/Create certain plugins before the Grid creation to avoid having odd behaviors.
     * Mostly because the column definitions might change after the grid creation, so we want to make sure to add it before then
     * @param columns
     * @param gridOptions
     */
    createExtensionsBeforeGridCreation(columns, gridOptions) {
      const featureWithColumnIndexPositions = [];
      if (gridOptions.enableCheckboxSelector) {
        if (!this.getCreatedExtensionByName("checkboxSelector")) {
          this._checkboxSelectColumn = new SlickCheckboxSelectColumn(this.pubSubService, this.sharedService.gridOptions.checkboxSelector);
          featureWithColumnIndexPositions.push({
            name: "checkboxSelector",
            instance: this._checkboxSelectColumn,
            columnIndexPosition: gridOptions?.checkboxSelector?.columnIndexPosition ?? featureWithColumnIndexPositions.length
          });
        }
      }
      if (gridOptions.enableRowMoveManager) {
        if (!this.getCreatedExtensionByName("rowMoveManager")) {
          this._rowMoveManagerPlugin = new SlickRowMoveManager(this.pubSubService);
          featureWithColumnIndexPositions.push({
            name: "rowMoveManager",
            instance: this._rowMoveManagerPlugin,
            columnIndexPosition: gridOptions?.rowMoveManager?.columnIndexPosition ?? featureWithColumnIndexPositions.length
          });
        }
      }
      if (gridOptions.enableRowBasedEdit) {
        if (!this.getCreatedExtensionByName("rowBasedEdit")) {
          this._rowBasedEdit = new SlickRowBasedEdit(this.extensionUtility, this.pubSubService);
          featureWithColumnIndexPositions.push({
            name: "rowBasedEdit",
            instance: this._rowBasedEdit,
            columnIndexPosition: gridOptions?.rowMoveManager?.columnIndexPosition ?? featureWithColumnIndexPositions.length
          });
        }
      }
      if (gridOptions.preRegisterExternalExtensions) {
        const extraExtensions = gridOptions.preRegisterExternalExtensions(this.pubSubService);
        extraExtensions.forEach((extension) => {
          featureWithColumnIndexPositions.push(extension);
          this._requireInitExternalExtensions.push(extension);
          if (!this._extensionList[extension.name]) {
            this.addExtensionToList(extension.name, extension);
          }
        });
      }
      this.createExtensionByTheirColumnIndex(featureWithColumnIndexPositions, columns, gridOptions);
      if (gridOptions.enableDraggableGrouping) {
        if (!this.getCreatedExtensionByName("draggableGrouping")) {
          this._draggleGroupingPlugin = new SlickDraggableGrouping(this.extensionUtility, this.pubSubService, this.sharedService);
          if (this._draggleGroupingPlugin) {
            gridOptions.enableColumnReorder = this._draggleGroupingPlugin.setupColumnReorder.bind(this._draggleGroupingPlugin);
            this._extensionCreatedList["draggableGrouping"] = {
              name: "draggableGrouping",
              instance: this._draggleGroupingPlugin
            };
          }
        }
      }
    }
    /** Hide a column from the grid */
    hideColumn(column) {
      this.sharedService.slickGrid?.updateColumnById(column.id, { hidden: true }, true);
    }
    /** Refresh the dataset through the Backend Service */
    refreshBackendDataset(gridOptions) {
      this.extensionUtility.refreshBackendDataset(gridOptions);
    }
    /**
     * Remove a column from the grid by it's index in the grid
     * @param columns input
     * @param index
     */
    removeColumnByIndex(columns, index2) {
      if (Array.isArray(columns)) {
        return columns.filter((_el, i) => index2 !== i);
      }
      return columns;
    }
    /** Translate all possible Extensions at once */
    translateAllExtensions(lang) {
      this.translateCellMenu();
      this.translateContextMenu();
      this.translateHeaderMenu();
      this.translateRowEditPlugin();
      this.translateColumnHeaders(lang);
    }
    /** Translate the Cell Menu titles, we need to loop through all column definition to re-translate them */
    translateCellMenu() {
      this._cellMenuPlugin?.translateCellMenu();
    }
    /** Translate the Column Picker and it's last 2 checkboxes */
    translateColumnPicker() {
      this._columnPickerControl?.translateColumnPicker();
    }
    /** Translate the Context Menu titles, we need to loop through all column definition to re-translate them */
    translateContextMenu() {
      this._contextMenuPlugin?.translateContextMenu();
    }
    /**
     * Translate the Header Menu titles, we need to loop through all column definition to re-translate them
     */
    translateGridMenu() {
      this._gridMenuControl?.translateGridMenu();
    }
    /**
     * Translate the Header Menu titles, we need to loop through all column definition to re-translate them
     */
    translateHeaderMenu() {
      this._headerMenuPlugin?.translateHeaderMenu();
    }
    /**
     * Translate the action column buttons of the Row Based Edit Plugin
     */
    translateRowEditPlugin() {
      this._rowBasedEdit?.translate();
    }
    /**
     * Translate manually the header titles.
     * We could optionally pass a locale (that will change currently loaded locale), else it will use current locale
     * @param locale to use
     * @param new column definitions (optional)
     */
    translateColumnHeaders(locale, newColumns, shouldUpdateColumns = true) {
      if (this.sharedService && this.gridOptions && this.gridOptions.enableTranslate && (!this.translaterService || !this.translaterService.translate)) {
        throw new Error('[Slickgrid-Universal] requires a Translate Service to be installed and configured when the grid option "enableTranslate" is enabled.');
      }
      if (locale && this.translaterService?.use && this.translaterService.getCurrentLanguage() !== locale) {
        this.translaterService.use(locale);
      }
      let columns = newColumns;
      if (!columns) {
        columns = this.sharedService.columnDefinitions;
      }
      this.translateItems(columns, "nameKey", "name");
      this.translateItems(this.sharedService.allColumns, "nameKey", "name");
      this.translateItems(this.sharedService.allColumns, "columnGroupKey", "columnGroup");
      if (shouldUpdateColumns) {
        this.sharedService.slickGrid?.updateColumns();
      }
      if (this.gridOptions.enableColumnPicker && this._columnPickerControl) {
        this._columnPickerControl.translateColumnPicker();
      }
      if (this.gridOptions.enableGridMenu && this._gridMenuControl) {
        this._gridMenuControl.translateGridMenu();
      }
    }
    /**
     * Render (or re-render) the column headers from column definitions.
     * calling setColumns() will trigger a grid re-render
     */
    renderColumnHeaders(newColumns, forceColumnDefinitionsOverwrite = false) {
      let collection = newColumns;
      if (!collection) {
        collection = this.sharedService.columnDefinitions;
      }
      if (Array.isArray(collection) && this.sharedService.slickGrid?.setColumns) {
        if (collection.length > this.sharedService.allColumns.length || forceColumnDefinitionsOverwrite) {
          this.sharedService.allColumns = collection;
        }
        this.sharedService.slickGrid.setColumns(collection);
      }
      if (this.gridOptions.enableColumnPicker && this._columnPickerControl) {
        this._columnPickerControl.columns = this.sharedService.allColumns;
        this._columnPickerControl.translateColumnPicker();
      }
      if (this.gridOptions.enableGridMenu && this._gridMenuControl) {
        this._gridMenuControl.columns = this.sharedService.allColumns ?? [];
        this._gridMenuControl.recreateGridMenu();
        this._gridMenuControl.translateGridMenu();
      }
    }
    //
    // protected functions
    // -------------------
    /**
     * Some extension (feature) have specific `columnIndexPosition` that the developer want to use, we need to make sure these indexes are respected in the column definitions in the order provided.
     * The following 3 features could have that optional `columnIndexPosition` and we need to respect their column order, we will first sort by their optional order and only after we will create them by their specific order.
     * We'll process them by their position (if provided, else use same order that they were inserted)
     * @param featureWithIndexPositions
     * @param columns
     * @param gridOptions
     */
    createExtensionByTheirColumnIndex(featureWithIndexPositions, columns, gridOptions) {
      featureWithIndexPositions.sort((feat1, feat2) => (feat1?.columnIndexPosition ?? 0) - (feat2?.columnIndexPosition ?? 0));
      featureWithIndexPositions.forEach((feature) => {
        const instance = feature.instance.create(columns, gridOptions);
        if (instance) {
          this._extensionCreatedList[feature.name] = { name: feature.name, instance };
        }
      });
    }
    /** Translate an array of items from an input key and assign translated value to the output key */
    translateItems(items, inputKey, outputKey) {
      if (this.gridOptions?.enableTranslate && !this.translaterService?.translate) {
        throw new Error('[Slickgrid-Universal] requires a Translate Service to be installed and configured when the grid option "enableTranslate" is enabled.');
      }
      this.extensionUtility.translateItems(items, inputKey, outputKey);
    }
  };

  // node_modules/dequal/lite/index.mjs
  var has = Object.prototype.hasOwnProperty;
  function dequal(foo, bar) {
    var ctor, len;
    if (foo === bar) return true;
    if (foo && bar && (ctor = foo.constructor) === bar.constructor) {
      if (ctor === Date) return foo.getTime() === bar.getTime();
      if (ctor === RegExp) return foo.toString() === bar.toString();
      if (ctor === Array) {
        if ((len = foo.length) === bar.length) {
          while (len-- && dequal(foo[len], bar[len])) ;
        }
        return len === -1;
      }
      if (!ctor || typeof foo === "object") {
        len = 0;
        for (ctor in foo) {
          if (has.call(foo, ctor) && ++len && !has.call(bar, ctor)) return false;
          if (!(ctor in bar) || !dequal(foo[ctor], bar[ctor])) return false;
        }
        return Object.keys(bar).length === len;
      }
    }
    return foo !== foo && bar !== bar;
  }

  // node_modules/@slickgrid-universal/common/dist/filter-conditions/filterUtilities.js
  function compareObjects(o1, o2, compareKey) {
    if (compareKey && (o1?.hasOwnProperty(compareKey) || o2?.hasOwnProperty(compareKey))) {
      return o1[compareKey] === o2 || o1 === o2[compareKey] || o1[compareKey] === o2[compareKey];
    }
    if (typeof o1 === "object" && typeof o2 === "object" && Object.keys(o1).length !== Object.keys(o2).length) {
      return false;
    }
    for (const p in o1) {
      if (o1.hasOwnProperty(p)) {
        if (o1[p] !== o2[p]) {
          return false;
        }
      }
    }
    return true;
  }
  function isCollectionOperator(operator) {
    const inputOperator = operator?.toUpperCase() || "";
    switch (inputOperator) {
      case "IN":
      case "NIN":
      case "NOT_IN":
      case "IN_CONTAINS":
      case "NIN_CONTAINS":
      case "NOT_IN_CONTAINS":
      case "IN_COLLECTION":
      case "NOT_IN_COLLECTION":
        return true;
      default:
        return false;
    }
  }
  var testFilterCondition = (operator, value1, value2) => {
    switch (operator.toUpperCase()) {
      case "<":
      case "LT":
        return value1 < value2;
      case "<=":
      case "LE":
        return value1 <= value2;
      case ">":
      case "GT":
        return value1 > value2;
      case ">=":
      case "GE":
        return value1 >= value2;
      case "!=":
      case "<>":
      case "NE":
        return value1 !== value2;
      case "=":
      case "==":
      case "EQ":
        return value1 === value2;
      case "IN":
        return value2 && Array.isArray(value2) ? value2.includes(value1) : false;
      case "NIN":
      case "NOT_IN":
        return value2 && Array.isArray(value2) ? !value2.includes(value1) : false;
      case "IN_CONTAINS":
        if (value2 && Array.isArray(value2) && typeof value1 === "string") {
          return value2.some((item) => value1.split(/[,]+/).map((val) => val.trim()).includes(item));
        }
        return false;
      case "NIN_CONTAINS":
      case "NOT_IN_CONTAINS":
        if (value2 && Array.isArray(value2) && typeof value1 === "string") {
          return !value2.some((item) => value1.split(/[,]+/).map((val) => val.trim()).includes(item));
        }
        return false;
      case "IN_COLLECTION":
        if (value1 && value2 && Array.isArray(value1) && Array.isArray(value2)) {
          return value2.some((item) => value1.includes(item));
        }
        return false;
      case "NOT_IN_COLLECTION":
        if (value1 && value2 && Array.isArray(value1) && Array.isArray(value2)) {
          return !value2.some((item) => value1.includes(item));
        }
        return false;
    }
    return true;
  };

  // node_modules/@slickgrid-universal/common/dist/filter-conditions/stringFilterCondition.js
  var executeStringFilterCondition = ((options, parsedSearchValues) => {
    let [searchValue1, searchValue2] = parsedSearchValues;
    if (searchValue1 === void 0 && !options.operator) {
      return true;
    }
    options.cellValue = options.cellValue === void 0 || options.cellValue === null ? "" : options.cellValue.toString();
    const cellValue = options?.ignoreAccentOnStringFilterAndSort ? removeAccentFromText(options.cellValue, true) : options.cellValue.toLowerCase();
    if (typeof searchValue1 === "string") {
      searchValue1 = options?.ignoreAccentOnStringFilterAndSort ? removeAccentFromText(searchValue1, true) : searchValue1.toLowerCase();
    }
    if (typeof searchValue2 === "string") {
      searchValue2 = options?.ignoreAccentOnStringFilterAndSort ? removeAccentFromText(searchValue2, true) : searchValue2.toLowerCase();
    }
    if (options.operator === "StartsWithEndsWith" && searchValue1 !== void 0 && searchValue2 !== void 0) {
      return testStartsWithEndsWith(cellValue, [searchValue1, searchValue2]);
    } else if (searchValue1 !== void 0 && searchValue2 !== void 0) {
      let operator = options?.operator ?? options.defaultFilterRangeOperator;
      if (operator !== "RangeInclusive" && operator !== "RangeExclusive") {
        operator = options.defaultFilterRangeOperator;
      }
      const isInclusive = operator === "RangeInclusive";
      const searchResult12 = testStringCondition(isInclusive ? ">=" : ">", cellValue, searchValue1, options.searchInputLastChar);
      const searchResult2 = testStringCondition(isInclusive ? "<=" : "<", cellValue, searchValue2, options.searchInputLastChar);
      return searchResult12 && searchResult2;
    }
    const searchResult1 = testStringCondition(options.operator, cellValue, searchValue1, options.searchInputLastChar);
    return searchResult1;
  });
  function getFilterParsedText(inputSearchTerms) {
    const defaultSearchTerm = "";
    let searchTerms = Array.isArray(inputSearchTerms) && inputSearchTerms || [defaultSearchTerm];
    const parsedSearchValues = [];
    let searchValue1;
    let searchValue2;
    if (searchTerms.length === 1 && typeof searchTerms[0] === "string") {
      const st = searchTerms[0];
      if (st.indexOf("..") > 0) {
        searchTerms = st.split("..");
      } else if (st.indexOf("*") > 0 && st.indexOf("*") < st.length - 1) {
        searchTerms = st.split("*");
      }
    }
    if (searchTerms.length === 2) {
      searchValue1 = `${searchTerms[0]}`;
      searchValue2 = `${searchTerms[1]}`;
    } else {
      const parsedSearchValue = Array.isArray(inputSearchTerms) && inputSearchTerms.length > 0 ? inputSearchTerms[0] : "";
      searchValue1 = parsedSearchValue === void 0 || parsedSearchValue === null ? "" : `${parsedSearchValue}`;
    }
    if (searchValue1 !== void 0 && searchValue2 !== void 0) {
      parsedSearchValues.push(searchValue1, searchValue2);
    } else if (searchValue1 !== void 0) {
      parsedSearchValues.push(searchValue1);
    }
    return parsedSearchValues;
  }
  function testStringCondition(operator, cellValue, searchValue, searchInputLastChar) {
    if (operator === "*" || operator === "EndsWith" || operator === "*z") {
      return cellValue.endsWith(searchValue);
    } else if (operator === "" && searchInputLastChar === "*" || operator === "StartsWith" || operator === "a*") {
      return cellValue.startsWith(searchValue);
    } else if (operator === "" || operator === "Contains") {
      return cellValue.indexOf(searchValue) > -1;
    } else if (operator === "<>" || operator === "Not_Contains") {
      return cellValue.indexOf(searchValue) === -1;
    }
    return testFilterCondition(operator || "==", cellValue, searchValue);
  }
  function testStartsWithEndsWith(cellValue, [startW, endW]) {
    return cellValue.startsWith(startW) && cellValue.endsWith(endW);
  }

  // node_modules/@slickgrid-universal/common/dist/filter-conditions/objectFilterCondition.js
  var executeObjectFilterCondition = ((options, parsedSearchValue) => {
    if (parsedSearchValue === void 0 && !options.operator) {
      return true;
    }
    const operator = (options.operator || "").toUpperCase();
    switch (operator) {
      case "!=":
      case "<>":
      case "NE":
        return !compareObjects(options.cellValue, parsedSearchValue, options.dataKey);
      case "=":
      case "==":
      case "EQ":
      default:
        return compareObjects(options.cellValue, parsedSearchValue, options.dataKey);
    }
  });
  function getFilterParsedObjectResult(inputSearchTerms) {
    const parsedSearchValue = Array.isArray(inputSearchTerms) && inputSearchTerms.length > 0 ? inputSearchTerms[0] : "";
    return parsedSearchValue || "";
  }

  // node_modules/@slickgrid-universal/common/dist/filter-conditions/numberFilterCondition.js
  var executeNumberFilterCondition = ((options, parsedSearchValues) => {
    const cellValue = parseFloat(options.cellValue);
    const [searchValue1, searchValue2] = parsedSearchValues;
    if (searchValue1 === void 0 && !options.operator) {
      return true;
    }
    if (searchValue1 !== void 0 && searchValue2 !== void 0) {
      let operator = options?.operator ?? options.defaultFilterRangeOperator;
      if (operator !== "RangeInclusive" && operator !== "RangeExclusive") {
        operator = options.defaultFilterRangeOperator;
      }
      const isInclusive = operator === "RangeInclusive";
      const resultCondition1 = testFilterCondition(isInclusive ? ">=" : ">", cellValue, +searchValue1);
      const resultCondition2 = testFilterCondition(isInclusive ? "<=" : "<", cellValue, +searchValue2);
      return resultCondition1 && resultCondition2;
    }
    return testFilterCondition(options.operator || "==", cellValue, +searchValue1);
  });
  function getFilterParsedNumbers(inputSearchTerms) {
    const defaultSearchTerm = 0;
    const searchTerms = Array.isArray(inputSearchTerms) && inputSearchTerms || [defaultSearchTerm];
    const parsedSearchValues = [];
    let searchValue1;
    let searchValue2;
    if (searchTerms.length === 2 || typeof searchTerms[0] === "string" && searchTerms[0].indexOf("..") > 0) {
      const searchValues = searchTerms.length === 2 ? searchTerms : searchTerms[0].split("..");
      searchValue1 = parseFloat(Array.isArray(searchValues) ? searchValues[0] : "");
      searchValue2 = parseFloat(Array.isArray(searchValues) ? searchValues[1] : "");
    } else {
      searchValue1 = parseFloat(searchTerms[0]);
    }
    if (isNumber(searchValue1, true) && isNumber(searchValue2, true)) {
      parsedSearchValues.push(searchValue1, searchValue2);
    } else if (isNumber(searchValue1, true)) {
      parsedSearchValues.push(searchValue1);
    }
    return parsedSearchValues;
  }

  // node_modules/@slickgrid-universal/common/dist/filter-conditions/booleanFilterCondition.js
  var executeBooleanFilterCondition = ((options, parsedSearchValue) => {
    return parseBoolean(options.cellValue) === parseBoolean(parsedSearchValue);
  });
  function getFilterParsedBoolean(inputSearchTerms) {
    const searchTerm = Array.isArray(inputSearchTerms) && inputSearchTerms[0] || false;
    return parseBoolean(searchTerm);
  }

  // node_modules/@slickgrid-universal/common/dist/filter-conditions/collectionSearchFilterCondition.js
  var executeCollectionSearchFilterCondition = (options) => {
    const filterOperator = options.operator;
    let cellValue;
    if (Array.isArray(options.cellValue) && (filterOperator === "IN_COLLECTION" || filterOperator === "NOT_IN_COLLECTION")) {
      cellValue = options.cellValue.length ? options.cellValue.map((value) => `${value}`) : [];
    } else {
      cellValue = options.cellValue === void 0 || options.cellValue === null ? "" : `${options.cellValue}`;
    }
    return testFilterCondition(filterOperator || "IN", cellValue, options.searchTerms || []);
  };

  // node_modules/@slickgrid-universal/common/dist/filter-conditions/dateFilterCondition.js
  function executeDateFilterCondition(options, parsedSearchDates) {
    const filterSearchType = options && (options.filterSearchType || options.fieldType) || "dateIso";
    const FORMAT = mapTempoDateFormatWithFieldType(filterSearchType);
    const [searchDate1, searchDate2] = parsedSearchDates;
    const dateCell = tryParseDate(options.cellValue, FORMAT, true);
    if (!searchDate1 && !searchDate2 || !dateCell) {
      return false;
    }
    const dateCellTimestamp = FORMAT === "ISO8601" || FORMAT.toLowerCase().includes("h") ? dateCell.valueOf() : dayStart(new Date(dateCell)).valueOf();
    if (searchDate1 && searchDate2) {
      let operator = options?.operator ?? options.defaultFilterRangeOperator;
      if (operator !== "RangeInclusive" && operator !== "RangeExclusive") {
        operator = options.defaultFilterRangeOperator;
      }
      const isInclusive = operator === "RangeInclusive";
      const resultCondition1 = testFilterCondition(isInclusive ? ">=" : ">", dateCellTimestamp, searchDate1.valueOf());
      const resultCondition2 = testFilterCondition(isInclusive ? "<=" : "<", dateCellTimestamp, searchDate2.valueOf());
      return resultCondition1 && resultCondition2;
    }
    const dateSearchTimestamp1 = FORMAT === "ISO8601" || FORMAT.toLowerCase().includes("h") ? searchDate1.valueOf() : dayStart(new Date(searchDate1)).valueOf();
    return testFilterCondition(options.operator || "==", dateCellTimestamp, dateSearchTimestamp1);
  }
  function getFilterParsedDates(inputSearchTerms, inputFilterSearchType) {
    const searchTerms = Array.isArray(inputSearchTerms) && inputSearchTerms || [];
    const filterSearchType = inputFilterSearchType || "dateIso";
    const FORMAT = mapTempoDateFormatWithFieldType(filterSearchType);
    const parsedSearchValues = [];
    if (searchTerms.length === 2 || typeof searchTerms[0] === "string" && searchTerms[0].indexOf("..") > 0) {
      const searchValues = searchTerms.length === 2 ? searchTerms : searchTerms[0].split("..");
      const searchValue1 = Array.isArray(searchValues) && searchValues[0] || "";
      const searchValue2 = Array.isArray(searchValues) && searchValues[1] || "";
      const searchDate1 = tryParseDate(searchValue1, FORMAT, true);
      const searchDate2 = tryParseDate(searchValue2, FORMAT, true);
      if (!searchDate1 || !searchDate2) {
        return [];
      }
      parsedSearchValues.push(searchDate1, searchDate2);
    } else {
      const searchDate1 = tryParseDate(searchTerms[0], FORMAT, true);
      if (!searchDate1) {
        return [];
      }
      parsedSearchValues.push(searchDate1);
    }
    return parsedSearchValues;
  }

  // node_modules/@slickgrid-universal/common/dist/filter-conditions/filterConditionProcesses.js
  function getParsedSearchTermsByFieldType(inputSearchTerms, inputFilterSearchType) {
    const generalizedType = getVarTypeOfByColumnFieldType(inputFilterSearchType);
    let parsedSearchValues;
    switch (generalizedType) {
      case "boolean":
        parsedSearchValues = getFilterParsedBoolean(inputSearchTerms);
        break;
      case "date":
        parsedSearchValues = getFilterParsedDates(inputSearchTerms, inputFilterSearchType);
        break;
      case "number":
        parsedSearchValues = getFilterParsedNumbers(inputSearchTerms);
        break;
      case "object":
        parsedSearchValues = getFilterParsedObjectResult(inputSearchTerms);
        break;
      case "string":
        parsedSearchValues = getFilterParsedText(inputSearchTerms);
        break;
    }
    return parsedSearchValues;
  }
  function getVarTypeOfByColumnFieldType(fieldType) {
    if (isColumnDateType(fieldType)) {
      return "date";
    }
    switch (fieldType) {
      case "boolean":
        return "boolean";
      case "integer":
      case "float":
      case "number":
        return "number";
      case "object":
        return "object";
      case "string":
      case "text":
      case "password":
      case "readonly":
      default:
        return "string";
    }
  }

  // node_modules/@slickgrid-universal/common/dist/filter-conditions/filterConditions.index.js
  var executeFilterConditionTest = ((options, parsedSearchTerms) => {
    if (isCollectionOperator(options.operator)) {
      return executeCollectionSearchFilterCondition(options);
    }
    const generalizedType = getVarTypeOfByColumnFieldType(options.filterSearchType || options.fieldType);
    switch (generalizedType) {
      case "boolean":
        return executeBooleanFilterCondition(options, parsedSearchTerms);
      case "date":
        return executeDateFilterCondition(options, parsedSearchTerms || []);
      case "number":
        return executeNumberFilterCondition(options, parsedSearchTerms || []);
      case "object":
        return executeObjectFilterCondition(options, parsedSearchTerms);
      case "string":
      default:
        return executeStringFilterCondition(options, parsedSearchTerms || []);
    }
  });
  var FilterConditions = {
    executeFilterConditionTest,
    booleanFilter: executeBooleanFilterCondition,
    collectionSearchFilter: executeCollectionSearchFilterCondition,
    numberFilter: executeNumberFilterCondition,
    stringFilter: executeStringFilterCondition
  };

  // node_modules/@slickgrid-universal/common/dist/filters/filterFactory.js
  var FilterFactory = class {
    constructor(config, translaterService, collectionService, rxjs) {
      this.config = config;
      this.translaterService = translaterService;
      this.collectionService = collectionService;
      this.rxjs = rxjs;
      this._options = this.config?.options ?? {};
    }
    addRxJsResource(rxjs) {
      this.rxjs = rxjs;
    }
    // Uses the User model to create a new User
    createFilter(columnFilter) {
      let filter;
      if (columnFilter?.model) {
        filter = typeof columnFilter.model === "function" ? new columnFilter.model(this.translaterService, this.collectionService, this.rxjs) : columnFilter.model;
      }
      if (!filter && this._options.defaultFilter) {
        filter = new this._options.defaultFilter(this.translaterService, this.collectionService, this.rxjs);
      }
      return filter;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/filter.service.js
  var FilterService = class {
    constructor(filterFactory, pubSubService, sharedService, backendUtilities, rxjs) {
      this.filterFactory = filterFactory;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this.backendUtilities = backendUtilities;
      this.rxjs = rxjs;
      this._isFilterFirstRender = true;
      this._firstColumnIdRendered = "";
      this._filtersMetadata = [];
      this._columnFilters = {};
      this._isTreePresetExecuted = false;
      this._previousFilters = [];
      this._onSearchChange = new SlickEvent();
      this._eventHandler = new SlickEventHandler();
      if (this.rxjs) {
        this.httpCancelRequests$ = this.rxjs.createSubject();
      }
    }
    /** Getter of the SlickGrid Event Handler */
    get eventHandler() {
      return this._eventHandler;
    }
    /** Getter to know if the filter was already rendered or if it was its first time render */
    get isFilterFirstRender() {
      return this._isFilterFirstRender;
    }
    /** Getter of the SlickGrid Event Handler */
    get onSearchChange() {
      return this._onSearchChange;
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get _gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    /** Getter for the Column Definitions pulled through the Grid Object */
    get _columns() {
      return this._grid?.getColumns() ?? [];
    }
    /** Getter of SlickGrid DataView object */
    get _dataView() {
      return this._grid?.getData() ?? {};
    }
    addRxJsResource(rxjs) {
      this.rxjs = rxjs;
    }
    /**
     * Initialize the Service
     * @param grid
     */
    init(grid) {
      this._grid = grid;
      if (this._gridOptions && this._gridOptions.enableTreeData && this._gridOptions.treeDataOptions) {
        this._grid.setSortColumns([{ columnId: this._gridOptions.treeDataOptions.columnId, sortAsc: true }]);
      }
    }
    dispose() {
      this._eventHandler.unsubscribeAll();
      if (this.httpCancelRequests$ && this.rxjs?.isObservable(this.httpCancelRequests$)) {
        this.httpCancelRequests$.next();
        this.httpCancelRequests$.complete();
      }
      this.disposeColumnFilters();
      this._onSearchChange = null;
    }
    /**
     * Dispose of the filters, since it's a singleton, we don't want to affect other grids with same columns
     */
    disposeColumnFilters() {
      this.removeAllColumnFiltersProperties();
      if (Array.isArray(this._filtersMetadata)) {
        let filter = this._filtersMetadata.pop();
        while (filter) {
          if (typeof filter?.destroy === "function") {
            filter.destroy();
          }
          filter = this._filtersMetadata.pop();
        }
      }
    }
    /**
     * Bind a backend filter hook to the grid
     * @param grid SlickGrid Grid object
     */
    bindBackendOnFilter(grid) {
      this._filtersMetadata = [];
      this._eventHandler.subscribe(grid.onHeaderRowCellRendered, (_e, args) => {
        if (args.column.id === this._firstColumnIdRendered) {
          this._isFilterFirstRender = false;
        }
        this.addFilterTemplateToHeaderRow(args, this._isFilterFirstRender);
        if (this._firstColumnIdRendered === "") {
          this._firstColumnIdRendered = args.column.id;
        }
      });
      this.subscribeToOnHeaderRowCellRendered(grid);
      if (this._onSearchChange) {
        this._eventHandler.subscribe(this._onSearchChange, this.onBackendFilterChange.bind(this));
      }
    }
    /**
     * Bind a local filter hook to the grid
     * @param grid SlickGrid Grid object
     * @param gridOptions Grid Options object
     * @param dataView
     */
    bindLocalOnFilter(grid) {
      this._filtersMetadata = [];
      this._dataView.setFilterArgs({ columnFilters: this._columnFilters, grid: this._grid, dataView: this._dataView });
      this._dataView.setFilter(this.customLocalFilter.bind(this));
      if (this._onSearchChange) {
        this._eventHandler.subscribe(this._onSearchChange, async (_e, args) => {
          const isClearFilterEvent = args?.clearFilterTriggered ?? false;
          if (!isClearFilterEvent) {
            await this.emitFilterChanged("local", true);
          }
          if (this._gridOptions.enableTreeData === true) {
            this._tmpPreFilteredData = this.preFilterTreeData(this._dataView.getItems(), this._columnFilters);
          }
          if (!isClearFilterEvent) {
            await this.emitFilterChanged("local");
          }
          if (args.columnId !== null) {
            this._dataView.refresh();
          }
          this._previousFilters = this.extractBasicFilterDetails(this._columnFilters);
        });
      }
      this._eventHandler.subscribe(grid.onHeaderRowCellRendered, (_e, args) => {
        this.addFilterTemplateToHeaderRow(args);
      });
      this.subscribeToOnHeaderRowCellRendered(grid);
    }
    async clearFilterByColumnId(event, columnId) {
      await this.pubSubService.publish("onBeforeFilterClear", { columnId }, 0);
      const isBackendApi = this._gridOptions.backendServiceApi ?? false;
      const emitter = isBackendApi ? "remote" : "local";
      const currentFilterColumnIds = Object.keys(this._columnFilters);
      let currentColFilter;
      if (Array.isArray(currentFilterColumnIds)) {
        currentColFilter = currentFilterColumnIds.find((name) => name === `${columnId}`);
      }
      const colFilter = this._filtersMetadata.find((filter) => filter.columnDef.id === columnId);
      if (colFilter?.clear) {
        colFilter.clear(true);
      }
      if (isBackendApi && currentColFilter !== void 0) {
        this.onBackendFilterChange(event, {
          grid: this._grid,
          columnFilters: this._columnFilters
        });
      }
      await this.emitFilterChanged(emitter);
      return true;
    }
    /** Clear the search filters (below the column titles) */
    async clearFilters(triggerChange = true) {
      if (triggerChange) {
        await this.pubSubService.publish("onBeforeFilterClear", true, 0);
      }
      this._filtersMetadata.forEach((filter) => {
        if (filter?.clear) {
          filter.clear(false);
        }
      });
      this.removeAllColumnFiltersProperties();
      if (Array.isArray(this._columns)) {
        this._columns.forEach((columnDef) => {
          if (columnDef.filter?.searchTerms) {
            delete columnDef.filter.searchTerms;
          }
        });
      }
      if (this._dataView && this._grid) {
        this._dataView.refresh();
        this._grid.invalidate();
      }
      const backendApi = this._gridOptions.backendServiceApi;
      if (backendApi && triggerChange) {
        const callbackArgs = {
          clearFilterTriggered: true,
          shouldTriggerQuery: triggerChange,
          grid: this._grid,
          columnFilters: this._columnFilters
        };
        const queryResponse = backendApi.service.processOnFilterChanged(void 0, callbackArgs);
        const query = queryResponse;
        const totalItems = this._gridOptions.pagination?.totalItems ?? 0;
        this.backendUtilities?.executeBackendCallback(backendApi, query, callbackArgs, /* @__PURE__ */ new Date(), totalItems, {
          errorCallback: this.resetToPreviousSearchFilters.bind(this),
          successCallback: (responseArgs) => this._previousFilters = this.extractBasicFilterDetails(responseArgs.columnFilters),
          emitActionChangedCallback: this.emitFilterChanged.bind(this)
        });
      } else {
        this._previousFilters = this.extractBasicFilterDetails(this._columnFilters);
      }
      if (triggerChange) {
        this.pubSubService.publish("onFilterCleared", true);
      }
    }
    /** Local Grid Filter search */
    customLocalFilter(item, args) {
      const grid = args?.grid;
      const columnFilters = args?.columnFilters ?? {};
      const isGridWithTreeData = this._gridOptions.enableTreeData ?? false;
      const treeDataOptions = this._gridOptions.treeDataOptions;
      if (isGridWithTreeData && treeDataOptions) {
        const collapsedPropName = treeDataOptions.collapsedPropName ?? Constants.treeDataProperties.COLLAPSED_PROP;
        const parentPropName = treeDataOptions.parentPropName ?? Constants.treeDataProperties.PARENT_PROP;
        const childrenPropName = treeDataOptions?.childrenPropName ?? Constants.treeDataProperties.CHILDREN_PROP;
        const primaryDataId = this._gridOptions.datasetIdPropertyName ?? "id";
        const autoRecalcTotalsOnFilterChange = treeDataOptions.autoRecalcTotalsOnFilterChange ?? false;
        let isParentCollapsed = false;
        if (item[parentPropName] !== null) {
          let parent = this._dataView.getItemById(item[parentPropName]);
          while (parent) {
            if (parent[collapsedPropName]) {
              if (autoRecalcTotalsOnFilterChange) {
                isParentCollapsed = true;
              } else {
                return false;
              }
            }
            parent = this._dataView.getItemById(parent[parentPropName]);
          }
        }
        if (this._tmpPreFilteredData instanceof Set) {
          const filtered = this._tmpPreFilteredData.has(item[primaryDataId]);
          if (autoRecalcTotalsOnFilterChange) {
            const treeItem = findItemInTreeStructure(this.sharedService.hierarchicalDataset, (x2) => x2[primaryDataId] === item[primaryDataId], childrenPropName);
            if (treeItem) {
              treeItem.__filteredOut = !filtered;
            }
            if (isParentCollapsed) {
              return false;
            }
          }
          return filtered;
        }
      } else if (typeof columnFilters === "object") {
        for (const columnId of Object.keys(columnFilters)) {
          const searchColFilter = columnFilters[columnId];
          const columnDef = searchColFilter.columnDef;
          const columnFilterDef = columnDef?.filter;
          if (typeof columnFilterDef?.filterPredicate === "function") {
            if (!columnFilterDef.filterPredicate(item, searchColFilter)) {
              return false;
            }
          } else {
            const conditionOptions = this.preProcessFilterConditionOnDataContext(item, searchColFilter, grid);
            if (typeof conditionOptions === "boolean") {
              return conditionOptions;
            }
            let parsedSearchTerms = searchColFilter?.parsedSearchTerms;
            if (parsedSearchTerms === void 0) {
              parsedSearchTerms = getParsedSearchTermsByFieldType(searchColFilter.searchTerms, columnDef.type || "string");
              if (parsedSearchTerms !== void 0) {
                searchColFilter.parsedSearchTerms = parsedSearchTerms;
              }
            }
            if (!FilterConditions.executeFilterConditionTest(conditionOptions, parsedSearchTerms)) {
              return false;
            }
          }
        }
      }
      return true;
    }
    /**
     * Loop through each form input search filter and parse their searchTerms, for example a CompoundDate Filter will be parsed as a Date object.
     * Also if we are dealing with a text filter input, an operator can optionally be part of the filter itself and we need to extract it from there,
     * for example a filter of "John*" will be analyzed as { operator: StartsWith, searchTerms: ['John'] }
     * @param inputSearchTerms - filter search terms
     * @param columnFilter - column filter object (the object properties represent each column id and the value is the filter metadata)
     * @returns FilterConditionOption
     */
    parseFormInputFilterConditions(inputSearchTerms, columnFilter) {
      const searchValues = deepCopy(inputSearchTerms) || [];
      let fieldSearchValue = Array.isArray(searchValues) && searchValues.length === 1 ? searchValues[0] : "";
      const columnDef = columnFilter.columnDef;
      const fieldType = columnDef.filter?.type ?? columnDef.type ?? "string";
      let matches2 = null;
      if (fieldType !== "object") {
        fieldSearchValue = fieldSearchValue === void 0 || fieldSearchValue === null ? "" : `${fieldSearchValue}`;
        const autoParseInputFilterOperator = columnDef.autoParseInputFilterOperator ?? this._gridOptions.autoParseInputFilterOperator;
        matches2 = autoParseInputFilterOperator !== false ? fieldSearchValue.match(/^((.*[^\\*\r\n])[*]{1}(.*[^*\r\n]))|^([<>!=*]{0,2})(.*[^<>!=*])?([*])*$/) || [] : [fieldSearchValue, "", "", "", "", fieldSearchValue, ""];
      }
      const comboStartsWith = matches2?.[2] || "";
      const comboEndsWith = matches2?.[3] || "";
      let operator = matches2?.[4] || columnFilter.operator;
      let searchTerm = matches2?.[1] || matches2?.[5] || "";
      const inputLastChar = matches2?.[6] || (operator === "*z" ? "*" : "");
      if (typeof fieldSearchValue === "string") {
        fieldSearchValue = fieldSearchValue.replace(`'`, `''`);
        if (comboStartsWith && comboEndsWith) {
          searchTerm = fieldSearchValue;
          operator = "StartsWithEndsWith";
        } else if (operator === "*" || operator === "*z") {
          operator = "EndsWith";
        } else if (operator === "a*" || inputLastChar === "*") {
          operator = "StartsWith";
        }
      }
      if (Array.isArray(matches2) && matches2.length >= 1 && Array.isArray(searchValues) && searchValues.length === 1) {
        searchValues[0] = searchTerm.length > 0 && searchTerm.substring(0, 1) === " " ? searchTerm.substring(1) : searchTerm;
      }
      return {
        dataKey: columnDef.dataKey,
        fieldType,
        searchTerms: searchValues || [],
        operator,
        searchInputLastChar: inputLastChar,
        filterSearchType: columnDef.filterSearchType,
        defaultFilterRangeOperator: this._gridOptions.defaultFilterRangeOperator
      };
    }
    /**
     * PreProcess the filter(s) condition(s) on each item data context, the result might be a boolean or a FilterConditionOption object.
     * It will be a boolean when the searchTerms are invalid or the column is not found (if so it will return True and the item won't be filtered out from the grid)
     * or else return a FilterConditionOption object with the necessary info for the test condition needs to be processed in a further stage.
     * @param item - item data context
     * @param columnFilter - column filter object (the object properties represent each column id and the value is the filter metadata)
     * @param grid - SlickGrid object
     * @returns FilterConditionOption or boolean
     */
    preProcessFilterConditionOnDataContext(item, columnFilter, grid) {
      const columnDef = columnFilter.columnDef;
      const columnId = columnFilter.columnId;
      let columnIndex = grid.getColumnIndex(columnId);
      const columns = this._grid.getColumns();
      if (!columnDef && Array.isArray(columns)) {
        columnIndex = columns.findIndex((col) => col.field === columnId);
      }
      if (!columnDef) {
        return true;
      }
      if (this._gridOptions.enableRowDetailView) {
        const metadataPrefix = this._gridOptions.rowDetailView?.keyPrefix || "__";
        if (item[`${metadataPrefix}isPadding`] && item[`${metadataPrefix}parent`]) {
          item = item[`${metadataPrefix}parent`];
        }
      }
      let queryFieldName = columnDef.filter?.queryField || columnDef.queryFieldFilter || columnDef.queryField || columnDef.field || "";
      if (typeof columnDef.queryFieldNameGetterFn === "function") {
        queryFieldName = columnDef.queryFieldNameGetterFn(item);
      }
      const fieldType = columnDef.filter?.type ?? columnDef.type ?? "string";
      let cellValue = item[queryFieldName];
      if (queryFieldName?.indexOf(".") >= 0) {
        cellValue = getDescendantProperty(item, queryFieldName);
      }
      const operator = columnFilter.operator;
      const searchValues = columnFilter.searchTerms || [];
      if (!searchValues || Array.isArray(searchValues) && (searchValues.length === 0 || searchValues.length === 1 && operator === searchValues[0])) {
        return true;
      }
      if (searchValues && Array.isArray(searchValues) && fieldType !== "object") {
        for (let k2 = 0, ln = searchValues.length; k2 < ln; k2++) {
          searchValues[k2] = (searchValues[k2] === void 0 || searchValues[k2] === null ? "" : searchValues[k2]) + "";
        }
      }
      if (columnDef?.params?.useFormatterOuputToFilter === true) {
        const idPropName = this._gridOptions.datasetIdPropertyName || "id";
        const rowIndex = this._dataView && typeof this._dataView.getIdxById === "function" ? this._dataView.getIdxById(item[idPropName]) : 0;
        const formattedCellValue = columnDef && typeof columnDef.formatter === "function" ? columnDef.formatter(rowIndex || 0, columnIndex, cellValue, columnDef, item, this._grid) : "";
        cellValue = stripTags(formattedCellValue);
      }
      if (typeof cellValue === "number") {
        cellValue = cellValue.toString();
      }
      return {
        dataKey: columnDef.dataKey,
        fieldType,
        searchTerms: searchValues,
        cellValue,
        operator,
        searchInputLastChar: columnFilter.searchInputLastChar,
        filterSearchType: columnDef.filterSearchType,
        ignoreAccentOnStringFilterAndSort: this._gridOptions.ignoreAccentOnStringFilterAndSort ?? false,
        defaultFilterRangeOperator: this._gridOptions.defaultFilterRangeOperator
      };
    }
    /**
     * When using Tree Data, we need to prefilter (search) the data prior, the result will be an array of IDs which are the node(s) and their parent nodes when necessary.
     * This will then be passed to the DataView setFilter(customLocalFilter), which will itself loop through the list of IDs and display/hide the row when found.
     * We do this in 2 steps so that we can still use the DataSet setFilter()
     */
    preFilterTreeData(inputItems, columnFilters) {
      const treeDataOptions = this._gridOptions.treeDataOptions;
      const collapsedPropName = treeDataOptions?.collapsedPropName ?? Constants.treeDataProperties.COLLAPSED_PROP;
      const parentPropName = treeDataOptions?.parentPropName ?? Constants.treeDataProperties.PARENT_PROP;
      const hasChildrenPropName = treeDataOptions?.hasChildrenPropName ?? Constants.treeDataProperties.HAS_CHILDREN_PROP;
      const primaryDataId = this._gridOptions.datasetIdPropertyName ?? "id";
      const treeDataToggledItems = this._gridOptions.presets?.treeData?.toggledItems;
      const isInitiallyCollapsed = this._gridOptions.treeDataOptions?.initiallyCollapsed ?? false;
      const treeDataColumnId = this._gridOptions.treeDataOptions?.columnId;
      const excludeChildrenWhenFilteringTree = this._gridOptions.treeDataOptions?.excludeChildrenWhenFilteringTree;
      const isNotExcludingChildAndValidateOnlyTreeColumn = !excludeChildrenWhenFilteringTree && this._gridOptions.treeDataOptions?.autoApproveParentItemWhenTreeColumnIsValid === true;
      const treeObj = {};
      const filteredChildrenAndParents = /* @__PURE__ */ new Set();
      const filteredParents = /* @__PURE__ */ new Map();
      if (Array.isArray(inputItems)) {
        inputItems.forEach((inputItem) => {
          treeObj[inputItem[primaryDataId]] = inputItem;
          delete treeObj[inputItem[primaryDataId]].__used;
        });
        if (typeof columnFilters === "object") {
          Object.keys(columnFilters).forEach((columnId) => {
            const columnFilter = columnFilters[columnId];
            const searchValues = columnFilter?.searchTerms ? deepCopy(columnFilter.searchTerms) : [];
            const inputSearchConditions = this.parseFormInputFilterConditions(searchValues, columnFilter);
            const columnDef = columnFilter.columnDef;
            const fieldType = columnDef?.filter?.type ?? columnDef?.type ?? "string";
            const parsedSearchTerms = getParsedSearchTermsByFieldType(inputSearchConditions.searchTerms, fieldType);
            if (parsedSearchTerms !== void 0) {
              columnFilter.parsedSearchTerms = parsedSearchTerms;
            }
          });
        }
        inputItems.forEach((item) => {
          const hasChildren = item[hasChildrenPropName];
          let matchFilter = true;
          for (const columnId of Object.keys(columnFilters)) {
            const columnFilter = columnFilters[columnId];
            const conditionOptionResult = this.preProcessFilterConditionOnDataContext(item, columnFilter, this._grid);
            if (conditionOptionResult) {
              const parsedSearchTerms = columnFilter?.parsedSearchTerms;
              const conditionResult = typeof conditionOptionResult === "boolean" ? conditionOptionResult : FilterConditions.executeFilterConditionTest(conditionOptionResult, parsedSearchTerms);
              if (conditionResult && isNotExcludingChildAndValidateOnlyTreeColumn && hasChildren && columnFilter.columnId === treeDataColumnId) {
                filteredParents.set(item[primaryDataId], true);
                break;
              }
              if (conditionResult || !excludeChildrenWhenFilteringTree && filteredParents.get(item[parentPropName]) === true) {
                if (hasChildren && columnFilter.columnId === treeDataColumnId) {
                  filteredParents.set(item[primaryDataId], true);
                }
                if (conditionResult || !excludeChildrenWhenFilteringTree && columnFilter.columnId === treeDataColumnId) {
                  continue;
                }
              } else {
                if (hasChildren && filteredParents.get(item[parentPropName]) !== true && columnFilter.columnId !== treeDataColumnId) {
                  filteredParents.set(item[primaryDataId], false);
                }
              }
            }
            matchFilter = false;
            continue;
          }
          if (matchFilter) {
            filteredChildrenAndParents.add(item[primaryDataId]);
            let parent = treeObj[item[parentPropName]] ?? false;
            const presetToggleShouldBeCollapsed = !isInitiallyCollapsed;
            if (!this._isTreePresetExecuted && Array.isArray(treeDataToggledItems) && treeDataToggledItems.some((collapsedItem) => collapsedItem.itemId === parent.id && collapsedItem.isCollapsed === presetToggleShouldBeCollapsed)) {
              parent[collapsedPropName] = presetToggleShouldBeCollapsed;
            }
            while (parent) {
              parent.__used ?? filteredChildrenAndParents.add(parent[primaryDataId]);
              treeObj[parent[primaryDataId]].__used = true;
              parent = treeObj[parent[parentPropName]] ?? false;
            }
          }
        });
      }
      this._isTreePresetExecuted = true;
      return filteredChildrenAndParents;
    }
    getColumnFilters() {
      return this._columnFilters;
    }
    getPreviousFilters() {
      return this._previousFilters;
    }
    getFiltersMetadata() {
      return this._filtersMetadata;
    }
    getCurrentLocalFilters() {
      const currentFilters = [];
      if (this._columnFilters) {
        for (const colId of Object.keys(this._columnFilters)) {
          const columnFilter = this._columnFilters[colId];
          const filter = { columnId: colId || "" };
          if (columnFilter?.searchTerms) {
            filter.searchTerms = columnFilter.searchTerms;
          }
          if (columnFilter.operator) {
            filter.operator = columnFilter.operator;
          }
          if (columnFilter.targetSelector) {
            filter.targetSelector = columnFilter.targetSelector;
          }
          if (Array.isArray(filter.searchTerms) && filter.searchTerms.length > 0) {
            currentFilters.push(filter);
          }
        }
      }
      return currentFilters;
    }
    /**
     * A simple function that will be called to emit a change when a filter changes.
     * Other services, like Pagination, can then subscribe to it.
     * @param caller
     */
    emitFilterChanged(caller, isBeforeExecution = false) {
      const eventName = isBeforeExecution ? "onBeforeFilterChange" : "onFilterChanged";
      if (caller === "remote" && this._gridOptions.backendServiceApi) {
        let currentFilters = [];
        const backendService = this._gridOptions.backendServiceApi.service;
        if (backendService?.getCurrentFilters) {
          currentFilters = backendService.getCurrentFilters();
        }
        return this.pubSubService.publish(eventName, currentFilters);
      } else if (caller === "local") {
        return this.pubSubService.publish(eventName, this.getCurrentLocalFilters());
      }
    }
    async onBackendFilterChange(event, args) {
      const isTriggeringQueryEvent = args?.shouldTriggerQuery;
      if (isTriggeringQueryEvent) {
        await this.emitFilterChanged("remote", true);
      }
      if (!args?.grid) {
        throw new Error('Something went wrong when trying to bind the "onBackendFilterChange(event, args)" function, it seems that "args" is not populated correctly');
      }
      const backendApi = this._gridOptions.backendServiceApi;
      if (!backendApi || !backendApi.process || !backendApi.service) {
        throw new Error(`BackendServiceApi requires at least a "process" function and a "service" defined`);
      }
      const startTime = /* @__PURE__ */ new Date();
      backendApi.preProcess?.();
      if (isTriggeringQueryEvent) {
        const query = await backendApi.service.processOnFilterChanged(event, args);
        const totalItems = this._gridOptions.pagination?.totalItems ?? 0;
        this.backendUtilities?.executeBackendCallback(backendApi, query, args, startTime, totalItems, {
          errorCallback: this.resetToPreviousSearchFilters.bind(this),
          successCallback: (responseArgs) => this._previousFilters = this.extractBasicFilterDetails(responseArgs.columnFilters),
          emitActionChangedCallback: this.emitFilterChanged.bind(this),
          httpCancelRequestSubject: this.httpCancelRequests$
        });
      }
    }
    /**
     * When user passes an array of preset filters, we need to pre-populate each column filter searchTerm(s)
     * The process is to loop through the preset filters array, find the associated column from columnDefinitions and fill in the filter object searchTerm(s)
     * This is basically the same as if we would manually add searchTerm(s) to a column filter object in the column definition, but we do it programmatically.
     * At the end of the day, when creating the Filter (DOM Element), it will use these searchTerm(s) so we can take advantage of that without recoding each Filter type (DOM element)
     * @param grid
     */
    populateColumnFilterSearchTermPresets(filters) {
      if (Array.isArray(filters)) {
        this._columns.forEach((columnDef) => {
          if (columnDef.filter?.searchTerms) {
            delete columnDef.filter.searchTerms;
          }
          const columnPreset = filters.find((presetFilter) => presetFilter.columnId === columnDef.id);
          if (columnPreset && Array.isArray(columnPreset?.searchTerms)) {
            columnDef.filter = columnDef.filter || {};
            columnDef.filter.operator = columnPreset.operator || columnDef.filter.operator || "";
            columnDef.filter.searchTerms = columnPreset.searchTerms;
          }
        });
        if (this._gridOptions.enableTreeData) {
          this.refreshTreeDataFilters();
        }
        this._previousFilters = this.extractBasicFilterDetails(this._columnFilters);
      }
      return this._columns;
    }
    /**
     * when we have a Filter Presets on a Tree Data View grid, we need to call the pre-filtering of tree data
     * we need to do this because Tree Data is the only type of grid that requires a pre-filter (preFilterTreeData) to be executed before the final filtering
     * @param {Array<Object>} [items] - optional flat array of parent/child items to use while redoing the full sort & refresh
     */
    refreshTreeDataFilters(items) {
      const inputItems = items ?? this._dataView?.getItems() ?? [];
      if (this._dataView && this._gridOptions.enableTreeData && inputItems.length > 0) {
        this._tmpPreFilteredData = this.preFilterTreeData(inputItems, this._columnFilters);
        this._dataView.refresh();
      } else if (inputItems.length === 0 && Array.isArray(this.sharedService.hierarchicalDataset) && this.sharedService.hierarchicalDataset.length > 0) {
        queueMicrotaskPolyfill(() => this.refreshTreeDataFilters());
      }
    }
    /**
     * Toggle the Filter Functionality
     * @param {boolean} isFilterDisabled - optionally force a disable/enable of the Sort Functionality? Defaults to True
     * @param {boolean} clearFiltersWhenDisabled - when disabling the Filter, do we also want to clear all the filters as well? Defaults to True
     */
    disableFilterFunctionality(isFilterDisabled = true, clearFiltersWhenDisabled = true) {
      const prevShowFilterFlag = this._gridOptions.enableFiltering;
      const newShowFilterFlag = !prevShowFilterFlag;
      if (newShowFilterFlag !== isFilterDisabled) {
        if (clearFiltersWhenDisabled && isFilterDisabled) {
          this.clearFilters();
        }
        this.disableAllFilteringCommands(isFilterDisabled);
        this._grid.setOptions({ enableFiltering: newShowFilterFlag }, false, true);
        this._grid.setHeaderRowVisibility(newShowFilterFlag);
        this._gridOptions.enableFiltering = !isFilterDisabled;
        this.sharedService.gridOptions = this._gridOptions;
        this._grid.updateColumns();
      }
    }
    /**
     * Reset (revert) to previous filters, it could be because you prevented `onBeforeSearchChange` OR a Backend Error was thrown.
     * It will reapply the previous filter state in the UI.
     */
    resetToPreviousSearchFilters() {
      this.updateFilters(this._previousFilters, false, false, false);
    }
    /**
     * Toggle the Filter Functionality (show/hide the header row filter bar as well)
     * @param {boolean} clearFiltersWhenDisabled - when disabling the filters, do we want to clear the filters before hiding the filters? Defaults to True
     */
    toggleFilterFunctionality(clearFiltersWhenDisabled = true) {
      const prevShowFilterFlag = this._gridOptions.enableFiltering;
      this.disableFilterFunctionality(prevShowFilterFlag, clearFiltersWhenDisabled);
    }
    /**
     * Toggle the Header Row filter bar (this does not disable the Filtering itself, you can use "toggleFilterFunctionality()" instead, however this will reset any column positions)
     */
    toggleHeaderFilterRow() {
      let showHeaderRow = this._gridOptions.showHeaderRow ?? false;
      showHeaderRow = !showHeaderRow;
      this._grid.setHeaderRowVisibility(showHeaderRow);
      if (showHeaderRow === true) {
        this._grid.updateColumns();
      }
    }
    /**
     * Set the sort icons in the UI (ONLY the icons, it does not do any sorting)
     * The column sort icons are not necessarily inter-connected to the sorting functionality itself,
     * you can change the sorting icons separately by passing an array of columnId/sortAsc and that will change ONLY the icons
     * @param sortColumns
     */
    setSortColumnIcons(sortColumns) {
      if (this._grid && Array.isArray(sortColumns)) {
        this._grid.setSortColumns(sortColumns);
      }
    }
    /**
     * Update Filters dynamically just by providing an array of filter(s).
     * You can also choose emit (default) a Filter Changed event that will be picked by the Grid State Service.
     *
     * Also for backend service only, you can choose to trigger a backend query (default) or not if you wish to do it later,
     * this could be useful when using updateFilters & updateSorting and you wish to only send the backend query once.
     * @param filters array
     * @param triggerEvent defaults to True, do we want to emit a filter changed event?
     * @param triggerBackendQuery defaults to True, which will query the backend.
     * @param triggerOnSearchChangeEvent defaults to False, can be useful with Tree Data structure where the onSearchEvent has to run to execute a prefiltering step
     */
    async updateFilters(filters, emitChangedEvent = true, triggerBackendQuery = true, triggerOnSearchChangeEvent = false) {
      if (!this._filtersMetadata || this._filtersMetadata.length === 0 || !this._gridOptions || !this._gridOptions.enableFiltering) {
        throw new Error('[Slickgrid-Universal] in order to use "updateFilters" method, you need to have Filterable Columns defined in your grid and "enableFiltering" set in your Grid Options');
      }
      if (Array.isArray(filters)) {
        this.clearFilters(false);
        filters.forEach((newFilter) => {
          const uiFilter = this._filtersMetadata.find((filter) => newFilter.columnId === filter.columnDef.id);
          if (newFilter && uiFilter) {
            const newOperator = newFilter.operator || uiFilter.defaultOperator;
            this.updateColumnFilters(newFilter.searchTerms, uiFilter.columnDef, newOperator);
            uiFilter.setValues(newFilter.searchTerms || [], newOperator);
            if (triggerOnSearchChangeEvent || this._gridOptions.enableTreeData) {
              this.callbackSearchEvent(void 0, {
                columnDef: uiFilter.columnDef,
                operator: newOperator,
                searchTerms: newFilter.searchTerms,
                shouldTriggerQuery: true,
                forceOnSearchChangeEvent: true
              });
            }
          }
        });
        const backendApi = this._gridOptions.backendServiceApi;
        const emitterType = backendApi ? "remote" : "local";
        if (emitChangedEvent) {
          await this.emitFilterChanged(emitterType, true);
        }
        this._dataView.refresh();
        if (backendApi) {
          const backendApiService = backendApi?.service;
          if (backendApiService?.updateFilters) {
            backendApiService.updateFilters(filters, true);
            if (triggerBackendQuery) {
              this.backendUtilities?.refreshBackendDataset(this._gridOptions);
            }
          }
        }
        if (emitChangedEvent) {
          await this.emitFilterChanged(emitterType);
        }
      }
      return true;
    }
    /**
     * **NOTE**: This should only ever be used when having a global external search and hidding the grid inline filters (with `enableFiltering: true` and `showHeaderRow: false`).
     * For inline filters, please use `updateFilters()` instead.
     *
     * Update a Single Filter dynamically just by providing (columnId, operator and searchTerms)
     * You can also choose emit (default) a Filter Changed event that will be picked by the Grid State Service.
     * Also for backend service only, you can choose to trigger a backend query (default) or not if you wish to do it later,
     * this could be useful when using updateFilters & updateSorting and you wish to only send the backend query once.
     * @param filters array
     * @param triggerEvent defaults to True, do we want to emit a filter changed event?
     * @param triggerBackendQuery defaults to True, which will query the backend.
     */
    async updateSingleFilter(filter, emitChangedEvent = true, triggerBackendQuery = true) {
      const columnDef = this._grid.getColumns().find((col) => col.id === filter.columnId);
      if (columnDef && filter.columnId) {
        this._columnFilters = {};
        const emptySearchTermReturnAllValues = columnDef.filter?.emptySearchTermReturnAllValues ?? true;
        if (Array.isArray(filter.searchTerms) && (filter.searchTerms.length > 1 || filter.searchTerms.length === 1 && (!emptySearchTermReturnAllValues || filter.searchTerms[0] !== ""))) {
          this._columnFilters[filter.columnId] = {
            columnId: filter.columnId,
            operator: filter.operator,
            searchTerms: filter.searchTerms,
            columnDef,
            type: columnDef.type ?? "string"
          };
        }
        const backendApi = this._gridOptions.backendServiceApi;
        const emitterType = backendApi ? "remote" : "local";
        if (emitChangedEvent) {
          await this.emitFilterChanged(emitterType, true);
        }
        if (backendApi) {
          const backendApiService = backendApi?.service;
          if (backendApiService?.updateFilters) {
            backendApiService.updateFilters(this._columnFilters, true);
            if (triggerBackendQuery) {
              this.backendUtilities?.refreshBackendDataset(this._gridOptions);
            }
          }
        } else {
          this._dataView.setFilterArgs({
            columnFilters: this._columnFilters,
            grid: this._grid
          });
          if (this._gridOptions.enableTreeData) {
            this.refreshTreeDataFilters();
          }
          this._dataView.refresh();
        }
        if (emitChangedEvent) {
          await this.emitFilterChanged(emitterType);
        }
      }
      return true;
    }
    /**
     * Draw DOM Element Filter on custom HTML element
     * @param column - column id or column object
     * @param filterContainer - id element HTML or DOM element filter
     */
    drawFilterTemplate(column, filterContainer) {
      let filterContainerElm;
      if (typeof filterContainer === "string") {
        filterContainerElm = document.querySelector(filterContainer);
        if (filterContainerElm === null) {
          return null;
        }
      } else {
        filterContainerElm = filterContainer;
      }
      const columnDef = typeof column === "string" ? this._grid.getColumns().find((col) => col.id === column) : column;
      const columnId = columnDef?.id ?? "";
      if (columnId !== "selector" && columnDef?.filterable) {
        let searchTerms;
        let operator;
        const newFilter = this.filterFactory.createFilter(columnDef.filter);
        operator = columnDef && columnDef.filter && columnDef.filter.operator || newFilter && newFilter.operator;
        if (this._columnFilters[columnDef.id]) {
          searchTerms = this._columnFilters[columnDef.id].searchTerms || void 0;
          operator = this._columnFilters[columnDef.id].operator || void 0;
        } else if (columnDef.filter) {
          searchTerms = columnDef.filter.searchTerms || void 0;
          this.updateColumnFilters(searchTerms, columnDef, operator);
        }
        const filterArguments = {
          grid: this._grid,
          operator,
          searchTerms,
          columnDef,
          filterContainerElm,
          callback: this.callbackSearchEvent.bind(this)
        };
        if (newFilter) {
          newFilter.init(filterArguments);
          if (searchTerms && newFilter.setValues) {
            newFilter.setValues(searchTerms, operator);
          }
        }
        return newFilter;
      }
      return null;
    }
    // --
    // protected functions
    // -------------------
    /** Add all created filters (from their template) to the header row section area */
    addFilterTemplateToHeaderRow(args, isFilterFirstRender = true) {
      const columnDef = args.column;
      const columnId = columnDef?.id ?? "";
      if (columnId !== "selector" && columnDef?.filterable && !columnDef?.hidden) {
        let searchTerms;
        let operator;
        const newFilter = this.filterFactory.createFilter(columnDef.filter);
        operator = columnDef && columnDef.filter && columnDef.filter.operator || newFilter && newFilter.operator;
        if (this._columnFilters[columnDef.id]) {
          searchTerms = this._columnFilters[columnDef.id].searchTerms || void 0;
          operator = this._columnFilters[columnDef.id].operator || void 0;
        } else if (columnDef.filter) {
          searchTerms = columnDef.filter.searchTerms || void 0;
          this.updateColumnFilters(searchTerms, columnDef, operator);
        }
        const filterArguments = {
          grid: this._grid,
          operator,
          searchTerms,
          columnDef,
          filterContainerElm: args.node,
          callback: this.callbackSearchEvent.bind(this)
        };
        if (newFilter && filterArguments.filterContainerElm) {
          newFilter.init(filterArguments, isFilterFirstRender);
          const filterExistIndex = this._filtersMetadata.findIndex((filter) => newFilter.columnDef.id === filter.columnDef.id);
          if (filterExistIndex === -1) {
            this._filtersMetadata.push(newFilter);
          } else {
            this._filtersMetadata[filterExistIndex] = newFilter;
          }
          if (searchTerms && newFilter.setValues) {
            newFilter.setValues(searchTerms, operator);
          }
        }
      }
    }
    /**
     * Callback method that is called and executed by the individual Filter (DOM element),
     * for example when user starts typing chars on a search input (which uses InputFilter), this Filter will execute the callback from an input change event.
     */
    callbackSearchEvent(event, args) {
      if (args) {
        const searchTerm = event?.target ? event.target.value : void 0;
        const searchTerms = args.searchTerms && Array.isArray(args.searchTerms) ? args.searchTerms : searchTerm ? [searchTerm] : void 0;
        const columnDef = args.columnDef || null;
        const columnId = columnDef?.id ?? "";
        const fieldType = columnDef?.filter?.type ?? columnDef?.type ?? "string";
        const operator = args.operator || void 0;
        const hasSearchTerms = searchTerms && Array.isArray(searchTerms);
        const termsCount = hasSearchTerms && searchTerms && searchTerms.length;
        const oldColumnFilters = { ...this._columnFilters };
        const emptySearchTermReturnAllValues = columnDef.filter?.emptySearchTermReturnAllValues ?? true;
        let parsedSearchTerms;
        if (columnDef && columnId) {
          if (!hasSearchTerms || termsCount === 0 || termsCount === 1 && Array.isArray(searchTerms) && emptySearchTermReturnAllValues && searchTerms[0] === "") {
            delete this._columnFilters[columnId];
          } else {
            const colId = `${columnId}`;
            const colFilter = {
              columnId: colId,
              columnDef,
              parsedSearchTerms: [],
              type: fieldType,
              targetSelector: this.getSelectorStringFromElement(event?.target)
            };
            const inputSearchConditions = this.parseFormInputFilterConditions(searchTerms, colFilter);
            colFilter.operator = operator || inputSearchConditions.operator || mapOperatorByFieldType(fieldType);
            parsedSearchTerms = getParsedSearchTermsByFieldType(inputSearchConditions.searchTerms, fieldType);
            if (parsedSearchTerms !== void 0) {
              colFilter.parsedSearchTerms = parsedSearchTerms;
            }
            colFilter.searchTerms = inputSearchConditions.searchTerms || [];
            this._columnFilters[colId] = colFilter;
          }
        }
        const eventData = event && typeof event.isPropagationStopped !== "function" ? extend({}, new SlickEventData(), event) : event;
        const eventKey = event?.key;
        if (this._onSearchChange && (args.forceOnSearchChangeEvent || eventKey === "Enter" || !dequal(oldColumnFilters, this._columnFilters))) {
          const eventArgs = {
            clearFilterTriggered: args.clearFilterTriggered,
            shouldTriggerQuery: args.shouldTriggerQuery,
            columnId,
            columnDef,
            columnFilters: this._columnFilters,
            operator: operator || mapOperatorByFieldType(fieldType),
            searchTerms,
            parsedSearchTerms,
            grid: this._grid,
            target: event?.target
          };
          const onBeforeDispatchResult = this.pubSubService.publish("onBeforeSearchChange", eventArgs);
          if (onBeforeDispatchResult === false) {
            if (this._gridOptions.resetFilterSearchValueAfterOnBeforeCancellation) {
              this.resetToPreviousSearchFilters();
            }
          } else {
            this._onSearchChange.notify(eventArgs, eventData);
          }
        }
      }
    }
    /**
     * Loop through all column definitions and do the following thing
     * 1. loop through each Header Menu commands and change the "hidden" commands to show/hide depending if it's enabled/disabled
     * Also note that we aren't deleting any properties, we just toggle their flags so that we can reloop through at later point in time.
     * (if we previously deleted these properties we wouldn't be able to change them back since these properties wouldn't exist anymore, hence why we just hide the commands)
     * @param {boolean} isDisabling - are we disabling the filter functionality? Defaults to true
     */
    disableAllFilteringCommands(isDisabling = true) {
      const columns = this._grid.getColumns();
      columns.forEach((col) => {
        if (col?.header?.menu) {
          col.header.menu.commandItems?.forEach((menuItem) => {
            if (menuItem && typeof menuItem !== "string") {
              const menuCommand = menuItem.command;
              if (menuCommand === "clear-filter") {
                menuItem.hidden = isDisabling;
              }
            }
          });
        }
      });
      const commandItems = this._gridOptions.gridMenu?.commandItems;
      if (commandItems) {
        commandItems.forEach((menuItem) => {
          if (menuItem && typeof menuItem !== "string") {
            const menuCommand = menuItem.command;
            if (menuCommand === "clear-filter" || menuCommand === "toggle-filter") {
              menuItem.hidden = isDisabling;
            }
          }
        });
      }
      return columns;
    }
    /**
     * From a ColumnFilters object, extract only the basic filter details (columnId, operator & searchTerms)
     * @param {Object} columnFiltersObject - columnFilters object
     * @returns - basic details of a column filter
     */
    extractBasicFilterDetails(columnFiltersObject) {
      const filters = [];
      if (columnFiltersObject && typeof columnFiltersObject === "object") {
        for (const columnId of Object.keys(columnFiltersObject)) {
          const { operator, searchTerms } = columnFiltersObject[`${columnId}`];
          filters.push({ columnId, operator, searchTerms });
        }
      }
      return filters;
    }
    getSelectorStringFromElement(elm) {
      if (elm?.localName) {
        return elm?.className ? `${elm.localName}.${Array.from(elm.classList).join(".")}` : elm.localName;
      }
      return "";
    }
    /**
     * When clearing or disposing of all filters, we need to loop through all columnFilters and delete them 1 by 1
     * only trying to make columnFilter an empty (without looping) would not trigger a dataset change
     */
    removeAllColumnFiltersProperties() {
      if (typeof this._columnFilters === "object") {
        Object.keys(this._columnFilters).forEach((columnId) => {
          if (columnId && this._columnFilters[columnId]) {
            delete this._columnFilters[columnId];
          }
        });
      }
    }
    /**
     * Subscribe to `onBeforeHeaderRowCellDestroy` to destroy Filter(s) to avoid leak and not keep orphan filters
     * @param {Object} grid - Slick Grid object
     */
    subscribeToOnHeaderRowCellRendered(grid) {
      this._eventHandler.subscribe(grid.onBeforeHeaderRowCellDestroy, (_e, args) => {
        const colFilter = this._filtersMetadata.find((filter) => filter.columnDef.id === args.column.id);
        colFilter?.destroy?.();
      });
    }
    updateColumnFilters(searchTerms, columnDef, operator) {
      const fieldType = columnDef.filter?.type ?? columnDef.type ?? "string";
      const parsedSearchTerms = getParsedSearchTermsByFieldType(searchTerms, fieldType);
      if (searchTerms && columnDef) {
        this._columnFilters[columnDef.id] = {
          columnId: columnDef.id,
          columnDef,
          searchTerms,
          operator,
          parsedSearchTerms,
          type: fieldType
        };
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/grid.service.js
  var GridServiceDeleteOptionDefaults = { skipError: false, triggerEvent: true };
  var GridServiceInsertOptionDefaults = {
    highlightRow: true,
    resortGrid: false,
    selectRow: false,
    scrollRowIntoView: true,
    skipError: false,
    triggerEvent: true
  };
  var GridServiceUpdateOptionDefaults = {
    highlightRow: false,
    selectRow: false,
    scrollRowIntoView: false,
    skipError: false,
    triggerEvent: true
  };
  var HideColumnOptionDefaults = {
    applySetColumns: true,
    autoResizeColumns: true,
    triggerEvent: true,
    hideFromColumnPicker: false,
    hideFromGridMenu: false
  };
  var ShowColumnOptionDefaults = { autoResizeColumns: true, triggerEvent: true };
  var GridService = class {
    constructor(gridStateService, filterService, pubSubService, paginationService, sharedService, sortService, treeDataService) {
      this.gridStateService = gridStateService;
      this.filterService = filterService;
      this.pubSubService = pubSubService;
      this.paginationService = paginationService;
      this.sharedService = sharedService;
      this.sortService = sortService;
      this.treeDataService = treeDataService;
      this.pluginName = "GridService";
    }
    /** Getter of SlickGrid DataView object */
    get _dataView() {
      return this._grid?.getData();
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get _gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    dispose() {
      this._rowSelectionPlugin?.dispose();
    }
    init(grid) {
      this._grid = grid;
    }
    /** Clear all Filters & Sorts */
    clearAllFiltersAndSorts() {
      this.sortService?.clearSorting(false);
      this.filterService?.clearFilters();
    }
    /** Clear all the pinning (frozen) options */
    clearPinning(resetColumns = true) {
      this.sharedService.slickGrid.setOptions({
        frozenColumn: -1,
        frozenRow: -1,
        frozenBottom: false,
        enableMouseWheelScrollHandler: false
      });
      if (resetColumns) {
        this.sharedService.slickGrid.updateColumns();
      }
    }
    /**
     * Set pinning (frozen) grid options
     * @param  {Object} pinningOptions - which pinning/frozen options to modify
     * @param {Boolean} shouldAutosizeColumns - defaults to True, should we call an `autosizeColumns()` after the pinning is done?
     * @param {Boolean} suppressRender - do we want to supress the grid re-rendering? (defaults to false)
     * @param {Boolean} suppressColumnSet - do we want to supress the columns set, via `setColumns()` method? (defaults to true)
     * @param {Boolean} suppressUpdateColumns - do we want to supress the columns update, via `updateColumns()` method? (defaults to false, if you're calling `grid.setColumns()` afterward then you should suppress this)
     */
    setPinning(pinningOptions, shouldAutosizeColumns = true, suppressRender = false, suppressColumnSet = true, suppressUpdateColumns = false) {
      if (isObjectEmpty(pinningOptions)) {
        this.clearPinning();
      } else {
        this.sharedService.slickGrid.setOptions(pinningOptions, suppressRender, suppressColumnSet);
        this.sharedService.frozenVisibleColumnId = this._grid.getFrozenColumnId();
        this.sharedService.gridOptions = this._grid.getOptions();
      }
      if (shouldAutosizeColumns) {
        this.sharedService.slickGrid.autosizeColumns();
      }
      if (!suppressUpdateColumns) {
        this._grid.updateColumns();
      }
    }
    /**
     * Get all column set in the grid, that is all visible/hidden columns
     * and also include any extra columns used by some plugins (like Row Selection, Row Detail, ...)
     */
    getAllColumnDefinitions() {
      return this._grid.getColumns();
    }
    /** Get only visible column definitions and also include any extra columns by some plugins (like Row Selection, Row Detail, ...) */
    getVisibleColumnDefinitions() {
      return this._grid.getVisibleColumns();
    }
    /**
     * From a SlickGrid Event triggered get the Column Definition and Item Data Context
     *
     * For example the SlickGrid onClick will return cell arguments when subscribing to it.
     * From these cellArgs, we want to get the Column Definition and Item Data
     * @param cell event args
     * @return object with columnDef and dataContext
     */
    getColumnFromEventArguments(args) {
      if (!args?.grid || !args.grid.getColumnByIdx || !args.grid.getDataItem) {
        throw new Error("[Slickgrid-Universal] To get the column definition and data, we need to have these arguments passed as objects (row, cell, grid)");
      }
      return {
        row: args.row,
        cell: args.cell,
        columnDef: args.grid.getColumnByIdx(args.cell) || {},
        dataContext: args.grid.getDataItem(args.row),
        dataView: this._dataView,
        grid: this._grid
      };
    }
    /** Get data item by it's row index number */
    getDataItemByRowNumber(rowNumber) {
      if (!this._grid || typeof this._grid.getDataItem !== "function") {
        throw new Error(`[Slickgrid-Universal] We could not find SlickGrid Grid object or it's "getDataItem" method`);
      }
      return this._grid.getDataItem(rowNumber);
    }
    /** Get the Data Item from a grid row index */
    getDataItemByRowIndex(index2) {
      if (!this._grid || typeof this._grid.getDataItem !== "function") {
        throw new Error('[Slickgrid-Universal] We could not find SlickGrid Grid object and/or "getDataItem" method');
      }
      return this._grid.getDataItem(index2);
    }
    /** Get the Data Item from an array of grid row indexes */
    getDataItemByRowIndexes(indexes) {
      if (!this._grid || typeof this._grid.getDataItem !== "function") {
        throw new Error('[Slickgrid-Universal] We could not find SlickGrid Grid object and/or "getDataItem" method');
      }
      const dataItems = [];
      if (Array.isArray(indexes)) {
        indexes.forEach((idx) => {
          dataItems.push(this._grid.getDataItem(idx));
        });
      }
      return dataItems;
    }
    /** Get the currently selected row indexes */
    getSelectedRows() {
      if (!this._grid || typeof this._grid.getSelectedRows !== "function") {
        throw new Error('[Slickgrid-Universal] We could not find SlickGrid Grid object and/or "getSelectedRows" method');
      }
      return this._grid.getSelectedRows();
    }
    /** Get the currently selected rows item data */
    getSelectedRowsDataItem() {
      if (!this._grid || typeof this._grid.getSelectedRows !== "function") {
        throw new Error('[Slickgrid-Universal] We could not find SlickGrid Grid object and/or "getSelectedRows" method');
      }
      const selectedRowIndexes = this._grid.getSelectedRows();
      return this.getDataItemByRowIndexes(selectedRowIndexes);
    }
    /**
     * Hide a Column from the Grid by its id, the column will just become hidden and will still show up in columnPicker/gridMenu
     * @param {string | number} columnId - column definition id
     * @param {boolean} triggerEvent - do we want to trigger an event (`onHideColumns`) when column becomes hidden? Defaults to true.
     * @return {number} columnIndex - column index position when found or -1
     */
    hideColumnById(columnId, options) {
      if (this._grid) {
        options = { ...HideColumnOptionDefaults, ...options };
        const currentColumns = this._grid.getColumns();
        const colIndexFound = currentColumns.findIndex((col) => col.id === columnId);
        if (colIndexFound >= 0) {
          const visibleColumns = arrayRemoveItemByIndex(currentColumns, colIndexFound);
          this._grid.updateColumnById(columnId, { hidden: true }, options.applySetColumns !== false);
          const columnIndexFromAllColumns = this.getAllColumnDefinitions().findIndex((col) => col.id === columnId);
          if (columnIndexFromAllColumns) {
            if (options?.hideFromColumnPicker) {
              this.getAllColumnDefinitions()[columnIndexFromAllColumns].excludeFromColumnPicker = true;
            }
            if (options?.hideFromGridMenu) {
              this.getAllColumnDefinitions()[columnIndexFromAllColumns].excludeFromGridMenu = true;
            }
          }
          this.executeVisibilityCommands(options, ["onHideColumns"], visibleColumns);
          return colIndexFound;
        }
      }
      return -1;
    }
    /**
     * Hide multiple columns by their Ids, the column will just become hidden and will still show up in columnPicker/gridMenu
     * @param {Array<string | number>} columnIds - column ids to hide
     * @param {boolean} triggerEvent - do we want to trigger an event "onHideColumns" when column becomes hidden? Defaults to true.
     */
    hideColumnByIds(columnIds, options) {
      if (Array.isArray(columnIds)) {
        const finalVisibleColumns = this._grid.getColumns().filter((c) => !columnIds.includes(c.id));
        options = { ...HideColumnOptionDefaults, ...options };
        for (const columnId of columnIds) {
          this.hideColumnById(columnId, { ...options, triggerEvent: false, applySetColumns: false, autoResizeColumns: false });
        }
        this._grid.updateColumns();
        this.executeVisibilityCommands(options, ["onHideColumns"], finalVisibleColumns);
      }
    }
    /**
     * Show multiple columns by their Ids, any column outside the provided array will be considered hidden but will still show up in columnPicker/gridMenu
     * @param {Array<string | number>} columnIds - column ids to show
     * @param {boolean} triggerEvent - do we want to trigger an event (onShowColumns) when column becomes hidden? Defaults to true.
     */
    showColumnByIds(columnIds, options) {
      if (this._grid) {
        options = { ...ShowColumnOptionDefaults, ...options };
        this._grid.getColumns().forEach((col) => {
          this._grid.updateColumnById(col.id, { hidden: !columnIds.includes(col.id) });
        });
        this._grid.updateColumns();
        this.executeVisibilityCommands(options, ["onShowColumns"], this._grid.getColumns());
      }
    }
    executeVisibilityCommands(options, eventNames, columns) {
      if (options?.autoResizeColumns) {
        this._grid.autosizeColumns();
      }
      if (options?.triggerEvent) {
        eventNames.forEach((name) => this.pubSubService.publish(name, { columns }));
      }
    }
    /**
     * Highlight then fade a row for certain duration (ms).
     * @param {Number} rowNumber - grid row number
     * @param {Number} [duration] - duration in ms
     */
    highlightRow(rowNumber, duration) {
      if (!this._grid.getSelectionModel()) {
        const selectionType = this._gridOptions.selectionOptions?.selectionType || "row";
        this._rowSelectionPlugin = new SlickHybridSelectionModel({ ...this._gridOptions.selectionOptions, selectionType });
        this._grid.setSelectionModel(this._rowSelectionPlugin);
      }
      duration ||= this._gridOptions.rowHighlightDuration;
      if (Array.isArray(rowNumber)) {
        rowNumber.forEach((row) => this._grid.highlightRow(row));
      } else {
        this._grid.highlightRow(rowNumber, duration);
      }
    }
    /** Select the selected row by a row index */
    setSelectedRow(rowIndex) {
      this._grid?.setSelectedRows([rowIndex]);
    }
    /** Set selected rows with provided array of row indexes */
    setSelectedRows(rowIndexes) {
      this._grid?.setSelectedRows(rowIndexes);
    }
    /** Re-Render the Grid */
    renderGrid() {
      this._grid?.invalidate();
    }
    /**
     * Reset the grid to it's original state (clear any filters, sorting & pagination if exists) .
     * The column definitions could be passed as argument to reset (this can be used after a Grid State reset)
     * The reset will clear the Filters & Sort, then will reset the Columns to their original state
     */
    resetGrid(columns) {
      this.clearPinning(false);
      if (this._grid) {
        const originalColumns = this.sharedService.allColumns || [];
        if (Array.isArray(originalColumns) && originalColumns.length > 0) {
          this._grid.setColumns(originalColumns);
          if (this._gridOptions?.enableAutoSizeColumns) {
            this._grid.autosizeColumns();
          }
          this.gridStateService.resetColumns(columns);
        }
      }
      if (typeof this.filterService?.clearFilters === "function") {
        this.filterService.clearFilters();
      }
      if (typeof this.sortService?.clearSorting === "function") {
        this.sortService.clearSorting();
      }
    }
    /**
     * Add an item (data item) to the datagrid, by default it will highlight (flashing) the inserted row but we can disable it too
     * @param item object which must contain a unique "id" property and any other suitable properties
     * @param options: provide the possibility to do certain actions after or during the upsert (highlightRow, resortGrid, selectRow, triggerEvent)
     * @return rowIndex: typically index 0 when adding to position "top" or a different number when adding to the "bottom"
     */
    addItem(item, options) {
      const insertOptions = { ...GridServiceInsertOptionDefaults, ...options };
      if (!insertOptions?.skipError && (!this._grid || !this._gridOptions || !this._dataView)) {
        throw new Error("[Slickgrid-Universal] We could not find SlickGrid Grid, DataView objects");
      }
      const idPropName = this._gridOptions.datasetIdPropertyName || "id";
      if (!insertOptions?.skipError && (!item || !item.hasOwnProperty(idPropName))) {
        throw new Error(`[Slickgrid-Universal] Adding an item requires the item to include an "${idPropName}" property`);
      }
      if (this._gridOptions?.enableTreeData && options?.position === "top") {
        throw new Error('[Slickgrid-Universal] Please note that `addItem({ position: "top" })` is not supported when used with Tree Data because of the extra complexity.');
      }
      const insertPosition = insertOptions?.position;
      if (insertPosition === "bottom" || this._gridOptions?.enableTreeData) {
        this._dataView.addItem(item);
      } else {
        this._dataView.insertItem(0, item);
      }
      let rowNumber = 0;
      const itemId = item?.[idPropName] ?? "";
      if (this._gridOptions?.enableTreeData) {
        this.invalidateHierarchicalDataset();
        rowNumber = this._dataView.getRowById(itemId);
        if (insertOptions.scrollRowIntoView) {
          this._grid.scrollRowIntoView(rowNumber ?? 0, false);
        }
      } else if (insertOptions.resortGrid) {
        this._dataView.reSort();
        rowNumber = this._dataView.getRowById(itemId);
      } else {
        rowNumber = insertPosition === "bottom" ? this._dataView.getRowById(itemId) : 0;
        if (insertOptions.scrollRowIntoView) {
          this._grid.scrollRowIntoView(rowNumber ?? 0);
        }
      }
      if (insertOptions.highlightRow && rowNumber !== void 0) {
        this.highlightRow(rowNumber);
      }
      if (rowNumber !== void 0 && insertOptions.selectRow && this._gridOptions && (this._gridOptions.enableCheckboxSelector || this._gridOptions.enableSelection)) {
        this.setSelectedRow(rowNumber);
      }
      if (insertOptions.triggerEvent) {
        this.pubSubService.publish("onItemsAdded", [item]);
      }
      const isLocalGrid = !this._gridOptions?.backendServiceApi;
      if (isLocalGrid && this._gridOptions.enablePagination) {
        insertPosition === "bottom" ? this.paginationService.goToLastPage() : this.paginationService.goToFirstPage();
      }
      return rowNumber;
    }
    /**
     * Add item array (data item) to the datagrid, by default it will highlight (flashing) the inserted row but we can disable it too
     * @param item object arrays, which must contain unique "id" property and any other suitable properties
     * @param options: provide the possibility to do certain actions after or during the upsert (highlightRow, resortGrid, selectRow, triggerEvent)
     */
    addItems(items, options) {
      const insertOptions = { ...GridServiceInsertOptionDefaults, ...options };
      const idPropName = this._gridOptions.datasetIdPropertyName || "id";
      const insertPosition = insertOptions?.position;
      const rowNumbers = [];
      if (!Array.isArray(items)) {
        return [this.addItem(items, insertOptions) || 0];
      } else {
        this._dataView.beginUpdate(true);
        if (insertPosition === "bottom" || this._gridOptions?.enableTreeData) {
          this._dataView.addItems(items);
        } else {
          this._dataView.insertItems(0, items);
        }
        this._dataView.endUpdate();
      }
      if (this._gridOptions?.enableTreeData) {
        this.invalidateHierarchicalDataset();
        const firstItemId = items[0]?.[idPropName] ?? "";
        const rowNumber = this._dataView.getRowById(firstItemId);
        if (insertOptions.scrollRowIntoView) {
          this._grid.scrollRowIntoView(rowNumber ?? 0, false);
        }
      } else if (insertOptions.resortGrid) {
        this._dataView.reSort();
      }
      if (insertPosition && insertOptions.scrollRowIntoView && !this._gridOptions?.enableTreeData) {
        insertPosition === "bottom" ? this._grid.navigateBottom() : this._grid.navigateTop();
      }
      items.forEach((item) => rowNumbers.push(this._dataView.getRowById(item[idPropName])));
      if (insertOptions.highlightRow) {
        this.highlightRow(rowNumbers);
      }
      if (insertOptions.selectRow && this._gridOptions && (this._gridOptions.enableCheckboxSelector || this._gridOptions.enableSelection)) {
        this.setSelectedRows(rowNumbers);
      }
      if (insertOptions.triggerEvent) {
        this.pubSubService.publish("onItemsAdded", items);
      }
      return rowNumbers;
    }
    /**
     * Delete an existing item from the datagrid (dataView)
     * @param item object which must contain a unique "id" property and any other suitable properties
     * @param options: provide the possibility to do certain actions after or during the upsert (triggerEvent)
     * @return item id deleted
     */
    deleteItem(item, options) {
      options = { ...GridServiceDeleteOptionDefaults, ...options };
      const idPropName = this._gridOptions.datasetIdPropertyName || "id";
      if (!options?.skipError && (!item || !item.hasOwnProperty(idPropName))) {
        throw new Error(`[Slickgrid-Universal] Deleting an item requires the item to include an "${idPropName}" property`);
      }
      return this.deleteItemById(item[idPropName], options);
    }
    /**
     * Delete an array of existing items from the datagrid
     * @param item object which must contain a unique "id" property and any other suitable properties
     * @param options: provide the possibility to do certain actions after or during the upsert (triggerEvent)
     * @return item id deleted
     */
    deleteItems(items, options) {
      options = { ...GridServiceDeleteOptionDefaults, ...options };
      const idPropName = this._gridOptions.datasetIdPropertyName || "id";
      if (!Array.isArray(items)) {
        this.deleteItem(items, options);
        return [items[idPropName]];
      }
      this._dataView.beginUpdate(true);
      const itemIds = [];
      items.forEach((item) => {
        if (item?.[idPropName] !== void 0) {
          itemIds.push(item[idPropName]);
        }
      });
      this._dataView.deleteItems(itemIds);
      this._dataView.endUpdate();
      if (options.triggerEvent) {
        this.pubSubService.publish("onItemsDeleted", items);
      }
      return itemIds;
    }
    /**
     * Delete an existing item from the datagrid (dataView) by it's id
     * @param itemId: item unique id
     * @param options: provide the possibility to do certain actions after or during the upsert (triggerEvent)
     * @return item id deleted
     */
    deleteItemById(itemId, options) {
      options = { ...GridServiceDeleteOptionDefaults, ...options };
      if (!options?.skipError && (itemId === null || itemId === void 0)) {
        throw new Error(`[Slickgrid-Universal] Cannot delete a row without a valid "id"`);
      }
      const isSyncGridSelectionEnabled = this.gridStateService && this.gridStateService.needToPreserveRowSelection() || false;
      if (!isSyncGridSelectionEnabled && this._grid && this._gridOptions && (this._gridOptions.enableCheckboxSelector || this._gridOptions.enableSelection)) {
        this.setSelectedRows([]);
      }
      this._dataView.deleteItem(itemId);
      if (options.triggerEvent) {
        this.pubSubService.publish("onItemsDeleted", [itemId]);
      }
      return itemId;
    }
    /**
     * Delete an array of existing items from the datagrid
     * @param itemIds array of item unique IDs
     * @param options: provide the possibility to do certain actions after or during the upsert (triggerEvent)
     */
    deleteItemByIds(itemIds, options) {
      options = { ...GridServiceDeleteOptionDefaults, ...options };
      if (Array.isArray(itemIds)) {
        this._dataView.beginUpdate(true);
        for (let i = 0; i < itemIds.length; i++) {
          if (itemIds[i] !== null) {
            this.deleteItemById(itemIds[i], { triggerEvent: false });
          }
        }
        this._dataView.endUpdate();
        if (options.triggerEvent) {
          this.pubSubService.publish("onItemsDeleted", itemIds);
        }
        return itemIds;
      }
      return [];
    }
    /**
     * Update an existing item with new properties inside the datagrid
     * @param item object which must contain a unique "id" property and any other suitable properties
     * @param options: provide the possibility to do certain actions after or during the upsert (highlightRow, selectRow, triggerEvent)
     * @return grid row index
     */
    updateItem(item, options) {
      options = { ...GridServiceUpdateOptionDefaults, ...options };
      const idPropName = this._gridOptions.datasetIdPropertyName || "id";
      const itemId = !item || !item.hasOwnProperty(idPropName) ? void 0 : item[idPropName];
      if (!options?.skipError && itemId === void 0) {
        throw new Error(`[Slickgrid-Universal] Calling Update of an item requires the item to include an "${idPropName}" property`);
      }
      return this.updateItemById(itemId, item, options);
    }
    /**
     * Update an array of existing items with new properties inside the datagrid
     * @param item object arrays, which must contain unique "id" property and any other suitable properties
     * @param options: provide the possibility to do certain actions after or during the update (highlightRow, selectRow, triggerEvent)
     * @return grid row indexes
     */
    updateItems(items, options) {
      options = { ...GridServiceUpdateOptionDefaults, ...options };
      const idPropName = this._gridOptions.datasetIdPropertyName || "id";
      if (!Array.isArray(items)) {
        return [this.updateItem(items, options)];
      }
      this._dataView.beginUpdate(true);
      const rowNumbers = [];
      const itemIds = [];
      items.forEach((item) => {
        const itemId = !item || !item.hasOwnProperty(idPropName) ? void 0 : item[idPropName];
        itemIds.push(itemId);
        if (this._dataView.getIdxById(itemId) !== void 0) {
          const rowNumber = this._dataView.getRowById(itemId);
          if (rowNumber !== void 0) {
            rowNumbers.push(rowNumber);
            this._grid.updateRow(rowNumber);
          }
        }
      });
      this._dataView.updateItems(itemIds, items);
      this._dataView.endUpdate();
      if (this._gridOptions?.enableTreeData) {
        this.invalidateHierarchicalDataset();
      }
      if (options.highlightRow) {
        this.highlightRow(rowNumbers);
      }
      if (options.selectRow && this._gridOptions && (this._gridOptions.enableCheckboxSelector || this._gridOptions.enableSelection)) {
        this.setSelectedRows(rowNumbers);
      }
      if (options.triggerEvent) {
        this.pubSubService.publish("onItemsUpdated", items);
      }
      return rowNumbers;
    }
    /**
     * Update an existing item in the datagrid by it's id and new properties
     * @param itemId: item unique id
     * @param item object which must contain a unique "id" property and any other suitable properties
     * @param options: provide the possibility to do certain actions after or during the upsert (highlightRow, selectRow, triggerEvent)
     * @return grid row number
     */
    updateItemById(itemId, item, options) {
      options = { ...GridServiceUpdateOptionDefaults, ...options };
      if (!options?.skipError && itemId === void 0) {
        throw new Error(`[Slickgrid-Universal] Cannot update a row without a valid "id"`);
      }
      const rowNumber = this._dataView.getRowById(itemId);
      if (!options?.skipError && !item && !this._gridOptions.enablePagination) {
        throw new Error(`[Slickgrid-Universal] The item to update in the grid was not found with id: ${itemId}`);
      }
      if (this._dataView.getIdxById(itemId) !== void 0) {
        this._dataView.updateItem(itemId, item);
        if (rowNumber !== void 0) {
          this._grid.updateRow(rowNumber);
        }
        if (this._gridOptions?.enableTreeData) {
          this.invalidateHierarchicalDataset();
        }
        if (options.scrollRowIntoView && rowNumber !== void 0) {
          this._grid.scrollRowIntoView(rowNumber);
        }
        if (options.highlightRow && rowNumber !== void 0) {
          this.highlightRow(rowNumber);
        }
        if (rowNumber !== void 0 && options.selectRow && this._gridOptions && (this._gridOptions.enableCheckboxSelector || this._gridOptions.enableSelection)) {
          this.setSelectedRow(rowNumber);
        }
        if (options.triggerEvent) {
          this.pubSubService.publish("onItemsUpdated", [item]);
        }
      }
      return rowNumber;
    }
    /**
     * Insert a row into the grid if it doesn't already exist or update if it does.
     * @param item object which must contain a unique "id" property and any other suitable properties
     * @param options: provide the possibility to do certain actions after or during the upsert (highlightRow, resortGrid, selectRow, triggerEvent)
     */
    upsertItem(item, options) {
      options = { ...GridServiceInsertOptionDefaults, ...options };
      const idPropName = this._gridOptions.datasetIdPropertyName || "id";
      const itemId = !item || !item.hasOwnProperty(idPropName) ? void 0 : item[idPropName];
      if (!options?.skipError && itemId === void 0) {
        throw new Error(`[Slickgrid-Universal] Calling Upsert of an item requires the item to include an "${idPropName}" property`);
      }
      return this.upsertItemById(itemId, item, options);
    }
    /**
     * Update an array of existing items with new properties inside the datagrid
     * @param item object arrays, which must contain unique "id" property and any other suitable properties
     * @param options: provide the possibility to do certain actions after or during the upsert (highlightRow, resortGrid, selectRow, triggerEvent)
     * @return row numbers in the grid
     */
    upsertItems(items, options) {
      options = { ...GridServiceInsertOptionDefaults, ...options };
      if (!Array.isArray(items)) {
        return [this.upsertItem(items, options)];
      }
      this._dataView.beginUpdate(true);
      const upsertedRows = [];
      items.forEach((item) => {
        upsertedRows.push(this.upsertItem(item, { ...options, highlightRow: false, resortGrid: false, selectRow: false, triggerEvent: false }));
      });
      this._dataView.endUpdate();
      const rowNumbers = upsertedRows.map((upsertRow) => upsertRow.added !== void 0 ? upsertRow.added : upsertRow.updated);
      if (options.highlightRow) {
        this.highlightRow(rowNumbers);
      }
      if (options.selectRow && this._gridOptions && (this._gridOptions.enableCheckboxSelector || this._gridOptions.enableSelection)) {
        this.setSelectedRows(rowNumbers);
      }
      if (options.triggerEvent) {
        this.pubSubService.publish("onItemsUpserted", items);
        const addedItems = upsertedRows.filter((upsertRow) => upsertRow.added !== void 0);
        if (Array.isArray(addedItems) && addedItems.length > 0) {
          this.pubSubService.publish("onItemsAdded", addedItems);
        }
        const updatedItems = upsertedRows.filter((upsertRow) => upsertRow.updated !== void 0);
        if (Array.isArray(updatedItems) && updatedItems.length > 0) {
          this.pubSubService.publish("onItemsUpdated", updatedItems);
        }
      }
      return upsertedRows;
    }
    /**
     * Update an existing item in the datagrid by it's id and new properties
     * @param itemId: item unique id
     * @param item object which must contain a unique "id" property and any other suitable properties
     * @param options: provide the possibility to do certain actions after or during the upsert (highlightRow, resortGrid, selectRow, triggerEvent)
     * @return grid row number in the grid
     */
    upsertItemById(itemId, item, options) {
      let isItemAdded = false;
      options = { ...GridServiceInsertOptionDefaults, ...options };
      if (!options?.skipError && itemId === void 0 && !this.hasRowSelectionEnabled()) {
        throw new Error(`[Slickgrid-Universal] Calling Upsert of an item requires the item to include a valid and unique "id" property`);
      }
      let rowNumberAdded;
      let rowNumberUpdated;
      if (this._dataView.getRowById(itemId) === void 0) {
        rowNumberAdded = this.addItem(item, options);
        isItemAdded = true;
      } else {
        rowNumberUpdated = this.updateItem(item, {
          highlightRow: options.highlightRow,
          selectRow: options.selectRow,
          triggerEvent: options.triggerEvent
        });
        isItemAdded = false;
      }
      if (options.triggerEvent) {
        this.pubSubService.publish("onItemsUpserted", [item]);
        this.pubSubService.publish(isItemAdded ? "onItemsAdded" : "onItemsUpdated", [item]);
      }
      return { added: rowNumberAdded, updated: rowNumberUpdated };
    }
    /**
     * When dealing with hierarchical (tree) dataset, we can invalidate all the rows and force a full resort & re-render of the hierarchical tree dataset.
     * This method will automatically be called anytime user called `addItem()` or `addItems()` and it will reuse current column sorting when found (or use initial sort).
     * However please note that it won't be called when `updateItem`, if the data that gets updated does change the tree data column then you should call this method.
     * @param {Array<Object>} [items] - optional flat array of parent/child items to use while redoing the full sort & refresh
     */
    invalidateHierarchicalDataset(items) {
      if (this._gridOptions?.enableTreeData && this.treeDataService) {
        const inputItems = items ?? this._dataView.getItems();
        const sortCols = this.sortService.getCurrentColumnSorts();
        const sortedDatasetResult = this.treeDataService.convertFlatParentChildToTreeDatasetAndSort(inputItems || [], this.getAllColumnDefinitions(), this._gridOptions, sortCols);
        this.sharedService.hierarchicalDataset = sortedDatasetResult.hierarchical;
        this.filterService.refreshTreeDataFilters(items);
        this._dataView.setItems(sortedDatasetResult.flat);
        this._grid.invalidate();
      }
    }
    // --
    // protected functions
    // -------------------
    /** Check wether the grid has the Row Selection enabled */
    hasRowSelectionEnabled() {
      const selectionModel = this._grid.getSelectionModel();
      const { enableSelection, enableCheckboxSelector } = this._gridOptions;
      const isRowSelectionEnabled = !!(enableSelection || enableCheckboxSelector);
      return isRowSelectionEnabled && !!selectionModel;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/gridEvent.service.js
  var GridEventService = class {
    get eventHandler() {
      return this._eventHandler;
    }
    constructor() {
      this._eventHandler = new SlickEventHandler();
    }
    dispose() {
      this._eventHandler.unsubscribeAll();
    }
    /* OnBeforeEditCell Event */
    bindOnBeforeEditCell(grid) {
      const dataView = grid?.getData();
      this._eventHandler.subscribe(grid.onBeforeEditCell, (e2, args) => {
        if (!e2 || !args || !grid || args.cell === void 0 || !grid.getColumns || !grid.getDataItem) {
          return;
        }
        const column = grid.getColumnByIdx(args.cell);
        if (typeof column?.onBeforeEditCell === "function") {
          const returnedArgs = {
            row: args.row,
            cell: args.cell,
            dataView,
            grid,
            columnDef: column,
            dataContext: grid.getDataItem(args.row)
          };
          column.onBeforeEditCell(e2 instanceof SlickEventData ? e2.getNativeEvent() : e2, returnedArgs);
        }
      });
    }
    /* OnCellChange Event */
    bindOnCellChange(grid) {
      const dataView = grid?.getData();
      this._eventHandler.subscribe(grid.onCellChange, (e2, args) => {
        if (!e2 || !args || !grid || args.cell === void 0 || !grid.getColumns || !grid.getDataItem) {
          return;
        }
        const column = grid.getColumnByIdx(args.cell);
        if (typeof column?.onCellChange === "function") {
          const returnedArgs = {
            row: args.row,
            cell: args.cell,
            dataView,
            grid,
            columnDef: column,
            dataContext: grid.getDataItem(args.row)
          };
          column.onCellChange(e2 instanceof SlickEventData ? e2.getNativeEvent() : e2, returnedArgs);
        }
      });
    }
    /* OnClick Event */
    bindOnClick(grid) {
      const dataView = grid?.getData();
      this._eventHandler.subscribe(grid.onClick, (e2, args) => {
        if (!e2 || !args || !grid || args.cell === void 0 || !grid.getColumns || !grid.getDataItem) {
          return;
        }
        const column = grid.getColumnByIdx(args.cell);
        if (typeof column?.onCellClick === "function") {
          const returnedArgs = {
            row: args.row,
            cell: args.cell,
            dataView,
            grid,
            columnDef: column,
            dataContext: grid.getDataItem(args.row)
          };
          column.onCellClick(e2 instanceof SlickEventData ? e2.getNativeEvent() : e2, returnedArgs);
        }
      });
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/gridState.service.js
  var GridStateService = class {
    constructor(extensionService, filterService, pubSubService, sharedService, sortService, treeDataService) {
      this.extensionService = extensionService;
      this.filterService = filterService;
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this.sortService = sortService;
      this.treeDataService = treeDataService;
      this.pluginName = "GridStateService";
      this._columns = [];
      this._subscriptions = [];
      this._selectedRowIndexes = [];
      this._selectedRowDataContextIds = [];
      this._wasRecheckedAfterPageChange = true;
      this._eventHandler = new SlickEventHandler();
    }
    /** Getter of SlickGrid DataView object */
    get _dataView() {
      return this._grid?.getData() ?? {};
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get _gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    /** Getter of the selected data context object IDs */
    get selectedRowDataContextIds() {
      return this._selectedRowDataContextIds;
    }
    /** Setter of the selected data context object IDs */
    set selectedRowDataContextIds(dataContextIds) {
      this._selectedRowDataContextIds = dataContextIds;
    }
    /**
     * Initialize the Service
     * @param grid
     */
    init(grid) {
      this._grid = grid;
      this.subscribeToAllGridChanges(grid);
    }
    /** Dispose of all the SlickGrid & PubSub subscriptions */
    dispose() {
      this._columns = [];
      this._eventHandler.unsubscribeAll();
      this.pubSubService.unsubscribeAll(this._subscriptions);
    }
    /**
     * Dynamically change the arrangement/distribution of the columns Positions/Visibilities and optionally Widths.
     * For a column to have its visibly as hidden, it has to be part of the original list but excluded from the list provided as argument to be considered a hidden field.
     * If you are passing columns Width, then you probably don't want to trigger the autosizeColumns (2nd argument to False).
     * We could also resize the columns by their content but be aware that you can only trigger 1 type of resize at a time (either the 2nd argument or the 3rd last argument but not both at same time)
     * The resize by content could be called by the 3rd argument OR simply by enabling `enableAutoResizeColumnsByCellContent` but again this will only get executed when the 2nd argument is set to false.
     * @param {Array<Column>} definedColumns - defined columns
     * @param {Boolean} triggerAutoSizeColumns - True by default, do we also want to call the "autosizeColumns()" method to make the columns fit in the grid?
     * @param {Boolean} triggerColumnsFullResizeByContent - False by default, do we also want to call full columns resize by their content?
     */
    changeColumnsArrangement(definedColumns, triggerAutoSizeColumns = true, triggerColumnsFullResizeByContent = false) {
      if (Array.isArray(definedColumns) && definedColumns.length > 0) {
        const newArrangedColumns = this.getAssociatedGridColumns(this._grid, definedColumns);
        if (Array.isArray(newArrangedColumns) && newArrangedColumns.length > 0) {
          if (Array.isArray(this.sharedService.allColumns)) {
            const dynamicAddonColumnByIndexPositionList = [];
            if (this._gridOptions.enableRowMoveManager) {
              dynamicAddonColumnByIndexPositionList.push({
                columnId: this._gridOptions?.rowMoveManager?.columnId ?? "_move",
                columnIndexPosition: this._gridOptions?.rowMoveManager?.columnIndexPosition ?? 0
              });
            }
            if (this._gridOptions.enableCheckboxSelector) {
              dynamicAddonColumnByIndexPositionList.push({
                columnId: this._gridOptions?.checkboxSelector?.columnId ?? "_checkbox_selector",
                columnIndexPosition: this._gridOptions?.checkboxSelector?.columnIndexPosition ?? 0
              });
            }
            if (this._gridOptions.enableRowDetailView) {
              dynamicAddonColumnByIndexPositionList.push({
                columnId: this._gridOptions?.rowDetailView?.columnId ?? "_detail_selector",
                columnIndexPosition: this._gridOptions?.rowDetailView?.columnIndexPosition ?? 0
              });
            }
            this.addColumnDynamicWhenFeatureEnabled(dynamicAddonColumnByIndexPositionList, this.sharedService.allColumns, newArrangedColumns);
          }
          newArrangedColumns.forEach((col) => col.originalWidth = col.width || col.originalWidth);
          let allColumns = this._grid.getColumns();
          if (!allColumns.length) {
            allColumns = this._columns;
          }
          const orderedColumns = sortPresetColumns(allColumns, newArrangedColumns);
          this._grid.setColumns(orderedColumns);
          if (triggerAutoSizeColumns) {
            this._grid.autosizeColumns();
          } else if (triggerColumnsFullResizeByContent || this._gridOptions.enableAutoResizeColumnsByCellContent && !this._gridOptions.autosizeColumnsByCellContentOnFirstLoad) {
            this.pubSubService.publish("onFullResizeByContentRequested", { caller: "GridStateService" });
          }
        }
      }
    }
    /**
     * Get the current grid state (filters/sorters/pagination), by default we'll return only the visible columns
     * but with the argument `includeHiddenColumns` set to true, we could instead return all columns including their "hidden"
     * @param {Boolean} [includeHiddenColumns=false] - should we include all columns including the hidden column and their "hidden"
     * @return grid state
     */
    getCurrentGridState(includeHiddenColumns = false) {
      const isIncludingHiddenProps = !!(includeHiddenColumns || this._gridOptions.gridStateIncludeHiddenProps);
      const { frozenColumn, frozenRow, frozenBottom } = this.sharedService.gridOptions;
      const gridState = {
        columns: this.getCurrentColumns(isIncludingHiddenProps),
        filters: this.getCurrentFilters(),
        sorters: this.getCurrentSorters(),
        pinning: { frozenColumn, frozenRow, frozenBottom }
      };
      const currentGrouping = this.getCurrentGrouping();
      if (currentGrouping) {
        gridState.grouping = currentGrouping;
      }
      const currentPagination = this.getCurrentPagination();
      if (currentPagination) {
        gridState.pagination = currentPagination;
      }
      if (this.hasRowSelectionEnabled()) {
        const currentRowSelection = this.getCurrentRowSelections();
        if (currentRowSelection) {
          gridState.rowSelection = currentRowSelection;
        }
      }
      if (this._gridOptions?.enableTreeData) {
        const treeDataTreeToggleState = this.getCurrentTreeDataToggleState();
        if (treeDataTreeToggleState) {
          gridState.treeData = treeDataTreeToggleState;
        }
      }
      return gridState;
    }
    /**
     * Get the Columns (and their state: visibility/position) that are currently applied in the grid
     * @return {Array<Column>} current columns
     */
    getColumns() {
      return this._columns;
    }
    /**
     * From an array of Grid Column Definitions, get the associated Current Columns.
     * @param {Array<Column>} gridColumns
     * @param {Boolean} [includeHiddenProps=false] - should we also include the "hidden" on all the columns? defaults to `false`
     * @returns {{Array<CurrentColumn>} current columns
     */
    getAssociatedCurrentColumns(gridColumns, includeHiddenProps = false) {
      const currentColumns = [];
      if (Array.isArray(gridColumns)) {
        gridColumns.forEach((column) => {
          if (column?.id) {
            const currColumn = {
              columnId: column.id,
              cssClass: column.cssClass || "",
              headerCssClass: column.headerCssClass || "",
              width: column.width || 0
            };
            if (includeHiddenProps) {
              currColumn.hidden = column.hidden;
            }
            currentColumns.push(currColumn);
          }
        });
      }
      return currentColumns;
    }
    /**
     * From an array of Grid State Columns (`CurrentColumn[]`), get the associated Grid Column Definitions
     * @param {Object} grid
     * @param {Array<CurrentColumn>} currentColumns (e.g. Grid Preset)
     * @returns {Array<Column>} column definitions
     */
    getAssociatedGridColumns(grid, currentColumns) {
      const columns = [];
      const gridColumns = this.sharedService.allColumns || grid.getColumns();
      if (currentColumns && Array.isArray(currentColumns)) {
        currentColumns.forEach((currentColumn) => {
          const gridColumn = gridColumns.find((c) => c.id === currentColumn.columnId);
          if (gridColumn?.id) {
            columns.push({
              ...gridColumn,
              cssClass: currentColumn.cssClass || gridColumn.cssClass,
              headerCssClass: currentColumn.headerCssClass || gridColumn.headerCssClass,
              hidden: currentColumn.hidden || false,
              // for the width we will only pull the custom width or else nothing
              // since we don't want to use the default width that SlickGrid uses internally (which is 60px),
              // because that would cancel any column resize done by Slickgrid-Universal (like autoResizeColumnsByCellContent)
              width: currentColumn.width
            });
          }
        });
      }
      this._columns = columns;
      return columns;
    }
    /**
     * Get the Columns (and their states: visibility/position/width) that are currently applied in the grid
     * @param {Boolean} [includeHiddenColumns=false] - should we include all columns including the hidden column and their "hidden"
     * @return current columns
     */
    getCurrentColumns(includeHiddenColumns = false) {
      const columns = includeHiddenColumns ? this._grid.getColumns() : this._grid.getVisibleColumns();
      return this.getAssociatedCurrentColumns(columns, includeHiddenColumns);
    }
    /**
     * Get the Filters (and their state, columnId, searchTerm(s)) that are currently applied in the grid
     * @return current filters
     */
    getCurrentFilters() {
      if (this._gridOptions?.backendServiceApi) {
        const backendService = this._gridOptions.backendServiceApi.service;
        if (backendService?.getCurrentFilters) {
          return backendService.getCurrentFilters();
        }
      } else if (this.filterService?.getCurrentLocalFilters) {
        return this.filterService.getCurrentLocalFilters();
      }
      return null;
    }
    /**
     * Get the Grouping column IDs or null when there are no Grouping set
     * @returns current Grouping column IDs
     */
    getCurrentGrouping() {
      if (this._gridOptions?.enableGrouping || this._gridOptions.enableDraggableGrouping) {
        return this._dataView.getGrouping().map((g2) => g2.getter?.toString() || "");
      }
      return null;
    }
    /**
     * Get current Pagination (and its state, pageNumber, pageSize) that are currently applied in the grid
     * @return current pagination state
     */
    getCurrentPagination() {
      if (this._gridOptions?.enablePagination) {
        if (this._gridOptions.backendServiceApi) {
          const backendService = this._gridOptions.backendServiceApi.service;
          if (backendService?.getCurrentPagination) {
            return backendService.getCurrentPagination();
          }
        }
        return this.sharedService.currentPagination;
      }
      return null;
    }
    /**
     * Get the current Row Selections (and its state, gridRowIndexes, dataContextIds, filteredDataContextIds) that are currently applied in the grid
     * @return current row selections
     */
    getCurrentRowSelections() {
      if (this._grid && this._dataView && this.hasRowSelectionEnabled()) {
        return {
          gridRowIndexes: this._grid.getSelectedRows() || [],
          dataContextIds: this._dataView.getAllSelectedIds() || [],
          filteredDataContextIds: this._dataView.getAllSelectedFilteredIds() || []
        };
      }
      return null;
    }
    /**
     * Get the current Sorters (and their state, columnId, direction) that are currently applied in the grid
     * @return current sorters
     */
    getCurrentSorters() {
      if (this._gridOptions?.backendServiceApi) {
        const backendService = this._gridOptions.backendServiceApi.service;
        if (backendService?.getCurrentSorters) {
          return backendService.getCurrentSorters();
        }
      } else if (this.sortService?.getCurrentLocalSorters) {
        return this.sortService.getCurrentLocalSorters();
      }
      return null;
    }
    /**
     * Get the current list of Tree Data item(s) that got toggled in the grid
     * @returns {Array<TreeToggledItem>} treeDataToggledItems - items that were toggled (array of `parentId` and `isCollapsed` flag)
     */
    getCurrentTreeDataToggleState() {
      if (this._gridOptions?.enableTreeData && this.treeDataService) {
        return this.treeDataService.getCurrentToggleState();
      }
      return null;
    }
    /** Check whether the row selection needs to be preserved */
    needToPreserveRowSelection() {
      let preservedRowSelection = false;
      if (this._gridOptions?.dataView && this._gridOptions.dataView.hasOwnProperty("syncGridSelection")) {
        const syncGridSelection = this._gridOptions.dataView.syncGridSelection;
        if (typeof syncGridSelection === "boolean") {
          preservedRowSelection = this._gridOptions.dataView.syncGridSelection;
        } else if (typeof syncGridSelection === "object") {
          preservedRowSelection = syncGridSelection.preserveHidden || syncGridSelection.preserveHiddenOnSelectionChange;
        }
        if (preservedRowSelection && this._gridOptions.backendServiceApi && this._gridOptions.dataView.hasOwnProperty("syncGridSelectionWithBackendService")) {
          preservedRowSelection = this._gridOptions.dataView.syncGridSelectionWithBackendService;
        }
      }
      return preservedRowSelection;
    }
    resetColumns(columns) {
      const currentColumns = this.getAssociatedCurrentColumns(columns || this._columns);
      const isIncludingHiddenProps = !!this._gridOptions.gridStateIncludeHiddenProps;
      this.pubSubService.publish("onGridStateChanged", {
        change: { newValues: currentColumns, type: "columns" },
        gridState: this.getCurrentGridState(isIncludingHiddenProps)
      });
    }
    /**
     * Reset the grid to its original (all) columns, that is to display the entire set of columns with their original positions & visibilities
     * @param {Boolean} triggerAutoSizeColumns - True by default, do we also want to call the "autosizeColumns()" method to make the columns fit in the grid?
     */
    resetToOriginalColumns(triggerAutoSizeColumns = true) {
      this._grid.setColumns(this.sharedService.allColumns);
      if (triggerAutoSizeColumns) {
        this._grid.autosizeColumns();
      }
    }
    /** if we use Row Selection or the Checkbox Selector, we need to reset any selection */
    resetRowSelectionWhenRequired() {
      if (!this.needToPreserveRowSelection() && (this._gridOptions.enableSelection || this._gridOptions.enableCheckboxSelector)) {
        if (this.extensionService?.getExtensionByName?.("hybridSelection")?.instance) {
          this._grid.setSelectedRows([]);
        }
      }
    }
    /**
     * Subscribe to all necessary SlickGrid or Service Events that deals with a Grid change,
     * when triggered, we will publish a Grid State Event with current Grid State
     */
    subscribeToAllGridChanges(grid) {
      const isIncludingHiddenProps = !!this._gridOptions.gridStateIncludeHiddenProps;
      this._subscriptions.push(this.pubSubService.subscribe("onFilterChanged", (currentFilters) => {
        this.resetRowSelectionWhenRequired();
        this.pubSubService.publish("onGridStateChanged", {
          change: { newValues: currentFilters, type: "filter" },
          gridState: this.getCurrentGridState(isIncludingHiddenProps)
        });
      }));
      this._subscriptions.push(this.pubSubService.subscribe("onFilterCleared", () => {
        this.resetRowSelectionWhenRequired();
        this.pubSubService.publish("onGridStateChanged", {
          change: { newValues: [], type: "filter" },
          gridState: this.getCurrentGridState(isIncludingHiddenProps)
        });
      }));
      this._subscriptions.push(this.pubSubService.subscribe("onSortChanged", (currentSorters) => {
        this.resetRowSelectionWhenRequired();
        this.pubSubService.publish("onGridStateChanged", {
          change: { newValues: currentSorters, type: "sorter" },
          gridState: this.getCurrentGridState(isIncludingHiddenProps)
        });
      }));
      this._subscriptions.push(this.pubSubService.subscribe("onSortCleared", () => {
        this.resetRowSelectionWhenRequired();
        this.pubSubService.publish("onGridStateChanged", {
          change: { newValues: [], type: "sorter" },
          gridState: this.getCurrentGridState(isIncludingHiddenProps)
        });
      }));
      this.bindExtensionAddonEventToGridStateChange("columnPicker", "onColumnsChanged");
      this.bindExtensionAddonEventToGridStateChange("gridMenu", "onColumnsChanged");
      this.bindSlickGridColumnChangeEventToGridStateChange("onColumnsReordered", grid);
      this.bindSlickGridColumnChangeEventToGridStateChange("onColumnsResized", grid);
      this.bindSlickGridOnSetOptionsEventToGridStateChange(grid);
      if (this._gridOptions.enableSelection || this._gridOptions.enableCheckboxSelector) {
        this._eventHandler.subscribe(this._dataView.onSelectedRowIdsChanged, (_e, args) => {
          const previousSelectedRowIndexes = (this._selectedRowIndexes || []).slice();
          const previousSelectedFilteredRowDataContextIds = (this.selectedRowDataContextIds || []).slice();
          this.selectedRowDataContextIds = args.filteredIds;
          this._selectedRowIndexes = args.rows;
          if (!dequal(this.selectedRowDataContextIds, previousSelectedFilteredRowDataContextIds) || !dequal(this._selectedRowIndexes, previousSelectedRowIndexes)) {
            const newValues = {
              gridRowIndexes: this._selectedRowIndexes || [],
              dataContextIds: args.selectedRowIds,
              filteredDataContextIds: args.filteredIds
            };
            this.pubSubService.publish("onGridStateChanged", {
              change: { newValues, type: "rowSelection" },
              gridState: this.getCurrentGridState(isIncludingHiddenProps)
            });
          }
        });
      }
      this._subscriptions.push(this.pubSubService.subscribe(["onHideColumns", "onShowColumns"], (data) => {
        const currentColumns = this.getAssociatedCurrentColumns(data.columns);
        this.pubSubService.publish("onGridStateChanged", {
          change: { newValues: currentColumns, type: "columns" },
          gridState: this.getCurrentGridState(isIncludingHiddenProps)
        });
      }));
      this._subscriptions.push(this.pubSubService.subscribe("onTreeItemToggled", (toggleChange) => {
        this.pubSubService.publish("onGridStateChanged", {
          change: { newValues: toggleChange, type: "treeData" },
          gridState: this.getCurrentGridState(isIncludingHiddenProps)
        });
      }));
      this._subscriptions.push(this.pubSubService.subscribe("onTreeFullToggleEnd", (toggleChange) => {
        this.pubSubService.publish("onGridStateChanged", {
          change: { newValues: toggleChange, type: "treeData" },
          gridState: this.getCurrentGridState(isIncludingHiddenProps)
        });
      }));
    }
    // --
    // protected methods
    // ------------------
    /**
     * Add certain column(s), when the feature is/are enabled, to an output column definitions array (by reference).
     * Basically some features (for example: Row Selection, Row Detail, Row Move) will be added as column(s) dynamically and internally by the lib,
     * we just ask the developer to enable the feature, via flags, and internally the lib will create the necessary column.
     * So specifically for these column(s) and feature(s), we need to re-add them internally when the user calls the `changeColumnsArrangement()` method.
     * @param {Array<Object>} dynamicAddonColumnByIndexPositionList - array of plugin columnId and columnIndexPosition that will be re-added (if it wasn't already found in the output array) dynamically
     * @param {Array<Column>} fullColumns - full column definitions array that includes every columns (including Row Selection, Row Detail, Row Move when enabled)
     * @param {Array<Column>} newArrangedColumns - output array that will be use to show in the UI (it could have less columns than fullColumnDefinitions array since user might hide some columns)
     */
    addColumnDynamicWhenFeatureEnabled(dynamicAddonColumnByIndexPositionList, fullColumns, newArrangedColumns) {
      dynamicAddonColumnByIndexPositionList.sort((feat1, feat2) => feat1.columnIndexPosition - feat2.columnIndexPosition);
      dynamicAddonColumnByIndexPositionList.forEach((feature) => {
        const pluginColumnIdx = fullColumns.findIndex((col) => col.id === feature.columnId);
        const associatedGridCheckboxColumnIdx = newArrangedColumns.findIndex((col) => col.id === feature.columnId);
        if (pluginColumnIdx >= 0 && associatedGridCheckboxColumnIdx === -1) {
          const pluginColumn = fullColumns[pluginColumnIdx];
          pluginColumnIdx === 0 ? newArrangedColumns.unshift(pluginColumn) : newArrangedColumns.splice(pluginColumnIdx, 0, pluginColumn);
        }
      });
    }
    /**
     * Bind a SlickGrid Extension Event to a Grid State change event
     * @param extension name
     * @param event name
     */
    bindExtensionAddonEventToGridStateChange(extensionName, eventName) {
      const extension = this.extensionService?.getExtensionByName?.(extensionName);
      const slickEvent = extension?.instance?.[eventName];
      const isIncludingHiddenProps = !!this._gridOptions.gridStateIncludeHiddenProps;
      if (slickEvent && typeof slickEvent.subscribe === "function") {
        this._eventHandler.subscribe(slickEvent, (_e, args) => {
          const columns = args?.columns;
          const currentColumns = this.getAssociatedCurrentColumns(columns);
          this.pubSubService.publish("onGridStateChanged", {
            change: { newValues: currentColumns, type: "columns" },
            gridState: this.getCurrentGridState(isIncludingHiddenProps)
          });
        });
      }
    }
    /**
     * Bind a Grid Event (of Column changes) to a Grid State change event
     * @param event - event name
     * @param grid - SlickGrid object
     */
    bindSlickGridColumnChangeEventToGridStateChange(eventName, grid) {
      const slickGridEvent = grid?.[eventName];
      const isIncludingHiddenProps = !!this._gridOptions.gridStateIncludeHiddenProps;
      if (slickGridEvent && typeof slickGridEvent.subscribe === "function") {
        this._eventHandler.subscribe(slickGridEvent, () => {
          const columns = grid.getColumns();
          const currentColumns = this.getAssociatedCurrentColumns(columns);
          this.pubSubService.publish("onGridStateChanged", {
            change: { newValues: currentColumns, type: "columns" },
            gridState: this.getCurrentGridState(isIncludingHiddenProps)
          });
        });
      }
    }
    /**
     * Bind a Grid Event (of grid option changes) to a Grid State change event, if we detect that any of the pinning (frozen) options changes then we'll trigger a Grid State change
     * @param grid - SlickGrid object
     */
    bindSlickGridOnSetOptionsEventToGridStateChange(grid) {
      const onSetOptionsHandler = grid.onSetOptions;
      this._eventHandler.subscribe(onSetOptionsHandler, (_e, args) => {
        const { frozenBottom: frozenBottomBefore, frozenColumn: frozenColumnBefore, frozenRow: frozenRowBefore } = args.optionsBefore;
        const { frozenBottom: frozenBottomAfter, frozenColumn: frozenColumnAfter, frozenRow: frozenRowAfter } = args.optionsAfter;
        if (frozenBottomBefore !== frozenBottomAfter || frozenColumnBefore !== frozenColumnAfter || frozenRowBefore !== frozenRowAfter) {
          const isIncludingHiddenProps = !!this._gridOptions.gridStateIncludeHiddenProps;
          const newValues = { frozenBottom: frozenBottomAfter, frozenColumn: frozenColumnAfter, frozenRow: frozenRowAfter };
          const currentGridState = this.getCurrentGridState(isIncludingHiddenProps);
          this.pubSubService.publish("onGridStateChanged", {
            change: { newValues, type: "pinning" },
            gridState: currentGridState
          });
        }
      });
    }
    /** Check wether the grid has the Row Selection enabled */
    hasRowSelectionEnabled() {
      const selectionModel = this._grid.getSelectionModel();
      const isRowSelectionEnabled = !!(this._gridOptions.enableSelection || this._gridOptions.enableCheckboxSelector);
      return isRowSelectionEnabled && !!selectionModel;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/headerGrouping.service.js
  var HeaderGroupingService = class {
    constructor(extensionUtility) {
      this.extensionUtility = extensionUtility;
      this.pluginName = "HeaderGroupingService";
      this._subscriptions = [];
      this._eventHandler = new SlickEventHandler();
    }
    /** Getter of SlickGrid DataView object */
    get _dataView() {
      return this._grid?.getData() ?? {};
    }
    /** Getter of the SlickGrid Event Handler */
    get eventHandler() {
      return this._eventHandler;
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get _gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    /**
     * Initialize the Service
     * @param {SlickGrid} grid
     */
    init(grid) {
      this._grid = grid;
      if (grid && this._gridOptions) {
        if (this._gridOptions.createPreHeaderPanel) {
          if (this._gridOptions.enableTranslate) {
            this.translateHeaderGrouping();
          }
          this._eventHandler.subscribe(grid.onAfterUpdateColumns, () => this.renderPreHeaderRowGroupingTitles()).subscribe(grid.onColumnsReordered, () => this.renderPreHeaderRowGroupingTitles()).subscribe(grid.onRendered, () => this.renderPreHeaderRowGroupingTitles()).subscribe(grid.onAutosizeColumns, () => this.renderPreHeaderRowGroupingTitles()).subscribe(this._dataView.onRowCountChanged, () => this.delayRenderPreHeaderRowGroupingTitles(0)).subscribe(grid.onSetOptions, (_e, args) => {
            if (args?.optionsBefore?.frozenColumn !== args?.optionsAfter?.frozenColumn) {
              this.delayRenderPreHeaderRowGroupingTitles(0);
            }
          });
          this.delayRenderPreHeaderRowGroupingTitles(75);
        }
      }
    }
    dispose() {
      clearTimeout(this._timer);
      this._eventHandler.unsubscribeAll();
    }
    /** call "renderPreHeaderRowGroupingTitles()" with a setTimeout delay */
    delayRenderPreHeaderRowGroupingTitles(delay = 0) {
      clearTimeout(this._timer);
      this._timer = setTimeout(() => this.renderPreHeaderRowGroupingTitles(), delay);
    }
    /** Create or Render the Pre-Header Row Grouping Titles */
    renderPreHeaderRowGroupingTitles() {
      const colsCount = this._grid.getVisibleColumns().length;
      if (this._gridOptions?.frozenColumn !== void 0 && this._gridOptions.frozenColumn >= 0) {
        const frozenCol = this._gridOptions.frozenColumn;
        this.renderHeaderGroups(this._grid.getPreHeaderPanelLeft(), 0, frozenCol + 1);
        this.renderHeaderGroups(this._grid.getPreHeaderPanelRight(), frozenCol + 1, colsCount);
      } else {
        this.renderHeaderGroups(this._grid.getPreHeaderPanel(), 0, colsCount);
      }
    }
    renderHeaderGroups(preHeaderPanel, start, end) {
      emptyElement(preHeaderPanel);
      preHeaderPanel.className = "slick-header-columns";
      preHeaderPanel.style.left = "-1000px";
      preHeaderPanel.style.width = `${this._grid.getHeadersWidth()}px`;
      preHeaderPanel.parentElement?.classList.add("slick-header");
      const headerColumnWidthDiff = this._grid.getHeaderColumnWidthDiff();
      let colDef;
      let headerElm = null;
      let lastColumnGroup = "";
      let widthTotal = 0;
      const frozenHeaderWidthCalcDifferential = this._gridOptions?.frozenHeaderWidthCalcDifferential ?? 0;
      const isFrozenGrid = this._gridOptions?.frozenColumn !== void 0 && this._gridOptions.frozenColumn >= 0;
      const visibleColumns = this._grid.getVisibleColumns();
      for (let i = start; i < end; i++) {
        colDef = visibleColumns[i];
        if (colDef) {
          if (lastColumnGroup === colDef.columnGroup && i > 0) {
            widthTotal += colDef.width || 0;
            if (headerElm?.style) {
              headerElm.style.width = `${widthTotal - headerColumnWidthDiff - frozenHeaderWidthCalcDifferential}px`;
            }
          } else {
            widthTotal = colDef.width || 0;
            headerElm = createDomElement("div", {
              className: `slick-state-default slick-header-column ${isFrozenGrid ? "frozen" : ""}`,
              dataset: { group: colDef.columnGroup },
              style: { width: `${widthTotal - headerColumnWidthDiff}px` }
            });
            createDomElement("span", { className: "slick-column-name", textContent: colDef.columnGroup || "" }, headerElm);
            preHeaderPanel.appendChild(headerElm);
          }
          lastColumnGroup = colDef.columnGroup || "";
        }
      }
    }
    /** Translate Column Group texts and re-render them afterward. */
    translateHeaderGrouping() {
      const currentColumns = this._grid.getColumns();
      this.extensionUtility.translateItems(currentColumns, "columnGroupKey", "columnGroup");
      this._grid.updateColumns();
      this.renderPreHeaderRowGroupingTitles();
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/pagination.service.js
  var PaginationService = class {
    /** Constructor */
    constructor(pubSubService, sharedService, backendUtilities, rxjs) {
      this.pubSubService = pubSubService;
      this.sharedService = sharedService;
      this.backendUtilities = backendUtilities;
      this.rxjs = rxjs;
      this._initialized = false;
      this._isLocalGrid = true;
      this._dataFrom = 1;
      this._dataTo = 1;
      this._itemsPerPage = 0;
      this._pageCount = 1;
      this._pageNumber = 1;
      this._totalItems = 0;
      this._availablePageSizes = [];
      this._subscriptions = [];
      this._isCursorBased = false;
      this._eventHandler = new SlickEventHandler();
    }
    /** Getter of SlickGrid DataView object */
    get dataView() {
      return this.grid?.getData() ?? {};
    }
    set paginationOptions(paginationOptions) {
      this._paginationOptions = paginationOptions;
    }
    get paginationOptions() {
      return this._paginationOptions;
    }
    get availablePageSizes() {
      return this._availablePageSizes;
    }
    get dataFrom() {
      return this._dataFrom;
    }
    get dataTo() {
      return this._dataTo;
    }
    get itemsPerPage() {
      return this._itemsPerPage;
    }
    get pageCount() {
      return this._pageCount;
    }
    get pageNumber() {
      return this._pageNumber;
    }
    get totalItems() {
      return this._totalItems;
    }
    set totalItems(totalItems) {
      this._totalItems = totalItems;
      if (this._initialized) {
        this.refreshPagination();
      }
    }
    /**
     * https://dev.to/jackmarchant/offset-and-cursor-pagination-explained-b89
     * Cursor based pagination does not allow navigation to a page in the middle of a set of pages (eg: LinkedList vs Vector).
     *  Further, Pagination with page numbers only makes sense in non-relay style pagination
     *  Relay style pagination is better suited to infinite scrolling
     *
     * eg
     *  relay pagination - Infinte scrolling appending data
     *    page1: {startCursor: A, endCursor: B }
     *    page2: {startCursor: A, endCursor: C }
     *    page3: {startCursor: A, endCursor: D }
     *
     *  non-relay pagination - Getting page chunks
     *    page1: {startCursor: A, endCursor: B }
     *    page2: {startCursor: B, endCursor: C }
     *    page3: {startCursor: C, endCursor: D }
     */
    get isCursorBased() {
      return this._isCursorBased;
    }
    addRxJsResource(rxjs) {
      this.rxjs = rxjs;
    }
    init(grid, paginationOptions, backendServiceApi) {
      this._availablePageSizes = paginationOptions.pageSizes || [];
      this.grid = grid;
      this._backendServiceApi = backendServiceApi;
      this._paginationOptions = paginationOptions;
      this._isLocalGrid = !backendServiceApi;
      this._pageNumber = paginationOptions.pageNumber || 1;
      this._isCursorBased = backendServiceApi?.options?.useCursor ?? false;
      if (backendServiceApi && (!backendServiceApi.service || !backendServiceApi.process)) {
        throw new Error(`BackendServiceApi requires the following 2 properties "process" and "service" to be defined.`);
      }
      if (this._isLocalGrid && this.dataView) {
        this._eventHandler.subscribe(this.dataView.onPagingInfoChanged, (_e, pagingInfo) => {
          if (this._totalItems !== pagingInfo.totalRows) {
            this.updateTotalItems(pagingInfo.totalRows);
            this._previousPagination = {
              pageNumber: pagingInfo.pageNum,
              pageSize: pagingInfo.pageSize,
              pageSizes: this.availablePageSizes,
              totalItems: pagingInfo.totalRows
            };
          }
        });
        queueMicrotaskPolyfill(() => {
          if (this.dataView) {
            this.dataView.setRefreshHints({ isFilterUnchanged: true });
            this.dataView.setPagingOptions({ pageSize: this.paginationOptions.pageSize, pageNum: this._pageNumber - 1 });
          }
        });
      }
      this._subscriptions.push(this.pubSubService.subscribe(["onFilterChanged", "onFilterCleared"], () => this.resetPagination()));
      if (backendServiceApi?.options?.infiniteScroll) {
        this._subscriptions.push(this.pubSubService.subscribe("onSortChanged", () => this.resetPagination()));
      }
      if (this.dataView) {
        this._subscriptions.push(this.pubSubService.subscribe("onItemsAdded", (items) => this.processOnItemsAddedOrRemoved(items, true)));
        this._subscriptions.push(this.pubSubService.subscribe("onItemsDeleted", (items) => this.processOnItemsAddedOrRemoved(items, false)));
      }
      this.refreshPagination(false, false, true);
      const pagination = this.getFullPagination();
      this._previousPagination = {
        pageNumber: pagination.pageNumber,
        pageSize: pagination.pageSize,
        pageSizes: pagination.pageSizes,
        totalItems: this.totalItems
      };
      this._initialized = true;
      propertyObserver(paginationOptions, "totalItems", (newTotal) => this._totalItems = newTotal);
    }
    dispose() {
      this._initialized = false;
      this._eventHandler.unsubscribeAll();
      this.pubSubService.unsubscribeAll(this._subscriptions);
    }
    getCurrentPagination() {
      return {
        pageNumber: this._pageNumber,
        pageSize: this._itemsPerPage,
        pageSizes: this._availablePageSizes
      };
    }
    getFullPagination() {
      return {
        pageCount: this._pageCount,
        pageNumber: this._pageNumber,
        pageSize: this._itemsPerPage,
        pageSizes: this._availablePageSizes,
        totalItems: this._totalItems,
        dataFrom: this._dataFrom,
        dataTo: this._dataTo
      };
    }
    getCurrentPageNumber() {
      return this._pageNumber;
    }
    getCurrentItemPerPage() {
      return this._itemsPerPage;
    }
    changeItemPerPage(itemsPerPage, event, triggerChangeEvent = true) {
      this._pageNumber = 1;
      this._pageCount = Math.ceil(this._totalItems / itemsPerPage);
      this._itemsPerPage = itemsPerPage;
      return triggerChangeEvent ? this.processOnPageChanged(this._pageNumber, event) : Promise.resolve(this.getFullPagination());
    }
    goToFirstPage(event, triggerChangeEvent = true) {
      this._pageNumber = 1;
      if (triggerChangeEvent) {
        return this.isCursorBased && this._cursorPageInfo ? this.processOnPageChanged(this._pageNumber, event, {
          newPage: this._pageNumber,
          pageSize: this._itemsPerPage,
          first: this._itemsPerPage
        }) : this.processOnPageChanged(this._pageNumber, event);
      }
      return Promise.resolve(this.getFullPagination());
    }
    goToLastPage(event, triggerChangeEvent = true) {
      this._pageNumber = this._pageCount || 1;
      if (triggerChangeEvent) {
        return this.isCursorBased && this._cursorPageInfo ? this.processOnPageChanged(this._pageNumber, event, {
          newPage: this._pageNumber,
          pageSize: this._itemsPerPage,
          last: this._itemsPerPage
        }) : this.processOnPageChanged(this._pageNumber, event);
      }
      return Promise.resolve(this.getFullPagination());
    }
    goToNextPage(event, triggerChangeEvent = true) {
      if (this._pageNumber < this._pageCount) {
        this._pageNumber++;
        if (triggerChangeEvent) {
          return this.isCursorBased && this._cursorPageInfo ? this.processOnPageChanged(this._pageNumber, event, {
            newPage: this._pageNumber,
            pageSize: this._itemsPerPage,
            first: this._itemsPerPage,
            after: this._cursorPageInfo.endCursor
          }) : this.processOnPageChanged(this._pageNumber, event);
        } else {
          return Promise.resolve(this.getFullPagination());
        }
      }
      return Promise.resolve(false);
    }
    goToPageNumber(pageNumber, event, triggerChangeEvent = true) {
      if (this.isCursorBased) {
        console.assert(true, "Cursor based navigation cannot navigate to arbitrary page");
        return Promise.resolve(false);
      }
      const previousPageNumber = this._pageNumber;
      if (pageNumber < 1) {
        this._pageNumber = 1;
      } else if (pageNumber > this._pageCount) {
        this._pageNumber = this._pageCount;
      } else {
        this._pageNumber = pageNumber;
      }
      if (this._pageNumber !== previousPageNumber) {
        return triggerChangeEvent ? this.processOnPageChanged(this._pageNumber, event) : Promise.resolve(this.getFullPagination());
      }
      return Promise.resolve(false);
    }
    goToPreviousPage(event, triggerChangeEvent = true) {
      if (this._pageNumber > 1) {
        this._pageNumber--;
        if (triggerChangeEvent) {
          return this.isCursorBased && this._cursorPageInfo ? this.processOnPageChanged(this._pageNumber, event, {
            newPage: this._pageNumber,
            pageSize: this._itemsPerPage,
            last: this._itemsPerPage,
            before: this._cursorPageInfo.startCursor
          }) : this.processOnPageChanged(this._pageNumber, event);
        } else {
          return Promise.resolve(this.getFullPagination());
        }
      }
      return Promise.resolve(false);
    }
    refreshPagination(isPageNumberReset = false, triggerChangedEvent = true, triggerInitializedEvent = false) {
      const previousPagination = { ...this.getFullPagination() };
      if (this._paginationOptions) {
        const pagination2 = this._paginationOptions;
        if (!this._itemsPerPage) {
          if (this._isLocalGrid) {
            this._itemsPerPage = pagination2.pageSize;
          } else {
            this._itemsPerPage = +(this._backendServiceApi?.options?.paginationOptions?.first ? this._backendServiceApi.options.paginationOptions.first : pagination2.pageSize);
          }
        }
        if (isPageNumberReset || this._totalItems !== pagination2.totalItems) {
          if (isPageNumberReset) {
            this._pageNumber = 1;
            this.paginationOptions.pageNumber = 1;
          } else if (!this._initialized && pagination2.pageNumber && pagination2.pageNumber > 1) {
            this._pageNumber = pagination2.pageNumber || 1;
          }
          if (this._pageNumber === 1 && this._backendServiceApi) {
            this._backendServiceApi.service.resetPaginationOptions();
          }
        }
        this._availablePageSizes = pagination2.pageSizes || [];
        if (!this._totalItems && pagination2.totalItems) {
          this._totalItems = pagination2.totalItems;
        }
        this.recalculateFromToIndexes();
      }
      this._pageCount = Math.ceil(this._totalItems / this._itemsPerPage);
      this.sharedService.currentPagination = this.getCurrentPagination();
      this.pubSubService.publish(`onPaginationRefreshed`, this.getFullPagination());
      if (triggerChangedEvent && !dequal(previousPagination, this.getFullPagination())) {
        this.pubSubService.publish(`onPaginationChanged`, this.getFullPagination());
      }
      if (triggerInitializedEvent && !dequal(previousPagination, this.getFullPagination())) {
        this.pubSubService.publish(`onPaginationPresetsInitialized`, this.getFullPagination());
      }
      const pagination = this.getFullPagination();
      this._previousPagination = {
        pageNumber: pagination.pageNumber,
        pageSize: pagination.pageSize,
        pageSizes: pagination.pageSizes,
        totalItems: this.totalItems
      };
    }
    /** Reset the Pagination to first page and recalculate necessary numbers */
    resetPagination(triggerChangedEvent = true, scrollToTop = true) {
      if (this._isLocalGrid && this.dataView && this.sharedService?.gridOptions?.enablePagination) {
        this.dataView.setPagingOptions({ pageSize: this._itemsPerPage, pageNum: 0 });
      }
      this._cursorPageInfo = void 0;
      this.refreshPagination(true, triggerChangedEvent);
      if (scrollToTop) {
        this.grid.scrollTo(0);
      }
    }
    /**
     * Toggle the Pagination (show/hide), it will use the visible if defined else it will automatically inverse when called without argument
     *
     * IMPORTANT NOTE:
     * The Pagination must be created on initial page load, then only after can you toggle it.
     * Basically this method WILL NOT WORK to show the Pagination if it was never created from the start.
     */
    togglePaginationVisibility(visible) {
      if (this.grid && this.sharedService?.gridOptions) {
        const isVisible = visible !== void 0 ? visible : !this.sharedService.gridOptions.enablePagination;
        if (isVisible) {
          this.goToFirstPage();
        }
        if (this._isLocalGrid && this.dataView) {
          const pageSize = visible ? this._itemsPerPage : 0;
          this.dataView.setPagingOptions({ pageSize, pageNum: 0 });
        }
        this.sharedService.gridOptions.enablePagination = isVisible;
        this.pubSubService.publish(`onPaginationVisibilityChanged`, { visible: isVisible });
      }
    }
    processOnPageChanged(pageNumber, event, cursorArgs) {
      console.assert(!this.isCursorBased || cursorArgs, "Configured for cursor based pagination - cursorArgs expected");
      if (this.pubSubService.publish("onBeforePaginationChange", this.getFullPagination()) === false) {
        this.resetToPreviousPagination();
        return Promise.resolve(this.getFullPagination());
      }
      return new Promise((resolve, reject) => {
        this.recalculateFromToIndexes();
        if (this._isLocalGrid && this.dataView) {
          this.dataView.setPagingOptions({ pageSize: this._itemsPerPage, pageNum: pageNumber - 1 });
          this.pubSubService.publish(`onPaginationChanged`, this.getFullPagination());
          this.pubSubService.publish(`onPaginationRefreshed`, this.getFullPagination());
          resolve(this.getFullPagination());
        } else {
          const itemsPerPage = +this._itemsPerPage;
          const startTime = /* @__PURE__ */ new Date();
          if (this._backendServiceApi?.preProcess) {
            this._backendServiceApi.preProcess();
          }
          if (this._backendServiceApi?.process) {
            const query = this.isCursorBased && cursorArgs ? this._backendServiceApi.service.processOnPaginationChanged(event, cursorArgs) : this._backendServiceApi.service.processOnPaginationChanged(event, {
              newPage: pageNumber,
              pageSize: itemsPerPage
            });
            const process = this._backendServiceApi.process(query);
            if (process instanceof Promise) {
              process.then((processResult) => {
                this.backendUtilities?.executeBackendProcessesCallback(startTime, processResult, this._backendServiceApi, this._totalItems);
                const pagination = this.getFullPagination();
                this._previousPagination = {
                  pageNumber: pagination.pageNumber,
                  pageSize: pagination.pageSize,
                  pageSizes: pagination.pageSizes,
                  totalItems: this.totalItems
                };
                resolve(this.getFullPagination());
              }).catch((error) => {
                this.resetToPreviousPagination();
                this.backendUtilities?.onBackendError(error, this._backendServiceApi);
                if (!this._backendServiceApi?.onError || !this.backendUtilities?.onBackendError) {
                  reject(process);
                }
              });
            } else if (this.rxjs?.isObservable(process)) {
              this._subscriptions.push(process.subscribe((processResult) => {
                const pagination = this.getFullPagination();
                this._previousPagination = {
                  pageNumber: pagination.pageNumber,
                  pageSize: pagination.pageSize,
                  pageSizes: pagination.pageSizes,
                  totalItems: this.totalItems
                };
                resolve(this.backendUtilities?.executeBackendProcessesCallback(startTime, processResult, this._backendServiceApi, this._totalItems));
              }, (error) => {
                this.resetToPreviousPagination();
                this.backendUtilities?.onBackendError(error, this._backendServiceApi);
                if (!this._backendServiceApi?.onError || !this.backendUtilities?.onBackendError) {
                  reject(process);
                }
              }));
            }
            this.pubSubService.publish(`onPaginationRefreshed`, this.getFullPagination());
            this.pubSubService.publish(`onPaginationChanged`, this.getFullPagination());
          }
        }
      });
    }
    recalculateFromToIndexes() {
      if ((this._pageNumber - 1) * this._itemsPerPage > this._totalItems || this._totalItems > 0 && this._pageNumber === 0) {
        this._pageNumber = 1;
      }
      if (this._totalItems === 0) {
        this._dataFrom = 0;
        this._dataTo = 1;
        this._pageNumber = 0;
      } else {
        this._dataFrom = this._pageNumber > 1 ? this._pageNumber * this._itemsPerPage - this._itemsPerPage + 1 : 1;
        this._dataTo = this._totalItems < this._itemsPerPage ? this._totalItems : (this._pageNumber || 1) * this._itemsPerPage;
        if (this._dataTo > this._totalItems) {
          this._dataTo = this._totalItems;
        }
      }
      if (this._dataTo > this._totalItems) {
        this._dataTo = this._totalItems;
      } else if (this._totalItems < this._itemsPerPage) {
        this._dataTo = this._totalItems;
      }
    }
    /**
     * Reset (revert) to previous pagination, it could be because you prevented `onBeforePaginationChange`, `onBeforePagingInfoChanged` from DataView OR a Backend Error was thrown.
     * It will reapply the previous filter state in the UI.
     */
    resetToPreviousPagination() {
      const hasPageNumberChange = this._previousPagination?.pageNumber !== this.getFullPagination().pageNumber;
      const hasPageSizeChange = this._previousPagination?.pageSize !== this.getFullPagination().pageSize;
      if (hasPageSizeChange) {
        this.changeItemPerPage(this._previousPagination?.pageSize ?? 0, null, false);
      }
      if (hasPageNumberChange) {
        this.goToPageNumber(this._previousPagination?.pageNumber ?? 0, null, false);
      }
      if (hasPageNumberChange || hasPageSizeChange) {
        this.refreshPagination();
        this._backendServiceApi?.service?.updatePagination?.(this._previousPagination?.pageNumber ?? 0, this._previousPagination?.pageSize ?? 0);
      }
    }
    setCursorBased(isCursorBased) {
      this._isCursorBased = isCursorBased;
      if (isCursorBased) {
        this.setCursorPageInfo({ startCursor: "", endCursor: "", hasNextPage: false, hasPreviousPage: false });
      }
      this.goToFirstPage();
      this.pubSubService.publish(`onPaginationSetCursorBased`, { isCursorBased });
    }
    setCursorPageInfo(pageInfo) {
      this._cursorPageInfo = pageInfo;
    }
    updateTotalItems(totalItems, triggerChangedEvent = false) {
      this._totalItems = totalItems;
      if (this._paginationOptions) {
        this._paginationOptions.totalItems = totalItems;
        this.refreshPagination(false, triggerChangedEvent);
      }
    }
    // --
    // protected functions
    // --------------------
    /**
     * When item is added or removed, we will refresh the numbers on the pagination however we won't trigger a backend change
     * This will have a side effect though, which is that the "To" count won't be matching the "items per page" count,
     * that is a necessary side effect to avoid triggering a backend query just to refresh the paging,
     * basically we assume that this offset is fine for the time being,
     * until user does an action which will refresh the data hence the pagination which will then become normal again
     */
    processOnItemsAddedOrRemoved(items, isItemAdded = true) {
      if (Array.isArray(items) && items.length > 0) {
        const previousDataTo = this._dataTo;
        const itemCount = items.length;
        const itemCountWithDirection = isItemAdded ? +itemCount : -itemCount;
        this._totalItems += itemCountWithDirection;
        this.recalculateFromToIndexes();
        this._dataTo = previousDataTo + itemCountWithDirection;
        this.pubSubService.publish(`onPaginationChanged`, this.getFullPagination());
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/resizer.service.js
  var DATAGRID_BOTTOM_PADDING = 20;
  var DATAGRID_FOOTER_HEIGHT = 25;
  var DATAGRID_PAGINATION_HEIGHT = 35;
  var DATAGRID_MIN_HEIGHT = 180;
  var DATAGRID_MIN_WIDTH = 300;
  var DEFAULT_INTERVAL_RETRY_DELAY = 200;
  var ResizerService = class {
    get autoHeightRecalcRow() {
      return this._autoHeightRecalcRow || 100;
    }
    get isAutoHeightEnabled() {
      return !!(this.gridOptions.enableAutoResize && this.gridOptions.autoResize?.autoHeight);
    }
    get eventHandler() {
      return this._eventHandler;
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    /** Getter for the SlickGrid DataView */
    get dataView() {
      return this._grid?.getData();
    }
    /** Getter for the grid uid */
    get gridUid() {
      return this._grid?.getUID() ?? "";
    }
    get gridUidSelector() {
      return this.gridUid ? `.${this.gridUid}` : "";
    }
    get intervalRetryDelay() {
      return this._intervalRetryDelay;
    }
    set intervalRetryDelay(delay) {
      this._intervalRetryDelay = delay;
    }
    get resizeByContentOptions() {
      return this.gridOptions?.resizeByContentOptions ?? {};
    }
    constructor(pubSubService) {
      this.pubSubService = pubSubService;
      this._allHeaderHeight = 0;
      this._autoHeightRecalcRow = 0;
      this._intervalRetryDelay = DEFAULT_INTERVAL_RETRY_DELAY;
      this._isStopResizeIntervalRequested = false;
      this._hasResizedByContentAtLeastOnce = false;
      this._cResizeCheckRequired = false;
      this._totalColumnsWidthByContent = 0;
      this._resizePaused = false;
      this._subscriptions = [];
      this._singleCharWidth = 0;
      this._eventHandler = new SlickEventHandler();
      this._bindingEventService = new BindingEventService();
    }
    /** Dispose function when service is destroyed */
    dispose() {
      clearInterval(this._intervalId);
      clearTimeout(this._timer);
      if (this.gridOptions.autoResize?.resizeDetection === "container" && this._resizeObserver) {
        this._resizeObserver.disconnect();
      }
      this._bindingEventService.unbindAll();
      this._eventHandler?.unsubscribeAll();
      this.pubSubService.unsubscribeAll(this._subscriptions);
      this._subscriptions.length = 0;
    }
    init(grid, gridParentContainerElm) {
      if (!grid || !this.gridOptions || !gridParentContainerElm) {
        throw new Error(`
      [Slickgrid-Universal] Resizer Service requires a valid Grid object and DOM Element Container to be provided.
      You can fix this by setting your gridOption to use "enableAutoResize" or create an instance of the ResizerService by calling bindAutoResizeDataGrid() once.`);
      }
      this._grid = grid;
      this._gridContainerElm = gridParentContainerElm;
      const fixedGridSizes = this.gridOptions?.gridHeight || this.gridOptions?.gridWidth ? { height: this.gridOptions?.gridHeight, width: this.gridOptions?.gridWidth } : void 0;
      this._autoResizeOptions = this.gridOptions?.autoResize ?? { container: "grid1", bottomPadding: 0 };
      if (fixedGridSizes?.width && gridParentContainerElm?.style) {
        gridParentContainerElm.style.width = typeof fixedGridSizes.width === "string" ? fixedGridSizes.width : `${fixedGridSizes.width}px`;
      }
      this._gridDomElm = grid.getContainerNode();
      if (typeof this._autoResizeOptions.container === "string") {
        this._pageContainerElm = typeof this._autoResizeOptions.container === "string" ? document.querySelector(this._autoResizeOptions.container) : this._autoResizeOptions.container;
      } else {
        this._pageContainerElm = this._autoResizeOptions.container;
      }
      if (fixedGridSizes) {
        this._fixedHeight = fixedGridSizes.height;
        this._fixedWidth = fixedGridSizes.width;
      }
      if (this.gridOptions.enableAutoResize) {
        this._autoHeightRecalcRow = this.gridOptions.autoResize?.autoHeightRecalcRow ?? 100;
        this.bindAutoResizeDataGrid();
      }
      if (this.gridOptions.autoResize) {
        this._subscriptions.push(this.pubSubService.subscribe("onFullResizeByContentRequested", () => this.resizeColumnsByCellContent(true)));
      }
      this._eventHandler.subscribe(this._grid.onAutosizeColumns, () => {
        this.cacheHeaderHeightTotal();
      });
      if (this.gridOptions.enableColumnResizeOnDoubleClick) {
        this._subscriptions.push(this.pubSubService.subscribe("onHeaderMenuColumnResizeByContent", (data) => {
          this.handleSingleColumnResizeByContent(data.columnId);
        }));
        this._eventHandler.subscribe(this._grid.onColumnsResizeDblClick, (_e, args) => {
          this.handleSingleColumnResizeByContent(args.triggeredByColumn);
        });
      }
    }
    /** Bind an auto resize trigger on the datagrid, if that is enable then it will resize itself to the available space
     * Options: we could also provide a % factor to resize on each height/width independently
     */
    bindAutoResizeDataGrid(newSizes) {
      this.saveLastWindowSize();
      if (this.gridOptions.autoResize?.resizeDetection === "container") {
        if (!this._pageContainerElm) {
          throw new Error(`
          [Slickgrid-Universal] Resizer Service requires a container when gridOption.autoResize.resizeDetection="container"
          You can fix this by setting your gridOption.autoResize.container`);
        }
        if (!this._resizeObserver) {
          this._resizeObserver = new ResizeObserver(() => this.resizeObserverCallback());
        }
        this._resizeObserver.observe(this._pageContainerElm);
      } else {
        if (this._gridDomElm === void 0) {
          return null;
        }
        this.resizeGrid().then(() => this.resizeGridWhenStylingIsBrokenUntilCorrected()).catch((rejection) => console.log("Error:", rejection));
        this.resizeGrid(10, newSizes);
        this._bindingEventService.bind(window, "resize", () => {
          this.handleResizeGrid(newSizes);
        });
      }
    }
    handleResizeGrid(newSizes) {
      this.pubSubService.publish("onGridBeforeResize");
      this.saveLastWindowSize();
      if (!this._resizePaused) {
        this.resizeGrid(0, newSizes);
        this.resizeGrid(0, newSizes);
      }
    }
    resizeObserverCallback() {
      if (!this._resizePaused) {
        this.resizeGrid();
      }
    }
    /**
     * cache all header height total which will be used to calculate grid autoHeight with autoResize,
     * if you toggle any of the headers (column, header row, header menu), you should call this method to recalculate the header height total
     */
    cacheHeaderHeightTotal() {
      const topHeaderElm = this._gridContainerElm.querySelector(`${this.gridUidSelector} .slick-topheader-panel`);
      const paneHeaderElm = this._gridContainerElm.querySelector(`${this.gridUidSelector} .slick-pane-header`);
      const headerRowElm = this._gridContainerElm.querySelector(`${this.gridUidSelector} .slick-headerrow`);
      this._allHeaderHeight = (topHeaderElm?.offsetHeight || 0) + (paneHeaderElm?.offsetHeight || 0) + (headerRowElm?.offsetHeight || 0);
    }
    /**
     * Calculate the datagrid new height/width from the available space, also consider that a % factor might be applied to calculation
     * @param {GridOption} gridOptions
     */
    calculateGridNewDimensions(gridOptions) {
      const autoResizeOptions = gridOptions?.autoResize ?? {};
      const gridElmOffset = getOffset(this._gridDomElm);
      if (!window || !this._gridDomElm) {
        return null;
      }
      let bottomPadding = autoResizeOptions?.bottomPadding ?? DATAGRID_BOTTOM_PADDING;
      if (bottomPadding && gridOptions.enablePagination) {
        bottomPadding += DATAGRID_PAGINATION_HEIGHT;
      }
      if (bottomPadding && gridOptions.showCustomFooter) {
        const footerHeight = this.gridOptions?.customFooterOptions?.footerHeight ?? DATAGRID_FOOTER_HEIGHT;
        bottomPadding += parseInt(`${footerHeight}`, 10);
      }
      let gridHeight = 0;
      let gridOffsetTop = 0;
      if (autoResizeOptions.calculateAvailableSizeBy === "container") {
        gridHeight = getInnerSize(this._pageContainerElm, "height") || 0;
      } else {
        gridHeight = window.innerHeight || 0;
        gridOffsetTop = gridElmOffset.top;
      }
      const availableHeight = gridHeight - gridOffsetTop - bottomPadding;
      const availableWidth = getInnerSize(this._pageContainerElm, "width") || autoResizeOptions.calculateAvailableSizeBy !== "container" && window.innerWidth || 0;
      const maxHeight = autoResizeOptions?.maxHeight;
      const minHeight = autoResizeOptions?.minHeight ?? DATAGRID_MIN_HEIGHT;
      const maxWidth = autoResizeOptions?.maxWidth;
      const minWidth = autoResizeOptions?.minWidth ?? DATAGRID_MIN_WIDTH;
      let newHeight = availableHeight;
      let newWidth = autoResizeOptions?.rightPadding ? availableWidth - autoResizeOptions.rightPadding : availableWidth;
      if (gridOptions.enableAutoResize && this.isAutoHeightEnabled) {
        const dataLn = this.dataView.getLength();
        if (dataLn < this.autoHeightRecalcRow) {
          this._allHeaderHeight || this.cacheHeaderHeightTotal();
          const dataHeight = dataLn * gridOptions.rowHeight;
          const calcAutoHeight = this._allHeaderHeight + dataHeight;
          if (calcAutoHeight < newHeight) {
            newHeight = calcAutoHeight;
          }
        }
      }
      if (newHeight < minHeight) {
        newHeight = minHeight;
      }
      if (maxHeight && newHeight > maxHeight) {
        newHeight = maxHeight;
      }
      if (newWidth < minWidth) {
        newWidth = minWidth;
      }
      if (maxWidth && newWidth > maxWidth) {
        newWidth = maxWidth;
      }
      return {
        height: this._fixedHeight || newHeight,
        width: this._fixedWidth || newWidth
      };
    }
    /**
     * Return the last resize dimensions used by the service
     * @return {object} last dimensions (height, width)
     */
    getLastResizeDimensions() {
      return this._lastGridSizes;
    }
    /**
     * Provide the possibility to pause the resizer for some time, until user decides to re-enabled it later if he wish to.
     * @param {boolean} isResizePaused are we pausing the resizer?
     */
    pauseResizer(isResizePaused) {
      this._resizePaused = isResizePaused;
    }
    /**
     * Resize the datagrid to fit the browser height & width.
     * @param {number} delay to wait before resizing, defaults to 0 (in milliseconds)
     * @param {object} newSizes can optionally be passed (height, width)
     * @param {object} event that triggered the resize, defaults to null
     * @return If the browser supports it, we can return a Promise that would resolve with the new dimensions
     */
    resizeGrid(delay, newSizes) {
      return new Promise((resolve) => {
        delay = delay || 0;
        if (delay > 0) {
          clearTimeout(this._timer);
          this._timer = setTimeout(() => resolve(this.resizeGridCallback(newSizes)), delay);
        } else {
          resolve(this.resizeGridCallback(newSizes));
        }
      });
    }
    resizeGridCallback(newSizes) {
      const dimensions = this.resizeGridWithDimensions(newSizes);
      this.pubSubService.publish("onGridAfterResize", dimensions);
      if (this.gridOptions.enableAutoResizeColumnsByCellContent && (!this._lastGridSizes?.width || dimensions?.width !== this._lastGridSizes.width)) {
        this.resizeColumnsByCellContent(false);
      }
      this._lastGridSizes = dimensions;
      return dimensions;
    }
    resizeGridWithDimensions(newSizes) {
      const availableDimensions = this.calculateGridNewDimensions(this.gridOptions);
      if ((newSizes || availableDimensions) && this._gridDomElm) {
        const newHeight = newSizes?.height ? newSizes.height : availableDimensions?.height;
        const newWidth = newSizes?.width ? newSizes.width : availableDimensions?.width;
        if (!this.gridOptions.autoHeight) {
          this._gridDomElm.style.height = `${newHeight}px`;
        }
        this._gridDomElm.style.width = typeof newWidth === "string" ? newWidth : `${newWidth || 1}px`;
        if (this._gridContainerElm) {
          this._gridContainerElm.style.width = typeof newWidth === "string" ? newWidth : `${newWidth}px`;
        }
        if (this._grid?.resizeCanvas && this._gridContainerElm) {
          this._grid.resizeCanvas();
        }
        if (this._grid && this.gridOptions?.enableAutoSizeColumns) {
          if (this.gridUid && document.querySelector(this.gridUidSelector)) {
            if (!this._lastGridSizes || this._lastGridSizes.height !== newHeight || this._lastGridSizes.width !== newWidth) {
              this._grid.autosizeColumns();
            }
          }
        } else if (this.gridOptions.enableAutoResizeColumnsByCellContent && (!this._lastGridSizes?.width || newWidth !== this._lastGridSizes.width)) {
          this.resizeColumnsByCellContent(false);
        }
        this._gridDomElm.style.width = typeof newWidth === "string" ? newWidth : `${(newWidth || 1) + 0.2}px`;
        this._lastGridSizes = {
          height: newHeight || 0,
          width: newWidth || 0
        };
      }
      return this._lastGridSizes;
    }
    requestStopOfAutoFixResizeGrid(isStopRequired = true) {
      this._isStopResizeIntervalRequested = isStopRequired;
    }
    /**
     * Resize each column width by their cell text/value content (this could potentially go wider than the viewport and end up showing an horizontal scroll).
     * This operation requires to loop through each dataset item to inspect each cell content width and has a performance cost associated to this process.
     *
     * NOTE: please that for performance reasons we will only inspect the first 1000 rows,
     * however user could override it by using the grid option `resizeMaxItemToInspectCellContentWidth` to increase/decrease how many items to inspect.
     * @param {Boolean} recalculateColumnsTotalWidth - defaults to false, do we want to recalculate the necessary total columns width even if it was already calculated?
     */
    resizeColumnsByCellContent(recalculateColumnsTotalWidth = false) {
      const columns = this._grid.getColumns();
      const dataset = this.dataView.getItems();
      const columnWidths = {};
      let reRender = false;
      let readItemCount = 0;
      const viewportWidth = this._gridContainerElm?.offsetWidth ?? 0;
      if (!Array.isArray(dataset) || dataset.length === 0 || !recalculateColumnsTotalWidth && this._totalColumnsWidthByContent > 0 && this._totalColumnsWidthByContent < viewportWidth) {
        this._grid.autosizeColumns();
        return;
      }
      if (this._hasResizedByContentAtLeastOnce && this.gridOptions?.resizeByContentOnlyOnFirstLoad && !recalculateColumnsTotalWidth) {
        return;
      }
      this.pubSubService.publish("onBeforeResizeByContent", void 0, 0);
      if (this._totalColumnsWidthByContent === 0 || recalculateColumnsTotalWidth) {
        for (const columnDef of columns) {
          columnWidths[columnDef.id] = columnDef.originalWidth ?? columnDef.minWidth ?? 0;
        }
        readItemCount = this.calculateCellWidthByReadingDataset(columns, columnWidths, this.resizeByContentOptions.maxItemToInspectCellContentWidth);
        let totalColsWidth = 0;
        for (const column of columns) {
          const resizeAlwaysRecalculateWidth = column.resizeAlwaysRecalculateWidth ?? this.resizeByContentOptions.alwaysRecalculateColumnWidth ?? false;
          if (column.originalWidth && !resizeAlwaysRecalculateWidth) {
            column.width = column.originalWidth;
          } else if (columnWidths[column.id] !== void 0) {
            if (column.rerenderOnResize) {
              reRender = true;
            }
            this.applyNewCalculatedColumnWidthByReference(column, columnWidths[column.id]);
          }
          totalColsWidth += column.width || 0;
          this._totalColumnsWidthByContent = totalColsWidth;
        }
      }
      this._grid.updateColumns();
      this._hasResizedByContentAtLeastOnce = true;
      const calculateColumnWidths = {};
      for (const columnDef of columns) {
        calculateColumnWidths[columnDef.id] = columnDef.width;
      }
      this._totalColumnsWidthByContent > viewportWidth ? this._grid.reRenderColumns(reRender) : this._grid.autosizeColumns();
      this.pubSubService.publish("onAfterResizeByContent", { readItemCount, calculateColumnWidths });
    }
    // --
    // protected functions
    // ------------------
    /**
     * Step 1 - The first step will read through the entire dataset (unless max item count is reached),
     * it will analyze each cell of the grid and calculate its max width via its content and column definition info (it will do so by calling step 2 method while looping through each cell).
     * @param columnOrColumns - single or array of column definition(s)
     * @param columnWidths - column width object that will be updated by reference pointers
     * @param columnIndexOverride - an optional column index, if provided it will override the column index position
     * @returns - count of items that was read
     */
    calculateCellWidthByReadingDataset(columnOrColumns, columnWidths, maxItemToInspect = 1e3, columnIndexOverride) {
      const columns = Array.isArray(columnOrColumns) ? columnOrColumns : [columnOrColumns];
      const dataset = this.dataView.getItems();
      if (!this._singleCharWidth) {
        this._singleCharWidth = this.getAverageCharWidthByFont();
      }
      let readItemCount = 0;
      const maxSanitizedTextMap = {};
      for (const [rowIdx, item] of dataset.entries()) {
        if (rowIdx > maxItemToInspect) {
          break;
        }
        columns.forEach((columnDef, colIdx) => {
          const formattedData = parseFormatterWhenExist(columnDef?.formatter, rowIdx, columnIndexOverride ?? colIdx, columnDef, item, this._grid);
          const formattedDataSanitized = isPrimitiveOrHTML(formattedData) ? stripTags(formattedData) : "";
          if (!maxSanitizedTextMap[columnDef.id] || formattedDataSanitized.length > maxSanitizedTextMap[columnDef.id].length) {
            maxSanitizedTextMap[columnDef.id] = formattedDataSanitized;
          }
        });
        readItemCount = rowIdx + 1;
      }
      columns.forEach((columnDef) => {
        const resizeCellCharWidthInPx = this.resizeByContentOptions.cellCharWidthInPx ?? this._singleCharWidth;
        const charWidthPx = columnDef?.resizeCharWidthInPx ?? resizeCellCharWidthInPx;
        const maxSanitizedText = maxSanitizedTextMap[columnDef.id] || "";
        const formattedTextWidthInPx = Math.ceil(maxSanitizedText.length * charWidthPx);
        const resizeMaxWidthThreshold = columnDef.resizeMaxWidthThreshold;
        let finalWidth = formattedTextWidthInPx;
        if (resizeMaxWidthThreshold !== void 0 && formattedTextWidthInPx > resizeMaxWidthThreshold) {
          finalWidth = resizeMaxWidthThreshold;
        } else if (columnDef.maxWidth !== void 0 && formattedTextWidthInPx > columnDef.maxWidth) {
          finalWidth = columnDef.maxWidth;
        }
        if (columnDef.minWidth !== void 0 && finalWidth < columnDef.minWidth) {
          finalWidth = columnDef.minWidth;
        }
        columnWidths[columnDef.id] = finalWidth;
      });
      return readItemCount;
    }
    /** Get an average width in pixel of a single character, we'll make an average by using all alphabetical chars and common symbols and calculate the average. */
    getAverageCharWidthByFont() {
      let charWidth = 0;
      const gCanvas = this._gridContainerElm.querySelector(".grid-canvas");
      if (gCanvas) {
        let isTmpCellCreated = false;
        let sCell = gCanvas.querySelector(".slick-cell");
        if (!sCell) {
          const sRow = createDomElement("div", { className: "slick-row" });
          sCell = createDomElement("div", { className: "slick-cell" });
          sRow.appendChild(sCell);
          gCanvas.appendChild(sRow);
          isTmpCellCreated = true;
        }
        if (sCell) {
          const { fontFamily, fontSize } = getComputedStyle(sCell);
          const ctx = this.getBrowserCanvas();
          ctx.font = `${fontSize} ${fontFamily}`;
          const text = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*()_+-={}:<>?,./ ";
          charWidth = ctx.measureText(text).width / text.length;
          isTmpCellCreated && gCanvas.querySelector(".slick-row")?.remove();
        }
      }
      return charWidth;
    }
    getBrowserCanvas() {
      return document.createElement("canvas").getContext("2d");
    }
    /**
     * Step 3 - Apply the new calculated width, it might or might not use this calculated width depending on a few conditions.
     * One of those condition will be to check that the new width doesn't go over a maxWidth and/or a maxWidthThreshold
     * @param {Object} column - column definition to apply the width
     * @param {Number} calculatedColumnWidth - new calculated column width to possibly apply
     */
    applyNewCalculatedColumnWidthByReference(column, calculatedColumnWidth) {
      const resizeCellPaddingWidthInPx = this.resizeByContentOptions.cellPaddingWidthInPx ?? 6;
      const resizeFormatterPaddingWidthInPx = this.resizeByContentOptions.formatterPaddingWidthInPx ?? 6;
      const fieldType = column?.filter?.type ?? column?.type ?? "string";
      let newColWidth = calculatedColumnWidth;
      const stringWidthRatio = column?.resizeCalcWidthRatio ?? this.resizeByContentOptions.defaultRatioForStringType ?? 0.9;
      newColWidth *= fieldType === "string" ? stringWidthRatio : 1;
      newColWidth += resizeCellPaddingWidthInPx;
      if (column.resizeExtraWidthPadding) {
        newColWidth += column.resizeExtraWidthPadding;
      }
      if (column.editor && this.gridOptions.editable) {
        newColWidth += resizeFormatterPaddingWidthInPx;
      }
      if (column.maxWidth !== void 0 && newColWidth > column.maxWidth) {
        newColWidth = column.maxWidth;
      }
      if (column.resizeMaxWidthThreshold !== void 0 && newColWidth > column.resizeMaxWidthThreshold) {
        newColWidth = column.resizeMaxWidthThreshold;
      }
      newColWidth = Math.ceil(newColWidth);
      if (column.originalWidth === void 0 || column.resizeAlwaysRecalculateWidth === true || this.resizeByContentOptions.alwaysRecalculateColumnWidth === true) {
        column.width = this.readjustNewColumnWidthWhenOverLimit(column, newColWidth);
      }
    }
    handleSingleColumnResizeByContent(columnId) {
      const columns = this._grid.getColumns();
      const columnDefIdx = columns.findIndex((col) => col.id === columnId);
      if (columnDefIdx >= 0) {
        const columnDef = columns[columnDefIdx];
        const columnWidths = { [columnId]: columnDef.originalWidth ?? columnDef.minWidth ?? 0 };
        columnDef.originalWidth = void 0;
        this.calculateCellWidthByReadingDataset(columnDef, columnWidths, this.resizeByContentOptions.maxItemToInspectSingleColumnWidthByContent, columnDefIdx);
        this.applyNewCalculatedColumnWidthByReference(columnDef, columnWidths[columnId]);
        this._grid.reRenderColumns(columnDef?.rerenderOnResize ?? false);
      }
    }
    /**
     * Checks wether the new calculated column width is valid or not, if it's not then return a lower and acceptable width.
     * When using frozen (pinned) column, we cannot make our column wider than the grid viewport because it would become unusable/unscrollable
     * and so if we do reach that threshold then our calculated column width becomes officially invalid
     * @param {Object} column - column definition
     * @param {Number} newColumnWidth - calculated column width input
     * @returns boolean
     */
    readjustNewColumnWidthWhenOverLimit(column, newColumnWidth) {
      const frozenColumnIdx = this.gridOptions.frozenColumn ?? -1;
      const columnIdx = this._grid.getColumns().findIndex((col) => col.id === column.id) ?? 0;
      let adjustedWidth = newColumnWidth;
      if (frozenColumnIdx >= 0 && columnIdx <= frozenColumnIdx) {
        const allViewports = Array.from(this._grid.getViewports());
        if (allViewports) {
          const leftViewportWidth = allViewports.find((viewport) => viewport.classList.contains("slick-viewport-left"))?.clientWidth ?? 0;
          const rightViewportWidth = allViewports.find((viewport) => viewport.classList.contains("slick-viewport-right"))?.clientWidth ?? 0;
          const viewportFullWidth = leftViewportWidth + rightViewportWidth;
          const leftViewportWidthMinusCurrentCol = leftViewportWidth - (column.width ?? 0);
          const isGreaterThanFullViewportWidth = leftViewportWidthMinusCurrentCol + newColumnWidth > viewportFullWidth;
          if (isGreaterThanFullViewportWidth) {
            const resizeWidthToRemoveFromExceededWidthReadjustment = this.resizeByContentOptions.widthToRemoveFromExceededWidthReadjustment ?? 50;
            adjustedWidth = leftViewportWidth - leftViewportWidthMinusCurrentCol + rightViewportWidth - resizeWidthToRemoveFromExceededWidthReadjustment;
          }
        }
      }
      return Math.ceil(adjustedWidth);
    }
    /**
     * Just check if the grid is still shown in the DOM/UI (visible to the end user and not hidden by a parent element, e.g. Tab, Accordion, etc.)
     * @returns is grid shown
     */
    isGridVisibleInUI() {
      return !!document.querySelector(`${this.gridUidSelector}`)?.offsetParent;
    }
    /**
     * Patch for SalesForce, some issues arise when having a grid inside a Tab and user clicks in a different Tab without waiting for the grid to be rendered
     * in ideal world, we would simply call a resize when user comes back to the Tab with the grid (tab focused) but this is an extra step and we might not always have this event available.
     * The grid seems broken, the header titles seems to be showing up behind the grid data and the rendering seems broken.
     * Why it happens? Because SlickGrid can resize problem when the DOM element is hidden and that happens when user doesn't wait for the grid to be fully rendered and go in a different Tab.
     *
     * So the patch is to call a grid resize if the following 2 conditions are met
     *   1- header row is Y coordinate 0 (happens when user is not in current Tab)
     *   2- header titles are lower than the viewport of dataset (this can happen when user change Tab and DOM is not shown),
     * for these cases we'll resize until it's no longer true or until we reach a max time limit (70min)
     */
    resizeGridWhenStylingIsBrokenUntilCorrected() {
      const autoFixResizeTimeout = this.gridOptions?.autoFixResizeTimeout ?? 5 * 60 * 60;
      const autoFixResizeRequiredGoodCount = this.gridOptions?.autoFixResizeRequiredGoodCount ?? 5;
      const headerElm = this._gridContainerElm.querySelector(`${this.gridUidSelector} .slick-header`);
      const viewportElm = this._gridContainerElm.querySelector(`${this.gridUidSelector} .slick-viewport`);
      let intervalExecutionCounter = 0;
      let resizeGoodCount = 0;
      if (headerElm && viewportElm && this.gridOptions.autoFixResizeWhenBrokenStyleDetected) {
        const dataLn = this.dataView.getItemCount();
        const columns = this._grid.getColumns() || [];
        this._intervalId = setInterval(async () => {
          if (!this.isGridVisibleInUI()) {
            this._cResizeCheckRequired = true;
          }
          const headerTitleRowHeight = 44;
          const headerPos = getOffset(headerElm);
          let headerOffsetTop = headerPos.top;
          if (this.isGridVisibleInUI() && (this._cResizeCheckRequired || !this._lastWindowSize || this._lastWindowSize.height !== window.innerHeight || this._lastWindowSize.width !== window.innerWidth)) {
            if (this.gridOptions?.enableFiltering && this.gridOptions.headerRowHeight) {
              headerOffsetTop += this.gridOptions.headerRowHeight;
            }
            if (this.gridOptions?.createPreHeaderPanel && this.gridOptions.showPreHeaderPanel && this.gridOptions.preHeaderPanelHeight) {
              headerOffsetTop += this.gridOptions.preHeaderPanelHeight;
            }
            headerOffsetTop += headerTitleRowHeight;
            const viewportPos = getOffset(viewportElm);
            const viewportOffsetTop = viewportPos.top;
            const containerElmOffset = getOffset(this._gridContainerElm);
            let isResizeRequired = headerPos?.top === 0 || headerOffsetTop - viewportOffsetTop > 2 || containerElmOffset.left === 0 && containerElmOffset.top === 0 ? true : false;
            const renderedRangeRightPx = this._grid.getRenderedRange()?.rightPx ?? 0;
            if (!isResizeRequired && dataLn > 0 && renderedRangeRightPx === 0 && columns.length > 1) {
              isResizeRequired = true;
            }
            if (this._isStopResizeIntervalRequested) {
              isResizeRequired = false;
              intervalExecutionCounter = autoFixResizeTimeout;
            }
            if (this.isGridVisibleInUI() && (isResizeRequired || containerElmOffset.left === 0 || containerElmOffset.top === 0)) {
              await this.resizeGrid();
              if (resizeGoodCount < 5) {
                this._grid.updateColumns();
              }
              if (this.isGridVisibleInUI()) {
                isResizeRequired = false;
                this._cResizeCheckRequired = false;
              }
            }
            if (this.isGridVisibleInUI()) {
              resizeGoodCount++;
            }
            if (this.isGridVisibleInUI() && !isResizeRequired && (resizeGoodCount >= autoFixResizeRequiredGoodCount || intervalExecutionCounter++ >= autoFixResizeTimeout)) {
              clearInterval(this._intervalId);
            }
          }
        }, this.intervalRetryDelay);
      }
    }
    /** keep reference of the last window size for possible comparisons */
    saveLastWindowSize() {
      this._lastWindowSize = {
        height: window.innerHeight,
        width: window.innerWidth
      };
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/shared.service.js
  var SharedService = class {
    constructor() {
      this.currentPagination = null;
      this.isItemsDateParsed = false;
      this.frozenVisibleColumnId = null;
      this.hasColumnsReordered = false;
      this.hideHeaderRowAfterPageLoad = false;
    }
    // --
    // GETTERs / SETTERs
    /** Getter for All Columns  in the grid (hidden/visible) */
    get allColumns() {
      return this._allColumns;
    }
    /** Setter for All Columns  in the grid (hidden/visible) */
    set allColumns(allColumns) {
      this._allColumns = allColumns;
    }
    /** Getter for the Column Definitions pulled through the Grid Object */
    get columnDefinitions() {
      return this.slickGrid?.getColumns() ?? [];
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this._gridOptions || this.slickGrid?.getOptions() || {};
    }
    /** Setter for the Grid Options pulled through the Grid Object */
    set gridOptions(gridOptions) {
      this._gridOptions = gridOptions;
    }
    /** Getter to know if user want to hide header row after 1st page load */
    get externalRegisteredResources() {
      return this._externalRegisteredResources;
    }
    /** Setter for knowing if user want to hide header row after 1st page load */
    set externalRegisteredResources(externalRegisteredResources) {
      this._externalRegisteredResources = externalRegisteredResources;
    }
    /** Getter for the Hierarchical Tree Data dataset when the feature is enabled */
    get hierarchicalDataset() {
      return this._hierarchicalDataset;
    }
    /** Getter for the Hierarchical Tree Data dataset when the feature is enabled */
    set hierarchicalDataset(hierarchicalDataset) {
      this._hierarchicalDataset = hierarchicalDataset;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/sort.service.js
  var SortService = class {
    constructor(collectionService, sharedService, pubSubService, backendUtilities, rxjs) {
      this.collectionService = collectionService;
      this.sharedService = sharedService;
      this.pubSubService = pubSubService;
      this.backendUtilities = backendUtilities;
      this.rxjs = rxjs;
      this._currentLocalSorters = [];
      this._isBackendGrid = false;
      this._eventHandler = new SlickEventHandler();
      if (this.rxjs) {
        this.httpCancelRequests$ = this.rxjs.createSubject();
      }
    }
    /** Getter of the SlickGrid Event Handler */
    get eventHandler() {
      return this._eventHandler;
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get _gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    /** Getter for the Column Definitions pulled through the Grid Object */
    get _columns() {
      return this._grid?.getColumns() ?? [];
    }
    dispose() {
      this._eventHandler?.unsubscribeAll();
      if (this.httpCancelRequests$ && this.rxjs?.isObservable(this.httpCancelRequests$)) {
        this.httpCancelRequests$.next();
        this.httpCancelRequests$.complete();
      }
    }
    addRxJsResource(rxjs) {
      this.rxjs = rxjs;
    }
    /**
     * Bind a backend sort (single/multi) hook to the grid
     * @param grid SlickGrid Grid object
     * @param dataView SlickGrid DataView object
     */
    bindBackendOnSort(grid) {
      this._isBackendGrid = true;
      this._grid = grid;
      this._dataView = grid?.getData();
      this._eventHandler.subscribe(grid.onSort, this.onBackendSortChanged.bind(this));
    }
    /**
     * Bind a local sort (single/multi) hook to the grid
     * @param grid SlickGrid Grid object
     * @param gridOptions Grid Options object
     * @param dataView
     */
    bindLocalOnSort(grid) {
      this._isBackendGrid = false;
      this._grid = grid;
      this._dataView = grid?.getData();
      this.processTreeDataInitialSort();
      this._eventHandler.subscribe(grid.onSort, this.handleLocalOnSort.bind(this));
      if (this._gridOptions.preParseDateColumns) {
        this._eventHandler.subscribe(grid.onCellChange, (_e, args) => this.preParseSingleDateItem(args.item));
        this.pubSubService.subscribe(["onItemsAdded", "onItemsUpdated"], (item) => this.preParseSingleDateItem(item));
      }
    }
    /** when pre-parse is enabled, parse date strings and convert them to JS Date objects */
    preParseSingleDateItem(item) {
      if (this._gridOptions.preParseDateColumns) {
        const items = Array.isArray(item) ? item : [item];
        items.forEach((itm) => this.collectionService.parseSingleDateItem(itm, this._grid, this._gridOptions.preParseDateColumns));
      }
    }
    /** when pre-parse is enabled, parse date strings and convert them to JS Date objects */
    preParseAllDateItems() {
      if (this._gridOptions.preParseDateColumns) {
        const items = this._dataView?.getItems() || [];
        this.collectionService.preParseByMutationDateItems(items, this._grid, this._gridOptions.preParseDateColumns);
        this.sharedService.isItemsDateParsed = true;
      }
    }
    handleLocalOnSort(_e, args) {
      const sortColumns = args.multiColumnSort ? args.sortCols : new Array({
        columnId: args.sortCol?.id ?? "",
        sortAsc: args.sortAsc,
        sortCol: args.sortCol
      });
      this._currentLocalSorters = [];
      if (Array.isArray(sortColumns)) {
        sortColumns.forEach((sortColumn) => {
          if (sortColumn.sortCol) {
            this._currentLocalSorters.push({
              columnId: sortColumn.sortCol.id,
              direction: sortColumn.sortAsc ? "ASC" : "DESC"
            });
          }
        });
      }
      this.onLocalSortChanged(this._grid, sortColumns);
      this.emitSortChanged("local");
    }
    clearSortByColumnId(event, columnId) {
      const allSortedCols = this.getCurrentColumnSorts();
      const sortedColsWithoutCurrent = this.getCurrentColumnSorts(`${columnId}`);
      if (Array.isArray(allSortedCols) && Array.isArray(sortedColsWithoutCurrent) && allSortedCols.length !== sortedColsWithoutCurrent.length) {
        if (this._gridOptions.backendServiceApi) {
          this.onBackendSortChanged(event, { multiColumnSort: true, sortCols: sortedColsWithoutCurrent, grid: this._grid });
        } else if (this._dataView) {
          this.onLocalSortChanged(this._grid, sortedColsWithoutCurrent, true, true);
        } else {
          const isMultiSort = this._gridOptions.multiColumnSort || false;
          const sortOutput = isMultiSort ? sortedColsWithoutCurrent : sortedColsWithoutCurrent[0];
          this._grid.onSort.notify(sortOutput);
        }
        const updatedSortColumns = sortedColsWithoutCurrent.map((col) => {
          return {
            columnId: col?.sortCol?.id ?? "",
            sortAsc: col?.sortAsc,
            sortCol: col?.sortCol
          };
        });
        this._grid.setSortColumns(updatedSortColumns);
      }
      if (Array.isArray(sortedColsWithoutCurrent) && sortedColsWithoutCurrent.length === 0) {
        this.sortLocalGridByDefaultSortFieldId();
      }
    }
    /**
     * Clear Sorting
     * - 1st, remove the SlickGrid sort icons (this setSortColumns function call really does only that)
     * - 2nd, we also need to trigger a sort change
     *   - for a backend grid, we will trigger a backend sort changed with an empty sort columns array
     *   - however for a local grid, we need to pass a sort column and so we will sort by the 1st column
     * @param trigger query event after executing clear filter?
     */
    clearSorting(triggerQueryEvent = true) {
      if (this._grid && this._gridOptions && this._dataView) {
        this._grid.setSortColumns([]);
        if (triggerQueryEvent) {
          if (this._isBackendGrid) {
            this.onBackendSortChanged(void 0, {
              grid: this._grid,
              multiColumnSort: true,
              sortCols: [],
              clearSortTriggered: true
            });
          } else {
            if (this._columns && Array.isArray(this._columns) && this._columns.length > 0) {
              this.sortLocalGridByDefaultSortFieldId();
            }
          }
        } else if (this._isBackendGrid) {
          const backendService = this._gridOptions.backendServiceApi?.service;
          if (backendService?.clearSorters) {
            backendService.clearSorters();
          }
        }
      }
      this._currentLocalSorters = [];
      this.pubSubService.publish("onSortCleared", true);
    }
    /**
     * Toggle the Sorting Functionality
     * @param {boolean} isSortingDisabled - optionally force a disable/enable of the Sort Functionality? Defaults to True
     * @param {boolean} clearSortingWhenDisabled - when disabling the sorting, do we also want to clear the sorting as well? Defaults to True
     */
    disableSortFunctionality(isSortingDisabled = true, clearSortingWhenDisabled = true) {
      const prevSorting = this._gridOptions.enableSorting;
      const newSorting = !prevSorting;
      this._gridOptions.enableSorting = newSorting;
      if (isSortingDisabled) {
        if (clearSortingWhenDisabled) {
          this.clearSorting();
        }
        this._eventHandler.unsubscribeAll();
        this.disableAllSortingCommands(true);
      } else {
        this.disableAllSortingCommands(false);
        this._eventHandler.subscribe(this._grid.onSort, (e2, args) => this.handleLocalOnSort(e2, args));
      }
      this._grid.setOptions({ enableSorting: this._gridOptions.enableSorting }, false, true);
      this.sharedService.gridOptions = this._gridOptions;
      this._grid.updateColumns();
    }
    /**
     * Toggle the Sorting functionality
     * @param {boolean} clearSortingWhenDisabled - when disabling the sorting, do we also want to clear the sorting as well? Defaults to True
     */
    toggleSortFunctionality(clearSortingOnDisable = true) {
      const previousSorting = this._gridOptions.enableSorting;
      this.disableSortFunctionality(previousSorting, clearSortingOnDisable);
    }
    /**
     * A simple function that will be called to emit a change when a sort changes.
     * Other services, like Pagination, can then subscribe to it.
     * @param sender
     */
    emitSortChanged(sender, currentLocalSorters) {
      if (sender === "remote" && this._gridOptions.backendServiceApi) {
        let currentSorters = [];
        const backendService = this._gridOptions.backendServiceApi.service;
        if (backendService?.getCurrentSorters) {
          currentSorters = backendService.getCurrentSorters();
        }
        this.pubSubService.publish("onSortChanged", currentSorters);
      } else if (sender === "local") {
        if (currentLocalSorters) {
          this._currentLocalSorters = currentLocalSorters;
        }
        this.pubSubService.publish("onSortChanged", this.getCurrentLocalSorters());
      }
    }
    getCurrentLocalSorters() {
      return this._currentLocalSorters;
    }
    /**
     * Get current column sorts,
     * If a column is passed as an argument, that will be exclusion so we won't add this column to our output array since it is already in the array.
     * The usage of this method is that we want to know the sort prior to calling the next sorting command
     */
    getCurrentColumnSorts(excludedColumnId) {
      if (this._grid) {
        const oldSortColumns = this._grid.getSortColumns();
        if (Array.isArray(oldSortColumns)) {
          const sortedCols = oldSortColumns.reduce((cols, col) => {
            if (col && (!excludedColumnId || col.columnId !== excludedColumnId)) {
              cols.push({
                columnId: col.columnId || "",
                sortCol: this._columns[this._grid.getColumnIndex(col.columnId || "")],
                sortAsc: col.sortAsc
              });
            }
            return cols;
          }, []);
          return sortedCols;
        }
      }
      return [];
    }
    /** Load defined Sorting (sorters) into the grid */
    loadGridSorters(sorters) {
      this._currentLocalSorters = [];
      const sortCols = [];
      if (Array.isArray(sorters)) {
        const tmpSorters = this._gridOptions.multiColumnSort ? sorters : sorters.slice(0, 1);
        tmpSorters.forEach((sorter) => {
          const column = this._columns.find((col) => col.id === sorter.columnId);
          if (column) {
            if (!column.sortable) {
              let errorMsg = "[Slickgrid-Universal] Cannot add sort icon to a column that is not sortable, please add `sortable: true` to your column or remove it from your list of columns to sort.";
              if (this._gridOptions.enableTreeData) {
                errorMsg += " Also note that TreeData feature requires the column holding the tree (expand/collapse icons) to be sortable.";
              }
              throw new Error(errorMsg);
            }
            sortCols.push({
              columnId: column.id,
              sortAsc: sorter.direction.toUpperCase() === "ASC" ? true : false,
              sortCol: column
            });
            this._currentLocalSorters.push({
              columnId: String(column.id),
              direction: sorter.direction.toUpperCase()
            });
          }
        });
        this.onLocalSortChanged(this._grid, sortCols);
        this._grid.setSortColumns(sortCols.map((col) => ({ columnId: col.columnId, sortAsc: col.sortAsc })));
      }
      return sortCols;
    }
    /** Process the initial sort, typically it will sort ascending by the column that has the Tree Data unless user specifies a different initialSort */
    processTreeDataInitialSort() {
      if (this._gridOptions.enableTreeData && this._gridOptions.treeDataOptions) {
        const treeDataOptions = this._gridOptions.treeDataOptions;
        const columnWithTreeData = this._columns.find((col) => col.id === treeDataOptions.columnId);
        if (columnWithTreeData) {
          let sortDirection = "ASC";
          let sortTreeLevelColumn = { columnId: treeDataOptions.columnId, sortCol: columnWithTreeData, sortAsc: true };
          if (treeDataOptions?.initialSort?.columnId) {
            const initialSortColumnId = treeDataOptions.initialSort.columnId;
            const initialSortColumn = this._columns.find((col) => col.id === initialSortColumnId);
            sortDirection = (treeDataOptions.initialSort.direction || "ASC").toUpperCase();
            sortTreeLevelColumn = {
              columnId: initialSortColumnId,
              sortCol: initialSortColumn,
              sortAsc: sortDirection === "ASC"
            };
          }
          if (sortTreeLevelColumn?.columnId && this.sharedService?.hierarchicalDataset) {
            this.updateSorting([{ columnId: sortTreeLevelColumn.columnId || "", direction: sortDirection }]);
          }
        }
      }
    }
    /**
     * When working with Backend Service, we'll use the `onBeforeSort` which will return false since we want to manually apply the sort icons only after the server response
     * @param event - optional Event that triggered the sort
     * @param args - sort event arguments
     * @returns - False since we'll apply the sort icon(s) manually only after server responded
     */
    onBackendSortChanged(event, args) {
      if (!args?.grid) {
        throw new Error('Something went wrong when trying to bind the "onBackendSortChanged(event, args)" function, it seems that "args" is not populated correctly');
      }
      const gridOptions = args.grid?.getOptions() ?? {};
      const backendApi = gridOptions.backendServiceApi;
      if (!backendApi || !backendApi.process || !backendApi.service) {
        throw new Error(`BackendServiceApi requires at least a "process" function and a "service" defined`);
      }
      const startTime = /* @__PURE__ */ new Date();
      backendApi.preProcess?.();
      const query = backendApi.service.processOnSortChanged(event, args);
      const totalItems = gridOptions?.pagination?.totalItems || 0;
      this.backendUtilities?.executeBackendCallback(backendApi, query, args, startTime, totalItems, {
        emitActionChangedCallback: this.emitSortChanged.bind(this),
        errorCallback: () => {
          this._grid.setSortColumns(args.previousSortColumns || []);
          const sorterData = args.previousSortColumns?.map((cs) => ({
            columnId: cs.columnId,
            sortAsc: cs.sortAsc,
            sortCol: this._columns.find((col) => col.id === cs.columnId)
          }));
          backendApi?.service?.updateSorters?.(sorterData || []);
        },
        httpCancelRequestSubject: this.httpCancelRequests$
      });
    }
    /** When a Sort Changes on a Local grid (JSON dataset) */
    async onLocalSortChanged(grid, sortColumns, forceReSort = false, emitSortChanged = false) {
      const datasetIdPropertyName = this._gridOptions.datasetIdPropertyName || "id";
      const isTreeDataEnabled = this._gridOptions.enableTreeData || false;
      const dataView = grid.getData();
      await this.pubSubService.publish("onBeforeSortChange", { sortColumns }, 0);
      if (grid && dataView) {
        if (this._gridOptions.preParseDateColumns && !this.sharedService.isItemsDateParsed && sortColumns.some((c) => isColumnDateType(c.sortCol?.type))) {
          this.preParseAllDateItems();
        }
        if (forceReSort && !isTreeDataEnabled) {
          dataView.reSort();
        }
        if (isTreeDataEnabled && this._dataView && this.sharedService && Array.isArray(this.sharedService.hierarchicalDataset)) {
          const datasetSortResult = this.sortHierarchicalDataset(this.sharedService.hierarchicalDataset, sortColumns);
          this._dataView.setItems(datasetSortResult.flat, datasetIdPropertyName);
          this._dataView.onRowCountChanged.notify({
            previous: this._dataView.getFilteredItemCount(),
            current: this._dataView.getLength(),
            itemCount: this._dataView.getItemCount(),
            dataView: this._dataView,
            callingOnRowsChanged: true
          });
        } else {
          dataView.sort(this.sortComparers.bind(this, sortColumns));
        }
        grid.invalidate();
        if (emitSortChanged) {
          this.emitSortChanged("local", sortColumns.map((col) => {
            return {
              columnId: col.sortCol?.id ?? "id",
              direction: col.sortAsc ? "ASC" : "DESC"
            };
          }));
        }
      }
    }
    /**
     * Takes a hierarchical dataset and sort it recursively by reference and returns both flat & hierarchical sorted datasets.
     * Note: for perf reasons, it mutates the array by adding extra props like `treeLevel`
     */
    sortHierarchicalDataset(hierarchicalDataset, sortColumns, emitSortChanged = false) {
      this.sortTreeData(hierarchicalDataset, sortColumns);
      const dataViewIdIdentifier = this._gridOptions.datasetIdPropertyName || "id";
      const treeDataOpt = this._gridOptions.treeDataOptions || { columnId: "" };
      const treeDataOptions = {
        ...treeDataOpt,
        identifierPropName: treeDataOpt.identifierPropName ?? dataViewIdIdentifier,
        shouldAddTreeLevelNumber: true
      };
      const sortedFlatArray = flattenToParentChildArray(hierarchicalDataset, treeDataOptions);
      if (emitSortChanged) {
        this._currentLocalSorters = [];
        sortColumns.forEach((sortCol) => {
          this._currentLocalSorters.push({ columnId: sortCol.columnId, direction: sortCol.sortAsc ? "ASC" : "DESC" });
        });
        const emitterType = this._gridOptions.backendServiceApi ? "remote" : "local";
        this.emitSortChanged(emitterType);
      }
      return { hierarchical: hierarchicalDataset, flat: sortedFlatArray };
    }
    /** Call a local grid sort by its default sort field id (user can customize default field by configuring "defaultColumnSortFieldId" in the grid options, defaults to "id") */
    sortLocalGridByDefaultSortFieldId() {
      const sortColFieldId = this._gridOptions && this._gridOptions.defaultColumnSortFieldId || this._gridOptions.datasetIdPropertyName || "id";
      const sortCol = { id: sortColFieldId, field: sortColFieldId };
      this.onLocalSortChanged(this._grid, new Array({ columnId: sortCol.id, sortAsc: true, sortCol, clearSortTriggered: true }), false, true);
    }
    sortComparers(sortColumns, dataRow1, dataRow2) {
      if (Array.isArray(sortColumns)) {
        for (const sortColumn of sortColumns) {
          const result = this.sortComparer(sortColumn, dataRow1, dataRow2);
          if (result !== void 0) {
            return result;
          }
        }
      }
      return SortDirectionNumber.neutral;
    }
    sortComparer(sortColumn, dataRow1, dataRow2, querySortField) {
      if (sortColumn?.sortCol) {
        const columnDef = sortColumn.sortCol;
        const fieldType = columnDef.type || "string";
        const sortDirection = sortColumn.sortAsc ? SortDirectionNumber.asc : SortDirectionNumber.desc;
        let queryFieldName1 = querySortField || columnDef.queryFieldSorter || columnDef.queryField || columnDef.field;
        if (this._gridOptions.preParseDateColumns && isColumnDateType(fieldType) && sortColumn?.columnId) {
          queryFieldName1 = typeof this._gridOptions.preParseDateColumns === "string" ? `${this._gridOptions.preParseDateColumns}${sortColumn.columnId}` : `${sortColumn.columnId}`;
        }
        let queryFieldName2 = queryFieldName1;
        if (typeof columnDef.queryFieldNameGetterFn === "function") {
          queryFieldName1 = columnDef.queryFieldNameGetterFn(dataRow1);
          queryFieldName2 = columnDef.queryFieldNameGetterFn(dataRow2);
        }
        let value1 = dataRow1[queryFieldName1];
        let value2 = dataRow2[queryFieldName2];
        if (queryFieldName1?.indexOf(".") >= 0) {
          value1 = getDescendantProperty(dataRow1, queryFieldName1);
        }
        if (queryFieldName2?.indexOf(".") >= 0) {
          value2 = getDescendantProperty(dataRow2, queryFieldName2);
        }
        if (columnDef.sortComparer) {
          const customSortResult = columnDef.sortComparer(value1, value2, sortDirection, columnDef, this._gridOptions);
          if (customSortResult !== SortDirectionNumber.neutral) {
            return customSortResult;
          }
        } else {
          const sortResult = sortByFieldType(fieldType, value1, value2, sortDirection, columnDef, this._gridOptions);
          if (sortResult !== SortDirectionNumber.neutral) {
            return sortResult;
          }
        }
      }
      return void 0;
    }
    sortTreeData(treeArray, sortColumns) {
      if (Array.isArray(sortColumns)) {
        sortColumns.forEach((sortColumn) => {
          this.sortTreeChildren(treeArray, sortColumn, 0);
        });
      }
    }
    /** Sort the Tree Children of a hierarchical dataset by recursion */
    sortTreeChildren(treeArray, sortColumn, treeLevel) {
      const childrenPropName = this._gridOptions.treeDataOptions?.childrenPropName ?? "children";
      treeArray.sort((a, b) => this.sortComparer(sortColumn, a, b) ?? SortDirectionNumber.neutral);
      treeArray.forEach((item) => {
        if (item) {
          const hasChildren = item.hasOwnProperty(childrenPropName) && Array.isArray(item[childrenPropName]);
          if (hasChildren) {
            treeLevel++;
            this.sortTreeChildren(item[childrenPropName], sortColumn, treeLevel);
            treeLevel--;
          }
        }
      });
    }
    /**
     * Update Sorting (sorters) dynamically just by providing an array of sorter(s).
     * You can also choose emit (default) a Sort Changed event that will be picked by the Grid State Service.
     *
     * Also for backend service only, you can choose to trigger a backend query (default) or not if you wish to do it later,
     * this could be useful when using updateFilters & updateSorting and you wish to only send the backend query once.
     * @param sorters array
     * @param triggerEvent defaults to True, do we want to emit a sort changed event?
     * @param triggerBackendQuery defaults to True, which will query the backend.
     */
    updateSorting(sorters, emitChangedEvent = true, triggerBackendQuery = true) {
      if (!this._gridOptions || !this._gridOptions.enableSorting) {
        throw new Error('[Slickgrid-Universal] in order to use "updateSorting" method, you need to have Sortable Columns defined in your grid and "enableSorting" set in your Grid Options');
      }
      if (Array.isArray(sorters)) {
        const backendApi = this._gridOptions.backendServiceApi;
        if (backendApi) {
          const backendApiService = backendApi?.service;
          if (backendApiService?.updateSorters) {
            backendApiService.updateSorters(void 0, sorters);
            if (triggerBackendQuery) {
              this.backendUtilities?.refreshBackendDataset(this._gridOptions);
            }
          }
        } else {
          this.loadGridSorters(sorters);
        }
        if (emitChangedEvent) {
          const emitterType = backendApi ? "remote" : "local";
          this.emitSortChanged(emitterType);
        }
      }
    }
    // --
    // protected functions
    // -------------------
    /**
     * Loop through all column definitions and do the following 2 things
     * 1. disable/enable the "sortable" property of each column
     * 2. loop through each Header Menu commands and change the "hidden" commands to show/hide depending if it's enabled/disabled
     * Also note that we aren't deleting any properties, we just toggle their flags so that we can reloop through at later point in time.
     * (if we previously deleted these properties we wouldn't be able to change them back since these properties wouldn't exist anymore, hence why we just hide the commands)
     * @param {boolean} isDisabling - are we disabling the sort functionality? Defaults to true
     */
    disableAllSortingCommands(isDisabling = true) {
      const columns = this._grid.getColumns();
      columns.forEach((col) => {
        if (col.sortable !== void 0) {
          col.sortable = !isDisabling;
        }
        if (col?.header?.menu) {
          col.header.menu.commandItems?.forEach((menuItem) => {
            if (menuItem && typeof menuItem !== "string") {
              const menuCommand = menuItem.command;
              if (menuCommand === "sort-asc" || menuCommand === "sort-desc" || menuCommand === "clear-sort") {
                menuItem.hidden = isDisabling;
              }
            }
          });
        }
      });
      const commandItems = this._gridOptions.gridMenu?.commandItems;
      if (commandItems) {
        commandItems.forEach((menuItem) => {
          if (menuItem && typeof menuItem !== "string" && menuItem.command === "clear-sorting") {
            menuItem.hidden = isDisabling;
          }
        });
      }
      return columns;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/services/treeData.service.js
  var TreeDataService = class {
    constructor(pubSubService, filterService, sharedService, sortService) {
      this.pubSubService = pubSubService;
      this.filterService = filterService;
      this.sharedService = sharedService;
      this.sortService = sortService;
      this.pluginName = "TreeDataService";
      this._currentToggledItems = [];
      this._isLastFullToggleCollapsed = false;
      this._isOneCpuCyclePassed = false;
      this._isTreeDataEnabled = false;
      this._subscriptions = [];
      this._treeDataRecalcHandler = null;
      this._eventHandler = new SlickEventHandler();
      this._timer1 = setTimeout(() => this._isOneCpuCyclePassed = true);
    }
    set currentToggledItems(newToggledItems) {
      this._currentToggledItems = newToggledItems;
    }
    get dataset() {
      return this.dataView?.getItems();
    }
    get datasetHierarchical() {
      return this.sharedService.hierarchicalDataset;
    }
    /** Getter of SlickGrid DataView object */
    get dataView() {
      return this._grid?.getData();
    }
    /** Getter of the SlickGrid Event Handler */
    get eventHandler() {
      return this._eventHandler;
    }
    get gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    get datasetIdPropName() {
      return this.gridOptions.datasetIdPropertyName || "id";
    }
    get treeDataOptions() {
      return this.gridOptions.treeDataOptions;
    }
    dispose() {
      clearTimeout(this._timer1);
      clearTimeout(this._timer2);
      this._eventHandler.unsubscribeAll();
      this.pubSubService.unsubscribeAll(this._subscriptions);
    }
    init(grid) {
      this._grid = grid;
      this._isTreeDataEnabled = this.gridOptions?.enableTreeData ?? false;
      this._isLastFullToggleCollapsed = this.treeDataOptions?.initiallyCollapsed ?? false;
      this._currentToggledItems = this.gridOptions.presets?.treeData?.toggledItems ?? [];
      this._lastToggleStateChange = {
        type: this._isLastFullToggleCollapsed ? "full-collapse" : "full-expand",
        previousFullToggleType: this._isLastFullToggleCollapsed ? "full-collapse" : "full-expand",
        toggledItems: this._currentToggledItems
      };
      if (this._isTreeDataEnabled) {
        if (this.gridOptions?.multiColumnSort) {
          throw new Error('[Slickgrid-Universal] It looks like you are trying to use Tree Data with multi-column sorting, unfortunately it is not supported because of its complexity, you can disable it via "multiColumnSort: false" grid option and/or help in providing support for this feature.');
        }
        if (!this.gridOptions?.enableFiltering) {
          throw new Error('[Slickgrid-Universal] It looks like you are trying to use Tree Data without using the filtering option, unfortunately that is not possible with Tree Data since it relies heavily on the filters to expand/collapse the tree. You need to enable it via "enableFiltering: true"');
        }
        if (this.gridOptions?.backendServiceApi || this.gridOptions?.enablePagination) {
          throw new Error("[Slickgrid-Universal] It looks like you are trying to use Tree Data with Pagination and/or a Backend Service (OData, GraphQL) but unfortunately that is simply not supported because of its complexity.");
        }
        if (!this.gridOptions.treeDataOptions || !this.gridOptions.treeDataOptions.columnId) {
          throw new Error('[Slickgrid-Universal] When enabling tree data, you must also provide the "treeDataOption" property in your Grid Options with "childrenPropName" or "parentPropName" (depending if your array is hierarchical or flat) for the Tree Data to work properly.');
        }
      }
      this.initHierarchicalTree();
      this._eventHandler.subscribe(grid.onClick, this.handleOnCellClick.bind(this));
      this._eventHandler.subscribe(grid.onKeyDown, this.handleOnKeyDown.bind(this));
      this._subscriptions.push(this.pubSubService.subscribe("onGridMenuClearAllSorting", this.clearSorting.bind(this)));
      this._treeDataRecalcHandler = this.setAutoRecalcTotalsCallbackWhenFeatEnabled(this.gridOptions);
      this._eventHandler.subscribe(this.dataView.onRowCountChanged, () => {
        if (typeof this._treeDataRecalcHandler === "function" && this._isOneCpuCyclePassed) {
          clearTimeout(this._timer2);
          this._timer2 = setTimeout(() => this._treeDataRecalcHandler?.(), this.treeDataOptions?.autoRecalcTotalsDebounce ?? 0);
        }
      });
    }
    /** init the hierarchical tree when necessary (e.g. `initiallyCollapsed` requires to collapse all items) */
    initHierarchicalTree() {
      if (this.gridOptions?.treeDataOptions?.initiallyCollapsed) {
        this.toggleAllHierarchicalTree(this.datasetHierarchical || [], true, getTreeDataOptionPropName(this.treeDataOptions, "childrenPropName"), getTreeDataOptionPropName(this.treeDataOptions, "collapsedPropName"), getTreeDataOptionPropName(this.treeDataOptions, "lazyLoadingPropName"));
      }
    }
    /**
     * Apply different tree toggle state changes (to ALL rows, the entire dataset) by providing an array of parentIds that are designated as collapsed (or not).
     * User will have to provide an array of `parentId` and `isCollapsed` boolean and the code will only apply the ones that are tagged as collapsed, everything else will be expanded
     * @param {Array<TreeToggledItem>} treeToggledItems - array of parentId which are tagged as changed
     * @param {ToggleStateChangeType} previousFullToggleType - optionally provide the previous full toggle type ('full-expand' or 'full-collapse')
     * @param {Boolean} shouldPreProcessFullToggle - should we pre-process a full toggle on all items? defaults to True
     * @param {Boolean} shouldTriggerEvent - should we trigger a toggled item event? defaults to False
     */
    applyToggledItemStateChanges(treeToggledItems, previousFullToggleType, shouldPreProcessFullToggle = true, shouldTriggerEvent = false) {
      if (Array.isArray(treeToggledItems)) {
        const collapsedPropName = getTreeDataOptionPropName(this.treeDataOptions, "collapsedPropName");
        const hasChildrenPropName = getTreeDataOptionPropName(this.treeDataOptions, "hasChildrenPropName");
        this.dataView.beginUpdate(true);
        const previousFullToggle = previousFullToggleType ?? this._lastToggleStateChange.previousFullToggleType;
        const shouldCollapseAll = previousFullToggle === "full-collapse";
        if (previousFullToggleType) {
          this._lastToggleStateChange.previousFullToggleType = previousFullToggleType;
        }
        if (shouldPreProcessFullToggle) {
          (this.dataView.getItems() || []).forEach((item) => {
            if (item[hasChildrenPropName]) {
              item[collapsedPropName] = shouldCollapseAll;
            }
          });
        }
        this.dynamicallyToggleItemState(treeToggledItems, shouldTriggerEvent);
      }
    }
    /**
     * Dynamically toggle and change state of certain parent items by providing an array of parentIds that are designated as to be collapsed (or not).
     * User will have to provide an array of `parentId` and `isCollapsed` boolean, only the provided list of items will be toggled and nothing else.
     *
     * NOTE: the `applyToggledItemStateChanges()` method is very similar but on top of toggling the `treeToggledItems` it WILL ALSO collapse everything else.
     * @param {Array<TreeToggledItem>} treeToggledItems - array of parentId which are tagged as changed
     * @param {Boolean} shouldTriggerEvent - should we trigger a toggled item event? defaults to True
     */
    dynamicallyToggleItemState(treeToggledItems, shouldTriggerEvent = true) {
      if (Array.isArray(treeToggledItems)) {
        this.dataView.beginUpdate(true);
        treeToggledItems.forEach((collapsedItem) => {
          const item = this.dataView.getItemById(collapsedItem.itemId);
          this.updateToggledItem(item, collapsedItem.isCollapsed, true);
          if (shouldTriggerEvent) {
            const parentFoundIdx = this._currentToggledItems.findIndex((treeChange) => treeChange.itemId === collapsedItem.itemId);
            if (parentFoundIdx >= 0) {
              this._currentToggledItems[parentFoundIdx].isCollapsed = collapsedItem.isCollapsed;
            } else {
              this._currentToggledItems.push({ itemId: collapsedItem.itemId, isCollapsed: collapsedItem.isCollapsed });
            }
            this.pubSubService.publish("onTreeItemToggled", {
              ...this._lastToggleStateChange,
              fromItemId: collapsedItem.itemId,
              toggledItems: this._currentToggledItems,
              type: collapsedItem.isCollapsed ? "toggle-collapse" : "toggle-expand"
            });
          }
        });
        this.dataView.endUpdate();
        this.dataView.refresh();
      }
    }
    /**
     * Get the current toggle state that includes the type (toggle, full-expand, full-collapse) and toggled items (only applies when it's a parent toggle)
     * @returns {TreeToggleStateChange} treeDataToggledItems - items that were toggled (array of `parentId` and `isCollapsed` flag)
     */
    getCurrentToggleState() {
      return this._lastToggleStateChange;
    }
    getInitialSort(columns, gridOptions) {
      const treeDataOptions = gridOptions?.treeDataOptions;
      const initialColumnSorting = treeDataOptions?.initialSort ?? { columnId: treeDataOptions?.columnId ?? "", direction: "ASC" };
      const initialSortColumn = columns.find((col) => col.id === initialColumnSorting.columnId);
      return {
        columnId: initialColumnSorting.columnId,
        sortAsc: initialColumnSorting?.direction?.toUpperCase() !== "DESC",
        sortCol: initialSortColumn
      };
    }
    /**
     * Get the full item count of the Tree.
     * When an optional tree level is provided, it will return the count for only that dedicated level (for example providing 0 would return the item count of all parent items)
     * @param {Number} [treeLevel] - optional tree level to get item count from
     * @returns
     */
    getItemCount(treeLevel) {
      if (treeLevel !== void 0) {
        const levelPropName = getTreeDataOptionPropName(this.treeDataOptions, "levelPropName");
        return this.dataView.getItems().filter((dataContext) => dataContext[levelPropName] === treeLevel).length;
      }
      return this.dataView.getItemCount();
    }
    /**
     * Get the current list of Tree Data item(s) that got toggled in the grid (basically the parents that the user clicked on the toggle icon to expand/collapse the child)
     * @returns {Array<TreeToggledItem>} treeDataToggledItems - items that were toggled (array of `parentId` and `isCollapsed` flag)
     */
    getToggledItems() {
      return this._currentToggledItems;
    }
    /** Clear the sorting and set it back to initial sort */
    clearSorting() {
      const initialSort = this.getInitialSort(this.sharedService.columnDefinitions, this.sharedService.gridOptions);
      this.sortService.loadGridSorters([{ columnId: initialSort.columnId, direction: initialSort.sortAsc ? "ASC" : "DESC" }]);
    }
    /**
     * Takes a flat dataset, converts it into a hierarchical dataset, sort it by recursion and finally return back the final and sorted flat array.
     * Note: for perf reasons, it mutates the array by adding extra props like `treeLevel`
     * @param {Array<Object>} flatDataset - parent/child flat dataset
     * @param {Column[]} columns - column definitions
     * @param {Object} gridOptions - grid options
     * @param {Array<ColumnSort>} [columnSorts] - optional sort columns
     * @returns {Array<Object>} - tree dataset
     */
    convertFlatParentChildToTreeDatasetAndSort(flatDataset, columns, gridOptions, columnSorts) {
      const datasetHierarchical = this.convertFlatParentChildToTreeDataset(flatDataset, gridOptions);
      const columnSort = Array.isArray(columnSorts) && columnSorts.length ? columnSorts[0] : this.getInitialSort(columns, gridOptions);
      const datasetSortResult = this.sortService.sortHierarchicalDataset(datasetHierarchical, [columnSort], true);
      this._grid?.setSortColumns([columnSort]);
      return datasetSortResult;
    }
    /**
     * Takes a flat dataset, converts it into a hierarchical dataset
     * Note: for perf reasons, it mutates the array by adding extra props like `treeLevel`
     * @param {Array<Object>} flatDataset - parent/child flat dataset
     * @param {Object} gridOptions - grid options
     * @returns {Array<Object>} - tree dataset
     */
    convertFlatParentChildToTreeDataset(flatDataset, gridOptions) {
      const treeDataOpt = gridOptions?.treeDataOptions ?? { columnId: "id" };
      const treeDataOptions = {
        ...treeDataOpt,
        identifierPropName: treeDataOpt.identifierPropName ?? this.datasetIdPropName,
        initiallyCollapsed: this._isLastFullToggleCollapsed
        // use the last full toggled flag so that if we replace the entire dataset we will still use the last toggled flag (this flag is also initialized with `initiallyCollapsed` when provided)
      };
      return unflattenParentChildArrayToTree(flatDataset, treeDataOptions);
    }
    /**
     * Dynamically enable (or disable) Tree Totals auto-recalc feature when Aggregators exists
     * @param {Boolean} [enableFeature=true]
     */
    enableAutoRecalcTotalsFeature(enableFeature = true) {
      if (enableFeature && this._isTreeDataEnabled) {
        this._treeDataRecalcHandler = this.recalculateTreeTotals.bind(this, this.gridOptions);
      } else {
        this._treeDataRecalcHandler = null;
      }
    }
    /**
     * Recalculate all Tree Data totals, this requires Aggregators to be defined.
     * NOTE: this does **not** take the current filters in consideration
     * @param gridOptions
     */
    recalculateTreeTotals(gridOptions) {
      const treeDataOptions = gridOptions.treeDataOptions;
      const childrenPropName = getTreeDataOptionPropName(this.treeDataOptions, "childrenPropName");
      const levelPropName = getTreeDataOptionPropName(this.treeDataOptions, "levelPropName");
      if (treeDataOptions?.aggregators) {
        treeDataOptions.aggregators.forEach((aggregator) => {
          addTreeLevelAndAggregatorsByMutation(this.sharedService.hierarchicalDataset || [], {
            childrenPropName,
            levelPropName,
            aggregator
          });
        });
        this._grid.invalidate();
      }
    }
    /**
     * Takes a hierarchical (tree) input array and sort it (if an `initialSort` exist, it will use that to sort)
     * Note: for perf reasons, it mutates the array by adding extra props like `treeLevel`
     * @param {Array<Object>} hierarchicalDataset - input hierarchical tree dataset
     * @param {ColumnSort | ColumnSort[]} [inputColumnSorts] - column sort(s)
     * @returns {Object} sort result object that includes both the flat & tree data arrays
     */
    sortHierarchicalDataset(hierarchicalDataset, inputColumnSorts) {
      const columnSorts = inputColumnSorts ?? this.getInitialSort(this._grid.getColumns(), this.gridOptions);
      const finalColumnSorts = Array.isArray(columnSorts) ? columnSorts : [columnSorts];
      return this.sortService.sortHierarchicalDataset(hierarchicalDataset, finalColumnSorts);
    }
    /**
     * Toggle the collapsed values of all parent items (the ones with children), we can optionally provide a flag to force a collapse or expand
     * @param {Boolean} collapsing - optionally force a collapse/expand (True => collapse all, False => expand all)
     * @param {Boolean} shouldTriggerEvent - defaults to true, should we trigger an event? For example, we could disable this to avoid a Grid State change event.
     * @returns {Promise<void>} - returns a void Promise, the reason we use a Promise is simply to make sure that when we add a spinner, it doesn't start/stop only at the end of the process
     */
    async toggleTreeDataCollapse(collapsing, shouldTriggerEvent = true) {
      if (this.gridOptions?.enableTreeData) {
        const collapsedPropName = getTreeDataOptionPropName(this.treeDataOptions, "collapsedPropName");
        const childrenPropName = getTreeDataOptionPropName(this.treeDataOptions, "childrenPropName");
        const hasChildrenPropName = getTreeDataOptionPropName(this.treeDataOptions, "hasChildrenPropName");
        const lazyLoadingPropName = getTreeDataOptionPropName(this.treeDataOptions, "lazyLoadingPropName");
        if (shouldTriggerEvent) {
          await this.pubSubService.publish("onTreeFullToggleStart", { collapsing });
        }
        this.dataView.beginUpdate(true);
        this.toggleAllHierarchicalTree(this.sharedService.hierarchicalDataset || [], collapsing, childrenPropName, collapsedPropName, lazyLoadingPropName);
        this.toggleAllFlatDataItems(collapsing, hasChildrenPropName, lazyLoadingPropName);
        this.dataView.endUpdate();
        this.dataView.refresh();
        this._isLastFullToggleCollapsed = collapsing;
      }
      const toggleType = collapsing ? "full-collapse" : "full-expand";
      this._lastToggleStateChange = {
        type: toggleType,
        previousFullToggleType: toggleType,
        toggledItems: null
      };
      if (shouldTriggerEvent) {
        this.pubSubService.publish("onTreeFullToggleEnd", this._lastToggleStateChange);
      }
    }
    // --
    // protected functions
    // ------------------
    // allow expand if it isn't using lazy loading OR it the lazy node was already loaded
    checkIsLazyParentLoaded(item, collapsing, lazyLoadingPropName) {
      return collapsing || !this.treeDataOptions?.lazy || !collapsing && !(this.treeDataOptions.lazy && !item[lazyLoadingPropName]);
    }
    handleOnCellClick(event, args) {
      if (event && args) {
        const targetElm = event.target;
        const collapsedPropName = getTreeDataOptionPropName(this.treeDataOptions, "collapsedPropName");
        const childrenPropName = getTreeDataOptionPropName(this.treeDataOptions, "childrenPropName");
        const hasChildrenPropName = getTreeDataOptionPropName(this.treeDataOptions, "hasChildrenPropName");
        const lazyLoadingPropName = getTreeDataOptionPropName(this.treeDataOptions, "lazyLoadingPropName");
        if (typeof targetElm?.className === "string") {
          const isIconClicked = ["slick-group-toggle", "slick-tree-toggle"].some((cls) => targetElm.classList.contains(cls));
          const isElmToggled = this.treeDataOptions?.toggleOnNodeTitle ? isIconClicked || targetElm?.closest(".slick-tree-title") : isIconClicked;
          if (isElmToggled) {
            const item = this.dataView.getItem(args.row);
            if (item) {
              item[collapsedPropName] = !item[collapsedPropName];
              const isCollapsed = item[collapsedPropName];
              const itemId = item[this.datasetIdPropName];
              const parentFoundIdx = this._currentToggledItems.findIndex((treeChange) => treeChange.itemId === itemId);
              if (parentFoundIdx >= 0) {
                this._currentToggledItems[parentFoundIdx].isCollapsed = isCollapsed;
              } else {
                this._currentToggledItems.push({ itemId, isCollapsed });
              }
              this.dataView.updateItem(itemId, item);
              const searchTreePredicate = (treeItemToSearch) => treeItemToSearch[this.datasetIdPropName] === itemId;
              const treeItemFound = findItemInTreeStructure(this.sharedService.hierarchicalDataset || [], searchTreePredicate, childrenPropName);
              if (treeItemFound) {
                if (!this.treeDataOptions?.lazy) {
                  treeItemFound[collapsedPropName] = isCollapsed;
                } else if (typeof this.treeDataOptions.onLazyLoad === "function" && !isCollapsed) {
                  if (!treeItemFound?.[lazyLoadingPropName] || treeItemFound[lazyLoadingPropName] === "load-fail") {
                    this.dataView?.updateItem(treeItemFound[this.datasetIdPropName], {
                      ...treeItemFound,
                      [lazyLoadingPropName]: "loading",
                      [collapsedPropName]: true,
                      // keep parent collapse until fetch resolves
                      [hasChildrenPropName]: true
                    });
                    const previousFilters = this.filterService.getPreviousFilters();
                    this.treeDataOptions.onLazyLoad(
                      treeItemFound,
                      // 2.1 resolve callback (lazily fetch node children)
                      (data) => {
                        treeItemFound[collapsedPropName] = false;
                        if (data.length) {
                          data.forEach((newItem) => {
                            if (newItem[childrenPropName]) {
                              newItem[collapsedPropName] = true;
                            }
                          });
                          treeItemFound[childrenPropName] ??= [];
                          treeItemFound[childrenPropName].push(...data);
                          treeItemFound[lazyLoadingPropName] = "done";
                          const sortColumns = this.sortService.getCurrentColumnSorts();
                          const updatedCollection = this.sortService.sortHierarchicalDataset(this.sharedService.hierarchicalDataset || [], sortColumns);
                          this.dataView?.setItems(updatedCollection.flat, this.datasetIdPropName);
                        } else {
                          this.dataView?.updateItem(treeItemFound[this.datasetIdPropName], {
                            ...treeItemFound,
                            [lazyLoadingPropName]: "done",
                            [hasChildrenPropName]: true
                          });
                        }
                        if (previousFilters.length) {
                          this.filterService.updateFilters(previousFilters);
                        } else {
                          this.filterService.refreshTreeDataFilters();
                        }
                      },
                      // 2.2 reject failure callback (lazy fetch fails)
                      () => {
                        this.dataView?.updateItem(treeItemFound[this.datasetIdPropName], {
                          ...treeItemFound,
                          [lazyLoadingPropName]: "load-fail",
                          [hasChildrenPropName]: true
                        });
                      }
                    );
                  }
                }
              }
              this._grid.invalidate();
              this._lastToggleStateChange = {
                type: isCollapsed ? "toggle-collapse" : "toggle-expand",
                previousFullToggleType: this._isLastFullToggleCollapsed ? "full-collapse" : "full-expand",
                toggledItems: this._currentToggledItems
              };
              this.pubSubService.publish("onTreeItemToggled", {
                ...this._lastToggleStateChange,
                fromItemId: itemId
              });
            }
            event.stopImmediatePropagation();
          }
        }
      }
    }
    handleOnKeyDown(e2, args) {
      if (!this._grid.getEditorLock()?.isActive() && // do not intercept keyboard actions while inline editor is active
      (e2.key === "ArrowLeft" || e2.key === "ArrowRight" || e2.key === " ")) {
        const row = args?.row ?? this._grid.getActiveCell()?.row;
        const cell = args?.cell ?? this._grid.getActiveCell()?.cell;
        if (row !== void 0 && cell !== void 0) {
          const hasChildrenPropName = getTreeDataOptionPropName(this.treeDataOptions, "hasChildrenPropName");
          const collapsedPropName = getTreeDataOptionPropName(this.treeDataOptions, "collapsedPropName");
          const item = this.dataView.getItem(row);
          const isCollapsed = !!item?.[collapsedPropName];
          const shouldToggle = !!item?.[hasChildrenPropName] && (e2.key === " " || e2.key === "ArrowRight" && isCollapsed || e2.key === "ArrowLeft" && !isCollapsed);
          if (shouldToggle) {
            const cellGroupClassName = ".slick-group-toggle,.slick-tree-toggle";
            const cellGroupToggleElm = this._grid.getActiveCellNode()?.querySelector(cellGroupClassName);
            if (cellGroupToggleElm) {
              e2.preventDefault();
              e2.stopImmediatePropagation();
              const clickEvent = {
                target: cellGroupToggleElm,
                stopImmediatePropagation: () => e2.stopImmediatePropagation()
              };
              this.handleOnCellClick(clickEvent, {
                row,
                cell,
                grid: this._grid
              });
            }
          }
        }
      }
    }
    /**
     * Toggle an item in the flat dataset and update it in the grid.
     * NOTE: We should update the Tree Data collapse status (shouldUpdateTree=true) ONLY when toggling 1 item at a time,
     * however for toggle a batch (i.e. collapse all), we'll want to convert skip updating the tree but rather convert from flat to tree which is much quicker to execute.
     */
    updateToggledItem(item, isCollapsed, shouldUpdateTree) {
      const childrenPropName = getTreeDataOptionPropName(this.treeDataOptions, "childrenPropName");
      const collapsedPropName = getTreeDataOptionPropName(this.treeDataOptions, "collapsedPropName");
      if (item) {
        item[collapsedPropName] = isCollapsed;
        this.dataView.updateItem(item[this.datasetIdPropName], item);
        if (shouldUpdateTree) {
          const searchTreePredicate = (treeItemToSearch) => treeItemToSearch[this.datasetIdPropName] === item[this.datasetIdPropName];
          const treeItemFound = findItemInTreeStructure(this.sharedService.hierarchicalDataset || [], searchTreePredicate, childrenPropName);
          if (treeItemFound) {
            treeItemFound[collapsedPropName] = isCollapsed;
          }
        }
      }
    }
    /**
     * When using Tree Data with Aggregator and auto-recalc flag is enabled, we will define a callback handler
     * @return {Function | undefined} Tree Data totals recalculate callback when enabled
     */
    setAutoRecalcTotalsCallbackWhenFeatEnabled(gridOptions) {
      if (gridOptions?.enableTreeData && gridOptions?.treeDataOptions?.autoRecalcTotalsOnFilterChange && gridOptions?.treeDataOptions?.aggregators) {
        return this.recalculateTreeTotals.bind(this, gridOptions);
      }
      return null;
    }
    /**
     * Toggle all items of a hierarchical (tree) dataset, user must provide the children, collapsed & lazyCollapsing property names
     * NOTE: this does NOT change or update the flat dataset, it only changes the collapsed flag in the hierarchical tree only without updating the grid.
     */
    toggleAllHierarchicalTree(hierarchicalData, collapsing, childrenPropName, collapsedPropName, lazyLoadingPropName) {
      hierarchicalData.forEach((item) => {
        if (childrenPropName in item && this.checkIsLazyParentLoaded(item, collapsing, lazyLoadingPropName)) {
          item[collapsedPropName] = collapsing;
          this.toggleAllHierarchicalTree(item[childrenPropName], collapsing, childrenPropName, collapsedPropName, lazyLoadingPropName);
        }
      });
    }
    /**
     * Toggle all items of a flat dataset, user must provide the hasChildrenPropName & lazyLoadingPropName property names
     * NOTE: this does NOT change or update the hierarchical dataset, it only changes the collapsed flag in the flat dataset only without updating the grid.
     */
    toggleAllFlatDataItems(collapsing, hasChildrenPropName, lazyLoadingPropName) {
      const flatDataItems = this.dataView.getItems() || [];
      flatDataItems.forEach((item) => {
        if (item[hasChildrenPropName] && this.checkIsLazyParentLoaded(item, collapsing, lazyLoadingPropName)) {
          this.updateToggledItem(item, collapsing, false);
        }
      });
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/inputFilter.js
  var InputFilter = class {
    constructor(translaterService) {
      this.translaterService = translaterService;
      this._debounceTypingDelay = 0;
      this._shouldTriggerQuery = true;
      this._inputType = "text";
      this.inputFilterType = "single";
      this.searchTerms = [];
      this._bindEventService = new BindingEventService();
    }
    /** Getter for the Column Filter */
    get columnFilter() {
      return this.columnDef?.filter ?? {};
    }
    /** Getter to know what would be the default operator when none is specified */
    get defaultOperator() {
      return "";
    }
    /** Getter of input type (text, number, password) */
    get inputType() {
      return this._inputType;
    }
    /** Setter of input type (text, number, password) */
    set inputType(type) {
      this._inputType = type;
    }
    /** Getter for the Filter Operator */
    get operator() {
      return this.columnFilter?.operator ?? this.defaultOperator;
    }
    /** Setter for the Filter Operator */
    set operator(operator) {
      if (this.columnFilter) {
        this.columnFilter.operator = operator;
      }
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this.grid?.getOptions() ?? {};
    }
    get isCompoundFilter() {
      return this.inputFilterType === "compound";
    }
    /**
     * Initialize the Filter
     */
    init(args) {
      this.grid = args.grid;
      this.callback = args.callback;
      this.columnDef = args.columnDef;
      if (this.isCompoundFilter) {
        this.operator = args.operator || "";
      }
      this.searchTerms = args?.searchTerms ?? [];
      this._cellContainerElm = args.filterContainerElm;
      const backendApi = this.gridOptions?.backendServiceApi;
      this._debounceTypingDelay = (backendApi ? backendApi?.filterTypingDebounce ?? this.gridOptions?.defaultBackendServiceFilterTypingDebounce : this.gridOptions?.filterTypingDebounce) ?? 0;
      const searchTerm = Array.isArray(this.searchTerms) && this.searchTerms.length >= 0 ? this.searchTerms[0] : "";
      this.columnDef.filter ??= {};
      this.columnDef.filter.emptySearchTermReturnAllValues ??= false;
      this.createDomFilterElement(searchTerm);
      this._bindEventService.bind(this._filterInputElm, ["keyup", "change"], this.onTriggerEvent.bind(this));
      this._bindEventService.bind(this._filterInputElm, "wheel", this.onTriggerEvent.bind(this), {
        passive: true
      });
      if (this.isCompoundFilter && this._selectOperatorElm) {
        this._bindEventService.bind(this._selectOperatorElm, "change", this.onTriggerEvent.bind(this));
      }
    }
    /**
     * Clear the filter value
     */
    clear(shouldTriggerQuery = true) {
      if (this._filterInputElm) {
        this._shouldTriggerQuery = shouldTriggerQuery;
        this.searchTerms = [];
        this._filterInputElm.value = "";
        this._currentValue = void 0;
        this.updateFilterStyle(false);
        if (this.isCompoundFilter && this._selectOperatorElm) {
          this._selectOperatorElm.selectedIndex = 0;
        }
        this.onTriggerEvent(void 0, true);
      }
    }
    /**
     * destroy the filter
     */
    destroy() {
      this._bindEventService.unbindAll();
      this._selectOperatorElm?.remove?.();
      this._filterInputElm?.remove?.();
    }
    getValues() {
      return this._filterInputElm.value;
    }
    /** Set value(s) on the DOM element */
    setValues(values, operator, triggerChange = false) {
      const searchValues = Array.isArray(values) ? values : [values];
      let newInputValue = "";
      for (const value of searchValues) {
        if (!this.isCompoundFilter) {
          newInputValue = operator ? this.addOptionalOperatorIntoSearchString(value, operator) : value;
        } else {
          newInputValue = `${value}`;
        }
        this._filterInputElm.value = `${newInputValue ?? ""}`;
        this._currentValue = this._filterInputElm.value;
      }
      this._lastSearchValue = this._currentValue;
      this.updateFilterStyle(this.getValues() !== "");
      this.operator = operator || this.defaultOperator;
      if (operator && this._selectOperatorElm) {
        const operatorShorthand = mapOperatorToShorthandDesignation(this.operator);
        this._selectOperatorElm.value = operatorShorthand;
      }
      if (triggerChange) {
        this.onTriggerEvent(void 0, false);
      }
    }
    //
    // protected functions
    // ------------------
    /**
     * When loading the search string from the outside into the input text field, we should also add the prefix/suffix of the operator.
     * We do this so that if it was loaded by a Grid Presets then we should also add the operator into the search string
     * Let's take these 3 examples:
     * 1. (operator: '>=', searchTerms:[55]) should display as ">=55"
     * 2. (operator: 'StartsWith', searchTerms:['John']) should display as "John*"
     * 3. (operator: 'EndsWith', searchTerms:['John']) should display as "*John"
     * @param operator - operator string
     */
    addOptionalOperatorIntoSearchString(inputValue, operator) {
      let searchTermPrefix = "";
      let searchTermSuffix = "";
      let outputValue = inputValue === void 0 || inputValue === null ? "" : `${inputValue}`;
      if (operator) {
        switch (operator) {
          case "<>":
          case "!=":
          case "=":
          case "==":
          case ">":
          case ">=":
          case "<":
          case "<=":
            searchTermPrefix = operator;
            break;
          case "EQ":
            searchTermPrefix = "=";
            break;
          case "GE":
            searchTermPrefix = ">=";
            break;
          case "GT":
            searchTermPrefix = ">";
            break;
          case "NE":
            searchTermPrefix = "!=";
            break;
          case "LE":
            searchTermPrefix = "<=";
            break;
          case "LT":
            searchTermPrefix = "<";
            break;
          case "EndsWith":
          case "*z":
            searchTermPrefix = "*";
            break;
          case "StartsWith":
          case "a*":
            searchTermSuffix = "*";
            break;
        }
        outputValue = `${searchTermPrefix}${this.trimValueWhenEnabled(outputValue)}${searchTermSuffix}`;
      }
      return outputValue;
    }
    /** Get the available operator option values to populate the operator select dropdown list */
    getCompoundOperatorOptionValues() {
      const type = this.columnDef.type ? this.columnDef.type : "string";
      let operatorList;
      let listType = "text";
      if (this.columnFilter?.compoundOperatorList) {
        operatorList = this.columnFilter.compoundOperatorList;
      } else {
        switch (type) {
          case "string":
          case "text":
          case "readonly":
          case "password":
            listType = "text";
            operatorList = compoundOperatorString(this.gridOptions, this.translaterService);
            break;
          default:
            listType = "numeric";
            operatorList = compoundOperatorNumeric(this.gridOptions, this.translaterService);
            break;
        }
      }
      applyOperatorAltTextWhenExists(this.gridOptions, operatorList, listType);
      return operatorList;
    }
    /**
     * From the html template string, create a DOM element
     * @param {Object} searchTerm - filter search term
     * @returns {Object} DOM element filter
     */
    createDomFilterElement(searchTerm) {
      const columnId = this.columnDef?.id ?? "";
      emptyElement(this._cellContainerElm);
      let placeholder = this.gridOptions?.defaultFilterPlaceholder ?? "";
      if (this.columnFilter?.placeholder) {
        placeholder = this.columnFilter.placeholder;
      }
      let searchVal = `${searchTerm ?? ""}`;
      if (!this.isCompoundFilter) {
        searchVal = this.addOptionalOperatorIntoSearchString(searchVal, this.operator);
      }
      this._filterInputElm = createDomElement("input", {
        type: this._inputType || "text",
        autocomplete: "off",
        ariaAutoComplete: "none",
        placeholder,
        ariaLabel: this.columnFilter?.ariaLabel ?? `${toSentenceCase(columnId + "")} Search Filter`,
        className: `form-control filter-${columnId}`,
        value: searchVal,
        dataset: { columnid: `${columnId}` },
        tabIndex: 0
      });
      this.updateFilterStyle(!!searchTerm);
      if (searchTerm !== void 0) {
        this._currentValue = searchVal;
        this._lastSearchValue = this._currentValue;
      }
      if (!this.isCompoundFilter) {
        this._filterContainerElm = this._filterInputElm;
        this._filterInputElm.classList.add("search-filter", "slick-filter");
        this._cellContainerElm.appendChild(this._filterInputElm);
        this._cellContainerElm.appendChild(document.createElement("span"));
      } else {
        this._filterInputElm.classList.add("compound-input");
        this._selectOperatorElm = buildSelectOperator(this.getCompoundOperatorOptionValues(), this.grid);
        this._filterContainerElm = createDomElement("div", { className: `form-group search-filter slick-filter filter-${columnId}` });
        const containerInputGroupElm = createDomElement("div", { className: "input-group" }, this._filterContainerElm);
        const operatorInputGroupAddonElm = createDomElement("div", { className: "input-group-addon input-group-prepend operator" }, containerInputGroupElm);
        operatorInputGroupAddonElm.appendChild(this._selectOperatorElm);
        containerInputGroupElm.appendChild(this._filterInputElm);
        containerInputGroupElm.appendChild(createDomElement("span"));
        if (this.operator) {
          this._selectOperatorElm.value = mapOperatorToShorthandDesignation(this.operator);
        }
        if (this._filterContainerElm) {
          this._cellContainerElm.appendChild(this._filterContainerElm);
        }
      }
    }
    trimValueWhenEnabled(val) {
      const enableWhiteSpaceTrim = this.gridOptions.enableFilterTrimWhiteSpace || this.columnFilter.enableTrimWhiteSpace;
      if (typeof val === "string" && enableWhiteSpaceTrim) {
        return val.trim();
      }
      return val;
    }
    /**
     * Event handler to cover the following (keyup, change, mousewheel & spinner)
     * We will trigger the Filter Service callback from this handler
     */
    onTriggerEvent(event, isClearFilterEvent = false) {
      if (isClearFilterEvent) {
        this.callback(event, {
          columnDef: this.columnDef,
          clearFilterTriggered: isClearFilterEvent,
          shouldTriggerQuery: this._shouldTriggerQuery
        });
        this.updateFilterStyle(false);
      } else {
        const eventType = event?.type || "";
        const selectedOperator = this._selectOperatorElm?.value ?? (!this.isCompoundFilter ? "" : this.operator);
        const value = this.trimValueWhenEnabled(this._filterInputElm.value);
        if (event?.target?.tagName.toLowerCase() !== "select") {
          this._currentValue = value;
        }
        this.updateFilterStyle(value !== "");
        const callbackArgs = {
          columnDef: this.columnDef,
          operator: selectedOperator,
          searchTerms: value ? [value] : null,
          shouldTriggerQuery: this._shouldTriggerQuery
        };
        const typingDelay = eventType === "keyup" && event?.key !== "Enter" ? this._debounceTypingDelay : 0;
        const skipNullInput = this.columnFilter.skipCompoundOperatorFilterWithNullInput ?? this.gridOptions.skipCompoundOperatorFilterWithNullInput ?? this.gridOptions.skipCompoundOperatorFilterWithNullInput === void 0;
        const hasSkipNullValChanged = skipNullInput && isDefined(this._currentValue) || this._currentValue === "" && isDefined(this._lastSearchValue);
        if (!this.isCompoundFilter || !skipNullInput || hasSkipNullValChanged) {
          if (typingDelay > 0) {
            clearTimeout(this._timer);
            this._timer = setTimeout(() => this.callback(event, callbackArgs), typingDelay);
          } else {
            this.callback(event, callbackArgs);
          }
        }
        this._lastSearchValue = value;
      }
      this._shouldTriggerQuery = true;
    }
    /** add/remove "filled" CSS class */
    updateFilterStyle(isFilled) {
      this._filterContainerElm?.classList.toggle("filled", isFilled);
      this._filterInputElm.classList.toggle("filled", isFilled);
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/compoundInputFilter.js
  var CompoundInputFilter = class extends InputFilter {
    /**
     * Initialize the Filter
     */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this.inputType = "text";
      this.inputFilterType = "compound";
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/compoundInputNumberFilter.js
  var CompoundInputNumberFilter = class extends InputFilter {
    /** Initialize the Filter */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this.inputType = "number";
      this.inputFilterType = "compound";
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/compoundInputPasswordFilter.js
  var CompoundInputPasswordFilter = class extends InputFilter {
    /** Initialize the Filter */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this.inputType = "password";
      this.inputFilterType = "compound";
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/sliderFilter.js
  var DEFAULT_SLIDER_TRACK_FILLED_COLOR = "#86bff8";
  var GAP_BETWEEN_SLIDER_HANDLES = 0;
  var Z_INDEX_MIN_GAP = 20;
  var SliderFilter = class {
    constructor(translaterService) {
      this.translaterService = translaterService;
      this._clearFilterTriggered = false;
      this._shouldTriggerQuery = true;
      this._sliderTrackFilledColor = DEFAULT_SLIDER_TRACK_FILLED_COLOR;
      this.sliderType = "double";
      this.searchTerms = [];
      this._bindEventService = new BindingEventService();
    }
    /** Getter for the Column Filter */
    get columnFilter() {
      return this.columnDef?.filter ?? {};
    }
    /** Getter for the Current Slider Value */
    get currentValue() {
      return this._currentValue;
    }
    /** Getter for the Current Slider Values */
    get currentValues() {
      return this._currentValues;
    }
    /** Getter to know what would be the default operator when none is specified */
    get defaultOperator() {
      if (this.sliderType === "compound") {
        return "";
      } else if (this.sliderType === "single") {
        return "GE";
      }
      return this.gridOptions.defaultFilterRangeOperator || "RangeInclusive";
    }
    get filterOptions() {
      return { ...this.gridOptions.defaultFilterOptions?.slider, ...this.columnFilter?.options };
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this.grid?.getOptions() ?? {};
    }
    /** Getter for the current Slider Options */
    get sliderOptions() {
      return this._sliderOptions;
    }
    /** Getter for the Filter Operator */
    get operator() {
      return this._operator || (this.columnFilter.operator ?? this.defaultOperator);
    }
    /** Setter for the Filter Operator */
    set operator(operator) {
      this._operator = operator;
    }
    /** Initialize the Filter */
    init(args) {
      this.grid = args.grid;
      this.callback = args.callback;
      this.columnDef = args.columnDef;
      this.operator = args.operator || "";
      this.searchTerms = args?.searchTerms ?? [];
      this._argFilterContainerElm = args.filterContainerElm;
      this._sliderTrackFilledColor = getComputedStyle(document.documentElement).getPropertyValue("--slick-slider-filter-filled-track-color") || DEFAULT_SLIDER_TRACK_FILLED_COLOR;
      this._filterElm = this.createDomFilterElement(this.searchTerms);
    }
    /** Clear the filter value */
    clear(shouldTriggerQuery = true) {
      if (this._filterElm) {
        this._clearFilterTriggered = true;
        this._shouldTriggerQuery = shouldTriggerQuery;
        this.searchTerms = [];
        const lowestValue = +(this.filterOptions?.sliderStartValue ?? Constants.SLIDER_DEFAULT_MIN_VALUE);
        const highestValue = +(this.filterOptions?.sliderEndValue ?? Constants.SLIDER_DEFAULT_MAX_VALUE);
        if (this.sliderType === "double") {
          if (this._sliderLeftInputElm) {
            this._sliderLeftInputElm.value = `${lowestValue}`;
          }
          if (this._sliderRightInputElm) {
            this._sliderRightInputElm.value = `${highestValue}`;
          }
          this._currentValues = [lowestValue, highestValue];
          this.slideLeftInputChanged(new Event("change"), true);
          this.slideRightInputChanged(new Event("change"), true);
        } else {
          if (this._sliderRightInputElm) {
            this._sliderRightInputElm.value = `${lowestValue}`;
          }
          if (this._selectOperatorElm) {
            this._selectOperatorElm.selectedIndex = 0;
          }
          this._currentValue = lowestValue;
          this.slideRightInputChanged(new Event("change"), true);
        }
        const hideSliderNumbers = this.filterOptions?.hideSliderNumber ?? this.filterOptions?.hideSliderNumbers;
        if (!hideSliderNumbers) {
          if (this.sliderType === "double") {
            this.renderSliderValues(lowestValue, highestValue);
          } else {
            this.renderSliderValues(void 0, lowestValue);
          }
        }
        this.updateFilterStyle(false);
        this.callback(void 0, { columnDef: this.columnDef, clearFilterTriggered: true, shouldTriggerQuery, searchTerms: [] });
      }
    }
    /** destroy the filter */
    destroy() {
      this._bindEventService.unbindAll();
      this._sliderTrackElm?.remove();
      this._sliderLeftInputElm?.remove();
      this._sliderRightInputElm?.remove();
    }
    /**
     * Render both slider values (low/high) on screen
     * @param leftValue number
     * @param rightValue number
     */
    renderSliderValues(leftValue, rightValue, triggerTooltipMouseLeave = true) {
      const leftVal = leftValue?.toString() || "";
      const rightVal = rightValue?.toString() || "";
      if (this._leftSliderNumberElm?.textContent) {
        this._leftSliderNumberElm.textContent = leftVal;
      }
      if (this._rightSliderNumberElm?.textContent) {
        this._rightSliderNumberElm.textContent = rightVal;
      }
      this._sliderRangeContainElm.title = this.sliderType === "double" ? `${leftVal} - ${rightVal}` : `${rightVal}`;
      if (triggerTooltipMouseLeave) {
        this.grid.onHeaderRowMouseLeave.notify({ column: this.columnDef, grid: this.grid });
      }
    }
    /** get current slider value(s), it could be a single value or an array of 2 values depending on the slider filter type */
    getValues() {
      return this.sliderType === "double" ? this._currentValues : this._currentValue;
    }
    /**
     * Set value(s) on the DOM element
     * @params searchTerms
     */
    setValues(values, operator, triggerChange = false) {
      if (values) {
        let sliderVals = [];
        const term1 = Array.isArray(values) ? values?.[0] : values;
        if (Array.isArray(values) && values.length === 2) {
          sliderVals = values;
        } else {
          if (typeof term1 === "string" && term1.indexOf("..") > 0) {
            sliderVals = term1.split("..");
            this._currentValue = +(sliderVals?.[0] ?? 0);
          } else if (isDefined(term1) || term1 === "") {
            this._currentValue = term1 === null ? void 0 : +term1;
            sliderVals = [term1];
          }
        }
        if (this.sliderType !== "double" && this._sliderRightInputElm) {
          this._sliderRightInputElm.value = typeof values === "string" ? values : `${term1}`;
          this.renderSliderValues(void 0, this._sliderRightInputElm.value);
        } else if (Array.isArray(sliderVals) && sliderVals.length === 2) {
          if (!this.filterOptions?.hideSliderNumbers) {
            const [lowestValue, highestValue] = sliderVals;
            if (this._sliderLeftInputElm) {
              this._sliderLeftInputElm.value = String(lowestValue ?? Constants.SLIDER_DEFAULT_MIN_VALUE);
            }
            if (this._sliderRightInputElm) {
              this._sliderRightInputElm.value = String(highestValue ?? Constants.SLIDER_DEFAULT_MAX_VALUE);
            }
            this.renderSliderValues(...sliderVals);
          }
        }
      } else {
        this._currentValue = void 0;
        this._currentValues = void 0;
      }
      const val = this.getValues();
      const vals = val === void 0 ? [] : Array.isArray(val) ? val : [val];
      this.updateFilterStyle(vals.length > 0);
      if (operator !== void 0) {
        this.operator = operator;
      }
      if (this.operator && this._selectOperatorElm) {
        const operatorShorthand = mapOperatorToShorthandDesignation(this.operator);
        this._selectOperatorElm.value = operatorShorthand;
      }
      if (triggerChange) {
        this.callback(void 0, {
          columnDef: this.columnDef,
          operator: this.operator,
          searchTerms: vals,
          shouldTriggerQuery: true
        });
      }
    }
    /**
     * Create the Filter DOM element
     * Follows article with few modifications (without tooltip & neither slider track color)
     * https://codingartistweb.com/2021/06/double-range-slider-html-css-javascript/
     * @param searchTerm optional preset search terms
     */
    createDomFilterElement(searchTerms) {
      const columnId = this.columnDef?.id ?? "";
      emptyElement(this._argFilterContainerElm);
      const { minValue, maxValue, step } = this.getSliderConfigs();
      const defaultStartValue = +((Array.isArray(searchTerms) && searchTerms?.[0]) ?? this.filterOptions?.sliderStartValue ?? minValue);
      const defaultEndValue = +((Array.isArray(searchTerms) && searchTerms?.[1]) ?? this.filterOptions?.sliderEndValue ?? maxValue);
      this._sliderRangeContainElm = createDomElement("div", {
        className: `filter-input filter-${columnId} slider-input-container slider-values`,
        title: this.sliderType === "double" ? `${defaultStartValue} - ${defaultEndValue}` : `${defaultStartValue}`
      });
      this._sliderTrackElm = createDomElement("div", { className: "slider-track" });
      if (this.sliderType === "compound") {
        const spanPrependElm = createDomElement("span", { className: "input-group-addon input-group-prepend operator" });
        this._selectOperatorElm = buildSelectOperator(this.getOperatorOptionValues(), this.grid);
        spanPrependElm.appendChild(this._selectOperatorElm);
      }
      if (this.sliderType === "double") {
        this._sliderLeftInputElm = createDomElement("input", {
          type: "range",
          className: `slider-filter-input form-control`,
          ariaLabel: this.columnFilter.ariaLabel ?? `${toSentenceCase(columnId + "")} Search Filter`,
          defaultValue: `${defaultStartValue}`,
          value: `${defaultStartValue}`,
          min: `${minValue}`,
          max: `${maxValue}`,
          step: `${step}`,
          tabIndex: 0
        });
      }
      const rightDefaultVal = this.sliderType === "double" ? defaultEndValue : defaultStartValue;
      this._sliderRightInputElm = createDomElement("input", {
        type: "range",
        className: `slider-filter-input form-control compound-input`,
        ariaLabel: this.columnFilter.ariaLabel ?? `${toSentenceCase(columnId + "")} Search Filter`,
        defaultValue: `${rightDefaultVal}`,
        value: `${rightDefaultVal}`,
        min: `${minValue}`,
        max: `${maxValue}`,
        step: `${step}`,
        tabIndex: 0
      });
      const hideSliderNumbers = this.filterOptions?.hideSliderNumber ?? this.filterOptions?.hideSliderNumbers;
      const sliderNumberClass = hideSliderNumbers ? "" : "input-group";
      this._divContainerFilterElm = createDomElement("div", {
        className: `${sliderNumberClass} search-filter slick-filter slider-container slider-values filter-${columnId}`.trim()
      });
      this._sliderRangeContainElm.appendChild(this._sliderTrackElm);
      if (this.sliderType === "double" && this._sliderLeftInputElm) {
        this._sliderRangeContainElm.appendChild(this._sliderLeftInputElm);
      }
      this._sliderRangeContainElm.appendChild(this._sliderRightInputElm);
      if (hideSliderNumbers) {
        this._divContainerFilterElm.appendChild(this._sliderRangeContainElm);
      } else {
        let leftDivGroupElm;
        if (this.sliderType === "compound" && this._selectOperatorElm) {
          leftDivGroupElm = createDomElement("span", { className: "input-group-addon input-group-prepend operator" });
          leftDivGroupElm.appendChild(this._selectOperatorElm);
        } else if (this.sliderType === "double") {
          leftDivGroupElm = createDomElement("div", { className: `input-group-addon input-group-prepend slider-range-value` });
          this._leftSliderNumberElm = createDomElement("span", {
            className: `input-group-text lowest-range-${columnId}`,
            textContent: `${defaultStartValue}`
          });
          leftDivGroupElm.appendChild(this._leftSliderNumberElm);
        }
        const rightDivGroupElm = createDomElement("div", { className: `input-group-addon input-group-append slider-range-value` });
        this._rightSliderNumberElm = createDomElement("span", { className: `input-group-text highest-range-${columnId}`, textContent: `${rightDefaultVal}` }, rightDivGroupElm);
        if (leftDivGroupElm) {
          this._divContainerFilterElm.appendChild(leftDivGroupElm);
        }
        this._divContainerFilterElm.appendChild(this._sliderRangeContainElm);
        this._divContainerFilterElm.appendChild(rightDivGroupElm);
      }
      this._sliderOptions = { minValue, maxValue, step };
      this._currentValues = [defaultStartValue, defaultEndValue];
      if (Array.isArray(searchTerms) && searchTerms.length > 0 && searchTerms[0] !== "") {
        this.updateFilterStyle(true);
        this._currentValue = defaultStartValue;
      }
      if (this.filterOptions.sliderStartValue !== void 0 || this.columnFilter.minValue !== void 0) {
        this._currentValue = defaultStartValue;
      }
      this._argFilterContainerElm.appendChild(this._divContainerFilterElm);
      this.updateTrackFilledColorWhenEnabled();
      this._bindEventService.bind(this._sliderTrackElm, "click", this.sliderTrackClicked.bind(this));
      this._bindEventService.bind(this._sliderRightInputElm, ["input", "change"], this.slideRightInputChanged.bind(this));
      this._bindEventService.bind(this._sliderRightInputElm, ["change", "mouseup", "touchend"], (e2) => this.onValueChanged(e2));
      this._bindEventService.bind(this._sliderRightInputElm, ["keydown"], (e2) => this.handleKeyDown(e2, "right"));
      if (this.sliderType === "compound" && this._selectOperatorElm) {
        this._bindEventService.bind(this._selectOperatorElm, ["change"], this.onValueChanged.bind(this));
      } else if (this.sliderType === "double" && this._sliderLeftInputElm) {
        this._bindEventService.bind(this._sliderLeftInputElm, ["input", "change"], this.slideLeftInputChanged.bind(this));
        this._bindEventService.bind(this._sliderLeftInputElm, ["change", "mouseup", "touchend"], (e2) => this.onValueChanged(e2));
        this._bindEventService.bind(this._sliderLeftInputElm, ["keydown"], (e2) => this.handleKeyDown(e2, "left"));
      }
      return this._divContainerFilterElm;
    }
    getInputValue(elm) {
      return parseInt(elm?.value ?? "", 10);
    }
    /** Get the available operator option values to populate the operator select dropdown list */
    getOperatorOptionValues() {
      let operatorList;
      if (this.columnFilter.compoundOperatorList) {
        operatorList = this.columnFilter.compoundOperatorList;
      } else {
        operatorList = compoundOperatorNumeric(this.gridOptions, this.translaterService);
      }
      applyOperatorAltTextWhenExists(this.gridOptions, operatorList, "numeric");
      return operatorList;
    }
    /** get default slider defaults */
    getSliderConfigs() {
      return {
        minValue: +(this.columnFilter.minValue ?? Constants.SLIDER_DEFAULT_MIN_VALUE),
        maxValue: +(this.columnFilter.maxValue ?? Constants.SLIDER_DEFAULT_MAX_VALUE),
        step: +(this.columnFilter.valueStep ?? Constants.SLIDER_DEFAULT_STEP)
      };
    }
    /** use keydown event to increase/decrease slider value */
    handleKeyDown(event, side) {
      const e2 = event;
      if (this.filterOptions.useArrowToSlide !== false && (e2.key === "ArrowLeft" || e2.key === "ArrowRight")) {
        const { minValue, maxValue, step } = this.getSliderConfigs();
        const currentValue = Number(side === "left" ? this._sliderLeftInputElm?.value : this._sliderRightInputElm?.value);
        let newValue = e2.key === "ArrowLeft" ? currentValue - step : currentValue + step;
        newValue = newValue < minValue ? minValue : newValue > maxValue ? maxValue : newValue;
        let sliderRightVal = this.getInputValue(this._sliderRightInputElm);
        let sliderLeftVal = 0;
        if (this.sliderType === "compound" || this.sliderType === "single") {
          this._sliderRightInputElm.value = `${newValue}`;
          sliderRightVal = newValue;
        } else if (this.sliderType === "double" && this._sliderLeftInputElm) {
          this._sliderLeftInputElm.value = `${newValue}`;
          sliderLeftVal = newValue;
        }
        this.sliderLeftOrRightChanged(e2, side, sliderLeftVal, sliderRightVal);
        this.onValueChanged(e2);
        e2.stopPropagation();
        e2.preventDefault();
      }
    }
    /** handle value change event triggered, trigger filter callback & update "filled" class name */
    onValueChanged(e2, skipTriggerEvent = false) {
      const sliderRightVal = this.getInputValue(this._sliderRightInputElm);
      let value;
      let searchTerms;
      if (this.sliderType === "compound" || this.sliderType === "single") {
        if (e2?.target?.tagName?.toLowerCase() !== "select") {
          this._currentValue = +sliderRightVal;
        }
        value = this._currentValue;
        searchTerms = [value || "0"];
      } else if (this.sliderType === "double") {
        const sliderLeftVal = this.getInputValue(this._sliderLeftInputElm);
        const values = [sliderLeftVal, sliderRightVal];
        value = values.join("..");
        searchTerms = values;
      }
      if (this._clearFilterTriggered) {
        this.updateFilterStyle(false);
        this.callback(e2, {
          columnDef: this.columnDef,
          clearFilterTriggered: this._clearFilterTriggered,
          searchTerms: [],
          shouldTriggerQuery: this._shouldTriggerQuery
        });
      } else {
        const selectedOperator = this._selectOperatorElm?.value ?? this.operator;
        this.updateFilterStyle(value !== "");
        const skipNullInput = this.columnFilter.skipCompoundOperatorFilterWithNullInput ?? this.gridOptions.skipCompoundOperatorFilterWithNullInput;
        const hasSkipNullValChanged = skipNullInput && isDefined(this._currentValue) || !isDefined(this._currentValue) && isDefined(this._lastSearchValue);
        if (this.sliderType !== "compound" || !skipNullInput || hasSkipNullValChanged) {
          this.callback(e2, {
            columnDef: this.columnDef,
            operator: selectedOperator || "",
            searchTerms,
            shouldTriggerQuery: this._shouldTriggerQuery
          });
        }
      }
      this._clearFilterTriggered = false;
      this._shouldTriggerQuery = true;
      if (!skipTriggerEvent) {
        this.grid.onHeaderRowMouseEnter.notify({ column: this.columnDef, grid: this.grid }, new SlickEventData(e2));
      }
      this._lastSearchValue = value;
    }
    slideLeftInputChanged(e2, skipTriggerEvent = false) {
      const sliderLeftVal = this.getInputValue(this._sliderLeftInputElm);
      const sliderRightVal = this.getInputValue(this._sliderRightInputElm);
      if (this._sliderLeftInputElm && sliderRightVal - sliderLeftVal <= (this.filterOptions?.stopGapBetweenSliderHandles ?? GAP_BETWEEN_SLIDER_HANDLES)) {
        this._sliderLeftInputElm.value = String(sliderLeftVal - (this.filterOptions?.stopGapBetweenSliderHandles ?? GAP_BETWEEN_SLIDER_HANDLES));
      }
      if (this._sliderLeftInputElm && this._sliderRightInputElm) {
        if (+this._sliderLeftInputElm.value >= +this._sliderRightInputElm.value - Z_INDEX_MIN_GAP) {
          this._sliderLeftInputElm.style.zIndex = "1";
          this._sliderRightInputElm.style.zIndex = "0";
        } else {
          this._sliderLeftInputElm.style.zIndex = "0";
          this._sliderRightInputElm.style.zIndex = "1";
        }
      }
      this.sliderLeftOrRightChanged(e2, "left", sliderLeftVal, sliderRightVal, skipTriggerEvent);
    }
    slideRightInputChanged(e2, skipTriggerEvent = false) {
      const sliderLeftVal = this.getInputValue(this._sliderLeftInputElm);
      const sliderRightVal = this.getInputValue(this._sliderRightInputElm);
      if (this.sliderType === "double" && this._sliderRightInputElm && sliderRightVal - sliderLeftVal <= (this.filterOptions?.stopGapBetweenSliderHandles ?? GAP_BETWEEN_SLIDER_HANDLES)) {
        this._sliderRightInputElm.value = String(sliderLeftVal + (this.filterOptions?.stopGapBetweenSliderHandles ?? GAP_BETWEEN_SLIDER_HANDLES));
      }
      this.sliderLeftOrRightChanged(e2, "right", sliderLeftVal, sliderRightVal, skipTriggerEvent);
    }
    sliderLeftOrRightChanged(e2, side, sliderLeftVal, sliderRightVal, skipTriggerEvent = false) {
      let triggerEvent = true;
      this.updateTrackFilledColorWhenEnabled();
      this._sliderRangeContainElm.title = this.sliderType === "double" ? `${sliderLeftVal} - ${sliderRightVal}` : `${sliderRightVal}`;
      if (this.sliderType === "double" && this._sliderLeftInputElm && this._sliderRightInputElm) {
        if (side === "left" && sliderLeftVal > sliderRightVal) {
          this._sliderLeftInputElm.value = `${sliderRightVal}`;
          triggerEvent = false;
        } else if (side === "right" && sliderLeftVal > sliderRightVal) {
          this._sliderRightInputElm.value = `${sliderLeftVal}`;
          triggerEvent = false;
        }
      }
      const hideSliderNumbers = this.filterOptions?.hideSliderNumber ?? this.filterOptions?.hideSliderNumbers;
      if (!hideSliderNumbers) {
        if (this._leftSliderNumberElm?.textContent) {
          this._leftSliderNumberElm.textContent = this._sliderLeftInputElm?.value ?? "";
        }
        if (this._rightSliderNumberElm?.textContent) {
          this._rightSliderNumberElm.textContent = this._sliderRightInputElm?.value ?? "";
        }
      }
      if (this.filterOptions.filterWhileSliding) {
        this.onValueChanged(e2);
      }
      if (triggerEvent && !skipTriggerEvent) {
        this.grid.onHeaderRowMouseEnter.notify({ column: this.columnDef, grid: this.grid }, new SlickEventData(e2));
      }
    }
    sliderTrackClicked(e2) {
      e2.preventDefault();
      const sliderTrackX = e2.offsetX;
      const sliderTrackWidth = this._sliderTrackElm.offsetWidth;
      const trackPercentPosition = (sliderTrackX + 0) * 100 / sliderTrackWidth;
      if (this._sliderRightInputElm && this.sliderType !== "double") {
        this._sliderRightInputElm.value = `${trackPercentPosition}`;
        this._sliderRightInputElm.dispatchEvent(new Event("change"));
      } else {
        if (this._sliderLeftInputElm && this._sliderRightInputElm) {
          if (trackPercentPosition <= 50) {
            this._sliderLeftInputElm.value = `${trackPercentPosition}`;
            this._sliderLeftInputElm.dispatchEvent(new Event("change"));
          } else {
            this._sliderRightInputElm.value = `${trackPercentPosition}`;
            this._sliderRightInputElm.dispatchEvent(new Event("change"));
          }
        }
      }
    }
    updateTrackFilledColorWhenEnabled() {
      if (this.filterOptions?.enableSliderTrackColoring && this._sliderRightInputElm) {
        let percent1 = 0;
        if (this._sliderLeftInputElm) {
          percent1 = (+this._sliderLeftInputElm.value - +this._sliderLeftInputElm.min) / (this.sliderOptions?.maxValue ?? 0 - +this._sliderLeftInputElm.min) * 100;
        }
        const percent2 = (+this._sliderRightInputElm.value - +this._sliderRightInputElm.min) / (this.sliderOptions?.maxValue ?? 0 - +this._sliderRightInputElm.min) * 100;
        const bg = "linear-gradient(to right, %b %p1, %c %p1, %c %p2, %b %p2)".replace(/%b/g, "#eee").replace(/%c/g, this.filterOptions?.sliderTrackFilledColor || this._sliderTrackFilledColor || DEFAULT_SLIDER_TRACK_FILLED_COLOR).replace(/%p1/g, `${percent1}%`).replace(/%p2/g, `${percent2}%`);
        this._sliderTrackElm.style.background = bg;
        this._sliderOptions.sliderTrackBackground = bg;
      }
    }
    /** add/remove "filled" CSS class */
    updateFilterStyle(isFilled) {
      this._divContainerFilterElm.classList.toggle("filled", isFilled);
      this._filterElm?.classList.toggle("filled", isFilled);
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/compoundSliderFilter.js
  var CompoundSliderFilter = class extends SliderFilter {
    /**
     * Initialize the Filter
     */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this.sliderType = "compound";
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/dateRangeFilter.js
  var DateRangeFilter = class extends DateFilter {
    /** Initialize the Filter */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this.inputFilterType = "range";
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/inputMaskFilter.js
  var InputMaskFilter = class extends InputFilter {
    /** Initialize the Filter */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this._inputMask = "";
      this.inputType = "text";
    }
    /** Getter of the input mask, when provided */
    get inputMask() {
      return this._inputMask;
    }
    /**
     * Override the Filter init used by SlickGrid
     */
    init(args) {
      this.grid = args.grid;
      this.callback = args.callback;
      this.columnDef = args.columnDef;
      this.searchTerms = (args.hasOwnProperty("searchTerms") ? args.searchTerms : []) || [];
      this._cellContainerElm = args.filterContainerElm;
      if (this.columnDef?.params?.mask) {
        this._inputMask = this.columnDef.params.mask;
      } else if (this.columnFilter?.params?.mask) {
        this._inputMask = this.columnFilter.params.mask;
      }
      if (!this._inputMask) {
        throw new Error(`[Slickgrid-Universal] The Filters.inputMask requires the mask to be passed in the filter params or the column definition params
        for example:: this.columns: [{ id: 'phone', field: 'phone', filter: { model: Filters.inputMask, params: { mask: '000-000-0000' }}}]`);
      }
      const searchTerm = Array.isArray(this.searchTerms) && this.searchTerms.length >= 0 ? this.searchTerms[0] : "";
      this.createDomFilterElement(searchTerm);
      this._bindEventService.bind(this._filterInputElm, ["keyup", "blur", "change"], this.onTriggerEvent.bind(this));
    }
    /**
     * Event handler to cover the following (keyup, blur, change)
     * We will trigger the Filter Service callback from this handler
     */
    onTriggerEvent(event, isClearFilterEvent = false) {
      let value = "";
      if (event?.target?.value) {
        let targetValue = event?.target?.value ?? "";
        const enableWhiteSpaceTrim = this.gridOptions.enableFilterTrimWhiteSpace || this.columnFilter.enableTrimWhiteSpace;
        if (typeof targetValue === "string" && enableWhiteSpaceTrim) {
          targetValue = targetValue.trim();
        }
        const unmaskedValue = this.unmaskValue(targetValue);
        const maskedValue = this.maskValue(unmaskedValue);
        value = unmaskedValue;
        if (event?.keyCode >= 48) {
          this._filterInputElm.value = maskedValue;
          event.preventDefault();
        }
      }
      if (isClearFilterEvent) {
        this.callback(event, {
          columnDef: this.columnDef,
          clearFilterTriggered: isClearFilterEvent,
          shouldTriggerQuery: this._shouldTriggerQuery
        });
        this._filterInputElm.classList.remove("filled");
      } else {
        this._filterInputElm.classList.add("filled");
        this.callback(event, {
          columnDef: this.columnDef,
          operator: this.operator,
          searchTerms: [value],
          shouldTriggerQuery: this._shouldTriggerQuery
        });
      }
      this._shouldTriggerQuery = true;
    }
    /** From a regular string, we will use the mask to output a new string */
    maskValue(inputValue) {
      let i = 0;
      let maskedValue = "";
      if (this._inputMask) {
        maskedValue = this._inputMask.replace(/[09A]/gi, (match) => {
          if ((match === "0" || match === "9") && /\d+/g.test(inputValue[i]) || // mask is 0 or 9 and value is a digit
          match.toUpperCase() === "A" && /[^\d]+/gi.test(inputValue[i])) {
            return inputValue[i++] || "";
          }
          return "";
        });
      }
      return maskedValue;
    }
    /** From a masked string, we will remove the mask and make a regular string again */
    unmaskValue(maskedValue) {
      const valueWithoutSymbols = maskedValue.replace(/[^0-9a-z]*/gi, "");
      const maskWithoutSymbols = this._inputMask.replace(/[^0-9a-z]*/gi, "");
      let output = "";
      for (let i = 0; i < maskWithoutSymbols.length; i++) {
        if (valueWithoutSymbols[i]) {
          if ((maskWithoutSymbols[i] === "0" || maskWithoutSymbols[i] === "9") && /\d+/g.test(valueWithoutSymbols[i]) || // mask is 0 or 9 and value is a digit
          maskWithoutSymbols[i].toUpperCase() === "A" && /[^\d]+/gi.test(valueWithoutSymbols[i])) {
            output += valueWithoutSymbols[i];
          }
        }
      }
      return output;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/inputNumberFilter.js
  var InputNumberFilter = class extends InputFilter {
    /** Initialize the Filter */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this.inputType = "number";
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/inputPasswordFilter.js
  var InputPasswordFilter = class extends InputFilter {
    /** Initialize the Filter */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this.inputType = "password";
    }
  };

  // node_modules/multiple-select-vanilla/dist/index.js
  var lt = Object.defineProperty;
  var ot = (o, t, e2) => t in o ? lt(o, t, { enumerable: true, configurable: true, writable: true, value: e2 }) : o[t] = e2;
  var r = (o, t, e2) => ot(o, typeof t != "symbol" ? t + "" : t, e2);
  var J = { formatSelectAll() {
    return "[Select all]";
  }, formatAllSelected() {
    return "All selected";
  }, formatCountSelected(o, t) {
    return `${o} of ${t} selected`;
  }, formatNoMatchesFound() {
    return "No matches found";
  }, formatOkButton() {
    return "OK";
  }, formatLazyLoading() {
    return "Loading...";
  }, formatLazyFailed() {
    return "Failed to load data";
  } };
  var nt = 50;
  var rt = 4;
  var d = () => false;
  var Q = () => true;
  var Z = { name: "", placeholder: "", classes: "", classPrefix: "", data: void 0, locale: void 0, selectAll: true, single: void 0, singleRadio: false, multiple: false, hideOptgroupCheckboxes: false, multipleWidth: 80, width: void 0, dropWidth: void 0, maxHeight: 250, maxHeightUnit: "px", position: "bottom", displayValues: false, displayTitle: false, displayDelimiter: ", ", minimumCountSelected: 3, ellipsis: false, isOpen: false, keepOpen: false, openOnHover: false, container: null, filter: false, filterGroup: false, filterPlaceholder: "", filterAcceptOnEnter: false, filterByDataLength: void 0, customFilter(o) {
    let { text: t, label: e2, search: i } = o;
    return (e2 || t || "").includes(i);
  }, showClear: false, autoAdjustDropHeight: false, autoAdjustDropPosition: false, autoAdjustDropWidthByTextSize: false, adjustedHeightPadding: 10, useSelectOptionLabel: false, useSelectOptionLabelToHtml: false, navigationHighlight: true, infiniteScroll: false, virtualScroll: true, cssStyler: () => null, textTemplate: (o) => o.innerHTML.trim(), labelTemplate: (o) => o.label, onBeforeOpen: d, onChange: d, onOpen: d, onBeforeClose: Q, onClose: d, onCheckAll: d, onUncheckAll: d, onFocus: d, onBlur: d, onOptgroupClick: d, onBeforeClick: Q, onClick: d, onFilter: d, onFilterClear: d, onClear: d, onAfterCreate: d, onDestroy: d, onAfterDestroy: d, onDestroyed: d, sanitizer: (o) => {
    if ("setHTML" in Element.prototype) {
      let t = document.createElement("div");
      return t.setHTML(o, { sanitizer: new Sanitizer({ elements: ["i", "span", "div", "p", "b", "strong", "em", "br", "ul", "ol", "li", "a", "img"], attributes: ["class", "title", "alt", "src", "href", "target", "rel", "width", "height", "level"], replaceWithChildrenElements: [] }) }), t.innerHTML;
    }
    return o;
  } };
  var at = ["init", "getOptions", "refreshOptions", "getSelects", "setSelects", "enable", "disable", "open", "close", "check", "uncheck", "checkAll", "uncheckAll", "checkInvert", "focus", "blur", "refresh", "destroy"];
  Object.assign(Z, J);
  var ct = { BLOCK_ROWS: nt, CLUSTER_BLOCKS: rt, DEFAULTS: Z, METHODS: at };
  var f = ct;
  var T = class {
    constructor(t) {
      r(this, "_distinctEvent");
      r(this, "_boundedEvents", []);
      this._distinctEvent = t?.distinctEvent ?? false;
    }
    get boundedEvents() {
      return this._boundedEvents;
    }
    dispose() {
      this.unbindAll(), this._boundedEvents = [];
    }
    bind(t, e2, i, l, s = "") {
      let n = Array.isArray(e2) ? e2 : [e2];
      typeof t?.forEach == "function" ? t.forEach((a) => this.bindElementEvents(a, n, i, l, s)) : this.bindElementEvents(t, n, i, l, s);
    }
    hasBinding(t, e2) {
      return this._boundedEvents.some((i) => i.element === t && (!e2 || i.eventName === e2));
    }
    unbind(t, e2, i) {
      if (t) {
        let l = Array.isArray(t) ? t : [t], s = Array.isArray(e2) ? e2 : [e2 || ""];
        for (let n of l) {
          i || (i = this._boundedEvents.find((a) => {
            if (a.element === n && (!e2 || a.eventName === e2)) return a.listener;
          }));
          for (let a of s) n?.removeEventListener?.(a, i);
        }
      }
    }
    unbindAll(t) {
      if (t) {
        let e2 = Array.isArray(t) ? t : [t];
        for (let i = this._boundedEvents.length - 1; i >= 0; --i) {
          let l = this._boundedEvents[i];
          if (e2.some((s) => s === l.groupName)) {
            let { element: s, eventName: n, listener: a } = l;
            this.unbind(s, n, a), this._boundedEvents.splice(i, 1);
          }
        }
      } else for (; this._boundedEvents.length > 0; ) {
        let { element: e2, eventName: i, listener: l } = this._boundedEvents.pop();
        this.unbind(e2, i, l);
      }
    }
    bindElementEvents(t, e2, i, l, s = "") {
      for (let n of e2) (!this._distinctEvent || this._distinctEvent && !this.hasBinding(t, n)) && (t.addEventListener(n, i, l), this._boundedEvents.push({ element: t, eventName: n, listener: i, groupName: s }));
    }
  };
  function j(o, t, e2 = false) {
    let i = Object.keys(o), l = Object.keys(t);
    if (e2 && i.length !== l.length) return false;
    for (let s of i) if (l.includes(s) && o[s] !== t[s]) return false;
    return true;
  }
  function O(o) {
    return typeof o != "object" || o === null ? o : Array.isArray(o) ? o.map(O) : typeof o == "function" ? o : Object.fromEntries(Object.entries(o).map(([t, e2]) => [t, O(e2)]));
  }
  function v(o) {
    return o != null && o !== "";
  }
  function W(o, t) {
    return typeof o == "object" ? Object.fromEntries(t ? Object.entries(o).filter(([e2, i]) => !v(i) && !t.includes(e2) || v(i)) : Object.entries(o).filter(([e2, i]) => v(i))) : o;
  }
  function G(o) {
    let t = 0;
    return o.forEach((e2, i) => {
      e2.type === "optgroup" ? (e2._key = `group_${i}`, e2.visible = typeof e2.visible > "u" ? true : e2.visible, e2.children.forEach((l, s) => {
        l && (l.visible = typeof l?.visible > "u" ? true : l.visible, l.divider || (l._key = `option_${i}_${s}`, t += 1));
      })) : (e2.visible = typeof e2.visible > "u" ? true : e2.visible, e2.divider || (e2._key = `option_${i}`, t += 1));
    }), t;
  }
  function S(o, t, e2) {
    if (Array.isArray(o)) for (let i of o) {
      if (i[t] === e2 || i[t] === `${+i[t]}` && +i[t] === e2) return i;
      if (i.type === "optgroup") {
        for (let l of i.children) if (l && (l[t] === e2 || l[t] === `${+l[t]}` && +l[t] === e2)) return l;
      }
    }
  }
  function k(o) {
    return Object.keys(o).forEach((t) => v(o[t]) ? "" : delete o[t]), o;
  }
  function g(o, t) {
    if (typeof o != "string") return o;
    if (typeof t == "function") return t(o);
    if (typeof o.normalize == "function") return o.normalize("NFD").replace(/[\u0300-\u036F]/g, "");
    throw new Error("[Multiple-Select-Vanilla] `normalize()` function is not defined, you can optionally provide a custom parser via the `diacriticParser` option.");
  }
  function x(o) {
    let { innerHeight: t = 0, innerWidth: e2 = 0 } = window, { top: i, left: l } = it(), { top: s = 0, left: n = 0 } = C(o) || {};
    return { top: s - i, bottom: t - (s - i), left: n - l, right: e2 - (n - l) };
  }
  function M(o = "") {
    return o.split(" ").filter((t) => t);
  }
  function h(o, t, e2) {
    let i = document.createElement(o);
    return t && Object.keys(t).forEach((l) => {
      let s = t[l];
      typeof s == "object" ? Object.assign(i[l], s) : i[l] = t[l];
    }), e2?.appendChild(i), i;
  }
  function z(o, t, e2) {
    let i = o.props?.innerHTML ? et(o.props, "innerHTML") : o.props, l = h(o.tagName, W(i, ["className", "title", "style"]), t), s = e2;
    if (s || (s = l), o.props.innerHTML && (l.innerHTML = o.props.innerHTML), o.attrs) for (let n of Object.keys(o.attrs)) l.setAttribute(n, o.attrs[n]);
    if (o.children) for (let n of o.children) z(n, l, s);
    return t?.appendChild(l), l;
  }
  function L(o) {
    return o.hasOwnProperty("tagName") ? z(o) : document.createElement("li");
  }
  function y(o) {
    for (; o?.firstChild; ) o.lastChild && o.removeChild(o.lastChild);
    return o;
  }
  function C(o) {
    if (o) {
      let { top: t, left: e2, bottom: i, right: l } = o.getBoundingClientRect();
      return { top: t + window.pageYOffset, left: e2 + window.pageXOffset, bottom: i, right: l };
    }
  }
  function D(o, t, e2) {
    if (!o) return 0;
    let i = Number.parseFloat(o.style[e2]);
    if (!i || Number.isNaN(i)) {
      let l = e2 === "height" ? "Height" : "Width";
      switch (t) {
        case "outer":
          i = o[`offset${l}`];
          break;
        case "scroll":
          i = o[`scroll${l}`];
          break;
        default:
          i = o[`client${l}`];
          break;
      }
      i = o.getBoundingClientRect()[e2];
    }
    if (!i || Number.isNaN(i)) {
      let l = o.style.display, s = o.style.position;
      o.style.display = "block", o.style.position = "absolute", i = H(o, e2), o.style.display = l, o.style.position = s;
    }
    return i || 0;
  }
  function H(o, t) {
    let e2 = window.getComputedStyle(o)[t], i = Number.parseFloat(e2);
    return Number.isNaN(i) && (i = 0), i;
  }
  function R(o, t) {
    let e2 = null, i = o?.parentElement;
    for (; i; ) {
      let [l, s, n, a] = t.match(/^([a-z]*)([#.]{1})([a-z-]+)$/i) || [];
      if (n && a) for (let c of a.replace(n, "").split(" ")) i.classList.contains(c) && (s ? i?.tagName.toLowerCase() === s && (e2 = i) : e2 = i);
      i = i.parentElement;
    }
    return e2;
  }
  function tt(o) {
    if (v(o)) {
      let t = document.createElement("textarea");
      return t.innerHTML = o.toString().replace(/&#(\d+);/g, (e2, i) => String.fromCharCode(i)), t.value;
    }
    return "";
  }
  function q(o, t) {
    o.parentNode?.insertBefore(t, o.nextSibling);
  }
  function et(o, t) {
    let { [t]: e2, ...i } = o;
    return i;
  }
  function P(o, t) {
    o?.style && (o.style.display = o.style.display === "none" && t !== false || t === true ? "block" : "none");
  }
  function it() {
    return { left: window.pageXOffset || document.documentElement.scrollLeft || 0, top: window.pageYOffset || document.documentElement.scrollTop || 0 };
  }
  var A = class {
    constructor(t) {
      r(this, "clusterRows");
      r(this, "cache");
      r(this, "scrollEl");
      r(this, "blockHeight");
      r(this, "clusterHeight");
      r(this, "contentEl");
      r(this, "parentEl");
      r(this, "itemHeight");
      r(this, "lastCluster");
      r(this, "scrollTop");
      r(this, "dataStart");
      r(this, "dataEnd");
      r(this, "rows");
      r(this, "destroy");
      r(this, "callback");
      r(this, "sanitizer");
      this.rows = t.rows, this.scrollEl = t.scrollEl, this.contentEl = t.contentEl, this.parentEl = t.contentEl?.parentElement, this.callback = t.callback, this.cache = {}, this.scrollTop = this.scrollEl.scrollTop, this.initDOM(this.rows), this.scrollEl.scrollTop = this.scrollTop, this.lastCluster = 0;
      let e2 = () => {
        this.lastCluster !== (this.lastCluster = this.getNum()) && (this.initDOM(this.rows), this.callback());
      };
      this.scrollEl.addEventListener("scroll", e2, false), this.destroy = () => {
        this.scrollEl.removeEventListener("scroll", e2, false), y(this.contentEl);
      };
    }
    reset(t) {
      this.lastCluster = 0, this.cache = {}, y(this.contentEl), this.initDOM(t);
    }
    initDOM(t) {
      if (typeof this.clusterHeight > "u") {
        this.cache.scrollTop = this.scrollEl.scrollTop;
        let n = L(t[0]);
        this.contentEl.appendChild(n), this.contentEl.appendChild(n), this.contentEl.appendChild(n), this.cache.data = [t[0]], this.getRowsHeight();
      }
      let e2 = this.initData(t, this.getNum()), i = this.checkChanges("data", e2.rows), l = this.checkChanges("top", e2.topOffset), s = this.checkChanges("bottom", e2.bottomOffset);
      y(this.contentEl), i && l ? (e2.topOffset && this.contentEl.appendChild(this.getExtra("top", e2.topOffset)), e2.rows.forEach((n) => this.contentEl.appendChild(L(n))), e2.bottomOffset && this.contentEl.appendChild(this.getExtra("bottom", e2.bottomOffset))) : s && this.contentEl.lastChild && (this.contentEl.lastChild.style.height = `${e2.bottomOffset}px`);
    }
    getRowsHeight() {
      if (typeof this.itemHeight > "u") {
        let t = this.parentEl?.style.display || "";
        this.parentEl && (t === "" || t === "none") && (this.parentEl.style.display = "block");
        let e2 = this.contentEl.children, i = e2[Math.floor(e2.length / 2)];
        this.itemHeight = i.offsetHeight, this.parentEl && (this.parentEl.style.display = t);
      }
      this.blockHeight = this.itemHeight * f.BLOCK_ROWS, this.clusterRows = f.BLOCK_ROWS * f.CLUSTER_BLOCKS, this.clusterHeight = this.blockHeight * f.CLUSTER_BLOCKS;
    }
    getNum() {
      this.scrollTop = this.scrollEl.scrollTop;
      let t = (this.clusterHeight || 0) - (this.blockHeight || 0);
      return t && Math.floor(this.scrollTop / t) || 0;
    }
    initData(t, e2) {
      if (t.length < f.BLOCK_ROWS) return { topOffset: 0, bottomOffset: 0, rowsAbove: 0, rows: t };
      let i = Math.max((this.clusterRows - f.BLOCK_ROWS) * e2, 0), l = i + this.clusterRows, s = Math.max(i * this.itemHeight, 0), n = Math.max((t.length - l) * this.itemHeight, 0), a = [], c = i;
      s < 1 && c++;
      for (let p = i; p < l; p++) t[p] && a.push(t[p]);
      return this.dataStart = i, this.dataEnd = l, { topOffset: s, bottomOffset: n, rowsAbove: c, rows: a };
    }
    checkChanges(t, e2) {
      let i = e2 !== this.cache[t];
      return this.cache[t] = e2, i;
    }
    getExtra(t, e2) {
      let i = h("li", { className: `virtual-scroll-${t}` });
      return e2 && (i.style.height = `${e2}px`), i;
    }
  };
  var w = ".ms-select-all, ul li[data-key]";
  var ht = ".ms-select-all.highlighted, ul li[data-key].highlighted";
  var _ = class {
    constructor(t, e2) {
      r(this, "elm", t);
      r(this, "_bindEventService");
      r(this, "_isOpen", false);
      r(this, "isAllSelected", false);
      r(this, "isPartiallyAllSelected", false);
      r(this, "fromHtml", false);
      r(this, "choiceElm");
      r(this, "selectClearElm");
      r(this, "closeElm");
      r(this, "clearSearchIconElm");
      r(this, "filterText", "");
      r(this, "updateData", []);
      r(this, "data", []);
      r(this, "dataTotal");
      r(this, "dropElm");
      r(this, "okButtonElm");
      r(this, "filterParentElm");
      r(this, "lastFocusedItemKey", "");
      r(this, "lastMouseOverPosition", "");
      r(this, "ulElm");
      r(this, "parentElm");
      r(this, "labelElm");
      r(this, "selectAllParentElm");
      r(this, "selectAllElm");
      r(this, "searchInputElm");
      r(this, "selectGroupElms");
      r(this, "selectCheckboxElms");
      r(this, "noResultsElm");
      r(this, "options");
      r(this, "selectAllName", "");
      r(this, "selectGroupName", "");
      r(this, "selectItemName", "");
      r(this, "scrolledByMouse", false);
      r(this, "openDelayTimer");
      r(this, "updateDataStart");
      r(this, "updateDataEnd");
      r(this, "virtualScroll");
      r(this, "_currentHighlightIndex", -1);
      r(this, "_currentSelectedElm");
      r(this, "_isLazyLoaded", false);
      r(this, "isMoveUpRecalcRequired", false);
      r(this, "locales", {});
      this.options = Object.assign({}, f.DEFAULTS, this.elm.dataset, e2), this._bindEventService = new T({ distinctEvent: true });
    }
    get isRenderAsHtml() {
      return this.options.renderOptionLabelAsHtml || this.options.useSelectOptionLabelToHtml;
    }
    init() {
      this.initLocale(), this.initContainer(), this.initData(), this.initSelected(true), this.initFilter(), this.initDrop(), this.initView(), this.options.onAfterCreate();
    }
    destroy(t = true) {
      this.elm && this.parentElm && (this.options.onDestroy({ hardDestroy: t }), t && this.options.onHardDestroy(), this.elm.parentElement && this.parentElm.parentElement && this.elm.parentElement.insertBefore(this.elm, this.parentElm.parentElement.firstChild), this.elm.classList.remove("ms-offscreen"), this._bindEventService.unbindAll(), this.virtualScroll?.destroy(), this.dropElm?.remove(), this.dropElm = void 0, this.parentElm.parentNode?.removeChild(this.parentElm), this.fromHtml && (delete this.options.data, this.fromHtml = false), this.options.onAfterDestroy({ hardDestroy: t }), t && (this.options.onAfterHardDestroy?.(), Object.keys(this.options).forEach((e2) => delete this[e2])));
    }
    initLocale() {
      if (this.options.locale) {
        if (typeof this.options.locale == "object") {
          Object.assign(this.options, this.options.locale);
          return;
        }
        let t = this.options.locales || this.locales || {}, e2 = this.options.locale.split(/-|_/);
        if (e2[0] = e2[0].toLowerCase(), e2[1] && (e2[1] = e2[1].toUpperCase()), t[this.options.locale]) Object.assign(this.options, t[this.options.locale]);
        else if (t[e2.join("-")]) Object.assign(this.options, t[e2.join("-")]);
        else if (t[e2[0]]) Object.assign(this.options, t[e2[0]]);
        else throw new Error(`[multiple-select-vanilla] invalid locales "${this.options.locale}", make sure to import it before using it`);
      }
    }
    initContainer() {
      let t = this.elm.getAttribute("name") || this.options.name || "";
      this.options.classes && this.elm.classList.add(this.options.classes), this.options.classPrefix && (this.elm.classList.add(this.options.classPrefix), this.options.size && this.elm.classList.add(`${this.options.classPrefix}-${this.options.size}`)), this.elm.style.display = "none", this.labelElm = this.elm.closest("label"), !this.labelElm && this.elm.id && (this.labelElm = h("label", { htmlFor: this.elm.id })), this.labelElm?.querySelector("input") && (this.labelElm = null), this.options.single === void 0 && (this.options.single = !this.elm.multiple), this.parentElm = h("div", { className: M(`ms-parent ${this.elm.className || ""} ${this.options.classes}`).join(" "), dataset: { test: "sel" } }), this.options.darkMode && this.parentElm.classList.add("ms-dark-mode");
      let e2 = this.elm.getAttribute("title") || "";
      e2 && (this.parentElm.title = e2), this.options.placeholder = this.options.placeholder || this.elm.getAttribute("placeholder") || "", this.choiceElm = h("button", { className: "ms-choice", type: "button" }, this.parentElm), this.options.labelId && (this.choiceElm.id = this.options.labelId, this.choiceElm.setAttribute("aria-labelledby", this.options.labelId)), this.options.tabIndex !== void 0 && (this.choiceElm.tabIndex = this.options.tabIndex), this.choiceElm.appendChild(h("span", { className: "ms-placeholder", textContent: this.options.placeholder })), this.options.showClear && (this.selectClearElm = h("div", { className: "ms-icon ms-icon-close" }), this.selectClearElm.style.display = "none", this.choiceElm.appendChild(this.selectClearElm)), this.choiceElm.appendChild(h("div", { className: "ms-icon ms-icon-caret" })), this.dropElm = h("div", { className: `ms-drop ${this.options.position}`, ariaExpanded: "false" }, this.parentElm), this.options.darkMode && this.dropElm.classList.add("ms-dark-mode"), t && (this.dropElm.dataset.name = t);
      let i = this.elm.getAttribute("data-test") || this.options.dataTest;
      i && (this.parentElm.dataset.test = i, this.dropElm.dataset.test = i), this.closeElm = this.choiceElm.querySelector(".ms-icon-close");
      let l = ["minWidth", "maxWidth", "width", "dropWidth"];
      for (let s of l) if (this.options[s]) {
        let n = s === "dropWidth" ? "width" : s;
        this.dropElm.style[n] = typeof this.options[s] == "string" ? this.options[s] : `${this.options[s]}px`;
      }
      q(this.elm, this.parentElm), this.elm.disabled && (this.choiceElm.classList.add("disabled"), this.choiceElm.disabled = true), this.selectAllName = `selectAll${t}`, this.selectGroupName = `selectGroup${t}`, this.selectItemName = `selectItem${t}`, this.options.keepOpen || (this._bindEventService.unbindAll("body-click"), this._bindEventService.bind(document.body, "click", ((s) => {
        if (this.getEventTarget(s) === this.choiceElm || R(this.getEventTarget(s), ".ms-choice") === this.choiceElm) return;
        let n = this.getEventTarget(s), a = R(this.getEventTarget(s), ".ms-drop");
        this._isOpen && (n === this.dropElm || a !== this.dropElm && n !== this.elm) && this.close("body.click");
      }), void 0, "body-click"));
    }
    initData() {
      let t = [];
      if (this.options.data) {
        if (Array.isArray(this.options.data)) this.data = this.options.data.map((e2) => typeof e2 == "string" || typeof e2 == "number" ? { text: e2, value: e2 } : e2);
        else if (typeof this.options.data == "object") {
          for (let [e2, i] of Object.entries(this.options.data)) t.push({ value: e2, text: `${i}` });
          this.data = t;
        }
      } else this.elm.childNodes.forEach((e2) => {
        let i = this.initRow(e2);
        i && t.push(i);
      }), this.options.data = t, this.data = t, this.fromHtml = true;
      this.data && this.options.preFilter && (this.data = this.data.filter(this.options.preFilter)), this.data && this.options.preSort && (this.data = this.data.sort(this.options.preSort)), this.dataTotal = G(this.data || []);
    }
    initRow(t, e2) {
      let i = {};
      return t.tagName?.toLowerCase() === "option" ? (i.type = "option", i.text = this.options.textTemplate(t), i.value = t.value, i.visible = true, i.selected = !!t.selected, i.disabled = e2 || t.disabled, i.classes = t.getAttribute("class") || "", i.title = t.getAttribute("title") || "", t.dataset.value && (i._value = t.dataset.value), Object.keys(t.dataset).length && (i._data = t.dataset, i._data.divider && (i.divider = i._data.divider)), i) : t.tagName?.toLowerCase() === "optgroup" ? (i.type = "optgroup", i.label = this.options.labelTemplate(t), i.visible = true, i.selected = !!t.selected, i.disabled = t.disabled, i.children = [], Object.keys(t.dataset).length && (i._data = t.dataset), t.childNodes.forEach((l) => {
        i.children.push(this.initRow(l, i.disabled));
      }), i) : null;
    }
    initDrop() {
      this.initList(!this.options.lazyData), this.update(true), this.options.isOpen && this.open(0), this.options.openOnHover && this.parentElm && (this._bindEventService.bind(this.parentElm, "mouseover", () => this.open(null)), this._bindEventService.bind(this.parentElm, "mouseout", () => this.close("hover.mouseout")));
    }
    initFilter() {
      if (this.filterText = "", this.options.filter || !this.options.filterByDataLength) return;
      let t = 0;
      for (let e2 of this.data || []) e2.type === "optgroup" ? t += e2.children.length : t += 1;
      this.options.filter = t > this.options.filterByDataLength;
    }
    initList(t = true) {
      if (t && (this.options.filter && (this.filterParentElm = h("div", { className: "ms-search" }, this.dropElm), this.filterParentElm.appendChild(h("input", { autocomplete: "off", autocapitalize: "off", spellcheck: false, type: "text", placeholder: this.options.filterPlaceholder || "\u{1F50E}\uFE0E" })), this.options.showSearchClear && this.filterParentElm.appendChild(h("span", { className: "ms-icon ms-icon-close" }))), this.options.selectAll && !this.options.single)) {
        let e2 = this.elm.getAttribute("name") || this.options.name || "";
        this.selectAllParentElm = h("div", { className: "ms-select-all", dataset: { key: "select_all" } });
        let i = document.createElement("label"), s = `ms-icon ${this.isAllSelected ? "ms-icon-check" : `ms-icon-${this.isPartiallyAllSelected ? "partial-all" : "uncheck"}`}`, n = h("div", { className: "icon-checkbox-container" }, i);
        h("input", { type: "checkbox", ariaChecked: String(this.isAllSelected), checked: this.isAllSelected, dataset: { name: `selectAll${e2}` } }, n), h("div", { className: s }, n), i.appendChild(h("span", { textContent: this.formatSelectAll() })), this.selectAllParentElm.appendChild(i), this.dropElm?.appendChild(this.selectAllParentElm);
      }
      this.ulElm = h("ul", { className: "ms-list", role: "listbox", ariaExpanded: "false", ariaMultiSelectable: String(!this.options.single) }), this.dropElm?.appendChild(this.ulElm), t && this.options.showOkButton && !this.options.single && (this.okButtonElm = h("button", { className: "ms-ok-button", type: "button", textContent: this.formatOkButton() }, this.dropElm)), this.initListItems();
    }
    initListItems() {
      let t = 0, e2 = this.getListRows();
      if (this.options.selectAll && !this.options.single && (t = -1), this.options.virtualScroll && e2.length > f.BLOCK_ROWS * f.CLUSTER_BLOCKS) {
        let i = this.dropElm && this.dropElm?.style.display !== "none";
        !i && this.dropElm && (this.dropElm.style.left = "-10000", this.dropElm.style.display = "block", this.dropElm.ariaExpanded = "true");
        let l = () => {
          if (this.virtualScroll) {
            this._currentHighlightIndex = 0, this.updateDataStart = this.virtualScroll.dataStart + t, this.updateDataEnd = this.virtualScroll.dataEnd + t, this.updateDataStart < 0 && (this.updateDataStart = 0, this._currentHighlightIndex = 0);
            let s = this.getDataLength();
            this.updateDataEnd > s && (this.updateDataEnd = s), this.ulElm && (this.isMoveUpRecalcRequired ? this.recalculateArrowMove("up") : this.virtualScroll.dataStart > this.updateDataStart && this.recalculateArrowMove("down"));
          }
        };
        this.ulElm && (this.virtualScroll ? this.virtualScroll.reset(e2) : this.virtualScroll = new A({ rows: e2, scrollEl: this.ulElm, contentEl: this.ulElm, sanitizer: this.options.sanitizer, callback: () => {
          l(), this.events();
        } })), l(), !i && this.dropElm && (this.dropElm.style.left = "0", this.dropElm.style.display = "none", this.dropElm.ariaExpanded = "false");
      } else this.ulElm && (y(this.ulElm), e2.forEach((i) => this.ulElm.appendChild(L(i)))), this.updateDataStart = 0, this.updateDataEnd = this.updateData.length;
      return this.events(), e2;
    }
    getEventTarget(t) {
      try {
        return t.composedPath?.()[0] || t.path?.[0] || t.target;
      } catch {
        return t.target;
      }
    }
    getListRows() {
      let t = [];
      return this.updateData = [], this.data?.forEach((e2) => t.push(...this.initListItem(e2))), this.options.infiniteScroll && t.push({ tagName: "li", props: { className: "ms-infinite-option", role: "option" } }), t.push({ tagName: "li", props: { className: "ms-no-results", textContent: this.formatNoMatchesFound() } }), t;
    }
    initListItem(t, e2 = 0) {
      let i = t?.title || "", l = this.options.multiple ? "multiple" : "", s = this.options.single ? "radio" : "checkbox", n = !!t?.selected, a = this.options.single && !this.options.singleRadio, c = "";
      if (!t?.visible) return [];
      if (this.updateData.push(t), a && (c = "hide-radio "), t.selected && (c += "selected "), t.type === "optgroup") {
        let $ = [], I;
        if (this.options.hideOptgroupCheckboxes || this.options.single) I = { tagName: "span", props: { dataset: { name: this.selectGroupName, key: t._key } } };
        else {
          let N = { tagName: "input", props: { type: "checkbox", dataset: { name: this.selectGroupName, key: t._key }, checked: n, disabled: t.disabled } };
          if (a) I = N;
          else {
            let Y = false;
            "children" in t && t.children.some((st) => st?.selected) && (Y = true), I = { tagName: "div", props: { className: `icon-checkbox-container${s === "radio" ? " radio" : ""}` }, children: [N, { tagName: "div", props: { className: `ms-icon ${n ? s === "radio" ? "ms-icon-radio" : "ms-icon-check" : `ms-icon-${Y ? "partial-group" : "uncheck"}`}` } }] };
          }
        }
        !c.includes("hide-radio") && (this.options.hideOptgroupCheckboxes || this.options.single) && (c += "hide-radio ");
        let U = { tagName: "span", props: {} };
        this.applyAsTextOrHtmlWhenEnabled(U.props, t.label);
        let V = { tagName: "li", props: { className: M(`group${this.options.single || t.disabled ? " disabled" : ""} ${c}`).join(" "), role: "option", ariaSelected: String(n), dataset: { key: t._key } }, children: [{ tagName: "label", props: { className: M(`optgroup${this.options.single || t.disabled ? " disabled" : ""}`).join(" ") }, children: [I, U] }] }, X = this.options.cssStyler(t);
        return X && (V.props.style = X), $.push(V), t.children.forEach((N) => $.push(...this.initListItem(N, 1))), $;
      }
      if (c += t.classes || "", e2 && this.options.single && (c += `option-level-${e2} `), t.divider) return [{ tagName: "li", props: { className: "option-divider" } }];
      let p = l || c ? (l + c).trim() : "";
      t.disabled && (p += " disabled");
      let u = `${t.disabled ? "disabled" : ""}`, m = { tagName: "span", props: {} };
      this.applyAsTextOrHtmlWhenEnabled(m.props, t.text);
      let b = { tagName: "input", props: { type: s, value: encodeURI(t.value), dataset: { key: t._key, name: this.selectItemName }, checked: n, disabled: !!t.disabled } };
      t.selected && (b.attrs = { checked: "checked" });
      let B = { tagName: "div", props: { className: `icon-checkbox-container${s === "radio" ? " radio" : ""}` }, children: [b, { tagName: "div", props: { className: `ms-icon ${b.props.checked ? s === "radio" ? "ms-icon-radio" : "ms-icon-check" : "ms-icon-uncheck"}` } }] }, E = { tagName: "li", props: { role: "option", title: i, ariaSelected: String(n), dataset: { key: t._key } }, children: [{ tagName: "label", props: { className: u }, children: [a ? b : B, m] }] };
      p && (E.props.className = p);
      let K = this.options.cssStyler(t);
      return K && (E.props.style = K), [E];
    }
    initSelected(t = false) {
      let e2 = 0;
      for (let i of this.data || []) if (i.type === "optgroup") {
        let l = i.children.filter((s) => s?.selected && !s.disabled && s.visible).length;
        i.children.length && (i.selected = !this.options.single && l && l === i.children.filter((s) => s && !s.disabled && s.visible && !s.divider).length), e2 += l;
      } else e2 += i.selected && !i.disabled && i.visible ? 1 : 0;
      if (this.isAllSelected = this.data?.filter((i) => i.selected && !i.disabled && i.visible).length === this.data?.filter((i) => !i.disabled && i.visible && !i.divider).length, this.isPartiallyAllSelected = !this.isAllSelected && e2 > 0, !t) {
        let i = "";
        this.isAllSelected ? i = "onCheckAll" : e2 === 0 && (i = "onUncheckAll"), i && (this.options[i](), this.handleOnChange(i));
      }
    }
    initView() {
      let t;
      window.getComputedStyle ? (t = window.getComputedStyle(this.elm).width, t === "auto" && (t = D(this.dropElm, "outer", "width") + 20)) : t = D(this.elm, "outer", "width") + 20, this.parentElm.style.width = `${this.options.width || t}px`, this.elm.classList.add("ms-offscreen");
    }
    events() {
      this._bindEventService.unbindAll(["ok-button", "search-input", "select-all-checkbox", "input-checkbox-list", "group-checkbox-list", "hover-highlight", "arrow-highlight", "option-list-scroll"]), this.clearSearchIconElm = this.filterParentElm?.querySelector(".ms-icon-close"), this.searchInputElm = this.dropElm?.querySelector(".ms-search input"), this.selectAllElm = this.dropElm?.querySelector(`input[data-name="${this.selectAllName}"]`), this.selectGroupElms = this.dropElm?.querySelectorAll(`input[data-name="${this.selectGroupName}"],span[data-name="${this.selectGroupName}"]`), this.selectCheckboxElms = this.dropElm?.querySelectorAll(`input[data-name="${this.selectItemName}"]:enabled`), this.noResultsElm = this.dropElm?.querySelector(".ms-no-results");
      let t = (e2) => {
        e2.preventDefault(), !this.getEventTarget(e2).classList.contains("ms-icon-close") && (this.options.isOpen ? this.close("toggle.close") : this.open());
      };
      this.labelElm && this._bindEventService.bind(this.labelElm, "click", ((e2) => {
        this.getEventTarget(e2).nodeName.toLowerCase() === "label" && (t(e2), (!this.options.filter || !this.options.isOpen) && this.focus(), e2.stopPropagation());
      })), this._bindEventService.bind(this.choiceElm, "click", t), this.options.onFocus && this._bindEventService.bind(this.choiceElm, "focus", this.options.onFocus), this.options.onBlur && this._bindEventService.bind(this.choiceElm, "blur", this.options.onBlur), this._bindEventService.bind(this.parentElm, "keydown", ((e2) => {
        e2.code === "Escape" && this.handleEscapeKey();
      })), this.closeElm && this._bindEventService.bind(this.closeElm, "click", ((e2) => {
        e2.preventDefault(), this._checkAll(false, true), this.initSelected(false), this.updateSelected(), this.update(), this.options.onClear();
      })), this.clearSearchIconElm && this._bindEventService.bind(this.clearSearchIconElm, "click", ((e2) => {
        e2.preventDefault(), this.searchInputElm && (this.searchInputElm.value = "", this.searchInputElm.focus()), this._currentHighlightIndex = -1, this.moveHighlightDown(), this.filter(), this.options.onFilterClear();
      })), this.searchInputElm && (this._bindEventService.bind(this.searchInputElm, "keydown", ((e2) => {
        e2.code === "Tab" && e2.shiftKey && this.close("key.shift+tab");
      }), void 0, "search-input"), this._bindEventService.bind(this.searchInputElm, "keyup", ((e2) => {
        if (this.options.filterAcceptOnEnter && ["Enter", "Space"].includes(e2.code) && this.searchInputElm?.value) {
          if (this.options.single) {
            let i = [];
            this.selectCheckboxElms?.forEach((l) => {
              l.closest("li")?.style.display !== "none" && i.push(l);
            }), i.length && i[0].hasAttribute("data-name") && this.setSelects([i[0].value]);
          } else this.selectAllElm?.click();
          this.close(`key.${e2.code.toLowerCase()}`), this.focus();
          return;
        }
        this.filter();
      }), void 0, "search-input")), this.selectAllElm && this._bindEventService.bind(this.selectAllElm, "click", ((e2) => this._checkAll(e2.currentTarget?.checked)), void 0, "select-all-checkbox"), this.okButtonElm && this._bindEventService.bind(this.okButtonElm, "click", ((e2) => {
        t(e2), e2.stopPropagation();
      }), void 0, "ok-button"), this.selectGroupElms && this._bindEventService.bind(this.selectGroupElms, "click", ((e2) => {
        let i = e2.currentTarget, l = i.checked, s = S(this.data, "_key", i.dataset.key);
        this._checkGroup(s, l), this.options.onOptgroupClick(k({ label: s.label, selected: !!s.selected, data: s._data, children: s.children.filter((n) => n).map((n) => k({ label: n.text, value: n.value, selected: n.selected, disabled: n.disabled, data: n._data })) })), this.handleOnChange("onOptgroupClick", { label: s.label, selected: !!s.selected, type: s.type });
      }), void 0, "group-checkbox-list"), this.selectCheckboxElms && this._bindEventService.bind(this.selectCheckboxElms, "click", ((e2) => {
        let i = e2.currentTarget, l = i.checked, s = S(this.data, "_key", i.dataset.key), n = () => {
          this.options.single && this.options.isOpen && !this.options.keepOpen && this.close("selection");
        };
        if (this.options.onBeforeClick(s) === false) {
          n();
          return;
        }
        this._check(s, l), this.options.onClick(k({ label: s.text, value: s.value, selected: s.selected, data: s._data })), this.handleOnChange("onClick", { label: s.text, value: s.value, selected: s.selected, type: s.type }), n();
      }), void 0, "input-checkbox-list"), this.lastFocusedItemKey && this.dropElm && this.dropElm.querySelector(`li[data-key=${this.lastFocusedItemKey}]`)?.focus(), this.options.navigationHighlight && this.dropElm && (this._bindEventService.bind(this.dropElm, "mouseover", ((e2) => {
        let i = this.getEventTarget(e2).closest(".ms-select-all") || this.getEventTarget(e2).closest("li");
        if (this.dropElm?.contains(i) && this.lastMouseOverPosition !== `${e2.clientX}:${e2.clientY}`) {
          let l = this.dropElm?.querySelectorAll(w) || [], s = Array.from(l).findIndex((n) => n.dataset.key === i.dataset.key);
          this._currentHighlightIndex !== s && !i.classList.contains("disabled") && (this._currentSelectedElm = i, this._currentHighlightIndex = s, this.changeCurrentOptionHighlight(i));
        }
        this.lastMouseOverPosition = `${e2.clientX}:${e2.clientY}`;
      }), void 0, "hover-highlight"), this._bindEventService.bind(this.dropElm, "keydown", ((e2) => {
        switch (e2.key) {
          case "ArrowUp":
            e2.preventDefault(), this.moveHighlightUp();
            break;
          case "ArrowDown":
            e2.preventDefault(), this.moveHighlightDown();
            break;
          case "Escape":
            this.handleEscapeKey();
            break;
          case "Enter":
          case " ": {
            if (document.activeElement !== this.okButtonElm) {
              let i = this.getEventTarget(e2).closest(".ms-select-all") || this.getEventTarget(e2).closest("li");
              if (e2.key === " " && this.options.filter || this.options.filterAcceptOnEnter && !i) return;
              e2.preventDefault(), this._currentSelectedElm?.querySelector("input")?.click(), this.options.single && (this.choiceElm.focus(), this.lastFocusedItemKey = this.choiceElm?.dataset.key || "");
            }
            break;
          }
          case "Tab": {
            e2.preventDefault();
            let i = !this.options.single && this.options.showOkButton;
            i && e2.shiftKey && document.activeElement === this.okButtonElm ? (this.focusSelectAllOrList(), this.highlightCurrentOption(), this.filterParentElm?.querySelector("input")?.focus()) : i && !e2.shiftKey && document.activeElement !== this.okButtonElm ? (this.changeCurrentOptionHighlight(), this.okButtonElm?.focus()) : (this.options.onBlur(e2), this.options.isOpen && this.options.closeOnTab && this.close("blur"));
            break;
          }
        }
      }), void 0, "arrow-highlight")), this.ulElm && this.options.infiniteScroll && this._bindEventService.bind(this.ulElm, "scroll", this.infiniteScrollHandler.bind(this), void 0, "option-list-scroll");
    }
    handleOnChange(t, e2) {
      this.options.onChange({ eventName: t, item: e2, selection: { labels: this.getSelects("text"), values: this.getSelects("value") } });
    }
    handleEscapeKey() {
      this.options.keepOpen || (this.close("key.escape"), this.choiceElm.focus());
    }
    infiniteScrollHandler(t, e2, i) {
      let l = false;
      t && this.getEventTarget(t) && this.ulElm && this.scrolledByMouse ? this.getEventTarget(t).scrollTop + this.getEventTarget(t).clientHeight === this.ulElm.scrollHeight && (l = true) : e2 !== void 0 && e2 + 1 === i && (l = true), l && this.ulElm && (this.virtualScroll ? this.initListItems() : this.ulElm.scrollTop = 0, this._currentHighlightIndex = 0, this.highlightCurrentOption());
    }
    open(t = 0) {
      return new Promise((e2) => {
        t !== null && t >= 0 ? (window.clearTimeout(this.openDelayTimer), this.openDelayTimer = window.setTimeout(() => {
          this.openDrop(), e2();
        }, t)) : (this.openDrop(), e2());
      });
    }
    openDrop() {
      if (!this.dropElm || this.choiceElm?.classList.contains("disabled")) return;
      this.options.onBeforeOpen();
      let t = false;
      if (this.options.lazyData && !this._isLazyLoaded && (t = true, this.dropElm?.querySelector("ul.ms-list")?.remove(), this.options.lazyData((e2) => {
        this._isLazyLoaded = true, this.dropElm?.querySelector(".ms-loading")?.remove(), this.setData(e2, true);
      }, (e2) => {
        this.dropElm?.querySelector(".ms-loading")?.remove(), this.dropElm?.querySelector(".ms-lazy-failed")?.remove();
        let i = h("div", { className: "ms-lazy-failed" });
        i.appendChild(h("div", { className: "ms-icon ms-icon-lazy-failed" })), i.appendChild(h("span", { textContent: e2 ?? this.formatLazyFailed() })), this.dropElm?.appendChild(i);
      })), this.options.isOpen = true, this.parentElm.classList.add("ms-parent-open"), this.choiceElm?.querySelector("div.ms-icon-caret")?.classList.add("open"), this.dropElm.style.display = "block", this.dropElm.ariaExpanded = "true", this.selectAllElm?.parentElement && (this.selectAllElm.parentElement.style.display = "inline-flex"), this.noResultsElm && (this.noResultsElm.style.display = "none"), !this.getDataLength()) if (this.selectAllElm?.parentElement && (this.selectAllElm.parentElement.style.display = "none"), t && !this._isLazyLoaded && !this.dropElm.querySelector(".ms-loading")) {
        this.dropElm.querySelector(".ms-lazy-failed")?.remove();
        let e2 = h("div", { className: "ms-loading" });
        e2.appendChild(h("div", { className: "ms-icon ms-icon-loading ms-spin" })), e2.appendChild(h("span", { textContent: this.formatLazyLoading() })), this.dropElm.appendChild(e2);
      } else this.noResultsElm && (this.noResultsElm.style.display = "block");
      this.adjustDropSizeAndPosition(), (!t || this._isLazyLoaded) && (this.getDataLength() && this.options.filter ? (this.searchInputElm && (this.searchInputElm.value = "", this.searchInputElm.focus()), this.filter(true)) : this.focusSelectAllOrList()), this._currentHighlightIndex < 0 ? this.moveHighlightDown() : this.highlightCurrentOption(), this._isOpen = true, this.options.onOpen();
    }
    adjustDropSizeAndPosition() {
      if (this.dropElm) {
        if (this.options.container) {
          let l;
          this.options.container instanceof Node ? l = this.options.container : typeof this.options.container == "string" && (l = this.options.container === "body" ? document.body : document.querySelector(this.options.container)), l.appendChild(this.dropElm);
          let { top: s = 0, left: n = 0 } = C(this.parentElm) || {};
          this.dropElm.style.top = `${s + this.parentElm.offsetHeight}px`, this.dropElm.style.left = `${n}px`, this.dropElm.style.width = `${this.parentElm.offsetWidth}px`, this.dropElm.style.minWidth = "auto";
        }
        let t = this.options.minHeight, e2 = this.options.maxHeight;
        this.options.maxHeightUnit === "row" && (e2 = D(this.dropElm.querySelector("ul>li"), "outer", "height") * this.options.maxHeight), this.ulElm ??= this.dropElm.querySelector("ul"), this.ulElm && (t && (!this.options.lazyData || this._isLazyLoaded) && (this.ulElm.style.minHeight = `${t}px`), this.ulElm.style.maxHeight = `${e2}px`), this.dropElm.querySelectorAll(".multiple").forEach((l) => {
          l.style.width = `${this.options.multipleWidth}px`;
        }), this.options.autoAdjustDropWidthByTextSize && this.adjustDropWidthByText();
        let i = this.options.position;
        if (this.options.autoAdjustDropHeight) {
          if (this.options.autoAdjustDropPosition) {
            let { bottom: l, top: s } = x(this.dropElm), n = this.dropElm.getBoundingClientRect().height;
            i = l < n && s > l ? "top" : "bottom";
          }
          this.adjustDropHeight(i);
        }
        this.options.autoAdjustDropPosition && this.adjustDropPosition(true);
      }
    }
    focusSelectAllOrList() {
      this.selectAllElm ? this.selectAllElm.focus() : this.ulElm && (this.ulElm.tabIndex = 0, this.ulElm.focus());
    }
    highlightCurrentOption() {
      let t = this.dropElm?.querySelectorAll(w) || [];
      if (this._currentHighlightIndex <= t.length) {
        let e2 = t[this._currentHighlightIndex];
        e2 && (this.lastFocusedItemKey = e2.dataset.key || "", this._currentSelectedElm = e2, this.scrolledByMouse = false, e2.scrollIntoView({ block: "nearest" }), this.changeCurrentOptionHighlight(e2), window.setTimeout(() => this.scrolledByMouse = true, 10));
      }
    }
    changeCurrentOptionHighlight(t) {
      t?.classList.add("highlighted"), (this.dropElm?.querySelectorAll(ht) || []).forEach((i) => {
        i !== t && i.classList.remove("highlighted");
      });
    }
    moveHighlightDown() {
      let t = this.dropElm?.querySelectorAll(w) || [], e2 = t.length;
      this._currentHighlightIndex < e2 - 1 ? (this._currentHighlightIndex++, t[this._currentHighlightIndex]?.classList.contains("disabled") && this.moveHighlightDown()) : this.options.infiniteScroll && this.infiniteScrollHandler(null, this._currentHighlightIndex, e2), this.highlightCurrentOption();
    }
    moveHighlightUp() {
      let t = this.dropElm?.querySelectorAll(w) || [], e2 = this.options.single ? 0 : 1;
      if (this.virtualScroll && this._currentHighlightIndex <= e2 && this.updateDataStart > 0 && this.ulElm) {
        let i = t[this._currentHighlightIndex + (this.options.single ? 0 : 1)], l = i?.dataset.key;
        this.lastFocusedItemKey = l, this.ulElm.scrollTop = this.ulElm.scrollTop - i?.getBoundingClientRect().height || 10, this.isMoveUpRecalcRequired = true;
        return;
      }
      this._currentHighlightIndex > 0 && (this._currentHighlightIndex--, t[this._currentHighlightIndex]?.classList.contains("disabled") && this.moveHighlightUp()), this.highlightCurrentOption();
    }
    recalculateArrowMove(t) {
      let e2 = this.dropElm?.querySelectorAll(w) || [], i = Array.from(e2).findIndex((l) => l.dataset.key === this.lastFocusedItemKey);
      this._currentHighlightIndex = i - 1, t === "down" ? this.moveHighlightDown() : t === "up" && (this.moveHighlightUp(), this.isMoveUpRecalcRequired = false);
    }
    close(t) {
      this.options.onBeforeClose(t) !== false && (this._isOpen = false, this.options.isOpen = false, this.parentElm.classList.remove("ms-parent-open"), this.choiceElm?.querySelector("div.ms-icon-caret")?.classList.remove("open"), this.dropElm && (this.dropElm.style.display = "none", this.dropElm.ariaExpanded = "false", this.options.container && (this.parentElm.appendChild(this.dropElm), this.dropElm.style.top = "auto", this.dropElm.style.left = "auto")), this.options.onClose(t));
    }
    applyAsTextOrHtmlWhenEnabled(t, e2) {
      t || (t = {}), this.isRenderAsHtml ? t.innerHTML = typeof this.options.sanitizer == "function" ? this.options.sanitizer(e2) : e2 : t.textContent = tt(e2);
    }
    update(t = false) {
      let e2 = this.getSelects(), i = this.getSelects("text");
      this.options.displayValues && (i = e2);
      let l = this.choiceElm?.querySelector("span"), s = e2.length, n = null, a = () => this.options.useSelectOptionLabel || this.options.useSelectOptionLabelToHtml ? e2.join(this.options.displayDelimiter) : i.join(this.options.displayDelimiter);
      if (l) {
        if (s === 0) {
          let c = this.options.placeholder || "";
          l.classList.add("ms-placeholder"), this.applyAsTextOrHtmlWhenEnabled(l, c);
        } else s < this.options.minimumCountSelected ? n = a() : this.formatAllSelected() && s === this.dataTotal ? n = this.formatAllSelected() : this.options.ellipsis && s > this.options.minimumCountSelected ? n = `${i.slice(0, this.options.minimumCountSelected).join(this.options.displayDelimiter)}...` : this.formatCountSelected(s, this.dataTotal) && s > this.options.minimumCountSelected ? n = this.formatCountSelected(s, this.dataTotal) : n = a();
        if (n !== null && (l?.classList.remove("ms-placeholder"), this.applyAsTextOrHtmlWhenEnabled(l, n)), this.options.showClear && this.selectClearElm) {
          let c = n ? "block" : "none";
          this.selectClearElm.style.display = c;
        }
        if (this.options.displayTitle) {
          let c = this.options.useSelectOptionLabel || this.options.useSelectOptionLabelToHtml ? "value" : "text";
          l.title = this.getSelects(c).join(this.options.displayDelimiter);
        }
      }
      if (this.elm instanceof HTMLSelectElement) {
        let c = this.getSelects();
        this.options.single ? this.elm.value = c.length ? c[0] : "" : Array.from(this.elm.options).forEach((p) => {
          p.selected = c.some((u) => u === p.value);
        });
      }
      t || this.elm.dispatchEvent(new Event("change"));
    }
    updateSelected(t) {
      for (let i = this.updateDataStart; i < this.updateDataEnd; i++) {
        let l = this.updateData[i], s = this.dropElm?.querySelector(`input[data-key=${l._key}]`);
        if (s) {
          s.checked = l.selected;
          let n = s.closest("li"), a = n?.querySelector(".icon-checkbox-container div");
          if (n) {
            let c = false;
            "children" in l && l.children.some((p) => p?.selected) && (c = true), l.selected && !n.classList.contains("selected") ? (n.classList.add("selected"), n.ariaSelected = "true", a && (a.className = `ms-icon ms-icon-${s.type === "radio" ? "radio" : "check"}`)) : l.selected || (n.classList.remove("selected"), n.ariaSelected = "false", a && (a.className = `ms-icon ms-icon-${c ? "partial-group" : "uncheck"}`));
          }
        }
      }
      let e2 = !this.data?.some((i) => i.visible);
      if (this.selectAllElm) {
        this.selectAllElm.ariaChecked = String(this.isAllSelected);
        let i = this.dropElm?.querySelector(".ms-select-all .icon-checkbox-container div");
        if (i) {
          let l = "";
          this.isAllSelected ? l = "ms-icon-check" : this.isPartiallyAllSelected ? l = "ms-icon-partial-all" : l = "ms-icon-uncheck", i.className = `ms-icon ${l}`;
        }
        this.selectAllElm.checked = this.isAllSelected, P(this.selectAllElm.closest("li"), !e2);
      }
      P(this.noResultsElm, e2), this.virtualScroll && (this.virtualScroll.rows = t ?? this.getListRows());
    }
    getData() {
      return this.options.data;
    }
    getDataLength() {
      return this.data?.length ?? 0;
    }
    getOptions(t = true) {
      let e2 = Object.assign({}, this.options);
      return delete e2.data, t ? O(e2) : this.options;
    }
    refreshOptions(t) {
      j(this.options, t, true) || (this.options = Object.assign(this.options, t), this.destroy(false), this.init());
    }
    getDropElement() {
      return this.dropElm;
    }
    getParentElement() {
      return this.parentElm;
    }
    getSelects(t = "value") {
      let e2 = [];
      for (let i of this.data || []) if (i.type === "optgroup") {
        let l = i.children.filter((s) => s?.selected);
        if (!l.length) continue;
        if (t === "value" || this.options.single) e2.push(...l.map((s) => t === "value" && s._value || s[t]));
        else {
          let s = [];
          s.push("["), s.push(i.label), s.push(`: ${l.map((n) => n[t]).join(", ")}`), s.push("]"), e2.push(s.join(""));
        }
      } else i.selected && e2.push(t === "value" && i._value || i[t]);
      return e2;
    }
    setSelects(t, e2 = "value", i = false) {
      let l = false, s = (n) => {
        for (let a of n) {
          let c = false;
          if (e2 === "text") {
            let p = document.createElement("div");
            this.applyAsTextOrHtmlWhenEnabled(p, a.text), c = t.includes(p.textContent?.trim() ?? "");
          } else a && (c = t.includes(a._value || a.value), !c && a.value === `${+a.value}` && (c = t.includes(+a.value)));
          a && (a.selected !== c && (l = true), a.selected = c);
        }
      };
      for (let n of this.data || []) n.type === "optgroup" ? s(n.children) : s([n]);
      l && (this.initSelected(i), this.updateSelected(), this.update(i));
    }
    enable() {
      this.choiceElm && (this.choiceElm.classList.remove("disabled"), this.choiceElm.disabled = false);
    }
    disable() {
      this.choiceElm && (this.choiceElm?.classList.add("disabled"), this.choiceElm.disabled = true);
    }
    check(t) {
      let e2 = S(this.data, "value", t);
      e2 && this._check(e2, true);
    }
    uncheck(t) {
      let e2 = S(this.data, "value", t);
      e2 && this._check(e2, false);
    }
    _check(t, e2) {
      this.options.single && this._checkAll(false, true), t.selected = e2, this.initSelected(), this.updateSelected(), this.update();
    }
    checkAll() {
      this._checkAll(true);
    }
    uncheckAll() {
      this._checkAll(false);
    }
    setData(t, e2 = false) {
      y(this.elm), this.ulElm?.remove(), this.options.data = t, this.initData(), this.initList(e2), this.update(), this.adjustDropSizeAndPosition();
    }
    _checkAll(t, e2) {
      for (let i of this.data || []) i.type === "optgroup" ? this._checkGroup(i, t, true) : !i.disabled && !i.divider && (e2 || i.visible) && (i.selected = t);
      e2 || (this.initSelected(), this.updateSelected(), this.update());
    }
    _checkGroup(t, e2, i) {
      t.selected = e2, t.children.forEach((l) => {
        l && !l.disabled && !l.divider && (i || l.visible) && (l.selected = e2);
      }), i || (this.initSelected(), this.updateSelected(), this.update());
    }
    checkInvert() {
      if (!this.options.single) {
        for (let t of this.data || []) if (t.type === "optgroup") for (let e2 of t.children) e2 && (e2.divider || (e2.selected = !e2.selected));
        else t && !t.divider && (t.selected = !t.selected);
        this.initSelected(), this.updateSelected(), this.update();
      }
    }
    focus() {
      this.choiceElm?.focus(), this.options.onFocus();
    }
    blur() {
      this.choiceElm?.blur(), this.options.onBlur();
    }
    refresh() {
      this.destroy(false), this.init();
    }
    filter(t) {
      let e2 = this.searchInputElm?.value.trim() ?? "", i = e2.toLowerCase();
      if (this.filterText === i) return;
      this.filterText = i;
      for (let s of this.data || []) if (s.type === "optgroup") if (this.options.filterGroup) {
        let n = `${s?.label ?? ""}`;
        if (s != null) {
          let a = this.options.customFilter({ label: g(n.toString().toLowerCase(), this.options.diacriticParser), search: g(i, this.options.diacriticParser), originalLabel: n, originalSearch: e2, row: s });
          s.visible = a;
          for (let c of s.children) c && (c.visible = a);
        }
      } else {
        for (let n of s.children) if (n != null) {
          let a = `${n?.text ?? ""}`;
          n.visible = this.options.customFilter({ text: g(a.toString().toLowerCase(), this.options.diacriticParser), search: g(i, this.options.diacriticParser), originalText: a, originalSearch: e2, row: n, parent: s });
        }
        s.visible = s.children.some((n) => n?.visible);
      }
      else {
        let n = `${s?.text ?? ""}`;
        s.visible = this.options.customFilter({ text: g(n.toString().toLowerCase(), this.options.diacriticParser), search: g(i, this.options.diacriticParser), originalText: n, originalSearch: e2, row: s });
      }
      let l = this.initListItems();
      this.initSelected(t), this.updateSelected(l), t || this.options.onFilter(e2);
    }
    adjustDropHeight(t) {
      let e2 = t !== "top", i = this.filterParentElm?.getBoundingClientRect().height ?? 0, l = this.okButtonElm?.getBoundingClientRect().height ?? 0, s = this.options.single ? 0 : this.selectAllParentElm?.getBoundingClientRect().height ?? 0, n = i + l + s + 5, { bottom: a, top: c } = x(this.parentElm), p = this.options.maxHeight;
      if (e2 ? p = a - n - this.options.adjustedHeightPadding : p = c - n - this.options.adjustedHeightPadding, !this.options.maxHeight || this.options.maxHeight && p < this.options.maxHeight) {
        let u = this.dropElm?.querySelector("ul");
        return u && (u.style.maxHeight = `${p}px`), true;
      }
      return false;
    }
    adjustDropPosition(t) {
      let e2 = "bottom";
      if (this.dropElm && this.parentElm) {
        let { bottom: i, top: l } = x(this.dropElm), { top: s, left: n } = C(this.parentElm), a = this.dropElm.getBoundingClientRect().height, c = this.dropElm.getBoundingClientRect().width, p = document.body.offsetWidth || window.innerWidth, u = this.parentElm.getBoundingClientRect().width;
        if (i > a) e2 = "bottom";
        else if (a > i && l > i) {
          if (this.options.container) {
            let m = s - a;
            m < 0 && (m = 0), (m > 0 || t) && (e2 = "top", this.dropElm.style.top = `${m < 0 ? 0 : m}px`);
          } else e2 = "top", this.dropElm.classList.add(e2);
          this.dropElm.classList.remove("bottom");
        }
        p - c < n && (this.dropElm.style.left = `${n - (c - u)}px`);
      }
      return e2;
    }
    adjustDropWidthByText() {
      if (this.dropElm) {
        let e2 = this.parentElm.scrollWidth;
        (this.options.dropWidth || this.options.width) && (e2 = this.options.dropWidth || this.options.width || 0);
        let i = this.dropElm.querySelector(".ms-select-all span"), l = this.dropElm.querySelector("ul");
        if (l) {
          let s = 0, n = 0, a = (i?.clientWidth ?? 0) + n, p = l.scrollHeight > l.clientHeight ? this.getScrollbarWidth() : 0;
          n += H(l, "paddingLeft") * 2;
          let u = false;
          for (let m of Array.from(l.querySelectorAll("li"))) {
            let b = m.querySelector("label"), B = m.querySelector(".icon-checkbox-container"), E = m.querySelector("span");
            b && E?.textContent && (u || (n += H(m, "paddingLeft") * 2, n += H(b, "paddingLeft") * 2, n += H(E, "paddingLeft"), n += B?.offsetWidth ?? 0, u = true), E.offsetWidth > s && (s = E.offsetWidth));
          }
          s += n + p, s < a && (s = a), this.options.maxWidth && s > this.options.maxWidth && (s = this.options.maxWidth), this.options.minWidth && s < this.options.minWidth && (s = this.options.minWidth), (e2 === "100%" || +e2 < s) && (this.dropElm.style.minWidth = "auto", this.dropElm.style.width = `${s}px`);
        }
      }
    }
    getScrollbarWidth() {
      let t = h("div", { style: { visibility: "hidden", width: "100px" } });
      document.body.appendChild(t);
      let e2 = t.offsetWidth;
      t.style.overflow = "scroll";
      let i = h("div", { style: { width: "100%" } });
      t.appendChild(i);
      let l = i.offsetWidth;
      return t.parentNode?.removeChild(t), e2 - l;
    }
    formatAllSelected() {
      return this.options.allSelectedText || this.options.formatAllSelected();
    }
    formatCountSelected(t, e2) {
      return this.options.countSelectedText ? this.options.countSelectedText.replace("#", `${t}`).replace("%", `${e2}`) : this.options.formatCountSelected(t, e2);
    }
    formatNoMatchesFound() {
      return this.options.noMatchesFoundText || this.options.formatNoMatchesFound();
    }
    formatOkButton() {
      return this.options.okButtonText || this.options.formatOkButton();
    }
    formatLazyLoading() {
      return this.options.lazyLoadingText || this.options.formatLazyLoading();
    }
    formatLazyFailed() {
      return this.options.lazyFailedText || this.options.formatLazyFailed();
    }
    formatSelectAll() {
      return this.options.selectAllText || this.options.formatSelectAll();
    }
  };
  var pt = (o, t) => typeof o == "string" ? F(document.querySelectorAll(o), t) : o instanceof Node ? F([o], t) : F(o, t);
  function F(o, t) {
    let e2 = Array.from(o), i = [];
    for (let l = 0; l < e2.length; l++) {
      let s = e2[l];
      try {
        s._multipleSelect !== void 0 && (s._multipleSelect.destroy(), delete s._multipleSelect), s._multipleSelect = new _(s, t || {}), s._multipleSelect.init();
        let n = s._multipleSelect.getOptions(false);
        n.onHardDestroy = () => delete s._multipleSelect, n.onAfterHardDestroy = () => i[l] = null, i.push(s._multipleSelect);
      } catch (n) {
        console.error(n);
      }
    }
    return i.length === 1 ? i[0] : i;
  }

  // node_modules/@slickgrid-universal/common/dist/filters/selectFilter.js
  var SelectFilter = class {
    /**
     * Initialize the Filter
     */
    constructor(translaterService, collectionService, rxjs, isMultipleSelect = true) {
      this.translaterService = translaterService;
      this.collectionService = collectionService;
      this.rxjs = rxjs;
      this._isMultipleSelect = true;
      this._collectionLength = 0;
      this._collectionObservers = [];
      this._shouldTriggerQuery = true;
      this._isLazyDataLoaded = false;
      this.isFilled = false;
      this.enableTranslateLabel = false;
      this.subscriptions = [];
      this._isMultipleSelect = isMultipleSelect;
    }
    /** Getter for the Collection Options */
    get collectionOptions() {
      return this.columnDef?.filter?.collectionOptions ?? {};
    }
    get columnId() {
      return this.columnDef?.id ?? "";
    }
    /** Getter for the Filter Operator */
    get columnFilter() {
      return this.columnDef?.filter ?? {};
    }
    /** Getter for the Custom Structure if exist */
    get customStructure() {
      return this.columnDef?.filter?.customStructure;
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this.grid?.getOptions() ?? {};
    }
    /** Getter to know what would be the default operator when none is specified */
    get defaultOperator() {
      return this.isMultipleSelect ? "IN" : "EQ";
    }
    get filterOptions() {
      return { ...this.gridOptions.defaultFilterOptions?.select, ...this.columnFilter?.options };
    }
    /** Getter to know if the current filter is a multiple-select (false means it's a single select) */
    get isMultipleSelect() {
      return this._isMultipleSelect;
    }
    get msInstance() {
      return this._msInstance;
    }
    get selectOptions() {
      return this.defaultOptions;
    }
    /** Getter for the Filter Operator */
    get operator() {
      return this.columnFilter?.operator ?? this.defaultOperator;
    }
    /** Setter for the filter operator */
    set operator(operator) {
      if (this.columnFilter) {
        this.columnFilter.operator = operator;
      }
    }
    /** Initialize the filter template */
    init(args) {
      this.grid = args.grid;
      this.callback = args.callback;
      this.columnDef = args.columnDef;
      this.searchTerms = (args.hasOwnProperty("searchTerms") ? args.searchTerms : []) || [];
      this.filterContainerElm = args.filterContainerElm;
      if (!this.grid || !this.columnDef || !this.columnFilter || !this.columnFilter.collection && !this.columnFilter.collectionAsync && !this.columnFilter.collectionLazy) {
        throw new Error(`[Slickgrid-Universal] You need to pass a "collection" (or "collectionAsync") for the MultipleSelect/SingleSelect Filter to work correctly. Also each option should include a value/label pair (or value/labelKey when using Locale). For example:: { filter: model: Filters.multipleSelect, collection: [{ value: true, label: 'True' }, { value: false, label: 'False'}] }`);
      }
      this.enableTranslateLabel = this.columnFilter?.enableTranslateLabel ?? false;
      this.labelName = this.customStructure?.label ?? "label";
      this.labelPrefixName = this.customStructure?.labelPrefix ?? "labelPrefix";
      this.labelSuffixName = this.customStructure?.labelSuffix ?? "labelSuffix";
      this.optionLabel = this.customStructure?.optionLabel ?? "value";
      this.valueName = this.customStructure?.value ?? "value";
      if (this.enableTranslateLabel && (!this.translaterService || typeof this.translaterService.translate !== "function")) {
        throw new Error(`[select-filter] The Translate Service is required for the Select Filter to work correctly when "enableTranslateLabel" is set.`);
      }
      this._locales = this.gridOptions?.locales ?? Constants.locales;
      this.initMultipleSelectTemplate();
      let placeholder = this.gridOptions?.defaultFilterPlaceholder || "";
      if (this.columnFilter?.placeholder) {
        placeholder = this.columnFilter.placeholder;
      }
      this.defaultOptions.placeholder = placeholder || "";
      if (this._isMultipleSelect && this.columnDef?.filter) {
        this.columnDef.filter.emptySearchTermReturnAllValues ??= false;
      }
      const newCollection = this.columnFilter.collection || [];
      return new Promise(async (resolve, reject) => {
        try {
          let collectionOutput;
          if (this.columnFilter.collectionAsync && !this.columnFilter.collection) {
            collectionOutput = renderCollectionOptionsAsync(this.columnFilter.collectionAsync, this.columnDef, this.renderDomElement.bind(this), this.rxjs, this.subscriptions);
            resolve(collectionOutput);
          } else {
            collectionOutput = newCollection;
            this.renderDomElement(newCollection);
            resolve(newCollection);
          }
          if (this.columnFilter.collectionAsync || this.columnFilter.enableCollectionWatch) {
            await (collectionOutput ?? this.columnFilter.collectionAsync);
            this.watchCollectionChanges();
          }
        } catch (e2) {
          reject(e2);
        }
      });
    }
    /** Clear the filter values */
    clear(shouldTriggerQuery = true) {
      if (this._msInstance && this._collectionLength > 0) {
        this._msInstance.setSelects([]);
        this.updateFilterStyle(false);
        this.searchTerms = [];
        this._shouldTriggerQuery = shouldTriggerQuery;
        this.callback(void 0, {
          columnDef: this.columnDef,
          clearFilterTriggered: true,
          shouldTriggerQuery: this._shouldTriggerQuery
        });
        this._shouldTriggerQuery = true;
      }
    }
    /** destroy the filter */
    destroy() {
      if (typeof this._msInstance?.destroy === "function") {
        this._msInstance.destroy();
      }
      this.filterElm?.remove();
      unsubscribeAll(this.subscriptions);
    }
    /**
     * Get selected values retrieved from the multiple-selected element
     * @params selected items
     */
    getValues() {
      return this._msInstance?.getSelects() ?? [];
    }
    /** Set value(s) on the DOM element */
    setValues(values, operator, triggerChange = false) {
      if (values !== void 0 && this._msInstance) {
        values = Array.isArray(values) ? values.every((x2) => isPrimitiveValue(x2)) ? values.map(String) : values : [values];
        this._msInstance.setSelects(values);
      }
      this.updateFilterStyle(this.getValues().length > 0);
      this.operator = operator || this.defaultOperator;
      if (triggerChange) {
        this.onTriggerEvent();
      }
    }
    //
    // protected functions
    // ------------------
    /**
     * Subscribe to both CollectionObserver & PropertyObserver with BindingEngine.
     * They each have their own purpose, the "propertyObserver" will trigger once the collection is replaced entirely
     * while the "collectionObverser" will trigger on collection changes (`push`, `unshift`, `splice`, ...)
     */
    watchCollectionChanges() {
      if (this.columnFilter?.collection) {
        this._collectionObservers.push(collectionObserver(this.columnFilter.collection, this.watchCallback.bind(this)));
        propertyObserver(this.columnFilter, "collection", this.propertyObserverCallback.bind(this));
      }
    }
    propertyObserverCallback(newValue) {
      this.renderDomElement(newValue || []);
      if (this.columnFilter.collection) {
        this._collectionObservers.push(collectionObserver(this.columnFilter.collection, this.watchCallback.bind(this)));
      }
    }
    watchCallback(updatedArray) {
      this.renderDomElement(this.columnFilter.collection || updatedArray || []);
    }
    renderDomElement(inputCollection) {
      inputCollection = getCollectionFromObjectWhenEnabled(inputCollection, this.columnFilter);
      if (!Array.isArray(inputCollection)) {
        throw new Error('The "collection" passed to the Select Filter is not a valid array.');
      }
      let collection = [];
      if (inputCollection.length > 0) {
        collection = [...inputCollection];
      }
      if (this.collectionOptions?.addBlankEntry && Array.isArray(collection) && collection.length > 0 && collection[0][this.valueName] !== "") {
        collection.unshift(createBlankSelectEntry(this.labelName, this.valueName, this.labelPrefixName, this.labelSuffixName));
      }
      if (this.collectionOptions?.addCustomFirstEntry && Array.isArray(collection) && collection.length > 0 && collection[0][this.valueName] !== this.collectionOptions.addCustomFirstEntry[this.valueName]) {
        collection.unshift(this.collectionOptions.addCustomFirstEntry);
      }
      if (this.collectionOptions?.addCustomLastEntry && Array.isArray(collection) && collection.length > 0) {
        const lastCollectionIndex = collection.length - 1;
        if (collection[lastCollectionIndex][this.valueName] !== this.collectionOptions.addCustomLastEntry[this.valueName]) {
          collection.push(this.collectionOptions.addCustomLastEntry);
        }
      }
      const { dataCollection, hasFoundSearchTerm, selectElement } = this.filterSortAndParseCollection(collection);
      this.isFilled = hasFoundSearchTerm;
      this.createFilterElement(selectElement, dataCollection);
      this._collectionLength = dataCollection.length;
    }
    /**
     * From the Select DOM Element created earlier, create a Multiple/Single Select Filter using the multiple-select-vanilla.js lib
     * @param {Object} selectElement
     */
    createFilterElement(selectElement, dataCollection) {
      this.elementName = `filter-${this.columnId}`;
      this.defaultOptions.name = this.elementName;
      emptyElement(this.filterContainerElm);
      this.filterElm = selectElement;
      this.filterElm.dataset.columnId = `${this.columnId}`;
      this.updateFilterStyle(this.isFilled);
      this.filterContainerElm.appendChild(selectElement);
      this.filterElmOptions = { ...this.defaultOptions, ...this.filterOptions, data: dataCollection };
      this._msInstance = pt(selectElement, this.filterElmOptions);
      this.columnFilter.onInstantiated?.(this._msInstance);
    }
    filterSortAndParseCollection(inputCollection) {
      inputCollection = filterCollectionWithOptions(inputCollection, this.collectionService, this.columnFilter?.collectionFilterBy, this.collectionOptions);
      inputCollection = sortCollectionWithOptions(inputCollection, this.columnDef, this.collectionService, this.columnFilter?.collectionSortBy, this.enableTranslateLabel);
      return buildMsSelectCollectionList("filter", inputCollection, this.columnDef, this.grid, this.isMultipleSelect, this.translaterService, this.searchTerms || []);
    }
    initMultipleSelectTemplate() {
      const options = {
        autoAdjustDropHeight: true,
        autoAdjustDropPosition: true,
        autoAdjustDropWidthByTextSize: true,
        name: `${this.columnId}`,
        container: "body",
        closeOnTab: true,
        darkMode: !!this.gridOptions.darkMode,
        filter: false,
        // input search term on top of the select option list
        maxHeight: 275,
        single: true,
        singleRadio: true,
        showSearchClear: true,
        tabIndex: 0,
        renderOptionLabelAsHtml: this.columnFilter?.enableRenderHtml ?? false,
        sanitizer: (dirtyHtml) => this.grid.sanitizeHtmlString(dirtyHtml),
        // we will subscribe to the onClose event for triggering our callback
        // also add/remove "filled" class for styling purposes & refocus on ms-select parent after closing
        onClose: () => {
          this.onTriggerEvent();
          this._msInstance?.focus();
        },
        onClear: () => this.clear()
      };
      if (this.columnFilter.collectionLazy) {
        options.lazyData = (resolve, reject) => {
          const lazyProcess = this.columnFilter.collectionLazy?.(this.columnDef);
          fetchAsPromise(lazyProcess, this.rxjs).then((collectionData) => {
            collectionData = getCollectionFromObjectWhenEnabled(collectionData, this.columnFilter);
            const { dataCollection } = this.filterSortAndParseCollection(collectionData);
            resolve(dataCollection || []);
          }).catch((error) => reject(error));
        };
      }
      if (this._isMultipleSelect) {
        options.single = false;
        options.singleRadio = false;
        options.showOkButton = true;
        options.displayTitle = true;
        const translationPrefix = getTranslationPrefix(this.gridOptions);
        options.countSelectedText = this.translateOrDefault(`${translationPrefix}X_OF_Y_SELECTED`, this._locales?.TEXT_X_OF_Y_SELECTED);
        options.allSelectedText = this.translateOrDefault(`${translationPrefix}ALL_SELECTED`, this._locales?.TEXT_ALL_SELECTED);
        options.noMatchesFoundText = this.translateOrDefault(`${translationPrefix}NO_MATCHES_FOUND`, this._locales?.TEXT_NO_MATCHES_FOUND);
        options.okButtonText = this.translateOrDefault(`${translationPrefix}OK`, this._locales?.TEXT_OK);
        options.selectAllText = this.translateOrDefault(`${translationPrefix}SELECT_ALL`, this._locales?.TEXT_SELECT_ALL);
        options.lazyLoadingText = this.translateOrDefault(`${translationPrefix}LOADING`, this._locales?.TEXT_LOADING);
      }
      this.defaultOptions = options;
    }
    onTriggerEvent() {
      if (this._msInstance) {
        const selectedItems = this.getValues();
        this.updateFilterStyle(Array.isArray(selectedItems) && selectedItems.length > 1 || selectedItems.length === 1 && selectedItems[0] !== "");
        this.searchTerms = selectedItems;
        this.callback(void 0, {
          columnDef: this.columnDef,
          operator: this.operator,
          searchTerms: selectedItems,
          shouldTriggerQuery: this._shouldTriggerQuery
        });
        this._shouldTriggerQuery = true;
      }
    }
    /** add/remove "filled" CSS class */
    updateFilterStyle(isFilled) {
      this.isFilled = isFilled;
      this.filterElm?.classList.toggle("filled", isFilled);
      this._msInstance?.getParentElement()?.classList.toggle("filled", isFilled);
    }
    translateOrDefault(translationKey, defaultValue = "") {
      const isTranslateEnabled = this.gridOptions?.enableTranslate ?? false;
      return isTranslateEnabled && this.translaterService?.translate ? this.translaterService.translate(translationKey) : defaultValue;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/multipleSelectFilter.js
  var MultipleSelectFilter = class extends SelectFilter {
    /**
     * Initialize the Filter
     */
    constructor(translaterService, collectionService, rxjs) {
      super(translaterService, collectionService, rxjs, true);
      this.translaterService = translaterService;
      this.collectionService = collectionService;
      this.rxjs = rxjs;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/singleSelectFilter.js
  var SingleSelectFilter = class extends SelectFilter {
    /**
     * Initialize the Filter
     */
    constructor(translaterService, collectionService, rxjs) {
      super(translaterService, collectionService, rxjs, false);
      this.translaterService = translaterService;
      this.collectionService = collectionService;
      this.rxjs = rxjs;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/singleSliderFilter.js
  var SingleSliderFilter = class extends SliderFilter {
    /**
     * Initialize the Filter
     */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this.sliderType = "single";
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/sliderRangeFilter.js
  var SliderRangeFilter = class extends SliderFilter {
    /**
     * Initialize the Filter
     */
    constructor(translaterService) {
      super(translaterService);
      this.translaterService = translaterService;
      this.sliderType = "double";
    }
  };

  // node_modules/@slickgrid-universal/common/dist/filters/filters.index.js
  var Filters = {
    /** AutoComplete Filter (using https://github.com/kraaden/autocomplete) */
    autocompleter: AutocompleterFilter,
    /** Compound Date Filter (compound of Operator + Date picker) */
    compoundDate: CompoundDateFilter,
    /** Alias to compoundInputText to Compound Input Filter (compound of Operator + Input Text) */
    compoundInput: CompoundInputFilter,
    /** Compound Input Number Filter (compound of Operator + Input of type Number) */
    compoundInputNumber: CompoundInputNumberFilter,
    /** Compound Input Password Filter (compound of Operator + Input of type Password, also note that only the text shown in the UI will be masked, filter query is still plain text) */
    compoundInputPassword: CompoundInputPasswordFilter,
    /** Compound Input Text Filter (compound of Operator + Input Text) */
    compoundInputText: CompoundInputFilter,
    /** Compound Slider Filter (compound of Operator + Slider) */
    compoundSlider: CompoundSliderFilter,
    /** Range Date Filter (uses the Flactpickr Date picker with range option) */
    dateRange: DateRangeFilter,
    /** Alias to inputText, input type text filter (this is the default filter when no type is provided) */
    input: InputFilter,
    /**
     * Input Filter of type text that will be formatted with a mask output
     * e.g.: column: { filter: { model: Filters.inputMask }, params: { mask: '(000) 000-0000' }}
     */
    inputMask: InputMaskFilter,
    /** Input Filter of type Number */
    inputNumber: InputNumberFilter,
    /** Input Filter of type Password (note that only the text shown in the UI will be masked, filter query is still plain text) */
    inputPassword: InputPasswordFilter,
    /** Default Filter, input type text filter */
    inputText: InputFilter,
    /** Multiple Select filter, which uses 3rd party lib "multiple-select-vanilla" */
    multipleSelect: MultipleSelectFilter,
    /** Single Select filter, which uses 3rd party lib "multiple-select-vanilla" */
    singleSelect: SingleSelectFilter,
    /** Slider Filter (single value) */
    slider: SingleSliderFilter,
    /** Slider Range Filter (dual values, lowest/highest filter range) */
    sliderRange: SliderRangeFilter
  };

  // node_modules/@slickgrid-universal/common/dist/global-grid-options.js
  var PluginFlagMappings = /* @__PURE__ */ new Map([
    ["ExcelExportService", "enableExcelExport"],
    ["PdfExportService", "enablePdfExport"],
    ["TextExportService", "enableTextExport"],
    ["CompositeEditorComponent", "enableCompositeEditor"],
    ["RowDetailView", "enableRowDetailView"]
  ]);
  var GlobalGridOptions = {
    alwaysShowVerticalScroll: true,
    autoEdit: false,
    asyncEditorLoading: false,
    autoFitColumnsOnFirstLoad: true,
    autoFixResizeTimeout: 5 * 60 * 5,
    // retry interval is 200ms, so (5 * 60 * 5 => 5min)
    autoFixResizeRequiredGoodCount: 2,
    autoFixResizeWhenBrokenStyleDetected: false,
    autoParseInputFilterOperator: true,
    autoResize: {
      applyResizeToContainer: true,
      autoHeight: true,
      autoHeightRecalcRow: 100,
      calculateAvailableSizeBy: "window",
      bottomPadding: 20,
      minHeight: 250,
      minWidth: 300,
      rightPadding: 0
    },
    cellHighlightCssClass: "slick-cell-modified",
    checkboxSelector: {
      cssClass: "slick-cell-checkboxsel",
      width: 40
    },
    columnGroupSeparator: " - ",
    columnPicker: {
      hideForceFitButton: false,
      hideSyncResizeButton: true,
      headerColumnValueExtractor: pickerHeaderColumnValueExtractor
    },
    cellMenu: {
      autoAdjustDrop: true,
      autoAlignSide: true,
      hideCloseButton: true,
      hideCommandSection: false,
      hideOptionSection: false,
      showBulletWhenIconMissing: true,
      subItemChevronClass: "mdi mdi-chevron-down mdi-rotate-270"
    },
    compositeEditorOptions: {
      labels: {
        cancelButtonKey: "CANCEL",
        cloneButtonKey: "CLONE",
        resetEditorButtonTooltipKey: "RESET_INPUT_VALUE",
        resetFormButtonKey: "RESET_FORM",
        massSelectionButtonKey: "APPLY_TO_SELECTION",
        massSelectionStatusKey: "X_OF_Y_MASS_SELECTED",
        massUpdateButtonKey: "APPLY_MASS_UPDATE",
        massUpdateStatusKey: "ALL_X_RECORDS_SELECTED",
        saveButtonKey: "SAVE"
      }
    },
    contextMenu: {
      autoAdjustDrop: true,
      autoAlignSide: true,
      hideCloseButton: true,
      hideClearAllGrouping: false,
      hideCollapseAllGroups: false,
      hideCommandSection: false,
      hideCopyCellValueCommand: false,
      hideExpandAllGroups: false,
      hideExportCsvCommand: false,
      hideExportExcelCommand: false,
      hideExportPdfCommand: false,
      hideExportTextDelimitedCommand: true,
      hideMenuOnScroll: true,
      hideOptionSection: false,
      iconCollapseAllGroupsCommand: "mdi mdi-arrow-collapse",
      iconExpandAllGroupsCommand: "mdi mdi-arrow-expand",
      iconClearGroupingCommand: "mdi mdi-close",
      iconCopyCellValueCommand: "mdi mdi-content-copy",
      iconExportCsvCommand: "mdi mdi-file-document-outline",
      iconExportExcelCommand: "mdi mdi-file-excel-outline",
      iconExportTextDelimitedCommand: "mdi mdi-file-document-outline",
      showBulletWhenIconMissing: true,
      subItemChevronClass: "mdi mdi-chevron-down mdi-rotate-270"
    },
    customFooterOptions: {
      dateFormat: "YYYY-MM-DD, hh:mm a",
      hideRowSelectionCount: false,
      hideTotalItemCount: false,
      hideLastUpdateTimestamp: true,
      footerHeight: 25,
      leftContainerClass: "col-xs-12 col-sm-5",
      rightContainerClass: "col-xs-6 col-sm-7",
      metricSeparator: "|",
      metricTexts: {
        itemsKey: "ITEMS",
        ofKey: "OF",
        itemsSelectedKey: "ITEMS_SELECTED"
      }
    },
    customTooltip: {
      tooltipTextMaxLength: 700,
      maxWidth: 500
    },
    dataView: {
      // when enabled, this will preserve the row selection even after filtering/sorting/grouping
      syncGridSelection: {
        preserveHidden: false,
        preserveHiddenOnSelectionChange: true
      },
      syncGridSelectionWithBackendService: false
      // but disable it when using backend services
    },
    datasetIdPropertyName: "id",
    defaultFilter: Filters.input,
    defaultBackendServiceFilterTypingDebounce: 500,
    enableFilterTrimWhiteSpace: false,
    // do we want to trim white spaces on all Filters?
    enableFormattedDataCache: false,
    // pre-format and cache cell values for export performance
    formattedDataCacheBatchSize: 300,
    // max rows per animation frame (safety cap)
    formattedDataCacheFrameBudgetMs: 8,
    // ms of main-thread time allowed per animation frame (~half of 60fps budget)
    defaultFilterPlaceholder: "\u{1F50E}\uFE0E",
    defaultFilterRangeOperator: "RangeInclusive",
    defaultColumnSortFieldId: "id",
    draggableGrouping: {
      hideToggleAllButton: false,
      toggleAllButtonText: "",
      dropPlaceHolderTextKey: "DROP_COLUMN_HEADER_TO_GROUP_BY"
    },
    editable: false,
    editorNavigateOnArrows: false,
    editorTypingDebounce: 450,
    filterTypingDebounce: 0,
    enableEmptyDataWarningMessage: true,
    emptyDataWarning: {
      className: "slick-empty-data-warning",
      messageKey: "EMPTY_DATA_WARNING_MESSAGE",
      hideFrozenLeftWarning: false,
      hideFrozenRightWarning: false,
      leftViewportMarginLeft: "40%",
      rightViewportMarginLeft: "40%",
      frozenLeftViewportMarginLeft: "0px",
      frozenRightViewportMarginLeft: "40%"
    },
    enableAutoResize: true,
    enableAutoSizeColumns: true,
    enableCellNavigation: false,
    enableColumnPicker: true,
    enableColumnReorder: true,
    enableColumnResizeOnDoubleClick: true,
    enableContextMenu: true,
    enableGridMenu: true,
    enableHeaderMenu: true,
    enableMouseHoverHighlightRow: true,
    enableSorting: true,
    enableTextSelectionOnCells: true,
    explicitInitialization: true,
    excelExportOptions: {
      addGroupIndentation: true,
      autoDetectCellFormat: true,
      exportWithFormatter: false,
      filename: "export",
      format: "xlsx",
      groupingColumnHeaderTitle: "Group By",
      groupCollapsedSymbol: "\u2B9E",
      groupExpandedSymbol: "\u2B9F",
      groupingAggregatorRowText: "",
      sanitizeDataExport: true
    },
    pdfExportOptions: {
      addGroupIndentation: true,
      exportWithFormatter: false,
      filename: "export",
      pageOrientation: "portrait",
      pageSize: "a4",
      fontSize: 10,
      headerFontSize: 11,
      includeColumnHeaders: true,
      htmlDecode: true,
      sanitizeDataExport: true,
      groupingColumnHeaderTitle: "Group By",
      groupCollapsedSymbol: "+",
      groupExpandedSymbol: "-",
      groupingAggregatorRowText: "",
      repeatHeadersOnEachPage: true
    },
    textExportOptions: {
      delimiter: ",",
      exportWithFormatter: false,
      filename: "export",
      format: "csv",
      groupingColumnHeaderTitle: "Group By",
      groupingAggregatorRowText: "",
      sanitizeDataExport: true,
      useUtf8WithBom: true
    },
    eventNamingStyle: "lowerCase",
    forceFitColumns: false,
    frozenHeaderWidthCalcDifferential: 1,
    gridMenu: {
      dropSide: "left",
      commandLabels: {
        clearAllFiltersCommandKey: "CLEAR_ALL_FILTERS",
        clearAllSortingCommandKey: "CLEAR_ALL_SORTING",
        clearFrozenColumnsCommandKey: "CLEAR_PINNING",
        exportCsvCommandKey: "EXPORT_TO_CSV",
        exportExcelCommandKey: "EXPORT_TO_EXCEL",
        exportPdfCommandKey: "EXPORT_TO_PDF",
        exportTextDelimitedCommandKey: "EXPORT_TO_TAB_DELIMITED",
        refreshDatasetCommandKey: "REFRESH_DATASET",
        toggleDarkModeCommandKey: "TOGGLE_DARK_MODE",
        toggleFilterCommandKey: "TOGGLE_FILTER_ROW",
        togglePreHeaderCommandKey: "TOGGLE_PRE_HEADER_ROW"
      },
      hideClearAllFiltersCommand: false,
      hideClearAllSortingCommand: false,
      hideClearFrozenColumnsCommand: true,
      // opt-in command
      hideExportCsvCommand: false,
      hideExportExcelCommand: false,
      hideExportPdfCommand: false,
      hideExportTextDelimitedCommand: true,
      hideForceFitButton: false,
      hideRefreshDatasetCommand: false,
      hideSyncResizeButton: true,
      hideToggleFilterCommand: false,
      hideToggleDarkModeCommand: true,
      hideTogglePreHeaderCommand: false,
      iconCssClass: "mdi mdi-menu",
      iconClearAllFiltersCommand: "mdi mdi-filter-remove-outline",
      iconClearAllSortingCommand: "mdi mdi-sort-variant-off",
      iconClearFrozenColumnsCommand: "mdi mdi-pin-off-outline",
      iconExportCsvCommand: "mdi mdi-file-document-outline",
      iconExportExcelCommand: "mdi mdi-file-excel-outline",
      iconExportTextDelimitedCommand: "mdi mdi-file-document-outline",
      iconRefreshDatasetCommand: "mdi mdi-sync",
      iconToggleDarkModeCommand: "mdi mdi-brightness-4",
      iconToggleFilterCommand: "mdi mdi-flip-vertical",
      iconTogglePreHeaderCommand: "mdi mdi-flip-vertical",
      menuWidth: 16,
      resizeOnShowHeaderRow: true,
      showBulletWhenIconMissing: true,
      subItemChevronClass: "mdi mdi-chevron-down mdi-rotate-270",
      headerColumnValueExtractor: pickerHeaderColumnValueExtractor
    },
    headerMenu: {
      autoAlign: true,
      autoAlignOffset: 4,
      minWidth: 140,
      iconClearFilterCommand: "mdi mdi-filter-remove-outline",
      iconClearSortCommand: "mdi mdi-sort-variant-off",
      iconFilterShortcutSubMenu: "mdi mdi-filter-outline",
      iconFreezeColumns: "mdi mdi-pin-outline",
      iconUnfreezeColumns: "mdi mdi-pin-off-outline",
      iconSortAscCommand: "mdi mdi-sort-ascending",
      iconSortDescCommand: "mdi mdi-sort-descending",
      iconColumnHideCommand: "mdi mdi-close",
      iconColumnResizeByContentCommand: "mdi mdi-arrow-expand-horizontal",
      hideColumnResizeByContentCommand: false,
      hideColumnHideCommand: false,
      hideClearFilterCommand: false,
      hideClearSortCommand: false,
      hideFreezeColumnsCommand: true,
      // opt-in command
      hideSortCommands: false,
      subItemChevronClass: "mdi mdi-chevron-down mdi-rotate-270"
    },
    ignoreAccentOnStringFilterAndSort: false,
    multiColumnSort: true,
    numberedMultiColumnSort: true,
    tristateMultiColumnSort: false,
    sortColNumberInSeparateSpan: true,
    suppressActiveCellChangeOnEdit: false,
    pagination: {
      pageSizes: [10, 15, 20, 25, 30, 40, 50, 75, 100],
      pageSize: 25,
      totalItems: 0
    },
    rowDetailView: {
      collapseAllOnSort: true,
      cssClass: "detail-view-toggle",
      panelRows: 1,
      keyPrefix: "__",
      useRowClick: false,
      saveDetailViewOnScroll: false
    },
    headerRowHeight: 35,
    rowHeight: 35,
    topPanelHeight: 30,
    translationNamespaceSeparator: ":",
    resetFilterSearchValueAfterOnBeforeCancellation: true,
    resizeByContentOnlyOnFirstLoad: true,
    resizeByContentOptions: {
      alwaysRecalculateColumnWidth: false,
      cellPaddingWidthInPx: 14,
      defaultRatioForStringType: 0.88,
      formatterPaddingWidthInPx: 0,
      maxItemToInspectCellContentWidth: 1e3,
      maxItemToInspectSingleColumnWidthByContent: 5e3,
      widthToRemoveFromExceededWidthReadjustment: 50
    },
    rowTopOffsetRenderType: "transform",
    treeDataOptions: {
      exportIndentMarginLeft: 5,
      exportIndentationLeadingChar: "\u034F\u034F\u034F\u034F\u034F\u034F\u034F\u034F\u034F\xB7"
    }
  };
  function pickerHeaderColumnValueExtractor(column, gridOptions) {
    let colName = column?.columnPickerLabel ?? column?.name ?? "";
    if (colName instanceof HTMLElement || colName instanceof DocumentFragment) {
      colName = colName.textContent || "";
    }
    const headerGroup = column?.columnGroup || "";
    const columnGroupSeparator = gridOptions?.columnGroupSeparator ?? " - ";
    if (headerGroup) {
      return headerGroup + columnGroupSeparator + colName;
    }
    return colName;
  }

  // node_modules/@slickgrid-universal/common/dist/aggregators/sumAggregator.js
  var SumAggregator = class {
    constructor(field) {
      this._isInitialized = false;
      this._isTreeAggregator = false;
      this._sum = 0;
      this._itemCount = 0;
      this._type = "sum";
      this._field = field;
    }
    get field() {
      return this._field;
    }
    get isInitialized() {
      return this._isInitialized;
    }
    get type() {
      return this._type;
    }
    init(item, isTreeAggregator = false) {
      this._isTreeAggregator = isTreeAggregator;
      this._isInitialized = true;
      this._sum = 0;
      this._itemCount = 0;
      if (isTreeAggregator) {
        if (!item.__treeTotals) {
          item.__treeTotals = {};
        }
        if (item.__treeTotals[this._type] === void 0) {
          item.__treeTotals[this._type] = {};
          item.__treeTotals.count = {};
        }
        item.__treeTotals["count"][this._field] = 0;
        item.__treeTotals[this._type][this._field] = 0;
      }
    }
    accumulate(item, isTreeParent = false) {
      const val = item?.hasOwnProperty(this._field) ? item[this._field] : null;
      if (!this._isTreeAggregator) {
        if (isNumber(val)) {
          this._sum += parseFloat(val);
        }
      } else {
        if (isTreeParent) {
          if (!item.__treeTotals) {
            item.__treeTotals = {};
          }
          this.addGroupTotalPropertiesWhenNotExist(item.__treeTotals);
          this._sum = parseFloat(item.__treeTotals[this._type][this._field] ?? 0);
          this._itemCount = item.__treeTotals["count"][this._field] ?? 0;
        } else if (isNumber(val)) {
          this._sum = parseFloat(val);
          this._itemCount = 1;
        }
      }
    }
    storeResult(groupTotals) {
      if (!groupTotals || groupTotals[this._type] === void 0) {
        groupTotals[this._type] = {};
      }
      this.addGroupTotalPropertiesWhenNotExist(groupTotals);
      let sum = this._sum;
      let itemCount = this._itemCount;
      if (this._isTreeAggregator) {
        sum += groupTotals[this._type][this._field];
        itemCount += groupTotals["count"][this._field];
        groupTotals["count"][this._field] = itemCount;
      }
      groupTotals[this._type][this._field] = sum;
    }
    addGroupTotalPropertiesWhenNotExist(groupTotals) {
      if (groupTotals[this._type] === void 0) {
        groupTotals[this._type] = {};
      }
      if (this._isTreeAggregator && groupTotals["count"] === void 0) {
        groupTotals["count"] = {};
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/aggregators/maxAggregator.js
  var MaxAggregator = class {
    constructor(field) {
      this._isInitialized = false;
      this._isTreeAggregator = false;
      this._max = null;
      this._type = "max";
      this._field = field;
    }
    get field() {
      return this._field;
    }
    get isInitialized() {
      return this._isInitialized;
    }
    get type() {
      return this._type;
    }
    init(item, isTreeAggregator = false) {
      this._max = null;
      this._isInitialized = true;
      this._isTreeAggregator = isTreeAggregator;
      if (isTreeAggregator) {
        if (!item.__treeTotals) {
          item.__treeTotals = {};
        }
        if (item.__treeTotals[this._type] === void 0) {
          item.__treeTotals[this._type] = {};
        }
        item.__treeTotals[this._type][this._field] = null;
      }
    }
    accumulate(item, isTreeParent = false) {
      const val = item?.hasOwnProperty(this._field) ? item[this._field] : null;
      if (!this._isTreeAggregator) {
        this.keepMaxValueWhenFound(val);
      } else {
        if (isTreeParent) {
          if (!item.__treeTotals) {
            item.__treeTotals = {};
          }
          this.addGroupTotalPropertiesWhenNotExist(item.__treeTotals);
          const parentMax = item.__treeTotals[this._type][this._field] !== null ? parseFloat(item.__treeTotals[this._type][this._field]) : null;
          if (parentMax !== null && isNumber(parentMax) && (this._max === null || parentMax > this._max)) {
            this._max = parentMax;
          }
        } else if (isNumber(val)) {
          this.keepMaxValueWhenFound(val);
        }
      }
    }
    storeResult(groupTotals) {
      let max = this._max;
      this.addGroupTotalPropertiesWhenNotExist(groupTotals);
      if (this._isTreeAggregator && max !== null) {
        const parentMax = groupTotals[this._type][this._field];
        if (isNumber(parentMax) && parentMax > max) {
          max = parentMax;
        }
      }
      groupTotals[this._type][this._field] = max;
    }
    addGroupTotalPropertiesWhenNotExist(groupTotals) {
      if (groupTotals[this._type] === void 0) {
        groupTotals[this._type] = {};
      }
    }
    keepMaxValueWhenFound(val) {
      if (isNumber(val)) {
        if (this._max === null || val > this._max) {
          this._max = parseFloat(val);
        }
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/aggregators/distinctAggregator.js
  var DistinctAggregator = class {
    constructor(field) {
      this._isInitialized = false;
      this._distinctValues = [];
      this._type = "distinct";
      this._field = field;
    }
    get field() {
      return this._field;
    }
    get isInitialized() {
      return this._isInitialized;
    }
    get type() {
      return this._type;
    }
    init(_item, isTreeAggregator = false) {
      this._distinctValues = [];
      this._isInitialized = true;
      if (isTreeAggregator) {
        throw new Error("[Slickgrid-Universal] CloneAggregator is not currently supported for use with Tree Data");
      }
    }
    accumulate(item) {
      const val = item && item.hasOwnProperty(this._field) ? item[this._field] : void 0;
      if (this._distinctValues.indexOf(val) === -1 && val !== void 0) {
        this._distinctValues.push(val);
      }
    }
    storeResult(groupTotals) {
      if (!groupTotals || groupTotals[this._type] === void 0) {
        groupTotals[this._type] = {};
      }
      groupTotals[this._type][this._field] = this._distinctValues;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/aggregators/minAggregator.js
  var MinAggregator = class {
    constructor(field) {
      this._isInitialized = false;
      this._isTreeAggregator = false;
      this._min = null;
      this._type = "min";
      this._field = field;
    }
    get field() {
      return this._field;
    }
    get isInitialized() {
      return this._isInitialized;
    }
    get type() {
      return this._type;
    }
    init(item, isTreeAggregator = false) {
      this._min = null;
      this._isInitialized = true;
      this._isTreeAggregator = isTreeAggregator;
      if (isTreeAggregator) {
        if (!item.__treeTotals) {
          item.__treeTotals = {};
        }
        if (item.__treeTotals[this._type] === void 0) {
          item.__treeTotals[this._type] = {};
        }
        item.__treeTotals[this._type][this._field] = null;
      }
    }
    accumulate(item, isTreeParent = false) {
      const val = item?.hasOwnProperty(this._field) ? item[this._field] : null;
      if (!this._isTreeAggregator) {
        this.keepMinValueWhenFound(val);
      } else {
        if (isTreeParent) {
          if (!item.__treeTotals) {
            item.__treeTotals = {};
          }
          this.addGroupTotalPropertiesWhenNotExist(item.__treeTotals);
          const parentMin = item.__treeTotals[this._type][this._field] !== null ? parseFloat(item.__treeTotals[this._type][this._field]) : null;
          if (parentMin !== null && isNumber(parentMin) && (this._min === null || parentMin < this._min)) {
            this._min = parentMin;
          }
        } else if (isNumber(val)) {
          this.keepMinValueWhenFound(val);
        }
      }
    }
    storeResult(groupTotals) {
      let min = this._min;
      this.addGroupTotalPropertiesWhenNotExist(groupTotals);
      if (this._isTreeAggregator && min !== null) {
        const parentMin = groupTotals[this._type][this._field];
        if (isNumber(parentMin) && parentMin < min) {
          min = parentMin;
        }
      }
      groupTotals[this._type][this._field] = min;
    }
    addGroupTotalPropertiesWhenNotExist(groupTotals) {
      if (groupTotals[this._type] === void 0) {
        groupTotals[this._type] = {};
      }
    }
    keepMinValueWhenFound(val) {
      if (isNumber(val)) {
        if (this._min === null || val < this._min) {
          this._min = parseFloat(val);
        }
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/aggregators/countAggregator.js
  var CountAggregator = class {
    constructor(field) {
      this._isInitialized = false;
      this._isTreeAggregator = false;
      this._count = 0;
      this._type = "count";
      this._field = field;
    }
    get field() {
      return this._field;
    }
    get isInitialized() {
      return this._isInitialized;
    }
    get type() {
      return this._type;
    }
    init(item, isTreeAggregator = false) {
      this._count = 0;
      this._isInitialized = true;
      this._isTreeAggregator = isTreeAggregator;
      if (isTreeAggregator) {
        if (!item.__treeTotals) {
          item.__treeTotals = {};
        }
        if (item.__treeTotals[this._type] === void 0) {
          item.__treeTotals[this._type] = {};
        }
        item.__treeTotals[this._type][this._field] = 0;
      }
    }
    accumulate(item, isTreeParent = false) {
      const val = item?.hasOwnProperty(this._field) ? item[this._field] : null;
      if (this._isTreeAggregator) {
        if (isTreeParent) {
          if (!item.__treeTotals) {
            item.__treeTotals = {};
          }
          if (item.__treeTotals[this._type] === void 0) {
            item.__treeTotals[this._type] = {};
          }
          this._count = item.__treeTotals[this._type][this._field] ?? 0;
        } else if (isNumber(val)) {
          this._count = 1;
        }
      }
    }
    storeResult(groupTotals) {
      if (!groupTotals || groupTotals[this._type] === void 0) {
        groupTotals[this._type] = {};
      }
      let itemCount = this._count;
      if (this._isTreeAggregator) {
        itemCount += groupTotals[this._type][this._field];
      } else {
        itemCount = groupTotals.group?.rows?.length ?? 0;
      }
      groupTotals[this._type][this._field] = itemCount;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/aggregators/cloneAggregator.js
  var CloneAggregator = class {
    constructor(field) {
      this._isInitialized = false;
      this._data = "";
      this._type = "clone";
      this._field = field;
    }
    get field() {
      return this._field;
    }
    get isInitialized() {
      return this._isInitialized;
    }
    get type() {
      return this._type;
    }
    init(_item, isTreeAggregator = false) {
      this._data = "";
      this._isInitialized = true;
      if (isTreeAggregator) {
        throw new Error("[Slickgrid-Universal] CloneAggregator is not currently supported for use with Tree Data");
      }
    }
    accumulate(item) {
      const val = item && item.hasOwnProperty(this._field) ? item[this._field] : null;
      if (val !== null && val !== "") {
        this._data = val;
      }
    }
    storeResult(groupTotals) {
      if (!groupTotals || groupTotals[this._type] === void 0) {
        groupTotals[this._type] = {};
      }
      groupTotals[this._type][this._field] = this._data;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/aggregators/avgAggregator.js
  var AvgAggregator = class {
    constructor(field) {
      this._isInitialized = false;
      this._isTreeAggregator = false;
      this._nonNullCount = 0;
      this._sum = 0;
      this._type = "avg";
      this._field = field;
    }
    get field() {
      return this._field;
    }
    get isInitialized() {
      return this._isInitialized;
    }
    get type() {
      return this._type;
    }
    init(item, isTreeAggregator = false) {
      this._sum = 0;
      this._nonNullCount = 0;
      this._isInitialized = true;
      this._isTreeAggregator = isTreeAggregator;
      if (isTreeAggregator) {
        if (!item.__treeTotals) {
          item.__treeTotals = {};
        }
        if (item.__treeTotals[this._type] === void 0) {
          item.__treeTotals[this._type] = {};
          item.__treeTotals.sum = {};
          item.__treeTotals.count = {};
        }
        item.__treeTotals[this._type][this._field] = 0;
        item.__treeTotals["count"][this._field] = 0;
        item.__treeTotals["sum"][this._field] = 0;
      }
    }
    accumulate(item, isTreeParent = false) {
      const val = item?.hasOwnProperty(this._field) ? item[this._field] : null;
      if (!this._isTreeAggregator) {
        if (isNumber(val)) {
          this._nonNullCount++;
          this._sum += parseFloat(val);
        }
      } else {
        if (isTreeParent) {
          if (!item.__treeTotals) {
            item.__treeTotals = {};
          }
          this.addGroupTotalPropertiesWhenNotExist(item.__treeTotals);
          this._sum = parseFloat(item.__treeTotals["sum"][this._field] ?? 0);
          this._nonNullCount = item.__treeTotals["count"][this._field] ?? 0;
        } else if (isNumber(val)) {
          this._sum = parseFloat(val);
          this._nonNullCount = 1;
        }
      }
    }
    storeResult(groupTotals) {
      let sum = this._sum;
      let itemCount = this._nonNullCount;
      this.addGroupTotalPropertiesWhenNotExist(groupTotals);
      if (this._isTreeAggregator) {
        sum += groupTotals["sum"][this._field];
        itemCount += groupTotals["count"][this._field];
        groupTotals["sum"][this._field] = sum;
        groupTotals["count"][this._field] = itemCount;
      }
      if (itemCount !== 0) {
        groupTotals[this._type][this._field] = itemCount === 0 ? sum : sum / itemCount;
      }
    }
    addGroupTotalPropertiesWhenNotExist(groupTotals) {
      if (groupTotals[this._type] === void 0) {
        groupTotals[this._type] = {};
      }
      if (this._isTreeAggregator && groupTotals["sum"] === void 0) {
        groupTotals["sum"] = {};
      }
      if (this._isTreeAggregator && groupTotals["count"] === void 0) {
        groupTotals["count"] = {};
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/aggregators/aggregators.index.js
  var Aggregators = {
    /** Average Aggregator which calculate the average of a given group */
    Avg: AvgAggregator,
    /** Clone Aggregator will simply clone (copy) over the last defined value of a given group */
    Clone: CloneAggregator,
    /** Count Aggregator will count the number of rows in the group */
    Count: CountAggregator,
    /** Distinct Aggregator will return an array of distinct values found inside the given group */
    Distinct: DistinctAggregator,
    /** Minimum Aggregator which will find the minimum value inside the given group */
    Min: MinAggregator,
    /** Maximum Aggregator which will find the maximum value inside the given group */
    Max: MaxAggregator,
    /** Sum Aggregator which calculate the sum of a given group */
    Sum: SumAggregator
  };

  // node_modules/@slickgrid-universal/common/dist/editorValidators/textValidator.js
  function textValidator(inputValue, options) {
    const errorMsg = options.errorMessage;
    const isRequired = options.required;
    const minLength = options.minLength;
    const maxLength = options.maxLength;
    const operatorConditionalType = options.operatorConditionalType || "inclusive";
    const mapValidation = {
      "{{minLength}}": minLength,
      "{{maxLength}}": maxLength
    };
    let isValid = true;
    let outputMsg = "";
    const inputValueLength = inputValue?.length ?? 0;
    if (options.validator) {
      return options.validator(inputValue, options.editorArgs);
    }
    if (isRequired && inputValue === "") {
      isValid = false;
      outputMsg = errorMsg || Constants.VALIDATION_REQUIRED_FIELD;
    } else if (minLength !== void 0 && maxLength !== void 0 && (operatorConditionalType === "exclusive" && (inputValueLength <= minLength || inputValueLength >= maxLength) || operatorConditionalType === "inclusive" && (inputValueLength < minLength || inputValueLength > maxLength))) {
      isValid = false;
      outputMsg = errorMsg || Constants.VALIDATION_EDITOR_TEXT_LENGTH_BETWEEN.replace(/{{minLength}}|{{maxLength}}/gi, (matched) => mapValidation[matched]);
    } else if (minLength !== void 0 && inputValueLength !== null && (operatorConditionalType === "exclusive" && inputValueLength <= minLength || operatorConditionalType === "inclusive" && inputValueLength !== null && inputValueLength < minLength)) {
      isValid = false;
      const defaultErrorMsg = operatorConditionalType === "inclusive" ? Constants.VALIDATION_EDITOR_TEXT_MIN_LENGTH_INCLUSIVE : Constants.VALIDATION_EDITOR_TEXT_MIN_LENGTH;
      outputMsg = errorMsg || defaultErrorMsg.replace(/{{minLength}}/gi, (matched) => mapValidation[matched]);
    } else if (maxLength !== void 0 && inputValueLength !== null && (operatorConditionalType === "exclusive" && inputValueLength >= maxLength || operatorConditionalType === "inclusive" && inputValueLength !== null && inputValueLength > maxLength)) {
      isValid = false;
      const defaultErrorMsg = operatorConditionalType === "inclusive" ? Constants.VALIDATION_EDITOR_TEXT_MAX_LENGTH_INCLUSIVE : Constants.VALIDATION_EDITOR_TEXT_MAX_LENGTH;
      outputMsg = errorMsg || defaultErrorMsg.replace(/{{maxLength}}/gi, (matched) => mapValidation[matched]);
    }
    return { valid: isValid, msg: outputMsg };
  }

  // node_modules/@slickgrid-universal/common/dist/editors/autocompleterEditor.js
  var MIN_LENGTH = 3;
  var AutocompleterEditor = class {
    constructor(args) {
      this.args = args;
      this._isValueTouched = false;
      this._lastTriggeredByClearInput = false;
      this.disabled = false;
      this.forceUserInput = false;
      this.finalCollection = [];
      this.grid = args.grid;
      this._bindEventService = new BindingEventService();
      if (this.gridOptions?.translater) {
        this._translater = this.gridOptions.translater;
      }
      this._locales = this.gridOptions?.locales || Constants.locales;
      this.init();
    }
    /** Getter for the Autocomplete Option */
    get autocompleterOptions() {
      return this._autocompleterOptions || {};
    }
    /** Getter of the Collection */
    get collection() {
      return this.columnEditor?.collection ?? [];
    }
    /** Getter for the Editor DOM Element */
    get editorDomElement() {
      return this._inputElm;
    }
    /** Getter for the Final Collection used in the AutoCompleted Source (this may vary from the "collection" especially when providing a customStructure) */
    get elementCollection() {
      return this._elementCollection;
    }
    /** Get Column Definition object */
    get columnDef() {
      return this.args.column;
    }
    /** Get Column Editor object */
    get columnEditor() {
      return this.columnDef?.editor || {};
    }
    /** Getter for the Custom Structure if exist */
    get customStructure() {
      let customStructure = this.columnEditor?.customStructure;
      const columnType = this.columnEditor?.type ?? this.columnDef?.type;
      if (!customStructure && columnType === "object" && this.columnDef?.dataKey && this.columnDef?.labelKey) {
        customStructure = {
          label: this.columnDef.labelKey,
          value: this.columnDef.dataKey
        };
      }
      return customStructure;
    }
    /** Getter for the item data context object */
    get dataContext() {
      return this.args.item;
    }
    get editorOptions() {
      return { ...this.gridOptions.defaultEditorOptions?.autocompleter, ...this.columnEditor?.options };
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this.grid?.getOptions() ?? {};
    }
    /** Kraaden AutoComplete instance */
    get instance() {
      return this._instance;
    }
    get hasAutoCommitEdit() {
      return this.gridOptions.autoCommitEdit ?? false;
    }
    /** Get the Validator function, can be passed in Editor property or Column Definition */
    get validator() {
      return this.columnEditor?.validator ?? this.columnDef?.validator;
    }
    init() {
      this.labelName = this.customStructure?.label ?? "label";
      this.valueName = this.customStructure?.value ?? "value";
      this.labelPrefixName = this.customStructure?.labelPrefix ?? "labelPrefix";
      this.labelSuffixName = this.customStructure?.labelSuffix ?? "labelSuffix";
      let newCollection = this.columnEditor.collection;
      if (this.columnEditor?.collectionAsync && !newCollection) {
        newCollection = [];
      }
      this.renderDomElement(newCollection);
      if (this.disabled || this.columnEditor?.collectionAsync && Array.isArray(newCollection) && newCollection.length === 0) {
        this.disable(true);
      }
    }
    destroy() {
      clearTimeout(this._timer1);
      clearTimeout(this._timer2);
      this._bindEventService.unbindAll();
      if (typeof this._instance?.destroy === "function") {
        this._instance.destroy();
      }
      if (this._elementCollection) {
        this._elementCollection.length = 0;
      }
      this._inputElm?.remove();
    }
    disable(isDisabled = true) {
      const prevIsDisabled = this.disabled;
      this.disabled = isDisabled;
      if (this._inputElm) {
        if (isDisabled) {
          this._inputElm.disabled = true;
          const currentValue = this.getValue();
          if (prevIsDisabled !== isDisabled && this.args.isCompositeEditor && currentValue !== "") {
            this.clear(true);
          }
        } else {
          this._inputElm.disabled = false;
        }
      }
    }
    focus() {
      this.grid.focus("internal");
      if (this._inputElm) {
        this._inputElm.focus();
        this._inputElm.select();
      }
    }
    show() {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
    }
    getValue() {
      return this._inputElm.value;
    }
    setValue(inputValue, isApplyingValue = false, triggerOnCompositeEditorChange = true) {
      this._inputElm.value = inputValue?.hasOwnProperty(this.labelName) ? inputValue[this.labelName] : inputValue;
      if (isApplyingValue) {
        this._currentValue = inputValue;
        this._defaultTextValue = typeof inputValue === "string" ? inputValue : inputValue?.[this.labelName] ?? "";
        this.applyValue(this.args.item, this.serializeValue());
        const compositeEditorOptions = this.args.compositeEditorOptions;
        if (compositeEditorOptions && triggerOnCompositeEditorChange) {
          this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "system");
        }
      }
    }
    applyValue(item, state2) {
      let newValue = state2;
      const fieldName = this.columnDef?.field;
      if (fieldName !== void 0) {
        if (Array.isArray(this.collection) && this.collection.length > 0) {
          newValue = findOrDefault(this.collection, (collectionItem) => {
            if (collectionItem && isObject(state2) && collectionItem.hasOwnProperty(this.valueName)) {
              return collectionItem[this.valueName].toString() === (state2.hasOwnProperty(this.valueName) && state2[this.valueName].toString());
            } else if (collectionItem && typeof state2 === "string" && collectionItem.hasOwnProperty(this.valueName)) {
              return collectionItem[this.valueName].toString() === state2;
            }
            return collectionItem?.toString() === state2;
          }, "");
        }
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const validation = this.validate(null, newValue);
        newValue = validation?.valid ? newValue : "";
        if (isComplexObject) {
          const objectPath = this.columnEditor?.complexObjectPath ?? fieldName ?? "";
          setDeepValue(item, objectPath, newValue);
        } else {
          item[fieldName] = newValue;
        }
      }
    }
    isValueChanged() {
      const elmValue = this._inputElm.value;
      const lastEventKey = this._lastInputKeyEvent?.key;
      if (this.columnEditor?.alwaysSaveOnEnterKey && lastEventKey === "Enter") {
        return true;
      }
      const isValueChanged = !(elmValue === "" && (this._defaultTextValue === null || this._defaultTextValue === void 0)) && elmValue !== this._defaultTextValue;
      return this._lastTriggeredByClearInput || isValueChanged;
    }
    isValueTouched() {
      return this._isValueTouched;
    }
    loadValue(item) {
      const fieldName = this.columnDef?.field;
      if (item && fieldName !== void 0) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const data = isComplexObject ? getDescendantProperty(item, fieldName) : item[fieldName];
        this._currentValue = data;
        this._originalValue = data;
        this._defaultTextValue = typeof data === "string" ? data : data?.[this.labelName] ?? "";
        this._inputElm.value = this._defaultTextValue;
        this._inputElm.select();
      }
    }
    clear(clearByDisableCommand = false) {
      if (this._inputElm) {
        this._currentValue = "";
        this._defaultTextValue = "";
        this.setValue("", true);
      }
      this._isValueTouched = true;
      this._lastTriggeredByClearInput = true;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions) {
        const shouldDeleteFormValue = !clearByDisableCommand;
        this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "user", shouldDeleteFormValue);
      } else {
        this.save();
      }
    }
    /**
     * You can reset the input value,
     * when no value is provided it will use the original value to reset (could be useful with Composite Editor Modal with edit/clone)
     */
    reset(value, triggerCompositeEventWhenExist = true, clearByDisableCommand = false) {
      const inputValue = value ?? this._originalValue ?? "";
      if (this._inputElm) {
        this._currentValue = inputValue;
        this._defaultTextValue = typeof inputValue === "string" ? inputValue : inputValue?.[this.labelName] ?? "";
        this._inputElm.value = this._defaultTextValue;
      }
      this._isValueTouched = false;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions && triggerCompositeEventWhenExist) {
        const shouldDeleteFormValue = !clearByDisableCommand;
        this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "user", shouldDeleteFormValue);
      }
    }
    save() {
      const validation = this.validate();
      const isValid = validation?.valid ?? false;
      if (this.hasAutoCommitEdit && isValid) {
        this.grid.getEditorLock().commitCurrentEdit();
      } else {
        const navigateDown = this._lastInputKeyEvent?.key !== "Enter";
        this.args.commitChanges(navigateDown);
      }
    }
    serializeValue() {
      if (this._inputElm && this.editorOptions.forceUserInput) {
        const minLength = this.editorOptions?.minLength ?? MIN_LENGTH;
        this._currentValue = this._inputElm.value.length > minLength ? this._inputElm.value : this._currentValue;
      }
      if (this.customStructure && this._currentValue && this._currentValue.hasOwnProperty(this.valueName) && this.columnDef?.type !== "object" && this.columnEditor?.type !== "object") {
        return this._currentValue[this.valueName];
      } else if (this._currentValue && this._currentValue.value !== void 0) {
        if (this.columnDef?.type === "object" || this.columnEditor?.type === "object") {
          return {
            [this.labelName]: this._currentValue.label,
            [this.valueName]: this._currentValue.value
          };
        }
        return this._currentValue.value;
      }
      return this._currentValue;
    }
    validate(_targetElm, options) {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
      if (this.disabled) {
        return { valid: true, msg: "" };
      }
      const val = options?.inputValue ?? this._inputElm?.value;
      return textValidator(val, {
        editorArgs: this.args,
        errorMessage: this.columnEditor.errorMessage,
        minLength: this.columnEditor.minLength,
        maxLength: this.columnEditor.maxLength,
        operatorConditionalType: this.columnEditor.operatorConditionalType,
        required: this.args.isCompositeEditor ? false : this.columnEditor.required,
        validator: this.validator
      });
    }
    //
    // protected functions
    // ------------------
    /** when it's a Composite Editor, we'll check if the Editor is editable (by checking onBeforeEditCell) and if not Editable we'll disable the Editor */
    applyInputUsabilityState() {
      const activeCell = this.grid.getActiveCell();
      const isCellEditable = this.grid.onBeforeEditCell.notify({
        ...activeCell,
        item: this.dataContext,
        column: this.args.column,
        grid: this.grid,
        target: "composite",
        compositeEditorOptions: this.args.compositeEditorOptions
      }).getReturnValue();
      this.disable(isCellEditable === false);
    }
    handleChangeOnCompositeEditor(event, compositeEditorOptions, triggeredBy = "user", isCalledByClearValue = false) {
      const activeCell = this.grid.getActiveCell();
      const column = this.args.column;
      const columnId = this.columnDef?.id ?? "";
      const item = this.dataContext;
      const grid = this.grid;
      const newValue = this.serializeValue();
      if (this.validate().valid) {
        this.applyValue(this.dataContext, newValue);
      }
      this.applyValue(compositeEditorOptions.formValues, newValue);
      const isExcludeDisabledFieldFormValues = this.gridOptions?.compositeEditorOptions?.excludeDisabledFieldFormValues ?? false;
      if (isCalledByClearValue || this.disabled && isExcludeDisabledFieldFormValues && compositeEditorOptions.formValues.hasOwnProperty(columnId)) {
        delete compositeEditorOptions.formValues[columnId];
      }
      grid.onCompositeEditorChange.notify({
        ...activeCell,
        item,
        grid,
        column,
        formValues: compositeEditorOptions.formValues,
        editors: compositeEditorOptions.editors,
        triggeredBy
      }, new SlickEventData(event));
    }
    // this function should be protected but for unit tests purposes we'll make it public until a better solution is found
    // a better solution would be to get the autocomplete DOM element to work with selection but I couldn't find how to do that in Vitest
    handleSelect(item) {
      if (item !== void 0) {
        const selectedItem = item;
        this._currentValue = selectedItem;
        this._isValueTouched = true;
        const compositeEditorOptions = this.args.compositeEditorOptions;
        const hasCustomRenderItemCallback = this.editorOptions?.renderItem ?? false;
        const itemLabel = typeof selectedItem === "string" ? selectedItem : hasCustomRenderItemCallback ? selectedItem[this.labelName] : selectedItem.label;
        this.setValue(itemLabel);
        if (compositeEditorOptions) {
          this.handleChangeOnCompositeEditor(null, compositeEditorOptions);
        } else {
          this.save();
        }
        if (typeof this.editorOptions.onSelectItem === "function") {
          const { row, cell } = this.grid.getActiveCell() || {};
          this.editorOptions.onSelectItem(item, row, cell, this.args.column, this.args.item);
        }
        clearTimeout(this._timer1);
        this._timer1 = setTimeout(() => this._lastTriggeredByClearInput = false);
      }
      return false;
    }
    renderRegularItem(item) {
      const itemLabel = typeof item === "string" ? item : item?.label ?? "";
      return createDomElement("div", { textContent: itemLabel || "" });
    }
    renderCustomItem(item) {
      const templateString = this._autocompleterOptions?.renderItem?.templateCallback(item) ?? "";
      const tmpElm = document.createElement("div");
      applyHtmlToElement(tmpElm, templateString, this.gridOptions);
      return tmpElm;
    }
    renderCollectionItem(item) {
      const isRenderHtmlEnabled = this.columnEditor?.enableRenderHtml ?? false;
      const prefixText = item.labelPrefix || "";
      const labelText = item.label || "";
      const suffixText = item.labelSuffix || "";
      const finalText = prefixText + labelText + suffixText;
      const sanitizedText = this.grid.sanitizeHtmlString(finalText) || "";
      const div = document.createElement("div");
      div[isRenderHtmlEnabled ? "innerHTML" : "textContent"] = sanitizedText;
      return div;
    }
    renderDomElement(collection) {
      const columnId = this.columnDef?.id ?? "";
      const placeholder = this.columnEditor?.placeholder ?? "";
      const title = this.columnEditor?.title ?? "";
      this._editorInputGroupElm = createDomElement("div", { className: "autocomplete-container input-group" });
      const closeButtonGroupElm = createDomElement("span", {
        className: "input-group-btn input-group-append",
        dataset: { clear: "" }
      });
      this._clearButtonElm = createDomElement("button", { type: "button", className: "btn btn-default btn-clear" });
      this._clearButtonElm.appendChild(createDomElement("i", { className: "sgi icon-clear" }));
      this._inputElm = createDomElement("input", {
        type: "text",
        placeholder,
        title,
        autocomplete: "off",
        ariaAutoComplete: "none",
        className: `autocomplete form-control editor-text input-group-editor editor-${columnId}`,
        dataset: { input: "" }
      }, this._editorInputGroupElm);
      this._editorInputGroupElm.appendChild(document.createElement("span"));
      if (!this.editorOptions?.hideClearButton) {
        closeButtonGroupElm.appendChild(this._clearButtonElm);
        this._editorInputGroupElm.appendChild(closeButtonGroupElm);
        this._bindEventService.bind(this._clearButtonElm, "click", () => this.clear());
      }
      this._bindEventService.bind(this._inputElm, "focus", () => this._inputElm?.select());
      this._bindEventService.bind(this._inputElm, "keydown", ((event) => {
        this._lastInputKeyEvent = event;
        if (event.key === "ArrowLeft" || event.key === "ArrowRight" || event.key === "Home" || event.key === "End") {
          event.stopImmediatePropagation();
        }
        if (event.key === "Enter" && event.target.value === "" && this.columnEditor.alwaysSaveOnEnterKey) {
          this._currentValue = null;
        }
      }));
      let finalCollection = collection;
      if (finalCollection && this.columnEditor?.collectionOverride) {
        const overrideArgs = {
          column: this.columnDef,
          dataContext: this.dataContext,
          grid: this.grid,
          originalCollections: this.collection
        };
        if (this.args.compositeEditorOptions) {
          const { formValues, modalType } = this.args.compositeEditorOptions;
          overrideArgs.compositeEditorOptions = { formValues, modalType };
        }
        finalCollection = this.columnEditor.collectionOverride(finalCollection, overrideArgs);
      }
      if (finalCollection) {
        this.finalCollection = finalCollection;
      }
      if (Array.isArray(finalCollection)) {
        if (this.collection.every((x2) => isPrimitiveValue(x2))) {
          finalCollection = finalCollection.map((c) => ({ label: c, value: c }));
        } else {
          finalCollection = finalCollection.map((item) => ({
            label: item?.[this.labelName],
            value: item?.[this.valueName],
            labelPrefix: item?.[this.labelPrefixName] ?? "",
            labelSuffix: item?.[this.labelSuffixName] ?? ""
          }));
        }
        this._elementCollection = finalCollection;
      }
      this._autocompleterOptions = {
        input: this._inputElm,
        debounceWaitMs: 200,
        className: `slick-autocomplete ${this.editorOptions?.className ?? ""}`.trim(),
        emptyMsg: this.gridOptions.enableTranslate && this._translater?.translate ? this._translater.translate("NO_ELEMENTS_FOUND") : this._locales?.TEXT_NO_ELEMENTS_FOUND ?? "No elements found",
        customize: (_input, _inputRect, container) => {
          container.style.width = "";
        },
        onSelect: this.handleSelect.bind(this),
        ...this.editorOptions
      };
      if (this.gridOptions?.darkMode) {
        this._autocompleterOptions.className += " slick-dark-mode";
      }
      this.autocompleterOptions.className = classNameToList(this.autocompleterOptions.className).join(" ");
      if (this._autocompleterOptions.renderItem?.layout) {
        this._autocompleterOptions.className += ` autocomplete-custom-${toKebabCase(this._autocompleterOptions.renderItem.layout)}`;
        this._autocompleterOptions.render = this.renderCustomItem.bind(this);
      } else if (Array.isArray(collection)) {
        this._autocompleterOptions.render = this._autocompleterOptions.render?.bind(this) ?? this.renderCollectionItem.bind(this);
      } else if (!this._autocompleterOptions.render) {
        this._autocompleterOptions.render = this.renderRegularItem.bind(this);
      }
      if (this._autocompleterOptions?.fetch) {
        addAutocompleteLoadingByOverridingFetch(this._inputElm, this._autocompleterOptions);
        this._instance = autocomplete_es_default(this._autocompleterOptions);
      } else {
        this._instance = autocomplete_es_default({
          ...this._autocompleterOptions,
          fetch: (searchTerm, updateCallback) => {
            if (finalCollection) {
              updateCallback(finalCollection.filter((c) => {
                const label = (typeof c === "string" ? c : c?.label) || "";
                return label.toLowerCase().includes(searchTerm.toLowerCase());
              }));
            }
          }
        });
      }
      this.args.container.appendChild(this._editorInputGroupElm);
      this.columnEditor.onInstantiated?.(this._instance);
      if (!this.args.isCompositeEditor) {
        clearTimeout(this._timer2);
        this._timer2 = setTimeout(() => this.focus(), 50);
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/checkboxEditor.js
  var CheckboxEditor = class {
    constructor(args) {
      this.args = args;
      this._isValueTouched = false;
      this.disabled = false;
      this.grid = args.grid;
      this.gridOptions = this.grid.getOptions() || {};
      this._bindEventService = new BindingEventService();
      this.init();
    }
    /** Get Column Definition object */
    get columnDef() {
      return this.args.column;
    }
    /** Get Column Editor object */
    get columnEditor() {
      return this.columnDef?.editor || {};
    }
    /** Getter for the item data context object */
    get dataContext() {
      return this.args.item;
    }
    /** Getter for the Editor DOM Element */
    get editorDomElement() {
      return this._input;
    }
    get hasAutoCommitEdit() {
      return this.gridOptions.autoCommitEdit ?? false;
    }
    /** Get the Validator function, can be passed in Editor property or Column Definition */
    get validator() {
      return this.columnEditor?.validator ?? this.columnDef?.validator;
    }
    init() {
      const columnId = this.columnDef?.id ?? "";
      const compositeEditorOptions = this.args.compositeEditorOptions;
      this._checkboxContainerElm = createDomElement("div", { className: `checkbox-editor-container editor-${columnId}` });
      this._input = createDomElement("input", {
        type: "checkbox",
        value: "true",
        ariaLabel: this.columnEditor?.ariaLabel ?? `${toSentenceCase(columnId + "")} Checkbox Editor`,
        className: `editor-checkbox editor-${columnId}`,
        title: this.columnEditor?.title ?? ""
      });
      const cellContainer = this.args?.container;
      if (cellContainer && typeof cellContainer.appendChild === "function") {
        if (compositeEditorOptions) {
          this._checkboxContainerElm.appendChild(this._input);
          cellContainer.appendChild(this._checkboxContainerElm);
        } else {
          cellContainer.appendChild(this._input);
        }
      }
      if (this.hasAutoCommitEdit && !compositeEditorOptions) {
        this._bindEventService.bind(this._input, "click", () => {
          this._isValueTouched = true;
          this.save();
        });
      }
      if (compositeEditorOptions) {
        this._bindEventService.bind(this._input, "change", ((event) => {
          this._isValueTouched = true;
          this.handleChangeOnCompositeEditor(event, compositeEditorOptions);
        }));
      } else {
        this.focus();
      }
    }
    destroy() {
      this._bindEventService.unbindAll();
      this._input?.remove();
    }
    disable(isDisabled = true) {
      const prevIsDisabled = this.disabled;
      this.disabled = isDisabled;
      if (this._input) {
        if (isDisabled) {
          this._input.setAttribute("disabled", "disabled");
          this._checkboxContainerElm.classList.add("disabled");
          const currentValue = this.getValue();
          if (prevIsDisabled !== isDisabled && this.args?.compositeEditorOptions && currentValue !== false) {
            this.reset(false, true, true);
          }
        } else {
          this._input.removeAttribute("disabled");
          this._checkboxContainerElm.classList.remove("disabled");
        }
      }
    }
    focus() {
      this.grid.focus("internal");
      this._input?.focus();
    }
    /** pre-click, when enabled, will simply toggle the checkbox without requiring to double-click */
    preClick() {
      if (this._input) {
        this._input.checked = !this._input.checked;
      }
    }
    show() {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
    }
    getValue() {
      return this._input?.checked ?? false;
    }
    setValue(val, isApplyingValue = false, triggerOnCompositeEditorChange = true) {
      const isChecked = val ? true : false;
      if (this._input) {
        this._input.checked = isChecked;
      }
      if (isApplyingValue) {
        this.applyValue(this.args.item, this.serializeValue());
        const compositeEditorOptions = this.args.compositeEditorOptions;
        if (compositeEditorOptions && triggerOnCompositeEditorChange) {
          this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "system");
        }
      }
    }
    applyValue(item, state2) {
      const fieldName = this.columnDef?.field;
      if (fieldName !== void 0) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const validation = this.validate(null, state2);
        const newValue = validation && validation.valid ? state2 : "";
        if (isComplexObject) {
          const objectPath = this.columnEditor?.complexObjectPath ?? fieldName ?? "";
          setDeepValue(item, objectPath, newValue);
        } else {
          item[fieldName] = newValue;
        }
      }
    }
    isValueChanged() {
      return this.serializeValue() !== this._originalValue;
    }
    isValueTouched() {
      return this._isValueTouched;
    }
    loadValue(item) {
      const fieldName = this.columnDef?.field;
      if (item && fieldName !== void 0 && this._input) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const value = isComplexObject ? getDescendantProperty(item, fieldName) : item[fieldName];
        this._originalValue = value;
        this._input.checked = !!this._originalValue;
      }
    }
    /**
     * You can reset or clear the input value,
     * when no value is provided it will use the original value to reset (could be useful with Composite Editor Modal with edit/clone)
     */
    reset(value, triggerCompositeEventWhenExist = true, clearByDisableCommand = false) {
      const inputValue = value ?? this._originalValue ?? false;
      if (this._input) {
        this._originalValue = inputValue;
        this._input.checked = !!inputValue;
      }
      this._isValueTouched = false;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions && triggerCompositeEventWhenExist) {
        const shouldDeleteFormValue = !clearByDisableCommand;
        this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "user", shouldDeleteFormValue);
      }
    }
    save() {
      const validation = this.validate();
      const isValid = validation && validation.valid || false;
      if (this.hasAutoCommitEdit && isValid) {
        this.grid.getEditorLock().commitCurrentEdit();
      } else {
        this.args.commitChanges();
      }
    }
    serializeValue() {
      return this._input?.checked ?? false;
    }
    validate(_targetElm, options) {
      const isRequired = this.args?.compositeEditorOptions ? false : this.columnEditor.required;
      const isChecked = options?.inputValue ?? this._input?.checked;
      const errorMsg = this.columnEditor.errorMessage;
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
      if (this.disabled) {
        return { valid: true, msg: "" };
      }
      if (this.validator) {
        return this.validator(isChecked, this.args);
      }
      if (isRequired && !isChecked) {
        return {
          valid: false,
          msg: errorMsg || Constants.VALIDATION_REQUIRED_FIELD
        };
      }
      return {
        valid: true,
        msg: null
      };
    }
    // --
    // protected functions
    // ------------------
    /** when it's a Composite Editor, we'll check if the Editor is editable (by checking onBeforeEditCell) and if not Editable we'll disable the Editor */
    applyInputUsabilityState() {
      const activeCell = this.grid.getActiveCell();
      const isCellEditable = this.grid.onBeforeEditCell.notify({
        ...activeCell,
        item: this.dataContext,
        column: this.args.column,
        grid: this.grid,
        target: "composite",
        compositeEditorOptions: this.args.compositeEditorOptions
      }).getReturnValue();
      this.disable(isCellEditable === false);
    }
    handleChangeOnCompositeEditor(event, compositeEditorOptions, triggeredBy = "user", isCalledByClearValue = false) {
      const activeCell = this.grid.getActiveCell();
      const column = this.args.column;
      const columnId = this.columnDef?.id ?? "";
      const item = this.dataContext;
      const grid = this.grid;
      const newValue = this.serializeValue();
      if (this.validate().valid) {
        this.applyValue(this.dataContext, newValue);
      }
      this.applyValue(compositeEditorOptions.formValues, newValue);
      const isExcludeDisabledFieldFormValues = this.gridOptions?.compositeEditorOptions?.excludeDisabledFieldFormValues ?? false;
      if (isCalledByClearValue || this.disabled && isExcludeDisabledFieldFormValues && compositeEditorOptions.formValues.hasOwnProperty(columnId)) {
        delete compositeEditorOptions.formValues[columnId];
      }
      grid.onCompositeEditorChange.notify({
        ...activeCell,
        item,
        grid,
        column,
        formValues: compositeEditorOptions.formValues,
        editors: compositeEditorOptions.editors,
        triggeredBy
      }, new SlickEventData(event));
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/dateEditor.js
  var DateEditor = class {
    constructor(args) {
      this.args = args;
      this._isValueTouched = false;
      this._lastClickIsDate = false;
      this._lastTriggeredByClearDate = false;
      this.hasTimePicker = false;
      this.disabled = false;
      this.grid = args.grid;
      this.gridOptions = this.grid.getOptions() || {};
      if (this.gridOptions?.translater) {
        this._translaterService = this.gridOptions.translater;
      }
      this._bindEventService = new BindingEventService();
      this.init();
    }
    /** Get Column Definition object */
    get columnDef() {
      return this.args.column;
    }
    /** Get Column Editor object */
    get columnEditor() {
      return this.columnDef?.editor || {};
    }
    /** Getter for the item data context object */
    get dataContext() {
      return this.args.item;
    }
    /** Getter for the Editor DOM Element */
    get editorDomElement() {
      return this._inputElm;
    }
    /** Get options passed to the editor by the user */
    get editorOptions() {
      return { ...this.gridOptions.defaultEditorOptions?.date, ...this.columnEditor?.options };
    }
    get hasAutoCommitEdit() {
      return this.gridOptions.autoCommitEdit ?? false;
    }
    get pickerOptions() {
      return this._pickerMergedOptions;
    }
    /** Get the Validator function, can be passed in Editor property or Column Definition */
    get validator() {
      return this.columnEditor?.validator ?? this.columnDef?.validator;
    }
    async init() {
      if (this.args && this.columnDef) {
        const columnId = this.columnDef?.id ?? "";
        const gridOptions = this.args.grid.getOptions() || {};
        this.defaultDate = this.args.item?.[this.columnDef.field];
        const outputFieldType = this.columnDef.outputType || this.columnEditor.type || this.columnDef.type || "dateUtc";
        const outputFormat = mapTempoDateFormatWithFieldType(outputFieldType);
        const currentLocale = this._translaterService?.getCurrentLanguage?.() || gridOptions.locale || "en";
        if (outputFormat && (outputFormat === "ISO8601" || outputFormat.toLowerCase().includes("h"))) {
          this.hasTimePicker = true;
        }
        const pickerFormat = mapTempoDateFormatWithFieldType(this.hasTimePicker ? "dateTimeIsoAM_PM" : "dateIso");
        const pickerOptions = {
          inputMode: true,
          enableJumpToSelectedDate: true,
          firstWeekday: 0,
          enableDateToggle: false,
          locale: currentLocale,
          selectedTheme: this.gridOptions?.darkMode ? "dark" : "light",
          positionToInput: "auto",
          sanitizerHTML: (dirtyHtml) => this.grid.sanitizeHtmlString(dirtyHtml),
          selectedWeekends: [],
          onClickDate: () => {
            this._lastClickIsDate = true;
          },
          onShow: (self) => {
            setPickerFocus(self.context.mainElement);
          },
          onChangeToInput: (self) => {
            if (self.context.inputElement) {
              let selectedDate = "";
              if (self.context.selectedDates[0]) {
                selectedDate = self.context.selectedDates[0];
                self.context.inputElement.value = formatDateByFieldType(self.context.selectedDates[0], void 0, outputFieldType);
              } else {
                self.context.inputElement.value = "";
              }
              if (selectedDate && this.hasTimePicker) {
                const tempoDate = parse(selectedDate, pickerFormat);
                tempoDate.setHours(+(self.context.selectedHours || 0));
                tempoDate.setMinutes(+(self.context.selectedMinutes || 0));
                self.context.inputElement.value = formatDateByFieldType(tempoDate, void 0, outputFieldType);
              }
              if (this._lastClickIsDate) {
                this.handleOnDateChange();
                self.hide();
              }
            }
          }
        };
        if (this.hasTimePicker) {
          pickerOptions.selectionTimeMode = 24;
        }
        this._pickerMergedOptions = extend(true, {}, pickerOptions, this.editorOptions, { type: "default" });
        const inputCssClasses = `.editor-text.date-picker.editor-${columnId}.form-control.input-group-editor`;
        this._editorInputGroupElm = createDomElement("div", { className: "vanilla-picker input-group" });
        const closeButtonGroupElm = createDomElement("span", {
          className: "input-group-btn input-group-append",
          dataset: { clear: "" }
        });
        this._clearButtonElm = createDomElement("button", { type: "button", className: "btn btn-default btn-clear" });
        this._clearButtonElm.appendChild(createDomElement("i", { className: "sgi icon-clear" }));
        this._inputElm = createDomElement("input", {
          placeholder: this.columnEditor?.placeholder ?? "",
          title: this.columnEditor && this.columnEditor.title || "",
          className: inputCssClasses.replace(/\./g, " "),
          dataset: { input: "", defaultdate: this.defaultDate },
          readOnly: this.editorOptions?.allowInput === true ? false : true
        }, this._editorInputGroupElm);
        this.args.container.appendChild(this._editorInputGroupElm);
        if (!this.editorOptions?.hideClearButton) {
          closeButtonGroupElm.appendChild(this._clearButtonElm);
          this._editorInputGroupElm.appendChild(closeButtonGroupElm);
          this._bindEventService.bind(this._clearButtonElm, "click", () => {
            this.clear();
            this.handleOnDateChange();
          });
        }
        this._bindEventService.bind(this._inputElm, "keydown", ((event) => {
          if (this.editorOptions?.allowInput !== true) {
            return;
          }
          this._isValueTouched = true;
          this._lastInputKeyEvent = event;
          if (event.key === "ArrowLeft" || event.key === "ArrowRight" || event.key === "Home" || event.key === "End") {
            event.stopImmediatePropagation();
          }
        }));
        queueMicrotaskPolyfill(() => {
          this.calendarInstance = new Calendar(this._inputElm, this._pickerMergedOptions);
          this.calendarInstance.init();
          if (!this.args.isCompositeEditor) {
            this.show();
            this.focus();
          }
          if (this.calendarInstance) {
            setPickerDates(this.columnEditor, this._inputElm, this.calendarInstance, {
              columnDef: this.columnDef,
              newVal: this.defaultDate,
              updatePickerUI: true
            });
          }
          this.columnEditor.onInstantiated?.(this.calendarInstance);
        });
      }
    }
    destroy() {
      clearTimeout(this._timer);
      queueMicrotaskPolyfill(() => {
        this.hide();
        this.calendarInstance?.destroy();
        emptyElement(this._editorInputGroupElm);
        emptyElement(this._inputElm);
        this._editorInputGroupElm?.remove();
        this._inputElm?.remove();
      });
      this._bindEventService.unbindAll();
    }
    clear() {
      this._lastTriggeredByClearDate = true;
      if (this.calendarInstance) {
        resetDatePicker(this.calendarInstance);
      }
    }
    disable(isDisabled = true) {
      const prevIsDisabled = this.disabled;
      this.disabled = isDisabled;
      if (this._inputElm) {
        if (isDisabled) {
          this._inputElm.setAttribute("disabled", "disabled");
          this._clearButtonElm.disabled = true;
          const currentValue = this.getValue();
          if (prevIsDisabled !== isDisabled && this.args?.compositeEditorOptions && currentValue !== "") {
            this.reset("", true, true);
          }
        } else {
          this._inputElm.removeAttribute("disabled");
          this._clearButtonElm.disabled = false;
        }
      }
    }
    /**
     * Dynamically change an Editor option, this is especially useful with Composite Editor
     * since this is the only way to change option after the Editor is created (for example dynamically change "minDate" or another Editor)
     * @param {string} optionName
     * @param {newValue} newValue
     */
    changeEditorOption(optionName, newValue) {
      this.columnEditor.options ??= {};
      this.columnEditor.options[optionName] = newValue;
      this._pickerMergedOptions = extend(true, {}, this._pickerMergedOptions, { [optionName]: newValue });
      this.calendarInstance?.set(this._pickerMergedOptions, { dates: true, locale: true, month: true, time: true, year: true });
    }
    focus() {
      this.grid.focus("internal");
      this.show();
      this._inputElm?.focus();
    }
    hide() {
      this.calendarInstance?.hide();
    }
    show() {
      if (!this.args.isCompositeEditor && this.calendarInstance) {
        this.calendarInstance.show();
      } else if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
    }
    getValue() {
      return this._inputElm.value;
    }
    setValue(val, isApplyingValue = false, triggerOnCompositeEditorChange = true) {
      if (this.calendarInstance) {
        setPickerDates(this.columnEditor, this._inputElm, this.calendarInstance, {
          columnDef: this.columnDef,
          newVal: val
        });
      }
      if (isApplyingValue) {
        this.applyValue(this.args.item, this.serializeValue());
        const compositeEditorOptions = this.args.compositeEditorOptions;
        if (compositeEditorOptions && triggerOnCompositeEditorChange) {
          this.handleChangeOnCompositeEditor(compositeEditorOptions, "system");
        }
      }
    }
    applyValue(item, state2) {
      const fieldName = this.columnDef?.field;
      if (this.columnDef && fieldName !== void 0) {
        const saveFieldType = this.columnDef.saveOutputType || this.columnDef.outputType || this.columnEditor.type || this.columnDef.type || "dateUtc";
        const outputFieldType = this.columnDef.outputType || this.columnEditor.type || this.columnDef.type || "dateUtc";
        const isComplexObject = fieldName.indexOf(".") > 0;
        const validation = this.validate(null, state2);
        const newValue = state2 && validation?.valid ? formatDateByFieldType(state2, outputFieldType, saveFieldType) : "";
        if (isComplexObject) {
          const objectPath = this.columnEditor?.complexObjectPath ?? fieldName;
          setDeepValue(item, objectPath, newValue);
        } else {
          item[fieldName] = newValue;
        }
      }
    }
    isValueChanged() {
      let isChanged = false;
      const elmDateStr = this.getValue();
      const lastEventKey = this._lastInputKeyEvent?.key;
      if (this.editorOptions?.allowInput === true && this.columnEditor?.alwaysSaveOnEnterKey && lastEventKey === "Enter") {
        return true;
      }
      if (this.columnDef) {
        isChanged = this._lastTriggeredByClearDate || !(elmDateStr === "" && this._originalDate === "") && elmDateStr !== this._originalDate;
      }
      return isChanged;
    }
    isValueTouched() {
      return this._isValueTouched;
    }
    loadValue(item) {
      const fieldName = this.columnDef?.field;
      if (item && this.columnDef && fieldName !== void 0) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const value = isComplexObject ? getDescendantProperty(item, fieldName) : item[fieldName];
        const inputFieldType = this.columnEditor.type || this.columnDef?.type || "dateIso";
        const outputFieldType = this.columnDef.outputType || this.columnEditor.type || this.columnDef.type || "dateIso";
        const formattedDate = formatDateByFieldType(value, inputFieldType, outputFieldType);
        this._originalDate = formattedDate !== "" ? value : "";
        this._inputElm.value = formattedDate;
      }
    }
    /**
     * You can reset or clear the input value,
     * when no value is provided it will use the original value to reset (could be useful with Composite Editor Modal with edit/clone)
     */
    reset(value, triggerCompositeEventWhenExist = true, clearByDisableCommand = false) {
      const inputValue = value ?? this._originalDate ?? "";
      if (this.calendarInstance) {
        this._originalDate = inputValue;
        this.calendarInstance.selectedDates = [inputValue];
        if (inputValue) {
          setPickerDates(this.columnEditor, this._inputElm, this.calendarInstance, {
            columnDef: this.columnDef,
            newVal: inputValue
          });
        } else {
          resetDatePicker(this.calendarInstance);
        }
      }
      this._isValueTouched = false;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions && triggerCompositeEventWhenExist) {
        const shouldDeleteFormValue = !clearByDisableCommand;
        this.handleChangeOnCompositeEditor(compositeEditorOptions, "user", shouldDeleteFormValue);
      }
    }
    save() {
      if (this.hasAutoCommitEdit && this.validate()?.valid) {
        this.grid.getEditorLock().commitCurrentEdit();
      } else {
        this.args.commitChanges();
      }
    }
    serializeValue() {
      const domValue = this.getValue();
      if (!domValue) {
        return "";
      }
      return domValue;
    }
    validate(_targetElm, options) {
      const isRequired = this.args?.compositeEditorOptions ? false : this.columnEditor.required;
      const elmValue = options?.inputValue ?? this._inputElm?.value;
      const errorMsg = this.columnEditor.errorMessage;
      if (this.args.compositeEditorOptions) {
        this.applyInputUsabilityState();
      }
      if (this.disabled) {
        return { valid: true, msg: "" };
      }
      if (this.validator) {
        return this.validator(elmValue, this.args);
      }
      if (isRequired && elmValue === "") {
        return { valid: false, msg: errorMsg || Constants.VALIDATION_REQUIRED_FIELD };
      }
      return { valid: true, msg: null };
    }
    //
    // protected functions
    // ------------------
    /** when it's a Composite Editor, we'll check if the Editor is editable (by checking onBeforeEditCell) and if not Editable we'll disable the Editor */
    applyInputUsabilityState() {
      const activeCell = this.grid.getActiveCell();
      const isCellEditable = this.grid.onBeforeEditCell.notify({
        ...activeCell,
        item: this.dataContext,
        column: this.args.column,
        grid: this.grid,
        target: "composite",
        compositeEditorOptions: this.args.compositeEditorOptions
      }).getReturnValue();
      this.disable(isCellEditable === false);
    }
    handleOnDateChange() {
      this._isValueTouched = true;
      if (this.args) {
        const compositeEditorOptions = this.args.compositeEditorOptions;
        if (compositeEditorOptions) {
          this.handleChangeOnCompositeEditor(compositeEditorOptions);
        } else {
          this.save();
        }
      }
      clearTimeout(this._timer);
      this._timer = setTimeout(() => this._lastTriggeredByClearDate = false);
    }
    handleChangeOnCompositeEditor(compositeEditorOptions, triggeredBy = "user", isCalledByClearValue = false) {
      const activeCell = this.grid.getActiveCell();
      const column = this.args.column;
      const columnId = this.columnDef?.id ?? "";
      const item = this.dataContext;
      const grid = this.grid;
      const newValue = this.serializeValue();
      if (this.validate().valid) {
        this.applyValue(this.dataContext, newValue);
      }
      this.applyValue(compositeEditorOptions.formValues, newValue);
      const isExcludeDisabledFieldFormValues = this.gridOptions?.compositeEditorOptions?.excludeDisabledFieldFormValues ?? false;
      if (isCalledByClearValue || this.disabled && isExcludeDisabledFieldFormValues && compositeEditorOptions.formValues.hasOwnProperty(columnId)) {
        delete compositeEditorOptions.formValues[columnId];
      }
      grid.onCompositeEditorChange.notify({
        ...activeCell,
        item,
        grid,
        column,
        formValues: compositeEditorOptions.formValues,
        editors: compositeEditorOptions.editors,
        triggeredBy
      }, new SlickEventData());
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editorValidators/floatValidator.js
  function floatValidator(inputValue, options) {
    const floatNumber = !isNaN(inputValue) ? parseFloat(inputValue) : null;
    const decPlaces = options.decimal || 0;
    const isRequired = options.required;
    const minValue = options.minValue;
    const maxValue = options.maxValue;
    const operatorConditionalType = options.operatorConditionalType || "inclusive";
    const errorMsg = options.errorMessage;
    const mapValidation = {
      "{{minValue}}": minValue,
      "{{maxValue}}": maxValue,
      "{{minDecimal}}": 0,
      "{{maxDecimal}}": decPlaces
    };
    let isValid = true;
    let outputMsg = "";
    if (typeof options.validator === "function") {
      return options.validator(inputValue, options.editorArgs);
    } else if (isRequired && inputValue === "") {
      isValid = false;
      outputMsg = errorMsg || Constants.VALIDATION_REQUIRED_FIELD;
    } else if (inputValue !== "" && (isNaN(inputValue) || decPlaces === 0 && !/^[-+]?(\d*(\.)?(\d)*)$/.test(inputValue))) {
      isValid = false;
      outputMsg = errorMsg || Constants.VALIDATION_EDITOR_VALID_NUMBER;
    } else if (minValue !== void 0 && maxValue !== void 0 && floatNumber !== null && (operatorConditionalType === "exclusive" && (floatNumber <= +minValue || floatNumber >= +maxValue) || operatorConditionalType === "inclusive" && (floatNumber < +minValue || floatNumber > +maxValue))) {
      isValid = false;
      outputMsg = errorMsg || Constants.VALIDATION_EDITOR_NUMBER_BETWEEN.replace(/{{minValue}}|{{maxValue}}/gi, (matched) => mapValidation[matched]);
    } else if (minValue !== void 0 && floatNumber !== null && (operatorConditionalType === "exclusive" && floatNumber <= +minValue || operatorConditionalType === "inclusive" && floatNumber < +minValue)) {
      isValid = false;
      const defaultErrorMsg = operatorConditionalType === "inclusive" ? Constants.VALIDATION_EDITOR_NUMBER_MIN_INCLUSIVE : Constants.VALIDATION_EDITOR_NUMBER_MIN;
      outputMsg = errorMsg || defaultErrorMsg.replace(/{{minValue}}/gi, (matched) => mapValidation[matched]);
    } else if (maxValue !== void 0 && floatNumber !== null && (operatorConditionalType === "exclusive" && floatNumber >= +maxValue || operatorConditionalType === "inclusive" && floatNumber > +maxValue)) {
      isValid = false;
      const defaultErrorMsg = operatorConditionalType === "inclusive" ? Constants.VALIDATION_EDITOR_NUMBER_MAX_INCLUSIVE : Constants.VALIDATION_EDITOR_NUMBER_MAX;
      outputMsg = errorMsg || defaultErrorMsg.replace(/{{maxValue}}/gi, (matched) => mapValidation[matched]);
    } else if (decPlaces > 0 && !new RegExp(`^[-+]?(\\d*(\\.)?(\\d){0,${decPlaces}})$`).test(inputValue)) {
      isValid = false;
      outputMsg = errorMsg || Constants.VALIDATION_EDITOR_DECIMAL_BETWEEN.replace(/{{minDecimal}}|{{maxDecimal}}/gi, (matched) => mapValidation[matched]);
    }
    return { valid: isValid, msg: outputMsg };
  }

  // node_modules/@slickgrid-universal/common/dist/editorValidators/integerValidator.js
  function integerValidator(inputValue, options) {
    let intNumber = !isNaN(inputValue) ? parseInt(inputValue, 10) : null;
    if (intNumber !== null && isNaN(intNumber)) {
      intNumber = null;
    }
    const errorMsg = options.errorMessage;
    const isRequired = options.required;
    const minValue = options.minValue;
    const maxValue = options.maxValue;
    const operatorConditionalType = options.operatorConditionalType || "inclusive";
    const mapValidation = {
      "{{minValue}}": minValue,
      "{{maxValue}}": maxValue
    };
    let isValid = true;
    let outputMsg = "";
    if (options.validator) {
      return options.validator(inputValue, options.editorArgs);
    } else if (isRequired && inputValue === "") {
      isValid = false;
      outputMsg = errorMsg || Constants.VALIDATION_REQUIRED_FIELD;
    } else if (inputValue !== "" && (isNaN(inputValue) || !/^[+-]?\d+$/.test(inputValue))) {
      isValid = false;
      outputMsg = errorMsg || Constants.VALIDATION_EDITOR_VALID_INTEGER;
    } else if (minValue !== void 0 && maxValue !== void 0 && intNumber !== null && (operatorConditionalType === "exclusive" && (intNumber <= +minValue || intNumber >= +maxValue) || operatorConditionalType === "inclusive" && (intNumber < +minValue || intNumber > +maxValue))) {
      isValid = false;
      outputMsg = errorMsg || Constants.VALIDATION_EDITOR_INTEGER_BETWEEN.replace(/{{minValue}}|{{maxValue}}/gi, (matched) => mapValidation[matched]);
    } else if (minValue !== void 0 && intNumber !== null && (operatorConditionalType === "exclusive" && intNumber <= +minValue || operatorConditionalType === "inclusive" && intNumber !== null && intNumber < +minValue)) {
      isValid = false;
      const defaultErrorMsg = operatorConditionalType === "inclusive" ? Constants.VALIDATION_EDITOR_INTEGER_MIN_INCLUSIVE : Constants.VALIDATION_EDITOR_INTEGER_MIN;
      outputMsg = errorMsg || defaultErrorMsg.replace(/{{minValue}}/gi, (matched) => mapValidation[matched]);
    } else if (maxValue !== void 0 && intNumber !== null && (operatorConditionalType === "exclusive" && intNumber >= +maxValue || operatorConditionalType === "inclusive" && intNumber !== null && intNumber > +maxValue)) {
      isValid = false;
      const defaultErrorMsg = operatorConditionalType === "inclusive" ? Constants.VALIDATION_EDITOR_INTEGER_MAX_INCLUSIVE : Constants.VALIDATION_EDITOR_INTEGER_MAX;
      outputMsg = errorMsg || defaultErrorMsg.replace(/{{maxValue}}/gi, (matched) => mapValidation[matched]);
    }
    return { valid: isValid, msg: outputMsg };
  }

  // node_modules/@slickgrid-universal/common/dist/editorValidators/sliderValidator.js
  function sliderValidator(inputValue, options) {
    const isRequired = options.required;
    const minValue = options.minValue;
    const maxValue = options.maxValue;
    const errorMsg = options.errorMessage;
    const mapValidation = {
      "{{minValue}}": minValue,
      "{{maxValue}}": maxValue
    };
    if (options.validator) {
      return options.validator(inputValue, options.editorArgs);
    } else if (isRequired && inputValue === "") {
      return {
        valid: false,
        msg: errorMsg || Constants.VALIDATION_REQUIRED_FIELD
      };
    } else if (minValue !== void 0 && maxValue !== void 0 && inputValue !== null && (inputValue < minValue || inputValue > maxValue)) {
      return {
        valid: false,
        msg: errorMsg || Constants.VALIDATION_EDITOR_NUMBER_BETWEEN.replace(/{{minValue}}|{{maxValue}}/gi, (matched) => {
          return mapValidation[matched];
        })
      };
    }
    return { valid: true, msg: null };
  }

  // node_modules/@slickgrid-universal/common/dist/editors/dualInputEditor.js
  var DualInputEditor = class {
    constructor(args) {
      this.args = args;
      this._isInternalFocusInProgress = false;
      this._isValueSaveCalled = false;
      this._isLeftValueTouched = false;
      this._isRightValueTouched = false;
      this.disabled = false;
      this.grid = args.grid;
      this.gridOptions = this.grid.getOptions() || {};
      this._eventHandler = new SlickEventHandler();
      this._bindEventService = new BindingEventService();
      this.init();
      this._eventHandler.subscribe(this.grid.onValidationError, () => this._isValueSaveCalled = true);
    }
    /** Get Column Definition object */
    get columnDef() {
      return this.args.column;
    }
    /** Get Column Editor object */
    get columnEditor() {
      return this.columnDef?.editor || {};
    }
    /** Getter for the item data context object */
    get dataContext() {
      return this.args.item;
    }
    /** Getter for the Editor DOM Element */
    get editorDomElement() {
      return { leftInput: this._leftInput, rightInput: this._rightInput };
    }
    get editorParams() {
      return this.columnEditor.params || {};
    }
    get eventHandler() {
      return this._eventHandler;
    }
    get hasAutoCommitEdit() {
      return this.gridOptions.autoCommitEdit ?? false;
    }
    get isValueSaveCalled() {
      return this._isValueSaveCalled;
    }
    /** Get the Shared Validator function, can be passed in Editor property or Column Definition */
    get validator() {
      return this.columnEditor?.validator ?? this.columnDef?.validator;
    }
    init() {
      if (!this.editorParams || !this.editorParams.leftInput || !this.editorParams.leftInput.field || !this.editorParams.rightInput || !this.editorParams.rightInput.field) {
        throw new Error(`[Slickgrid-Universal] Please make sure that your Combo Input Editor has params defined with "leftInput" and "rightInput" (example: { editor: { model: Editors.comboInput, params: { leftInput: { field: 'firstName' }, { rightSide: { field: 'lastName' } }}}`);
      }
      this._leftFieldName = this.editorParams.leftInput?.field;
      this._rightFieldName = this.editorParams.rightInput?.field;
      this._leftInput = this.createInput("leftInput");
      this._rightInput = this.createInput("rightInput");
      const containerElm = this.args?.container;
      if (containerElm && typeof containerElm.appendChild === "function") {
        containerElm.appendChild(this._leftInput);
        containerElm.appendChild(this._rightInput);
      }
      this._bindEventService.bind(this._leftInput, "keydown", ((event) => this.handleKeyDown(event, "leftInput")));
      this._bindEventService.bind(this._rightInput, "keydown", ((event) => this.handleKeyDown(event, "rightInput")));
      if (this.hasAutoCommitEdit) {
        this._bindEventService.bind(this._leftInput, "focusout", ((event) => this.handleFocusOut(event, "leftInput")));
        this._bindEventService.bind(this._rightInput, "focusout", ((event) => this.handleFocusOut(event, "rightInput")));
      }
      if (this.args.isCompositeEditor) {
        this._bindEventService.bind(this._leftInput, "input", this.handleChangeOnCompositeEditorDebounce.bind(this));
        this._bindEventService.bind(this._rightInput, "input", this.handleChangeOnCompositeEditorDebounce.bind(this));
      } else {
        clearTimeout(this._timer);
        this._timer = setTimeout(() => this._leftInput.select(), 50);
      }
    }
    handleFocusOut(event, position) {
      if (!this._isInternalFocusInProgress) {
        const targetClassNames = event.relatedTarget?.className || "";
        if (!this.args.isCompositeEditor && targetClassNames.indexOf("dual-editor") === -1 && this._lastEventType !== "focusout-right") {
          if (position === "rightInput" || position === "leftInput" && this._lastEventType !== "focusout-left") {
            if (position === "leftInput") {
              this._isLeftValueTouched = true;
            } else {
              this._isRightValueTouched = true;
            }
            this.save();
          }
        }
        const side = position === "leftInput" ? "left" : "right";
        this._lastEventType = `${event?.type}-${side}`;
      }
    }
    handleKeyDown(event, position) {
      if (position === "leftInput") {
        this._isLeftValueTouched = true;
      } else {
        this._isRightValueTouched = true;
      }
      this._lastInputKeyEvent = event;
      if (event.key === "ArrowLeft" || event.key === "ArrowRight" || event.key === "Home" || event.key === "End" || event.key === "Tab") {
        event.stopImmediatePropagation();
      }
    }
    destroy() {
      clearTimeout(this._timer);
      this._eventHandler.unsubscribeAll();
      this._bindEventService.unbindAll();
      this._leftInput?.remove();
      this._rightInput?.remove();
    }
    createInput(position) {
      const editorSideParams = this.editorParams[position];
      const columnId = this.columnDef?.id ?? "";
      const idPropName = this.gridOptions.datasetIdPropertyName || "id";
      const itemId = this.args?.item?.[idPropName] ?? "";
      let fieldType = editorSideParams.type || "text";
      if (fieldType === "float" || fieldType === "integer") {
        fieldType = "number";
      }
      const input = createDomElement("input", {
        type: fieldType || "text",
        id: `item-${itemId}-${position}`,
        ariaLabel: this.columnEditor?.ariaLabel ?? `${toSentenceCase(columnId + "")} Input Editor`,
        className: `dual-editor-text editor-${columnId} ${position.replace(/input/gi, "")}`,
        autocomplete: "off",
        ariaAutoComplete: "none",
        placeholder: editorSideParams.placeholder || "",
        title: editorSideParams.title || ""
      });
      if (fieldType === "readonly") {
        input.readOnly = true;
        fieldType = "text";
      }
      if (fieldType === "number") {
        input.step = this.getInputDecimalSteps(position);
      }
      return input;
    }
    disable(isDisabled = true) {
      const prevIsDisabled = this.disabled;
      this.disabled = isDisabled;
      if (this._leftInput && this._rightInput) {
        if (isDisabled) {
          this._leftInput.setAttribute("disabled", "disabled");
          this._rightInput.setAttribute("disabled", "disabled");
          if (prevIsDisabled !== isDisabled && this.args.isCompositeEditor) {
            this.reset("", true, true);
          }
        } else {
          this._leftInput.removeAttribute("disabled");
          this._rightInput.removeAttribute("disabled");
        }
      }
    }
    focus() {
      this._isInternalFocusInProgress = true;
      this.grid.focus("internal");
      this._isInternalFocusInProgress = false;
    }
    show() {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
    }
    getValues() {
      const obj = {};
      const leftInputValue = this._leftInput.value;
      const rightInputValue = this._rightInput.value;
      const isLeftInputTypeNumber = this.editorParams.leftInput && (this.editorParams.leftInput.type === "float" || this.editorParams.leftInput.type === "integer");
      const isRightInputTypeNumber = this.editorParams.rightInput && (this.editorParams.rightInput.type === "float" || this.editorParams.rightInput.type === "integer");
      const resultLeftValue = leftInputValue !== "" && isLeftInputTypeNumber ? +this._leftInput.value : leftInputValue || "";
      const resultRightValue = rightInputValue !== "" && isRightInputTypeNumber ? +this._rightInput.value : rightInputValue || "";
      setDeepValue(obj, this._leftFieldName, resultLeftValue);
      setDeepValue(obj, this._rightFieldName, resultRightValue);
      return obj;
    }
    setValues(values) {
      if (Array.isArray(values) && values.length === 2) {
        this._leftInput.value = `${values[0]}`;
        this._rightInput.value = `${values[1]}`;
      }
    }
    applyValue(item, state2) {
      this.applyValueByPosition(item, state2, "leftInput");
      this.applyValueByPosition(item, state2, "rightInput");
    }
    applyValueByPosition(item, state2, position) {
      const fieldName = position === "leftInput" ? this._leftFieldName : this._rightFieldName;
      if (fieldName !== void 0) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        let fieldNameToUse = fieldName;
        if (isComplexObject) {
          const complexFieldNames = fieldName.split(/\.(.*)/);
          fieldNameToUse = complexFieldNames.length > 1 ? complexFieldNames[1] : complexFieldNames;
        }
        const stateValue = isComplexObject ? getDescendantProperty(state2, fieldNameToUse) : state2[fieldName];
        const validation = this.validate(null, { position, inputValue: stateValue });
        if (isComplexObject) {
          const newValueFromComplex = getDescendantProperty(state2, fieldNameToUse);
          const newValue = validation && validation.valid ? newValueFromComplex : "";
          const objectPath = this.columnEditor?.complexObjectPath ?? fieldName ?? "";
          setDeepValue(item, objectPath, newValue);
        } else if (fieldName) {
          item[fieldName] = validation && validation.valid ? state2[fieldName] : "";
        }
      }
    }
    isValueChanged() {
      const leftElmValue = this._leftInput.value;
      const rightElmValue = this._rightInput.value;
      const leftEditorParams = this.editorParams?.leftInput;
      const rightEditorParams = this.editorParams?.rightInput;
      const lastEventKey = this._lastInputKeyEvent?.key;
      if ((leftEditorParams && leftEditorParams.alwaysSaveOnEnterKey || rightEditorParams && rightEditorParams.alwaysSaveOnEnterKey) && lastEventKey === "Enter") {
        return true;
      }
      const leftResult = !(leftElmValue === "" && (this._originalLeftValue === null || this._originalLeftValue === void 0)) && leftElmValue !== this._originalLeftValue;
      const rightResult = !(rightElmValue === "" && (this._originalRightValue === null || this._originalRightValue === void 0)) && rightElmValue !== this._originalRightValue;
      return leftResult || rightResult;
    }
    isValueTouched() {
      return this._isLeftValueTouched || this._isRightValueTouched;
    }
    loadValue(item) {
      this.loadValueByPosition(item, "leftInput");
      this.loadValueByPosition(item, "rightInput");
      this._leftInput.select();
    }
    loadValueByPosition(item, position) {
      const fieldName = position === "leftInput" ? this._leftFieldName : this._rightFieldName;
      const originalValuePosition = position === "leftInput" ? "_originalLeftValue" : "_originalRightValue";
      const inputVarPosition = position === "leftInput" ? "_leftInput" : "_rightInput";
      if (item && fieldName !== void 0) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const itemValue = isComplexObject ? getDescendantProperty(item, fieldName) : item.hasOwnProperty(fieldName) ? item[fieldName] : "";
        this[originalValuePosition] = itemValue;
        if (this.editorParams[position].type === "float") {
          const decimalPlaces = this.getDecimalPlaces(position);
          if (decimalPlaces !== null && (this[originalValuePosition] || this[originalValuePosition] === 0) && this[originalValuePosition] !== void 0) {
            this[originalValuePosition] = (+this[originalValuePosition]).toFixed(decimalPlaces);
          }
        }
        if (this[inputVarPosition]) {
          this[inputVarPosition].value = `${this[originalValuePosition]}`;
        }
      }
    }
    /**
     * You can reset or clear the input value,
     * when no value is provided it will use the original value to reset (could be useful with Composite Editor Modal with edit/clone)
     */
    reset(value, triggerCompositeEventWhenExist = true, clearByDisableCommand = false) {
      const inputLeftValue = value ?? this._originalLeftValue ?? "";
      const inputRightValue = value ?? this._originalRightValue ?? "";
      if (this._leftInput && this._rightInput) {
        this._originalLeftValue = inputLeftValue;
        this._originalRightValue = inputRightValue;
        this._leftInput.value = `${inputLeftValue}`;
        this._rightInput.value = `${inputRightValue}`;
      }
      this._isLeftValueTouched = false;
      this._isRightValueTouched = false;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions && triggerCompositeEventWhenExist) {
        const shouldDeleteFormValue = !clearByDisableCommand;
        this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "user", shouldDeleteFormValue);
      }
    }
    save() {
      const validation = this.validate();
      const isValid = validation && validation.valid || false;
      if (!this._isValueSaveCalled) {
        if (this.hasAutoCommitEdit && isValid) {
          this.grid.getEditorLock().commitCurrentEdit();
        } else {
          this.args.commitChanges();
        }
        this._isValueSaveCalled = true;
      }
    }
    serializeValue() {
      const obj = {};
      const leftValue = this.serializeValueByPosition("leftInput");
      const rightValue = this.serializeValueByPosition("rightInput");
      setDeepValue(obj, this._leftFieldName, leftValue);
      setDeepValue(obj, this._rightFieldName, rightValue);
      return obj;
    }
    serializeValueByPosition(position) {
      const elmValue = position === "leftInput" ? this._leftInput.value : this._rightInput.value;
      if (elmValue === "" || isNaN(+elmValue)) {
        return elmValue;
      }
      let rtn = parseFloat(elmValue);
      const decPlaces = this.getDecimalPlaces(position);
      if (decPlaces !== null && (rtn || rtn === 0) && rtn.toFixed) {
        rtn = parseFloat(rtn.toFixed(decPlaces));
      }
      return rtn;
    }
    getDecimalPlaces(position) {
      const defaultDecimalPlaces = 0;
      const positionSide = position === "leftInput" ? "leftInput" : "rightInput";
      const sideParams = this.editorParams[positionSide];
      const rtn = sideParams?.decimal;
      if (rtn === void 0) {
        return defaultDecimalPlaces;
      }
      return rtn;
    }
    getInputDecimalSteps(position) {
      const decimals = this.getDecimalPlaces(position);
      let zeroString = "";
      for (let i = 1; i < decimals; i++) {
        zeroString += "0";
      }
      if (decimals > 0) {
        return `0.${zeroString}1`;
      }
      return "1";
    }
    validate(_targetElm, options) {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
      if (this.disabled) {
        return { valid: true, msg: "" };
      }
      if (options) {
        const position = options.position || "leftInput";
        const posValidation = this.validateByPosition(position, options.inputValue);
        if (!posValidation.valid) {
          const input = position === "leftInput" ? this._leftInput : this._rightInput;
          input.select();
          return posValidation;
        }
      } else {
        const leftValidation = this.validateByPosition("leftInput");
        const rightValidation = this.validateByPosition("rightInput");
        if (!leftValidation.valid) {
          this._leftInput.select();
          return leftValidation;
        }
        if (!rightValidation.valid) {
          this._rightInput.select();
          return rightValidation;
        }
      }
      return { valid: true, msg: "" };
    }
    validateByPosition(position, inputValue) {
      const positionEditorParams = this.editorParams[position];
      let currentVal = "";
      if (inputValue) {
        currentVal = inputValue;
      } else {
        const input = position === "leftInput" ? this._leftInput : this._rightInput;
        currentVal = input && input.value;
      }
      const commonValidator = this.validator;
      currentVal = typeof commonValidator === "function" ? this.getValues() : currentVal;
      const baseValidatorOptions = {
        editorArgs: this.args,
        errorMessage: positionEditorParams.errorMessage,
        required: this.args.isCompositeEditor ? false : positionEditorParams.required,
        validator: typeof commonValidator === "function" ? commonValidator : positionEditorParams.validator
      };
      switch (positionEditorParams.type) {
        case "float":
          return floatValidator(currentVal, {
            ...baseValidatorOptions,
            decimal: this.getDecimalPlaces(position),
            minValue: positionEditorParams.minValue,
            maxValue: positionEditorParams.maxValue,
            operatorConditionalType: positionEditorParams.operatorConditionalType
          });
        case "integer":
          return integerValidator(currentVal, {
            ...baseValidatorOptions,
            minValue: positionEditorParams.minValue,
            maxValue: positionEditorParams.maxValue,
            operatorConditionalType: positionEditorParams.operatorConditionalType
          });
        case "text":
        case "password":
        default:
          return textValidator(currentVal, baseValidatorOptions);
      }
    }
    /** when it's a Composite Editor, we'll check if the Editor is editable (by checking onBeforeEditCell) and if not Editable we'll disable the Editor */
    applyInputUsabilityState() {
      const activeCell = this.grid.getActiveCell();
      const isCellEditable = this.grid.onBeforeEditCell.notify({
        ...activeCell,
        item: this.dataContext,
        column: this.args.column,
        grid: this.grid,
        target: "composite",
        compositeEditorOptions: this.args.compositeEditorOptions
      }).getReturnValue();
      this.disable(isCellEditable === false);
    }
    handleChangeOnCompositeEditor(event, compositeEditorOptions, triggeredBy = "user", isCalledByClearValue = false) {
      const activeCell = this.grid.getActiveCell();
      const column = this.args.column;
      const leftInputId = this.columnEditor.params?.leftInput?.field ?? "";
      const rightInputId = this.columnEditor.params?.rightInput?.field ?? "";
      const item = this.dataContext;
      const grid = this.grid;
      const newValues = this.serializeValue();
      if (this.validate().valid) {
        this.applyValue(this.dataContext, newValues);
      }
      this.applyValue(compositeEditorOptions.formValues, newValues);
      const isExcludeDisabledFieldFormValues = this.gridOptions?.compositeEditorOptions?.excludeDisabledFieldFormValues ?? false;
      if (isCalledByClearValue || this.disabled && isExcludeDisabledFieldFormValues && compositeEditorOptions.formValues.hasOwnProperty(leftInputId)) {
        delete compositeEditorOptions.formValues[leftInputId];
      }
      if (isCalledByClearValue || this.disabled && isExcludeDisabledFieldFormValues && compositeEditorOptions.formValues.hasOwnProperty(rightInputId)) {
        delete compositeEditorOptions.formValues[rightInputId];
      }
      grid.onCompositeEditorChange.notify({
        ...activeCell,
        item,
        grid,
        column,
        formValues: compositeEditorOptions.formValues,
        editors: compositeEditorOptions.editors,
        triggeredBy
      }, new SlickEventData(event));
    }
    handleChangeOnCompositeEditorDebounce(event) {
      const compositeEditorOptions = this.args?.compositeEditorOptions;
      if (compositeEditorOptions) {
        const typingDelay = this.gridOptions?.editorTypingDebounce ?? 500;
        clearTimeout(this._timer);
        this._timer = setTimeout(() => this.handleChangeOnCompositeEditor(event, compositeEditorOptions), typingDelay);
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/inputEditor.js
  var DEFAULT_DECIMAL_PLACES = 0;
  var InputEditor = class {
    constructor(args, inputType = "text") {
      this.args = args;
      this._isInternalFocusInProgress = false;
      this._inputType = "text";
      this._isValueTouched = false;
      this.disabled = false;
      this.grid = args.grid;
      this.gridOptions = args.grid?.getOptions();
      this._bindEventService = new BindingEventService();
      this.inputType = inputType;
      this.init();
    }
    /** Get Column Definition object */
    get columnDef() {
      return this.args.column;
    }
    /** Get Column Editor object */
    get columnEditor() {
      return this.columnDef?.editor || {};
    }
    /** Getter for the item data context object */
    get dataContext() {
      return this.args.item;
    }
    /** Getter for the Editor DOM Element */
    get editorDomElement() {
      return this._input;
    }
    get hasAutoCommitEdit() {
      return this.gridOptions.autoCommitEdit ?? false;
    }
    /** Getter of input type (text, number, password) */
    get inputType() {
      return this._inputType;
    }
    /** Setter of input type (text, number, password) */
    set inputType(type) {
      this._inputType = type;
    }
    /** Get the Validator function, can be passed in Editor property or Column Definition */
    get validator() {
      return this.columnEditor?.validator ?? this.columnDef?.validator;
    }
    init() {
      const columnId = this.columnDef?.id ?? "";
      this._input = createDomElement("input", {
        type: this._inputType || "text",
        autocomplete: "off",
        ariaAutoComplete: "none",
        ariaLabel: this.columnEditor?.ariaLabel ?? `${toSentenceCase(columnId + "")} Input Editor`,
        className: `editor-text editor-${columnId}`,
        placeholder: this.columnEditor?.placeholder ?? "",
        title: this.columnEditor?.title ?? ""
      });
      if (this.inputType === "number") {
        this._input.step = `${this.columnEditor.valueStep !== void 0 ? this.columnEditor.valueStep : this.getInputDecimalSteps()}`;
      }
      const cellContainer = this.args.container;
      if (cellContainer && typeof cellContainer.appendChild === "function") {
        cellContainer.appendChild(this._input);
      }
      this._bindEventService.bind(this._input, "focus", () => this._input?.select());
      this._bindEventService.bind(this._input, "keydown", ((event) => {
        this._isValueTouched = true;
        this._lastInputKeyEvent = event;
        if (!this.gridOptions.editorNavigateOnArrows && (event.key === "ArrowLeft" || event.key === "ArrowRight" || event.key === "Home" || event.key === "End")) {
          event.stopImmediatePropagation();
        }
      }));
      if (this.hasAutoCommitEdit && !this.args.isCompositeEditor) {
        this._bindEventService.bind(this._input, ["focusout", "blur"], () => {
          if (!this._isInternalFocusInProgress) {
            this._isValueTouched = true;
            this.save();
          }
        });
      }
      if (this.args.isCompositeEditor) {
        this._bindEventService.bind(this._input, ["input", "paste"], this.handleOnInputChange.bind(this));
        if (this.inputType === "number") {
          this._bindEventService.bind(this._input, "wheel", this.handleOnMouseWheel.bind(this), { passive: true });
        }
      }
    }
    destroy() {
      clearTimeout(this._timer);
      this._bindEventService.unbindAll();
      this._input?.remove();
    }
    disable(isDisabled = true) {
      const prevIsDisabled = this.disabled;
      this.disabled = isDisabled;
      if (this._input) {
        if (isDisabled) {
          this._input.setAttribute("disabled", "disabled");
          const currentValue = this.getValue();
          if (prevIsDisabled !== isDisabled && this.args.isCompositeEditor && currentValue !== "") {
            this.reset("", true, true);
          }
        } else {
          this._input.removeAttribute("disabled");
        }
      }
    }
    focus() {
      this._isInternalFocusInProgress = true;
      this.grid.focus("internal");
      this._input?.focus();
      this._isInternalFocusInProgress = false;
    }
    getDecimalPlaces() {
      let rtn = this.columnEditor?.decimal ?? this.columnEditor?.params?.decimalPlaces ?? void 0;
      if (rtn === void 0) {
        rtn = DEFAULT_DECIMAL_PLACES;
      }
      return !rtn && rtn !== 0 ? 0 : rtn;
    }
    /** when editor is a float input editor, we'll want to know how many decimals to show */
    getInputDecimalSteps() {
      const decimals = this.getDecimalPlaces();
      let zeroString = "";
      for (let i = 1; i < decimals; i++) {
        zeroString += "0";
      }
      if (decimals > 0) {
        return `0.${zeroString}1`;
      }
      return "1";
    }
    show() {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
    }
    getValue() {
      return this._input?.value || "";
    }
    setValue(value, isApplyingValue = false, triggerOnCompositeEditorChange = true) {
      if (this._input) {
        this._input.value = `${value}`;
        if (isApplyingValue) {
          this.applyValue(this.args.item, this.serializeValue());
          const compositeEditorOptions = this.args.compositeEditorOptions;
          if (compositeEditorOptions && triggerOnCompositeEditorChange) {
            this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "system");
          }
        }
      }
    }
    applyValue(item, state2) {
      const fieldName = this.columnDef?.field;
      if (fieldName !== void 0) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const validation = this.validate(null, state2);
        const newValue = validation?.valid ? state2 : "";
        if (isComplexObject) {
          const objectPath = this.columnEditor?.complexObjectPath ?? fieldName ?? "";
          setDeepValue(item, objectPath, newValue);
        } else if (fieldName) {
          item[fieldName] = newValue;
        }
      }
    }
    isValueChanged() {
      const elmValue = this._input?.value;
      const lastEventKey = this._lastInputKeyEvent?.key;
      if (this.columnEditor?.alwaysSaveOnEnterKey && lastEventKey === "Enter") {
        return true;
      }
      return !(elmValue === "" && (this._originalValue === null || this._originalValue === void 0)) && elmValue !== this._originalValue;
    }
    isValueTouched() {
      return this._isValueTouched;
    }
    loadValue(item) {
      const fieldName = this.columnDef?.field;
      if (item && fieldName !== void 0 && this._input) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const value = isComplexObject ? getDescendantProperty(item, fieldName) : item.hasOwnProperty(fieldName) && item[fieldName] || "";
        this._originalValue = value;
        this._input.value = this._originalValue;
        this._input.select();
      }
    }
    /**
     * You can reset or clear the input value,
     * when no value is provided it will use the original value to reset (could be useful with Composite Editor Modal with edit/clone)
     */
    reset(value, triggerCompositeEventWhenExist = true, clearByDisableCommand = false) {
      const inputValue = value ?? this._originalValue ?? "";
      if (this._input) {
        this._originalValue = inputValue;
        this._input.value = `${inputValue}`;
      }
      this._isValueTouched = false;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions && triggerCompositeEventWhenExist) {
        const shouldDeleteFormValue = !clearByDisableCommand;
        this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "user", shouldDeleteFormValue);
      }
    }
    save() {
      const validation = this.validate();
      const isValid = validation && validation.valid || false;
      if (this.hasAutoCommitEdit && isValid) {
        this.grid.getEditorLock().commitCurrentEdit();
      } else {
        this.args.commitChanges();
      }
    }
    serializeValue() {
      return this._input?.value ?? "";
    }
    validate(_targetElm, options) {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
      if (this.disabled) {
        return { valid: true, msg: "" };
      }
      const elmValue = options?.inputValue ?? this._input?.value;
      return textValidator(elmValue, {
        editorArgs: this.args,
        errorMessage: this.columnEditor.errorMessage,
        minLength: this.columnEditor.minLength,
        maxLength: this.columnEditor.maxLength,
        operatorConditionalType: this.columnEditor.operatorConditionalType,
        required: this.args.isCompositeEditor ? false : this.columnEditor.required,
        validator: this.validator
      });
    }
    // --
    // protected functions
    // ------------------
    /** when it's a Composite Editor, we'll check if the Editor is editable (by checking onBeforeEditCell) and if not Editable we'll disable the Editor */
    applyInputUsabilityState() {
      const activeCell = this.grid.getActiveCell();
      const isCellEditable = this.grid.onBeforeEditCell.notify({
        ...activeCell,
        item: this.dataContext,
        column: this.args.column,
        grid: this.grid,
        target: "composite",
        compositeEditorOptions: this.args.compositeEditorOptions
      }).getReturnValue();
      this.disable(isCellEditable === false);
    }
    handleChangeOnCompositeEditor(event, compositeEditorOptions, triggeredBy = "user", isCalledByClearValue = false) {
      const activeCell = this.grid.getActiveCell();
      const column = this.args.column;
      const columnId = this.columnDef?.id ?? "";
      const item = this.dataContext;
      const grid = this.grid;
      const newValue = this.serializeValue();
      if (this.validate().valid) {
        this.applyValue(this.dataContext, newValue);
      }
      this.applyValue(compositeEditorOptions.formValues, newValue);
      const isExcludeDisabledFieldFormValues = this.gridOptions?.compositeEditorOptions?.excludeDisabledFieldFormValues ?? false;
      if (isCalledByClearValue || this.disabled && isExcludeDisabledFieldFormValues && compositeEditorOptions.formValues.hasOwnProperty(columnId)) {
        delete compositeEditorOptions.formValues[columnId];
      }
      grid.onCompositeEditorChange.notify({
        ...activeCell,
        item,
        grid,
        column,
        formValues: compositeEditorOptions.formValues,
        editors: compositeEditorOptions.editors,
        triggeredBy
      }, new SlickEventData(event));
    }
    handleOnInputChange(event) {
      this._isValueTouched = true;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions) {
        const typingDelay = this.gridOptions?.editorTypingDebounce ?? 500;
        clearTimeout(this._timer);
        this._timer = setTimeout(() => this.handleChangeOnCompositeEditor(event, compositeEditorOptions), typingDelay);
      }
    }
    /** When the input value changes (this will cover the input spinner arrows on the right) */
    handleOnMouseWheel(event) {
      this._isValueTouched = true;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions) {
        this.handleChangeOnCompositeEditor(event, compositeEditorOptions);
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/floatEditor.js
  var FloatEditor = class extends InputEditor {
    constructor(args) {
      super(args, "number");
      this.args = args;
    }
    loadValue(item) {
      const fieldName = this.columnDef?.field;
      if (fieldName !== void 0) {
        if (item && fieldName !== void 0 && this._input) {
          const isComplexObject = fieldName?.indexOf(".") > 0;
          const value = isComplexObject ? getDescendantProperty(item, fieldName) : item[fieldName];
          this._originalValue = value;
          const decPlaces = this.getDecimalPlaces();
          if (decPlaces !== null && (this._originalValue || this._originalValue === 0) && this._originalValue !== void 0) {
            this._originalValue = (+this._originalValue).toFixed(decPlaces);
          }
          this._input.value = `${this._originalValue}`;
          this._input.select();
        }
      }
    }
    serializeValue() {
      const elmValue = this._input?.value;
      if (elmValue === void 0 || elmValue === "" || isNaN(+elmValue)) {
        return elmValue;
      }
      let rtn = parseFloat(elmValue);
      const decPlaces = this.getDecimalPlaces();
      if (decPlaces !== null && (rtn || rtn === 0) && rtn.toFixed) {
        rtn = parseFloat(rtn.toFixed(decPlaces));
      }
      return rtn;
    }
    validate(_targetElm, options) {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
      if (this.disabled) {
        return { valid: true, msg: "" };
      }
      const elmValue = options?.inputValue ?? this._input?.value;
      return floatValidator(elmValue, {
        editorArgs: this.args,
        errorMessage: this.columnEditor.errorMessage,
        decimal: this.getDecimalPlaces(),
        minValue: this.columnEditor.minValue,
        maxValue: this.columnEditor.maxValue,
        operatorConditionalType: this.columnEditor.operatorConditionalType,
        required: this.args.isCompositeEditor ? false : this.columnEditor.required,
        validator: this.validator
      });
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/inputPasswordEditor.js
  var InputPasswordEditor = class extends InputEditor {
    /** Initialize the Editor */
    constructor(args) {
      super(args, "password");
      this.args = args;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/integerEditor.js
  var IntegerEditor = class extends InputEditor {
    constructor(args) {
      super(args, "number");
      this.args = args;
    }
    loadValue(item) {
      const fieldName = this.columnDef?.field;
      if (item && fieldName !== void 0 && this._input) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const value = isComplexObject ? getDescendantProperty(item, fieldName) : item[fieldName];
        this._originalValue = isNaN(value) || value === null || value === void 0 ? value : `${value}`;
        this._input.value = `${this._originalValue}`;
        this._input.select();
      }
    }
    serializeValue() {
      const elmValue = this._input?.value;
      if (elmValue === void 0 || elmValue === "" || isNaN(+elmValue)) {
        return elmValue;
      }
      const output = isNaN(+elmValue) ? elmValue : parseInt(elmValue, 10);
      return isNaN(+output) ? elmValue : output;
    }
    validate(_targetElm, options) {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
      if (this.disabled) {
        return { valid: true, msg: "" };
      }
      const elmValue = options?.inputValue ?? this.getValue();
      return integerValidator(elmValue, {
        editorArgs: this.args,
        errorMessage: this.columnEditor.errorMessage,
        minValue: this.columnEditor.minValue,
        maxValue: this.columnEditor.maxValue,
        operatorConditionalType: this.columnEditor.operatorConditionalType,
        required: this.args.isCompositeEditor ? false : this.columnEditor.required,
        validator: this.validator
      });
    }
    // --
    // protected functions
    // ------------------
    /** When the input value changes (this will cover the input spinner arrows on the right) */
    handleOnMouseWheel(event) {
      this._isValueTouched = true;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions) {
        this.handleChangeOnCompositeEditor(event, compositeEditorOptions);
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/longTextEditor.js
  var LongTextEditor = class {
    constructor(args) {
      this.args = args;
      this._isValueTouched = false;
      this.disabled = false;
      this.grid = args.grid;
      this.gridOptions = args.grid?.getOptions();
      const options = this.gridOptions || this.args.column.params || {};
      if (options?.translater) {
        this._translater = options.translater;
      }
      this._locales = this.gridOptions?.locales || Constants.locales;
      this._bindEventService = new BindingEventService();
      this._eventHandler = new SlickEventHandler();
      this.init();
    }
    /** Get Column Definition object */
    get columnDef() {
      return this.args.column;
    }
    /** Get Column Editor object */
    get columnEditor() {
      return this.columnDef?.editor ?? {};
    }
    /** Getter for the item data context object */
    get dataContext() {
      return this.args.item;
    }
    /** Getter for the Editor DOM Element */
    get editorDomElement() {
      return this._textareaElm;
    }
    get editorOptions() {
      return { ...this.gridOptions.defaultEditorOptions?.longText, ...this.columnEditor?.options };
    }
    get hasAutoCommitEdit() {
      return this.gridOptions?.autoCommitEdit ?? false;
    }
    /** Get the Validator function, can be passed in Editor property or Column Definition */
    get validator() {
      return this.columnEditor?.validator ?? this.columnDef?.validator;
    }
    init() {
      let cancelText = "";
      let saveText = "";
      if (this._translater?.translate && this.gridOptions.enableTranslate) {
        const translationPrefix = getTranslationPrefix(this.gridOptions);
        const cancelKey = this.editorOptions.buttonTexts?.cancelKey ?? `${translationPrefix}CANCEL`;
        const saveKey = this.editorOptions.buttonTexts?.saveKey ?? `${translationPrefix}SAVE`;
        cancelText = this._translater.translate(`${translationPrefix}${cancelKey}`);
        saveText = this._translater.translate(`${translationPrefix}${saveKey}`);
      } else {
        cancelText = this.editorOptions.buttonTexts?.cancel ?? this._locales?.TEXT_CANCEL ?? "Cancel";
        saveText = this.editorOptions.buttonTexts?.save ?? this._locales?.TEXT_SAVE ?? "Save";
      }
      const columnId = this.columnDef?.id ?? "";
      const maxLength = this.columnEditor?.maxLength;
      const textAreaRows = this.editorOptions?.rows ?? 4;
      const containerElm = this.args.isCompositeEditor ? this.args.container : document.body;
      this._wrapperElm = createDomElement("div", {
        className: `slick-large-editor-text editor-${columnId}`,
        style: { position: this.args.isCompositeEditor ? "relative" : "absolute" }
      });
      if (this.gridOptions?.darkMode) {
        this._wrapperElm.classList.add("slick-dark-mode");
      }
      containerElm.appendChild(this._wrapperElm);
      const useColWidth = !this.args.isCompositeEditor && (this.editorOptions?.useColumnWidth ?? false);
      this._textareaElm = createDomElement("textarea", {
        ariaLabel: this.columnEditor?.ariaLabel ?? `${toSentenceCase(columnId + "")} Text Editor`,
        cols: this.editorOptions?.cols ?? 40,
        rows: this.args.isCompositeEditor && textAreaRows > 3 ? 3 : textAreaRows,
        placeholder: this.columnEditor?.placeholder ?? "",
        title: this.columnEditor?.title ?? "",
        style: useColWidth ? { width: `${this._getContainerInnerWidth()}px` } : {}
      }, this._wrapperElm);
      if (useColWidth) {
        this._eventHandler.subscribe(this.grid.onColumnsResized, this.handleColumnsResized.bind(this));
      }
      const editorFooterElm = createDomElement("div", { className: "editor-footer" });
      const countContainerElm = createDomElement("span", { className: "counter" });
      this._currentLengthElm = createDomElement("span", { className: "text-length", textContent: "0" });
      countContainerElm.appendChild(this._currentLengthElm);
      if (maxLength !== void 0) {
        countContainerElm.appendChild(createDomElement("span", { className: "separator", textContent: "/" }));
        countContainerElm.appendChild(createDomElement("span", { className: "max-length", textContent: `${maxLength}` }));
      }
      editorFooterElm.appendChild(countContainerElm);
      if (!this.args.isCompositeEditor) {
        const cancelBtnElm = createDomElement("button", { className: "btn btn-cancel btn-default btn-xs", textContent: cancelText });
        const saveBtnElm = createDomElement("button", { className: "btn btn-save btn-primary btn-xs", textContent: saveText });
        if (this.editorOptions.saveButtonPosition === "left") {
          editorFooterElm.appendChild(saveBtnElm);
          editorFooterElm.appendChild(cancelBtnElm);
        } else {
          editorFooterElm.appendChild(cancelBtnElm);
          editorFooterElm.appendChild(saveBtnElm);
        }
        this._bindEventService.bind(cancelBtnElm, "click", this.cancel.bind(this));
        this._bindEventService.bind(saveBtnElm, "click", this.save.bind(this));
        this.position(this.args?.position);
        this._textareaElm.focus();
        this._textareaElm.select();
      }
      this._wrapperElm.appendChild(editorFooterElm);
      this._bindEventService.bind(this._textareaElm, "keydown", this.handleKeyDown.bind(this));
      this._bindEventService.bind(this._textareaElm, "input", this.handleOnInputChange.bind(this));
      this._bindEventService.bind(this._textareaElm, "paste", this.handleOnInputChange.bind(this));
    }
    cancel() {
      const value = this._defaultTextValue || "";
      this._textareaElm.value = value;
      this._currentLengthElm.textContent = `${value.length}`;
      if (this.args?.cancelChanges) {
        this.args.cancelChanges();
      }
    }
    hide() {
      if (!this.args.isCompositeEditor) {
        this._wrapperElm.style.display = "none";
      }
    }
    show() {
      this._wrapperElm.style.display = "block";
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
    }
    destroy() {
      clearTimeout(this._timer);
      this._eventHandler.unsubscribeAll();
      this._bindEventService.unbindAll();
      this._wrapperElm?.remove();
    }
    disable(isDisabled = true) {
      const prevIsDisabled = this.disabled;
      this.disabled = isDisabled;
      if (this._textareaElm && this._wrapperElm) {
        if (isDisabled) {
          this._textareaElm.disabled = true;
          this._wrapperElm.classList.add("disabled");
          const currentValue = this.getValue();
          if (prevIsDisabled !== isDisabled && this.args.isCompositeEditor && currentValue !== "") {
            this.reset("", true, true);
          }
        } else {
          this._textareaElm.disabled = false;
          this._wrapperElm.classList.remove("disabled");
        }
      }
    }
    focus() {
      this.grid.focus("internal");
      if (this._textareaElm) {
        this._textareaElm.focus();
        this._textareaElm.select();
      }
    }
    getValue() {
      return this._textareaElm.value;
    }
    setValue(val, isApplyingValue = false, triggerOnCompositeEditorChange = true) {
      this._textareaElm.value = val;
      this._currentLengthElm.textContent = `${val.length}`;
      if (isApplyingValue) {
        this.applyValue(this.args.item, this.serializeValue());
        const compositeEditorOptions = this.args.compositeEditorOptions;
        if (compositeEditorOptions && triggerOnCompositeEditorChange) {
          this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "system");
        }
      }
    }
    applyValue(item, state2) {
      const fieldName = this.columnDef?.field;
      if (fieldName !== void 0) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const validation = this.validate(void 0, state2);
        const newValue = validation?.valid ? state2 : "";
        if (isComplexObject) {
          const objectPath = this.columnEditor?.complexObjectPath ?? fieldName ?? "";
          setDeepValue(item, objectPath, newValue);
        } else {
          item[fieldName] = newValue;
        }
      }
    }
    isValueChanged() {
      const elmValue = this._textareaElm.value;
      return !(elmValue === "" && (this._defaultTextValue === null || this._defaultTextValue === void 0)) && elmValue !== this._defaultTextValue;
    }
    isValueTouched() {
      return this._isValueTouched;
    }
    loadValue(item) {
      const fieldName = this.columnDef?.field;
      if (item && fieldName !== void 0) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const value = isComplexObject ? getDescendantProperty(item, fieldName) : item[fieldName];
        this._defaultTextValue = value || "";
        this._textareaElm.value = this._defaultTextValue;
        this._currentLengthElm.textContent = this._defaultTextValue.length;
        this._textareaElm.defaultValue = this._defaultTextValue;
        this._textareaElm.select();
      }
    }
    /**
     * Reposition the LongText Editor to be right over the cell, so that it looks like we opened the editor on top of the cell when in reality we just reposition (absolute) over the cell.
     * By default we use an "auto" mode which will allow to position the LongText Editor to the best logical position in the window, also when we say position, we are talking about the relative position against the grid cell.
     * We can assume that in 80% of the time the default position is bottom right, the default is "auto" but we can also override this and use a specific position.
     * Most of the time positioning of the editor will be to the "right" of the cell is ok but if our column is completely on the right side then we'll want to change the position to "left" align.
     * Same goes for the top/bottom position, Most of the time positioning the editor to the "bottom" but we are clicking on a cell at the bottom of the grid then we might need to reposition to "top" instead.
     * NOTE: this only applies to Inline Editing and will not have any effect when using the Composite Editor modal window.
     */
    position(parentPosition) {
      const containerOffset = getOffset(this.args.container);
      const containerHeight = this.args.container.offsetHeight;
      const containerWidth = this.args.container.offsetWidth;
      const calculatedEditorHeight = this._wrapperElm.getBoundingClientRect().height || this.args.position.height;
      const calculatedEditorWidth = this._wrapperElm.getBoundingClientRect().width || this.args.position.width;
      const calculatedBodyHeight = document.body.offsetHeight || window.innerHeight;
      const calculatedBodyWidth = document.body.offsetWidth || window.innerWidth;
      const containerPaddingLeft = parseFloat(getComputedStyle(this.args.container).paddingLeft) || 0;
      let newPositionTop = this.args.container ? containerOffset.top : parentPosition.top ?? 0;
      let newPositionLeft = this.args.container ? containerOffset.left + containerPaddingLeft : parentPosition.left ?? 0;
      const position = this.editorOptions?.position ?? "auto";
      if (position === "left" || position === "auto" && newPositionLeft + calculatedEditorWidth > calculatedBodyWidth) {
        const marginRightAdjustment = this.editorOptions?.marginRight ?? 0;
        newPositionLeft -= calculatedEditorWidth - containerWidth + marginRightAdjustment;
      }
      if (position === "top" || position === "auto" && newPositionTop + calculatedEditorHeight > calculatedBodyHeight) {
        newPositionTop -= calculatedEditorHeight - containerHeight;
      }
      this._wrapperElm.style.top = `${newPositionTop}px`;
      this._wrapperElm.style.left = `${newPositionLeft}px`;
    }
    /**
     * You can reset or clear the input value,
     * when no value is provided it will use the original value to reset (could be useful with Composite Editor Modal with edit/clone)
     */
    reset(value, triggerCompositeEventWhenExist = true, clearByDisableCommand = false) {
      const inputValue = value ?? this._defaultTextValue ?? "";
      if (this._textareaElm) {
        this._defaultTextValue = inputValue;
        this._textareaElm.value = inputValue;
        this._currentLengthElm.textContent = inputValue.length;
      }
      this._isValueTouched = false;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions && triggerCompositeEventWhenExist) {
        const shouldDeleteFormValue = !clearByDisableCommand;
        this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "user", shouldDeleteFormValue);
      }
    }
    save() {
      const validation = this.validate();
      const isValid = validation?.valid ?? false;
      if (this.hasAutoCommitEdit && isValid) {
        this.grid.getEditorLock().commitCurrentEdit();
      } else {
        this.args.commitChanges();
      }
    }
    serializeValue() {
      return this._textareaElm.value;
    }
    validate(_targetElm, options) {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
      if (this.disabled) {
        return { valid: true, msg: "" };
      }
      const elmValue = options?.inputValue ?? this._textareaElm?.value;
      return textValidator(elmValue, {
        editorArgs: this.args,
        errorMessage: this.columnEditor.errorMessage,
        minLength: this.columnEditor.minLength,
        maxLength: this.columnEditor.maxLength,
        operatorConditionalType: this.columnEditor.operatorConditionalType,
        required: this.args.isCompositeEditor ? false : this.columnEditor.required,
        validator: this.validator
      });
    }
    // --
    // protected functions
    // ------------------
    /** when it's a Composite Editor, we'll check if the Editor is editable (by checking onBeforeEditCell) and if not Editable we'll disable the Editor */
    applyInputUsabilityState() {
      const activeCell = this.grid.getActiveCell();
      const isCellEditable = this.grid.onBeforeEditCell.notify({
        ...activeCell,
        item: this.dataContext,
        column: this.args.column,
        grid: this.grid,
        target: "composite",
        compositeEditorOptions: this.args.compositeEditorOptions
      }).getReturnValue();
      this.disable(isCellEditable === false);
    }
    handleColumnsResized() {
      if (this._textareaElm) {
        this._textareaElm.style.width = `${this._getContainerInnerWidth()}px`;
      }
    }
    _getContainerInnerWidth() {
      const container = this.args.container;
      const containerStyle = getComputedStyle(container);
      const wrapperStyle = getComputedStyle(this._wrapperElm);
      return container.clientWidth - (parseFloat(containerStyle.paddingLeft) || 0) - (parseFloat(containerStyle.paddingRight) || 0) - (parseFloat(wrapperStyle.paddingLeft) || 0) - (parseFloat(wrapperStyle.paddingRight) || 0);
    }
    handleKeyDown(e2) {
      const key = e2.key;
      this._isValueTouched = true;
      if (!this.args.isCompositeEditor) {
        if (key === "Enter" && e2.ctrlKey || e2.ctrlKey && e2.key.toUpperCase() === "S") {
          e2.preventDefault();
          this.save();
        } else if (key === "Escape") {
          e2.preventDefault();
          this.cancel();
        } else if (key === "Tab") {
          e2.preventDefault();
          e2.shiftKey ? this.grid.navigatePrev() : this.grid.navigateNext();
        }
      }
    }
    /** On every input change event, we'll update the current text length counter */
    handleOnInputChange(event) {
      const compositeEditorOptions = this.args.compositeEditorOptions;
      const maxLength = this.columnEditor?.maxLength;
      let isTruncated = false;
      if (maxLength) {
        isTruncated = this.truncateText(this._textareaElm, maxLength);
      }
      if (isTruncated) {
        this._currentLengthElm.textContent = `${maxLength}`;
      } else {
        const newText = event.type === "paste" ? event.clipboardData.getData("text") : event.target.value;
        this._currentLengthElm.textContent = `${newText.length}`;
      }
      if (compositeEditorOptions) {
        const typingDelay = this.gridOptions?.editorTypingDebounce ?? 500;
        clearTimeout(this._timer);
        this._timer = setTimeout(() => this.handleChangeOnCompositeEditor(event, compositeEditorOptions), typingDelay);
      }
    }
    handleChangeOnCompositeEditor(event, compositeEditorOptions, triggeredBy = "user", isCalledByClearValue = false) {
      const activeCell = this.grid.getActiveCell();
      const column = this.args.column;
      const columnId = this.columnDef?.id ?? "";
      const item = this.dataContext;
      const grid = this.grid;
      const newValue = this.serializeValue();
      if (this.validate().valid) {
        this.applyValue(this.dataContext, newValue);
      }
      this.applyValue(compositeEditorOptions.formValues, newValue);
      const isExcludeDisabledFieldFormValues = this.gridOptions?.compositeEditorOptions?.excludeDisabledFieldFormValues ?? false;
      if (isCalledByClearValue || this.disabled && isExcludeDisabledFieldFormValues && compositeEditorOptions.formValues.hasOwnProperty(columnId)) {
        delete compositeEditorOptions.formValues[columnId];
      }
      grid.onCompositeEditorChange.notify({
        ...activeCell,
        item,
        grid,
        column,
        formValues: compositeEditorOptions.formValues,
        editors: compositeEditorOptions.editors,
        triggeredBy
      }, new SlickEventData(event));
    }
    /**
     * Truncate text if the value is longer than the acceptable max length
     * @param inputElm - textarea html element
     * @param maxLength - max acceptable length
     * @returns truncated - returns True if it truncated or False otherwise
     */
    truncateText(inputElm, maxLength) {
      const text = inputElm.value + "";
      if (text.length > maxLength) {
        inputElm.value = text.substring(0, maxLength);
        return true;
      }
      return false;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/selectEditor.js
  var SelectEditor = class {
    constructor(args, isMultipleSelect) {
      this.args = args;
      this.isMultipleSelect = isMultipleSelect;
      this._isValueTouched = false;
      this._isDisposingOrCallingSave = false;
      this.disabled = false;
      this.enableTranslateLabel = false;
      this.finalCollection = [];
      this.grid = args.grid;
      this.gridOptions = this.grid.getOptions() || {};
      if (this.gridOptions?.translater) {
        this._translaterService = this.gridOptions.translater;
      }
      this._locales = this.gridOptions.locales || Constants.locales;
      this.elementName = `editor-${this.columnId}`;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      const libOptions = {
        autoAdjustDropHeight: true,
        autoAdjustDropPosition: true,
        autoAdjustDropWidthByTextSize: true,
        container: "body",
        darkMode: !!this.gridOptions.darkMode,
        filter: false,
        isOpen: !this.args.isCompositeEditor,
        maxHeight: 275,
        minHeight: 25,
        name: this.elementName,
        single: true,
        singleRadio: true,
        renderOptionLabelAsHtml: this.columnEditor?.enableRenderHtml ?? false,
        sanitizer: (dirtyHtml) => this.grid.sanitizeHtmlString(dirtyHtml),
        onClick: () => this._isValueTouched = true,
        onCheckAll: () => this._isValueTouched = true,
        onUncheckAll: () => this._isValueTouched = true,
        onBlur: (e2) => {
          const keyEvt = e2;
          if (keyEvt?.key === "Tab" && this._msInstance?.getOptions().isOpen) {
            keyEvt.preventDefault();
            this._msInstance.close("blur");
            if (!this.args.isCompositeEditor) {
              keyEvt.shiftKey ? this.grid.navigatePrev() : this.grid.navigateNext();
            }
          }
        },
        onClose: (reason) => {
          if (reason === "key.escape" || !this.hasAutoCommitEdit && !this.isValueChanged()) {
            if (reason === "key.escape") {
              this.cancel();
            }
            return;
          }
          if (compositeEditorOptions) {
            this.handleChangeOnCompositeEditor(compositeEditorOptions);
          } else {
            this._isDisposingOrCallingSave = true;
            this.save(this.hasAutoCommitEdit);
          }
        }
      };
      if (isMultipleSelect) {
        libOptions.single = false;
        libOptions.singleRadio = false;
        libOptions.displayTitle = true;
        libOptions.showOkButton = true;
        const translationPrefix = getTranslationPrefix(this.gridOptions);
        libOptions.countSelectedText = this.translateOrDefault(`${translationPrefix}X_OF_Y_SELECTED`, this._locales?.TEXT_X_OF_Y_SELECTED);
        libOptions.allSelectedText = this.translateOrDefault(`${translationPrefix}ALL_SELECTED`, this._locales?.TEXT_ALL_SELECTED);
        libOptions.selectAllText = this.translateOrDefault(`${translationPrefix}SELECT_ALL`, this._locales?.TEXT_SELECT_ALL);
        libOptions.okButtonText = this.translateOrDefault(`${translationPrefix}OK`, this._locales?.TEXT_OK);
        libOptions.noMatchesFoundText = this.translateOrDefault(`${translationPrefix}NO_MATCHES_FOUND`, this._locales?.TEXT_NO_MATCHES_FOUND);
      }
      this.defaultOptions = libOptions;
      this.init();
    }
    /** Get the Collection */
    get collection() {
      return this.columnEditor?.collection ?? [];
    }
    /** Getter for the Collection Options */
    get collectionOptions() {
      return this.columnEditor?.collectionOptions;
    }
    /** Get Column Definition object */
    get columnDef() {
      return this.args.column;
    }
    get columnId() {
      return this.columnDef?.id ?? "";
    }
    /** Get Column Editor object */
    get columnEditor() {
      return this.columnDef?.editor ?? {};
    }
    /** Getter for item data context object */
    get dataContext() {
      return this.args.item;
    }
    get editorOptions() {
      return { ...this.gridOptions.defaultEditorOptions?.select, ...this.columnEditor?.options };
    }
    /** Getter for the Custom Structure if exist */
    get customStructure() {
      return this.columnDef?.editor?.customStructure;
    }
    get hasAutoCommitEdit() {
      return this.gridOptions.autoCommitEdit ?? false;
    }
    get msInstance() {
      return this._msInstance;
    }
    get selectOptions() {
      return this.defaultOptions;
    }
    /**
     * The current selected values (multiple select) from the collection
     */
    get currentValues() {
      const selectedValuesSet = /* @__PURE__ */ new Set();
      (this._msInstance?.getSelects("value") ?? []).forEach((x2) => selectedValuesSet.add(x2.toString()));
      if (this.collection.every((x2) => typeof x2 === "number" || typeof x2 === "string")) {
        return this.collection.filter((c) => selectedValuesSet.has(c?.toString()));
      }
      const separatorBetweenLabels = this.collectionOptions?.separatorBetweenTextLabels ?? "";
      const isIncludingPrefixSuffix = this.collectionOptions?.includePrefixSuffixToSelectedValues ?? false;
      return this.collection.filter((c) => selectedValuesSet.has(c?.[this.valueName]?.toString())).map((c) => {
        const labelText = c[this.valueName];
        let prefixText = c[this.labelPrefixName] || "";
        let suffixText = c[this.labelSuffixName] || "";
        const fieldName = this.columnDef?.field ?? "";
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const serializeComplexValueFormat = this.columnEditor?.serializeComplexValueFormat ?? "object";
        if (isComplexObject && typeof c === "object" && serializeComplexValueFormat === "object") {
          return c;
        }
        prefixText = this.translatePrefixSuffix(prefixText);
        suffixText = this.translatePrefixSuffix(suffixText);
        if (isIncludingPrefixSuffix) {
          const tmpOptionArray = [prefixText, labelText, suffixText].filter((text) => text);
          return tmpOptionArray.join(separatorBetweenLabels);
        }
        return labelText;
      });
    }
    /**
     * The current selected values (single select) from the collection
     */
    get currentValue() {
      const selectedValues = this._msInstance?.getSelects() ?? [];
      const selectedValue = selectedValues.length ? selectedValues[0] : "";
      const fieldName = this.columnDef?.field;
      if (fieldName !== void 0) {
        if (this.collection.every((x2) => typeof x2 === "number" || typeof x2 === "string")) {
          return findOrDefault(this.collection, (c) => c?.toString?.() === `${selectedValue}`);
        }
        const separatorBetweenLabels = this.collectionOptions?.separatorBetweenTextLabels ?? "";
        const isIncludingPrefixSuffix = this.collectionOptions?.includePrefixSuffixToSelectedValues ?? false;
        const itemFound = findOrDefault(this.collection, (c) => c.hasOwnProperty(this.valueName) && c[this.valueName]?.toString() === `${selectedValue}`);
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const serializeComplexValueFormat = this.columnEditor?.serializeComplexValueFormat ?? "object";
        if (isComplexObject && typeof itemFound === "object" && serializeComplexValueFormat === "object") {
          return itemFound;
        } else if (itemFound && itemFound.hasOwnProperty(this.valueName)) {
          const labelText = itemFound[this.valueName];
          if (isIncludingPrefixSuffix) {
            let prefixText = itemFound[this.labelPrefixName] || "";
            let suffixText = itemFound[this.labelSuffixName] || "";
            prefixText = this.translatePrefixSuffix(prefixText);
            suffixText = this.translatePrefixSuffix(suffixText);
            const tmpOptionArray = [prefixText, labelText, suffixText].filter((text) => text);
            return tmpOptionArray.join(separatorBetweenLabels);
          }
          return labelText;
        }
      }
      return "";
    }
    /** Get the Validator function, can be passed in Editor property or Column Definition */
    get validator() {
      return this.columnEditor?.validator ?? this.columnDef?.validator;
    }
    init() {
      if (!this.columnDef || !this.columnDef.editor || !this.columnDef.editor.collection && !this.columnDef.editor.collectionAsync) {
        throw new Error(`[Slickgrid-Universal] You need to pass a "collection" (or "collectionAsync") inside Column Definition Editor for the MultipleSelect/SingleSelect Editor to work correctly.
      Also each option should include a value/label pair (or value/labelKey when using Locale).
      For example: { editor: { collection: [{ value: true, label: 'True' },{ value: false, label: 'False'}] } }`);
      }
      this._collectionService = new CollectionService(this._translaterService);
      this.enableTranslateLabel = this.columnEditor?.enableTranslateLabel ?? false;
      this.labelName = this.customStructure?.label ?? "label";
      this.labelPrefixName = this.customStructure?.labelPrefix ?? "labelPrefix";
      this.labelSuffixName = this.customStructure?.labelSuffix ?? "labelSuffix";
      this.optionLabel = this.customStructure?.optionLabel ?? "value";
      this.valueName = this.customStructure?.value ?? "value";
      if (this.enableTranslateLabel && (!this._translaterService || typeof this._translaterService.translate !== "function")) {
        throw new Error('[Slickgrid-Universal] requires a Translate Service to be installed and configured when the grid option "enableTranslate" is enabled.');
      }
      this.renderDomElement(this.collection);
      if (this.disabled || this.columnEditor?.collectionAsync && Array.isArray(this.collection) && this.collection.length === 0) {
        this.disable(true);
      }
    }
    getValue() {
      return this.isMultipleSelect ? this.currentValues : this.currentValue;
    }
    setValue(value, isApplyingValue = false, triggerOnCompositeEditorChange = true) {
      if (this.isMultipleSelect && Array.isArray(value)) {
        this.loadMultipleValues(value);
      } else {
        this.loadSingleValue(value);
      }
      if (isApplyingValue) {
        this.applyValue(this.args.item, this.serializeValue());
        const compositeEditorOptions = this.args.compositeEditorOptions;
        if (compositeEditorOptions && triggerOnCompositeEditorChange) {
          this.handleChangeOnCompositeEditor(compositeEditorOptions, "system");
        }
      }
    }
    cancel() {
      if (this.args?.cancelChanges) {
        this.args.cancelChanges();
      }
    }
    hide() {
      if (this._msInstance) {
        this._msInstance.close();
      }
    }
    show(openDelay) {
      if (!this.args.isCompositeEditor && this._msInstance) {
        this._msInstance.open(openDelay);
      } else if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
    }
    applyValue(item, state2) {
      const fieldName = this.columnDef?.field;
      const fieldType = this.columnDef?.type;
      let newValue = state2;
      if (fieldName !== void 0) {
        if ((fieldType === "number" || fieldType === "integer" || fieldType === "boolean") && !isNaN(parseFloat(state2))) {
          newValue = parseFloat(state2);
        }
        if (this.isMultipleSelect && typeof state2 === "string" && state2.indexOf(",") >= 0) {
          newValue = state2.split(",");
        }
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const validation = this.validate(null, newValue);
        newValue = validation?.valid ? newValue : "";
        if (isComplexObject) {
          const objectPath = this.columnEditor?.complexObjectPath ?? fieldName ?? "";
          setDeepValue(item, objectPath, newValue);
        } else {
          item[fieldName] = newValue;
        }
      }
    }
    destroy() {
      if (this._msInstance && this.hasAutoCommitEdit && this.isValueChanged() && !this._isDisposingOrCallingSave && !this.args.isCompositeEditor) {
        this._isDisposingOrCallingSave = true;
        this.save(true);
      }
      this._isDisposingOrCallingSave = true;
      if (typeof this._msInstance?.destroy === "function") {
        this._msInstance.destroy();
      }
      this.editorElm?.remove();
      this._msInstance = void 0;
    }
    loadValue(item) {
      const fieldName = this.columnDef?.field;
      const isComplexObject = fieldName !== void 0 && fieldName?.indexOf(".") > 0;
      if (item && fieldName !== void 0) {
        const objectPath = this.columnEditor?.complexObjectPath ?? fieldName;
        const currentValue = isComplexObject ? getDescendantProperty(item, objectPath) : item.hasOwnProperty(fieldName) && item[fieldName];
        const value = isComplexObject && currentValue?.hasOwnProperty(this.valueName) ? currentValue[this.valueName] : currentValue;
        if (this.isMultipleSelect && Array.isArray(value)) {
          this.loadMultipleValues(value);
        } else {
          this.loadSingleValue(value);
        }
      }
    }
    loadMultipleValues(currentValues) {
      if (Array.isArray(currentValues)) {
        this.originalValue = currentValues.map((i) => typeof i === "number" || typeof i === "boolean" ? `${i}` : i);
        this._msInstance?.setSelects(this.originalValue);
        const compositeEditorOptions = this.args.compositeEditorOptions;
        if (compositeEditorOptions) {
          this.handleChangeOnCompositeEditor(compositeEditorOptions);
        }
      }
    }
    loadSingleValue(currentValue) {
      this.originalValue = typeof currentValue === "number" || typeof currentValue === "boolean" ? `${currentValue}` : currentValue;
      this._msInstance?.setSelects([this.originalValue]);
    }
    serializeValue() {
      return this.isMultipleSelect ? this.currentValues : this.currentValue;
    }
    /**
     * Dynamically change an Editor option, this is especially useful with Composite Editor
     * since this is the only way to change option after the Editor is created (for example dynamically change "minDate" or another Editor)
     * @param {string} optionName - MultipleSelect option name
     * @param {newValue} newValue - MultipleSelect new option value
     */
    changeEditorOption(optionName, newValue) {
      if (this.columnEditor) {
        this.columnEditor.options ??= {};
        this.columnEditor.options[optionName] = newValue;
        this.editorElmOptions = { ...this.editorElmOptions, [optionName]: newValue };
        this._msInstance?.refreshOptions(this.editorElmOptions);
      }
    }
    disable(isDisabled = true) {
      const prevIsDisabled = this.disabled;
      this.disabled = isDisabled;
      if (this._msInstance) {
        if (isDisabled) {
          this._msInstance.disable();
          const currentValues = this.getValue();
          const isValueBlank = Array.isArray(currentValues) && this.isMultipleSelect ? currentValues?.[0] === "" : currentValues === "";
          if (prevIsDisabled !== isDisabled && this.args.isCompositeEditor && !isValueBlank) {
            this.reset("", true, true);
          }
        } else {
          this._msInstance.enable();
        }
      }
    }
    focus() {
      this.grid.focus("internal");
      this._msInstance?.focus();
    }
    isValueChanged() {
      const valueSelection = this._msInstance?.getSelects();
      if (this.isMultipleSelect) {
        const isEqual2 = dequal(valueSelection, this.originalValue);
        return !isEqual2;
      }
      const value = Array.isArray(valueSelection) && valueSelection.length > 0 ? valueSelection[0] : void 0;
      return value !== void 0 && value !== this.originalValue;
    }
    isValueTouched() {
      return this._isValueTouched;
    }
    /**
     * You can reset or clear the input value,
     * when no value is provided it will use the original value to reset (could be useful with Composite Editor Modal with edit/clone)
     */
    reset(value, triggerCompositeEventWhenExist = true, clearByDisableCommand = false) {
      const inputValue = value ?? this.originalValue;
      if (this._msInstance) {
        this.originalValue = this.isMultipleSelect ? inputValue !== void 0 ? [inputValue] : [] : inputValue;
        const selection = this.originalValue === void 0 ? [] : [this.originalValue];
        this._msInstance.setSelects(selection);
      }
      this._isValueTouched = false;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions && triggerCompositeEventWhenExist) {
        const shouldDeleteFormValue = !clearByDisableCommand;
        this.handleChangeOnCompositeEditor(compositeEditorOptions, "user", shouldDeleteFormValue);
      }
    }
    save(forceCommitCurrentEdit = false) {
      const validation = this.validate();
      const isValid = validation?.valid ?? false;
      if ((!this._isDisposingOrCallingSave || forceCommitCurrentEdit) && this.hasAutoCommitEdit && isValid) {
        this.grid.getEditorLock().commitCurrentEdit();
      } else {
        this.args.commitChanges();
      }
    }
    validate(_targetElm, options) {
      const isRequired = this.args.isCompositeEditor ? false : this.columnEditor?.required;
      const elmValue = options?.inputValue ?? this._msInstance?.getSelects();
      const errorMsg = this.columnEditor?.errorMessage;
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
      if (this.disabled) {
        return { valid: true, msg: "" };
      }
      if (this.validator) {
        const value = options !== void 0 ? options : this.isMultipleSelect ? this.currentValues : this.currentValue;
        return this.validator(value, this.args);
      }
      if (isRequired && (elmValue === "" || Array.isArray(elmValue) && elmValue.length === 0)) {
        return {
          valid: false,
          msg: errorMsg || Constants.VALIDATION_REQUIRED_FIELD
        };
      }
      return {
        valid: true,
        msg: null
      };
    }
    //
    // protected functions
    // ------------------
    /** when it's a Composite Editor, we'll check if the Editor is editable (by checking onBeforeEditCell) and if not Editable we'll disable the Editor */
    applyInputUsabilityState() {
      const activeCell = this.grid.getActiveCell();
      const isCellEditable = this.grid.onBeforeEditCell.notify({
        ...activeCell,
        item: this.dataContext,
        column: this.args.column,
        grid: this.grid,
        target: "composite",
        compositeEditorOptions: this.args.compositeEditorOptions
      }).getReturnValue();
      this.disable(isCellEditable === false);
    }
    translatePrefixSuffix(prefixSuffix) {
      return this.enableTranslateLabel && this._translaterService && prefixSuffix && typeof prefixSuffix === "string" ? this._translaterService.translate(prefixSuffix || " ") : prefixSuffix;
    }
    translateOrDefault(translationKey, defaultValue = "") {
      const isTranslateEnabled = this.gridOptions?.enableTranslate ?? false;
      return isTranslateEnabled && this._translaterService?.translate ? this._translaterService.translate(translationKey) : defaultValue;
    }
    renderDomElement(inputCollection) {
      inputCollection = getCollectionFromObjectWhenEnabled(inputCollection, this.columnEditor);
      if (!Array.isArray(inputCollection)) {
        throw new Error('The "collection" passed to the Select Editor is not a valid array.');
      }
      let collection = [];
      if (inputCollection.length > 0) {
        collection = [...inputCollection];
      }
      if (this.collectionOptions?.addBlankEntry && Array.isArray(collection) && collection.length > 0 && collection[0][this.valueName] !== "") {
        collection.unshift(createBlankSelectEntry(this.labelName, this.valueName, this.labelPrefixName, this.labelSuffixName));
        this.collection.unshift(createBlankSelectEntry(this.labelName, this.valueName, this.labelPrefixName, this.labelSuffixName));
      }
      if (this.collectionOptions?.addCustomFirstEntry && Array.isArray(collection) && collection.length > 0 && collection[0][this.valueName] !== this.collectionOptions.addCustomFirstEntry[this.valueName]) {
        collection.unshift(this.collectionOptions.addCustomFirstEntry);
        this.collection.unshift(this.collectionOptions.addCustomFirstEntry);
      }
      if (this.collectionOptions?.addCustomLastEntry && Array.isArray(collection) && collection.length > 0) {
        const lastCollectionIndex = collection.length - 1;
        if (collection[lastCollectionIndex][this.valueName] !== this.collectionOptions.addCustomLastEntry[this.valueName]) {
          collection.push(this.collectionOptions.addCustomLastEntry);
        }
      }
      let finalCollection = collection;
      finalCollection = filterCollectionWithOptions(finalCollection, this._collectionService, this.columnEditor?.collectionFilterBy, this.collectionOptions);
      finalCollection = sortCollectionWithOptions(finalCollection, this.columnDef, this._collectionService, this.columnEditor?.collectionSortBy, this.enableTranslateLabel);
      if (this.columnEditor?.collectionOverride) {
        const overrideArgs = {
          column: this.columnDef,
          dataContext: this.dataContext,
          grid: this.grid,
          originalCollections: this.collection
        };
        if (this.args.compositeEditorOptions) {
          const { formValues, modalType } = this.args.compositeEditorOptions;
          overrideArgs.compositeEditorOptions = { formValues, modalType };
        }
        finalCollection = this.columnEditor.collectionOverride(finalCollection, overrideArgs);
      }
      this.finalCollection = finalCollection;
      const selectBuildResult = buildMsSelectCollectionList("editor", finalCollection, this.columnDef, this.grid, this.isMultipleSelect, this._translaterService);
      this.createDomElement(selectBuildResult.selectElement, selectBuildResult.dataCollection);
    }
    /**
     * From the Select DOM Element created earlier, create a Multiple/Single Select Editor using the multiple-select-vanilla.js lib
     * @param {Object} selectElement
     */
    createDomElement(selectElement, dataCollection) {
      const cellContainer = this.args.container;
      if (selectElement && cellContainer && typeof cellContainer.appendChild === "function") {
        emptyElement(cellContainer);
        cellContainer.appendChild(selectElement);
      }
      const placeholder = this.columnEditor?.placeholder ?? "";
      this.defaultOptions.placeholder = placeholder || "";
      this.editorElmOptions = { ...this.defaultOptions, ...this.editorOptions, data: dataCollection };
      this._msInstance = pt(selectElement, this.editorElmOptions);
      this.editorElm = this._msInstance.getParentElement();
      this.columnEditor.onInstantiated?.(this._msInstance);
    }
    handleChangeOnCompositeEditor(compositeEditorOptions, triggeredBy = "user", isCalledByClearValue = false) {
      const activeCell = this.grid.getActiveCell();
      const column = this.args.column;
      const item = this.dataContext;
      const grid = this.grid;
      const newValues = this.serializeValue();
      if (this.validate().valid) {
        this.applyValue(this.dataContext, newValues);
      }
      this.applyValue(compositeEditorOptions.formValues, newValues);
      const isExcludeDisabledFieldFormValues = this.gridOptions?.compositeEditorOptions?.excludeDisabledFieldFormValues ?? false;
      if (isCalledByClearValue || this.disabled && isExcludeDisabledFieldFormValues && compositeEditorOptions.formValues.hasOwnProperty(this.columnId)) {
        delete compositeEditorOptions.formValues[this.columnId];
      }
      grid.onCompositeEditorChange.notify({
        ...activeCell,
        item,
        grid,
        column,
        formValues: compositeEditorOptions.formValues,
        editors: compositeEditorOptions.editors,
        triggeredBy
      }, new SlickEventData());
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/multipleSelectEditor.js
  var MultipleSelectEditor = class extends SelectEditor {
    /**
     * Initialize the Editor
     */
    constructor(args) {
      super(args, true);
      this.args = args;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/singleSelectEditor.js
  var SingleSelectEditor = class extends SelectEditor {
    /**
     * Initialize the Editor
     */
    constructor(args) {
      super(args, false);
      this.args = args;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/sliderEditor.js
  var SliderEditor = class {
    constructor(args) {
      this.args = args;
      this._defaultValue = 0;
      this._isValueTouched = false;
      this._sliderNumberElm = null;
      this.disabled = false;
      this.grid = args.grid;
      this.gridOptions = this.grid.getOptions() || {};
      this._bindEventService = new BindingEventService();
      this.init();
    }
    /** Get Column Definition object */
    get columnDef() {
      return this.args.column;
    }
    /** Get Column Editor object */
    get columnEditor() {
      return this.columnDef?.editor ?? {};
    }
    /** Getter for the item data context object */
    get dataContext() {
      return this.args.item;
    }
    /** Getter for the Editor DOM Element */
    get editorDomElement() {
      return this._editorElm;
    }
    /** Getter for the Editor Input DOM Element */
    get editorInputDomElement() {
      return this._inputElm;
    }
    get editorOptions() {
      return { ...this.gridOptions.defaultEditorOptions?.slider, ...this.columnEditor?.options };
    }
    get hasAutoCommitEdit() {
      return this.gridOptions.autoCommitEdit ?? false;
    }
    /** Getter for the current Slider Options */
    get sliderOptions() {
      return this._sliderOptions;
    }
    /** Get the Validator function, can be passed in Editor property or Column Definition */
    get validator() {
      return this.columnEditor.validator ?? this.columnDef?.validator;
    }
    init() {
      this._cellContainerElm = this.args?.container;
      if (this._cellContainerElm && this.columnDef) {
        const compositeEditorOptions = this.args.compositeEditorOptions;
        this._editorElm = this.buildDomElement();
        this._cellContainerElm.appendChild(this._editorElm);
        this._bindEventService.bind(this._sliderTrackElm, ["click", "mouseup"], this.sliderTrackClicked.bind(this));
        this._bindEventService.bind(this._inputElm, ["change", "mouseup", "touchend"], this.handleChangeEvent.bind(this));
        this._bindEventService.bind(this._inputElm, ["keydown"], this.handleKeyDown.bind(this));
        this._bindEventService.bind(this._inputElm, ["input", "change"], this.handleChangeSliderNumber.bind(this));
        if (!compositeEditorOptions) {
          this.focus();
        }
      }
    }
    cancel() {
      if (this._inputElm) {
        this._inputElm.value = `${this._originalValue}`;
      }
      this.args.cancelChanges();
    }
    destroy() {
      this._bindEventService.unbindAll();
      this._sliderTrackElm?.remove();
      this._inputElm?.remove();
      this._editorElm?.remove();
    }
    disable(isDisabled = true) {
      const prevIsDisabled = this.disabled;
      this.disabled = isDisabled;
      if (this._inputElm) {
        if (isDisabled) {
          this._inputElm.disabled = true;
          const currentValue = this.getValue();
          if (prevIsDisabled !== isDisabled && this.args?.compositeEditorOptions && currentValue !== "") {
            this.reset(0, true, true);
          }
        } else {
          this._inputElm.disabled = false;
        }
      }
    }
    focus() {
      this.grid.focus("internal");
      this._inputElm?.focus();
    }
    show() {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
    }
    getValue() {
      return this._inputElm?.value ?? "";
    }
    setValue(value, isApplyingValue = false, triggerOnCompositeEditorChange = true) {
      if (this._inputElm) {
        this._inputElm.value = `${value}`;
      }
      if (this._sliderNumberElm) {
        this._sliderNumberElm.textContent = `${value}`;
      }
      if (isApplyingValue) {
        this.applyValue(this.args.item, this.serializeValue());
        const compositeEditorOptions = this.args.compositeEditorOptions;
        if (compositeEditorOptions && triggerOnCompositeEditorChange) {
          this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "system");
        }
      }
    }
    applyValue(item, state2) {
      const fieldName = this.columnDef?.field ?? "";
      if (fieldName !== void 0) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        const validation = this.validate(void 0, state2);
        const newValue = validation && validation.valid ? state2 : "";
        if (isComplexObject) {
          const objectPath = this.columnEditor.complexObjectPath ?? fieldName ?? "";
          setDeepValue(item, objectPath, newValue);
        } else if (item) {
          item[fieldName] = newValue;
        }
      }
    }
    /**
     * Dynamically change an Editor option, this is especially useful with Composite Editor
     * since this is the only way to change option after the Editor is created (for example dynamically change "minDate" or another Editor)
     * @param {string} optionName - Slider editor option name
     * @param {newValue} newValue - Slider editor new option value
     */
    changeEditorOption(optionName, newValue) {
      if (this.columnEditor) {
        this.columnEditor.options ??= {};
        this.columnEditor.options[optionName] = newValue;
        this._sliderOptions[optionName] = newValue;
        switch (optionName) {
          case "hideSliderNumber":
            this.renderSliderNumber(this._editorElm, 0);
            break;
          case "sliderStartValue":
            this._inputElm.value = `${newValue}`;
            this._inputElm.defaultValue = `${newValue}`;
            break;
          case "maxValue":
          case "minValue":
          case "step":
            this._inputElm[optionName.replace("Value", "")] = `${newValue}`;
            break;
        }
      }
    }
    isValueChanged() {
      const elmValue = this._inputElm?.value ?? "";
      return !(elmValue === "" && this._originalValue === void 0) && +elmValue !== this._originalValue;
    }
    isValueTouched() {
      return this._isValueTouched;
    }
    loadValue(item) {
      const fieldName = this.columnDef?.field ?? "";
      if (item && fieldName !== void 0) {
        const isComplexObject = fieldName?.indexOf(".") > 0;
        let value = isComplexObject ? getDescendantProperty(item, fieldName) : item.hasOwnProperty(fieldName) ? item[fieldName] : this._defaultValue;
        if (value === "" || value === null || value === void 0) {
          value = this._defaultValue;
        }
        this._originalValue = +value;
        if (this._inputElm) {
          this._inputElm.value = `${value}`;
          this._inputElm.title = `${value}`;
        }
        if (this._sliderNumberElm) {
          this._sliderNumberElm.textContent = `${value}`;
        }
      }
      this.updateTrackFilledColorWhenEnabled();
    }
    /**
     * You can reset or clear the input value,
     * when no value is provided it will use the original value to reset (could be useful with Composite Editor Modal with edit/clone)
     */
    reset(value, triggerCompositeEventWhenExist = true, clearByDisableCommand = false) {
      const inputValue = value ?? this._originalValue ?? 0;
      if (this._inputElm) {
        this._inputElm.value = `${inputValue}`;
      }
      if (this._sliderNumberElm) {
        this._sliderNumberElm.textContent = `${inputValue}`;
      }
      this._isValueTouched = false;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions && triggerCompositeEventWhenExist) {
        const shouldDeleteFormValue = !clearByDisableCommand;
        this.handleChangeOnCompositeEditor(null, compositeEditorOptions, "user", shouldDeleteFormValue);
      }
    }
    save() {
      if (this.hasAutoCommitEdit && this.validate()?.valid) {
        this.grid.getEditorLock().commitCurrentEdit();
      } else {
        this.args.commitChanges();
      }
    }
    serializeValue() {
      const elmValue = this._inputElm?.value ?? "";
      return elmValue !== "" ? parseInt(elmValue, 10) : this._originalValue;
    }
    validate(_targetElm, options) {
      if (this.args.isCompositeEditor) {
        this.applyInputUsabilityState();
      }
      if (this.disabled) {
        return { valid: true, msg: "" };
      }
      const elmValue = options?.inputValue ?? this._inputElm?.value;
      return sliderValidator(elmValue, {
        editorArgs: this.args,
        errorMessage: this.columnEditor.errorMessage,
        minValue: this.columnEditor.minValue,
        maxValue: this.columnEditor.maxValue,
        required: this.args.isCompositeEditor ? false : this.columnEditor.required,
        validator: this.validator
      });
    }
    //
    // protected functions
    // ------------------
    /**
     * Create the HTML template as a string
     */
    buildDomElement() {
      const columnId = this.columnDef?.id ?? "";
      const title = this.columnEditor.title ?? "";
      this._sliderOptions = this.getSliderConfigs();
      const defaultValue = this.editorOptions.sliderStartValue ?? this._sliderOptions.minValue;
      this._defaultValue = +defaultValue;
      this._sliderTrackElm = createDomElement("div", { className: "slider-track" });
      this._inputElm = createDomElement("input", {
        type: "range",
        title,
        defaultValue: `${defaultValue}`,
        value: `${defaultValue}`,
        min: `${this._sliderOptions.minValue}`,
        max: `${this._sliderOptions.maxValue}`,
        step: `${this.columnEditor.valueStep ?? Constants.SLIDER_DEFAULT_STEP}`,
        ariaLabel: this.columnEditor.ariaLabel ?? `${toSentenceCase(`${columnId}`)} Slider Editor`,
        className: `slider-editor-input editor-${columnId}`
      });
      const divContainerElm = createDomElement("div", { className: "slider-container slider-editor" });
      const sliderInputContainerElm = createDomElement("div", { className: "slider-input-container slider-editor" });
      sliderInputContainerElm.appendChild(this._sliderTrackElm);
      sliderInputContainerElm.appendChild(this._inputElm);
      divContainerElm.appendChild(sliderInputContainerElm);
      this.renderSliderNumber(divContainerElm, defaultValue);
      return divContainerElm;
    }
    /** get default slider defaults */
    getSliderConfigs() {
      return {
        minValue: +(this.columnEditor.minValue ?? Constants.SLIDER_DEFAULT_MIN_VALUE),
        maxValue: +(this.columnEditor.maxValue ?? Constants.SLIDER_DEFAULT_MAX_VALUE),
        step: +(this.columnEditor.valueStep ?? Constants.SLIDER_DEFAULT_STEP)
      };
    }
    renderSliderNumber(divContainerElm, defaultValue) {
      if (!this.editorOptions.hideSliderNumber) {
        divContainerElm.classList.add("input-group");
        const divGroupAddonElm = createDomElement("div", { className: "input-group-addon input-group-append slider-value" });
        this._sliderNumberElm = createDomElement("span", { className: `input-group-text`, textContent: `${defaultValue}` });
        divGroupAddonElm.appendChild(this._sliderNumberElm);
        divContainerElm.appendChild(divGroupAddonElm);
      } else {
        divContainerElm.querySelector(".slider-value")?.remove();
      }
    }
    /** when it's a Composite Editor, we'll check if the Editor is editable (by checking onBeforeEditCell) and if not Editable we'll disable the Editor */
    applyInputUsabilityState() {
      const activeCell = this.grid.getActiveCell();
      const isCellEditable = this.grid.onBeforeEditCell.notify({
        ...activeCell,
        item: this.dataContext,
        column: this.args.column,
        grid: this.grid,
        target: "composite",
        compositeEditorOptions: this.args.compositeEditorOptions
      }).getReturnValue();
      this.disable(isCellEditable === false);
    }
    handleChangeEvent(event) {
      this._isValueTouched = true;
      const compositeEditorOptions = this.args.compositeEditorOptions;
      if (compositeEditorOptions) {
        this.handleChangeOnCompositeEditor(event, compositeEditorOptions);
      } else {
        this.save();
      }
    }
    handleChangeSliderNumber(event) {
      const value = event.target?.value ?? "";
      if (value !== "") {
        if (!this.editorOptions.hideSliderNumber && this._sliderNumberElm) {
          this._sliderNumberElm.textContent = value;
        }
        this._inputElm.title = value;
        if (!this.args?.compositeEditorOptions) {
          this.grid.onMouseEnter.notify({ column: this.columnDef, grid: this.grid }, new SlickEventData(event));
        }
      }
      this.updateTrackFilledColorWhenEnabled();
    }
    handleChangeOnCompositeEditor(event, compositeEditorOptions, triggeredBy = "user", isCalledByClearValue = false) {
      const activeCell = this.grid.getActiveCell();
      const column = this.args.column;
      const columnId = this.columnDef?.id ?? "";
      const item = this.dataContext;
      const grid = this.grid;
      const newValue = this.serializeValue();
      if (this.validate().valid) {
        this.applyValue(this.dataContext, newValue);
      }
      this.applyValue(compositeEditorOptions.formValues, newValue);
      const isExcludeDisabledFieldFormValues = this.gridOptions?.compositeEditorOptions?.excludeDisabledFieldFormValues ?? false;
      if (isCalledByClearValue || this.disabled && isExcludeDisabledFieldFormValues && compositeEditorOptions.formValues.hasOwnProperty(columnId)) {
        delete compositeEditorOptions.formValues[columnId];
      }
      grid.onCompositeEditorChange.notify({
        ...activeCell,
        item,
        grid,
        column,
        formValues: compositeEditorOptions.formValues,
        editors: compositeEditorOptions.editors,
        triggeredBy
      }, new SlickEventData(event));
    }
    /** use keydown event to increase/decrease slider value */
    handleKeyDown(e2) {
      if (this.editorOptions.useArrowToSlide !== false && (e2.key === "ArrowLeft" || e2.key === "ArrowRight")) {
        const { minValue, maxValue, step } = this.getSliderConfigs();
        const currentValue = Number(this._inputElm.value);
        let newValue = e2.key === "ArrowLeft" ? currentValue - step : currentValue + step;
        newValue = newValue < minValue ? minValue : newValue > maxValue ? maxValue : newValue;
        this._inputElm.value = `${newValue}`;
        this.handleChangeSliderNumber(e2);
        e2.stopPropagation();
        e2.preventDefault();
      }
    }
    sliderTrackClicked(e2) {
      e2.preventDefault();
      const sliderTrackX = e2.offsetX;
      const sliderTrackWidth = this._sliderTrackElm.offsetWidth;
      const trackPercentPosition = (sliderTrackX + 0) * 100 / sliderTrackWidth;
      if (this._inputElm) {
        this._inputElm.value = `${trackPercentPosition}`;
        this._inputElm.dispatchEvent(new Event("change"));
      }
    }
    updateTrackFilledColorWhenEnabled() {
      if (this.editorOptions.enableSliderTrackColoring && this._inputElm) {
        const percent1 = 0;
        const percent2 = (+this.getValue() - +this._inputElm.min) / (this.sliderOptions?.maxValue ?? 0 - +this._inputElm.min) * 100;
        const bg = "linear-gradient(to right, %b %p1, %c %p1, %c %p2, %b %p2)".replace(/%b/g, "#eee").replace(/%c/g, this.editorOptions?.sliderTrackFilledColor ?? "var(--slick-slider-filter-thumb-color, #86bff8)").replace(/%p1/g, `${percent1}%`).replace(/%p2/g, `${percent2}%`);
        this._sliderTrackElm.style.background = bg;
        this._sliderOptions.sliderTrackBackground = bg;
      }
    }
  };

  // node_modules/@slickgrid-universal/common/dist/editors/editors.index.js
  var Editors = {
    /** Autocompleter Editor (using https://github.com/kraaden/autocomplete) */
    autocompleter: AutocompleterEditor,
    /** Checkbox Editor (uses native checkbox DOM element) */
    checkbox: CheckboxEditor,
    /** Date Picker Editor (which uses 3rd party lib "vanilla-calendar-pro") */
    date: DateEditor,
    /** Dual Input Editor, default input type is text but it could be (integer/float/number/password/text) */
    dualInput: DualInputEditor,
    /** Float Number Editor using an input of type "number" */
    float: FloatEditor,
    /** Integer Number Editor using an input of type "number" */
    integer: IntegerEditor,
    /**
     * Long Text Editor (uses a textarea) for longer text (you can also optionally configure its size).
     * When ready to Save you can click on the "Save" and/or use shortcuts (Ctrl+ENTER or Ctrl+s).
     */
    longText: LongTextEditor,
    /** Multiple Select editor (which uses 3rd party lib "multiple-select-vanilla") */
    multipleSelect: MultipleSelectEditor,
    /** Editor with an input of type Password (note that only the text shown in the UI will be masked, the editor value is still plain text) */
    password: InputPasswordEditor,
    /** Single Select editor (which uses 3rd party lib "multiple-select-vanilla") */
    singleSelect: SingleSelectEditor,
    /** Slider Editor using an input of type "range" */
    slider: SliderEditor,
    /** text Editor using an input of type "text" (this is the default editor when no type is provided) */
    text: InputEditor
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/arrayObjectToCsvFormatter.js
  var arrayObjectToCsvFormatter = (_row, _cell, value, columnDef, dataContext) => {
    const params = columnDef?.params ?? {};
    const propertyNames = params.propertyNames;
    const isIncludingTitle = params.includeTitle ?? true;
    let parentObjectKeyName = params.dataContextProperty;
    if (!parentObjectKeyName) {
      parentObjectKeyName = columnDef?.field?.split(".")[0] || "";
    }
    if (!propertyNames || !Array.isArray(propertyNames) || !parentObjectKeyName) {
      throw new Error(`Formatters.arrayObjectToCsv requires you to pass an array of "propertyNames" (declared in "params") that you want to pull the data from.
      For example, if we have an array of user objects that have the property of firstName & lastName then we need to pass in your column definition:: { params: { propertyNames: ['firtName'] }}.
      Optionally, you can also pass the "dataContextProperty" if you wish to run this on another completely different field of the dataContext object.`);
    }
    if (Array.isArray(dataContext[parentObjectKeyName]) && dataContext[parentObjectKeyName].length > 0) {
      const outputStrings = [];
      dataContext[parentObjectKeyName].forEach((data) => {
        const strings = [];
        propertyNames.forEach((prop) => {
          strings.push(data[prop]);
        });
        outputStrings.push(strings.join(" "));
      });
      const output = outputStrings.join(", ");
      const elm = createDomElement("span", { textContent: output });
      if (isIncludingTitle) {
        elm.title = output;
      }
      return elm;
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/arrayToCsvFormatter.js
  var arrayToCsvFormatter = (_row, _cell, value) => {
    if (Array.isArray(value) && value.length > 0) {
      const values = value.join(", ");
      return createDomElement("span", { title: values, textContent: values });
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/checkmarkMaterialFormatter.js
  var checkmarkMaterialFormatter = (_row, _cell, value) => {
    let isChecked = false;
    const isValidNumber = isNumber(value);
    if (isValidNumber) {
      value = +value;
    }
    if (value === true || isValidNumber && +value > 0 || typeof value === "string" && value.length > 0 && value.toLowerCase() !== "false" && value !== "0") {
      isChecked = true;
    }
    return isChecked ? createDomElement("i", { className: "mdi font-18px mdi-check checkmark-icon", ariaHidden: "true" }) : "";
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/collectionEditorFormatter.js
  var collectionEditorFormatter = (row, cell, value, columnDef, dataContext, grid) => {
    if (!value || !columnDef?.editor?.collection?.length) {
      return value;
    }
    const { editor, editor: { collection } } = columnDef;
    const labelName = editor.customStructure?.label ?? "label";
    const valueName = editor.customStructure?.value ?? "value";
    if (Array.isArray(value)) {
      if (collection.every((x2) => typeof x2 === "string")) {
        return arrayToCsvFormatter(row, cell, value.map((v2) => findOrDefault(collection, (c) => c === v2)), columnDef, dataContext, grid);
      }
      return arrayToCsvFormatter(row, cell, value.map((v2) => findOrDefault(collection, (c) => c[valueName] === v2)[labelName]), columnDef, dataContext, grid);
    }
    return findOrDefault(collection, (c) => c[valueName] === value)[labelName] || "";
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/collectionFormatter.js
  var collectionFormatter = (row, cell, value, columnDef, dataContext, grid) => {
    if (!value || !columnDef?.params?.collection?.length) {
      return value;
    }
    const { params, params: { collection } } = columnDef;
    const labelName = params.customStructure?.label ?? "label";
    const valueName = params.customStructure?.value ?? "value";
    if (Array.isArray(value)) {
      return arrayToCsvFormatter(row, cell, value.map((v2) => findOrDefault(collection, (c) => c[valueName] === v2)[labelName]), columnDef, dataContext, grid);
    }
    return findOrDefault(collection, (c) => c[valueName] === value)[labelName] || "";
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/complexObjectFormatter.js
  var complexObjectFormatter = (_row, _cell, cellValue, columnDef, dataContext) => {
    const columnParams = columnDef.params || {};
    const complexField = columnParams.complexField ?? columnParams.complexFieldLabel ?? columnDef.field;
    if (!complexField) {
      throw new Error(`For the Formatters.complexObject to work properly, you need to tell it which property of the complex object to use.
      There are 3 ways to provide it:
      1- via the generic "params" with a "complexField" property on your Column Definition, example: this.columnDefs = [{ id: 'user', field: 'user', params: { complexField: 'user.firstName' } }]
      2- via the generic "params" with a "complexField" and a "labelKey" property on your Column Definition, example: this.columnDefs = [{ id: 'user', field: 'user', labelKey: 'firstName', params: { complexField: 'user' } }]
      3- via the field name that includes a dot notation, example: this.columnDefs = [{ id: 'user', field: 'user.firstName'}] `);
    }
    if (columnDef.labelKey && dataContext.hasOwnProperty(complexField)) {
      return dataContext[complexField]?.[columnDef.labelKey];
    }
    if (typeof complexField === "string" && complexField.indexOf(".") > 0) {
      let outputValue = complexField.split(".").reduce((obj, i) => obj?.hasOwnProperty(i) ? obj[i] : "", dataContext);
      if (outputValue === void 0 || outputValue === null || typeof outputValue === "object" && Object.entries(outputValue).length === 0 && !(outputValue instanceof Date)) {
        outputValue = "";
      }
      return outputValue;
    }
    return cellValue;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/decimalFormatter.js
  var decimalFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    const { minDecimal, maxDecimal, numberPrefix, numberSuffix, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "decimal", "cell");
    if (isNumber(value)) {
      return formatNumber(value, minDecimal, maxDecimal, wrapNegativeNumber, numberPrefix, numberSuffix, decimalSeparator, thousandSeparator);
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/dollarFormatter.js
  var dollarFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    if (isNumber(value)) {
      const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "currency", "cell");
      return formatNumber(value, minDecimal, maxDecimal, wrapNegativeNumber, "$", "", decimalSeparator, thousandSeparator);
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/dollarColoredFormatter.js
  var dollarColoredFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    if (isNumber(value)) {
      const textContent = dollarFormatter(_row, _cell, value, columnDef, _dataContext, grid);
      return createDomElement("span", {
        ariaHidden: "true",
        style: {
          color: value >= 0 ? "green" : "red"
        },
        textContent
      });
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/dollarColoredBoldFormatter.js
  var dollarColoredBoldFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    const spanOrValue = dollarColoredFormatter(_row, _cell, value, columnDef, _dataContext, grid);
    if (spanOrValue instanceof HTMLElement) {
      spanOrValue.style.fontWeight = "bold";
    }
    return spanOrValue;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/currencyFormatter.js
  var currencyFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    const { currencyPrefix, currencySuffix, minDecimal, maxDecimal, numberPrefix, numberSuffix, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "decimal", "cell");
    if (isNumber(value)) {
      const formattedNumber = formatNumber(value, minDecimal, maxDecimal, wrapNegativeNumber, currencyPrefix, currencySuffix, decimalSeparator, thousandSeparator);
      return `${numberPrefix}${formattedNumber}${numberSuffix}`;
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/htmlDecodeFormatter.js
  var htmlDecodeFormatter = (_row, _cell, value, _col, _item, grid) => {
    const decodedVal = htmlDecode(value);
    if (decodedVal) {
      const containerElm = createDocumentFragmentOrElement(grid.getOptions());
      containerElm.textContent = decodedVal;
      return { html: containerElm };
    }
    return decodedVal;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/hyperlinkFormatter.js
  var hyperlinkFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    const gridOptions = grid.getOptions() ?? {};
    const columnParams = columnDef?.params ?? {};
    const displayedText = runOptionalHtmlSanitizer(columnParams.hyperlinkText || value, gridOptions.sanitizer);
    const outputLink = runOptionalHtmlSanitizer(columnParams.hyperlinkUrl || value, gridOptions.sanitizer);
    const matchUrl = outputLink.match(/^(http|ftp|https):\/\/[\w\-_]+(\.[\w\-_]+)+([\w\-.,@?^=%&amp;:/~+#]*[\w\-@?^=%&amp;/~+#])?/i);
    if (Array.isArray(matchUrl) && matchUrl.length > 0) {
      return createDomElement("a", { href: matchUrl[0], textContent: displayedText });
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/iconBooleanFormatter.js
  var iconBooleanFormatter = (_row, _cell, value, columnDef) => {
    const columnParams = columnDef?.params || {};
    const cssClasses = columnParams.cssClass;
    if (!cssClasses) {
      throw new Error('[Slickgrid-Universal] When using `Formatters.iconBoolean`, you must provide You must provide the "cssClass", e.g.: { formatter: Formatters.iconBoolean, params: { cssClass: "mdi mdi-check" }}');
    }
    let isTruthy = false;
    const isNumber2 = !isNaN(value);
    if (value === true || isNumber2 && parseFloat(value) > 0 || !isNumber2 && typeof value === "string" && value.toLowerCase() !== "false" && value !== "0") {
      isTruthy = true;
    }
    return isTruthy ? createDomElement("i", { className: cssClasses, ariaHidden: "true" }) : "";
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/iconFormatter.js
  var iconFormatter = (_row, _cell, _value, columnDef) => {
    const columnParams = columnDef?.params ?? {};
    const cssClasses = columnParams.iconCssClass || columnParams.icon || columnParams.formatterIcon;
    if (!cssClasses) {
      throw new Error('[Slickgrid-Universal] When using `Formatters.icon`, you must provide the "iconCssClass" via the generic "params". (e.g.: `{ formatter: Formatters.icon, params: { iconCssClass: "mdi mdi-magnify" }}`');
    }
    const title = columnParams.title || null;
    return createDomElement("i", { className: cssClasses, ariaHidden: "true", title });
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/maskFormatter.js
  var maskFormatter = (_row, _cell, value, columnDef, data, grid) => {
    const params = columnDef.params || {};
    const mask = params.mask;
    if (!mask) {
      throw new Error(`You must provide a "mask" via the generic "params" options (e.g.: { formatter: Formatters.mask, params: { mask: '000-000' }}`);
    }
    if (value) {
      let i = 0;
      const v2 = value.toString();
      const containerElm = createDocumentFragmentOrElement(grid.getOptions());
      containerElm.textContent = mask.replace(/[09A]/gi, () => v2[i++] || "");
      return containerElm;
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/percentCompleteBarFormatter.js
  var percentCompleteBarFormatter = (_row, _cell, value) => {
    if (!isNumber(value)) {
      return "";
    }
    let color = "";
    let percent = parseFloat(value);
    if (percent > 100) {
      percent = 100;
    }
    color = percent < 30 ? "red" : percent < 70 ? "silver" : "green";
    return createDomElement("span", {
      className: "percent-complete-bar",
      title: `${percent}%`,
      style: { background: color, width: `${percent}%` }
    });
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/percentCompleteBarWithTextFormatter.js
  var percentCompleteBarWithTextFormatter = (_row, _cell, value) => {
    if (!isNumber(value)) {
      return "";
    }
    let color = "";
    let percent = parseFloat(value);
    if (percent > 100) {
      percent = 100;
    }
    color = percent < 30 ? "red" : percent < 70 ? "silver" : "green";
    return createDomElement("div", {
      className: "percent-complete-bar-with-text",
      title: `${percent}%`,
      textContent: `${percent}%`,
      style: { background: color, width: `${percent}%` }
    });
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/percentCompleteFormatter.js
  var percentCompleteFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "percent", "cell");
    if (isNumber(value)) {
      const color = value < 50 ? "red" : "green";
      return createDomElement("span", {
        style: { color },
        textContent: value > 100 ? "100%" : formatNumber(value, minDecimal, maxDecimal, wrapNegativeNumber, "", "%", decimalSeparator, thousandSeparator)
      });
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/percentFormatter.js
  var percentFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "percent", "cell");
    if (isNumber(value)) {
      const percentValue = value * 100;
      return formatNumber(percentValue, minDecimal, maxDecimal, wrapNegativeNumber, "", "%", decimalSeparator, thousandSeparator);
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/percentSymbolFormatter.js
  var percentSymbolFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "percent", "cell");
    if (isNumber(value)) {
      return formatNumber(value, minDecimal, maxDecimal, wrapNegativeNumber, "", "%", decimalSeparator, thousandSeparator);
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/progressBarFormatter.js
  var progressBarFormatter = (_row, _cell, value) => {
    if (!isNumber(value)) {
      return "";
    }
    let inputNumber = parseFloat(value);
    if (inputNumber > 100) {
      inputNumber = 100;
    }
    const color = inputNumber < 30 ? "danger" : inputNumber < 70 ? "warning" : "success";
    const container = createDomElement("div", { className: "progress" });
    container.appendChild(createDomElement("div", {
      className: `progress-bar progress-bar-${color} bg-${color}`,
      role: "progressbar",
      ariaValueNow: String(inputNumber),
      ariaValueMin: "0",
      ariaValueMax: "100",
      textContent: `${inputNumber}%`,
      style: { minWidth: "2em", width: `${inputNumber}%` }
    }));
    return container;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/translateBooleanFormatter.js
  var translateBooleanFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    const gridOptions = grid?.getOptions() ?? {};
    const translater = gridOptions.translater || columnDef?.params?.translater;
    if (!translater || typeof translater.translate !== "function") {
      throw new Error(`"Formatters.translateBoolean" requires the Translate Service to be provided as a Grid Options "translater" (or "i18n" depending on which framework you use).
    For example: this.gridOptions = { enableTranslate: true, translater: this.translateService }`);
    }
    if (value !== void 0 && value !== null && typeof value !== "string") {
      value = value + "";
    }
    return value ? translater.translate(value.toUpperCase()) : "";
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/translateFormatter.js
  var translateFormatter = (_row, _cell, value, columnDef, _dataContext, grid) => {
    const gridOptions = grid?.getOptions() ?? {};
    const translater = gridOptions.translater || columnDef?.params?.translater;
    if (!translater || typeof translater.translate !== "function") {
      throw new Error(`"Formatters.translate" requires the Translate Service to be provided as a Grid Options "translater" (or "i18n" depending on which framework you use).
    For example: this.gridOptions = { enableTranslate: true, translater: this.translateService }`);
    }
    if (value !== void 0 && value !== null && typeof value !== "string") {
      value = value + "";
    }
    return value ? translater.translate(value) : "";
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/treeExportFormatter.js
  var treeExportFormatter = (row, cell, value, columnDef, dataContext, grid) => {
    const gridOptions = grid.getOptions();
    const treeDataOptions = gridOptions?.treeDataOptions;
    const collapsedPropName = treeDataOptions?.collapsedPropName ?? Constants.treeDataProperties.COLLAPSED_PROP;
    const hasChildrenPropName = treeDataOptions?.hasChildrenPropName ?? Constants.treeDataProperties.HAS_CHILDREN_PROP;
    const treeLevelPropName = treeDataOptions?.levelPropName ?? Constants.treeDataProperties.TREE_LEVEL_PROP;
    const indentMarginLeft = treeDataOptions?.exportIndentMarginLeft ?? 5;
    const exportIndentationLeadingChar = treeDataOptions?.exportIndentationLeadingChar ?? ".";
    const exportIndentationLeadingSpaceCount = treeDataOptions?.exportIndentationLeadingSpaceCount ?? 3;
    const groupCollapsedSymbol = gridOptions?.excelExportOptions?.groupCollapsedSymbol ?? "\u2B9E";
    const groupExpandedSymbol = gridOptions?.excelExportOptions?.groupExpandedSymbol ?? "\u2B9F";
    let outputValue = value;
    outputValue = getCellValueFromQueryFieldGetter(columnDef, dataContext, value);
    if (outputValue === null || outputValue === void 0 || dataContext === void 0) {
      return "";
    }
    if (!dataContext.hasOwnProperty(treeLevelPropName)) {
      throw new Error('[Slickgrid-Universal] You must provide valid "treeDataOptions" in your Grid Options, however it seems that we could not find any tree level info on the current item datacontext row.');
    }
    const treeLevel = dataContext[treeLevelPropName] ?? 0;
    let toggleSymbol = "";
    let indentation = 0;
    if (dataContext[hasChildrenPropName]) {
      toggleSymbol = dataContext[collapsedPropName] ? groupCollapsedSymbol : groupExpandedSymbol;
      indentation = treeLevel === 0 ? 0 : indentMarginLeft * treeLevel;
    } else {
      indentation = indentMarginLeft * (treeLevel === 0 ? 0 : treeLevel + 1);
    }
    const indentSpacer = addWhiteSpaces(indentation);
    if (treeDataOptions?.titleFormatter) {
      outputValue = parseFormatterWhenExist(treeDataOptions.titleFormatter, row, cell, columnDef, dataContext, grid);
    }
    const leadingChar = treeLevel === 0 && toggleSymbol ? "" : treeLevel === 0 ? `${exportIndentationLeadingChar}${addWhiteSpaces(exportIndentationLeadingSpaceCount)}` : exportIndentationLeadingChar;
    outputValue = `${leadingChar}${indentSpacer}${toggleSymbol} ${outputValue}`;
    const sanitizedOutputValue = stripTags(outputValue);
    return sanitizedOutputValue;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/treeFormatter.js
  var treeFormatter = (row, cell, value, columnDef, dataContext, grid) => {
    const gridOptions = grid.getOptions();
    const treeDataOptions = gridOptions.treeDataOptions ?? {};
    const indentMarginLeft = treeDataOptions.indentMarginLeft ?? 15;
    const collapsedPropName = treeDataOptions.collapsedPropName ?? Constants.treeDataProperties.COLLAPSED_PROP;
    const hasChildrenPropName = treeDataOptions.hasChildrenPropName ?? Constants.treeDataProperties.HAS_CHILDREN_PROP;
    const treeLevelPropName = treeDataOptions.levelPropName ?? Constants.treeDataProperties.TREE_LEVEL_PROP;
    const lazyLoadingPropName = treeDataOptions.lazyLoadingPropName ?? Constants.treeDataProperties.LAZY_LOADING_PROP;
    let outputValue = getCellValueFromQueryFieldGetter(columnDef, dataContext, value);
    if (outputValue === null || outputValue === void 0 || dataContext === void 0) {
      return "";
    }
    if (!dataContext.hasOwnProperty(treeLevelPropName)) {
      throw new Error('[Slickgrid-Universal] You must provide valid "treeDataOptions" in your Grid Options, however it seems that we could not find any tree level info on the current item datacontext row.');
    }
    const lazyLoading = dataContext[lazyLoadingPropName];
    const treeLevel = dataContext[treeLevelPropName] ?? 0;
    const slickTreeLevelClass = `slick-tree-level-${treeLevel}`;
    let toggleClass2 = "";
    if (dataContext[hasChildrenPropName]) {
      toggleClass2 = lazyLoading && lazyLoading !== "done" ? lazyLoading : dataContext[collapsedPropName] ? "collapsed" : "expanded";
    }
    const indentSpacerElm = createDomElement("span", { style: { display: "inline-block", width: `${indentMarginLeft * treeLevel}px` } });
    const spanToggleClass = `slick-group-toggle slick-tree-toggle ${toggleClass2}`.trim();
    const spanIconElm = createDomElement("div", { className: spanToggleClass, ariaExpanded: String(toggleClass2 === "expanded") });
    const containerElm = createDocumentFragmentOrElement(gridOptions);
    containerElm.appendChild(indentSpacerElm);
    containerElm.appendChild(spanIconElm);
    if (lazyLoading === "load-fail") {
      containerElm.appendChild(createDomElement("span", { className: "slick-tree-load-fail" }));
    }
    if (treeDataOptions.titleFormatter) {
      outputValue = parseFormatterWhenExist(treeDataOptions.titleFormatter, row, cell, columnDef, dataContext, grid);
    }
    const treeTitleElm = createDomElement("span", { className: "slick-tree-title" });
    if (gridOptions.treeDataOptions?.toggleOnNodeTitle) {
      treeTitleElm.classList.add("pointer");
    }
    applyHtmlToElement(treeTitleElm, outputValue, gridOptions);
    treeTitleElm.setAttribute("level", treeLevel);
    containerElm.appendChild(treeTitleElm);
    return { addClasses: slickTreeLevelClass, html: containerElm };
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/treeParseTotalsFormatter.js
  var treeParseTotalsFormatter = (row, cell, value, columnDef, dataContext, grid) => {
    const gridOptions = grid.getOptions();
    const hasChildrenPropName = gridOptions.treeDataOptions?.hasChildrenPropName ?? Constants.treeDataProperties.HAS_CHILDREN_PROP;
    const { groupTotalsFormatter, treeTotalsFormatter, params } = columnDef;
    if (!groupTotalsFormatter && !treeTotalsFormatter) {
      throw new Error('[Slickgrid-Universal] When using Formatters.treeParseTotals, you must provide a total formatter via "groupTotalsFormatter" or "treeTotalsFormatter".');
    }
    if (dataContext[hasChildrenPropName] && dataContext?.__treeTotals && (groupTotalsFormatter || treeTotalsFormatter)) {
      const totalFormatter = treeTotalsFormatter ?? groupTotalsFormatter;
      return totalFormatter(dataContext?.__treeTotals, columnDef, grid);
    } else if (params.formatters) {
      let currentValue = value;
      for (const formatter of params.formatters) {
        if (!dataContext[hasChildrenPropName] && !dataContext?.__treeTotals && typeof formatter === "function") {
          currentValue = formatter(row, cell, currentValue, columnDef, dataContext, grid) || value;
        }
      }
      return currentValue;
    }
    return value;
  };

  // node_modules/@slickgrid-universal/common/dist/formatters/formatters.index.js
  var allFormatters = {
    arrayObjectToCsv: arrayObjectToCsvFormatter,
    arrayToCsv: arrayToCsvFormatter,
    checkmarkMaterial: checkmarkMaterialFormatter,
    complex: complexObjectFormatter,
    complexObject: complexObjectFormatter,
    collection: collectionFormatter,
    collectionEditor: collectionEditorFormatter,
    currency: currencyFormatter,
    date: getBaseDateFormatter(),
    // --
    // add all the date Formatters dynamically below
    decimal: decimalFormatter,
    dollar: dollarFormatter,
    dollarColored: dollarColoredFormatter,
    dollarColoredBold: dollarColoredBoldFormatter,
    hyperlink: hyperlinkFormatter,
    icon: iconFormatter,
    htmlDecode: htmlDecodeFormatter,
    iconBoolean: iconBooleanFormatter,
    mask: maskFormatter,
    multiple: multipleFormatter,
    percent: percentFormatter,
    percentComplete: percentCompleteFormatter,
    percentCompleteBar: percentCompleteBarFormatter,
    percentCompleteBarWithText: percentCompleteBarWithTextFormatter,
    percentSymbol: percentSymbolFormatter,
    progressBar: progressBarFormatter,
    translate: translateFormatter,
    translateBoolean: translateBooleanFormatter,
    tree: treeFormatter,
    treeParseTotals: treeParseTotalsFormatter,
    treeExport: treeExportFormatter
  };
  getAllDateFieldTypes().forEach((dateType) => {
    if (dateType !== "date") {
      allFormatters[dateType] = getAssociatedDateFormatter(dateType, dateType);
    }
  });
  var Formatters = allFormatters;

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/sumTotalsFormatter.js
  var sumTotalsFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.sum?.[field];
    const params = columnDef?.params ?? {};
    const prefix = params.groupFormatterPrefix || "";
    const suffix = params.groupFormatterSuffix || "";
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "regular", "group");
    if (isNumber(val)) {
      const formattedNumber = formatNumber(val, minDecimal, maxDecimal, wrapNegativeNumber, "", "", decimalSeparator, thousandSeparator);
      return `${prefix}${formattedNumber}${suffix}`;
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/sumTotalsDollarFormatter.js
  var sumTotalsDollarFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.sum?.[field];
    const params = columnDef?.params ?? {};
    const prefix = params.groupFormatterPrefix || "";
    const suffix = params.groupFormatterSuffix || "";
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "currency", "group");
    if (isNumber(val)) {
      const formattedNumber = formatNumber(val, minDecimal, maxDecimal, wrapNegativeNumber, "$", "", decimalSeparator, thousandSeparator);
      return `${prefix}${formattedNumber}${suffix}`;
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/sumTotalsDollarColoredFormatter.js
  var sumTotalsDollarColoredFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.sum?.[field];
    if (isNumber(val)) {
      const color = val >= 0 ? "green" : "red";
      const textContent = sumTotalsDollarFormatter(totals, columnDef, grid);
      return createDomElement("span", {
        style: { color },
        textContent
      });
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/sumTotalsDollarColoredBoldFormatter.js
  var sumTotalsDollarColoredBoldFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.sum?.[field];
    if (isNumber(val)) {
      const color = val >= 0 ? "green" : "red";
      const textContent = sumTotalsDollarFormatter(totals, columnDef, grid);
      return createDomElement("span", {
        style: {
          color,
          fontWeight: "bold"
        },
        textContent
      });
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/sumTotalsDollarBoldFormatter.js
  var sumTotalsDollarBoldFormatter = (totals, columnDef, grid) => {
    const textContent = sumTotalsDollarFormatter(totals, columnDef, grid);
    if (textContent) {
      return createDomElement("span", {
        style: { fontWeight: "bold" },
        textContent
      });
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/sumTotalsColoredFormatter.js
  var sumTotalsColoredFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.sum?.[field];
    if (isNumber(val)) {
      const color = val >= 0 ? "green" : "red";
      const textContent = sumTotalsFormatter(totals, columnDef, grid);
      return createDomElement("span", {
        style: { color },
        textContent
      });
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/sumTotalsBoldFormatter.js
  var sumTotalsBoldFormatter = (totals, columnDef, grid) => {
    const textContent = sumTotalsFormatter(totals, columnDef, grid);
    if (textContent) {
      return createDomElement("span", { style: { fontWeight: "bold" }, textContent });
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/minTotalsFormatter.js
  var minTotalsFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.min?.[field];
    const params = columnDef?.params ?? {};
    const prefix = params.groupFormatterPrefix || "";
    const suffix = params.groupFormatterSuffix || "";
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "regular", "group");
    if (isNumber(val)) {
      const formattedNumber = formatNumber(val, minDecimal, maxDecimal, wrapNegativeNumber, "", "", decimalSeparator, thousandSeparator);
      return `${prefix}${formattedNumber}${suffix}`;
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/maxTotalsFormatter.js
  var maxTotalsFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.max?.[field];
    const params = columnDef?.params ?? {};
    const prefix = params.groupFormatterPrefix || "";
    const suffix = params.groupFormatterSuffix || "";
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "regular", "group");
    if (isNumber(val)) {
      const formattedNumber = formatNumber(val, minDecimal, maxDecimal, wrapNegativeNumber, "", "", decimalSeparator, thousandSeparator);
      return `${prefix}${formattedNumber}${suffix}`;
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/avgTotalsCurrencyFormatter.js
  var avgTotalsCurrencyFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.avg?.[field];
    const params = columnDef?.params ?? {};
    const prefix = params.groupFormatterPrefix || "";
    const suffix = params.groupFormatterSuffix || "";
    const currencyPrefix = params.groupFormatterCurrencyPrefix || "";
    const currencySuffix = params.groupFormatterCurrencySuffix || "";
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "currency", "group");
    if (isNumber(val)) {
      const formattedNumber = formatNumber(val, minDecimal, maxDecimal, wrapNegativeNumber, currencyPrefix, currencySuffix, decimalSeparator, thousandSeparator);
      return `${prefix}${formattedNumber}${suffix}`;
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/avgTotalsDollarFormatter.js
  var avgTotalsDollarFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.avg?.[field];
    const params = columnDef?.params ?? {};
    const prefix = params.groupFormatterPrefix || "";
    const suffix = params.groupFormatterSuffix || "";
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "currency", "group");
    if (isNumber(val)) {
      const formattedNumber = formatNumber(val, minDecimal, maxDecimal, wrapNegativeNumber, "$", "", decimalSeparator, thousandSeparator);
      return `${prefix}${formattedNumber}${suffix}`;
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/avgTotalsFormatter.js
  var avgTotalsFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    let val = totals.avg?.[field];
    const params = columnDef?.params ?? {};
    let prefix = params.groupFormatterPrefix || "";
    const suffix = params.groupFormatterSuffix || "";
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "regular", "group");
    if (isNumber(val)) {
      if (val < 0) {
        val = Math.abs(val);
        if (!wrapNegativeNumber) {
          prefix += "-";
        } else {
          if (isNaN(minDecimal) && isNaN(maxDecimal)) {
            const outputVal = thousandSeparatorFormatted(Math.round(val), thousandSeparator);
            return `${prefix}(${outputVal})${suffix}`;
          }
          return `${prefix}(${decimalFormatted(val, minDecimal, maxDecimal, decimalSeparator, thousandSeparator)})${suffix}`;
        }
      }
      if (isNaN(minDecimal) && isNaN(maxDecimal)) {
        const outputVal = thousandSeparatorFormatted(Math.round(val), thousandSeparator);
        return `${prefix}${outputVal}${suffix}`;
      }
      return `${prefix}${decimalFormatted(val, minDecimal, maxDecimal, decimalSeparator, thousandSeparator)}${suffix}`;
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/avgTotalsPercentageFormatter.js
  var avgTotalsPercentageFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    let val = totals.avg?.[field];
    const params = columnDef?.params ?? {};
    let prefix = params.groupFormatterPrefix || "";
    const suffix = params.groupFormatterSuffix || "";
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "percent", "group");
    if (isNumber(val)) {
      if (val < 0) {
        val = Math.abs(val);
        if (!wrapNegativeNumber) {
          prefix += "-";
        } else {
          if (isNaN(minDecimal) && isNaN(maxDecimal)) {
            const outputVal = thousandSeparatorFormatted(Math.round(val), thousandSeparator);
            return `${prefix}(${outputVal}%)${suffix}`;
          }
          return `${prefix}(${decimalFormatted(val, minDecimal, maxDecimal, decimalSeparator, thousandSeparator)}%)${suffix}`;
        }
      }
      if (isNaN(minDecimal) && isNaN(maxDecimal)) {
        const outputVal = thousandSeparatorFormatted(Math.round(val), thousandSeparator);
        return `${prefix}${outputVal}%${suffix}`;
      }
      return `${prefix}${decimalFormatted(val, minDecimal, maxDecimal, decimalSeparator, thousandSeparator)}%${suffix}`;
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/sumTotalsCurrencyFormatter.js
  var sumTotalsCurrencyFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.sum?.[field];
    const params = columnDef?.params ?? {};
    const prefix = params.groupFormatterPrefix || "";
    const suffix = params.groupFormatterSuffix || "";
    const currencyPrefix = params.groupFormatterCurrencyPrefix || "";
    const currencySuffix = params.groupFormatterCurrencySuffix || "";
    const { minDecimal, maxDecimal, decimalSeparator, thousandSeparator, wrapNegativeNumber } = retrieveFormatterOptions(columnDef, grid, "currency", "group");
    if (isNumber(val)) {
      const formattedNumber = formatNumber(val, minDecimal, maxDecimal, wrapNegativeNumber, currencyPrefix, currencySuffix, decimalSeparator, thousandSeparator);
      return `${prefix}${formattedNumber}${suffix}`;
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/sumTotalsCurrencyColoredFormatter.js
  var sumTotalsCurrencyColoredFormatter = (totals, columnDef, grid) => {
    const field = columnDef.field ?? "";
    const val = totals.sum?.[field];
    if (isNumber(val)) {
      const color = val >= 0 ? "green" : "red";
      const textContent = sumTotalsCurrencyFormatter(totals, columnDef, grid);
      return createDomElement("span", {
        style: { color },
        textContent
      });
    }
    return "";
  };

  // node_modules/@slickgrid-universal/common/dist/grouping-formatters/groupingFormatters.index.js
  var GroupTotalFormatters = {
    /**
     * Average all the column totals
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g.: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    avgTotals: avgTotalsFormatter,
    /**
     * Average all the column totals and display currency prefix/suffix via "groupFormatterCurrencyPrefix" and/or "groupFormatterCurrencySuffix"
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g.: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    avgTotalsCurrency: avgTotalsCurrencyFormatter,
    /**
     * Average all the column totals and display '$' at the end of the value
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g.: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    avgTotalsDollar: avgTotalsDollarFormatter,
    /**
     * Average all the column totals and display '%' at the end of the value
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g.: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    avgTotalsPercentage: avgTotalsPercentageFormatter,
    /**
     * Show max value of all the column totals
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g.: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    maxTotals: maxTotalsFormatter,
    /**
     * Show min value of all the column totals
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g.: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    minTotals: minTotalsFormatter,
    /**
     * Sums up all the column totals
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g.: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    sumTotals: sumTotalsFormatter,
    /**
     * Sums up all the column totals and display it in bold font weight
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    sumTotalsBold: sumTotalsBoldFormatter,
    /**
     * Sums up all the column totals, change color of text to red/green on negative/positive value
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    sumTotalsColored: sumTotalsColoredFormatter,
    /**
     * Sums up all the column totals and display currency
     * Extra options available in "params":: "groupFormatterPrefix", "groupFormatterSuffix", "groupFormatterCurrencyPrefix" and/or "groupFormatterCurrencySuffix"
     * e.g: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    sumTotalsCurrency: sumTotalsCurrencyFormatter,
    /**
     * Sums up all the column totals and display currency with color of red/green text on negative/positive values
     * Extra options available in "params":: "groupFormatterPrefix", "groupFormatterSuffix", "groupFormatterCurrencyPrefix" and/or "groupFormatterCurrencySuffix"
     * e.g: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    sumTotalsCurrencyColored: sumTotalsCurrencyColoredFormatter,
    /**
     * Sums up all the column totals and display dollar sign
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    sumTotalsDollar: sumTotalsDollarFormatter,
    /**
     * Sums up all the column totals and display dollar sign and show it in bold font weight
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    sumTotalsDollarBold: sumTotalsDollarBoldFormatter,
    /**
     * Sums up all the column totals, change color of text to red/green on negative/positive value
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    sumTotalsDollarColored: sumTotalsDollarColoredFormatter,
    /**
     * Sums up all the column totals, change color of text to red/green on negative/positive value, show it in bold font weight as well
     * Extra options available in "params":: "groupFormatterPrefix" and "groupFormatterSuffix", e.g: params: { groupFormatterPrefix: '<i>Total</i>: ', groupFormatterSuffix: '$' }
     */
    sumTotalsDollarColoredBold: sumTotalsDollarColoredBoldFormatter
  };

  // node_modules/@slickgrid-universal/common/dist/slickgrid-config.js
  var SlickgridConfig = class {
    constructor() {
      this.options = GlobalGridOptions;
    }
  };

  // node_modules/@slickgrid-universal/common/dist/index.js
  var Utilities = {
    ...backendUtility_service_exports,
    ...observers_exports,
    ...utilities_exports,
    ...sortUtilities_exports,
    ...dist_exports,
    deepAssign: deepMerge
  };

  // node_modules/@slickgrid-universal/empty-warning-component/dist/slick-empty-warning.component.js
  var SlickEmptyWarningComponent = class {
    constructor() {
      this.pluginName = "EmptyWarningComponent";
      this._isPreviouslyShown = false;
      this._warningLeftElement = null;
      this._warningRightElement = null;
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    init(grid, containerService) {
      this._grid = grid;
      this._translaterService = containerService.get("TranslaterService");
    }
    dispose() {
      emptyElement(this._warningLeftElement);
      emptyElement(this._warningRightElement);
      this._warningLeftElement?.remove();
      this._warningRightElement?.remove();
    }
    /**
     * Display a warning of empty data when the filtered dataset is empty
     * NOTE: to make this code reusable, you could (should) move this code into a utility service
     * @param isShowing - are we showing the message?
     * @param options - any styling options you'd like to pass like the text color
     */
    showEmptyDataMessage(isShowing = true, options) {
      if (!this._grid || !this.gridOptions || this._isPreviouslyShown === isShowing) {
        return false;
      }
      this._isPreviouslyShown = isShowing;
      const gridUid = this._grid.getUID();
      const defaultMessage = "No data to display.";
      const mergedOptions = { message: defaultMessage, ...this.gridOptions.emptyDataWarning, ...options };
      const emptyDataClassName = mergedOptions?.className ?? "slick-empty-data-warning";
      this._warningLeftElement = document.querySelector(`.${gridUid} .${emptyDataClassName}`);
      const gridCanvasLeftElm = document.querySelector(`.${gridUid} .grid-canvas.grid-canvas-left`);
      const gridCanvasRightElm = document.querySelector(`.${gridUid} .grid-canvas.grid-canvas-right`);
      const leftElementMarginLeft = mergedOptions.leftViewportMarginLeft ?? 0;
      const rightElementMarginLeft = mergedOptions.rightViewportMarginLeft ?? 0;
      const leftElementFrozenMarginLeft = mergedOptions.frozenLeftViewportMarginLeft ?? 0;
      const rightElementFrozenMarginLeft = mergedOptions.frozenRightViewportMarginLeft ?? 0;
      const isFrozenGrid = this.gridOptions?.frozenColumn !== void 0 && this.gridOptions.frozenColumn >= 0;
      const leftViewportMarginLeft = typeof leftElementMarginLeft === "string" ? leftElementMarginLeft : `${leftElementMarginLeft}px`;
      const rightViewportMarginLeft = typeof rightElementMarginLeft === "string" ? rightElementMarginLeft : `${rightElementMarginLeft}px`;
      if (this.gridOptions.autoHeight) {
        const leftPaneElm = document.querySelector(`.${gridUid} .slick-pane.slick-pane-top.slick-pane-left`);
        if (leftPaneElm && leftPaneElm.style && gridCanvasLeftElm && gridCanvasLeftElm.style) {
          const leftPaneHeight = parseInt(leftPaneElm.style.height, 10) || 0;
          const cellRowHeight = this.gridOptions?.rowHeight ?? 0;
          const filterRowHeight = this.gridOptions.enableFiltering ? this.gridOptions?.headerRowHeight ?? 0 : 0;
          const preHeaderRowHeight = this.gridOptions.createPreHeaderPanel ? this.gridOptions?.preHeaderPanelHeight ?? 0 : 0;
          if (isShowing) {
            let leftPaneMinHeight = leftPaneHeight !== null && leftPaneHeight < 100 ? leftPaneHeight : 100;
            leftPaneMinHeight += filterRowHeight + preHeaderRowHeight;
            leftPaneElm.style.minHeight = `${leftPaneMinHeight}px`;
            gridCanvasLeftElm.style.minHeight = `${cellRowHeight}px`;
          }
        }
      }
      let warningMessage = mergedOptions.message;
      if (this.gridOptions.enableTranslate && this._translaterService && mergedOptions?.messageKey) {
        warningMessage = this._translaterService.translate(mergedOptions.messageKey);
      }
      if (!this._warningLeftElement && gridCanvasLeftElm && gridCanvasRightElm) {
        this._warningLeftElement = document.createElement("div");
        this._warningLeftElement.classList.add(...classNameToList(emptyDataClassName), "left");
        applyHtmlToElement(this._warningLeftElement, warningMessage, this.gridOptions);
        this._warningRightElement = this._warningLeftElement.cloneNode(true);
        this._warningRightElement.classList.add("right");
        gridCanvasRightElm.appendChild(this._warningRightElement);
        gridCanvasLeftElm.appendChild(this._warningLeftElement);
      }
      if (this._warningLeftElement) {
        let leftDisplay = isShowing ? "flex" : "none";
        if (isFrozenGrid && isShowing) {
          leftDisplay = mergedOptions.hideFrozenLeftWarning ? "none" : "flex";
        }
        this._warningLeftElement.style.display = leftDisplay;
        const leftFrozenMarginLeft = typeof leftElementFrozenMarginLeft === "string" ? leftElementFrozenMarginLeft : `${leftElementFrozenMarginLeft}px`;
        this._warningLeftElement.style.marginLeft = isFrozenGrid ? leftFrozenMarginLeft : leftViewportMarginLeft;
      }
      if (this._warningRightElement) {
        let rightDisplay = isShowing ? "flex" : "none";
        if (isFrozenGrid && isShowing) {
          rightDisplay = mergedOptions.hideFrozenRightWarning ? "none" : "flex";
        }
        this._warningRightElement.style.display = rightDisplay;
        const rightFrozenMarginLeft = typeof rightElementFrozenMarginLeft === "string" ? rightElementFrozenMarginLeft : `${rightElementFrozenMarginLeft}px`;
        this._warningRightElement.style.marginLeft = isFrozenGrid ? rightFrozenMarginLeft : rightViewportMarginLeft;
      }
      return isShowing;
    }
  };

  // node_modules/@slickgrid-universal/pagination-component/dist/slick-pagination.component.js
  var SlickPaginationComponent = class {
    constructor() {
      this._enableTranslate = false;
      this._subscriptions = [];
      this.currentPagination = {};
      this.firstButtonClasses = "";
      this.lastButtonClasses = "";
      this.prevButtonClasses = "";
      this.nextButtonClasses = "";
      this.textItemsPerPage = "items per page";
      this.textItems = "items";
      this.textOf = "of";
      this.textPage = "Page";
      this._bindingHelper = new BindingHelper();
      this._bindingEventService = new BindingEventService();
    }
    get availablePageSizes() {
      return this._paginationService.availablePageSizes || [];
    }
    get dataFrom() {
      return this._paginationService.dataFrom;
    }
    get dataTo() {
      return this._paginationService.dataTo;
    }
    get itemsPerPage() {
      return this._paginationService.itemsPerPage;
    }
    set itemsPerPage(count) {
      this._paginationService.changeItemPerPage(count);
    }
    get pageCount() {
      return this._paginationService.pageCount;
    }
    get pageNumber() {
      return this._paginationService.pageNumber;
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this._grid?.getOptions() ?? {};
    }
    get gridUid() {
      return this._grid?.getUID() || "";
    }
    get locales() {
      return this.gridOptions?.locales ?? Constants.locales;
    }
    get totalItems() {
      return this._paginationService.totalItems;
    }
    get isLeftPaginationDisabled() {
      return this.pageNumber === 1 || this.totalItems === 0;
    }
    get isRightPaginationDisabled() {
      return this.pageNumber === this.pageCount || this.totalItems === 0;
    }
    init(grid, paginationService, pubSubService, translaterService) {
      this._grid = grid;
      this._pubSubService = pubSubService;
      this._translaterService = translaterService;
      this._paginationService = paginationService;
      this.currentPagination = paginationService.getFullPagination();
      this._bindingHelper.querySelectorPrefix = this.gridUid ? `.${this.gridUid} ` : "";
      this._enableTranslate = this.gridOptions?.enableTranslate ?? false;
      if (this._enableTranslate && (!this._translaterService || !this._translaterService.translate)) {
        throw new Error('[Slickgrid-Universal] requires a Translate Service to be installed and configured when the grid option "enableTranslate" is enabled.');
      }
      this.translatePaginationTexts();
      if (this._enableTranslate && this._pubSubService?.subscribe) {
        const translateEventName = this._translaterService?.eventName ?? "onLanguageChange";
        this._subscriptions.push(this._pubSubService.subscribe(translateEventName, () => this.translatePaginationTexts()));
      }
      this._subscriptions.push(this._pubSubService.subscribe("onPaginationRefreshed", (paginationChanges) => {
        Object.keys(paginationChanges).forEach((key) => this.currentPagination[key] = paginationChanges[key]);
        this.updatePageButtonsUsability();
        if (this._spanInfoFromToElm?.style) {
          this._spanInfoFromToElm.style.display = this.currentPagination.totalItems === 0 ? "none" : "";
        }
      }), this._pubSubService.subscribe("onPaginationSetCursorBased", () => {
        this.disposeDom();
        this.renderPagination(this._gridContainerElm);
      }));
    }
    /** dispose of all Subscriptions, DOM element & bindings */
    dispose() {
      this._pubSubService.unsubscribeAll(this._subscriptions);
      this.disposeDom();
    }
    /** dispose of the DOM element & bindings only (regular PubSub subscription will be preserved) */
    disposeDom() {
      this._bindingEventService.unbindAll();
      this._bindingHelper.dispose();
      emptyElement(this._paginationElement);
      this._paginationElement.remove();
    }
    /** render the Pagination in the DOM with all events & bindings */
    renderPagination(containerElm) {
      this._gridContainerElm = containerElm;
      const paginationElm = this.createPaginationContainer();
      const divNavContainerElm = createDomElement("div", { className: "slick-pagination-nav" });
      const leftNavElm = createDomElement("nav", { ariaLabel: "Page navigation" });
      const leftUlElm = createDomElement("ul", { className: "pagination" }, leftNavElm);
      this._seekFirstElm = createDomElement("li", { className: "page-item seek-first" }, leftUlElm);
      this._seekFirstElm.appendChild(createDomElement("a", { className: "page-link sgi icon-seek-first", ariaLabel: "First Page", role: "button" }));
      this._seekPrevElm = createDomElement("li", { className: "page-item seek-prev" }, leftUlElm);
      this._seekPrevElm.appendChild(createDomElement("a", { className: "page-link sgi icon-seek-prev", ariaLabel: "Previous Page", role: "button" }));
      const pageNumberSectionElm = this.createPageNumberSection();
      const rightNavElm = createDomElement("nav", { ariaLabel: "Page navigation" });
      const rightUlElm = createDomElement("ul", { className: "pagination" }, rightNavElm);
      this._seekNextElm = createDomElement("li", { className: "page-item seek-next" }, rightUlElm);
      this._seekNextElm.appendChild(createDomElement("a", { className: "page-link sgi icon-seek-next", ariaLabel: "Next Page", role: "button" }));
      this._seekEndElm = createDomElement("li", { className: "page-item seek-end" }, rightUlElm);
      this._seekEndElm.appendChild(createDomElement("a", { className: "page-link sgi icon-seek-end", ariaLabel: "Last Page", role: "button" }));
      paginationElm.appendChild(divNavContainerElm);
      divNavContainerElm.appendChild(leftNavElm);
      divNavContainerElm.appendChild(pageNumberSectionElm);
      divNavContainerElm.appendChild(rightNavElm);
      const paginationSettingsElm = this.createPaginationSettingsSection();
      paginationElm.appendChild(divNavContainerElm);
      paginationElm.appendChild(paginationSettingsElm);
      this._paginationElement.appendChild(paginationElm);
      if (containerElm?.appendChild && this._paginationElement) {
        containerElm.appendChild(this._paginationElement);
      }
      this.renderPageSizes();
      this.addBindings();
      this.addEventListeners();
      this.updatePageButtonsUsability();
    }
    /** Render and fill the Page Sizes <select> element */
    renderPageSizes() {
      if (this._itemPerPageElm && Array.isArray(this.availablePageSizes)) {
        for (const option2 of this.availablePageSizes) {
          createDomElement("option", { value: `${option2}`, text: `${option2}` }, this._itemPerPageElm);
        }
      }
    }
    /** Add some DOM Element bindings */
    addBindings() {
      this._bindingHelper.addElementBinding(this, "firstButtonClasses", "li.page-item.seek-first", "className");
      this._bindingHelper.addElementBinding(this, "prevButtonClasses", "li.page-item.seek-prev", "className");
      this._bindingHelper.addElementBinding(this, "lastButtonClasses", "li.page-item.seek-end", "className");
      this._bindingHelper.addElementBinding(this, "nextButtonClasses", "li.page-item.seek-next", "className");
      this._bindingHelper.addElementBinding(this.currentPagination, "dataFrom", "span.item-from", "textContent");
      this._bindingHelper.addElementBinding(this.currentPagination, "dataTo", "span.item-to", "textContent");
      this._bindingHelper.addElementBinding(this.currentPagination, "totalItems", "span.total-items", "textContent");
      this._bindingHelper.addElementBinding(this.currentPagination, "pageCount", "span.page-count", "textContent");
      this._bindingHelper.addElementBinding(this.currentPagination, "pageSize", "select.items-per-page", "value");
      this._paginationService.isCursorBased ? this._bindingHelper.addElementBinding(this.currentPagination, "pageNumber", "span.page-number", "textContent") : this._bindingHelper.addElementBinding(this.currentPagination, "pageNumber", "input.page-number", "value", "change", this.changeToCurrentPage.bind(this));
      this._bindingHelper.addElementBinding(this, "textItems", "span.text-items", "textContent");
      this._bindingHelper.addElementBinding(this, "textItemsPerPage", "span.text-item-per-page", "textContent");
      this._bindingHelper.addElementBinding(this, "textOf", "span.text-of", "textContent");
      this._bindingHelper.addElementBinding(this, "textPage", "span.text-page", "textContent");
    }
    /** Add some DOM Element event listeners */
    addEventListeners() {
      this._bindingEventService.bind(this._seekFirstElm, "click", this.changeToFirstPage.bind(this));
      this._bindingEventService.bind(this._seekEndElm, "click", this.changeToLastPage.bind(this));
      this._bindingEventService.bind(this._seekNextElm, "click", this.changeToNextPage.bind(this));
      this._bindingEventService.bind(this._seekPrevElm, "click", this.changeToPreviousPage.bind(this));
      this._bindingEventService.bind(this._itemPerPageElm, "change", this.updateItemsPerPage.bind(this));
    }
    changeToFirstPage(event) {
      if (!this.isLeftPaginationDisabled) {
        this._paginationService.goToFirstPage(event);
      }
    }
    changeToLastPage(event) {
      if (!this.isRightPaginationDisabled) {
        this._paginationService.goToLastPage(event);
      }
    }
    changeToNextPage(event) {
      if (!this.isRightPaginationDisabled) {
        this._paginationService.goToNextPage(event);
      }
    }
    changeToPreviousPage(event) {
      if (!this.isLeftPaginationDisabled) {
        this._paginationService.goToPreviousPage(event);
      }
    }
    changeToCurrentPage(pageNumber) {
      this._paginationService.goToPageNumber(+pageNumber);
    }
    updateItemsPerPage(event) {
      this.itemsPerPage = +(event?.target?.value ?? 0);
    }
    /** Translate all the texts shown in the UI, use ngx-translate service when available or custom locales when service is null */
    translatePaginationTexts() {
      const localeOrDefault = (key) => this.locales[key] || key;
      if (this._enableTranslate && this._translaterService?.translate) {
        const translationPrefix = getTranslationPrefix(this.gridOptions);
        this.textItemsPerPage = this._translaterService.translate(`${translationPrefix}ITEMS_PER_PAGE`);
        this.textItems = this._translaterService.translate(`${translationPrefix}ITEMS`);
        this.textOf = this._translaterService.translate(`${translationPrefix}OF`);
        this.textPage = this._translaterService.translate(`${translationPrefix}PAGE`);
      } else if (this.locales) {
        this.textItemsPerPage = localeOrDefault("TEXT_ITEMS_PER_PAGE");
        this.textItems = localeOrDefault("TEXT_ITEMS");
        this.textOf = localeOrDefault("TEXT_OF");
        this.textPage = localeOrDefault("TEXT_PAGE");
      }
    }
    // --
    // protected functions
    // --------------------
    /** Create the Pagination Container */
    createPaginationContainer() {
      const paginationContainerElm = createDomElement("div", {
        id: "pager",
        className: `slick-pagination-container ${this.gridUid} pager`,
        style: { width: "100%" }
      });
      const paginationElm = createDomElement("div", { className: "slick-pagination" }, paginationContainerElm);
      this._paginationElement = paginationContainerElm;
      return paginationElm;
    }
    createPageNumberSection() {
      const divElm = createDomElement("div", { className: "slick-page-number" });
      createDomElement("span", { className: "text-page", textContent: "Page" }, divElm);
      divElm.appendChild(document.createTextNode(" "));
      if (this._paginationService.isCursorBased) {
        createDomElement("span", {
          className: "page-number",
          ariaLabel: "Page Number",
          dataset: { test: "page-number-label" },
          textContent: "1"
        }, divElm);
      } else {
        createDomElement("input", {
          type: "text",
          className: "form-control page-number",
          ariaLabel: "Page Number",
          value: "1",
          size: 1,
          dataset: { test: "page-number-input" }
        }, divElm);
      }
      divElm.appendChild(document.createTextNode(" "));
      createDomElement("span", { className: "text-of", textContent: "of" }, divElm);
      divElm.appendChild(document.createTextNode(" "));
      createDomElement("span", { className: "page-count", dataset: { test: "page-count" } }, divElm);
      return divElm;
    }
    createPaginationSettingsSection() {
      const spanContainerElm = createDomElement("span", { className: "slick-pagination-settings" });
      this._itemPerPageElm = createDomElement("select", { id: "items-per-page-label", ariaLabel: "Items per Page", className: "items-per-page" }, spanContainerElm);
      spanContainerElm.appendChild(document.createTextNode(" "));
      createDomElement("span", { className: "text-item-per-page", textContent: "items per page" }, spanContainerElm);
      spanContainerElm.appendChild(document.createTextNode(", "));
      const spanPaginationCount = createDomElement("span", { className: "slick-pagination-count" }, spanContainerElm);
      this._spanInfoFromToElm = createDomElement("span", { className: "page-info-from-to" }, spanPaginationCount);
      createDomElement("span", { className: "item-from", ariaLabel: "Page Item From", dataset: { test: "item-from" } }, this._spanInfoFromToElm);
      this._spanInfoFromToElm.appendChild(document.createTextNode("-"));
      createDomElement("span", { className: "item-to", ariaLabel: "Page Item To", dataset: { test: "item-to" } }, this._spanInfoFromToElm);
      this._spanInfoFromToElm.appendChild(document.createTextNode(" "));
      createDomElement("span", { className: "text-of", textContent: "of" }, this._spanInfoFromToElm);
      this._spanInfoFromToElm.appendChild(document.createTextNode(" "));
      const spanInfoTotalElm = createDomElement("span", { className: "page-info-total-items" }, spanPaginationCount);
      createDomElement("span", { className: "total-items", ariaLabel: "Total Items", dataset: { test: "total-items" } }, spanInfoTotalElm);
      spanInfoTotalElm.appendChild(document.createTextNode(" "));
      createDomElement("span", { className: "text-items", textContent: "items" }, spanInfoTotalElm);
      spanInfoTotalElm.appendChild(document.createTextNode(" "));
      return spanContainerElm;
    }
    updatePageButtonsUsability() {
      this.firstButtonClasses = `page-item seek-first${this.isLeftPaginationDisabled ? " disabled" : ""}`;
      this.prevButtonClasses = `page-item seek-prev${this.isLeftPaginationDisabled ? " disabled" : ""}`;
      this.lastButtonClasses = `page-item seek-end${this.isRightPaginationDisabled ? " disabled" : ""}`;
      this.nextButtonClasses = `page-item seek-next${this.isRightPaginationDisabled ? " disabled" : ""}`;
    }
  };

  // node_modules/@slickgrid-universal/custom-footer-component/dist/slick-footer.component.js
  var SlickFooterComponent = class {
    get eventHandler() {
      return this._eventHandler;
    }
    /** Getter for the grid uid */
    get gridUid() {
      return this.grid?.getUID() ?? "";
    }
    get gridUidSelector() {
      return this.gridUid ? `.${this.gridUid}` : "";
    }
    /** Getter for the Grid Options pulled through the Grid Object */
    get gridOptions() {
      return this.grid?.getOptions() ?? {};
    }
    get locales() {
      return this.gridOptions?.locales ?? Constants.locales;
    }
    set metrics(metrics) {
      this.renderMetrics(metrics);
    }
    get leftFooterText() {
      return document.querySelector(`.slick-custom-footer${this.gridUidSelector} .left-footer`)?.textContent ?? "";
    }
    set leftFooterText(text) {
      this.renderLeftFooterText(text);
    }
    get rightFooterText() {
      return document.querySelector(`.slick-custom-footer${this.gridUidSelector} .right-footer`)?.textContent ?? "";
    }
    set rightFooterText(text) {
      this.renderRightFooterText(text);
    }
    constructor(grid, customFooterOptions, pubSubService, translaterService) {
      this.grid = grid;
      this.customFooterOptions = customFooterOptions;
      this.pubSubService = pubSubService;
      this.translaterService = translaterService;
      this._enableTranslate = false;
      this._isLeftFooterOriginallyEmpty = true;
      this._isLeftFooterDisplayingSelectionRowCount = false;
      this._isRightFooterOriginallyEmpty = true;
      this._selectedRowCount = 0;
      this._subscriptions = [];
      this._bindingHelper = new BindingHelper();
      this._bindingHelper.querySelectorPrefix = `.${this.gridUid} `;
      this._eventHandler = new SlickEventHandler();
      this._enableTranslate = this.gridOptions?.enableTranslate ?? false;
      this._isLeftFooterOriginallyEmpty = !this.gridOptions.customFooterOptions?.leftFooterText;
      this._isRightFooterOriginallyEmpty = !this.gridOptions.customFooterOptions?.rightFooterText;
      this.registerOnSelectedRowsChangedWhenEnabled(customFooterOptions);
      if (this._enableTranslate && (!this.translaterService || !this.translaterService.translate)) {
        throw new Error('[Slickgrid-Universal] requires a Translate Service to be installed and configured when the grid option "enableTranslate" is enabled.');
      }
      this.translateCustomFooterTexts();
      if (this._enableTranslate && this.pubSubService?.subscribe) {
        const translateEventName = this.translaterService?.eventName ?? "onLanguageChange";
        this._subscriptions.push(this.pubSubService.subscribe(translateEventName, () => this.translateCustomFooterTexts()));
      }
    }
    dispose() {
      this._eventHandler.unsubscribeAll();
      this.pubSubService.unsubscribeAll(this._subscriptions);
      this._bindingHelper.dispose();
      emptyElement(this._footerElement);
      this._footerElement?.remove();
    }
    /**
     * We could optionally display a custom footer below the grid to show some metrics (last update, item count with/without filters)
     * It's an opt-in, user has to enable "showCustomFooter" and it cannot be used when there's already a Pagination since they display the same kind of info
     */
    renderFooter(gridParentContainerElm) {
      this.translateCustomFooterTexts();
      this.createFooterContainer(gridParentContainerElm);
    }
    /** Render element attribute values */
    renderMetrics(metrics) {
      const lastUpdateTimestamp = metrics?.endTime ? format(metrics.endTime, this.customFooterOptions.dateFormat, "en-US") : "";
      this._bindingHelper.setElementAttributeValue("span.last-update-timestamp", "textContent", lastUpdateTimestamp);
      this._bindingHelper.setElementAttributeValue("span.item-count", "textContent", metrics.itemCount);
      this._bindingHelper.setElementAttributeValue("span.total-count", "textContent", metrics.totalItemCount);
      if (this.customFooterOptions.metricTexts?.lastUpdate) {
        this._bindingHelper.addElementBinding(this.customFooterOptions.metricTexts, "lastUpdate", "span.text-last-update", "textContent");
      }
      this._bindingHelper.addElementBinding(this.customFooterOptions.metricTexts, "items", "span.text-items", "textContent");
      this._bindingHelper.addElementBinding(this.customFooterOptions.metricTexts, "of", "span.text-of", "textContent");
    }
    /** Render the left side footer text */
    renderLeftFooterText(text) {
      this._bindingHelper.setElementAttributeValue("div.left-footer", "textContent", text);
    }
    /** Render the right side footer text */
    renderRightFooterText(text) {
      this._bindingHelper.setElementAttributeValue("div.right-footer", "textContent", text);
    }
    /** Translate all Custom Footer Texts (footer with metrics) */
    translateCustomFooterTexts() {
      if (this.gridOptions.enableTranslate && this.translaterService?.translate) {
        this.customFooterOptions.metricTexts = this.customFooterOptions.metricTexts || {};
        for (const propName of Object.keys(this.customFooterOptions.metricTexts)) {
          if (propName.lastIndexOf("Key") > 0) {
            const propNameWithoutKey = propName.substring(0, propName.lastIndexOf("Key"));
            this.customFooterOptions.metricTexts[propNameWithoutKey] = this.translaterService.translate(this.customFooterOptions.metricTexts[propName] || " ");
          }
        }
        if (this._isLeftFooterDisplayingSelectionRowCount) {
          this.leftFooterText = `${this._selectedRowCount} ${this.customFooterOptions.metricTexts.itemsSelected}`;
        }
      } else if (this.locales) {
        this.customFooterOptions.metricTexts = this.customFooterOptions.metricTexts || {};
        this.customFooterOptions.metricTexts.lastUpdate ||= this.localeOrDefault("TEXT_LAST_UPDATE");
        this.customFooterOptions.metricTexts.items ||= this.localeOrDefault("TEXT_ITEMS");
        this.customFooterOptions.metricTexts.itemsSelected ||= this.localeOrDefault("TEXT_ITEMS_SELECTED");
        this.customFooterOptions.metricTexts.of ||= this.localeOrDefault("TEXT_OF");
      }
    }
    // --
    // protected functions
    // --------------------
    localeOrDefault(key) {
      return this.locales[key] || key;
    }
    /** Create the Footer Container */
    createFooterContainer(gridParentContainerElm) {
      const footerElm = createDomElement("div", {
        className: `slick-custom-footer ${this.gridUid}`,
        style: {
          width: "100%",
          height: `${this.customFooterOptions.footerHeight || 20}px`
        }
      });
      const leftFooterElm = createDomElement("div", { className: `left-footer ${this.customFooterOptions.leftContainerClass}` });
      applyHtmlToElement(leftFooterElm, this.customFooterOptions.leftFooterText, this.gridOptions);
      footerElm.appendChild(leftFooterElm);
      footerElm.appendChild(this.createFooterRightContainer());
      this._footerElement = footerElm;
      if (gridParentContainerElm?.appendChild && this._footerElement) {
        gridParentContainerElm.appendChild(this._footerElement);
      }
    }
    /** Create the Right Section Footer */
    createFooterRightContainer() {
      const rightFooterElm = createDomElement("div", {
        className: `right-footer ${this.customFooterOptions.rightContainerClass || ""}`
      });
      if (!this._isRightFooterOriginallyEmpty) {
        applyHtmlToElement(rightFooterElm, this.customFooterOptions.rightFooterText, this.gridOptions);
      } else if (!this.customFooterOptions.hideMetrics) {
        rightFooterElm.classList.add("metrics");
        const lastUpdateElm = createDomElement("span", { className: "timestamp" }, rightFooterElm);
        if (!this.customFooterOptions.hideLastUpdateTimestamp) {
          const footerLastUpdateElm = this.createFooterLastUpdate();
          if (footerLastUpdateElm) {
            lastUpdateElm.appendChild(footerLastUpdateElm);
          }
        }
        rightFooterElm.appendChild(createDomElement("span", { className: "item-count", textContent: `${this.metrics?.itemCount ?? "0"}` }));
        if (!this.customFooterOptions.hideTotalItemCount) {
          rightFooterElm.appendChild(document.createTextNode("\r\n"));
          rightFooterElm.appendChild(createDomElement("span", { className: "text-of", textContent: ` ${this.customFooterOptions.metricTexts?.of ?? "of"} ` }));
          rightFooterElm.appendChild(document.createTextNode("\r\n"));
          rightFooterElm.appendChild(createDomElement("span", { className: "total-count", textContent: `${this.metrics?.totalItemCount ?? "0"}` }));
        }
        rightFooterElm.appendChild(document.createTextNode("\r\n"));
        rightFooterElm.appendChild(createDomElement("span", {
          className: "text-items",
          textContent: ` ${this.customFooterOptions.metricTexts?.items ?? "items"} `
        }));
      }
      return rightFooterElm;
    }
    /** Create the Right Section Last Update Timestamp */
    createFooterLastUpdate() {
      const lastUpdateText = this.customFooterOptions?.metricTexts?.lastUpdate ?? "Last Update";
      const lastUpdateTimestamp = this.metrics?.endTime ? format(this.metrics?.endTime, this.customFooterOptions.dateFormat, "en-US") : "";
      const lastUpdateContainerElm = createDomElement("span");
      lastUpdateContainerElm.appendChild(createDomElement("span", { className: "text-last-update", textContent: lastUpdateText }));
      lastUpdateContainerElm.appendChild(document.createTextNode("\r\n"));
      lastUpdateContainerElm.appendChild(createDomElement("span", { className: "last-update-timestamp", textContent: lastUpdateTimestamp }));
      lastUpdateContainerElm.appendChild(createDomElement("span", { className: "separator", textContent: ` ${this.customFooterOptions.metricSeparator || ""} ` }));
      return lastUpdateContainerElm;
    }
    /**
     * When user has row selections enabled and does not have any custom text shown on the left side footer,
     * we will show the row selection count on the bottom left side of the footer (by subscribing to the SlickGrid `onSelectedRowsChanged` event).
     * @param customFooterOptions
     */
    registerOnSelectedRowsChangedWhenEnabled(customFooterOptions) {
      const isRowSelectionEnabled = this.gridOptions.enableCheckboxSelector || this.gridOptions.enableSelection;
      if (isRowSelectionEnabled && customFooterOptions && !customFooterOptions.hideRowSelectionCount && this._isLeftFooterOriginallyEmpty) {
        this._isLeftFooterDisplayingSelectionRowCount = true;
        const selectedCountText = customFooterOptions.metricTexts?.itemsSelected ?? this.localeOrDefault("TEXT_ITEMS_SELECTED");
        customFooterOptions.leftFooterText = `0 ${selectedCountText}`;
        this._eventHandler.subscribe(this.grid.onSelectedRowsChanged, (_e, args) => {
          this._selectedRowCount = args.rows.length;
          const selectedCountText2 = customFooterOptions.metricTexts?.itemsSelected ?? this.localeOrDefault("TEXT_ITEMS_SELECTED");
          this.leftFooterText = `${this._selectedRowCount} ${selectedCountText2}`;
        });
      }
    }
  };

  // node_modules/@slickgrid-universal/vanilla-bundle/dist/services/universalContainer.service.js
  var UniversalContainerService = class {
    constructor() {
      this.dependencies = [];
    }
    get(key) {
      const dependency = this.dependencies.find((dep) => dep.key === key);
      if (dependency?.instance) {
        return dependency.instance;
      }
      return null;
    }
    dispose() {
      this.dependencies = [];
    }
    registerInstance(key, instance) {
      const dependency = this.dependencies.some((dep) => dep.key === key);
      if (!dependency) {
        this.dependencies.push({ key, instance });
      }
    }
  };

  // node_modules/@slickgrid-universal/vanilla-bundle/dist/components/slick-vanilla-grid-bundle.js
  var WARN_NO_PREPARSE_DATE_SIZE = 1e4;
  var SlickVanillaGridBundle = class {
    get backendService() {
      return this.gridOptions.backendServiceApi?.service;
    }
    get eventHandler() {
      return this._eventHandler;
    }
    get columnDefinitions() {
      return this._columns || [];
    }
    set columnDefinitions(columns) {
      this.columnDefinitionsChanged(columns);
    }
    get dataset() {
      return this.dataView?.getItems() || [];
    }
    set dataset(newDataset) {
      const prevDatasetLn = this._currentDatasetLength;
      const isDatasetEqual = dequal(newDataset, this.dataset || []);
      let data = this._gridOptions?.enableDeepCopyDatasetOnPageLoad ? deepCopy([...newDataset]) : newDataset;
      if (this.slickGrid && this.gridOptions?.enableTreeData && Array.isArray(newDataset) && (newDataset.length > 0 || newDataset.length !== prevDatasetLn || !isDatasetEqual)) {
        this._isDatasetHierarchicalInitialized = false;
        data = this.sortTreeDataset(newDataset, !isDatasetEqual);
      }
      this.refreshGridData(data || []);
      this._currentDatasetLength = (newDataset || []).length;
      if (this.slickGrid && this.gridOptions.autoFitColumnsOnFirstLoad && prevDatasetLn === 0 && !this._isAutosizeColsCalled) {
        this.slickGrid.autosizeColumns();
        this._isAutosizeColsCalled = true;
      }
      this.suggestDateParsingWhenHelpful();
    }
    get datasetHierarchical() {
      return this.sharedService.hierarchicalDataset;
    }
    set datasetHierarchical(newHierarchicalDataset) {
      const isDatasetEqual = dequal(newHierarchicalDataset, this.sharedService.hierarchicalDataset || []);
      const prevFlatDatasetLn = this._currentDatasetLength;
      this.sharedService.hierarchicalDataset = newHierarchicalDataset;
      if (newHierarchicalDataset && this.columnDefinitions && this.filterService?.clearFilters) {
        this.filterService.clearFilters();
      }
      if (this.dataView && newHierarchicalDataset && this.slickGrid && this.sortService?.processTreeDataInitialSort) {
        this.sortService.processTreeDataInitialSort();
        this.treeDataService.initHierarchicalTree();
        queueMicrotaskPolyfill(() => {
          const flatDatasetLn = this.dataView?.getItemCount() ?? 0;
          if (flatDatasetLn > 0 && (flatDatasetLn !== prevFlatDatasetLn || !isDatasetEqual)) {
            this.filterService.refreshTreeDataFilters();
          }
        });
      }
      this._isDatasetHierarchicalInitialized = true;
    }
    set eventPubSubService(pubSub) {
      this._eventPubSubService = pubSub;
    }
    set isDatasetHierarchicalInitialized(isInitialized) {
      this._isDatasetHierarchicalInitialized = isInitialized;
    }
    get gridOptions() {
      return this._gridOptions || {};
    }
    set gridOptions(options) {
      options ??= {};
      let mergedOptions;
      if (this.slickGrid?.getOptions) {
        mergedOptions = extend(true, {}, this.slickGrid.getOptions(), options);
      } else {
        mergedOptions = this.mergeGridOptions(options);
      }
      if (this.sharedService?.gridOptions && this.slickGrid?.setOptions) {
        this.sharedService.gridOptions = mergedOptions;
        this.slickGrid.setOptions(mergedOptions, false, true);
        this.slickGrid.reRenderColumns(true);
      }
      this.setDarkMode(options.darkMode);
      this._gridOptions = mergedOptions;
    }
    get paginationOptions() {
      return this._paginationOptions;
    }
    set paginationOptions(newPaginationOptions) {
      if (newPaginationOptions && this._paginationOptions) {
        this._paginationOptions = { ...this._paginationOptions, ...newPaginationOptions };
      } else {
        this._paginationOptions = newPaginationOptions;
      }
      this.gridOptions.pagination = this._paginationOptions;
      this.paginationService.updateTotalItems(newPaginationOptions?.totalItems ?? 0, true);
    }
    get isDatasetInitialized() {
      return this._isDatasetInitialized;
    }
    set isDatasetInitialized(isInitialized) {
      this._isDatasetInitialized = isInitialized;
    }
    get isGridInitialized() {
      return this._isGridInitialized;
    }
    get instances() {
      return this._slickerGridInstances;
    }
    get extensions() {
      return this._extensions;
    }
    get registeredResources() {
      return this._registeredResources;
    }
    /**
     * Slicker Grid Bundle constructor
     * @param {Object} gridParentContainerElm - div HTML DOM element container
     * @param {Array<Column>} columnDefs - Column Definitions
     * @param {Object} options - Grid Options
     * @param {Array<Object>} dataset - Dataset
     * @param {Array<Object>} hierarchicalDataset - Hierarchical Dataset
     * @param {Object} services - Typically only used for Unit Testing when we want to pass Mocked/Stub Services
     */
    constructor(gridParentContainerElm, columnDefs, options, dataset, hierarchicalDataset, services) {
      this._currentDatasetLength = 0;
      this._darkMode = false;
      this._collectionObservers = [];
      this._gridOptions = {};
      this._hideHeaderRowAfterPageLoad = false;
      this._isAutosizeColsCalled = false;
      this._isDatasetInitialized = false;
      this._isDatasetHierarchicalInitialized = false;
      this._isGridInitialized = false;
      this._isLocalGrid = true;
      this._isPaginationInitialized = false;
      this._registeredResources = [];
      this._scrollEndCalled = false;
      this._slickgridInitialized = false;
      this.customDataView = false;
      this.totalItems = 0;
      this.subscriptions = [];
      this.showPagination = false;
      if (!gridParentContainerElm || gridParentContainerElm.querySelectorAll(".slickgrid-container").length !== 0) {
        return;
      }
      gridParentContainerElm.classList.add("grid-pane");
      this._gridParentContainerElm = gridParentContainerElm;
      this._gridContainerElm = document.createElement("div");
      this._gridContainerElm.classList.add("slickgrid-container");
      gridParentContainerElm.appendChild(this._gridContainerElm);
      this._hideHeaderRowAfterPageLoad = options?.showHeaderRow === false;
      this._columns = columnDefs || [];
      if (this._columns.length > 0) {
        this.copyColumnWidthsReference(this._columns);
      }
      this._registeredResources = options?.externalResources || [];
      this._gridOptions = this.mergeGridOptions(options || {});
      if (this._gridOptions.darkMode) {
        this.setDarkMode(true);
      }
      this.universalContainerService = services?.universalContainerService ?? new UniversalContainerService();
      this.translaterService = services?.translaterService ?? this._gridOptions?.translater;
      this._eventPubSubService = services?.eventPubSubService ?? new EventPubSubService(gridParentContainerElm);
      this._eventPubSubService.eventNamingStyle = this._gridOptions?.eventNamingStyle ?? "camelCase";
      const slickgridConfig = new SlickgridConfig();
      this.backendUtilityService = services?.backendUtilityService ?? new BackendUtilityService();
      this.gridEventService = services?.gridEventService ?? new GridEventService();
      this.sharedService = services?.sharedService ?? new SharedService();
      this.collectionService = services?.collectionService ?? new CollectionService(this.translaterService);
      this.extensionUtility = services?.extensionUtility ?? new ExtensionUtility(this.sharedService, this.backendUtilityService, this.translaterService);
      this.filterFactory = new FilterFactory(slickgridConfig, this.translaterService, this.collectionService);
      this.filterService = services?.filterService ?? new FilterService(this.filterFactory, this._eventPubSubService, this.sharedService, this.backendUtilityService);
      this.resizerService = services?.resizerService ?? new ResizerService(this._eventPubSubService);
      this.sortService = services?.sortService ?? new SortService(this.collectionService, this.sharedService, this._eventPubSubService, this.backendUtilityService);
      this.treeDataService = services?.treeDataService ?? new TreeDataService(this._eventPubSubService, this.filterService, this.sharedService, this.sortService);
      this.paginationService = services?.paginationService ?? new PaginationService(this._eventPubSubService, this.sharedService, this.backendUtilityService);
      this.extensionService = services?.extensionService ?? new ExtensionService(this.extensionUtility, this.filterService, this._eventPubSubService, this.sharedService, this.sortService, this.treeDataService, this.translaterService, () => this.gridService);
      this.gridStateService = services?.gridStateService ?? new GridStateService(this.extensionService, this.filterService, this._eventPubSubService, this.sharedService, this.sortService, this.treeDataService);
      this.gridService = services?.gridService ?? new GridService(this.gridStateService, this.filterService, this._eventPubSubService, this.paginationService, this.sharedService, this.sortService, this.treeDataService);
      this.headerGroupingService = services?.headerGroupingService ?? new HeaderGroupingService(this.extensionUtility);
      if (hierarchicalDataset) {
        this.sharedService.hierarchicalDataset = (this._gridOptions?.enableDeepCopyDatasetOnPageLoad ? deepCopy([...hierarchicalDataset]) : hierarchicalDataset) || [];
      }
      const eventHandler = new SlickEventHandler();
      this.universalContainerService.registerInstance("PubSubService", this._eventPubSubService);
      this.universalContainerService.registerInstance("EventPubSubService", this._eventPubSubService);
      this.universalContainerService.registerInstance("ExtensionUtility", this.extensionUtility);
      this.universalContainerService.registerInstance("FilterService", this.filterService);
      this.universalContainerService.registerInstance("CollectionService", this.collectionService);
      this.universalContainerService.registerInstance("ExtensionService", this.extensionService);
      this.universalContainerService.registerInstance("GridEventService", this.gridEventService);
      this.universalContainerService.registerInstance("GridService", this.gridService);
      this.universalContainerService.registerInstance("GridStateService", this.gridStateService);
      this.universalContainerService.registerInstance("HeaderGroupingService", this.headerGroupingService);
      this.universalContainerService.registerInstance("PaginationService", this.paginationService);
      this.universalContainerService.registerInstance("ResizerService", this.resizerService);
      this.universalContainerService.registerInstance("SharedService", this.sharedService);
      this.universalContainerService.registerInstance("SortService", this.sortService);
      this.universalContainerService.registerInstance("TranslaterService", this.translaterService);
      this.universalContainerService.registerInstance("TreeDataService", this.treeDataService);
      this.initialization(this._gridContainerElm, eventHandler, dataset);
    }
    emptyGridContainerElm() {
      const gridContainerId = this.gridOptions?.gridContainerId ?? "grid1";
      const gridContainerElm = document.querySelector(`#${gridContainerId}`);
      emptyElement(gridContainerElm);
    }
    /** Dispose of the Component */
    dispose(shouldEmptyDomElementContainer = false) {
      this._eventPubSubService?.publish("onBeforeGridDestroy", this.slickGrid);
      this._eventHandler?.unsubscribeAll();
      this.extensionService?.dispose();
      this.filterService?.dispose();
      this.gridEventService?.dispose();
      this.gridService?.dispose();
      this.gridStateService?.dispose();
      this.headerGroupingService?.dispose();
      this.paginationService?.dispose();
      this.resizerService?.dispose();
      this.sortService?.dispose();
      this.treeDataService?.dispose();
      this.universalContainerService?.dispose();
      this.backendService?.dispose?.();
      this.disposeExternalResources();
      this.slickFooter?.dispose();
      this.slickEmptyWarning?.dispose();
      this.paginationComponent?.dispose();
      unsubscribeAll(this.subscriptions);
      this._eventPubSubService?.unsubscribeAll();
      this.dataView?.setItems([]);
      if (typeof this.dataView?.destroy === "function") {
        this.dataView?.destroy();
      }
      this.slickGrid?.destroy(true);
      this.slickGrid = null;
      if (this.backendServiceApi) {
        for (const prop of Object.keys(this.backendServiceApi)) {
          this.backendServiceApi[prop] = null;
        }
        this.backendServiceApi = void 0;
      }
      for (const prop of Object.keys(this.columnDefinitions)) {
        this.columnDefinitions[prop] = null;
      }
      for (const prop of Object.keys(this.sharedService)) {
        this.sharedService[prop] = null;
      }
      this.datasetHierarchical = void 0;
      this._columns = [];
      this._collectionObservers.forEach((obs) => obs?.disconnect());
      this._eventPubSubService?.unsubscribeAll();
      this._slickerGridInstances = null;
      if (shouldEmptyDomElementContainer) {
        this.emptyGridContainerElm();
        emptyElement(this._gridContainerElm);
        emptyElement(this._gridParentContainerElm);
        this._gridContainerElm?.remove();
        this._gridParentContainerElm?.remove();
      }
    }
    disposeExternalResources() {
      if (Array.isArray(this._registeredResources)) {
        while (this._registeredResources.length > 0) {
          const res = this._registeredResources.pop();
          if (typeof res?.dispose === "function") {
            res.dispose();
          }
        }
      }
      this._registeredResources = [];
    }
    initialization(gridContainerElm, eventHandler, inputDataset) {
      if (this.gridOptions && (this.gridOptions.frozenRow !== void 0 && this.gridOptions.frozenRow >= 0 || this.gridOptions.frozenColumn !== void 0 && this.gridOptions.frozenColumn >= 0) && this.gridOptions.enableMouseWheelScrollHandler === void 0) {
        this.gridOptions.enableMouseWheelScrollHandler = true;
      }
      this._gridContainerElm = gridContainerElm;
      this._eventPubSubService.publish("onBeforeGridCreate", true);
      this._isAutosizeColsCalled = false;
      this._eventHandler = eventHandler;
      this._gridOptions = this.mergeGridOptions(this._gridOptions || {});
      this.backendServiceApi = this._gridOptions?.backendServiceApi;
      this._isLocalGrid = !this.backendServiceApi;
      this._eventPubSubService.eventNamingStyle = this._gridOptions?.eventNamingStyle ?? "camelCase";
      this._paginationOptions = this.gridOptions?.pagination;
      if (this._gridOptions.backendServiceApi && !this._gridOptions.backendServiceApi?.disableInternalPostProcess) {
        this.createBackendApiInternalPostProcessCallback(this._gridOptions);
      }
      if (!this.customDataView) {
        const dataviewInlineFilters = this._gridOptions?.dataView?.inlineFilters ?? false;
        let dataViewOptions = { ...this._gridOptions.dataView, inlineFilters: dataviewInlineFilters };
        if (this.gridOptions.draggableGrouping || this.gridOptions.enableGrouping) {
          this.groupItemMetadataProvider = new SlickGroupItemMetadataProvider(this._gridOptions.groupItemMetadataOption);
          this.sharedService.groupItemMetadataProvider = this.groupItemMetadataProvider;
          dataViewOptions = { ...dataViewOptions, groupItemMetadataProvider: this.groupItemMetadataProvider };
        }
        this.dataView = new SlickDataView(dataViewOptions, this._eventPubSubService);
        this._eventPubSubService.publish("onDataviewCreated", this.dataView);
      }
      this.preRegisterResources();
      this._columns = this.loadSlickGridEditors(this._columns || []);
      if (this._gridOptions?.autoAddCustomEditorFormatter) {
        autoAddEditorFormatterToColumnsWithEditor(this._columns, this._gridOptions.autoAddCustomEditorFormatter);
      }
      this.sharedService.allColumns = this._columns;
      this.extensionService.createExtensionsBeforeGridCreation(this._columns, this._gridOptions);
      if (this.gridOptions.presets?.pinning) {
        this.gridOptions = { ...this.gridOptions, ...this.gridOptions.presets.pinning };
      }
      this.slickGrid = new SlickGrid(gridContainerElm, this.dataView, this._columns, this._gridOptions, this._eventPubSubService);
      if (typeof this.dataView.setGrid === "function") {
        this.dataView.setGrid(this.slickGrid);
      }
      this.sharedService.dataView = this.dataView;
      this.sharedService.slickGrid = this.slickGrid;
      this.sharedService.gridContainerElement = this._gridContainerElm;
      if (this.groupItemMetadataProvider) {
        this.slickGrid.registerPlugin(this.groupItemMetadataProvider);
      }
      this.registerResources();
      this.extensionService.bindDifferentExtensions();
      this.bindDifferentHooks(this.slickGrid, this._gridOptions, this.dataView);
      this._slickgridInitialized = true;
      this.sharedService.frozenVisibleColumnId = this.slickGrid.getFrozenColumnId();
      this.slickGrid.init();
      this.resizerService.init(this.slickGrid, this._gridParentContainerElm);
      if (!this.gridOptions.enablePagination && this.gridOptions.showCustomFooter && this.gridOptions.customFooterOptions) {
        this.slickFooter = new SlickFooterComponent(this.slickGrid, this.gridOptions.customFooterOptions, this._eventPubSubService, this.translaterService);
        this.slickFooter.renderFooter(this._gridParentContainerElm);
      }
      inputDataset = inputDataset || [];
      const initialDataset = this.gridOptions?.enableTreeData ? this.sortTreeDataset(inputDataset) : inputDataset;
      if (this.dataView) {
        this.dataView.beginUpdate();
        this.dataView.setItems(initialDataset, this._gridOptions.datasetIdPropertyName);
        this._currentDatasetLength = inputDataset.length;
        this.dataView.endUpdate();
      }
      if (this.slickGrid?.getSelectionModel() && this._gridOptions?.dataView?.hasOwnProperty("syncGridSelection")) {
        let preservedRowSelectionWithBackend = false;
        if (this._gridOptions.backendServiceApi && this._gridOptions.dataView.hasOwnProperty("syncGridSelectionWithBackendService")) {
          preservedRowSelectionWithBackend = this._gridOptions.dataView.syncGridSelectionWithBackendService;
        }
        const syncGridSelection = this._gridOptions.dataView.syncGridSelection;
        if (typeof syncGridSelection === "boolean") {
          let preservedRowSelection = syncGridSelection;
          if (!this._isLocalGrid) {
            preservedRowSelection = syncGridSelection && preservedRowSelectionWithBackend;
          }
          this.dataView?.syncGridSelection(this.slickGrid, preservedRowSelection);
        } else if (typeof syncGridSelection === "object") {
          this.dataView?.syncGridSelection(this.slickGrid, syncGridSelection.preserveHidden, syncGridSelection.preserveHiddenOnSelectionChange);
        }
      }
      if ((this.dataView?.getLength() ?? 0) > 0) {
        if (!this._isDatasetInitialized && (this._gridOptions.enableCheckboxSelector || this._gridOptions.enableSelection)) {
          this.loadRowSelectionPresetWhenExists();
        }
        this.loadFilterPresetsWhenDatasetInitialized();
        this._isDatasetInitialized = true;
      } else {
        this.displayEmptyDataWarning(true);
      }
      if (this._hideHeaderRowAfterPageLoad) {
        this.showHeaderRow(false);
        this.sharedService.hideHeaderRowAfterPageLoad = this._hideHeaderRowAfterPageLoad;
      }
      this.gridEventService.bindOnBeforeEditCell(this.slickGrid);
      this.gridEventService.bindOnCellChange(this.slickGrid);
      this.gridEventService.bindOnClick(this.slickGrid);
      if (this.gridOptions?.backendServiceApi) {
        this.bindBackendCallbackFunctions(this.gridOptions);
      }
      this._eventPubSubService.publish("onGridCreated", this.slickGrid);
      if (!this.customDataView) {
        this.executeAfterDataviewCreated(this.gridOptions);
      }
      this.bindResizeHook(this.slickGrid, this.gridOptions);
      if (!this._isPaginationInitialized && !this.datasetHierarchical && this.gridOptions?.enablePagination && this._isLocalGrid) {
        this.showPagination = true;
        this.loadLocalGridPagination(this.dataset);
      }
      this._slickerGridInstances = {
        // Slick Grid & DataView objects
        dataView: this.dataView,
        slickGrid: this.slickGrid,
        // public methods
        dispose: this.dispose.bind(this),
        // return all available Services (non-singleton)
        backendService: this.backendService,
        eventPubSubService: this._eventPubSubService,
        filterService: this.filterService,
        gridEventService: this.gridEventService,
        gridStateService: this.gridStateService,
        gridService: this.gridService,
        headerGroupingService: this.headerGroupingService,
        extensionService: this.extensionService,
        extensionUtility: this.extensionUtility,
        paginationService: this.paginationService,
        resizerService: this.resizerService,
        sortService: this.sortService,
        treeDataService: this.treeDataService
      };
      this._extensions = this.extensionService?.extensionList;
      this._eventPubSubService.publish("onSlickerGridCreated", this.instances);
      this._isGridInitialized = true;
      this.suggestDateParsingWhenHelpful();
      this.observeColumnDefinitions();
    }
    hasBackendInfiniteScroll() {
      return !!this.backendService?.options?.infiniteScroll;
    }
    mergeGridOptions(gridOptions) {
      const options = extend(true, {}, GlobalGridOptions, gridOptions);
      if (options.enableFiltering && !options.showHeaderRow) {
        options.showHeaderRow = options.enableFiltering;
      }
      if (options?.pagination && (gridOptions.enablePagination || gridOptions.backendServiceApi) && gridOptions.pagination && Array.isArray(gridOptions.pagination.pageSizes)) {
        options.pagination.pageSizes = gridOptions.pagination.pageSizes;
      }
      if (this.sharedService && !options.enableFiltering && options.enablePagination && this._isLocalGrid) {
        options.enableFiltering = true;
        options.showHeaderRow = false;
        this._hideHeaderRowAfterPageLoad = true;
        this.sharedService.hideHeaderRowAfterPageLoad = true;
      }
      return options;
    }
    /**
     * Define our internal Post Process callback, it will execute internally after we get back result from the Process backend call
     * Currently ONLY available with the GraphQL Backend Service.
     * The behavior is to refresh the Dataset & Pagination without requiring the user to create his own PostProcess every time
     */
    createBackendApiInternalPostProcessCallback(gridOptions) {
      const backendApi = gridOptions?.backendServiceApi;
      if (backendApi?.service) {
        const backendApiService = backendApi.service;
        if (
          /* backendApiService instanceof GraphqlService || */
          typeof backendApiService.getDatasetName === "function"
        ) {
          backendApi.internalPostProcess = (processResult) => {
            const datasetName = backendApi && backendApiService && typeof backendApiService.getDatasetName === "function" ? backendApiService.getDatasetName() : "";
            if (!Array.isArray(processResult) && processResult?.data?.[datasetName]) {
              const data = processResult.data[datasetName].hasOwnProperty("nodes") ? processResult.data[datasetName].nodes : processResult.data[datasetName];
              const totalCount = processResult.data[datasetName].hasOwnProperty("totalCount") ? processResult.data[datasetName].totalCount : processResult.data[datasetName].length;
              this.refreshGridData(data, totalCount || 0);
            }
          };
        }
      }
    }
    bindDifferentHooks(grid, gridOptions, dataView) {
      if (gridOptions.enableTranslate && this.translaterService?.addPubSubMessaging) {
        this.translaterService.addPubSubMessaging(this._eventPubSubService);
      }
      if (gridOptions.enableTranslate) {
        this.extensionService.translateAllExtensions();
      }
      this.subscriptions.push(this._eventPubSubService.subscribe("onLanguageChange", (args) => {
        if (gridOptions.enableTranslate) {
          this.extensionService.translateAllExtensions(args.language);
          if (gridOptions.createPreHeaderPanel && (gridOptions.createTopHeaderPanel || !gridOptions.enableDraggableGrouping)) {
            this.headerGroupingService.translateHeaderGrouping();
          }
        }
      }));
      if (gridOptions.backendServiceApi) {
        const backendApi = gridOptions.backendServiceApi;
        if (backendApi?.service?.init) {
          backendApi.service.init(backendApi.options, gridOptions.pagination, this.slickGrid, this.sharedService);
        }
      }
      if (dataView && grid) {
        if (gridOptions.enableFiltering) {
          this.filterService.init(grid);
          if (gridOptions.backendServiceApi && !gridOptions.backendServiceApi.useLocalFiltering) {
            this.filterService.bindBackendOnFilter(grid);
          } else {
            this.filterService.bindLocalOnFilter(grid);
          }
        }
        if (gridOptions.enableSorting) {
          if (gridOptions.backendServiceApi && !gridOptions.backendServiceApi.useLocalSorting) {
            this.sortService.bindBackendOnSort(grid);
          } else {
            this.sortService.bindLocalOnSort(grid);
          }
        }
        this._eventHandler.subscribe(dataView.onRowCountChanged, (_e, args) => {
          if (!gridOptions.enableRowDetailView || !Array.isArray(args.changedRows) || args.changedRows.length === args.itemCount) {
            grid.invalidate();
          } else {
            grid.invalidateRows(args.changedRows);
            grid.render();
          }
          this.handleOnItemCountChanged(this.dataView?.getFilteredItemCount() || 0, this.dataView?.getItemCount() ?? 0);
        });
        this._eventHandler.subscribe(dataView.onSetItemsCalled, (_e, args) => {
          this.sharedService.isItemsDateParsed = false;
          this.handleOnItemCountChanged(this.dataView?.getFilteredItemCount() || 0, args.itemCount);
          if (args.itemCount > 0 && (gridOptions.autosizeColumnsByCellContentOnFirstLoad || gridOptions.enableAutoResizeColumnsByCellContent)) {
            this.resizerService.resizeColumnsByCellContent(!gridOptions?.resizeByContentOnlyOnFirstLoad);
          }
        });
        if ((gridOptions?.enableFiltering || gridOptions?.dataView?.globalItemMetadataProvider) && !gridOptions.enableRowDetailView) {
          this._eventHandler.subscribe(dataView.onRowsChanged, (_e, { calledOnRowCountChanged, rows }) => {
            if (!calledOnRowCountChanged && Array.isArray(rows)) {
              const ranges = grid.getRenderedRange();
              rows.filter((row) => row >= ranges.top && row <= ranges.bottom).forEach((row) => grid.updateRow(row));
              grid.render();
            }
          });
        }
        this._eventHandler.subscribe(grid.onColumnsReordered, () => {
          this.sharedService.hasColumnsReordered = true;
        });
        this._eventHandler.subscribe(grid.onSetOptions, (_e, args) => {
          if (args.optionsBefore.darkMode !== args.optionsAfter.darkMode) {
            this.setDarkMode(args.optionsAfter.darkMode);
          }
        });
        this.loadColumnPresetsWhenDatasetInitialized();
        this.loadFilterPresetsWhenDatasetInitialized();
      }
    }
    bindBackendCallbackFunctions(gridOptions) {
      const backendApi = gridOptions.backendServiceApi;
      const backendApiService = backendApi?.service;
      const serviceOptions = backendApiService?.options ?? {};
      const isExecuteCommandOnInit = !serviceOptions ? false : serviceOptions?.hasOwnProperty("executeProcessCommandOnInit") ? serviceOptions["executeProcessCommandOnInit"] : true;
      if (backendApiService) {
        if (gridOptions?.presets) {
          if (backendApiService.updateFilters && Array.isArray(gridOptions.presets.filters) && gridOptions.presets.filters.length > 0) {
            backendApiService.updateFilters(gridOptions.presets.filters, true);
          }
          if (backendApiService.updateSorters && Array.isArray(gridOptions.presets.sorters) && gridOptions.presets.sorters.length > 0) {
            const sortColumns = this._gridOptions?.multiColumnSort ? gridOptions.presets.sorters : gridOptions.presets.sorters.slice(0, 1);
            backendApiService.updateSorters(void 0, sortColumns);
          }
          if (backendApiService.updatePagination && gridOptions.presets.pagination && !this.hasBackendInfiniteScroll()) {
            const { pageNumber, pageSize } = gridOptions.presets.pagination;
            backendApiService.updatePagination(pageNumber, pageSize);
          }
        } else {
          const columnFilters = this.filterService.getColumnFilters();
          if (columnFilters && backendApiService.updateFilters) {
            backendApiService.updateFilters(columnFilters, false);
          }
        }
        if (backendApi && backendApiService && (backendApi.onInit || isExecuteCommandOnInit)) {
          const query = typeof backendApiService.buildQuery === "function" ? backendApiService.buildQuery() : "";
          const process = isExecuteCommandOnInit ? backendApi.process?.(query) ?? null : backendApi.onInit?.(query) ?? null;
          queueMicrotaskPolyfill(() => {
            const backendUtilityService = this.backendUtilityService;
            const startTime = /* @__PURE__ */ new Date();
            backendApi.preProcess?.();
            const totalItems = this.gridOptions?.pagination?.totalItems ?? 0;
            if (process instanceof Promise) {
              process.then((processResult) => backendUtilityService.executeBackendProcessesCallback(startTime, processResult, backendApi, totalItems)).catch((error) => backendUtilityService.onBackendError(error, backendApi));
            } else if (process && this.rxjs?.isObservable(process)) {
              this.subscriptions.push(process.subscribe((processResult) => backendUtilityService.executeBackendProcessesCallback(startTime, processResult, backendApi, totalItems), (error) => backendUtilityService.onBackendError(error, backendApi)));
            }
          });
        }
        if (backendApi.service.options?.infiniteScroll) {
          this.addBackendInfiniteScrollCallback();
        }
      }
    }
    addBackendInfiniteScrollCallback() {
      if (this.slickGrid && this.gridOptions.backendServiceApi && this.hasBackendInfiniteScroll() && !this.gridOptions.backendServiceApi?.onScrollEnd) {
        const onScrollEnd = () => {
          this.backendUtilityService.setInfiniteScrollBottomHit(true);
          this.paginationService.goToNextPage().then((hasNext) => {
            if (!hasNext) {
              this.backendUtilityService.setInfiniteScrollBottomHit(false);
            }
          });
        };
        this.gridOptions.backendServiceApi.onScrollEnd = onScrollEnd;
        this._eventHandler.subscribe(this.slickGrid.onScroll, (_e, args) => {
          const viewportElm = args.grid.getViewportNode();
          if (["mousewheel", "scroll"].includes(args.triggeredBy || "") && this.paginationService?.totalItems && args.scrollTop > 0 && Math.ceil(viewportElm.offsetHeight + args.scrollTop) >= args.scrollHeight) {
            if (!this._scrollEndCalled) {
              onScrollEnd();
              this._scrollEndCalled = true;
            }
          }
        });
        const orgPostProcess = this.gridOptions.backendServiceApi.postProcess;
        this.gridOptions.backendServiceApi.postProcess = (processResult) => {
          this._scrollEndCalled = false;
          if (orgPostProcess) {
            orgPostProcess(processResult);
          }
        };
      }
    }
    bindResizeHook(grid, options) {
      if (options.autoFitColumnsOnFirstLoad && options.autosizeColumnsByCellContentOnFirstLoad || options.enableAutoSizeColumns && options.enableAutoResizeColumnsByCellContent) {
        throw new Error(`[Slickgrid-Universal] You cannot enable both autosize/fit viewport & resize by content, you must choose which resize technique to use. You can enable these 2 options ("autoFitColumnsOnFirstLoad" and "enableAutoSizeColumns") OR these other 2 options ("autosizeColumnsByCellContentOnFirstLoad" and "enableAutoResizeColumnsByCellContent").`);
      }
      if (options.gridHeight || options.gridWidth) {
        this.resizerService.resizeGrid(0, { height: options.gridHeight, width: options.gridWidth });
      } else {
        this.resizerService.resizeGrid();
      }
      if (grid && options?.enableAutoResize && options.autoFitColumnsOnFirstLoad && options.enableAutoSizeColumns && !this._isAutosizeColsCalled) {
        grid.autosizeColumns();
        this._isAutosizeColsCalled = true;
      }
    }
    executeAfterDataviewCreated(gridOptions) {
      if (gridOptions.enableSorting) {
        if (gridOptions.presets && Array.isArray(gridOptions.presets.sorters)) {
          const sortColumns = this._gridOptions?.multiColumnSort ? gridOptions.presets.sorters : gridOptions.presets.sorters.slice(0, 1);
          this.sortService.loadGridSorters(sortColumns);
        }
      }
    }
    /**
     * On a Pagination changed, we will trigger a Grid State changed with the new pagination info
     * Also if we use Row Selection or the Checkbox Selector with a Backend Service (Odata, GraphQL), we need to reset any selection
     */
    paginationChanged(pagination) {
      const isSyncGridSelectionEnabled = this.gridStateService?.needToPreserveRowSelection() ?? false;
      if (this.slickGrid && !isSyncGridSelectionEnabled && this._gridOptions?.backendServiceApi && (this.gridOptions.enableSelection || this.gridOptions.enableCheckboxSelector)) {
        this.slickGrid.setSelectedRows([]);
      }
      const { pageNumber, pageSize } = pagination;
      if (this.sharedService && pageSize !== void 0 && pageNumber !== void 0) {
        this.sharedService.currentPagination = { pageNumber, pageSize };
      }
      this._eventPubSubService.publish("onGridStateChanged", {
        change: { newValues: { pageNumber, pageSize }, type: "pagination" },
        gridState: this.gridStateService.getCurrentGridState()
      });
    }
    /**
     * When dataset changes, we need to refresh the entire grid UI & possibly resize it as well
     * @param dataset
     */
    refreshGridData(dataset, totalCount) {
      if (this.slickGrid && this._gridOptions) {
        if (this._gridOptions.enablePagination && this._isLocalGrid) {
          this.showPagination = true;
          this.loadLocalGridPagination(dataset);
        }
        if (this._gridOptions.enableEmptyDataWarningMessage && Array.isArray(dataset)) {
          const finalTotalCount = totalCount || dataset.length;
          this.displayEmptyDataWarning(finalTotalCount < 1);
        }
        if (Array.isArray(dataset) && this.slickGrid && this.dataView?.setItems) {
          this.dataView.setItems(dataset, this._gridOptions.datasetIdPropertyName);
          if (!this._gridOptions.backendServiceApi && !this._gridOptions.enableTreeData) {
            this.dataView.reSort();
          }
          if (dataset.length > 0) {
            if (!this._isDatasetInitialized) {
              this.loadFilterPresetsWhenDatasetInitialized();
              if (this._gridOptions.enableCheckboxSelector) {
                this.loadRowSelectionPresetWhenExists();
              }
            }
            this._isDatasetInitialized = true;
          }
          if (dataset) {
            this.slickGrid.invalidate();
          }
          this.showPagination = !!(this._gridOptions && (this._gridOptions.enablePagination || this._gridOptions.backendServiceApi && this._gridOptions.enablePagination === void 0));
          if (this._paginationOptions && this._gridOptions?.pagination && this._gridOptions?.backendServiceApi) {
            const paginationOptions = this.setPaginationOptionsWhenPresetDefined(this._gridOptions, this._paginationOptions);
            const totalRecords = totalCount !== void 0 ? totalCount : this._gridOptions?.pagination?.totalItems;
            if (totalRecords !== void 0 && totalRecords !== this.totalItems) {
              this.totalItems = +totalRecords;
            }
            if (!this._isPaginationInitialized) {
              this.initializePaginationService(paginationOptions);
            } else {
              this.paginationService.updateTotalItems(this.totalItems);
            }
          }
          if (this.slickGrid && this._gridOptions.enableAutoResize) {
            const delay = this._gridOptions.autoResize && this._gridOptions.autoResize.delay;
            this.resizerService.resizeGrid(delay || 10);
          }
        }
      }
    }
    /**
     * Dynamically change or update the column definitions list.
     * We will re-render the grid so that the new header and data shows up correctly.
     * If using translater, we also need to trigger a re-translate of the column headers
     */
    updateColumnDefinitionsList(newColumns) {
      if (this.slickGrid && this._gridOptions && Array.isArray(newColumns)) {
        newColumns = this.loadSlickGridEditors(newColumns);
        if (this._gridOptions.autoAddCustomEditorFormatter) {
          autoAddEditorFormatterToColumnsWithEditor(newColumns, this._gridOptions.autoAddCustomEditorFormatter);
        }
        if (this._gridOptions.enableTranslate) {
          this.extensionService.translateColumnHeaders(void 0, newColumns, false);
        }
        this.extensionService.renderColumnHeaders(newColumns, true);
        if (this.slickGrid && this._gridOptions?.enableAutoSizeColumns) {
          this.slickGrid.autosizeColumns();
        } else if (this._gridOptions?.enableAutoResizeColumnsByCellContent && this.resizerService?.resizeColumnsByCellContent) {
          this.resizerService.resizeColumnsByCellContent();
        }
      }
    }
    /**
     * Show the filter row displayed on first row, we can optionally pass false to hide it.
     * @param showing
     */
    showHeaderRow(showing = true) {
      this.slickGrid?.setHeaderRowVisibility(showing);
      if (this.slickGrid && showing === true && this._isGridInitialized) {
        this.slickGrid.setColumns(this.columnDefinitions);
      }
      return showing;
    }
    setData(data, shouldAutosizeColumns = false) {
      if (shouldAutosizeColumns) {
        this._isAutosizeColsCalled = false;
        this._currentDatasetLength = 0;
      }
      this.dataset = data || [];
    }
    /**
     * Check if there's any Pagination Presets defined in the Grid Options,
     * if there are then load them in the paginationOptions object
     */
    setPaginationOptionsWhenPresetDefined(gridOptions, paginationOptions) {
      if (gridOptions.presets?.pagination && paginationOptions && !this._isPaginationInitialized) {
        if (this.hasBackendInfiniteScroll()) {
          console.warn("[Slickgrid-Universal] `presets.pagination` is not supported with Infinite Scroll, reverting to first page.");
        } else {
          paginationOptions.pageSize = gridOptions.presets.pagination.pageSize;
          paginationOptions.pageNumber = gridOptions.presets.pagination.pageNumber;
        }
      }
      return paginationOptions;
    }
    setDarkMode(dark = false) {
      this._gridParentContainerElm.classList.toggle("slick-dark-mode", dark);
    }
    // --
    // protected functions
    // ------------------
    /**
     * Loop through all column definitions and copy the original optional `width` properties optionally provided by the user.
     * We will use this when doing a resize by cell content, if user provided a `width` it won't override it.
     */
    copyColumnWidthsReference(columns) {
      columns.forEach((col) => col.originalWidth = col.width);
    }
    displayEmptyDataWarning(showWarning = true) {
      if (this.gridOptions.enableEmptyDataWarningMessage) {
        this.slickEmptyWarning?.showEmptyDataMessage(showWarning);
      }
    }
    /** When data changes in the DataView, we'll refresh the metrics and/or display a warning if the dataset is empty */
    handleOnItemCountChanged(currentPageRowItemCount, totalItemCount) {
      this._currentDatasetLength = totalItemCount;
      this.metrics = {
        startTime: /* @__PURE__ */ new Date(),
        endTime: /* @__PURE__ */ new Date(),
        itemCount: currentPageRowItemCount,
        totalItemCount
      };
      if (this.slickFooter) {
        this.slickFooter.metrics = this.metrics;
      }
      if (this._isLocalGrid && this._gridOptions?.enableEmptyDataWarningMessage) {
        this.displayEmptyDataWarning(currentPageRowItemCount === 0);
      }
      if (this._gridOptions.enableAutoResize && this.resizerService.isAutoHeightEnabled && currentPageRowItemCount > 0) {
        this.resizerService.resizeGrid();
      }
    }
    /** Initialize the Pagination Service once */
    initializePaginationService(paginationOptions) {
      if (this.slickGrid && this.gridOptions) {
        this.paginationData = {
          gridOptions: this.gridOptions,
          paginationService: this.paginationService
        };
        this.paginationService.totalItems = this.totalItems;
        this.paginationService.init(this.slickGrid, paginationOptions, this.backendServiceApi);
        this.subscriptions.push(this._eventPubSubService.subscribe("onPaginationChanged", (paginationChanges) => this.paginationChanged(paginationChanges)), this._eventPubSubService.subscribe("onPaginationVisibilityChanged", (visibility) => {
          this.showPagination = visibility?.visible ?? false;
          if (this.gridOptions?.backendServiceApi) {
            this.backendUtilityService?.refreshBackendDataset(this.gridOptions);
          }
          this.renderPagination(this.showPagination);
        }));
        this.renderPagination();
        this._isPaginationInitialized = true;
      }
    }
    /** handler for when column definitions changes */
    columnDefinitionsChanged(columns) {
      if (columns) {
        this._columns = columns;
      }
      if (this._isGridInitialized) {
        this.updateColumnDefinitionsList(this.columnDefinitions);
      }
      if (this.columnDefinitions.length > 0) {
        this.copyColumnWidthsReference(this.columnDefinitions);
      }
    }
    /**
     * assignment changes are not triggering on the column definitions, for that
     * we can use our internal array observer for any changes done via (push, pop, shift, ...)
     */
    observeColumnDefinitions() {
      this._collectionObservers.push(collectionObserver(this.columnDefinitions, this.columnDefinitionsChanged.bind(this)));
    }
    /**
     * Render (or dispose) the Pagination Component, user can optionally provide False (to not show it) which will in term dispose of the Pagination,
     * also while disposing we can choose to omit the disposable of the Pagination Service (if we are simply toggling the Pagination, we want to keep the Service alive)
     * @param {Boolean} showPagination - show (new render) or not (dispose) the Pagination
     * @param {Boolean} shouldDisposePaginationService - when disposing the Pagination, do we also want to dispose of the Pagination Service? (defaults to True)
     */
    renderPagination(showPagination = true) {
      if (this.slickGrid && this._gridOptions?.enablePagination && !this._isPaginationInitialized && showPagination) {
        const PaginationClass = this.gridOptions.customPaginationComponent ?? SlickPaginationComponent;
        this.paginationComponent = new PaginationClass();
        this.paginationComponent.init(this.slickGrid, this.paginationService, this._eventPubSubService, this.translaterService);
        this.paginationComponent.renderPagination(this._gridParentContainerElm);
        this._isPaginationInitialized = true;
      } else if (!showPagination) {
        this.paginationComponent?.dispose();
        this._isPaginationInitialized = false;
      }
    }
    /** Load the Editor Collection asynchronously and replace the "collection" property when Promise resolves */
    loadEditorCollectionAsync(column) {
      if (column?.editor) {
        column.editor.disabled = true;
        fetchAsPromise(column.editor.collectionAsync, this.rxjs).then((resolvedCollection) => {
          this.updateEditorCollection(column, resolvedCollection);
        });
      }
    }
    /** Load any possible Columns Grid Presets */
    loadColumnPresetsWhenDatasetInitialized() {
      if (this.slickGrid && this.gridOptions.presets && Array.isArray(this.gridOptions.presets.columns) && this.gridOptions.presets.columns.length > 0) {
        this.gridStateService.changeColumnsArrangement(this.gridOptions.presets.columns, false);
      }
    }
    /** Load any possible Filters Grid Presets */
    loadFilterPresetsWhenDatasetInitialized() {
      if (this.gridOptions && !this.customDataView) {
        if (this.gridOptions.presets && (Array.isArray(this.gridOptions.presets.filters) || Array.isArray(this.gridOptions.presets?.treeData?.toggledItems))) {
          this.filterService.populateColumnFilterSearchTermPresets(this.gridOptions.presets?.filters || []);
        }
      }
    }
    /**
     * local grid, check if we need to show the Pagination
     * if so then also check if there's any presets and finally initialize the PaginationService
     * a local grid with Pagination presets will potentially have a different total of items, we'll need to get it from the DataView and update our total
     */
    loadLocalGridPagination(dataset) {
      if (this.gridOptions && this._paginationOptions) {
        this.totalItems = Array.isArray(dataset) ? dataset.length : 0;
        if (this._paginationOptions && this.dataView?.getPagingInfo) {
          const slickPagingInfo = this.dataView.getPagingInfo();
          if (slickPagingInfo?.hasOwnProperty("totalRows") && this._paginationOptions.totalItems !== slickPagingInfo.totalRows) {
            this.totalItems = slickPagingInfo?.totalRows || 0;
          }
        }
        this._paginationOptions.totalItems = this.totalItems;
        const paginationOptions = this.setPaginationOptionsWhenPresetDefined(this.gridOptions, this._paginationOptions);
        this.initializePaginationService(paginationOptions);
      }
    }
    /** Load any Row Selections into the DataView that were presets by the user */
    loadRowSelectionPresetWhenExists() {
      const presets = this.gridOptions?.presets;
      const selectionModel = this.slickGrid?.getSelectionModel();
      const enableRowSelection = this.gridOptions && (this.gridOptions.enableCheckboxSelector || this.gridOptions.enableSelection);
      if (this.slickGrid && this.dataView && enableRowSelection && selectionModel && presets?.rowSelection && (Array.isArray(presets.rowSelection.gridRowIndexes) || Array.isArray(presets.rowSelection.dataContextIds))) {
        let dataContextIds = presets.rowSelection.dataContextIds;
        let gridRowIndexes = presets.rowSelection.gridRowIndexes;
        if (Array.isArray(dataContextIds) && dataContextIds.length > 0) {
          gridRowIndexes = this.dataView.mapIdsToRows(dataContextIds) || [];
        } else if (Array.isArray(gridRowIndexes) && gridRowIndexes.length > 0) {
          dataContextIds = this.dataView.mapRowsToIds(gridRowIndexes) || [];
        }
        if (this.slickGrid && Array.isArray(gridRowIndexes)) {
          this.slickGrid.setSelectedRows(gridRowIndexes);
          this.dataView.setSelectedIds(dataContextIds || [], {
            isRowBeingAdded: true,
            shouldTriggerEvent: false,
            // do not trigger when presetting the grid
            applyRowSelectionToGrid: true
          });
        }
      }
    }
    /** Add a register a new external resource, user could also optional dispose all previous resources before pushing any new resources to the resources array list. */
    registerExternalResources(resources, disposePreviousResources = false) {
      if (disposePreviousResources) {
        this.disposeExternalResources();
      }
      resources.forEach((res) => this._registeredResources.push(res));
      this.initializeExternalResources(resources);
    }
    resetExternalResources() {
      this._registeredResources = [];
    }
    /** Pre-Register any Resource that don't require SlickGrid to be instantiated (for example RxJS Resource) */
    preRegisterResources() {
      if (Array.isArray(this._registeredResources)) {
        for (const resource of this._registeredResources) {
          if (resource?.pluginName === "RxJsResource") {
            this.registerRxJsResource(resource);
          }
        }
      }
    }
    /** initialized & auto-enable external registered resources, e.g. if user registers `ExcelExportService` then let's auto-enable `enableExcelExport:true` */
    autoEnableInitializedResources(resource) {
      if (this.slickGrid && typeof resource.init === "function") {
        resource.init(this.slickGrid, this.universalContainerService);
      }
      if ("pluginName" in resource) {
        const pluginFlagName = PluginFlagMappings.get(resource.pluginName);
        if (pluginFlagName && this._gridOptions[pluginFlagName] !== false) {
          this._gridOptions[pluginFlagName] = true;
          this.slickGrid?.setOptions({ [pluginFlagName]: true });
        }
      }
    }
    initializeExternalResources(resources) {
      if (Array.isArray(resources)) {
        for (const resource of resources) {
          this.autoEnableInitializedResources(resource);
        }
      }
    }
    registerResources() {
      if (Array.isArray(this._registeredResources)) {
        this.sharedService.externalRegisteredResources = this._registeredResources;
      }
      this._registeredResources.push(this.gridService, this.gridStateService);
      if (this.gridOptions.createPreHeaderPanel && (this.gridOptions.createTopHeaderPanel || !this.gridOptions.enableDraggableGrouping)) {
        this._registeredResources.push(this.headerGroupingService);
      }
      if (this.gridOptions.enableTreeData) {
        this._registeredResources.push(this.treeDataService);
      }
      if (this.gridOptions.enableTranslate) {
        this.extensionService.translateColumnHeaders();
      }
      this.slickEmptyWarning = new SlickEmptyWarningComponent();
      this._registeredResources.push(this.slickEmptyWarning);
      this.initializeExternalResources(this._registeredResources);
    }
    /** Register the RxJS Resource in all necessary services which uses */
    registerRxJsResource(resource) {
      this.rxjs = resource;
      this.backendUtilityService.addRxJsResource(this.rxjs);
      this.filterFactory.addRxJsResource(this.rxjs);
      this.filterService.addRxJsResource(this.rxjs);
      this.sortService.addRxJsResource(this.rxjs);
      this.paginationService.addRxJsResource(this.rxjs);
      this.universalContainerService.registerInstance("RxJsFacade", this.rxjs);
      this.universalContainerService.registerInstance("RxJsResource", this.rxjs);
    }
    /**
     * Takes a flat dataset with parent/child relationship, sort it (via its tree structure) and return the sorted flat array
     * @returns {Array<Object>} sort flat parent/child dataset
     */
    sortTreeDataset(flatDatasetInput, forceGridRefresh = false) {
      const prevDatasetLn = this._currentDatasetLength;
      let sortedDatasetResult;
      let flatDatasetOutput = [];
      if (this._isDatasetHierarchicalInitialized && this.datasetHierarchical) {
        sortedDatasetResult = this.treeDataService.sortHierarchicalDataset(this.datasetHierarchical);
        flatDatasetOutput = sortedDatasetResult.flat;
      } else if (Array.isArray(flatDatasetInput) && flatDatasetInput.length > 0) {
        sortedDatasetResult = this.treeDataService.convertFlatParentChildToTreeDatasetAndSort(flatDatasetInput, this._columns || [], this.gridOptions);
        this.sharedService.hierarchicalDataset = sortedDatasetResult.hierarchical;
        flatDatasetOutput = sortedDatasetResult.flat;
      }
      if (flatDatasetInput.length > 0 && (forceGridRefresh || flatDatasetInput.length !== prevDatasetLn)) {
        this.filterService.refreshTreeDataFilters(flatDatasetOutput);
      }
      return flatDatasetOutput;
    }
    /** Prepare and load all SlickGrid editors, if an async editor is found then we'll also execute it. */
    loadSlickGridEditors(columnDefinitions) {
      const columns = Array.isArray(columnDefinitions) ? columnDefinitions : [];
      if (columns.some((col) => `${col.id}`.includes("."))) {
        console.warn('[Slickgrid-Universal] Make sure that none of your Column Definition "id" property includes a dot in its name because that will cause some problems with the Editors. For example if your column definition "field" property is "user.firstName" then use "firstName" as the column "id".');
      }
      return columns.map((column) => {
        if (column.editor?.collectionAsync) {
          this.loadEditorCollectionAsync(column);
        }
        return { ...column, editorClass: column.editor?.model };
      });
    }
    suggestDateParsingWhenHelpful() {
      if (!this.gridOptions.silenceWarnings && this.dataView && this.dataView.getItemCount() > WARN_NO_PREPARSE_DATE_SIZE && !this.gridOptions.preParseDateColumns && this.slickGrid?.getColumns().some((c) => isColumnDateType(c.type))) {
        console.warn("[Slickgrid-Universal] For getting better perf, we suggest you enable the `preParseDateColumns` grid option, for more info visit => https://ghiscoding.gitbook.io/slickgrid-universal/column-functionalities/sorting#pre-parse-date-columns-for-better-perf");
      }
    }
    /**
     * When the Editor(s) has a "editor.collection" property, we'll load the async collection.
     * Since this is called after the async call resolves, the pointer will not be the same as the "column" argument passed.
     */
    updateEditorCollection(column, newCollection) {
      if (this.slickGrid && column.editor) {
        column.editor.collection = newCollection;
        column.editor.disabled = false;
        const currentEditor = this.slickGrid.getCellEditor();
        if (currentEditor?.disable && currentEditor.renderDomElement) {
          if (typeof currentEditor.destroy === "function") {
            currentEditor.destroy();
          }
          currentEditor.disable(false);
          currentEditor.renderDomElement(newCollection);
        }
      }
    }
  };

  // node_modules/@slickgrid-universal/vanilla-bundle/dist/index.js
  var Slicker = {
    GridBundle: SlickVanillaGridBundle,
    Aggregators,
    BindingService,
    Editors,
    Enums: enums_exports,
    Filters,
    Formatters,
    GroupTotalFormatters,
    SortComparers,
    Utilities
  };
  if (typeof window !== "undefined") {
    window.Slicker = Slicker;
  }
  return __toCommonJS(index_exports);
})();
/*! Bundled license information:

vanilla-calendar-pro/index.mjs:
  (*! name: vanilla-calendar-pro v3.1.0 | url: https://github.com/uvarov-frontend/vanilla-calendar-pro *)

sortablejs/modular/sortable.core.esm.js:
  (**!
   * Sortable 1.15.7
   * @author	RubaXa   <trash@rubaxa.org>
   * @author	owenm    <owen23355@gmail.com>
   * @license MIT
   *)
*/
